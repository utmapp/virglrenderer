/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgisurface.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGISURFACE_H
#define NPT_PROTOCOL_HOST_IDXGISURFACE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGISurface::GetDesc
 */

struct npt_command_IDXGISurface_GetDesc {
    void *_self;
    DXGI_SURFACE_DESC * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISurface_GetDesc_args_temp(struct npt_cs_decoder *dec,
                                          struct npt_command_IDXGISurface_GetDesc *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SURFACE_DESC));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_SURFACE_DESC));
}

static inline void
npt_replace_IDXGISurface_GetDesc_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISurface_GetDesc *args)
{
}

static inline void
npt_encode_IDXGISurface_GetDesc_reply(struct npt_cs_encoder *enc,
                                      const struct npt_command_IDXGISurface_GetDesc *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                 NPT_METHOD_IDXGISurface_GetDesc),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_SURFACE_DESC(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGISurface_GetDesc(struct npt_dispatch_context *ctx,
                                  uint32_t cmd_flags,
                                  uint64_t object_id)
{
    struct npt_command_IDXGISurface_GetDesc args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISurface_GetDesc_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISURFACE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface_GetDesc on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISurface_GetDesc_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface_GetDesc: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISurface_GetDesc _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISurface_GetDesc, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISurface_GetDesc);

    if (ctx->idxgisurface_dispatch_overrides && ctx->idxgisurface_dispatch_overrides->GetDesc) {
        args.ret = ctx->idxgisurface_dispatch_overrides->GetDesc(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISurface_GetDesc_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISurface::Map
 */

struct npt_command_IDXGISurface_Map {
    void *_self;
    DXGI_MAPPED_RECT * pLockedRect;
    UINT MapFlags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISurface_Map_args_temp(struct npt_cs_decoder *dec,
                                      struct npt_command_IDXGISurface_Map *args)
{
    /* Decode input parameters from the wire */

    npt_decode_UINT(dec, &args->MapFlags);
    /* Allocate temp storage for output-only parameters */
    args->pLockedRect = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MAPPED_RECT));
    if (!args->pLockedRect) return;
    memset(args->pLockedRect, 0, sizeof(DXGI_MAPPED_RECT));

}

static inline void
npt_replace_IDXGISurface_Map_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISurface_Map *args)
{
}

static inline void
npt_encode_IDXGISurface_Map_reply(struct npt_cs_encoder *enc,
                                  const struct npt_command_IDXGISurface_Map *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                 NPT_METHOD_IDXGISurface_Map),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pLockedRect))
        npt_encode_DXGI_MAPPED_RECT(enc, args->pLockedRect);
}

static inline void
npt_dispatch_IDXGISurface_Map(struct npt_dispatch_context *ctx,
                              uint32_t cmd_flags,
                              uint64_t object_id)
{
    struct npt_command_IDXGISurface_Map args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISurface_Map_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISURFACE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface_Map on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISurface_Map_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface_Map: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISurface_Map _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISurface_Map, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISurface_Map);

    if (ctx->idxgisurface_dispatch_overrides && ctx->idxgisurface_dispatch_overrides->Map) {
        args.ret = ctx->idxgisurface_dispatch_overrides->Map(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pLockedRect, args.MapFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISurface_Map_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISurface::Unmap
 */

struct npt_command_IDXGISurface_Unmap {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISurface_Unmap_args_temp(struct npt_cs_decoder *dec,
                                        struct npt_command_IDXGISurface_Unmap *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGISurface_Unmap_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISurface_Unmap *args)
{
}

static inline void
npt_encode_IDXGISurface_Unmap_reply(struct npt_cs_encoder *enc,
                                    const struct npt_command_IDXGISurface_Unmap *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface,
                                 NPT_METHOD_IDXGISurface_Unmap),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISurface_Unmap(struct npt_dispatch_context *ctx,
                                uint32_t cmd_flags,
                                uint64_t object_id)
{
    struct npt_command_IDXGISurface_Unmap args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISurface_Unmap_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISURFACE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface_Unmap on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISurface_Unmap_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface_Unmap: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISurface_Unmap _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISurface_Unmap, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISurface_Unmap);

    if (ctx->idxgisurface_dispatch_overrides && ctx->idxgisurface_dispatch_overrides->Unmap) {
        args.ret = ctx->idxgisurface_dispatch_overrides->Unmap(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISurface_Unmap_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISurface1::GetDC
 */

struct npt_command_IDXGISurface1_GetDC {
    void *_self;
    BOOL Discard;
    HDC * phdc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISurface1_GetDC_args_temp(struct npt_cs_decoder *dec,
                                         struct npt_command_IDXGISurface1_GetDC *args)
{
    /* Decode input parameters from the wire */
    npt_decode_BOOL(dec, &args->Discard);

    /* Allocate temp storage for output-only parameters */

    args->phdc = npt_cs_decoder_alloc_temp(dec, sizeof(HDC));
    if (!args->phdc) return;
    memset(args->phdc, 0, sizeof(HDC));
}

static inline void
npt_replace_IDXGISurface1_GetDC_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISurface1_GetDC *args)
{
}

static inline void
npt_encode_IDXGISurface1_GetDC_reply(struct npt_cs_encoder *enc,
                                     const struct npt_command_IDXGISurface1_GetDC *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                 NPT_METHOD_IDXGISurface1_GetDC),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    /* ERROR: phdc (HDC) is not serializable */
    return;
}

static inline void
npt_dispatch_IDXGISurface1_GetDC(struct npt_dispatch_context *ctx,
                                 uint32_t cmd_flags,
                                 uint64_t object_id)
{
    struct npt_command_IDXGISurface1_GetDC args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISurface1_GetDC_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISURFACE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface1_GetDC on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISurface1_GetDC_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface1_GetDC: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISurface1_GetDC _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISurface1_GetDC, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISurface1_GetDC);

    if (ctx->idxgisurface1_dispatch_overrides && ctx->idxgisurface1_dispatch_overrides->GetDC) {
        args.ret = ctx->idxgisurface1_dispatch_overrides->GetDC(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Discard, args.phdc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISurface1_GetDC_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISurface1::ReleaseDC
 */

struct npt_command_IDXGISurface1_ReleaseDC {
    void *_self;
    RECT * pDirtyRect;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISurface1_ReleaseDC_args_temp(struct npt_cs_decoder *dec,
                                             struct npt_command_IDXGISurface1_ReleaseDC *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDirtyRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
        if (!args->pDirtyRect) return;
        npt_decode_RECT(dec, (RECT *)args->pDirtyRect);
    } else {
        args->pDirtyRect = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISurface1_ReleaseDC_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISurface1_ReleaseDC *args)
{
}

static inline void
npt_encode_IDXGISurface1_ReleaseDC_reply(struct npt_cs_encoder *enc,
                                         const struct npt_command_IDXGISurface1_ReleaseDC *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface1,
                                 NPT_METHOD_IDXGISurface1_ReleaseDC),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISurface1_ReleaseDC(struct npt_dispatch_context *ctx,
                                     uint32_t cmd_flags,
                                     uint64_t object_id)
{
    struct npt_command_IDXGISurface1_ReleaseDC args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISurface1_ReleaseDC_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISURFACE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface1_ReleaseDC on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISurface1_ReleaseDC_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface1_ReleaseDC: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISurface1_ReleaseDC _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISurface1_ReleaseDC, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISurface1_ReleaseDC);

    if (ctx->idxgisurface1_dispatch_overrides && ctx->idxgisurface1_dispatch_overrides->ReleaseDC) {
        args.ret = ctx->idxgisurface1_dispatch_overrides->ReleaseDC(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDirtyRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISurface1_ReleaseDC_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISurface2::GetResource
 */

struct npt_command_IDXGISurface2_GetResource {
    void *_self;
    const IID * riid;
    void ** ppParentResource;
    UINT * pSubresourceIndex;
    /* Shadow: the guest-allocated id for ppParentResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppParentResource -> *ppParentResource} in the
     * context object table. */
    uint64_t _guest_id_ppParentResource;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISurface2_GetResource_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_IDXGISurface2_GetResource *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppParentResource = _gid;
        args->ppParentResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppParentResource) return;
        *args->ppParentResource = NULL;
    }

    /* Allocate temp storage for output-only parameters */


    args->pSubresourceIndex = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pSubresourceIndex) return;
    memset(args->pSubresourceIndex, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGISurface2_GetResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISurface2_GetResource *args)
{
}

static inline void
npt_encode_IDXGISurface2_GetResource_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_IDXGISurface2_GetResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISurface2,
                                 NPT_METHOD_IDXGISurface2_GetResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppParentResource (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pSubresourceIndex))
        npt_encode_UINT(enc, args->pSubresourceIndex);
}

static inline void
npt_dispatch_IDXGISurface2_GetResource(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_IDXGISurface2_GetResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISurface2_GetResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISURFACE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface2_GetResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISurface2_GetResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISurface2_GetResource: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISurface2_GetResource _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISurface2_GetResource, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISurface2_GetResource);

    if (ctx->idxgisurface2_dispatch_overrides && ctx->idxgisurface2_dispatch_overrides->GetResource) {
        args.ret = ctx->idxgisurface2_dispatch_overrides->GetResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.riid, args.ppParentResource, args.pSubresourceIndex);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppParentResource && *args.ppParentResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppParentResource, *args.ppParentResource,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISurface2_GetResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGISURFACE_H */

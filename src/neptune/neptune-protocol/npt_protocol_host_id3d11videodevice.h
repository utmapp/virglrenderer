/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11videodevice.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11VIDEODEVICE_H
#define NPT_PROTOCOL_HOST_ID3D11VIDEODEVICE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11VideoDevice::CreateVideoDecoder
 */

struct npt_command_ID3D11VideoDevice_CreateVideoDecoder {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pVideoDesc;
    const D3D11_VIDEO_DECODER_CONFIG * pConfig;
    ID3D11VideoDecoder ** ppDecoder;
    /* Shadow: the guest-allocated id for ppDecoder arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDecoder -> *ppDecoder} in the
     * context object table. */
    uint64_t _guest_id_ppDecoder;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoDecoder_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11VideoDevice_CreateVideoDecoder *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pVideoDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_DESC));
        if (!args->pVideoDesc) return;
        npt_decode_D3D11_VIDEO_DECODER_DESC(dec, (D3D11_VIDEO_DECODER_DESC *)args->pVideoDesc);
    } else {
        args->pVideoDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pConfig = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_CONFIG));
        if (!args->pConfig) return;
        npt_decode_D3D11_VIDEO_DECODER_CONFIG(dec, (D3D11_VIDEO_DECODER_CONFIG *)args->pConfig);
    } else {
        args->pConfig = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDecoder = _gid;
        args->ppDecoder = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDecoder) return;
        *args->ppDecoder = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoDevice_CreateVideoDecoder_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateVideoDecoder *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoDecoder_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11VideoDevice_CreateVideoDecoder *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoder),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDecoder (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateVideoDecoder(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateVideoDecoder args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateVideoDecoder_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoDecoder on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDecoder);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateVideoDecoder_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoDecoder: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDecoder);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateVideoDecoder _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateVideoDecoder, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateVideoDecoder);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateVideoDecoder) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateVideoDecoder(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pVideoDesc, args.pConfig, args.ppDecoder);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDecoder && *args.ppDecoder)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDecoder, *args.ppDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
    else if (args.ppDecoder)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDecoder);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateVideoDecoder_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CreateVideoProcessor
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessor {
    void *_self;
    ID3D11VideoProcessorEnumerator * pEnum;
    UINT RateConversionIndex;
    ID3D11VideoProcessor ** ppVideoProcessor;
    /* Shadow: the guest-allocated id for ppVideoProcessor arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVideoProcessor -> *ppVideoProcessor} in the
     * context object table. */
    uint64_t _guest_id_ppVideoProcessor;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessor_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11VideoDevice_CreateVideoProcessor *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pEnum = (ID3D11VideoProcessorEnumerator *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->RateConversionIndex);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppVideoProcessor = _gid;
        args->ppVideoProcessor = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppVideoProcessor) return;
        *args->ppVideoProcessor = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoDevice_CreateVideoProcessor_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateVideoProcessor *args)
{
    args->pEnum = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pEnum, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessor_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11VideoDevice_CreateVideoProcessor *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessor),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppVideoProcessor (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateVideoProcessor(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateVideoProcessor args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateVideoProcessor_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessor on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVideoProcessor);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateVideoProcessor_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessor: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVideoProcessor);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateVideoProcessor _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateVideoProcessor, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateVideoProcessor);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessor) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessor(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pEnum, args.RateConversionIndex, args.ppVideoProcessor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppVideoProcessor && *args.ppVideoProcessor)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppVideoProcessor, *args.ppVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
    else if (args.ppVideoProcessor)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVideoProcessor);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateVideoProcessor_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CreateAuthenticatedChannel
 */

struct npt_command_ID3D11VideoDevice_CreateAuthenticatedChannel {
    void *_self;
    D3D11_AUTHENTICATED_CHANNEL_TYPE ChannelType;
    ID3D11AuthenticatedChannel ** ppAuthenticatedChannel;
    /* Shadow: the guest-allocated id for ppAuthenticatedChannel arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAuthenticatedChannel -> *ppAuthenticatedChannel} in the
     * context object table. */
    uint64_t _guest_id_ppAuthenticatedChannel;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateAuthenticatedChannel_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D11VideoDevice_CreateAuthenticatedChannel *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D11_AUTHENTICATED_CHANNEL_TYPE(dec, &args->ChannelType);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppAuthenticatedChannel = _gid;
        args->ppAuthenticatedChannel = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppAuthenticatedChannel) return;
        *args->ppAuthenticatedChannel = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoDevice_CreateAuthenticatedChannel_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateAuthenticatedChannel *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_CreateAuthenticatedChannel_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D11VideoDevice_CreateAuthenticatedChannel *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateAuthenticatedChannel),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppAuthenticatedChannel (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateAuthenticatedChannel(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateAuthenticatedChannel args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateAuthenticatedChannel_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateAuthenticatedChannel on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppAuthenticatedChannel);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateAuthenticatedChannel_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateAuthenticatedChannel: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppAuthenticatedChannel);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateAuthenticatedChannel _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateAuthenticatedChannel, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateAuthenticatedChannel);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateAuthenticatedChannel) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateAuthenticatedChannel(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ChannelType, args.ppAuthenticatedChannel);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppAuthenticatedChannel && *args.ppAuthenticatedChannel)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppAuthenticatedChannel, *args.ppAuthenticatedChannel, NPT_OBJECT_TYPE_ID3D11AUTHENTICATEDCHANNEL);
    else if (args.ppAuthenticatedChannel)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppAuthenticatedChannel);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateAuthenticatedChannel_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CreateCryptoSession
 */

struct npt_command_ID3D11VideoDevice_CreateCryptoSession {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    const GUID * pKeyExchangeType;
    ID3D11CryptoSession ** ppCryptoSession;
    /* Shadow: the guest-allocated id for ppCryptoSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCryptoSession -> *ppCryptoSession} in the
     * context object table. */
    uint64_t _guest_id_ppCryptoSession;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateCryptoSession_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11VideoDevice_CreateCryptoSession *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pCryptoType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pCryptoType) return;
        npt_decode_GUID(dec, (GUID *)args->pCryptoType);
    } else {
        args->pCryptoType = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDecoderProfile = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pDecoderProfile) return;
        npt_decode_GUID(dec, (GUID *)args->pDecoderProfile);
    } else {
        args->pDecoderProfile = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pKeyExchangeType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pKeyExchangeType) return;
        npt_decode_GUID(dec, (GUID *)args->pKeyExchangeType);
    } else {
        args->pKeyExchangeType = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCryptoSession = _gid;
        args->ppCryptoSession = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCryptoSession) return;
        *args->ppCryptoSession = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoDevice_CreateCryptoSession_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateCryptoSession *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_CreateCryptoSession_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11VideoDevice_CreateCryptoSession *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateCryptoSession),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCryptoSession (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateCryptoSession(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateCryptoSession args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateCryptoSession_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateCryptoSession on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCryptoSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateCryptoSession_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateCryptoSession: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCryptoSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateCryptoSession _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateCryptoSession, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateCryptoSession);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateCryptoSession) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateCryptoSession(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoType, args.pDecoderProfile, args.pKeyExchangeType, args.ppCryptoSession);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCryptoSession && *args.ppCryptoSession)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCryptoSession, *args.ppCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
    else if (args.ppCryptoSession)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCryptoSession);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateCryptoSession_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CreateVideoDecoderOutputView
 */

struct npt_command_ID3D11VideoDevice_CreateVideoDecoderOutputView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC * pDesc;
    ID3D11VideoDecoderOutputView ** ppVDOVView;
    /* Shadow: the guest-allocated id for ppVDOVView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVDOVView -> *ppVDOVView} in the
     * context object table. */
    uint64_t _guest_id_ppVDOVView;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoDecoderOutputView_args_temp(struct npt_cs_decoder *dec,
                                                                    struct npt_command_ID3D11VideoDevice_CreateVideoDecoderOutputView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC(dec, (D3D11_VIDEO_DECODER_OUTPUT_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppVDOVView = _gid;
        args->ppVDOVView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppVDOVView) return;
        *args->ppVDOVView = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoDevice_CreateVideoDecoderOutputView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateVideoDecoderOutputView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoDecoderOutputView_reply(struct npt_cs_encoder *enc,
                                                                const struct npt_command_ID3D11VideoDevice_CreateVideoDecoderOutputView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoDecoderOutputView),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppVDOVView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateVideoDecoderOutputView(struct npt_dispatch_context *ctx,
                                                            uint32_t cmd_flags,
                                                            uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateVideoDecoderOutputView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateVideoDecoderOutputView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoDecoderOutputView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVDOVView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateVideoDecoderOutputView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoDecoderOutputView: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVDOVView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateVideoDecoderOutputView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateVideoDecoderOutputView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateVideoDecoderOutputView);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateVideoDecoderOutputView) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateVideoDecoderOutputView(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc, args.ppVDOVView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppVDOVView && *args.ppVDOVView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppVDOVView, *args.ppVDOVView, NPT_OBJECT_TYPE_ID3D11VIDEODECODEROUTPUTVIEW);
    else if (args.ppVDOVView)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVDOVView);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateVideoDecoderOutputView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CreateVideoProcessorInputView
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessorInputView {
    void *_self;
    ID3D11Resource * pResource;
    ID3D11VideoProcessorEnumerator * pEnum;
    const D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC * pDesc;
    ID3D11VideoProcessorInputView ** ppVPIView;
    /* Shadow: the guest-allocated id for ppVPIView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVPIView -> *ppVPIView} in the
     * context object table. */
    uint64_t _guest_id_ppVPIView;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessorInputView_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D11VideoDevice_CreateVideoProcessorInputView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pEnum = (ID3D11VideoProcessorEnumerator *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC(dec, (D3D11_VIDEO_PROCESSOR_INPUT_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppVPIView = _gid;
        args->ppVPIView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppVPIView) return;
        *args->ppVPIView = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoDevice_CreateVideoProcessorInputView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateVideoProcessorInputView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pEnum = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pEnum, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessorInputView_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D11VideoDevice_CreateVideoProcessorInputView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorInputView),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppVPIView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateVideoProcessorInputView(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateVideoProcessorInputView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateVideoProcessorInputView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessorInputView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVPIView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateVideoProcessorInputView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessorInputView: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVPIView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateVideoProcessorInputView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateVideoProcessorInputView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateVideoProcessorInputView);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessorInputView) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessorInputView(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pEnum, args.pDesc, args.ppVPIView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppVPIView && *args.ppVPIView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppVPIView, *args.ppVPIView, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORINPUTVIEW);
    else if (args.ppVPIView)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVPIView);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateVideoProcessorInputView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CreateVideoProcessorOutputView
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessorOutputView {
    void *_self;
    ID3D11Resource * pResource;
    ID3D11VideoProcessorEnumerator * pEnum;
    const D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC * pDesc;
    ID3D11VideoProcessorOutputView ** ppVPOView;
    /* Shadow: the guest-allocated id for ppVPOView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVPOView -> *ppVPOView} in the
     * context object table. */
    uint64_t _guest_id_ppVPOView;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessorOutputView_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11VideoDevice_CreateVideoProcessorOutputView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pEnum = (ID3D11VideoProcessorEnumerator *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC(dec, (D3D11_VIDEO_PROCESSOR_OUTPUT_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppVPOView = _gid;
        args->ppVPOView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppVPOView) return;
        *args->ppVPOView = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoDevice_CreateVideoProcessorOutputView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateVideoProcessorOutputView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pEnum = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pEnum, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessorOutputView_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11VideoDevice_CreateVideoProcessorOutputView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorOutputView),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppVPOView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateVideoProcessorOutputView(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateVideoProcessorOutputView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateVideoProcessorOutputView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessorOutputView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVPOView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateVideoProcessorOutputView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessorOutputView: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVPOView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateVideoProcessorOutputView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateVideoProcessorOutputView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateVideoProcessorOutputView);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessorOutputView) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessorOutputView(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pEnum, args.pDesc, args.ppVPOView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppVPOView && *args.ppVPOView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppVPOView, *args.ppVPOView, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOROUTPUTVIEW);
    else if (args.ppVPOView)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVPOView);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateVideoProcessorOutputView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CreateVideoProcessorEnumerator
 */

struct npt_command_ID3D11VideoDevice_CreateVideoProcessorEnumerator {
    void *_self;
    const D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pDesc;
    ID3D11VideoProcessorEnumerator ** ppEnum;
    /* Shadow: the guest-allocated id for ppEnum arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppEnum -> *ppEnum} in the
     * context object table. */
    uint64_t _guest_id_ppEnum;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CreateVideoProcessorEnumerator_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11VideoDevice_CreateVideoProcessorEnumerator *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_CONTENT_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_VIDEO_PROCESSOR_CONTENT_DESC(dec, (D3D11_VIDEO_PROCESSOR_CONTENT_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppEnum = _gid;
        args->ppEnum = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppEnum) return;
        *args->ppEnum = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoDevice_CreateVideoProcessorEnumerator_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CreateVideoProcessorEnumerator *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_CreateVideoProcessorEnumerator_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11VideoDevice_CreateVideoProcessorEnumerator *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CreateVideoProcessorEnumerator),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppEnum (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11VideoDevice_CreateVideoProcessorEnumerator(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CreateVideoProcessorEnumerator args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CreateVideoProcessorEnumerator_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessorEnumerator on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppEnum);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CreateVideoProcessorEnumerator_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CreateVideoProcessorEnumerator: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppEnum);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CreateVideoProcessorEnumerator _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CreateVideoProcessorEnumerator, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CreateVideoProcessorEnumerator);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessorEnumerator) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CreateVideoProcessorEnumerator(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.ppEnum);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppEnum && *args.ppEnum)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppEnum, *args.ppEnum, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
    else if (args.ppEnum)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppEnum);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CreateVideoProcessorEnumerator_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::GetVideoDecoderProfileCount
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfileCount {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderProfileCount_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfileCount *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11VideoDevice_GetVideoDecoderProfileCount_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfileCount *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderProfileCount_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfileCount *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfileCount),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoDevice_GetVideoDecoderProfileCount(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfileCount args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_GetVideoDecoderProfileCount_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderProfileCount on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_GetVideoDecoderProfileCount_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderProfileCount: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_GetVideoDecoderProfileCount _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_GetVideoDecoderProfileCount, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_GetVideoDecoderProfileCount);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderProfileCount) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderProfileCount(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_GetVideoDecoderProfileCount_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::GetVideoDecoderProfile
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfile {
    void *_self;
    UINT Index;
    GUID * pDecoderProfile;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderProfile_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfile *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Index);

    /* Allocate temp storage for output-only parameters */

    args->pDecoderProfile = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
    if (!args->pDecoderProfile) return;
    memset(args->pDecoderProfile, 0, sizeof(GUID));
}

static inline void
npt_replace_ID3D11VideoDevice_GetVideoDecoderProfile_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfile *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderProfile_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfile *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderProfile),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDecoderProfile))
        npt_encode_GUID(enc, args->pDecoderProfile);
}

static inline void
npt_dispatch_ID3D11VideoDevice_GetVideoDecoderProfile(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_GetVideoDecoderProfile args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_GetVideoDecoderProfile_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderProfile on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_GetVideoDecoderProfile_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderProfile: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_GetVideoDecoderProfile _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_GetVideoDecoderProfile, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_GetVideoDecoderProfile);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderProfile) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderProfile(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Index, args.pDecoderProfile);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_GetVideoDecoderProfile_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CheckVideoDecoderFormat
 */

struct npt_command_ID3D11VideoDevice_CheckVideoDecoderFormat {
    void *_self;
    const GUID * pDecoderProfile;
    DXGI_FORMAT Format;
    BOOL * pSupported;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CheckVideoDecoderFormat_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11VideoDevice_CheckVideoDecoderFormat *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDecoderProfile = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pDecoderProfile) return;
        npt_decode_GUID(dec, (GUID *)args->pDecoderProfile);
    } else {
        args->pDecoderProfile = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_DXGI_FORMAT(dec, &args->Format);

    /* Allocate temp storage for output-only parameters */


    args->pSupported = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pSupported) return;
    memset(args->pSupported, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11VideoDevice_CheckVideoDecoderFormat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CheckVideoDecoderFormat *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_CheckVideoDecoderFormat_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11VideoDevice_CheckVideoDecoderFormat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CheckVideoDecoderFormat),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pSupported))
        npt_encode_BOOL(enc, args->pSupported);
}

static inline void
npt_dispatch_ID3D11VideoDevice_CheckVideoDecoderFormat(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CheckVideoDecoderFormat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CheckVideoDecoderFormat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CheckVideoDecoderFormat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CheckVideoDecoderFormat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CheckVideoDecoderFormat: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CheckVideoDecoderFormat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CheckVideoDecoderFormat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CheckVideoDecoderFormat);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CheckVideoDecoderFormat) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CheckVideoDecoderFormat(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoderProfile, args.Format, args.pSupported);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CheckVideoDecoderFormat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::GetVideoDecoderConfigCount
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfigCount {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pDesc;
    UINT * pCount;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderConfigCount_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfigCount *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_VIDEO_DECODER_DESC(dec, (D3D11_VIDEO_DECODER_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */

    args->pCount = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pCount) return;
    memset(args->pCount, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11VideoDevice_GetVideoDecoderConfigCount_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfigCount *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderConfigCount_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfigCount *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfigCount),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pCount))
        npt_encode_UINT(enc, args->pCount);
}

static inline void
npt_dispatch_ID3D11VideoDevice_GetVideoDecoderConfigCount(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfigCount args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_GetVideoDecoderConfigCount_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderConfigCount on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_GetVideoDecoderConfigCount_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderConfigCount: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_GetVideoDecoderConfigCount _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_GetVideoDecoderConfigCount, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_GetVideoDecoderConfigCount);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderConfigCount) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderConfigCount(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pCount);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_GetVideoDecoderConfigCount_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::GetVideoDecoderConfig
 */

struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfig {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pDesc;
    UINT Index;
    D3D11_VIDEO_DECODER_CONFIG * pConfig;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_GetVideoDecoderConfig_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfig *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_VIDEO_DECODER_DESC(dec, (D3D11_VIDEO_DECODER_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->Index);

    /* Allocate temp storage for output-only parameters */


    args->pConfig = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_CONFIG));
    if (!args->pConfig) return;
    memset(args->pConfig, 0, sizeof(D3D11_VIDEO_DECODER_CONFIG));
}

static inline void
npt_replace_ID3D11VideoDevice_GetVideoDecoderConfig_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfig *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_GetVideoDecoderConfig_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfig *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetVideoDecoderConfig),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pConfig))
        npt_encode_D3D11_VIDEO_DECODER_CONFIG(enc, args->pConfig);
}

static inline void
npt_dispatch_ID3D11VideoDevice_GetVideoDecoderConfig(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_GetVideoDecoderConfig args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_GetVideoDecoderConfig_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderConfig on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_GetVideoDecoderConfig_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetVideoDecoderConfig: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_GetVideoDecoderConfig _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_GetVideoDecoderConfig, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_GetVideoDecoderConfig);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderConfig) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->GetVideoDecoderConfig(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.Index, args.pConfig);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_GetVideoDecoderConfig_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::GetContentProtectionCaps
 */

struct npt_command_ID3D11VideoDevice_GetContentProtectionCaps {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    D3D11_VIDEO_CONTENT_PROTECTION_CAPS * pCaps;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_GetContentProtectionCaps_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11VideoDevice_GetContentProtectionCaps *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pCryptoType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pCryptoType) return;
        npt_decode_GUID(dec, (GUID *)args->pCryptoType);
    } else {
        args->pCryptoType = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDecoderProfile = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pDecoderProfile) return;
        npt_decode_GUID(dec, (GUID *)args->pDecoderProfile);
    } else {
        args->pDecoderProfile = NULL;
    }

    /* Allocate temp storage for output-only parameters */


    args->pCaps = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_CONTENT_PROTECTION_CAPS));
    if (!args->pCaps) return;
    memset(args->pCaps, 0, sizeof(D3D11_VIDEO_CONTENT_PROTECTION_CAPS));
}

static inline void
npt_replace_ID3D11VideoDevice_GetContentProtectionCaps_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_GetContentProtectionCaps *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_GetContentProtectionCaps_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11VideoDevice_GetContentProtectionCaps *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_GetContentProtectionCaps),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pCaps))
        npt_encode_D3D11_VIDEO_CONTENT_PROTECTION_CAPS(enc, args->pCaps);
}

static inline void
npt_dispatch_ID3D11VideoDevice_GetContentProtectionCaps(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_GetContentProtectionCaps args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_GetContentProtectionCaps_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetContentProtectionCaps on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_GetContentProtectionCaps_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_GetContentProtectionCaps: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_GetContentProtectionCaps _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_GetContentProtectionCaps, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_GetContentProtectionCaps);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->GetContentProtectionCaps) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->GetContentProtectionCaps(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoType, args.pDecoderProfile, args.pCaps);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_GetContentProtectionCaps_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::CheckCryptoKeyExchange
 */

struct npt_command_ID3D11VideoDevice_CheckCryptoKeyExchange {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    UINT Index;
    GUID * pKeyExchangeType;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_CheckCryptoKeyExchange_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11VideoDevice_CheckCryptoKeyExchange *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pCryptoType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pCryptoType) return;
        npt_decode_GUID(dec, (GUID *)args->pCryptoType);
    } else {
        args->pCryptoType = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDecoderProfile = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pDecoderProfile) return;
        npt_decode_GUID(dec, (GUID *)args->pDecoderProfile);
    } else {
        args->pDecoderProfile = NULL;
    }
    npt_decode_UINT(dec, &args->Index);

    /* Allocate temp storage for output-only parameters */



    args->pKeyExchangeType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
    if (!args->pKeyExchangeType) return;
    memset(args->pKeyExchangeType, 0, sizeof(GUID));
}

static inline void
npt_replace_ID3D11VideoDevice_CheckCryptoKeyExchange_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_CheckCryptoKeyExchange *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_CheckCryptoKeyExchange_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11VideoDevice_CheckCryptoKeyExchange *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_CheckCryptoKeyExchange),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pKeyExchangeType))
        npt_encode_GUID(enc, args->pKeyExchangeType);
}

static inline void
npt_dispatch_ID3D11VideoDevice_CheckCryptoKeyExchange(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_CheckCryptoKeyExchange args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_CheckCryptoKeyExchange_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CheckCryptoKeyExchange on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_CheckCryptoKeyExchange_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_CheckCryptoKeyExchange: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_CheckCryptoKeyExchange _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_CheckCryptoKeyExchange, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_CheckCryptoKeyExchange);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->CheckCryptoKeyExchange) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->CheckCryptoKeyExchange(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoType, args.pDecoderProfile, args.Index, args.pKeyExchangeType);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_CheckCryptoKeyExchange_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::SetPrivateData
 */

struct npt_command_ID3D11VideoDevice_SetPrivateData {
    void *_self;
    const GUID * guid;
    UINT DataSize;
    const void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_SetPrivateData_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11VideoDevice_SetPrivateData *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoDevice_SetPrivateData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_SetPrivateData *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice_SetPrivateData_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11VideoDevice_SetPrivateData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_SetPrivateData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoDevice_SetPrivateData(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_SetPrivateData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_SetPrivateData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_SetPrivateData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_SetPrivateData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_SetPrivateData: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_SetPrivateData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_SetPrivateData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_SetPrivateData);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->SetPrivateData) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->SetPrivateData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_SetPrivateData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice::SetPrivateDataInterface
 */

struct npt_command_ID3D11VideoDevice_SetPrivateDataInterface {
    void *_self;
    const GUID * guid;
    const IUnknown * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice_SetPrivateDataInterface_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11VideoDevice_SetPrivateDataInterface *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pData = (IUnknown *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoDevice_SetPrivateDataInterface_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice_SetPrivateDataInterface *args)
{
    args->pData = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pData, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_ID3D11VideoDevice_SetPrivateDataInterface_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11VideoDevice_SetPrivateDataInterface *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice,
                                 NPT_METHOD_ID3D11VideoDevice_SetPrivateDataInterface),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoDevice_SetPrivateDataInterface(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice_SetPrivateDataInterface args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice_SetPrivateDataInterface_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_SetPrivateDataInterface on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice_SetPrivateDataInterface_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice_SetPrivateDataInterface: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice_SetPrivateDataInterface _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice_SetPrivateDataInterface, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice_SetPrivateDataInterface);

    if (ctx->id3d11videodevice_dispatch_overrides && ctx->id3d11videodevice_dispatch_overrides->SetPrivateDataInterface) {
        args.ret = ctx->id3d11videodevice_dispatch_overrides->SetPrivateDataInterface(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice_SetPrivateDataInterface_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice1::GetCryptoSessionPrivateDataSize
 */

struct npt_command_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize {
    void *_self;
    const GUID * pCryptoType;
    const GUID * pDecoderProfile;
    const GUID * pKeyExchangeType;
    UINT * pPrivateInputSize;
    UINT * pPrivateOutputSize;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_args_temp(struct npt_cs_decoder *dec,
                                                                        struct npt_command_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pCryptoType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pCryptoType) return;
        npt_decode_GUID(dec, (GUID *)args->pCryptoType);
    } else {
        args->pCryptoType = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDecoderProfile = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pDecoderProfile) return;
        npt_decode_GUID(dec, (GUID *)args->pDecoderProfile);
    } else {
        args->pDecoderProfile = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pKeyExchangeType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pKeyExchangeType) return;
        npt_decode_GUID(dec, (GUID *)args->pKeyExchangeType);
    } else {
        args->pKeyExchangeType = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }


    /* Allocate temp storage for output-only parameters */



    args->pPrivateInputSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pPrivateInputSize) return;
    memset(args->pPrivateInputSize, 0, sizeof(UINT));
    args->pPrivateOutputSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pPrivateOutputSize) return;
    memset(args->pPrivateOutputSize, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_reply(struct npt_cs_encoder *enc,
                                                                    const struct npt_command_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pPrivateInputSize))
        npt_encode_UINT(enc, args->pPrivateInputSize);
    if (npt_encode_simple_pointer(enc, args->pPrivateOutputSize))
        npt_encode_UINT(enc, args->pPrivateOutputSize);
}

static inline void
npt_dispatch_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize(struct npt_dispatch_context *ctx,
                                                                uint32_t cmd_flags,
                                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize);

    if (ctx->id3d11videodevice1_dispatch_overrides && ctx->id3d11videodevice1_dispatch_overrides->GetCryptoSessionPrivateDataSize) {
        args.ret = ctx->id3d11videodevice1_dispatch_overrides->GetCryptoSessionPrivateDataSize(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoType, args.pDecoderProfile, args.pKeyExchangeType, args.pPrivateInputSize, args.pPrivateOutputSize);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice1_GetCryptoSessionPrivateDataSize_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice1::GetVideoDecoderCaps
 */

struct npt_command_ID3D11VideoDevice1_GetVideoDecoderCaps {
    void *_self;
    const GUID * pDecoderProfile;
    UINT SampleWidth;
    UINT SampleHeight;
    const DXGI_RATIONAL * pFrameRate;
    UINT BitRate;
    const GUID * pCryptoType;
    UINT * pDecoderCaps;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice1_GetVideoDecoderCaps_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11VideoDevice1_GetVideoDecoderCaps *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDecoderProfile = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pDecoderProfile) return;
        npt_decode_GUID(dec, (GUID *)args->pDecoderProfile);
    } else {
        args->pDecoderProfile = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->SampleWidth);
    npt_decode_UINT(dec, &args->SampleHeight);
    if (npt_decode_simple_pointer(dec)) {
        args->pFrameRate = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
        if (!args->pFrameRate) return;
        npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)args->pFrameRate);
    } else {
        args->pFrameRate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->BitRate);
    if (npt_decode_simple_pointer(dec)) {
        args->pCryptoType = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pCryptoType) return;
        npt_decode_GUID(dec, (GUID *)args->pCryptoType);
    } else {
        args->pCryptoType = NULL;
    }

    /* Allocate temp storage for output-only parameters */






    args->pDecoderCaps = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pDecoderCaps) return;
    memset(args->pDecoderCaps, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11VideoDevice1_GetVideoDecoderCaps_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice1_GetVideoDecoderCaps *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice1_GetVideoDecoderCaps_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11VideoDevice1_GetVideoDecoderCaps *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_GetVideoDecoderCaps),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDecoderCaps))
        npt_encode_UINT(enc, args->pDecoderCaps);
}

static inline void
npt_dispatch_ID3D11VideoDevice1_GetVideoDecoderCaps(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice1_GetVideoDecoderCaps args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice1_GetVideoDecoderCaps_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_GetVideoDecoderCaps on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice1_GetVideoDecoderCaps_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_GetVideoDecoderCaps: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice1_GetVideoDecoderCaps _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice1_GetVideoDecoderCaps, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice1_GetVideoDecoderCaps);

    if (ctx->id3d11videodevice1_dispatch_overrides && ctx->id3d11videodevice1_dispatch_overrides->GetVideoDecoderCaps) {
        args.ret = ctx->id3d11videodevice1_dispatch_overrides->GetVideoDecoderCaps(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoderProfile, args.SampleWidth, args.SampleHeight, args.pFrameRate, args.BitRate, args.pCryptoType, args.pDecoderCaps);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice1_GetVideoDecoderCaps_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice1::CheckVideoDecoderDownsampling
 */

struct npt_command_ID3D11VideoDevice1_CheckVideoDecoderDownsampling {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pInputDesc;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    const D3D11_VIDEO_DECODER_CONFIG * pInputConfig;
    const DXGI_RATIONAL * pFrameRate;
    const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc;
    BOOL * pSupported;
    BOOL * pRealTimeHint;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11VideoDevice1_CheckVideoDecoderDownsampling *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pInputDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_DESC));
        if (!args->pInputDesc) return;
        npt_decode_D3D11_VIDEO_DECODER_DESC(dec, (D3D11_VIDEO_DECODER_DESC *)args->pInputDesc);
    } else {
        args->pInputDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->InputColorSpace);
    if (npt_decode_simple_pointer(dec)) {
        args->pInputConfig = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_CONFIG));
        if (!args->pInputConfig) return;
        npt_decode_D3D11_VIDEO_DECODER_CONFIG(dec, (D3D11_VIDEO_DECODER_CONFIG *)args->pInputConfig);
    } else {
        args->pInputConfig = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pFrameRate = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
        if (!args->pFrameRate) return;
        npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)args->pFrameRate);
    } else {
        args->pFrameRate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pOutputDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_SAMPLE_DESC));
        if (!args->pOutputDesc) return;
        npt_decode_D3D11_VIDEO_SAMPLE_DESC(dec, (D3D11_VIDEO_SAMPLE_DESC *)args->pOutputDesc);
    } else {
        args->pOutputDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }


    /* Allocate temp storage for output-only parameters */





    args->pSupported = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pSupported) return;
    memset(args->pSupported, 0, sizeof(BOOL));
    args->pRealTimeHint = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pRealTimeHint) return;
    memset(args->pRealTimeHint, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice1_CheckVideoDecoderDownsampling *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11VideoDevice1_CheckVideoDecoderDownsampling *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_CheckVideoDecoderDownsampling),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pSupported))
        npt_encode_BOOL(enc, args->pSupported);
    if (npt_encode_simple_pointer(enc, args->pRealTimeHint))
        npt_encode_BOOL(enc, args->pRealTimeHint);
}

static inline void
npt_dispatch_ID3D11VideoDevice1_CheckVideoDecoderDownsampling(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice1_CheckVideoDecoderDownsampling args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_CheckVideoDecoderDownsampling on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_CheckVideoDecoderDownsampling: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice1_CheckVideoDecoderDownsampling _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice1_CheckVideoDecoderDownsampling, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice1_CheckVideoDecoderDownsampling);

    if (ctx->id3d11videodevice1_dispatch_overrides && ctx->id3d11videodevice1_dispatch_overrides->CheckVideoDecoderDownsampling) {
        args.ret = ctx->id3d11videodevice1_dispatch_overrides->CheckVideoDecoderDownsampling(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pInputDesc, args.InputColorSpace, args.pInputConfig, args.pFrameRate, args.pOutputDesc, args.pSupported, args.pRealTimeHint);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice1_CheckVideoDecoderDownsampling_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice1::RecommendVideoDecoderDownsampleParameters
 */

struct npt_command_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters {
    void *_self;
    const D3D11_VIDEO_DECODER_DESC * pInputDesc;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    const D3D11_VIDEO_DECODER_CONFIG * pInputConfig;
    const DXGI_RATIONAL * pFrameRate;
    D3D11_VIDEO_SAMPLE_DESC * pRecommendedOutputDesc;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_args_temp(struct npt_cs_decoder *dec,
                                                                                  struct npt_command_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pInputDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_DESC));
        if (!args->pInputDesc) return;
        npt_decode_D3D11_VIDEO_DECODER_DESC(dec, (D3D11_VIDEO_DECODER_DESC *)args->pInputDesc);
    } else {
        args->pInputDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->InputColorSpace);
    if (npt_decode_simple_pointer(dec)) {
        args->pInputConfig = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_CONFIG));
        if (!args->pInputConfig) return;
        npt_decode_D3D11_VIDEO_DECODER_CONFIG(dec, (D3D11_VIDEO_DECODER_CONFIG *)args->pInputConfig);
    } else {
        args->pInputConfig = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pFrameRate = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
        if (!args->pFrameRate) return;
        npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)args->pFrameRate);
    } else {
        args->pFrameRate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */




    args->pRecommendedOutputDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_SAMPLE_DESC));
    if (!args->pRecommendedOutputDesc) return;
    memset(args->pRecommendedOutputDesc, 0, sizeof(D3D11_VIDEO_SAMPLE_DESC));
}

static inline void
npt_replace_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_reply(struct npt_cs_encoder *enc,
                                                                              const struct npt_command_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice1,
                                 NPT_METHOD_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pRecommendedOutputDesc))
        npt_encode_D3D11_VIDEO_SAMPLE_DESC(enc, args->pRecommendedOutputDesc);
}

static inline void
npt_dispatch_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters(struct npt_dispatch_context *ctx,
                                                                          uint32_t cmd_flags,
                                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters);

    if (ctx->id3d11videodevice1_dispatch_overrides && ctx->id3d11videodevice1_dispatch_overrides->RecommendVideoDecoderDownsampleParameters) {
        args.ret = ctx->id3d11videodevice1_dispatch_overrides->RecommendVideoDecoderDownsampleParameters(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pInputDesc, args.InputColorSpace, args.pInputConfig, args.pFrameRate, args.pRecommendedOutputDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice1_RecommendVideoDecoderDownsampleParameters_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice2::CheckFeatureSupport
 */

struct npt_command_ID3D11VideoDevice2_CheckFeatureSupport {
    void *_self;
    D3D11_FEATURE_VIDEO Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice2_CheckFeatureSupport_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11VideoDevice2_CheckFeatureSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D11_FEATURE_VIDEO(dec, &args->Feature);

    npt_decode_UINT(dec, &args->FeatureSupportDataSize);
    /* Allocate temp storage for output-only parameters */

    if (args->FeatureSupportDataSize) {
        args->pFeatureSupportData = npt_cs_decoder_alloc_temp(dec, args->FeatureSupportDataSize);
        if (!args->pFeatureSupportData) return;
        memset(args->pFeatureSupportData, 0, args->FeatureSupportDataSize);
    } else {
        args->pFeatureSupportData = NULL;
    }

}

static inline void
npt_replace_ID3D11VideoDevice2_CheckFeatureSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice2_CheckFeatureSupport *args)
{
}

static inline void
npt_encode_ID3D11VideoDevice2_CheckFeatureSupport_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11VideoDevice2_CheckFeatureSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                 NPT_METHOD_ID3D11VideoDevice2_CheckFeatureSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pFeatureSupportData) {
        npt_encode_array_count(enc, args->FeatureSupportDataSize);
        npt_encode_blob_array(enc, args->pFeatureSupportData, args->FeatureSupportDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoDevice2_CheckFeatureSupport(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice2_CheckFeatureSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice2_CheckFeatureSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice2_CheckFeatureSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice2_CheckFeatureSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice2_CheckFeatureSupport: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice2_CheckFeatureSupport _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice2_CheckFeatureSupport, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice2_CheckFeatureSupport);

    if (ctx->id3d11videodevice2_dispatch_overrides && ctx->id3d11videodevice2_dispatch_overrides->CheckFeatureSupport) {
        args.ret = ctx->id3d11videodevice2_dispatch_overrides->CheckFeatureSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Feature, args.pFeatureSupportData, args.FeatureSupportDataSize);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice2_CheckFeatureSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoDevice2::NegotiateCryptoSessionKeyExchangeMT
 */

struct npt_command_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS flags;
    UINT DataSize;
    void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    npt_decode_D3D11_CRYPTO_SESSION_KEY_EXCHANGE_FLAGS(dec, &args->flags);
    npt_decode_UINT(dec, &args->DataSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
}

static inline void
npt_encode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoDevice2,
                                 NPT_METHOD_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pData) {
        npt_encode_array_count(enc, args->DataSize);
        npt_encode_blob_array(enc, args->pData, args->DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEODEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT);

    if (ctx->id3d11videodevice2_dispatch_overrides && ctx->id3d11videodevice2_dispatch_overrides->NegotiateCryptoSessionKeyExchangeMT) {
        args.ret = ctx->id3d11videodevice2_dispatch_overrides->NegotiateCryptoSessionKeyExchangeMT(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoSession, args.flags, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoDevice2_NegotiateCryptoSessionKeyExchangeMT_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11VIDEODEVICE_H */

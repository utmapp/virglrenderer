/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11authenticatedchannel.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11AUTHENTICATEDCHANNEL_H
#define NPT_PROTOCOL_HOST_ID3D11AUTHENTICATEDCHANNEL_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11AuthenticatedChannel::GetCertificateSize
 */

struct npt_command_ID3D11AuthenticatedChannel_GetCertificateSize {
    void *_self;
    UINT * pCertificateSize;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11AuthenticatedChannel_GetCertificateSize_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D11AuthenticatedChannel_GetCertificateSize *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pCertificateSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pCertificateSize) return;
    memset(args->pCertificateSize, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11AuthenticatedChannel_GetCertificateSize_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11AuthenticatedChannel_GetCertificateSize *args)
{
}

static inline void
npt_encode_ID3D11AuthenticatedChannel_GetCertificateSize_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D11AuthenticatedChannel_GetCertificateSize *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11AuthenticatedChannel,
                                 NPT_METHOD_ID3D11AuthenticatedChannel_GetCertificateSize),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pCertificateSize))
        npt_encode_UINT(enc, args->pCertificateSize);
}

static inline void
npt_dispatch_ID3D11AuthenticatedChannel_GetCertificateSize(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D11AuthenticatedChannel_GetCertificateSize args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11AuthenticatedChannel_GetCertificateSize_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11AUTHENTICATEDCHANNEL);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11AuthenticatedChannel_GetCertificateSize on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11AuthenticatedChannel_GetCertificateSize_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11AuthenticatedChannel_GetCertificateSize: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11AuthenticatedChannel_GetCertificateSize _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11AuthenticatedChannel_GetCertificateSize, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11AuthenticatedChannel_GetCertificateSize);

    if (ctx->id3d11authenticatedchannel_dispatch_overrides && ctx->id3d11authenticatedchannel_dispatch_overrides->GetCertificateSize) {
        args.ret = ctx->id3d11authenticatedchannel_dispatch_overrides->GetCertificateSize(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCertificateSize);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11AuthenticatedChannel_GetCertificateSize_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11AuthenticatedChannel::GetCertificate
 */

struct npt_command_ID3D11AuthenticatedChannel_GetCertificate {
    void *_self;
    UINT CertificateSize;
    BYTE * pCertificate;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11AuthenticatedChannel_GetCertificate_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11AuthenticatedChannel_GetCertificate *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->CertificateSize);

    /* Allocate temp storage for output-only parameters */

    if (args->CertificateSize) {
        args->pCertificate = npt_cs_decoder_alloc_temp_array(dec, sizeof(BYTE), args->CertificateSize);
        if (!args->pCertificate) return;
        memset(args->pCertificate, 0, sizeof(BYTE) * args->CertificateSize);
    } else {
        args->pCertificate = NULL;
    }
}

static inline void
npt_replace_ID3D11AuthenticatedChannel_GetCertificate_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11AuthenticatedChannel_GetCertificate *args)
{
}

static inline void
npt_encode_ID3D11AuthenticatedChannel_GetCertificate_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11AuthenticatedChannel_GetCertificate *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11AuthenticatedChannel,
                                 NPT_METHOD_ID3D11AuthenticatedChannel_GetCertificate),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pCertificate) {
        npt_encode_array_count(enc, args->CertificateSize);
        npt_encode_BYTE_array(enc, args->pCertificate, args->CertificateSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11AuthenticatedChannel_GetCertificate(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11AuthenticatedChannel_GetCertificate args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11AuthenticatedChannel_GetCertificate_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11AUTHENTICATEDCHANNEL);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11AuthenticatedChannel_GetCertificate on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11AuthenticatedChannel_GetCertificate_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11AuthenticatedChannel_GetCertificate: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11AuthenticatedChannel_GetCertificate _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11AuthenticatedChannel_GetCertificate, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11AuthenticatedChannel_GetCertificate);

    if (ctx->id3d11authenticatedchannel_dispatch_overrides && ctx->id3d11authenticatedchannel_dispatch_overrides->GetCertificate) {
        args.ret = ctx->id3d11authenticatedchannel_dispatch_overrides->GetCertificate(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.CertificateSize, args.pCertificate);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11AuthenticatedChannel_GetCertificate_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11AuthenticatedChannel::GetChannelHandle
 */

struct npt_command_ID3D11AuthenticatedChannel_GetChannelHandle {
    void *_self;
    HANDLE * pChannelHandle;
};

static inline void
npt_decode_ID3D11AuthenticatedChannel_GetChannelHandle_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D11AuthenticatedChannel_GetChannelHandle *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pChannelHandle = npt_cs_decoder_alloc_temp(dec, sizeof(HANDLE));
    if (!args->pChannelHandle) return;
    memset(args->pChannelHandle, 0, sizeof(HANDLE));
}

static inline void
npt_replace_ID3D11AuthenticatedChannel_GetChannelHandle_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11AuthenticatedChannel_GetChannelHandle *args)
{
}

static inline void
npt_encode_ID3D11AuthenticatedChannel_GetChannelHandle_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D11AuthenticatedChannel_GetChannelHandle *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11AuthenticatedChannel,
                                 NPT_METHOD_ID3D11AuthenticatedChannel_GetChannelHandle),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)*args->pChannelHandle));
}

static inline void
npt_dispatch_ID3D11AuthenticatedChannel_GetChannelHandle(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D11AuthenticatedChannel_GetChannelHandle args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11AuthenticatedChannel_GetChannelHandle_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11AUTHENTICATEDCHANNEL);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11AuthenticatedChannel_GetChannelHandle on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11AuthenticatedChannel_GetChannelHandle_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11AuthenticatedChannel_GetChannelHandle: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11AuthenticatedChannel_GetChannelHandle _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11AuthenticatedChannel_GetChannelHandle, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11AuthenticatedChannel_GetChannelHandle);

    if (ctx->id3d11authenticatedchannel_dispatch_overrides && ctx->id3d11authenticatedchannel_dispatch_overrides->GetChannelHandle) {
        ctx->id3d11authenticatedchannel_dispatch_overrides->GetChannelHandle(ctx, &args, _original);
    } else {
        _original(args._self, args.pChannelHandle);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11AuthenticatedChannel_GetChannelHandle_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11AUTHENTICATEDCHANNEL_H */

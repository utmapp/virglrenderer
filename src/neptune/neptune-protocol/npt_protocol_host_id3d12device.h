/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12device.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D12DEVICE_H
#define NPT_PROTOCOL_HOST_ID3D12DEVICE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12Device::GetNodeCount
 */

struct npt_command_ID3D12Device_GetNodeCount {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_ID3D12Device_GetNodeCount_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D12Device_GetNodeCount *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12Device_GetNodeCount_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetNodeCount *args)
{
}

static inline void
npt_encode_ID3D12Device_GetNodeCount_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D12Device_GetNodeCount *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetNodeCount),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_GetNodeCount(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetNodeCount args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetNodeCount_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetNodeCount on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetNodeCount_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetNodeCount: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetNodeCount _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetNodeCount, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetNodeCount);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetNodeCount) {
        args.ret = ctx->id3d12device_dispatch_overrides->GetNodeCount(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetNodeCount_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateCommandQueue
 */

struct npt_command_ID3D12Device_CreateCommandQueue {
    void *_self;
    const D3D12_COMMAND_QUEUE_DESC * pDesc;
    const IID * riid;
    void ** ppCommandQueue;
    /* Shadow: the guest-allocated id for ppCommandQueue arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandQueue -> *ppCommandQueue} in the
     * context object table. */
    uint64_t _guest_id_ppCommandQueue;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateCommandQueue_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D12Device_CreateCommandQueue *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_COMMAND_QUEUE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_COMMAND_QUEUE_DESC(dec, (D3D12_COMMAND_QUEUE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCommandQueue = _gid;
        args->ppCommandQueue = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCommandQueue) return;
        *args->ppCommandQueue = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateCommandQueue_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateCommandQueue *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateCommandQueue_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D12Device_CreateCommandQueue *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandQueue),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCommandQueue (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateCommandQueue(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateCommandQueue args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateCommandQueue_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandQueue on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandQueue);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateCommandQueue_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandQueue: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandQueue);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateCommandQueue _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateCommandQueue, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateCommandQueue);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateCommandQueue) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateCommandQueue(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppCommandQueue);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCommandQueue && *args.ppCommandQueue)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCommandQueue, *args.ppCommandQueue,
            npt_object_type_from_iid(args.riid));
    else if (args.ppCommandQueue)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandQueue);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateCommandQueue_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateCommandAllocator
 */

struct npt_command_ID3D12Device_CreateCommandAllocator {
    void *_self;
    D3D12_COMMAND_LIST_TYPE type;
    const IID * riid;
    void ** ppCommandAllocator;
    /* Shadow: the guest-allocated id for ppCommandAllocator arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandAllocator -> *ppCommandAllocator} in the
     * context object table. */
    uint64_t _guest_id_ppCommandAllocator;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateCommandAllocator_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device_CreateCommandAllocator *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_COMMAND_LIST_TYPE(dec, &args->type);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCommandAllocator = _gid;
        args->ppCommandAllocator = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCommandAllocator) return;
        *args->ppCommandAllocator = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateCommandAllocator_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateCommandAllocator *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateCommandAllocator_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device_CreateCommandAllocator *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandAllocator),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCommandAllocator (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateCommandAllocator(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateCommandAllocator args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateCommandAllocator_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandAllocator on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandAllocator);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateCommandAllocator_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandAllocator: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandAllocator);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateCommandAllocator _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateCommandAllocator, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateCommandAllocator);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateCommandAllocator) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateCommandAllocator(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.type, args.riid, args.ppCommandAllocator);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCommandAllocator && *args.ppCommandAllocator)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCommandAllocator, *args.ppCommandAllocator,
            npt_object_type_from_iid(args.riid));
    else if (args.ppCommandAllocator)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandAllocator);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateCommandAllocator_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateGraphicsPipelineState
 */

struct npt_command_ID3D12Device_CreateGraphicsPipelineState {
    void *_self;
    const D3D12_GRAPHICS_PIPELINE_STATE_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateGraphicsPipelineState_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D12Device_CreateGraphicsPipelineState *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_GRAPHICS_PIPELINE_STATE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_GRAPHICS_PIPELINE_STATE_DESC(dec, (D3D12_GRAPHICS_PIPELINE_STATE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPipelineState = _gid;
        args->ppPipelineState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPipelineState) return;
        *args->ppPipelineState = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateGraphicsPipelineState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateGraphicsPipelineState *args)
{
    if (args->pDesc)
        npt_replace_D3D12_GRAPHICS_PIPELINE_STATE_DESC_handle(ctx, (D3D12_GRAPHICS_PIPELINE_STATE_DESC *)args->pDesc);
}

static inline void
npt_encode_ID3D12Device_CreateGraphicsPipelineState_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D12Device_CreateGraphicsPipelineState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateGraphicsPipelineState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPipelineState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateGraphicsPipelineState(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateGraphicsPipelineState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateGraphicsPipelineState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateGraphicsPipelineState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateGraphicsPipelineState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateGraphicsPipelineState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateGraphicsPipelineState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateGraphicsPipelineState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateGraphicsPipelineState);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateGraphicsPipelineState) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateGraphicsPipelineState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppPipelineState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPipelineState && *args.ppPipelineState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPipelineState, *args.ppPipelineState,
            npt_object_type_from_iid(args.riid));
    else if (args.ppPipelineState)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateGraphicsPipelineState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateComputePipelineState
 */

struct npt_command_ID3D12Device_CreateComputePipelineState {
    void *_self;
    const D3D12_COMPUTE_PIPELINE_STATE_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateComputePipelineState_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D12Device_CreateComputePipelineState *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_COMPUTE_PIPELINE_STATE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_COMPUTE_PIPELINE_STATE_DESC(dec, (D3D12_COMPUTE_PIPELINE_STATE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPipelineState = _gid;
        args->ppPipelineState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPipelineState) return;
        *args->ppPipelineState = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateComputePipelineState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateComputePipelineState *args)
{
    if (args->pDesc)
        npt_replace_D3D12_COMPUTE_PIPELINE_STATE_DESC_handle(ctx, (D3D12_COMPUTE_PIPELINE_STATE_DESC *)args->pDesc);
}

static inline void
npt_encode_ID3D12Device_CreateComputePipelineState_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D12Device_CreateComputePipelineState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateComputePipelineState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPipelineState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateComputePipelineState(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateComputePipelineState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateComputePipelineState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateComputePipelineState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateComputePipelineState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateComputePipelineState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateComputePipelineState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateComputePipelineState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateComputePipelineState);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateComputePipelineState) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateComputePipelineState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppPipelineState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPipelineState && *args.ppPipelineState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPipelineState, *args.ppPipelineState,
            npt_object_type_from_iid(args.riid));
    else if (args.ppPipelineState)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateComputePipelineState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateCommandList
 */

struct npt_command_ID3D12Device_CreateCommandList {
    void *_self;
    UINT nodeMask;
    D3D12_COMMAND_LIST_TYPE type;
    ID3D12CommandAllocator * pCommandAllocator;
    ID3D12PipelineState * pInitialState;
    const IID * riid;
    void ** ppCommandList;
    /* Shadow: the guest-allocated id for ppCommandList arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandList -> *ppCommandList} in the
     * context object table. */
    uint64_t _guest_id_ppCommandList;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateCommandList_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D12Device_CreateCommandList *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->nodeMask);
    npt_decode_D3D12_COMMAND_LIST_TYPE(dec, &args->type);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCommandAllocator = (ID3D12CommandAllocator *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pInitialState = (ID3D12PipelineState *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCommandList = _gid;
        args->ppCommandList = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCommandList) return;
        *args->ppCommandList = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12Device_CreateCommandList_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateCommandList *args)
{
    args->pCommandAllocator = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCommandAllocator, NPT_OBJECT_TYPE_ID3D12COMMANDALLOCATOR);
    args->pInitialState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pInitialState, NPT_OBJECT_TYPE_ID3D12PIPELINESTATE);
}

static inline void
npt_encode_ID3D12Device_CreateCommandList_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D12Device_CreateCommandList *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandList),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCommandList (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateCommandList(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateCommandList args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateCommandList_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandList on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateCommandList_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandList: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateCommandList _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateCommandList, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateCommandList);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateCommandList) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateCommandList(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.nodeMask, args.type, args.pCommandAllocator, args.pInitialState, args.riid, args.ppCommandList);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCommandList && *args.ppCommandList)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCommandList, *args.ppCommandList,
            npt_object_type_from_iid(args.riid));
    else if (args.ppCommandList)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateCommandList_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CheckFeatureSupport
 */

struct npt_command_ID3D12Device_CheckFeatureSupport {
    void *_self;
    D3D12_FEATURE Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CheckFeatureSupport_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D12Device_CheckFeatureSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_FEATURE(dec, &args->Feature);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pFeatureSupportData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pFeatureSupportData) return;
            npt_decode_blob_array(dec, (void *)args->pFeatureSupportData, _blob_size);
        } else {
            args->pFeatureSupportData = NULL;
        }
    }
    npt_decode_UINT(dec, &args->FeatureSupportDataSize);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CheckFeatureSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CheckFeatureSupport *args)
{
}

static inline void
npt_encode_ID3D12Device_CheckFeatureSupport_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D12Device_CheckFeatureSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CheckFeatureSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pFeatureSupportData) {
        npt_encode_array_count(enc, args->FeatureSupportDataSize);
        npt_encode_blob_array(enc, args->pFeatureSupportData, args->FeatureSupportDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D12Device_CheckFeatureSupport(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D12Device_CheckFeatureSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CheckFeatureSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CheckFeatureSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CheckFeatureSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CheckFeatureSupport: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CheckFeatureSupport _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CheckFeatureSupport, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CheckFeatureSupport);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CheckFeatureSupport) {
        args.ret = ctx->id3d12device_dispatch_overrides->CheckFeatureSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Feature, args.pFeatureSupportData, args.FeatureSupportDataSize);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CheckFeatureSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateDescriptorHeap
 */

struct npt_command_ID3D12Device_CreateDescriptorHeap {
    void *_self;
    const D3D12_DESCRIPTOR_HEAP_DESC * pDescriptorHeapDesc;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateDescriptorHeap_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D12Device_CreateDescriptorHeap *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDescriptorHeapDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_DESCRIPTOR_HEAP_DESC));
        if (!args->pDescriptorHeapDesc) return;
        npt_decode_D3D12_DESCRIPTOR_HEAP_DESC(dec, (D3D12_DESCRIPTOR_HEAP_DESC *)args->pDescriptorHeapDesc);
    } else {
        args->pDescriptorHeapDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvHeap = _gid;
        args->ppvHeap = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvHeap) return;
        *args->ppvHeap = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateDescriptorHeap_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateDescriptorHeap *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateDescriptorHeap_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D12Device_CreateDescriptorHeap *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateDescriptorHeap),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvHeap (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateDescriptorHeap(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateDescriptorHeap args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateDescriptorHeap_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateDescriptorHeap on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateDescriptorHeap_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateDescriptorHeap: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateDescriptorHeap _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateDescriptorHeap, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateDescriptorHeap);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateDescriptorHeap) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateDescriptorHeap(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDescriptorHeapDesc, args.riid, args.ppvHeap);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvHeap && *args.ppvHeap)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvHeap, *args.ppvHeap,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvHeap)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateDescriptorHeap_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::GetDescriptorHandleIncrementSize
 */

struct npt_command_ID3D12Device_GetDescriptorHandleIncrementSize {
    void *_self;
    D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapType;
    UINT ret;
};

static inline void
npt_decode_ID3D12Device_GetDescriptorHandleIncrementSize_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D12Device_GetDescriptorHandleIncrementSize *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_DESCRIPTOR_HEAP_TYPE(dec, &args->DescriptorHeapType);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12Device_GetDescriptorHandleIncrementSize_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetDescriptorHandleIncrementSize *args)
{
}

static inline void
npt_encode_ID3D12Device_GetDescriptorHandleIncrementSize_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D12Device_GetDescriptorHandleIncrementSize *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetDescriptorHandleIncrementSize),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_GetDescriptorHandleIncrementSize(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetDescriptorHandleIncrementSize args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetDescriptorHandleIncrementSize_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetDescriptorHandleIncrementSize on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetDescriptorHandleIncrementSize_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetDescriptorHandleIncrementSize: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetDescriptorHandleIncrementSize _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetDescriptorHandleIncrementSize, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetDescriptorHandleIncrementSize);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetDescriptorHandleIncrementSize) {
        args.ret = ctx->id3d12device_dispatch_overrides->GetDescriptorHandleIncrementSize(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.DescriptorHeapType);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetDescriptorHandleIncrementSize_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateRootSignature
 */

struct npt_command_ID3D12Device_CreateRootSignature {
    void *_self;
    UINT nodeMask;
    const void * pBlobWithRootSignature;
    SIZE_T blobLengthInBytes;
    const IID * riid;
    void ** ppvRootSignature;
    /* Shadow: the guest-allocated id for ppvRootSignature arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvRootSignature -> *ppvRootSignature} in the
     * context object table. */
    uint64_t _guest_id_ppvRootSignature;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateRootSignature_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D12Device_CreateRootSignature *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->nodeMask);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pBlobWithRootSignature = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pBlobWithRootSignature) return;
            npt_decode_blob_array(dec, (void *)args->pBlobWithRootSignature, _blob_size);
        } else {
            args->pBlobWithRootSignature = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->blobLengthInBytes);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvRootSignature = _gid;
        args->ppvRootSignature = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvRootSignature) return;
        *args->ppvRootSignature = NULL;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12Device_CreateRootSignature_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateRootSignature *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateRootSignature_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D12Device_CreateRootSignature *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateRootSignature),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvRootSignature (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateRootSignature(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateRootSignature args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateRootSignature_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateRootSignature on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvRootSignature);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateRootSignature_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateRootSignature: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvRootSignature);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateRootSignature _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateRootSignature, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateRootSignature);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateRootSignature) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateRootSignature(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.nodeMask, args.pBlobWithRootSignature, args.blobLengthInBytes, args.riid, args.ppvRootSignature);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvRootSignature && *args.ppvRootSignature)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvRootSignature, *args.ppvRootSignature,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvRootSignature)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvRootSignature);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateRootSignature_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateConstantBufferView
 */

struct npt_command_ID3D12Device_CreateConstantBufferView {
    void *_self;
    const D3D12_CONSTANT_BUFFER_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device_CreateConstantBufferView_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D12Device_CreateConstantBufferView *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CONSTANT_BUFFER_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_CONSTANT_BUFFER_VIEW_DESC(dec, (D3D12_CONSTANT_BUFFER_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device_CreateConstantBufferView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateConstantBufferView *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateConstantBufferView_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D12Device_CreateConstantBufferView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateConstantBufferView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CreateConstantBufferView(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateConstantBufferView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateConstantBufferView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateConstantBufferView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateConstantBufferView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateConstantBufferView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateConstantBufferView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateConstantBufferView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateConstantBufferView);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateConstantBufferView) {
        ctx->id3d12device_dispatch_overrides->CreateConstantBufferView(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateConstantBufferView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateShaderResourceView
 */

struct npt_command_ID3D12Device_CreateShaderResourceView {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_SHADER_RESOURCE_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device_CreateShaderResourceView_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D12Device_CreateShaderResourceView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_SHADER_RESOURCE_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_SHADER_RESOURCE_VIEW_DESC(dec, (D3D12_SHADER_RESOURCE_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateShaderResourceView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateShaderResourceView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12Device_CreateShaderResourceView_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D12Device_CreateShaderResourceView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateShaderResourceView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CreateShaderResourceView(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateShaderResourceView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateShaderResourceView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateShaderResourceView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateShaderResourceView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateShaderResourceView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateShaderResourceView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateShaderResourceView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateShaderResourceView);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateShaderResourceView) {
        ctx->id3d12device_dispatch_overrides->CreateShaderResourceView(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource, args.pDesc, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateShaderResourceView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateUnorderedAccessView
 */

struct npt_command_ID3D12Device_CreateUnorderedAccessView {
    void *_self;
    ID3D12Resource * pResource;
    ID3D12Resource * pCounterResource;
    const D3D12_UNORDERED_ACCESS_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device_CreateUnorderedAccessView_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12Device_CreateUnorderedAccessView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCounterResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_UNORDERED_ACCESS_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_UNORDERED_ACCESS_VIEW_DESC(dec, (D3D12_UNORDERED_ACCESS_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device_CreateUnorderedAccessView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateUnorderedAccessView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pCounterResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCounterResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12Device_CreateUnorderedAccessView_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12Device_CreateUnorderedAccessView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateUnorderedAccessView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CreateUnorderedAccessView(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateUnorderedAccessView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateUnorderedAccessView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateUnorderedAccessView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateUnorderedAccessView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateUnorderedAccessView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateUnorderedAccessView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateUnorderedAccessView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateUnorderedAccessView);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateUnorderedAccessView) {
        ctx->id3d12device_dispatch_overrides->CreateUnorderedAccessView(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource, args.pCounterResource, args.pDesc, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateUnorderedAccessView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateRenderTargetView
 */

struct npt_command_ID3D12Device_CreateRenderTargetView {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_RENDER_TARGET_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device_CreateRenderTargetView_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device_CreateRenderTargetView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RENDER_TARGET_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RENDER_TARGET_VIEW_DESC(dec, (D3D12_RENDER_TARGET_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateRenderTargetView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateRenderTargetView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12Device_CreateRenderTargetView_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device_CreateRenderTargetView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateRenderTargetView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CreateRenderTargetView(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateRenderTargetView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateRenderTargetView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateRenderTargetView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateRenderTargetView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateRenderTargetView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateRenderTargetView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateRenderTargetView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateRenderTargetView);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateRenderTargetView) {
        ctx->id3d12device_dispatch_overrides->CreateRenderTargetView(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource, args.pDesc, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateRenderTargetView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateDepthStencilView
 */

struct npt_command_ID3D12Device_CreateDepthStencilView {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_DEPTH_STENCIL_VIEW_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device_CreateDepthStencilView_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device_CreateDepthStencilView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_DEPTH_STENCIL_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_DEPTH_STENCIL_VIEW_DESC(dec, (D3D12_DEPTH_STENCIL_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateDepthStencilView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateDepthStencilView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12Device_CreateDepthStencilView_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device_CreateDepthStencilView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateDepthStencilView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CreateDepthStencilView(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateDepthStencilView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateDepthStencilView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateDepthStencilView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateDepthStencilView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateDepthStencilView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateDepthStencilView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateDepthStencilView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateDepthStencilView);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateDepthStencilView) {
        ctx->id3d12device_dispatch_overrides->CreateDepthStencilView(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource, args.pDesc, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateDepthStencilView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateSampler
 */

struct npt_command_ID3D12Device_CreateSampler {
    void *_self;
    const D3D12_SAMPLER_DESC * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device_CreateSampler_args_temp(struct npt_cs_decoder *dec,
                                                struct npt_command_ID3D12Device_CreateSampler *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_SAMPLER_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_SAMPLER_DESC(dec, (D3D12_SAMPLER_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device_CreateSampler_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateSampler *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateSampler_reply(struct npt_cs_encoder *enc,
                                            const struct npt_command_ID3D12Device_CreateSampler *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateSampler),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CreateSampler(struct npt_dispatch_context *ctx,
                                        uint32_t cmd_flags,
                                        uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateSampler args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateSampler_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateSampler on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateSampler_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateSampler: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateSampler _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateSampler, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateSampler);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateSampler) {
        ctx->id3d12device_dispatch_overrides->CreateSampler(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateSampler_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CopyDescriptors
 */

struct npt_command_ID3D12Device_CopyDescriptors {
    void *_self;
    UINT NumDestDescriptorRanges;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pDestDescriptorRangeStarts;
    const UINT * pDestDescriptorRangeSizes;
    UINT NumSrcDescriptorRanges;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pSrcDescriptorRangeStarts;
    const UINT * pSrcDescriptorRangeSizes;
    D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType;
};

static inline void
npt_decode_ID3D12Device_CopyDescriptors_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D12Device_CopyDescriptors *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumDestDescriptorRanges);
    uint64_t _cnt_pDestDescriptorRangeStarts = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pDestDescriptorRangeStarts = npt_decode_array_count_unchecked(dec);
        args->pDestDescriptorRangeStarts = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_CPU_DESCRIPTOR_HANDLE), _cnt_pDestDescriptorRangeStarts);
        if (!args->pDestDescriptorRangeStarts) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pDestDescriptorRangeStarts; _i++)
            npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, (D3D12_CPU_DESCRIPTOR_HANDLE *)&args->pDestDescriptorRangeStarts[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pDestDescriptorRangeStarts = NULL;
    }
    uint64_t _cnt_pDestDescriptorRangeSizes = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pDestDescriptorRangeSizes = npt_decode_array_count_unchecked(dec);
        args->pDestDescriptorRangeSizes = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pDestDescriptorRangeSizes);
        if (!args->pDestDescriptorRangeSizes) return;
        npt_decode_UINT_array(dec, (UINT *)args->pDestDescriptorRangeSizes, _cnt_pDestDescriptorRangeSizes);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pDestDescriptorRangeSizes = NULL;
    }
    npt_decode_UINT(dec, &args->NumSrcDescriptorRanges);
    uint64_t _cnt_pSrcDescriptorRangeStarts = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pSrcDescriptorRangeStarts = npt_decode_array_count_unchecked(dec);
        args->pSrcDescriptorRangeStarts = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_CPU_DESCRIPTOR_HANDLE), _cnt_pSrcDescriptorRangeStarts);
        if (!args->pSrcDescriptorRangeStarts) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pSrcDescriptorRangeStarts; _i++)
            npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, (D3D12_CPU_DESCRIPTOR_HANDLE *)&args->pSrcDescriptorRangeStarts[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pSrcDescriptorRangeStarts = NULL;
    }
    uint64_t _cnt_pSrcDescriptorRangeSizes = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pSrcDescriptorRangeSizes = npt_decode_array_count_unchecked(dec);
        args->pSrcDescriptorRangeSizes = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pSrcDescriptorRangeSizes);
        if (!args->pSrcDescriptorRangeSizes) return;
        npt_decode_UINT_array(dec, (UINT *)args->pSrcDescriptorRangeSizes, _cnt_pSrcDescriptorRangeSizes);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pSrcDescriptorRangeSizes = NULL;
    }
    npt_decode_D3D12_DESCRIPTOR_HEAP_TYPE(dec, &args->DescriptorHeapsType);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pDestDescriptorRangeStarts && _cnt_pDestDescriptorRangeStarts < (uint64_t)(args->NumDestDescriptorRanges)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pDestDescriptorRangeSizes && _cnt_pDestDescriptorRangeSizes < (uint64_t)(args->NumDestDescriptorRanges)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pSrcDescriptorRangeStarts && _cnt_pSrcDescriptorRangeStarts < (uint64_t)(args->NumSrcDescriptorRanges)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pSrcDescriptorRangeSizes && _cnt_pSrcDescriptorRangeSizes < (uint64_t)(args->NumSrcDescriptorRanges)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D12Device_CopyDescriptors_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CopyDescriptors *args)
{
}

static inline void
npt_encode_ID3D12Device_CopyDescriptors_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D12Device_CopyDescriptors *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CopyDescriptors),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CopyDescriptors(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D12Device_CopyDescriptors args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CopyDescriptors_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CopyDescriptors on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CopyDescriptors_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CopyDescriptors: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CopyDescriptors _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CopyDescriptors, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CopyDescriptors);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CopyDescriptors) {
        ctx->id3d12device_dispatch_overrides->CopyDescriptors(ctx, &args, _original);
    } else {
        _original(args._self, args.NumDestDescriptorRanges, args.pDestDescriptorRangeStarts, args.pDestDescriptorRangeSizes, args.NumSrcDescriptorRanges, args.pSrcDescriptorRangeStarts, args.pSrcDescriptorRangeSizes, args.DescriptorHeapsType);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CopyDescriptors_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CopyDescriptorsSimple
 */

struct npt_command_ID3D12Device_CopyDescriptorsSimple {
    void *_self;
    UINT NumDescriptors;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptorRangeStart;
    D3D12_CPU_DESCRIPTOR_HANDLE SrcDescriptorRangeStart;
    D3D12_DESCRIPTOR_HEAP_TYPE DescriptorHeapsType;
};

static inline void
npt_decode_ID3D12Device_CopyDescriptorsSimple_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D12Device_CopyDescriptorsSimple *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumDescriptors);
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptorRangeStart);
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->SrcDescriptorRangeStart);
    npt_decode_D3D12_DESCRIPTOR_HEAP_TYPE(dec, &args->DescriptorHeapsType);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device_CopyDescriptorsSimple_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CopyDescriptorsSimple *args)
{
}

static inline void
npt_encode_ID3D12Device_CopyDescriptorsSimple_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D12Device_CopyDescriptorsSimple *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CopyDescriptorsSimple),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_CopyDescriptorsSimple(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D12Device_CopyDescriptorsSimple args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CopyDescriptorsSimple_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CopyDescriptorsSimple on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CopyDescriptorsSimple_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CopyDescriptorsSimple: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CopyDescriptorsSimple _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CopyDescriptorsSimple, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CopyDescriptorsSimple);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CopyDescriptorsSimple) {
        ctx->id3d12device_dispatch_overrides->CopyDescriptorsSimple(ctx, &args, _original);
    } else {
        _original(args._self, args.NumDescriptors, args.DestDescriptorRangeStart, args.SrcDescriptorRangeStart, args.DescriptorHeapsType);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CopyDescriptorsSimple_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::GetResourceAllocationInfo
 */

struct npt_command_ID3D12Device_GetResourceAllocationInfo {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC * pResourceDescs;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline void
npt_decode_ID3D12Device_GetResourceAllocationInfo_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12Device_GetResourceAllocationInfo *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->visibleMask);
    npt_decode_UINT(dec, &args->numResourceDescs);
    uint64_t _cnt_pResourceDescs = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pResourceDescs = npt_decode_array_count_unchecked(dec);
        args->pResourceDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_DESC), _cnt_pResourceDescs);
        if (!args->pResourceDescs) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pResourceDescs; _i++)
            npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)&args->pResourceDescs[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pResourceDescs = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pResourceDescs && _cnt_pResourceDescs < (uint64_t)(args->numResourceDescs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_GetResourceAllocationInfo_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetResourceAllocationInfo *args)
{
}

static inline void
npt_encode_ID3D12Device_GetResourceAllocationInfo_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12Device_GetResourceAllocationInfo *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetResourceAllocationInfo),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_D3D12_RESOURCE_ALLOCATION_INFO(enc, &args->ret);
}

static inline void
npt_dispatch_ID3D12Device_GetResourceAllocationInfo(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetResourceAllocationInfo args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetResourceAllocationInfo_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetResourceAllocationInfo on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetResourceAllocationInfo_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetResourceAllocationInfo: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetResourceAllocationInfo _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetResourceAllocationInfo, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetResourceAllocationInfo);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetResourceAllocationInfo) {
        args.ret = ctx->id3d12device_dispatch_overrides->GetResourceAllocationInfo(ctx, &args, _original);
    } else {
        /* COM x64 aggregate-return ABI: the hidden pointer (&args.ret)
         * rides in the call args; the returned pointer aliases it. */
        (void)_original(args._self, &args.ret, args.visibleMask, args.numResourceDescs, args.pResourceDescs);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetResourceAllocationInfo_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::GetCustomHeapProperties
 */

struct npt_command_ID3D12Device_GetCustomHeapProperties {
    void *_self;
    UINT nodeMask;
    D3D12_HEAP_TYPE heapType;
    D3D12_HEAP_PROPERTIES ret;
};

static inline void
npt_decode_ID3D12Device_GetCustomHeapProperties_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12Device_GetCustomHeapProperties *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->nodeMask);
    npt_decode_D3D12_HEAP_TYPE(dec, &args->heapType);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device_GetCustomHeapProperties_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetCustomHeapProperties *args)
{
}

static inline void
npt_encode_ID3D12Device_GetCustomHeapProperties_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12Device_GetCustomHeapProperties *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetCustomHeapProperties),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_D3D12_HEAP_PROPERTIES(enc, &args->ret);
}

static inline void
npt_dispatch_ID3D12Device_GetCustomHeapProperties(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetCustomHeapProperties args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetCustomHeapProperties_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetCustomHeapProperties on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetCustomHeapProperties_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetCustomHeapProperties: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetCustomHeapProperties _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetCustomHeapProperties, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetCustomHeapProperties);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetCustomHeapProperties) {
        args.ret = ctx->id3d12device_dispatch_overrides->GetCustomHeapProperties(ctx, &args, _original);
    } else {
        /* COM x64 aggregate-return ABI: the hidden pointer (&args.ret)
         * rides in the call args; the returned pointer aliases it. */
        (void)_original(args._self, &args.ret, args.nodeMask, args.heapType);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetCustomHeapProperties_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateCommittedResource
 */

struct npt_command_ID3D12Device_CreateCommittedResource {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialResourceState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateCommittedResource_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12Device_CreateCommittedResource *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pHeapProperties = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_HEAP_PROPERTIES));
        if (!args->pHeapProperties) return;
        npt_decode_D3D12_HEAP_PROPERTIES(dec, (D3D12_HEAP_PROPERTIES *)args->pHeapProperties);
    } else {
        args->pHeapProperties = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_HEAP_FLAGS(dec, &args->HeapFlags);
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InitialResourceState);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riidResource = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riidResource) return;
        npt_decode_IID(dec, (IID *)args->riidResource);
    } else {
        args->riidResource = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D12Device_CreateCommittedResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateCommittedResource *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateCommittedResource_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12Device_CreateCommittedResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommittedResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateCommittedResource(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateCommittedResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateCommittedResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommittedResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateCommittedResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommittedResource: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateCommittedResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateCommittedResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateCommittedResource);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateCommittedResource) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateCommittedResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHeapProperties, args.HeapFlags, args.pDesc, args.InitialResourceState, args.pOptimizedClearValue, args.riidResource, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riidResource));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateCommittedResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateHeap
 */

struct npt_command_ID3D12Device_CreateHeap {
    void *_self;
    const D3D12_HEAP_DESC * pDesc;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateHeap_args_temp(struct npt_cs_decoder *dec,
                                             struct npt_command_ID3D12Device_CreateHeap *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_HEAP_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_HEAP_DESC(dec, (D3D12_HEAP_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvHeap = _gid;
        args->ppvHeap = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvHeap) return;
        *args->ppvHeap = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateHeap_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateHeap *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateHeap_reply(struct npt_cs_encoder *enc,
                                         const struct npt_command_ID3D12Device_CreateHeap *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateHeap),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvHeap (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateHeap(struct npt_dispatch_context *ctx,
                                     uint32_t cmd_flags,
                                     uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateHeap args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateHeap_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateHeap on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateHeap_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateHeap: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateHeap _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateHeap, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateHeap);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateHeap) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateHeap(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppvHeap);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvHeap && *args.ppvHeap)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvHeap, *args.ppvHeap,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvHeap)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateHeap_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreatePlacedResource
 */

struct npt_command_ID3D12Device_CreatePlacedResource {
    void *_self;
    ID3D12Heap * pHeap;
    UINT64 HeapOffset;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreatePlacedResource_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D12Device_CreatePlacedResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pHeap = (ID3D12Heap *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->HeapOffset);
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InitialState);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D12Device_CreatePlacedResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreatePlacedResource *args)
{
    args->pHeap = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pHeap, NPT_OBJECT_TYPE_ID3D12HEAP);
}

static inline void
npt_encode_ID3D12Device_CreatePlacedResource_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D12Device_CreatePlacedResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreatePlacedResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreatePlacedResource(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreatePlacedResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreatePlacedResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreatePlacedResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreatePlacedResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreatePlacedResource: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreatePlacedResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreatePlacedResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreatePlacedResource);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreatePlacedResource) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreatePlacedResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHeap, args.HeapOffset, args.pDesc, args.InitialState, args.pOptimizedClearValue, args.riid, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreatePlacedResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateReservedResource
 */

struct npt_command_ID3D12Device_CreateReservedResource {
    void *_self;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateReservedResource_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device_CreateReservedResource *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InitialState);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12Device_CreateReservedResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateReservedResource *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateReservedResource_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device_CreateReservedResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateReservedResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateReservedResource(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateReservedResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateReservedResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateReservedResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateReservedResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateReservedResource: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateReservedResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateReservedResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateReservedResource);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateReservedResource) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateReservedResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.InitialState, args.pOptimizedClearValue, args.riid, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateReservedResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateSharedHandle
 */

struct npt_command_ID3D12Device_CreateSharedHandle {
    void *_self;
    ID3D12DeviceChild * pObject;
    const SECURITY_ATTRIBUTES * pAttributes;
    DWORD Access;
    const WCHAR * Name;
    HANDLE * pHandle;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateSharedHandle_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D12Device_CreateSharedHandle *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pObject = (ID3D12DeviceChild *)npt_object_from_id(_id);
    }
    /* pAttributes: non-serializable, skipped */
    npt_decode_DWORD(dec, &args->Access);
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->Name = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->Name) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->Name, _wstr_size);
        } else {
            args->Name = NULL;
        }
    }

    /* Allocate temp storage for output-only parameters */




    args->pHandle = npt_cs_decoder_alloc_temp(dec, sizeof(HANDLE));
    if (!args->pHandle) return;
    memset(args->pHandle, 0, sizeof(HANDLE));
}

static inline void
npt_replace_ID3D12Device_CreateSharedHandle_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateSharedHandle *args)
{
    args->pObject = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pObject, NPT_OBJECT_TYPE_ID3D12DEVICECHILD);
}

static inline void
npt_encode_ID3D12Device_CreateSharedHandle_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D12Device_CreateSharedHandle *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateSharedHandle),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)*args->pHandle));
}

static inline void
npt_dispatch_ID3D12Device_CreateSharedHandle(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateSharedHandle args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateSharedHandle_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateSharedHandle on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateSharedHandle_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateSharedHandle: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateSharedHandle _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateSharedHandle, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateSharedHandle);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateSharedHandle) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateSharedHandle(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pObject, args.pAttributes, args.Access, args.Name, args.pHandle);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateSharedHandle_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::OpenSharedHandle
 */

struct npt_command_ID3D12Device_OpenSharedHandle {
    void *_self;
    HANDLE NTHandle;
    const IID * riid;
    void ** ppvObj;
    /* Shadow: the guest-allocated id for ppvObj arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvObj -> *ppvObj} in the
     * context object table. */
    uint64_t _guest_id_ppvObj;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_OpenSharedHandle_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D12Device_OpenSharedHandle *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->NTHandle = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvObj = _gid;
        args->ppvObj = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvObj) return;
        *args->ppvObj = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_OpenSharedHandle_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_OpenSharedHandle *args)
{
    args->NTHandle = (HANDLE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->NTHandle);
}

static inline void
npt_encode_ID3D12Device_OpenSharedHandle_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D12Device_OpenSharedHandle *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_OpenSharedHandle),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvObj (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_OpenSharedHandle(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D12Device_OpenSharedHandle args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_OpenSharedHandle_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_OpenSharedHandle on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvObj);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_OpenSharedHandle_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_OpenSharedHandle: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvObj);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_OpenSharedHandle _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_OpenSharedHandle, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_OpenSharedHandle);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->OpenSharedHandle) {
        args.ret = ctx->id3d12device_dispatch_overrides->OpenSharedHandle(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NTHandle, args.riid, args.ppvObj);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvObj && *args.ppvObj)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvObj, *args.ppvObj,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvObj)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvObj);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_OpenSharedHandle_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::OpenSharedHandleByName
 */

struct npt_command_ID3D12Device_OpenSharedHandleByName {
    void *_self;
    const WCHAR * Name;
    DWORD Access;
    HANDLE * pNTHandle;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_OpenSharedHandleByName_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device_OpenSharedHandleByName *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->Name = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->Name) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->Name, _wstr_size);
        } else {
            args->Name = NULL;
        }
    }
    npt_decode_DWORD(dec, &args->Access);

    /* Allocate temp storage for output-only parameters */


    args->pNTHandle = npt_cs_decoder_alloc_temp(dec, sizeof(HANDLE));
    if (!args->pNTHandle) return;
    memset(args->pNTHandle, 0, sizeof(HANDLE));
}

static inline void
npt_replace_ID3D12Device_OpenSharedHandleByName_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_OpenSharedHandleByName *args)
{
}

static inline void
npt_encode_ID3D12Device_OpenSharedHandleByName_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device_OpenSharedHandleByName *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_OpenSharedHandleByName),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)*args->pNTHandle));
}

static inline void
npt_dispatch_ID3D12Device_OpenSharedHandleByName(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device_OpenSharedHandleByName args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_OpenSharedHandleByName_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_OpenSharedHandleByName on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_OpenSharedHandleByName_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_OpenSharedHandleByName: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_OpenSharedHandleByName _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_OpenSharedHandleByName, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_OpenSharedHandleByName);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->OpenSharedHandleByName) {
        args.ret = ctx->id3d12device_dispatch_overrides->OpenSharedHandleByName(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Name, args.Access, args.pNTHandle);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_OpenSharedHandleByName_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::MakeResident
 */

struct npt_command_ID3D12Device_MakeResident {
    void *_self;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_MakeResident_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D12Device_MakeResident *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumObjects);
    uint64_t _cnt_ppObjects = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppObjects) {
        args->ppObjects = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppObjects);
        if (!args->ppObjects) return;
        for (uint64_t _i = 0; _i < _cnt_ppObjects; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppObjects[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppObjects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppObjects && _cnt_ppObjects < (uint64_t)(args->NumObjects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device_MakeResident_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_MakeResident *args)
{
    if (args->ppObjects) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumObjects); _i++)
            args->ppObjects[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppObjects[_i], NPT_OBJECT_TYPE_ID3D12PAGEABLE);
    }
}

static inline void
npt_encode_ID3D12Device_MakeResident_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D12Device_MakeResident *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_MakeResident),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_MakeResident(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D12Device_MakeResident args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_MakeResident_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_MakeResident on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_MakeResident_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_MakeResident: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_MakeResident _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_MakeResident, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_MakeResident);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->MakeResident) {
        args.ret = ctx->id3d12device_dispatch_overrides->MakeResident(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumObjects, args.ppObjects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_MakeResident_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::Evict
 */

struct npt_command_ID3D12Device_Evict {
    void *_self;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_Evict_args_temp(struct npt_cs_decoder *dec,
                                        struct npt_command_ID3D12Device_Evict *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumObjects);
    uint64_t _cnt_ppObjects = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppObjects) {
        args->ppObjects = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppObjects);
        if (!args->ppObjects) return;
        for (uint64_t _i = 0; _i < _cnt_ppObjects; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppObjects[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppObjects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppObjects && _cnt_ppObjects < (uint64_t)(args->NumObjects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device_Evict_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_Evict *args)
{
    if (args->ppObjects) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumObjects); _i++)
            args->ppObjects[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppObjects[_i], NPT_OBJECT_TYPE_ID3D12PAGEABLE);
    }
}

static inline void
npt_encode_ID3D12Device_Evict_reply(struct npt_cs_encoder *enc,
                                    const struct npt_command_ID3D12Device_Evict *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_Evict),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_Evict(struct npt_dispatch_context *ctx,
                                uint32_t cmd_flags,
                                uint64_t object_id)
{
    struct npt_command_ID3D12Device_Evict args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_Evict_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_Evict on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_Evict_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_Evict: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_Evict _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_Evict, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_Evict);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->Evict) {
        args.ret = ctx->id3d12device_dispatch_overrides->Evict(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumObjects, args.ppObjects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_Evict_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateFence
 */

struct npt_command_ID3D12Device_CreateFence {
    void *_self;
    UINT64 InitialValue;
    D3D12_FENCE_FLAGS Flags;
    const IID * riid;
    void ** ppFence;
    /* Shadow: the guest-allocated id for ppFence arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppFence -> *ppFence} in the
     * context object table. */
    uint64_t _guest_id_ppFence;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateFence_args_temp(struct npt_cs_decoder *dec,
                                              struct npt_command_ID3D12Device_CreateFence *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT64(dec, &args->InitialValue);
    npt_decode_D3D12_FENCE_FLAGS(dec, &args->Flags);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppFence = _gid;
        args->ppFence = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppFence) return;
        *args->ppFence = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device_CreateFence_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateFence *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateFence_reply(struct npt_cs_encoder *enc,
                                          const struct npt_command_ID3D12Device_CreateFence *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateFence),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppFence (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateFence(struct npt_dispatch_context *ctx,
                                      uint32_t cmd_flags,
                                      uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateFence args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateFence_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateFence on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFence);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateFence_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateFence: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFence);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateFence _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateFence, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateFence);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateFence) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateFence(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.InitialValue, args.Flags, args.riid, args.ppFence);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppFence && *args.ppFence)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppFence, *args.ppFence,
            npt_object_type_from_iid(args.riid));
    else if (args.ppFence)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFence);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateFence_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::GetDeviceRemovedReason
 */

struct npt_command_ID3D12Device_GetDeviceRemovedReason {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_GetDeviceRemovedReason_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device_GetDeviceRemovedReason *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12Device_GetDeviceRemovedReason_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetDeviceRemovedReason *args)
{
}

static inline void
npt_encode_ID3D12Device_GetDeviceRemovedReason_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device_GetDeviceRemovedReason *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetDeviceRemovedReason),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_GetDeviceRemovedReason(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetDeviceRemovedReason args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetDeviceRemovedReason_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetDeviceRemovedReason on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetDeviceRemovedReason_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetDeviceRemovedReason: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetDeviceRemovedReason _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetDeviceRemovedReason, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetDeviceRemovedReason);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetDeviceRemovedReason) {
        args.ret = ctx->id3d12device_dispatch_overrides->GetDeviceRemovedReason(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetDeviceRemovedReason_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::GetCopyableFootprints
 */

struct npt_command_ID3D12Device_GetCopyableFootprints {
    void *_self;
    const D3D12_RESOURCE_DESC * pResourceDesc;
    UINT FirstSubresource;
    UINT NumSubresources;
    UINT64 BaseOffset;
    D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts;
    UINT * pNumRows;
    UINT64 * pRowSizeInBytes;
    UINT64 * pTotalBytes;
};

static inline void
npt_decode_ID3D12Device_GetCopyableFootprints_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D12Device_GetCopyableFootprints *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pResourceDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC));
        if (!args->pResourceDesc) return;
        npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)args->pResourceDesc);
    } else {
        args->pResourceDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->FirstSubresource);
    npt_decode_UINT(dec, &args->NumSubresources);
    npt_decode_UINT64(dec, &args->BaseOffset);




    /* Allocate temp storage for output-only parameters */




    if (args->NumSubresources) {
        args->pLayouts = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_PLACED_SUBRESOURCE_FOOTPRINT), args->NumSubresources);
        if (!args->pLayouts) return;
        memset(args->pLayouts, 0, sizeof(D3D12_PLACED_SUBRESOURCE_FOOTPRINT) * args->NumSubresources);
    } else {
        args->pLayouts = NULL;
    }
    if (args->NumSubresources) {
        args->pNumRows = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumSubresources);
        if (!args->pNumRows) return;
        memset(args->pNumRows, 0, sizeof(UINT) * args->NumSubresources);
    } else {
        args->pNumRows = NULL;
    }
    if (args->NumSubresources) {
        args->pRowSizeInBytes = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT64), args->NumSubresources);
        if (!args->pRowSizeInBytes) return;
        memset(args->pRowSizeInBytes, 0, sizeof(UINT64) * args->NumSubresources);
    } else {
        args->pRowSizeInBytes = NULL;
    }
    args->pTotalBytes = npt_cs_decoder_alloc_temp(dec, sizeof(UINT64));
    if (!args->pTotalBytes) return;
    memset(args->pTotalBytes, 0, sizeof(UINT64));
}

static inline void
npt_replace_ID3D12Device_GetCopyableFootprints_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetCopyableFootprints *args)
{
}

static inline void
npt_encode_ID3D12Device_GetCopyableFootprints_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D12Device_GetCopyableFootprints *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetCopyableFootprints),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pLayouts) {
        npt_encode_array_count(enc, args->NumSubresources);
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSubresources); _i++)
            npt_encode_D3D12_PLACED_SUBRESOURCE_FOOTPRINT(enc, &args->pLayouts[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumRows) {
        npt_encode_array_count(enc, args->NumSubresources);
        npt_encode_UINT_array(enc, args->pNumRows, args->NumSubresources);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pRowSizeInBytes) {
        npt_encode_array_count(enc, args->NumSubresources);
        npt_encode_UINT64_array(enc, args->pRowSizeInBytes, args->NumSubresources);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pTotalBytes))
        npt_encode_UINT64(enc, args->pTotalBytes);
}

static inline void
npt_dispatch_ID3D12Device_GetCopyableFootprints(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetCopyableFootprints args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetCopyableFootprints_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetCopyableFootprints on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetCopyableFootprints_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetCopyableFootprints: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetCopyableFootprints _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetCopyableFootprints, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetCopyableFootprints);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetCopyableFootprints) {
        ctx->id3d12device_dispatch_overrides->GetCopyableFootprints(ctx, &args, _original);
    } else {
        _original(args._self, args.pResourceDesc, args.FirstSubresource, args.NumSubresources, args.BaseOffset, args.pLayouts, args.pNumRows, args.pRowSizeInBytes, args.pTotalBytes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetCopyableFootprints_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateQueryHeap
 */

struct npt_command_ID3D12Device_CreateQueryHeap {
    void *_self;
    const D3D12_QUERY_HEAP_DESC * pDesc;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateQueryHeap_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D12Device_CreateQueryHeap *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_QUERY_HEAP_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_QUERY_HEAP_DESC(dec, (D3D12_QUERY_HEAP_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvHeap = _gid;
        args->ppvHeap = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvHeap) return;
        *args->ppvHeap = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device_CreateQueryHeap_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateQueryHeap *args)
{
}

static inline void
npt_encode_ID3D12Device_CreateQueryHeap_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D12Device_CreateQueryHeap *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateQueryHeap),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvHeap (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateQueryHeap(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateQueryHeap args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateQueryHeap_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateQueryHeap on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateQueryHeap_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateQueryHeap: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateQueryHeap _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateQueryHeap, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateQueryHeap);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateQueryHeap) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateQueryHeap(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppvHeap);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvHeap && *args.ppvHeap)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvHeap, *args.ppvHeap,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvHeap)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateQueryHeap_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::SetStablePowerState
 */

struct npt_command_ID3D12Device_SetStablePowerState {
    void *_self;
    BOOL Enable;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_SetStablePowerState_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D12Device_SetStablePowerState *args)
{
    /* Decode input parameters from the wire */
    npt_decode_BOOL(dec, &args->Enable);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12Device_SetStablePowerState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_SetStablePowerState *args)
{
}

static inline void
npt_encode_ID3D12Device_SetStablePowerState_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D12Device_SetStablePowerState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_SetStablePowerState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device_SetStablePowerState(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D12Device_SetStablePowerState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_SetStablePowerState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_SetStablePowerState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_SetStablePowerState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_SetStablePowerState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_SetStablePowerState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_SetStablePowerState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_SetStablePowerState);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->SetStablePowerState) {
        args.ret = ctx->id3d12device_dispatch_overrides->SetStablePowerState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Enable);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_SetStablePowerState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::CreateCommandSignature
 */

struct npt_command_ID3D12Device_CreateCommandSignature {
    void *_self;
    const D3D12_COMMAND_SIGNATURE_DESC * pDesc;
    ID3D12RootSignature * pRootSignature;
    const IID * riid;
    void ** ppvCommandSignature;
    /* Shadow: the guest-allocated id for ppvCommandSignature arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvCommandSignature -> *ppvCommandSignature} in the
     * context object table. */
    uint64_t _guest_id_ppvCommandSignature;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device_CreateCommandSignature_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device_CreateCommandSignature *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_COMMAND_SIGNATURE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_COMMAND_SIGNATURE_DESC(dec, (D3D12_COMMAND_SIGNATURE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRootSignature = (ID3D12RootSignature *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvCommandSignature = _gid;
        args->ppvCommandSignature = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvCommandSignature) return;
        *args->ppvCommandSignature = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device_CreateCommandSignature_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_CreateCommandSignature *args)
{
    args->pRootSignature = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRootSignature, NPT_OBJECT_TYPE_ID3D12ROOTSIGNATURE);
}

static inline void
npt_encode_ID3D12Device_CreateCommandSignature_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device_CreateCommandSignature *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_CreateCommandSignature),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvCommandSignature (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device_CreateCommandSignature(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device_CreateCommandSignature args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_CreateCommandSignature_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandSignature on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvCommandSignature);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_CreateCommandSignature_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_CreateCommandSignature: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvCommandSignature);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_CreateCommandSignature _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_CreateCommandSignature, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_CreateCommandSignature);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->CreateCommandSignature) {
        args.ret = ctx->id3d12device_dispatch_overrides->CreateCommandSignature(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pRootSignature, args.riid, args.ppvCommandSignature);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvCommandSignature && *args.ppvCommandSignature)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvCommandSignature, *args.ppvCommandSignature,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvCommandSignature)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvCommandSignature);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_CreateCommandSignature_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::GetResourceTiling
 */

struct npt_command_ID3D12Device_GetResourceTiling {
    void *_self;
    ID3D12Resource * pTiledResource;
    UINT * pNumTilesForEntireResource;
    D3D12_PACKED_MIP_INFO * pPackedMipDesc;
    D3D12_TILE_SHAPE * pStandardTileShapeForNonPackedMips;
    UINT * pNumSubresourceTilings;
    UINT FirstSubresourceTilingToGet;
    D3D12_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips;
};

static inline void
npt_decode_ID3D12Device_GetResourceTiling_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D12Device_GetResourceTiling *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTiledResource = (ID3D12Resource *)npt_object_from_id(_id);
    }



    if (npt_decode_simple_pointer(dec)) {
        args->pNumSubresourceTilings = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumSubresourceTilings) return;
        npt_decode_UINT(dec, (UINT *)args->pNumSubresourceTilings);
    } else {
        args->pNumSubresourceTilings = NULL;
    }
    npt_decode_UINT(dec, &args->FirstSubresourceTilingToGet);

    /* Allocate temp storage for output-only parameters */

    args->pNumTilesForEntireResource = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pNumTilesForEntireResource) return;
    memset(args->pNumTilesForEntireResource, 0, sizeof(UINT));
    args->pPackedMipDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_PACKED_MIP_INFO));
    if (!args->pPackedMipDesc) return;
    memset(args->pPackedMipDesc, 0, sizeof(D3D12_PACKED_MIP_INFO));
    args->pStandardTileShapeForNonPackedMips = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_TILE_SHAPE));
    if (!args->pStandardTileShapeForNonPackedMips) return;
    memset(args->pStandardTileShapeForNonPackedMips, 0, sizeof(D3D12_TILE_SHAPE));


    if ((args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0)) {
        args->pSubresourceTilingsForNonPackedMips = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_SUBRESOURCE_TILING), (args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0));
        if (!args->pSubresourceTilingsForNonPackedMips) return;
        memset(args->pSubresourceTilingsForNonPackedMips, 0, sizeof(D3D12_SUBRESOURCE_TILING) * (args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0));
    } else {
        args->pSubresourceTilingsForNonPackedMips = NULL;
    }
}

static inline void
npt_replace_ID3D12Device_GetResourceTiling_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetResourceTiling *args)
{
    args->pTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTiledResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12Device_GetResourceTiling_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D12Device_GetResourceTiling *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetResourceTiling),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumTilesForEntireResource))
        npt_encode_UINT(enc, args->pNumTilesForEntireResource);
    if (npt_encode_simple_pointer(enc, args->pPackedMipDesc))
        npt_encode_D3D12_PACKED_MIP_INFO(enc, args->pPackedMipDesc);
    if (npt_encode_simple_pointer(enc, args->pStandardTileShapeForNonPackedMips))
        npt_encode_D3D12_TILE_SHAPE(enc, args->pStandardTileShapeForNonPackedMips);
    if (npt_encode_simple_pointer(enc, args->pNumSubresourceTilings))
        npt_encode_UINT(enc, args->pNumSubresourceTilings);
    if (args->pSubresourceTilingsForNonPackedMips) {
        npt_encode_array_count(enc, (args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0));
        for (uint32_t _i = 0; _i < (uint32_t)((args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0)); _i++)
            npt_encode_D3D12_SUBRESOURCE_TILING(enc, &args->pSubresourceTilingsForNonPackedMips[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D12Device_GetResourceTiling(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetResourceTiling args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetResourceTiling_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetResourceTiling on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetResourceTiling_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetResourceTiling: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetResourceTiling _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetResourceTiling, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetResourceTiling);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetResourceTiling) {
        ctx->id3d12device_dispatch_overrides->GetResourceTiling(ctx, &args, _original);
    } else {
        _original(args._self, args.pTiledResource, args.pNumTilesForEntireResource, args.pPackedMipDesc, args.pStandardTileShapeForNonPackedMips, args.pNumSubresourceTilings, args.FirstSubresourceTilingToGet, args.pSubresourceTilingsForNonPackedMips);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetResourceTiling_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device::GetAdapterLuid
 */

struct npt_command_ID3D12Device_GetAdapterLuid {
    void *_self;
    LUID ret;
};

static inline void
npt_decode_ID3D12Device_GetAdapterLuid_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D12Device_GetAdapterLuid *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12Device_GetAdapterLuid_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device_GetAdapterLuid *args)
{
}

static inline void
npt_encode_ID3D12Device_GetAdapterLuid_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D12Device_GetAdapterLuid *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device,
                                 NPT_METHOD_ID3D12Device_GetAdapterLuid),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_LUID(enc, &args->ret);
}

static inline void
npt_dispatch_ID3D12Device_GetAdapterLuid(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D12Device_GetAdapterLuid args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device_GetAdapterLuid_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetAdapterLuid on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device_GetAdapterLuid_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device_GetAdapterLuid: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device_GetAdapterLuid _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device_GetAdapterLuid, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device_GetAdapterLuid);

    if (ctx->id3d12device_dispatch_overrides && ctx->id3d12device_dispatch_overrides->GetAdapterLuid) {
        args.ret = ctx->id3d12device_dispatch_overrides->GetAdapterLuid(ctx, &args, _original);
    } else {
        /* COM x64 aggregate-return ABI: the hidden pointer (&args.ret)
         * rides in the call args; the returned pointer aliases it. */
        (void)_original(args._self, &args.ret);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device_GetAdapterLuid_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device1::CreatePipelineLibrary
 */

struct npt_command_ID3D12Device1_CreatePipelineLibrary {
    void *_self;
    const void * pLibraryBlob;
    SIZE_T BlobLength;
    const IID * riid;
    void ** ppPipelineLibrary;
    /* Shadow: the guest-allocated id for ppPipelineLibrary arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineLibrary -> *ppPipelineLibrary} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineLibrary;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device1_CreatePipelineLibrary_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device1_CreatePipelineLibrary *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pLibraryBlob = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pLibraryBlob) return;
            npt_decode_blob_array(dec, (void *)args->pLibraryBlob, _blob_size);
        } else {
            args->pLibraryBlob = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BlobLength);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPipelineLibrary = _gid;
        args->ppPipelineLibrary = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPipelineLibrary) return;
        *args->ppPipelineLibrary = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device1_CreatePipelineLibrary_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device1_CreatePipelineLibrary *args)
{
}

static inline void
npt_encode_ID3D12Device1_CreatePipelineLibrary_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device1_CreatePipelineLibrary *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                 NPT_METHOD_ID3D12Device1_CreatePipelineLibrary),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPipelineLibrary (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device1_CreatePipelineLibrary(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device1_CreatePipelineLibrary args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device1_CreatePipelineLibrary_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device1_CreatePipelineLibrary on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineLibrary);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device1_CreatePipelineLibrary_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device1_CreatePipelineLibrary: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineLibrary);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device1_CreatePipelineLibrary _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device1_CreatePipelineLibrary, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device1_CreatePipelineLibrary);

    if (ctx->id3d12device1_dispatch_overrides && ctx->id3d12device1_dispatch_overrides->CreatePipelineLibrary) {
        args.ret = ctx->id3d12device1_dispatch_overrides->CreatePipelineLibrary(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pLibraryBlob, args.BlobLength, args.riid, args.ppPipelineLibrary);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPipelineLibrary && *args.ppPipelineLibrary)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPipelineLibrary, *args.ppPipelineLibrary,
            npt_object_type_from_iid(args.riid));
    else if (args.ppPipelineLibrary)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineLibrary);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device1_CreatePipelineLibrary_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device1::SetEventOnMultipleFenceCompletion
 */

struct npt_command_ID3D12Device1_SetEventOnMultipleFenceCompletion {
    void *_self;
    ID3D12Fence ** ppFences;
    const UINT64 * pFenceValues;
    UINT NumFences;
    D3D12_MULTIPLE_FENCE_WAIT_FLAGS Flags;
    HANDLE hEvent;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device1_SetEventOnMultipleFenceCompletion_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D12Device1_SetEventOnMultipleFenceCompletion *args)
{
    /* Decode input parameters from the wire */
    uint64_t _cnt_ppFences = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppFences) {
        args->ppFences = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppFences);
        if (!args->ppFences) return;
        for (uint64_t _i = 0; _i < _cnt_ppFences; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppFences[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppFences = NULL;
    }
    uint64_t _cnt_pFenceValues = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFenceValues = npt_decode_array_count_unchecked(dec);
        args->pFenceValues = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT64), _cnt_pFenceValues);
        if (!args->pFenceValues) return;
        npt_decode_UINT64_array(dec, (UINT64 *)args->pFenceValues, _cnt_pFenceValues);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFenceValues = NULL;
    }
    npt_decode_UINT(dec, &args->NumFences);
    npt_decode_D3D12_MULTIPLE_FENCE_WAIT_FLAGS(dec, &args->Flags);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppFences && _cnt_ppFences < (uint64_t)(args->NumFences)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pFenceValues && _cnt_pFenceValues < (uint64_t)(args->NumFences)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12Device1_SetEventOnMultipleFenceCompletion_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device1_SetEventOnMultipleFenceCompletion *args)
{
    if (args->ppFences) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumFences); _i++)
            args->ppFences[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppFences[_i], NPT_OBJECT_TYPE_ID3D12FENCE);
    }
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_ID3D12Device1_SetEventOnMultipleFenceCompletion_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D12Device1_SetEventOnMultipleFenceCompletion *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                 NPT_METHOD_ID3D12Device1_SetEventOnMultipleFenceCompletion),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device1_SetEventOnMultipleFenceCompletion(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D12Device1_SetEventOnMultipleFenceCompletion args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device1_SetEventOnMultipleFenceCompletion_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device1_SetEventOnMultipleFenceCompletion on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device1_SetEventOnMultipleFenceCompletion_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device1_SetEventOnMultipleFenceCompletion: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device1_SetEventOnMultipleFenceCompletion _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device1_SetEventOnMultipleFenceCompletion, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device1_SetEventOnMultipleFenceCompletion);

    if (ctx->id3d12device1_dispatch_overrides && ctx->id3d12device1_dispatch_overrides->SetEventOnMultipleFenceCompletion) {
        args.ret = ctx->id3d12device1_dispatch_overrides->SetEventOnMultipleFenceCompletion(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ppFences, args.pFenceValues, args.NumFences, args.Flags, args.hEvent);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device1_SetEventOnMultipleFenceCompletion_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device1::SetResidencyPriority
 */

struct npt_command_ID3D12Device1_SetResidencyPriority {
    void *_self;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    const D3D12_RESIDENCY_PRIORITY * pPriorities;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device1_SetResidencyPriority_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D12Device1_SetResidencyPriority *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumObjects);
    uint64_t _cnt_ppObjects = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppObjects) {
        args->ppObjects = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppObjects);
        if (!args->ppObjects) return;
        for (uint64_t _i = 0; _i < _cnt_ppObjects; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppObjects[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppObjects = NULL;
    }
    uint64_t _cnt_pPriorities = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pPriorities = npt_decode_array_count_unchecked(dec);
        args->pPriorities = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESIDENCY_PRIORITY), _cnt_pPriorities);
        if (!args->pPriorities) return;
        npt_decode_D3D12_RESIDENCY_PRIORITY_array(dec, (D3D12_RESIDENCY_PRIORITY *)args->pPriorities, _cnt_pPriorities);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pPriorities = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppObjects && _cnt_ppObjects < (uint64_t)(args->NumObjects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pPriorities && _cnt_pPriorities < (uint64_t)(args->NumObjects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device1_SetResidencyPriority_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device1_SetResidencyPriority *args)
{
    if (args->ppObjects) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumObjects); _i++)
            args->ppObjects[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppObjects[_i], NPT_OBJECT_TYPE_ID3D12PAGEABLE);
    }
}

static inline void
npt_encode_ID3D12Device1_SetResidencyPriority_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D12Device1_SetResidencyPriority *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device1,
                                 NPT_METHOD_ID3D12Device1_SetResidencyPriority),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device1_SetResidencyPriority(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D12Device1_SetResidencyPriority args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device1_SetResidencyPriority_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device1_SetResidencyPriority on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device1_SetResidencyPriority_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device1_SetResidencyPriority: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device1_SetResidencyPriority _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device1_SetResidencyPriority, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device1_SetResidencyPriority);

    if (ctx->id3d12device1_dispatch_overrides && ctx->id3d12device1_dispatch_overrides->SetResidencyPriority) {
        args.ret = ctx->id3d12device1_dispatch_overrides->SetResidencyPriority(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumObjects, args.ppObjects, args.pPriorities);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device1_SetResidencyPriority_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device2::CreatePipelineState
 */

struct npt_command_ID3D12Device2_CreatePipelineState {
    void *_self;
    const D3D12_PIPELINE_STATE_STREAM_DESC * pDesc;
    const IID * riid;
    void ** ppPipelineState;
    /* Shadow: the guest-allocated id for ppPipelineState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPipelineState -> *ppPipelineState} in the
     * context object table. */
    uint64_t _guest_id_ppPipelineState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device2_CreatePipelineState_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D12Device2_CreatePipelineState *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_PIPELINE_STATE_STREAM_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_PIPELINE_STATE_STREAM_DESC(dec, (D3D12_PIPELINE_STATE_STREAM_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPipelineState = _gid;
        args->ppPipelineState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPipelineState) return;
        *args->ppPipelineState = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device2_CreatePipelineState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device2_CreatePipelineState *args)
{
}

static inline void
npt_encode_ID3D12Device2_CreatePipelineState_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D12Device2_CreatePipelineState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device2,
                                 NPT_METHOD_ID3D12Device2_CreatePipelineState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPipelineState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device2_CreatePipelineState(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D12Device2_CreatePipelineState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device2_CreatePipelineState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device2_CreatePipelineState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device2_CreatePipelineState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device2_CreatePipelineState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device2_CreatePipelineState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device2_CreatePipelineState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device2_CreatePipelineState);

    if (ctx->id3d12device2_dispatch_overrides && ctx->id3d12device2_dispatch_overrides->CreatePipelineState) {
        args.ret = ctx->id3d12device2_dispatch_overrides->CreatePipelineState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppPipelineState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPipelineState && *args.ppPipelineState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPipelineState, *args.ppPipelineState,
            npt_object_type_from_iid(args.riid));
    else if (args.ppPipelineState)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPipelineState);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device2_CreatePipelineState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device3::OpenExistingHeapFromAddress
 */

struct npt_command_ID3D12Device3_OpenExistingHeapFromAddress {
    void *_self;
    const void * pAddress;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device3_OpenExistingHeapFromAddress_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12Device3_OpenExistingHeapFromAddress *args)
{
    /* Decode input parameters from the wire */
    /* ERROR: pAddress (void) is not serializable */
    return;
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvHeap = _gid;
        args->ppvHeap = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvHeap) return;
        *args->ppvHeap = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device3_OpenExistingHeapFromAddress_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device3_OpenExistingHeapFromAddress *args)
{
}

static inline void
npt_encode_ID3D12Device3_OpenExistingHeapFromAddress_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12Device3_OpenExistingHeapFromAddress *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                 NPT_METHOD_ID3D12Device3_OpenExistingHeapFromAddress),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvHeap (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device3_OpenExistingHeapFromAddress(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12Device3_OpenExistingHeapFromAddress args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device3_OpenExistingHeapFromAddress_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device3_OpenExistingHeapFromAddress on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device3_OpenExistingHeapFromAddress_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device3_OpenExistingHeapFromAddress: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device3_OpenExistingHeapFromAddress _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device3_OpenExistingHeapFromAddress, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device3_OpenExistingHeapFromAddress);

    if (ctx->id3d12device3_dispatch_overrides && ctx->id3d12device3_dispatch_overrides->OpenExistingHeapFromAddress) {
        args.ret = ctx->id3d12device3_dispatch_overrides->OpenExistingHeapFromAddress(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pAddress, args.riid, args.ppvHeap);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvHeap && *args.ppvHeap)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvHeap, *args.ppvHeap,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvHeap)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device3_OpenExistingHeapFromAddress_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device3::OpenExistingHeapFromFileMapping
 */

struct npt_command_ID3D12Device3_OpenExistingHeapFromFileMapping {
    void *_self;
    HANDLE hFileMapping;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device3_OpenExistingHeapFromFileMapping_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D12Device3_OpenExistingHeapFromFileMapping *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hFileMapping = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvHeap = _gid;
        args->ppvHeap = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvHeap) return;
        *args->ppvHeap = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device3_OpenExistingHeapFromFileMapping_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device3_OpenExistingHeapFromFileMapping *args)
{
    args->hFileMapping = (HANDLE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->hFileMapping);
}

static inline void
npt_encode_ID3D12Device3_OpenExistingHeapFromFileMapping_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D12Device3_OpenExistingHeapFromFileMapping *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                 NPT_METHOD_ID3D12Device3_OpenExistingHeapFromFileMapping),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvHeap (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device3_OpenExistingHeapFromFileMapping(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D12Device3_OpenExistingHeapFromFileMapping args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device3_OpenExistingHeapFromFileMapping_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device3_OpenExistingHeapFromFileMapping on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device3_OpenExistingHeapFromFileMapping_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device3_OpenExistingHeapFromFileMapping: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device3_OpenExistingHeapFromFileMapping _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device3_OpenExistingHeapFromFileMapping, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device3_OpenExistingHeapFromFileMapping);

    if (ctx->id3d12device3_dispatch_overrides && ctx->id3d12device3_dispatch_overrides->OpenExistingHeapFromFileMapping) {
        args.ret = ctx->id3d12device3_dispatch_overrides->OpenExistingHeapFromFileMapping(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hFileMapping, args.riid, args.ppvHeap);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvHeap && *args.ppvHeap)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvHeap, *args.ppvHeap,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvHeap)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device3_OpenExistingHeapFromFileMapping_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device3::EnqueueMakeResident
 */

struct npt_command_ID3D12Device3_EnqueueMakeResident {
    void *_self;
    D3D12_RESIDENCY_FLAGS Flags;
    UINT NumObjects;
    ID3D12Pageable ** ppObjects;
    ID3D12Fence * pFenceToSignal;
    UINT64 FenceValueToSignal;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device3_EnqueueMakeResident_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D12Device3_EnqueueMakeResident *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_RESIDENCY_FLAGS(dec, &args->Flags);
    npt_decode_UINT(dec, &args->NumObjects);
    uint64_t _cnt_ppObjects = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppObjects) {
        args->ppObjects = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppObjects);
        if (!args->ppObjects) return;
        for (uint64_t _i = 0; _i < _cnt_ppObjects; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppObjects[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppObjects = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pFenceToSignal = (ID3D12Fence *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->FenceValueToSignal);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppObjects && _cnt_ppObjects < (uint64_t)(args->NumObjects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12Device3_EnqueueMakeResident_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device3_EnqueueMakeResident *args)
{
    if (args->ppObjects) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumObjects); _i++)
            args->ppObjects[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppObjects[_i], NPT_OBJECT_TYPE_ID3D12PAGEABLE);
    }
    args->pFenceToSignal = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pFenceToSignal, NPT_OBJECT_TYPE_ID3D12FENCE);
}

static inline void
npt_encode_ID3D12Device3_EnqueueMakeResident_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D12Device3_EnqueueMakeResident *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device3,
                                 NPT_METHOD_ID3D12Device3_EnqueueMakeResident),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device3_EnqueueMakeResident(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D12Device3_EnqueueMakeResident args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device3_EnqueueMakeResident_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device3_EnqueueMakeResident on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device3_EnqueueMakeResident_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device3_EnqueueMakeResident: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device3_EnqueueMakeResident _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device3_EnqueueMakeResident, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device3_EnqueueMakeResident);

    if (ctx->id3d12device3_dispatch_overrides && ctx->id3d12device3_dispatch_overrides->EnqueueMakeResident) {
        args.ret = ctx->id3d12device3_dispatch_overrides->EnqueueMakeResident(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Flags, args.NumObjects, args.ppObjects, args.pFenceToSignal, args.FenceValueToSignal);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device3_EnqueueMakeResident_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device4::CreateCommandList1
 */

struct npt_command_ID3D12Device4_CreateCommandList1 {
    void *_self;
    UINT nodeMask;
    D3D12_COMMAND_LIST_TYPE type;
    D3D12_COMMAND_LIST_FLAGS flags;
    const IID * riid;
    void ** ppCommandList;
    /* Shadow: the guest-allocated id for ppCommandList arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandList -> *ppCommandList} in the
     * context object table. */
    uint64_t _guest_id_ppCommandList;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device4_CreateCommandList1_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D12Device4_CreateCommandList1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->nodeMask);
    npt_decode_D3D12_COMMAND_LIST_TYPE(dec, &args->type);
    npt_decode_D3D12_COMMAND_LIST_FLAGS(dec, &args->flags);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCommandList = _gid;
        args->ppCommandList = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCommandList) return;
        *args->ppCommandList = NULL;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12Device4_CreateCommandList1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device4_CreateCommandList1 *args)
{
}

static inline void
npt_encode_ID3D12Device4_CreateCommandList1_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D12Device4_CreateCommandList1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateCommandList1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCommandList (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device4_CreateCommandList1(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D12Device4_CreateCommandList1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device4_CreateCommandList1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateCommandList1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device4_CreateCommandList1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateCommandList1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device4_CreateCommandList1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device4_CreateCommandList1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device4_CreateCommandList1);

    if (ctx->id3d12device4_dispatch_overrides && ctx->id3d12device4_dispatch_overrides->CreateCommandList1) {
        args.ret = ctx->id3d12device4_dispatch_overrides->CreateCommandList1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.nodeMask, args.type, args.flags, args.riid, args.ppCommandList);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCommandList && *args.ppCommandList)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCommandList, *args.ppCommandList,
            npt_object_type_from_iid(args.riid));
    else if (args.ppCommandList)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device4_CreateCommandList1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device4::CreateProtectedResourceSession
 */

struct npt_command_ID3D12Device4_CreateProtectedResourceSession {
    void *_self;
    const D3D12_PROTECTED_RESOURCE_SESSION_DESC * pDesc;
    const IID * riid;
    void ** ppSession;
    /* Shadow: the guest-allocated id for ppSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSession -> *ppSession} in the
     * context object table. */
    uint64_t _guest_id_ppSession;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device4_CreateProtectedResourceSession_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D12Device4_CreateProtectedResourceSession *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_PROTECTED_RESOURCE_SESSION_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_PROTECTED_RESOURCE_SESSION_DESC(dec, (D3D12_PROTECTED_RESOURCE_SESSION_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSession = _gid;
        args->ppSession = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSession) return;
        *args->ppSession = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device4_CreateProtectedResourceSession_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device4_CreateProtectedResourceSession *args)
{
}

static inline void
npt_encode_ID3D12Device4_CreateProtectedResourceSession_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D12Device4_CreateProtectedResourceSession *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateProtectedResourceSession),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSession (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device4_CreateProtectedResourceSession(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D12Device4_CreateProtectedResourceSession args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device4_CreateProtectedResourceSession_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateProtectedResourceSession on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device4_CreateProtectedResourceSession_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateProtectedResourceSession: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device4_CreateProtectedResourceSession _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device4_CreateProtectedResourceSession, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device4_CreateProtectedResourceSession);

    if (ctx->id3d12device4_dispatch_overrides && ctx->id3d12device4_dispatch_overrides->CreateProtectedResourceSession) {
        args.ret = ctx->id3d12device4_dispatch_overrides->CreateProtectedResourceSession(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppSession);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSession && *args.ppSession)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSession, *args.ppSession,
            npt_object_type_from_iid(args.riid));
    else if (args.ppSession)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSession);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device4_CreateProtectedResourceSession_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device4::CreateCommittedResource1
 */

struct npt_command_ID3D12Device4_CreateCommittedResource1 {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialResourceState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device4_CreateCommittedResource1_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12Device4_CreateCommittedResource1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pHeapProperties = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_HEAP_PROPERTIES));
        if (!args->pHeapProperties) return;
        npt_decode_D3D12_HEAP_PROPERTIES(dec, (D3D12_HEAP_PROPERTIES *)args->pHeapProperties);
    } else {
        args->pHeapProperties = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_HEAP_FLAGS(dec, &args->HeapFlags);
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InitialResourceState);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pProtectedSession = (ID3D12ProtectedResourceSession *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riidResource = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riidResource) return;
        npt_decode_IID(dec, (IID *)args->riidResource);
    } else {
        args->riidResource = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */








}

static inline void
npt_replace_ID3D12Device4_CreateCommittedResource1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device4_CreateCommittedResource1 *args)
{
    args->pProtectedSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pProtectedSession, NPT_OBJECT_TYPE_ID3D12PROTECTEDRESOURCESESSION);
}

static inline void
npt_encode_ID3D12Device4_CreateCommittedResource1_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12Device4_CreateCommittedResource1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateCommittedResource1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device4_CreateCommittedResource1(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12Device4_CreateCommittedResource1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device4_CreateCommittedResource1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateCommittedResource1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device4_CreateCommittedResource1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateCommittedResource1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device4_CreateCommittedResource1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device4_CreateCommittedResource1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device4_CreateCommittedResource1);

    if (ctx->id3d12device4_dispatch_overrides && ctx->id3d12device4_dispatch_overrides->CreateCommittedResource1) {
        args.ret = ctx->id3d12device4_dispatch_overrides->CreateCommittedResource1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHeapProperties, args.HeapFlags, args.pDesc, args.InitialResourceState, args.pOptimizedClearValue, args.pProtectedSession, args.riidResource, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riidResource));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device4_CreateCommittedResource1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device4::CreateHeap1
 */

struct npt_command_ID3D12Device4_CreateHeap1 {
    void *_self;
    const D3D12_HEAP_DESC * pDesc;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device4_CreateHeap1_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D12Device4_CreateHeap1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_HEAP_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_HEAP_DESC(dec, (D3D12_HEAP_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pProtectedSession = (ID3D12ProtectedResourceSession *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvHeap = _gid;
        args->ppvHeap = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvHeap) return;
        *args->ppvHeap = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device4_CreateHeap1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device4_CreateHeap1 *args)
{
    args->pProtectedSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pProtectedSession, NPT_OBJECT_TYPE_ID3D12PROTECTEDRESOURCESESSION);
}

static inline void
npt_encode_ID3D12Device4_CreateHeap1_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D12Device4_CreateHeap1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateHeap1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvHeap (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device4_CreateHeap1(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D12Device4_CreateHeap1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device4_CreateHeap1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateHeap1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device4_CreateHeap1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateHeap1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device4_CreateHeap1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device4_CreateHeap1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device4_CreateHeap1);

    if (ctx->id3d12device4_dispatch_overrides && ctx->id3d12device4_dispatch_overrides->CreateHeap1) {
        args.ret = ctx->id3d12device4_dispatch_overrides->CreateHeap1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pProtectedSession, args.riid, args.ppvHeap);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvHeap && *args.ppvHeap)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvHeap, *args.ppvHeap,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvHeap)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device4_CreateHeap1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device4::CreateReservedResource1
 */

struct npt_command_ID3D12Device4_CreateReservedResource1 {
    void *_self;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device4_CreateReservedResource1_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D12Device4_CreateReservedResource1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InitialState);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pProtectedSession = (ID3D12ProtectedResourceSession *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12Device4_CreateReservedResource1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device4_CreateReservedResource1 *args)
{
    args->pProtectedSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pProtectedSession, NPT_OBJECT_TYPE_ID3D12PROTECTEDRESOURCESESSION);
}

static inline void
npt_encode_ID3D12Device4_CreateReservedResource1_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D12Device4_CreateReservedResource1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_CreateReservedResource1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device4_CreateReservedResource1(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D12Device4_CreateReservedResource1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device4_CreateReservedResource1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateReservedResource1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device4_CreateReservedResource1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_CreateReservedResource1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device4_CreateReservedResource1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device4_CreateReservedResource1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device4_CreateReservedResource1);

    if (ctx->id3d12device4_dispatch_overrides && ctx->id3d12device4_dispatch_overrides->CreateReservedResource1) {
        args.ret = ctx->id3d12device4_dispatch_overrides->CreateReservedResource1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.InitialState, args.pOptimizedClearValue, args.pProtectedSession, args.riid, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device4_CreateReservedResource1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device4::GetResourceAllocationInfo1
 */

struct npt_command_ID3D12Device4_GetResourceAllocationInfo1 {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC * pResourceDescs;
    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline void
npt_decode_ID3D12Device4_GetResourceAllocationInfo1_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D12Device4_GetResourceAllocationInfo1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->visibleMask);
    npt_decode_UINT(dec, &args->numResourceDescs);
    uint64_t _cnt_pResourceDescs = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pResourceDescs = npt_decode_array_count_unchecked(dec);
        args->pResourceDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_DESC), _cnt_pResourceDescs);
        if (!args->pResourceDescs) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pResourceDescs; _i++)
            npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)&args->pResourceDescs[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pResourceDescs = NULL;
    }

    /* A counted array must carry every element the callee will read. */
    if (_cnt_pResourceDescs && _cnt_pResourceDescs < (uint64_t)(args->numResourceDescs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



    if (args->numResourceDescs) {
        args->pResourceAllocationInfo1 = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_ALLOCATION_INFO1), args->numResourceDescs);
        if (!args->pResourceAllocationInfo1) return;
        memset(args->pResourceAllocationInfo1, 0, sizeof(D3D12_RESOURCE_ALLOCATION_INFO1) * args->numResourceDescs);
    } else {
        args->pResourceAllocationInfo1 = NULL;
    }
}

static inline void
npt_replace_ID3D12Device4_GetResourceAllocationInfo1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device4_GetResourceAllocationInfo1 *args)
{
}

static inline void
npt_encode_ID3D12Device4_GetResourceAllocationInfo1_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D12Device4_GetResourceAllocationInfo1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device4,
                                 NPT_METHOD_ID3D12Device4_GetResourceAllocationInfo1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_D3D12_RESOURCE_ALLOCATION_INFO(enc, &args->ret);
    if (args->pResourceAllocationInfo1) {
        npt_encode_array_count(enc, args->numResourceDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(args->numResourceDescs); _i++)
            npt_encode_D3D12_RESOURCE_ALLOCATION_INFO1(enc, &args->pResourceAllocationInfo1[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D12Device4_GetResourceAllocationInfo1(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D12Device4_GetResourceAllocationInfo1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device4_GetResourceAllocationInfo1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_GetResourceAllocationInfo1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device4_GetResourceAllocationInfo1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device4_GetResourceAllocationInfo1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device4_GetResourceAllocationInfo1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device4_GetResourceAllocationInfo1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device4_GetResourceAllocationInfo1);

    if (ctx->id3d12device4_dispatch_overrides && ctx->id3d12device4_dispatch_overrides->GetResourceAllocationInfo1) {
        args.ret = ctx->id3d12device4_dispatch_overrides->GetResourceAllocationInfo1(ctx, &args, _original);
    } else {
        /* COM x64 aggregate-return ABI: the hidden pointer (&args.ret)
         * rides in the call args; the returned pointer aliases it. */
        (void)_original(args._self, &args.ret, args.visibleMask, args.numResourceDescs, args.pResourceDescs, args.pResourceAllocationInfo1);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device4_GetResourceAllocationInfo1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::CreateLifetimeTracker
 */

struct npt_command_ID3D12Device5_CreateLifetimeTracker {
    void *_self;
    ID3D12LifetimeOwner * pOwner;
    const IID * riid;
    void ** ppvTracker;
    /* Shadow: the guest-allocated id for ppvTracker arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvTracker -> *ppvTracker} in the
     * context object table. */
    uint64_t _guest_id_ppvTracker;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device5_CreateLifetimeTracker_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device5_CreateLifetimeTracker *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pOwner = (ID3D12LifetimeOwner *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvTracker = _gid;
        args->ppvTracker = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvTracker) return;
        *args->ppvTracker = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device5_CreateLifetimeTracker_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_CreateLifetimeTracker *args)
{
    args->pOwner = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pOwner, NPT_OBJECT_TYPE_ID3D12LIFETIMEOWNER);
}

static inline void
npt_encode_ID3D12Device5_CreateLifetimeTracker_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device5_CreateLifetimeTracker *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CreateLifetimeTracker),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvTracker (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device5_CreateLifetimeTracker(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device5_CreateLifetimeTracker args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_CreateLifetimeTracker_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CreateLifetimeTracker on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvTracker);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_CreateLifetimeTracker_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CreateLifetimeTracker: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvTracker);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_CreateLifetimeTracker _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_CreateLifetimeTracker, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_CreateLifetimeTracker);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->CreateLifetimeTracker) {
        args.ret = ctx->id3d12device5_dispatch_overrides->CreateLifetimeTracker(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pOwner, args.riid, args.ppvTracker);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvTracker && *args.ppvTracker)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvTracker, *args.ppvTracker,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvTracker)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvTracker);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_CreateLifetimeTracker_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::RemoveDevice
 */

struct npt_command_ID3D12Device5_RemoveDevice {
    void *_self;
};

static inline void
npt_decode_ID3D12Device5_RemoveDevice_args_temp(struct npt_cs_decoder *dec,
                                                struct npt_command_ID3D12Device5_RemoveDevice *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12Device5_RemoveDevice_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_RemoveDevice *args)
{
}

static inline void
npt_encode_ID3D12Device5_RemoveDevice_reply(struct npt_cs_encoder *enc,
                                            const struct npt_command_ID3D12Device5_RemoveDevice *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_RemoveDevice),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device5_RemoveDevice(struct npt_dispatch_context *ctx,
                                        uint32_t cmd_flags,
                                        uint64_t object_id)
{
    struct npt_command_ID3D12Device5_RemoveDevice args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_RemoveDevice_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_RemoveDevice on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_RemoveDevice_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_RemoveDevice: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_RemoveDevice _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_RemoveDevice, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_RemoveDevice);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->RemoveDevice) {
        ctx->id3d12device5_dispatch_overrides->RemoveDevice(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_RemoveDevice_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::EnumerateMetaCommands
 */

struct npt_command_ID3D12Device5_EnumerateMetaCommands {
    void *_self;
    UINT * pNumMetaCommands;
    D3D12_META_COMMAND_DESC * pDescs;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device5_EnumerateMetaCommands_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device5_EnumerateMetaCommands *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pNumMetaCommands = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumMetaCommands) return;
        npt_decode_UINT(dec, (UINT *)args->pNumMetaCommands);
    } else {
        args->pNumMetaCommands = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */

    if (*args->pNumMetaCommands) {
        args->pDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_META_COMMAND_DESC), *args->pNumMetaCommands);
        if (!args->pDescs) return;
        memset(args->pDescs, 0, sizeof(D3D12_META_COMMAND_DESC) * *args->pNumMetaCommands);
    } else {
        args->pDescs = NULL;
    }
}

static inline void
npt_replace_ID3D12Device5_EnumerateMetaCommands_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_EnumerateMetaCommands *args)
{
}

static inline void
npt_encode_ID3D12Device5_EnumerateMetaCommands_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device5_EnumerateMetaCommands *args)
{
    /* ID3D12Device5::EnumerateMetaCommands: reply contains a type with
     * runtime-unbounded wire size (e.g. DRED chain pointers, root-
     * signature parameter arrays).  A pre-reserved fixed-size reply
     * slot cannot represent this without truncating the host data, so
     * the guest gets a clean reply-mismatch via the encoder fatal
     * flag rather than a corrupted stream. */
    npt_cs_encoder_set_fatal(enc);
    return;
}

static inline void
npt_dispatch_ID3D12Device5_EnumerateMetaCommands(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device5_EnumerateMetaCommands args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_EnumerateMetaCommands_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_EnumerateMetaCommands on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_EnumerateMetaCommands_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_EnumerateMetaCommands: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_EnumerateMetaCommands _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_EnumerateMetaCommands, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_EnumerateMetaCommands);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->EnumerateMetaCommands) {
        args.ret = ctx->id3d12device5_dispatch_overrides->EnumerateMetaCommands(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pNumMetaCommands, args.pDescs);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_EnumerateMetaCommands_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::EnumerateMetaCommandParameters
 */

struct npt_command_ID3D12Device5_EnumerateMetaCommandParameters {
    void *_self;
    const GUID * CommandId;
    D3D12_META_COMMAND_PARAMETER_STAGE Stage;
    UINT * pTotalStructureSizeInBytes;
    UINT * pParameterCount;
    D3D12_META_COMMAND_PARAMETER_DESC * pParameterDescs;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device5_EnumerateMetaCommandParameters_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D12Device5_EnumerateMetaCommandParameters *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->CommandId = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->CommandId) return;
        npt_decode_GUID(dec, (GUID *)args->CommandId);
    } else {
        args->CommandId = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_META_COMMAND_PARAMETER_STAGE(dec, &args->Stage);

    if (npt_decode_simple_pointer(dec)) {
        args->pParameterCount = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pParameterCount) return;
        npt_decode_UINT(dec, (UINT *)args->pParameterCount);
    } else {
        args->pParameterCount = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */


    args->pTotalStructureSizeInBytes = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pTotalStructureSizeInBytes) return;
    memset(args->pTotalStructureSizeInBytes, 0, sizeof(UINT));

    if (*args->pParameterCount) {
        args->pParameterDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_META_COMMAND_PARAMETER_DESC), *args->pParameterCount);
        if (!args->pParameterDescs) return;
        memset(args->pParameterDescs, 0, sizeof(D3D12_META_COMMAND_PARAMETER_DESC) * *args->pParameterCount);
    } else {
        args->pParameterDescs = NULL;
    }
}

static inline void
npt_replace_ID3D12Device5_EnumerateMetaCommandParameters_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_EnumerateMetaCommandParameters *args)
{
}

static inline void
npt_encode_ID3D12Device5_EnumerateMetaCommandParameters_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D12Device5_EnumerateMetaCommandParameters *args)
{
    /* ID3D12Device5::EnumerateMetaCommandParameters: reply contains a type with
     * runtime-unbounded wire size (e.g. DRED chain pointers, root-
     * signature parameter arrays).  A pre-reserved fixed-size reply
     * slot cannot represent this without truncating the host data, so
     * the guest gets a clean reply-mismatch via the encoder fatal
     * flag rather than a corrupted stream. */
    npt_cs_encoder_set_fatal(enc);
    return;
}

static inline void
npt_dispatch_ID3D12Device5_EnumerateMetaCommandParameters(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D12Device5_EnumerateMetaCommandParameters args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_EnumerateMetaCommandParameters_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_EnumerateMetaCommandParameters on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_EnumerateMetaCommandParameters_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_EnumerateMetaCommandParameters: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_EnumerateMetaCommandParameters _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_EnumerateMetaCommandParameters, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_EnumerateMetaCommandParameters);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->EnumerateMetaCommandParameters) {
        args.ret = ctx->id3d12device5_dispatch_overrides->EnumerateMetaCommandParameters(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.CommandId, args.Stage, args.pTotalStructureSizeInBytes, args.pParameterCount, args.pParameterDescs);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_EnumerateMetaCommandParameters_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::CreateMetaCommand
 */

struct npt_command_ID3D12Device5_CreateMetaCommand {
    void *_self;
    const GUID * CommandId;
    UINT NodeMask;
    const void * pCreationParametersData;
    SIZE_T CreationParametersDataSizeInBytes;
    const IID * riid;
    void ** ppMetaCommand;
    /* Shadow: the guest-allocated id for ppMetaCommand arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppMetaCommand -> *ppMetaCommand} in the
     * context object table. */
    uint64_t _guest_id_ppMetaCommand;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device5_CreateMetaCommand_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D12Device5_CreateMetaCommand *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->CommandId = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->CommandId) return;
        npt_decode_GUID(dec, (GUID *)args->CommandId);
    } else {
        args->CommandId = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->NodeMask);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pCreationParametersData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pCreationParametersData) return;
            npt_decode_blob_array(dec, (void *)args->pCreationParametersData, _blob_size);
        } else {
            args->pCreationParametersData = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->CreationParametersDataSizeInBytes);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppMetaCommand = _gid;
        args->ppMetaCommand = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppMetaCommand) return;
        *args->ppMetaCommand = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12Device5_CreateMetaCommand_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_CreateMetaCommand *args)
{
}

static inline void
npt_encode_ID3D12Device5_CreateMetaCommand_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D12Device5_CreateMetaCommand *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CreateMetaCommand),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppMetaCommand (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device5_CreateMetaCommand(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D12Device5_CreateMetaCommand args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_CreateMetaCommand_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CreateMetaCommand on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppMetaCommand);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_CreateMetaCommand_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CreateMetaCommand: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppMetaCommand);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_CreateMetaCommand _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_CreateMetaCommand, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_CreateMetaCommand);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->CreateMetaCommand) {
        args.ret = ctx->id3d12device5_dispatch_overrides->CreateMetaCommand(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.CommandId, args.NodeMask, args.pCreationParametersData, args.CreationParametersDataSizeInBytes, args.riid, args.ppMetaCommand);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppMetaCommand && *args.ppMetaCommand)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppMetaCommand, *args.ppMetaCommand,
            npt_object_type_from_iid(args.riid));
    else if (args.ppMetaCommand)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppMetaCommand);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_CreateMetaCommand_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::CreateStateObject
 */

struct npt_command_ID3D12Device5_CreateStateObject {
    void *_self;
    const D3D12_STATE_OBJECT_DESC * pDesc;
    const IID * riid;
    void ** ppStateObject;
    /* Shadow: the guest-allocated id for ppStateObject arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppStateObject -> *ppStateObject} in the
     * context object table. */
    uint64_t _guest_id_ppStateObject;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device5_CreateStateObject_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D12Device5_CreateStateObject *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_STATE_OBJECT_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_STATE_OBJECT_DESC(dec, (D3D12_STATE_OBJECT_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppStateObject = _gid;
        args->ppStateObject = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppStateObject) return;
        *args->ppStateObject = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device5_CreateStateObject_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_CreateStateObject *args)
{
    if (args->pDesc)
        npt_replace_D3D12_STATE_OBJECT_DESC_handle(ctx, (D3D12_STATE_OBJECT_DESC *)args->pDesc);
}

static inline void
npt_encode_ID3D12Device5_CreateStateObject_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D12Device5_CreateStateObject *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CreateStateObject),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppStateObject (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device5_CreateStateObject(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D12Device5_CreateStateObject args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_CreateStateObject_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CreateStateObject on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppStateObject);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_CreateStateObject_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CreateStateObject: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppStateObject);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_CreateStateObject _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_CreateStateObject, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_CreateStateObject);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->CreateStateObject) {
        args.ret = ctx->id3d12device5_dispatch_overrides->CreateStateObject(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppStateObject);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppStateObject && *args.ppStateObject)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppStateObject, *args.ppStateObject,
            npt_object_type_from_iid(args.riid));
    else if (args.ppStateObject)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppStateObject);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_CreateStateObject_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::GetRaytracingAccelerationStructurePrebuildInfo
 */

struct npt_command_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo {
    void *_self;
    const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS * pDesc;
    D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO * pInfo;
};

static inline void
npt_decode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_args_temp(struct npt_cs_decoder *dec,
                                                                                  struct npt_command_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS));
        if (!args->pDesc) return;
        npt_decode_D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS(dec, (D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_INPUTS *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */

    args->pInfo = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO));
    if (!args->pInfo) return;
    memset(args->pInfo, 0, sizeof(D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO));
}

static inline void
npt_replace_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo *args)
{
}

static inline void
npt_encode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_reply(struct npt_cs_encoder *enc,
                                                                              const struct npt_command_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pInfo))
        npt_encode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_PREBUILD_INFO(enc, args->pInfo);
}

static inline void
npt_dispatch_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo(struct npt_dispatch_context *ctx,
                                                                          uint32_t cmd_flags,
                                                                          uint64_t object_id)
{
    struct npt_command_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->GetRaytracingAccelerationStructurePrebuildInfo) {
        ctx->id3d12device5_dispatch_overrides->GetRaytracingAccelerationStructurePrebuildInfo(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc, args.pInfo);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_GetRaytracingAccelerationStructurePrebuildInfo_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device5::CheckDriverMatchingIdentifier
 */

struct npt_command_ID3D12Device5_CheckDriverMatchingIdentifier {
    void *_self;
    D3D12_SERIALIZED_DATA_TYPE SerializedDataType;
    const D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER * pIdentifierToCheck;
    D3D12_DRIVER_MATCHING_IDENTIFIER_STATUS ret;
};

static inline void
npt_decode_ID3D12Device5_CheckDriverMatchingIdentifier_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12Device5_CheckDriverMatchingIdentifier *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_SERIALIZED_DATA_TYPE(dec, &args->SerializedDataType);
    if (npt_decode_simple_pointer(dec)) {
        args->pIdentifierToCheck = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER));
        if (!args->pIdentifierToCheck) return;
        npt_decode_D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER(dec, (D3D12_SERIALIZED_DATA_DRIVER_MATCHING_IDENTIFIER *)args->pIdentifierToCheck);
    } else {
        args->pIdentifierToCheck = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device5_CheckDriverMatchingIdentifier_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device5_CheckDriverMatchingIdentifier *args)
{
}

static inline void
npt_encode_ID3D12Device5_CheckDriverMatchingIdentifier_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12Device5_CheckDriverMatchingIdentifier *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device5,
                                 NPT_METHOD_ID3D12Device5_CheckDriverMatchingIdentifier),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device5_CheckDriverMatchingIdentifier(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12Device5_CheckDriverMatchingIdentifier args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device5_CheckDriverMatchingIdentifier_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CheckDriverMatchingIdentifier on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device5_CheckDriverMatchingIdentifier_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device5_CheckDriverMatchingIdentifier: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device5_CheckDriverMatchingIdentifier _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device5_CheckDriverMatchingIdentifier, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device5_CheckDriverMatchingIdentifier);

    if (ctx->id3d12device5_dispatch_overrides && ctx->id3d12device5_dispatch_overrides->CheckDriverMatchingIdentifier) {
        args.ret = ctx->id3d12device5_dispatch_overrides->CheckDriverMatchingIdentifier(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.SerializedDataType, args.pIdentifierToCheck);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device5_CheckDriverMatchingIdentifier_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device6::SetBackgroundProcessingMode
 */

struct npt_command_ID3D12Device6_SetBackgroundProcessingMode {
    void *_self;
    D3D12_BACKGROUND_PROCESSING_MODE Mode;
    D3D12_MEASUREMENTS_ACTION MeasurementsAction;
    HANDLE hEventToSignalUponCompletion;
    BOOL * pbFurtherMeasurementsDesired;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device6_SetBackgroundProcessingMode_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12Device6_SetBackgroundProcessingMode *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_BACKGROUND_PROCESSING_MODE(dec, &args->Mode);
    npt_decode_D3D12_MEASUREMENTS_ACTION(dec, &args->MeasurementsAction);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEventToSignalUponCompletion = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */



    args->pbFurtherMeasurementsDesired = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pbFurtherMeasurementsDesired) return;
    memset(args->pbFurtherMeasurementsDesired, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D12Device6_SetBackgroundProcessingMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device6_SetBackgroundProcessingMode *args)
{
    args->hEventToSignalUponCompletion = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEventToSignalUponCompletion);
}

static inline void
npt_encode_ID3D12Device6_SetBackgroundProcessingMode_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12Device6_SetBackgroundProcessingMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device6,
                                 NPT_METHOD_ID3D12Device6_SetBackgroundProcessingMode),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pbFurtherMeasurementsDesired))
        npt_encode_BOOL(enc, args->pbFurtherMeasurementsDesired);
}

static inline void
npt_dispatch_ID3D12Device6_SetBackgroundProcessingMode(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12Device6_SetBackgroundProcessingMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device6_SetBackgroundProcessingMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE6);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device6_SetBackgroundProcessingMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device6_SetBackgroundProcessingMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device6_SetBackgroundProcessingMode: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device6_SetBackgroundProcessingMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device6_SetBackgroundProcessingMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device6_SetBackgroundProcessingMode);

    if (ctx->id3d12device6_dispatch_overrides && ctx->id3d12device6_dispatch_overrides->SetBackgroundProcessingMode) {
        args.ret = ctx->id3d12device6_dispatch_overrides->SetBackgroundProcessingMode(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Mode, args.MeasurementsAction, args.hEventToSignalUponCompletion, args.pbFurtherMeasurementsDesired);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device6_SetBackgroundProcessingMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device7::AddToStateObject
 */

struct npt_command_ID3D12Device7_AddToStateObject {
    void *_self;
    const D3D12_STATE_OBJECT_DESC * pAddition;
    ID3D12StateObject * pStateObjectToGrowFrom;
    const IID * riid;
    void ** ppNewStateObject;
    /* Shadow: the guest-allocated id for ppNewStateObject arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppNewStateObject -> *ppNewStateObject} in the
     * context object table. */
    uint64_t _guest_id_ppNewStateObject;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device7_AddToStateObject_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D12Device7_AddToStateObject *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pAddition = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_STATE_OBJECT_DESC));
        if (!args->pAddition) return;
        npt_decode_D3D12_STATE_OBJECT_DESC(dec, (D3D12_STATE_OBJECT_DESC *)args->pAddition);
    } else {
        args->pAddition = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pStateObjectToGrowFrom = (ID3D12StateObject *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppNewStateObject = _gid;
        args->ppNewStateObject = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppNewStateObject) return;
        *args->ppNewStateObject = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device7_AddToStateObject_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device7_AddToStateObject *args)
{
    if (args->pAddition)
        npt_replace_D3D12_STATE_OBJECT_DESC_handle(ctx, (D3D12_STATE_OBJECT_DESC *)args->pAddition);
    args->pStateObjectToGrowFrom = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pStateObjectToGrowFrom, NPT_OBJECT_TYPE_ID3D12STATEOBJECT);
}

static inline void
npt_encode_ID3D12Device7_AddToStateObject_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D12Device7_AddToStateObject *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                 NPT_METHOD_ID3D12Device7_AddToStateObject),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppNewStateObject (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device7_AddToStateObject(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D12Device7_AddToStateObject args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device7_AddToStateObject_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE7);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device7_AddToStateObject on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppNewStateObject);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device7_AddToStateObject_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device7_AddToStateObject: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppNewStateObject);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device7_AddToStateObject _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device7_AddToStateObject, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device7_AddToStateObject);

    if (ctx->id3d12device7_dispatch_overrides && ctx->id3d12device7_dispatch_overrides->AddToStateObject) {
        args.ret = ctx->id3d12device7_dispatch_overrides->AddToStateObject(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pAddition, args.pStateObjectToGrowFrom, args.riid, args.ppNewStateObject);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppNewStateObject && *args.ppNewStateObject)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppNewStateObject, *args.ppNewStateObject,
            npt_object_type_from_iid(args.riid));
    else if (args.ppNewStateObject)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppNewStateObject);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device7_AddToStateObject_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device7::CreateProtectedResourceSession1
 */

struct npt_command_ID3D12Device7_CreateProtectedResourceSession1 {
    void *_self;
    const D3D12_PROTECTED_RESOURCE_SESSION_DESC1 * pDesc;
    const IID * riid;
    void ** ppSession;
    /* Shadow: the guest-allocated id for ppSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSession -> *ppSession} in the
     * context object table. */
    uint64_t _guest_id_ppSession;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device7_CreateProtectedResourceSession1_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D12Device7_CreateProtectedResourceSession1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_PROTECTED_RESOURCE_SESSION_DESC1));
        if (!args->pDesc) return;
        npt_decode_D3D12_PROTECTED_RESOURCE_SESSION_DESC1(dec, (D3D12_PROTECTED_RESOURCE_SESSION_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSession = _gid;
        args->ppSession = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSession) return;
        *args->ppSession = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device7_CreateProtectedResourceSession1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device7_CreateProtectedResourceSession1 *args)
{
}

static inline void
npt_encode_ID3D12Device7_CreateProtectedResourceSession1_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D12Device7_CreateProtectedResourceSession1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device7,
                                 NPT_METHOD_ID3D12Device7_CreateProtectedResourceSession1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSession (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device7_CreateProtectedResourceSession1(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D12Device7_CreateProtectedResourceSession1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device7_CreateProtectedResourceSession1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE7);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device7_CreateProtectedResourceSession1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device7_CreateProtectedResourceSession1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device7_CreateProtectedResourceSession1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device7_CreateProtectedResourceSession1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device7_CreateProtectedResourceSession1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device7_CreateProtectedResourceSession1);

    if (ctx->id3d12device7_dispatch_overrides && ctx->id3d12device7_dispatch_overrides->CreateProtectedResourceSession1) {
        args.ret = ctx->id3d12device7_dispatch_overrides->CreateProtectedResourceSession1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppSession);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSession && *args.ppSession)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSession, *args.ppSession,
            npt_object_type_from_iid(args.riid));
    else if (args.ppSession)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSession);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device7_CreateProtectedResourceSession1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device8::GetResourceAllocationInfo2
 */

struct npt_command_ID3D12Device8_GetResourceAllocationInfo2 {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC1 * pResourceDescs;
    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline void
npt_decode_ID3D12Device8_GetResourceAllocationInfo2_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D12Device8_GetResourceAllocationInfo2 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->visibleMask);
    npt_decode_UINT(dec, &args->numResourceDescs);
    uint64_t _cnt_pResourceDescs = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pResourceDescs = npt_decode_array_count_unchecked(dec);
        args->pResourceDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_DESC1), _cnt_pResourceDescs);
        if (!args->pResourceDescs) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pResourceDescs; _i++)
            npt_decode_D3D12_RESOURCE_DESC1(dec, (D3D12_RESOURCE_DESC1 *)&args->pResourceDescs[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pResourceDescs = NULL;
    }

    /* A counted array must carry every element the callee will read. */
    if (_cnt_pResourceDescs && _cnt_pResourceDescs < (uint64_t)(args->numResourceDescs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



    if (args->numResourceDescs) {
        args->pResourceAllocationInfo1 = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_ALLOCATION_INFO1), args->numResourceDescs);
        if (!args->pResourceAllocationInfo1) return;
        memset(args->pResourceAllocationInfo1, 0, sizeof(D3D12_RESOURCE_ALLOCATION_INFO1) * args->numResourceDescs);
    } else {
        args->pResourceAllocationInfo1 = NULL;
    }
}

static inline void
npt_replace_ID3D12Device8_GetResourceAllocationInfo2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device8_GetResourceAllocationInfo2 *args)
{
}

static inline void
npt_encode_ID3D12Device8_GetResourceAllocationInfo2_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D12Device8_GetResourceAllocationInfo2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_GetResourceAllocationInfo2),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_D3D12_RESOURCE_ALLOCATION_INFO(enc, &args->ret);
    if (args->pResourceAllocationInfo1) {
        npt_encode_array_count(enc, args->numResourceDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(args->numResourceDescs); _i++)
            npt_encode_D3D12_RESOURCE_ALLOCATION_INFO1(enc, &args->pResourceAllocationInfo1[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D12Device8_GetResourceAllocationInfo2(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D12Device8_GetResourceAllocationInfo2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device8_GetResourceAllocationInfo2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE8);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_GetResourceAllocationInfo2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device8_GetResourceAllocationInfo2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_GetResourceAllocationInfo2: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device8_GetResourceAllocationInfo2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device8_GetResourceAllocationInfo2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device8_GetResourceAllocationInfo2);

    if (ctx->id3d12device8_dispatch_overrides && ctx->id3d12device8_dispatch_overrides->GetResourceAllocationInfo2) {
        args.ret = ctx->id3d12device8_dispatch_overrides->GetResourceAllocationInfo2(ctx, &args, _original);
    } else {
        /* COM x64 aggregate-return ABI: the hidden pointer (&args.ret)
         * rides in the call args; the returned pointer aliases it. */
        (void)_original(args._self, &args.ret, args.visibleMask, args.numResourceDescs, args.pResourceDescs, args.pResourceAllocationInfo1);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device8_GetResourceAllocationInfo2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device8::CreateCommittedResource2
 */

struct npt_command_ID3D12Device8_CreateCommittedResource2 {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_RESOURCE_STATES InitialResourceState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device8_CreateCommittedResource2_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12Device8_CreateCommittedResource2 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pHeapProperties = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_HEAP_PROPERTIES));
        if (!args->pHeapProperties) return;
        npt_decode_D3D12_HEAP_PROPERTIES(dec, (D3D12_HEAP_PROPERTIES *)args->pHeapProperties);
    } else {
        args->pHeapProperties = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_HEAP_FLAGS(dec, &args->HeapFlags);
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC1));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC1(dec, (D3D12_RESOURCE_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InitialResourceState);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pProtectedSession = (ID3D12ProtectedResourceSession *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riidResource = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riidResource) return;
        npt_decode_IID(dec, (IID *)args->riidResource);
    } else {
        args->riidResource = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */








}

static inline void
npt_replace_ID3D12Device8_CreateCommittedResource2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device8_CreateCommittedResource2 *args)
{
    args->pProtectedSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pProtectedSession, NPT_OBJECT_TYPE_ID3D12PROTECTEDRESOURCESESSION);
}

static inline void
npt_encode_ID3D12Device8_CreateCommittedResource2_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12Device8_CreateCommittedResource2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_CreateCommittedResource2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device8_CreateCommittedResource2(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12Device8_CreateCommittedResource2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device8_CreateCommittedResource2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE8);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_CreateCommittedResource2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device8_CreateCommittedResource2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_CreateCommittedResource2: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device8_CreateCommittedResource2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device8_CreateCommittedResource2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device8_CreateCommittedResource2);

    if (ctx->id3d12device8_dispatch_overrides && ctx->id3d12device8_dispatch_overrides->CreateCommittedResource2) {
        args.ret = ctx->id3d12device8_dispatch_overrides->CreateCommittedResource2(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHeapProperties, args.HeapFlags, args.pDesc, args.InitialResourceState, args.pOptimizedClearValue, args.pProtectedSession, args.riidResource, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riidResource));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device8_CreateCommittedResource2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device8::CreatePlacedResource1
 */

struct npt_command_ID3D12Device8_CreatePlacedResource1 {
    void *_self;
    ID3D12Heap * pHeap;
    UINT64 HeapOffset;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_RESOURCE_STATES InitialState;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device8_CreatePlacedResource1_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12Device8_CreatePlacedResource1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pHeap = (ID3D12Heap *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->HeapOffset);
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC1));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC1(dec, (D3D12_RESOURCE_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InitialState);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D12Device8_CreatePlacedResource1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device8_CreatePlacedResource1 *args)
{
    args->pHeap = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pHeap, NPT_OBJECT_TYPE_ID3D12HEAP);
}

static inline void
npt_encode_ID3D12Device8_CreatePlacedResource1_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12Device8_CreatePlacedResource1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_CreatePlacedResource1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device8_CreatePlacedResource1(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12Device8_CreatePlacedResource1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device8_CreatePlacedResource1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE8);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_CreatePlacedResource1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device8_CreatePlacedResource1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_CreatePlacedResource1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device8_CreatePlacedResource1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device8_CreatePlacedResource1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device8_CreatePlacedResource1);

    if (ctx->id3d12device8_dispatch_overrides && ctx->id3d12device8_dispatch_overrides->CreatePlacedResource1) {
        args.ret = ctx->id3d12device8_dispatch_overrides->CreatePlacedResource1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHeap, args.HeapOffset, args.pDesc, args.InitialState, args.pOptimizedClearValue, args.riid, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device8_CreatePlacedResource1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device8::CreateSamplerFeedbackUnorderedAccessView
 */

struct npt_command_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView {
    void *_self;
    ID3D12Resource * pTargetedResource;
    ID3D12Resource * pFeedbackResource;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTargetedResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pFeedbackResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView *args)
{
    args->pTargetedResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTargetedResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pFeedbackResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pFeedbackResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE8);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView);

    if (ctx->id3d12device8_dispatch_overrides && ctx->id3d12device8_dispatch_overrides->CreateSamplerFeedbackUnorderedAccessView) {
        ctx->id3d12device8_dispatch_overrides->CreateSamplerFeedbackUnorderedAccessView(ctx, &args, _original);
    } else {
        _original(args._self, args.pTargetedResource, args.pFeedbackResource, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device8_CreateSamplerFeedbackUnorderedAccessView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device8::GetCopyableFootprints1
 */

struct npt_command_ID3D12Device8_GetCopyableFootprints1 {
    void *_self;
    const D3D12_RESOURCE_DESC1 * pResourceDesc;
    UINT FirstSubresource;
    UINT NumSubresources;
    UINT64 BaseOffset;
    D3D12_PLACED_SUBRESOURCE_FOOTPRINT * pLayouts;
    UINT * pNumRows;
    UINT64 * pRowSizeInBytes;
    UINT64 * pTotalBytes;
};

static inline void
npt_decode_ID3D12Device8_GetCopyableFootprints1_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12Device8_GetCopyableFootprints1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pResourceDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC1));
        if (!args->pResourceDesc) return;
        npt_decode_D3D12_RESOURCE_DESC1(dec, (D3D12_RESOURCE_DESC1 *)args->pResourceDesc);
    } else {
        args->pResourceDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->FirstSubresource);
    npt_decode_UINT(dec, &args->NumSubresources);
    npt_decode_UINT64(dec, &args->BaseOffset);




    /* Allocate temp storage for output-only parameters */




    if (args->NumSubresources) {
        args->pLayouts = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_PLACED_SUBRESOURCE_FOOTPRINT), args->NumSubresources);
        if (!args->pLayouts) return;
        memset(args->pLayouts, 0, sizeof(D3D12_PLACED_SUBRESOURCE_FOOTPRINT) * args->NumSubresources);
    } else {
        args->pLayouts = NULL;
    }
    if (args->NumSubresources) {
        args->pNumRows = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumSubresources);
        if (!args->pNumRows) return;
        memset(args->pNumRows, 0, sizeof(UINT) * args->NumSubresources);
    } else {
        args->pNumRows = NULL;
    }
    if (args->NumSubresources) {
        args->pRowSizeInBytes = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT64), args->NumSubresources);
        if (!args->pRowSizeInBytes) return;
        memset(args->pRowSizeInBytes, 0, sizeof(UINT64) * args->NumSubresources);
    } else {
        args->pRowSizeInBytes = NULL;
    }
    args->pTotalBytes = npt_cs_decoder_alloc_temp(dec, sizeof(UINT64));
    if (!args->pTotalBytes) return;
    memset(args->pTotalBytes, 0, sizeof(UINT64));
}

static inline void
npt_replace_ID3D12Device8_GetCopyableFootprints1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device8_GetCopyableFootprints1 *args)
{
}

static inline void
npt_encode_ID3D12Device8_GetCopyableFootprints1_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12Device8_GetCopyableFootprints1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device8,
                                 NPT_METHOD_ID3D12Device8_GetCopyableFootprints1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pLayouts) {
        npt_encode_array_count(enc, args->NumSubresources);
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSubresources); _i++)
            npt_encode_D3D12_PLACED_SUBRESOURCE_FOOTPRINT(enc, &args->pLayouts[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumRows) {
        npt_encode_array_count(enc, args->NumSubresources);
        npt_encode_UINT_array(enc, args->pNumRows, args->NumSubresources);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pRowSizeInBytes) {
        npt_encode_array_count(enc, args->NumSubresources);
        npt_encode_UINT64_array(enc, args->pRowSizeInBytes, args->NumSubresources);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pTotalBytes))
        npt_encode_UINT64(enc, args->pTotalBytes);
}

static inline void
npt_dispatch_ID3D12Device8_GetCopyableFootprints1(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12Device8_GetCopyableFootprints1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device8_GetCopyableFootprints1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE8);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_GetCopyableFootprints1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device8_GetCopyableFootprints1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device8_GetCopyableFootprints1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device8_GetCopyableFootprints1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device8_GetCopyableFootprints1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device8_GetCopyableFootprints1);

    if (ctx->id3d12device8_dispatch_overrides && ctx->id3d12device8_dispatch_overrides->GetCopyableFootprints1) {
        ctx->id3d12device8_dispatch_overrides->GetCopyableFootprints1(ctx, &args, _original);
    } else {
        _original(args._self, args.pResourceDesc, args.FirstSubresource, args.NumSubresources, args.BaseOffset, args.pLayouts, args.pNumRows, args.pRowSizeInBytes, args.pTotalBytes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device8_GetCopyableFootprints1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device9::CreateShaderCacheSession
 */

struct npt_command_ID3D12Device9_CreateShaderCacheSession {
    void *_self;
    const D3D12_SHADER_CACHE_SESSION_DESC * pDesc;
    const IID * riid;
    void ** ppvSession;
    /* Shadow: the guest-allocated id for ppvSession arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvSession -> *ppvSession} in the
     * context object table. */
    uint64_t _guest_id_ppvSession;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device9_CreateShaderCacheSession_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12Device9_CreateShaderCacheSession *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_SHADER_CACHE_SESSION_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_SHADER_CACHE_SESSION_DESC(dec, (D3D12_SHADER_CACHE_SESSION_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvSession = _gid;
        args->ppvSession = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvSession) return;
        *args->ppvSession = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Device9_CreateShaderCacheSession_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device9_CreateShaderCacheSession *args)
{
}

static inline void
npt_encode_ID3D12Device9_CreateShaderCacheSession_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12Device9_CreateShaderCacheSession *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                 NPT_METHOD_ID3D12Device9_CreateShaderCacheSession),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvSession (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device9_CreateShaderCacheSession(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12Device9_CreateShaderCacheSession args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device9_CreateShaderCacheSession_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE9);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device9_CreateShaderCacheSession on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device9_CreateShaderCacheSession_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device9_CreateShaderCacheSession: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvSession);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device9_CreateShaderCacheSession _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device9_CreateShaderCacheSession, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device9_CreateShaderCacheSession);

    if (ctx->id3d12device9_dispatch_overrides && ctx->id3d12device9_dispatch_overrides->CreateShaderCacheSession) {
        args.ret = ctx->id3d12device9_dispatch_overrides->CreateShaderCacheSession(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.riid, args.ppvSession);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvSession && *args.ppvSession)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvSession, *args.ppvSession,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvSession)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvSession);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device9_CreateShaderCacheSession_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device9::ShaderCacheControl
 */

struct npt_command_ID3D12Device9_ShaderCacheControl {
    void *_self;
    D3D12_SHADER_CACHE_KIND_FLAGS Kinds;
    D3D12_SHADER_CACHE_CONTROL_FLAGS Control;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device9_ShaderCacheControl_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D12Device9_ShaderCacheControl *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_SHADER_CACHE_KIND_FLAGS(dec, &args->Kinds);
    npt_decode_D3D12_SHADER_CACHE_CONTROL_FLAGS(dec, &args->Control);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device9_ShaderCacheControl_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device9_ShaderCacheControl *args)
{
}

static inline void
npt_encode_ID3D12Device9_ShaderCacheControl_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D12Device9_ShaderCacheControl *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                 NPT_METHOD_ID3D12Device9_ShaderCacheControl),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device9_ShaderCacheControl(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D12Device9_ShaderCacheControl args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device9_ShaderCacheControl_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE9);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device9_ShaderCacheControl on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device9_ShaderCacheControl_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device9_ShaderCacheControl: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device9_ShaderCacheControl _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device9_ShaderCacheControl, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device9_ShaderCacheControl);

    if (ctx->id3d12device9_dispatch_overrides && ctx->id3d12device9_dispatch_overrides->ShaderCacheControl) {
        args.ret = ctx->id3d12device9_dispatch_overrides->ShaderCacheControl(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Kinds, args.Control);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device9_ShaderCacheControl_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device9::CreateCommandQueue1
 */

struct npt_command_ID3D12Device9_CreateCommandQueue1 {
    void *_self;
    const D3D12_COMMAND_QUEUE_DESC * pDesc;
    const IID * CreatorID;
    const IID * riid;
    void ** ppCommandQueue;
    /* Shadow: the guest-allocated id for ppCommandQueue arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandQueue -> *ppCommandQueue} in the
     * context object table. */
    uint64_t _guest_id_ppCommandQueue;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device9_CreateCommandQueue1_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D12Device9_CreateCommandQueue1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_COMMAND_QUEUE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_COMMAND_QUEUE_DESC(dec, (D3D12_COMMAND_QUEUE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->CreatorID = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->CreatorID) return;
        npt_decode_IID(dec, (IID *)args->CreatorID);
    } else {
        args->CreatorID = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCommandQueue = _gid;
        args->ppCommandQueue = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCommandQueue) return;
        *args->ppCommandQueue = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device9_CreateCommandQueue1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device9_CreateCommandQueue1 *args)
{
}

static inline void
npt_encode_ID3D12Device9_CreateCommandQueue1_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D12Device9_CreateCommandQueue1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device9,
                                 NPT_METHOD_ID3D12Device9_CreateCommandQueue1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCommandQueue (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device9_CreateCommandQueue1(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D12Device9_CreateCommandQueue1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device9_CreateCommandQueue1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE9);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device9_CreateCommandQueue1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandQueue);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device9_CreateCommandQueue1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device9_CreateCommandQueue1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandQueue);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device9_CreateCommandQueue1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device9_CreateCommandQueue1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device9_CreateCommandQueue1);

    if (ctx->id3d12device9_dispatch_overrides && ctx->id3d12device9_dispatch_overrides->CreateCommandQueue1) {
        args.ret = ctx->id3d12device9_dispatch_overrides->CreateCommandQueue1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.CreatorID, args.riid, args.ppCommandQueue);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCommandQueue && *args.ppCommandQueue)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCommandQueue, *args.ppCommandQueue,
            npt_object_type_from_iid(args.CreatorID));
    else if (args.ppCommandQueue)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandQueue);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device9_CreateCommandQueue1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device10::CreateCommittedResource3
 */

struct npt_command_ID3D12Device10_CreateCommittedResource3 {
    void *_self;
    const D3D12_HEAP_PROPERTIES * pHeapProperties;
    D3D12_HEAP_FLAGS HeapFlags;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_BARRIER_LAYOUT InitialLayout;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    UINT32 NumCastableFormats;
    const DXGI_FORMAT * pCastableFormats;
    const IID * riidResource;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device10_CreateCommittedResource3_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D12Device10_CreateCommittedResource3 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pHeapProperties = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_HEAP_PROPERTIES));
        if (!args->pHeapProperties) return;
        npt_decode_D3D12_HEAP_PROPERTIES(dec, (D3D12_HEAP_PROPERTIES *)args->pHeapProperties);
    } else {
        args->pHeapProperties = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_HEAP_FLAGS(dec, &args->HeapFlags);
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC1));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC1(dec, (D3D12_RESOURCE_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_BARRIER_LAYOUT(dec, &args->InitialLayout);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pProtectedSession = (ID3D12ProtectedResourceSession *)npt_object_from_id(_id);
    }
    npt_decode_UINT32(dec, &args->NumCastableFormats);
    uint64_t _cnt_pCastableFormats = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pCastableFormats = npt_decode_array_count_unchecked(dec);
        args->pCastableFormats = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_FORMAT), _cnt_pCastableFormats);
        if (!args->pCastableFormats) return;
        npt_decode_DXGI_FORMAT_array(dec, (DXGI_FORMAT *)args->pCastableFormats, _cnt_pCastableFormats);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pCastableFormats = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riidResource = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riidResource) return;
        npt_decode_IID(dec, (IID *)args->riidResource);
    } else {
        args->riidResource = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pCastableFormats && _cnt_pCastableFormats < (uint64_t)(args->NumCastableFormats)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */










}

static inline void
npt_replace_ID3D12Device10_CreateCommittedResource3_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device10_CreateCommittedResource3 *args)
{
    args->pProtectedSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pProtectedSession, NPT_OBJECT_TYPE_ID3D12PROTECTEDRESOURCESESSION);
}

static inline void
npt_encode_ID3D12Device10_CreateCommittedResource3_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D12Device10_CreateCommittedResource3 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                 NPT_METHOD_ID3D12Device10_CreateCommittedResource3),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device10_CreateCommittedResource3(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D12Device10_CreateCommittedResource3 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device10_CreateCommittedResource3_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE10);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device10_CreateCommittedResource3 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device10_CreateCommittedResource3_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device10_CreateCommittedResource3: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device10_CreateCommittedResource3 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device10_CreateCommittedResource3, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device10_CreateCommittedResource3);

    if (ctx->id3d12device10_dispatch_overrides && ctx->id3d12device10_dispatch_overrides->CreateCommittedResource3) {
        args.ret = ctx->id3d12device10_dispatch_overrides->CreateCommittedResource3(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHeapProperties, args.HeapFlags, args.pDesc, args.InitialLayout, args.pOptimizedClearValue, args.pProtectedSession, args.NumCastableFormats, args.pCastableFormats, args.riidResource, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riidResource));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device10_CreateCommittedResource3_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device10::CreatePlacedResource2
 */

struct npt_command_ID3D12Device10_CreatePlacedResource2 {
    void *_self;
    ID3D12Heap * pHeap;
    UINT64 HeapOffset;
    const D3D12_RESOURCE_DESC1 * pDesc;
    D3D12_BARRIER_LAYOUT InitialLayout;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    UINT32 NumCastableFormats;
    const DXGI_FORMAT * pCastableFormats;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device10_CreatePlacedResource2_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12Device10_CreatePlacedResource2 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pHeap = (ID3D12Heap *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->HeapOffset);
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC1));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC1(dec, (D3D12_RESOURCE_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_BARRIER_LAYOUT(dec, &args->InitialLayout);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    npt_decode_UINT32(dec, &args->NumCastableFormats);
    uint64_t _cnt_pCastableFormats = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pCastableFormats = npt_decode_array_count_unchecked(dec);
        args->pCastableFormats = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_FORMAT), _cnt_pCastableFormats);
        if (!args->pCastableFormats) return;
        npt_decode_DXGI_FORMAT_array(dec, (DXGI_FORMAT *)args->pCastableFormats, _cnt_pCastableFormats);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pCastableFormats = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pCastableFormats && _cnt_pCastableFormats < (uint64_t)(args->NumCastableFormats)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */









}

static inline void
npt_replace_ID3D12Device10_CreatePlacedResource2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device10_CreatePlacedResource2 *args)
{
    args->pHeap = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pHeap, NPT_OBJECT_TYPE_ID3D12HEAP);
}

static inline void
npt_encode_ID3D12Device10_CreatePlacedResource2_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12Device10_CreatePlacedResource2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                 NPT_METHOD_ID3D12Device10_CreatePlacedResource2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device10_CreatePlacedResource2(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12Device10_CreatePlacedResource2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device10_CreatePlacedResource2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE10);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device10_CreatePlacedResource2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device10_CreatePlacedResource2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device10_CreatePlacedResource2: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device10_CreatePlacedResource2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device10_CreatePlacedResource2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device10_CreatePlacedResource2);

    if (ctx->id3d12device10_dispatch_overrides && ctx->id3d12device10_dispatch_overrides->CreatePlacedResource2) {
        args.ret = ctx->id3d12device10_dispatch_overrides->CreatePlacedResource2(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHeap, args.HeapOffset, args.pDesc, args.InitialLayout, args.pOptimizedClearValue, args.NumCastableFormats, args.pCastableFormats, args.riid, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device10_CreatePlacedResource2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device10::CreateReservedResource2
 */

struct npt_command_ID3D12Device10_CreateReservedResource2 {
    void *_self;
    const D3D12_RESOURCE_DESC * pDesc;
    D3D12_BARRIER_LAYOUT InitialLayout;
    const D3D12_CLEAR_VALUE * pOptimizedClearValue;
    ID3D12ProtectedResourceSession * pProtectedSession;
    UINT32 NumCastableFormats;
    const DXGI_FORMAT * pCastableFormats;
    const IID * riid;
    void ** ppvResource;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device10_CreateReservedResource2_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12Device10_CreateReservedResource2 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RESOURCE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RESOURCE_DESC(dec, (D3D12_RESOURCE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_BARRIER_LAYOUT(dec, &args->InitialLayout);
    if (npt_decode_simple_pointer(dec)) {
        args->pOptimizedClearValue = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CLEAR_VALUE));
        if (!args->pOptimizedClearValue) return;
        npt_decode_D3D12_CLEAR_VALUE(dec, (D3D12_CLEAR_VALUE *)args->pOptimizedClearValue);
    } else {
        args->pOptimizedClearValue = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pProtectedSession = (ID3D12ProtectedResourceSession *)npt_object_from_id(_id);
    }
    npt_decode_UINT32(dec, &args->NumCastableFormats);
    uint64_t _cnt_pCastableFormats = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pCastableFormats = npt_decode_array_count_unchecked(dec);
        args->pCastableFormats = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_FORMAT), _cnt_pCastableFormats);
        if (!args->pCastableFormats) return;
        npt_decode_DXGI_FORMAT_array(dec, (DXGI_FORMAT *)args->pCastableFormats, _cnt_pCastableFormats);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pCastableFormats = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pCastableFormats && _cnt_pCastableFormats < (uint64_t)(args->NumCastableFormats)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */








}

static inline void
npt_replace_ID3D12Device10_CreateReservedResource2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device10_CreateReservedResource2 *args)
{
    args->pProtectedSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pProtectedSession, NPT_OBJECT_TYPE_ID3D12PROTECTEDRESOURCESESSION);
}

static inline void
npt_encode_ID3D12Device10_CreateReservedResource2_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12Device10_CreateReservedResource2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device10,
                                 NPT_METHOD_ID3D12Device10_CreateReservedResource2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device10_CreateReservedResource2(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12Device10_CreateReservedResource2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device10_CreateReservedResource2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE10);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device10_CreateReservedResource2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device10_CreateReservedResource2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device10_CreateReservedResource2: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device10_CreateReservedResource2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device10_CreateReservedResource2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device10_CreateReservedResource2);

    if (ctx->id3d12device10_dispatch_overrides && ctx->id3d12device10_dispatch_overrides->CreateReservedResource2) {
        args.ret = ctx->id3d12device10_dispatch_overrides->CreateReservedResource2(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.InitialLayout, args.pOptimizedClearValue, args.pProtectedSession, args.NumCastableFormats, args.pCastableFormats, args.riid, args.ppvResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvResource)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvResource);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device10_CreateReservedResource2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device11::CreateSampler2
 */

struct npt_command_ID3D12Device11_CreateSampler2 {
    void *_self;
    const D3D12_SAMPLER_DESC2 * pDesc;
    D3D12_CPU_DESCRIPTOR_HANDLE DestDescriptor;
};

static inline void
npt_decode_ID3D12Device11_CreateSampler2_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D12Device11_CreateSampler2 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_SAMPLER_DESC2));
        if (!args->pDesc) return;
        npt_decode_D3D12_SAMPLER_DESC2(dec, (D3D12_SAMPLER_DESC2 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DestDescriptor);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Device11_CreateSampler2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device11_CreateSampler2 *args)
{
}

static inline void
npt_encode_ID3D12Device11_CreateSampler2_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D12Device11_CreateSampler2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device11,
                                 NPT_METHOD_ID3D12Device11_CreateSampler2),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Device11_CreateSampler2(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D12Device11_CreateSampler2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device11_CreateSampler2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE11);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device11_CreateSampler2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device11_CreateSampler2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device11_CreateSampler2: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device11_CreateSampler2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device11_CreateSampler2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device11_CreateSampler2);

    if (ctx->id3d12device11_dispatch_overrides && ctx->id3d12device11_dispatch_overrides->CreateSampler2) {
        ctx->id3d12device11_dispatch_overrides->CreateSampler2(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc, args.DestDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device11_CreateSampler2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device12::GetResourceAllocationInfo3
 */

struct npt_command_ID3D12Device12_GetResourceAllocationInfo3 {
    void *_self;
    UINT visibleMask;
    UINT numResourceDescs;
    const D3D12_RESOURCE_DESC1 * pResourceDescs;
    const UINT32 * pNumCastableFormats;
    const DXGI_FORMAT ** ppCastableFormats;
    D3D12_RESOURCE_ALLOCATION_INFO1 * pResourceAllocationInfo1;
    D3D12_RESOURCE_ALLOCATION_INFO ret;
};

static inline void
npt_decode_ID3D12Device12_GetResourceAllocationInfo3_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12Device12_GetResourceAllocationInfo3 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->visibleMask);
    npt_decode_UINT(dec, &args->numResourceDescs);
    uint64_t _cnt_pResourceDescs = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pResourceDescs = npt_decode_array_count_unchecked(dec);
        args->pResourceDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_DESC1), _cnt_pResourceDescs);
        if (!args->pResourceDescs) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pResourceDescs; _i++)
            npt_decode_D3D12_RESOURCE_DESC1(dec, (D3D12_RESOURCE_DESC1 *)&args->pResourceDescs[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pResourceDescs = NULL;
    }
    uint64_t _cnt_pNumCastableFormats = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pNumCastableFormats = npt_decode_array_count_unchecked(dec);
        args->pNumCastableFormats = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT32), _cnt_pNumCastableFormats);
        if (!args->pNumCastableFormats) return;
        npt_decode_UINT32_array(dec, (UINT32 *)args->pNumCastableFormats, _cnt_pNumCastableFormats);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pNumCastableFormats = NULL;
    }
    /* ppCastableFormats: unsized optional, skip */
    (void)npt_decode_array_count_unchecked(dec);
    (void)args->ppCastableFormats;

    /* A counted array must carry every element the callee will read. */
    if (_cnt_pResourceDescs && _cnt_pResourceDescs < (uint64_t)(args->numResourceDescs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pNumCastableFormats && _cnt_pNumCastableFormats < (uint64_t)(args->numResourceDescs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





    if (args->numResourceDescs) {
        args->pResourceAllocationInfo1 = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_ALLOCATION_INFO1), args->numResourceDescs);
        if (!args->pResourceAllocationInfo1) return;
        memset(args->pResourceAllocationInfo1, 0, sizeof(D3D12_RESOURCE_ALLOCATION_INFO1) * args->numResourceDescs);
    } else {
        args->pResourceAllocationInfo1 = NULL;
    }
}

static inline void
npt_replace_ID3D12Device12_GetResourceAllocationInfo3_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device12_GetResourceAllocationInfo3 *args)
{
}

static inline void
npt_encode_ID3D12Device12_GetResourceAllocationInfo3_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12Device12_GetResourceAllocationInfo3 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device12,
                                 NPT_METHOD_ID3D12Device12_GetResourceAllocationInfo3),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_D3D12_RESOURCE_ALLOCATION_INFO(enc, &args->ret);
    if (args->pResourceAllocationInfo1) {
        npt_encode_array_count(enc, args->numResourceDescs);
        for (uint32_t _i = 0; _i < (uint32_t)(args->numResourceDescs); _i++)
            npt_encode_D3D12_RESOURCE_ALLOCATION_INFO1(enc, &args->pResourceAllocationInfo1[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D12Device12_GetResourceAllocationInfo3(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12Device12_GetResourceAllocationInfo3 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device12_GetResourceAllocationInfo3_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE12);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device12_GetResourceAllocationInfo3 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device12_GetResourceAllocationInfo3_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device12_GetResourceAllocationInfo3: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device12_GetResourceAllocationInfo3 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device12_GetResourceAllocationInfo3, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device12_GetResourceAllocationInfo3);

    if (ctx->id3d12device12_dispatch_overrides && ctx->id3d12device12_dispatch_overrides->GetResourceAllocationInfo3) {
        args.ret = ctx->id3d12device12_dispatch_overrides->GetResourceAllocationInfo3(ctx, &args, _original);
    } else {
        /* COM x64 aggregate-return ABI: the hidden pointer (&args.ret)
         * rides in the call args; the returned pointer aliases it. */
        (void)_original(args._self, &args.ret, args.visibleMask, args.numResourceDescs, args.pResourceDescs, args.pNumCastableFormats, args.ppCastableFormats, args.pResourceAllocationInfo1);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device12_GetResourceAllocationInfo3_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device13::OpenExistingHeapFromAddress1
 */

struct npt_command_ID3D12Device13_OpenExistingHeapFromAddress1 {
    void *_self;
    const void * pAddress;
    SIZE_T size;
    const IID * riid;
    void ** ppvHeap;
    /* Shadow: the guest-allocated id for ppvHeap arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvHeap -> *ppvHeap} in the
     * context object table. */
    uint64_t _guest_id_ppvHeap;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device13_OpenExistingHeapFromAddress1_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12Device13_OpenExistingHeapFromAddress1 *args)
{
    /* Decode input parameters from the wire */
    /* ERROR: pAddress (void) is not serializable */
    return;
    npt_decode_SIZE_T(dec, &args->size);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvHeap = _gid;
        args->ppvHeap = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvHeap) return;
        *args->ppvHeap = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12Device13_OpenExistingHeapFromAddress1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device13_OpenExistingHeapFromAddress1 *args)
{
}

static inline void
npt_encode_ID3D12Device13_OpenExistingHeapFromAddress1_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12Device13_OpenExistingHeapFromAddress1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device13,
                                 NPT_METHOD_ID3D12Device13_OpenExistingHeapFromAddress1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvHeap (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device13_OpenExistingHeapFromAddress1(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12Device13_OpenExistingHeapFromAddress1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device13_OpenExistingHeapFromAddress1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE13);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device13_OpenExistingHeapFromAddress1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device13_OpenExistingHeapFromAddress1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device13_OpenExistingHeapFromAddress1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device13_OpenExistingHeapFromAddress1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device13_OpenExistingHeapFromAddress1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device13_OpenExistingHeapFromAddress1);

    if (ctx->id3d12device13_dispatch_overrides && ctx->id3d12device13_dispatch_overrides->OpenExistingHeapFromAddress1) {
        args.ret = ctx->id3d12device13_dispatch_overrides->OpenExistingHeapFromAddress1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pAddress, args.size, args.riid, args.ppvHeap);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvHeap && *args.ppvHeap)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvHeap, *args.ppvHeap,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvHeap)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvHeap);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device13_OpenExistingHeapFromAddress1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Device14::CreateRootSignatureFromSubobjectInLibrary
 */

struct npt_command_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary {
    void *_self;
    UINT nodeMask;
    const void * pLibraryBlob;
    SIZE_T blobLengthInBytes;
    const WCHAR * subobjectName;
    const IID * riid;
    void ** ppvRootSignature;
    /* Shadow: the guest-allocated id for ppvRootSignature arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvRootSignature -> *ppvRootSignature} in the
     * context object table. */
    uint64_t _guest_id_ppvRootSignature;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_args_temp(struct npt_cs_decoder *dec,
                                                                              struct npt_command_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->nodeMask);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pLibraryBlob = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pLibraryBlob) return;
            npt_decode_blob_array(dec, (void *)args->pLibraryBlob, _blob_size);
        } else {
            args->pLibraryBlob = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->blobLengthInBytes);
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->subobjectName = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->subobjectName) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->subobjectName, _wstr_size);
        } else {
            args->subobjectName = NULL;
        }
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvRootSignature = _gid;
        args->ppvRootSignature = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvRootSignature) return;
        *args->ppvRootSignature = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary *args)
{
}

static inline void
npt_encode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_reply(struct npt_cs_encoder *enc,
                                                                          const struct npt_command_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Device14,
                                 NPT_METHOD_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvRootSignature (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary(struct npt_dispatch_context *ctx,
                                                                      uint32_t cmd_flags,
                                                                      uint64_t object_id)
{
    struct npt_command_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICE14);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvRootSignature);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvRootSignature);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary);

    if (ctx->id3d12device14_dispatch_overrides && ctx->id3d12device14_dispatch_overrides->CreateRootSignatureFromSubobjectInLibrary) {
        args.ret = ctx->id3d12device14_dispatch_overrides->CreateRootSignatureFromSubobjectInLibrary(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.nodeMask, args.pLibraryBlob, args.blobLengthInBytes, args.subobjectName, args.riid, args.ppvRootSignature);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvRootSignature && *args.ppvRootSignature)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvRootSignature, *args.ppvRootSignature,
            npt_object_type_from_iid(args.riid));
    else if (args.ppvRootSignature)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppvRootSignature);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Device14_CreateRootSignatureFromSubobjectInLibrary_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D12DEVICE_H */

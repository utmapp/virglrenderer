/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiobject.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGIOBJECT_H
#define NPT_PROTOCOL_HOST_IDXGIOBJECT_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIObject::SetPrivateData
 */

struct npt_command_IDXGIObject_SetPrivateData {
    void *_self;
    const GUID * Name;
    UINT DataSize;
    const void * pData;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIObject_SetPrivateData_args_temp(struct npt_cs_decoder *dec,
                                                struct npt_command_IDXGIObject_SetPrivateData *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->Name = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->Name) return;
        npt_decode_GUID(dec, (GUID *)args->Name);
    } else {
        args->Name = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGIObject_SetPrivateData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIObject_SetPrivateData *args)
{
}

static inline void
npt_encode_IDXGIObject_SetPrivateData_reply(struct npt_cs_encoder *enc,
                                            const struct npt_command_IDXGIObject_SetPrivateData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_SetPrivateData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIObject_SetPrivateData(struct npt_dispatch_context *ctx,
                                        uint32_t cmd_flags,
                                        uint64_t object_id)
{
    struct npt_command_IDXGIObject_SetPrivateData args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIObject_SetPrivateData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_SetPrivateData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIObject_SetPrivateData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_SetPrivateData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIObject_SetPrivateData _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIObject_SetPrivateData, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIObject_SetPrivateData);

    if (ctx->idxgiobject_dispatch_overrides && ctx->idxgiobject_dispatch_overrides->SetPrivateData) {
        args.ret = ctx->idxgiobject_dispatch_overrides->SetPrivateData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Name, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIObject_SetPrivateData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIObject::SetPrivateDataInterface
 */

struct npt_command_IDXGIObject_SetPrivateDataInterface {
    void *_self;
    const GUID * Name;
    const IUnknown * pUnknown;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIObject_SetPrivateDataInterface_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_IDXGIObject_SetPrivateDataInterface *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->Name = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->Name) return;
        npt_decode_GUID(dec, (GUID *)args->Name);
    } else {
        args->Name = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pUnknown = (IUnknown *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIObject_SetPrivateDataInterface_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIObject_SetPrivateDataInterface *args)
{
    args->pUnknown = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pUnknown, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIObject_SetPrivateDataInterface_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_IDXGIObject_SetPrivateDataInterface *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_SetPrivateDataInterface),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIObject_SetPrivateDataInterface(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_IDXGIObject_SetPrivateDataInterface args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIObject_SetPrivateDataInterface_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_SetPrivateDataInterface on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIObject_SetPrivateDataInterface_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_SetPrivateDataInterface: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIObject_SetPrivateDataInterface _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIObject_SetPrivateDataInterface, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIObject_SetPrivateDataInterface);

    if (ctx->idxgiobject_dispatch_overrides && ctx->idxgiobject_dispatch_overrides->SetPrivateDataInterface) {
        args.ret = ctx->idxgiobject_dispatch_overrides->SetPrivateDataInterface(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Name, args.pUnknown);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIObject_SetPrivateDataInterface_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIObject::GetPrivateData
 */

struct npt_command_IDXGIObject_GetPrivateData {
    void *_self;
    const GUID * Name;
    UINT * pDataSize;
    void * pData;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIObject_GetPrivateData_args_temp(struct npt_cs_decoder *dec,
                                                struct npt_command_IDXGIObject_GetPrivateData *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->Name = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->Name) return;
        npt_decode_GUID(dec, (GUID *)args->Name);
    } else {
        args->Name = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDataSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pDataSize) return;
        npt_decode_UINT(dec, (UINT *)args->pDataSize);
    } else {
        args->pDataSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */


    if (*args->pDataSize) {
        args->pData = npt_cs_decoder_alloc_temp(dec, *args->pDataSize);
        if (!args->pData) return;
        memset(args->pData, 0, *args->pDataSize);
    } else {
        args->pData = NULL;
    }
}

static inline void
npt_replace_IDXGIObject_GetPrivateData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIObject_GetPrivateData *args)
{
}

static inline void
npt_encode_IDXGIObject_GetPrivateData_reply(struct npt_cs_encoder *enc,
                                            const struct npt_command_IDXGIObject_GetPrivateData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_GetPrivateData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDataSize))
        npt_encode_UINT(enc, args->pDataSize);
    if (args->pData) {
        npt_encode_array_count(enc, *args->pDataSize);
        npt_encode_blob_array(enc, args->pData, *args->pDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_IDXGIObject_GetPrivateData(struct npt_dispatch_context *ctx,
                                        uint32_t cmd_flags,
                                        uint64_t object_id)
{
    struct npt_command_IDXGIObject_GetPrivateData args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIObject_GetPrivateData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_GetPrivateData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIObject_GetPrivateData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_GetPrivateData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIObject_GetPrivateData _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIObject_GetPrivateData, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIObject_GetPrivateData);

    if (ctx->idxgiobject_dispatch_overrides && ctx->idxgiobject_dispatch_overrides->GetPrivateData) {
        args.ret = ctx->idxgiobject_dispatch_overrides->GetPrivateData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Name, args.pDataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIObject_GetPrivateData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIObject::GetParent
 */

struct npt_command_IDXGIObject_GetParent {
    void *_self;
    const IID * riid;
    void ** ppParent;
    /* Shadow: the guest-allocated id for ppParent arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppParent -> *ppParent} in the
     * context object table. */
    uint64_t _guest_id_ppParent;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIObject_GetParent_args_temp(struct npt_cs_decoder *dec,
                                           struct npt_command_IDXGIObject_GetParent *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppParent = _gid;
        args->ppParent = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppParent) return;
        *args->ppParent = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIObject_GetParent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIObject_GetParent *args)
{
}

static inline void
npt_encode_IDXGIObject_GetParent_reply(struct npt_cs_encoder *enc,
                                       const struct npt_command_IDXGIObject_GetParent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIObject,
                                 NPT_METHOD_IDXGIObject_GetParent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppParent (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIObject_GetParent(struct npt_dispatch_context *ctx,
                                   uint32_t cmd_flags,
                                   uint64_t object_id)
{
    struct npt_command_IDXGIObject_GetParent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIObject_GetParent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_GetParent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIObject_GetParent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIObject_GetParent: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIObject_GetParent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIObject_GetParent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIObject_GetParent);

    if (ctx->idxgiobject_dispatch_overrides && ctx->idxgiobject_dispatch_overrides->GetParent) {
        args.ret = ctx->idxgiobject_dispatch_overrides->GetParent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.riid, args.ppParent);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppParent && *args.ppParent)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppParent, *args.ppParent,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIObject_GetParent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGIOBJECT_H */

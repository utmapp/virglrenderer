/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgioutput.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGIOUTPUT_H
#define NPT_PROTOCOL_HOST_IDXGIOUTPUT_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIOutput::GetDesc
 */

struct npt_command_IDXGIOutput_GetDesc {
    void *_self;
    DXGI_OUTPUT_DESC * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_GetDesc_args_temp(struct npt_cs_decoder *dec,
                                         struct npt_command_IDXGIOutput_GetDesc *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_OUTPUT_DESC));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_OUTPUT_DESC));
}

static inline void
npt_replace_IDXGIOutput_GetDesc_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_GetDesc *args)
{
}

static inline void
npt_encode_IDXGIOutput_GetDesc_reply(struct npt_cs_encoder *enc,
                                     const struct npt_command_IDXGIOutput_GetDesc *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetDesc),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_OUTPUT_DESC(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGIOutput_GetDesc(struct npt_dispatch_context *ctx,
                                 uint32_t cmd_flags,
                                 uint64_t object_id)
{
    struct npt_command_IDXGIOutput_GetDesc args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_GetDesc_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetDesc on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_GetDesc_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetDesc: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_GetDesc _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_GetDesc, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_GetDesc);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->GetDesc) {
        args.ret = ctx->idxgioutput_dispatch_overrides->GetDesc(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_GetDesc_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::GetDisplayModeList
 */

struct npt_command_IDXGIOutput_GetDisplayModeList {
    void *_self;
    DXGI_FORMAT EnumFormat;
    UINT Flags;
    UINT * pNumModes;
    DXGI_MODE_DESC * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_GetDisplayModeList_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_IDXGIOutput_GetDisplayModeList *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->EnumFormat);
    npt_decode_UINT(dec, &args->Flags);
    if (npt_decode_simple_pointer(dec)) {
        args->pNumModes = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumModes) return;
        npt_decode_UINT(dec, (UINT *)args->pNumModes);
    } else {
        args->pNumModes = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */



    if (*args->pNumModes) {
        args->pDesc = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_MODE_DESC), *args->pNumModes);
        if (!args->pDesc) return;
        memset(args->pDesc, 0, sizeof(DXGI_MODE_DESC) * *args->pNumModes);
    } else {
        args->pDesc = NULL;
    }
}

static inline void
npt_replace_IDXGIOutput_GetDisplayModeList_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_GetDisplayModeList *args)
{
}

static inline void
npt_encode_IDXGIOutput_GetDisplayModeList_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_IDXGIOutput_GetDisplayModeList *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetDisplayModeList),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumModes))
        npt_encode_UINT(enc, args->pNumModes);
    /* pDesc: array sized by output count 'pNumModes'.  On
     * failure the host may have written the required count but left the
     * array contents unpopulated, so only encode elements on success. */
    if ((HRESULT)args->ret >= 0) {
        if (args->pDesc) {
        npt_encode_array_count(enc, *args->pNumModes);
        for (uint32_t _i = 0; _i < (uint32_t)(*args->pNumModes); _i++)
            npt_encode_DXGI_MODE_DESC(enc, &args->pDesc[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_IDXGIOutput_GetDisplayModeList(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_IDXGIOutput_GetDisplayModeList args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_GetDisplayModeList_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetDisplayModeList on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_GetDisplayModeList_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetDisplayModeList: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_GetDisplayModeList _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_GetDisplayModeList, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_GetDisplayModeList);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->GetDisplayModeList) {
        args.ret = ctx->idxgioutput_dispatch_overrides->GetDisplayModeList(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.EnumFormat, args.Flags, args.pNumModes, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_GetDisplayModeList_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::FindClosestMatchingMode
 */

struct npt_command_IDXGIOutput_FindClosestMatchingMode {
    void *_self;
    const DXGI_MODE_DESC * pModeToMatch;
    DXGI_MODE_DESC * pClosestMatch;
    IUnknown * pConcernedDevice;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_FindClosestMatchingMode_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_IDXGIOutput_FindClosestMatchingMode *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pModeToMatch = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MODE_DESC));
        if (!args->pModeToMatch) return;
        npt_decode_DXGI_MODE_DESC(dec, (DXGI_MODE_DESC *)args->pModeToMatch);
    } else {
        args->pModeToMatch = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pConcernedDevice = (IUnknown *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

    args->pClosestMatch = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MODE_DESC));
    if (!args->pClosestMatch) return;
    memset(args->pClosestMatch, 0, sizeof(DXGI_MODE_DESC));

}

static inline void
npt_replace_IDXGIOutput_FindClosestMatchingMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_FindClosestMatchingMode *args)
{
    args->pConcernedDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pConcernedDevice, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIOutput_FindClosestMatchingMode_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_IDXGIOutput_FindClosestMatchingMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_FindClosestMatchingMode),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pClosestMatch))
        npt_encode_DXGI_MODE_DESC(enc, args->pClosestMatch);
}

static inline void
npt_dispatch_IDXGIOutput_FindClosestMatchingMode(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_IDXGIOutput_FindClosestMatchingMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_FindClosestMatchingMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_FindClosestMatchingMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_FindClosestMatchingMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_FindClosestMatchingMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_FindClosestMatchingMode _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_FindClosestMatchingMode, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_FindClosestMatchingMode);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->FindClosestMatchingMode) {
        args.ret = ctx->idxgioutput_dispatch_overrides->FindClosestMatchingMode(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pModeToMatch, args.pClosestMatch, args.pConcernedDevice);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_FindClosestMatchingMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::WaitForVBlank
 */

struct npt_command_IDXGIOutput_WaitForVBlank {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_WaitForVBlank_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_IDXGIOutput_WaitForVBlank *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIOutput_WaitForVBlank_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_WaitForVBlank *args)
{
}

static inline void
npt_encode_IDXGIOutput_WaitForVBlank_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_IDXGIOutput_WaitForVBlank *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_WaitForVBlank),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput_WaitForVBlank(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_IDXGIOutput_WaitForVBlank args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_WaitForVBlank_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_WaitForVBlank on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_WaitForVBlank_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_WaitForVBlank: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_WaitForVBlank _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_WaitForVBlank, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_WaitForVBlank);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->WaitForVBlank) {
        args.ret = ctx->idxgioutput_dispatch_overrides->WaitForVBlank(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_WaitForVBlank_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::TakeOwnership
 */

struct npt_command_IDXGIOutput_TakeOwnership {
    void *_self;
    IUnknown * pDevice;
    BOOL Exclusive;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_TakeOwnership_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_IDXGIOutput_TakeOwnership *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->Exclusive);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIOutput_TakeOwnership_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_TakeOwnership *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIOutput_TakeOwnership_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_IDXGIOutput_TakeOwnership *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_TakeOwnership),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput_TakeOwnership(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_IDXGIOutput_TakeOwnership args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_TakeOwnership_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_TakeOwnership on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_TakeOwnership_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_TakeOwnership: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_TakeOwnership _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_TakeOwnership, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_TakeOwnership);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->TakeOwnership) {
        args.ret = ctx->idxgioutput_dispatch_overrides->TakeOwnership(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDevice, args.Exclusive);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_TakeOwnership_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::ReleaseOwnership
 */

struct npt_command_IDXGIOutput_ReleaseOwnership {
    void *_self;
};

static inline void
npt_decode_IDXGIOutput_ReleaseOwnership_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_IDXGIOutput_ReleaseOwnership *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIOutput_ReleaseOwnership_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_ReleaseOwnership *args)
{
}

static inline void
npt_encode_IDXGIOutput_ReleaseOwnership_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_IDXGIOutput_ReleaseOwnership *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_ReleaseOwnership),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput_ReleaseOwnership(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_IDXGIOutput_ReleaseOwnership args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_ReleaseOwnership_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_ReleaseOwnership on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_ReleaseOwnership_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_ReleaseOwnership: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_ReleaseOwnership _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_ReleaseOwnership, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_ReleaseOwnership);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->ReleaseOwnership) {
        ctx->idxgioutput_dispatch_overrides->ReleaseOwnership(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_ReleaseOwnership_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::GetGammaControlCapabilities
 */

struct npt_command_IDXGIOutput_GetGammaControlCapabilities {
    void *_self;
    DXGI_GAMMA_CONTROL_CAPABILITIES * pGammaCaps;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_GetGammaControlCapabilities_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_IDXGIOutput_GetGammaControlCapabilities *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pGammaCaps = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_GAMMA_CONTROL_CAPABILITIES));
    if (!args->pGammaCaps) return;
    memset(args->pGammaCaps, 0, sizeof(DXGI_GAMMA_CONTROL_CAPABILITIES));
}

static inline void
npt_replace_IDXGIOutput_GetGammaControlCapabilities_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_GetGammaControlCapabilities *args)
{
}

static inline void
npt_encode_IDXGIOutput_GetGammaControlCapabilities_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_IDXGIOutput_GetGammaControlCapabilities *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetGammaControlCapabilities),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pGammaCaps))
        npt_encode_DXGI_GAMMA_CONTROL_CAPABILITIES(enc, args->pGammaCaps);
}

static inline void
npt_dispatch_IDXGIOutput_GetGammaControlCapabilities(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_IDXGIOutput_GetGammaControlCapabilities args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_GetGammaControlCapabilities_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetGammaControlCapabilities on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_GetGammaControlCapabilities_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetGammaControlCapabilities: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_GetGammaControlCapabilities _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_GetGammaControlCapabilities, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_GetGammaControlCapabilities);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->GetGammaControlCapabilities) {
        args.ret = ctx->idxgioutput_dispatch_overrides->GetGammaControlCapabilities(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pGammaCaps);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_GetGammaControlCapabilities_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::SetGammaControl
 */

struct npt_command_IDXGIOutput_SetGammaControl {
    void *_self;
    const DXGI_GAMMA_CONTROL * pArray;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_SetGammaControl_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_IDXGIOutput_SetGammaControl *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pArray = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_GAMMA_CONTROL));
        if (!args->pArray) return;
        npt_decode_DXGI_GAMMA_CONTROL(dec, (DXGI_GAMMA_CONTROL *)args->pArray);
    } else {
        args->pArray = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIOutput_SetGammaControl_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_SetGammaControl *args)
{
}

static inline void
npt_encode_IDXGIOutput_SetGammaControl_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_IDXGIOutput_SetGammaControl *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_SetGammaControl),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput_SetGammaControl(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_IDXGIOutput_SetGammaControl args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_SetGammaControl_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_SetGammaControl on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_SetGammaControl_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_SetGammaControl: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_SetGammaControl _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_SetGammaControl, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_SetGammaControl);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->SetGammaControl) {
        args.ret = ctx->idxgioutput_dispatch_overrides->SetGammaControl(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pArray);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_SetGammaControl_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::GetGammaControl
 */

struct npt_command_IDXGIOutput_GetGammaControl {
    void *_self;
    DXGI_GAMMA_CONTROL * pArray;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_GetGammaControl_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_IDXGIOutput_GetGammaControl *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pArray = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_GAMMA_CONTROL));
    if (!args->pArray) return;
    memset(args->pArray, 0, sizeof(DXGI_GAMMA_CONTROL));
}

static inline void
npt_replace_IDXGIOutput_GetGammaControl_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_GetGammaControl *args)
{
}

static inline void
npt_encode_IDXGIOutput_GetGammaControl_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_IDXGIOutput_GetGammaControl *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetGammaControl),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pArray))
        npt_encode_DXGI_GAMMA_CONTROL(enc, args->pArray);
}

static inline void
npt_dispatch_IDXGIOutput_GetGammaControl(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_IDXGIOutput_GetGammaControl args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_GetGammaControl_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetGammaControl on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_GetGammaControl_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetGammaControl: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_GetGammaControl _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_GetGammaControl, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_GetGammaControl);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->GetGammaControl) {
        args.ret = ctx->idxgioutput_dispatch_overrides->GetGammaControl(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pArray);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_GetGammaControl_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::SetDisplaySurface
 */

struct npt_command_IDXGIOutput_SetDisplaySurface {
    void *_self;
    IDXGISurface * pScanoutSurface;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_SetDisplaySurface_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGIOutput_SetDisplaySurface *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pScanoutSurface = (IDXGISurface *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIOutput_SetDisplaySurface_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_SetDisplaySurface *args)
{
    args->pScanoutSurface = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pScanoutSurface, NPT_OBJECT_TYPE_IDXGISURFACE);
}

static inline void
npt_encode_IDXGIOutput_SetDisplaySurface_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGIOutput_SetDisplaySurface *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_SetDisplaySurface),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput_SetDisplaySurface(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGIOutput_SetDisplaySurface args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_SetDisplaySurface_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_SetDisplaySurface on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_SetDisplaySurface_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_SetDisplaySurface: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_SetDisplaySurface _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_SetDisplaySurface, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_SetDisplaySurface);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->SetDisplaySurface) {
        args.ret = ctx->idxgioutput_dispatch_overrides->SetDisplaySurface(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pScanoutSurface);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_SetDisplaySurface_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::GetDisplaySurfaceData
 */

struct npt_command_IDXGIOutput_GetDisplaySurfaceData {
    void *_self;
    IDXGISurface * pDestination;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_GetDisplaySurfaceData_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_IDXGIOutput_GetDisplaySurfaceData *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDestination = (IDXGISurface *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIOutput_GetDisplaySurfaceData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_GetDisplaySurfaceData *args)
{
    args->pDestination = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDestination, NPT_OBJECT_TYPE_IDXGISURFACE);
}

static inline void
npt_encode_IDXGIOutput_GetDisplaySurfaceData_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_IDXGIOutput_GetDisplaySurfaceData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetDisplaySurfaceData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput_GetDisplaySurfaceData(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_IDXGIOutput_GetDisplaySurfaceData args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_GetDisplaySurfaceData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetDisplaySurfaceData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_GetDisplaySurfaceData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetDisplaySurfaceData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_GetDisplaySurfaceData _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_GetDisplaySurfaceData, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_GetDisplaySurfaceData);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->GetDisplaySurfaceData) {
        args.ret = ctx->idxgioutput_dispatch_overrides->GetDisplaySurfaceData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDestination);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_GetDisplaySurfaceData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput::GetFrameStatistics
 */

struct npt_command_IDXGIOutput_GetFrameStatistics {
    void *_self;
    DXGI_FRAME_STATISTICS * pStats;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput_GetFrameStatistics_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_IDXGIOutput_GetFrameStatistics *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pStats = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_FRAME_STATISTICS));
    if (!args->pStats) return;
    memset(args->pStats, 0, sizeof(DXGI_FRAME_STATISTICS));
}

static inline void
npt_replace_IDXGIOutput_GetFrameStatistics_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput_GetFrameStatistics *args)
{
}

static inline void
npt_encode_IDXGIOutput_GetFrameStatistics_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_IDXGIOutput_GetFrameStatistics *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput,
                                 NPT_METHOD_IDXGIOutput_GetFrameStatistics),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pStats))
        npt_encode_DXGI_FRAME_STATISTICS(enc, args->pStats);
}

static inline void
npt_dispatch_IDXGIOutput_GetFrameStatistics(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_IDXGIOutput_GetFrameStatistics args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput_GetFrameStatistics_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetFrameStatistics on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput_GetFrameStatistics_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput_GetFrameStatistics: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput_GetFrameStatistics _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput_GetFrameStatistics, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput_GetFrameStatistics);

    if (ctx->idxgioutput_dispatch_overrides && ctx->idxgioutput_dispatch_overrides->GetFrameStatistics) {
        args.ret = ctx->idxgioutput_dispatch_overrides->GetFrameStatistics(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pStats);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput_GetFrameStatistics_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput1::GetDisplayModeList1
 */

struct npt_command_IDXGIOutput1_GetDisplayModeList1 {
    void *_self;
    DXGI_FORMAT EnumFormat;
    UINT Flags;
    UINT * pNumModes;
    DXGI_MODE_DESC1 * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput1_GetDisplayModeList1_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_IDXGIOutput1_GetDisplayModeList1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->EnumFormat);
    npt_decode_UINT(dec, &args->Flags);
    if (npt_decode_simple_pointer(dec)) {
        args->pNumModes = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumModes) return;
        npt_decode_UINT(dec, (UINT *)args->pNumModes);
    } else {
        args->pNumModes = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */



    if (*args->pNumModes) {
        args->pDesc = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_MODE_DESC1), *args->pNumModes);
        if (!args->pDesc) return;
        memset(args->pDesc, 0, sizeof(DXGI_MODE_DESC1) * *args->pNumModes);
    } else {
        args->pDesc = NULL;
    }
}

static inline void
npt_replace_IDXGIOutput1_GetDisplayModeList1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput1_GetDisplayModeList1 *args)
{
}

static inline void
npt_encode_IDXGIOutput1_GetDisplayModeList1_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_IDXGIOutput1_GetDisplayModeList1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_GetDisplayModeList1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumModes))
        npt_encode_UINT(enc, args->pNumModes);
    /* pDesc: array sized by output count 'pNumModes'.  On
     * failure the host may have written the required count but left the
     * array contents unpopulated, so only encode elements on success. */
    if ((HRESULT)args->ret >= 0) {
        if (args->pDesc) {
        npt_encode_array_count(enc, *args->pNumModes);
        for (uint32_t _i = 0; _i < (uint32_t)(*args->pNumModes); _i++)
            npt_encode_DXGI_MODE_DESC1(enc, &args->pDesc[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_IDXGIOutput1_GetDisplayModeList1(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_IDXGIOutput1_GetDisplayModeList1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput1_GetDisplayModeList1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_GetDisplayModeList1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput1_GetDisplayModeList1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_GetDisplayModeList1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput1_GetDisplayModeList1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput1_GetDisplayModeList1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput1_GetDisplayModeList1);

    if (ctx->idxgioutput1_dispatch_overrides && ctx->idxgioutput1_dispatch_overrides->GetDisplayModeList1) {
        args.ret = ctx->idxgioutput1_dispatch_overrides->GetDisplayModeList1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.EnumFormat, args.Flags, args.pNumModes, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput1_GetDisplayModeList1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput1::FindClosestMatchingMode1
 */

struct npt_command_IDXGIOutput1_FindClosestMatchingMode1 {
    void *_self;
    const DXGI_MODE_DESC1 * pModeToMatch;
    DXGI_MODE_DESC1 * pClosestMatch;
    IUnknown * pConcernedDevice;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput1_FindClosestMatchingMode1_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_IDXGIOutput1_FindClosestMatchingMode1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pModeToMatch = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MODE_DESC1));
        if (!args->pModeToMatch) return;
        npt_decode_DXGI_MODE_DESC1(dec, (DXGI_MODE_DESC1 *)args->pModeToMatch);
    } else {
        args->pModeToMatch = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pConcernedDevice = (IUnknown *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

    args->pClosestMatch = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MODE_DESC1));
    if (!args->pClosestMatch) return;
    memset(args->pClosestMatch, 0, sizeof(DXGI_MODE_DESC1));

}

static inline void
npt_replace_IDXGIOutput1_FindClosestMatchingMode1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput1_FindClosestMatchingMode1 *args)
{
    args->pConcernedDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pConcernedDevice, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIOutput1_FindClosestMatchingMode1_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_IDXGIOutput1_FindClosestMatchingMode1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_FindClosestMatchingMode1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pClosestMatch))
        npt_encode_DXGI_MODE_DESC1(enc, args->pClosestMatch);
}

static inline void
npt_dispatch_IDXGIOutput1_FindClosestMatchingMode1(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_IDXGIOutput1_FindClosestMatchingMode1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput1_FindClosestMatchingMode1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_FindClosestMatchingMode1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput1_FindClosestMatchingMode1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_FindClosestMatchingMode1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput1_FindClosestMatchingMode1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput1_FindClosestMatchingMode1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput1_FindClosestMatchingMode1);

    if (ctx->idxgioutput1_dispatch_overrides && ctx->idxgioutput1_dispatch_overrides->FindClosestMatchingMode1) {
        args.ret = ctx->idxgioutput1_dispatch_overrides->FindClosestMatchingMode1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pModeToMatch, args.pClosestMatch, args.pConcernedDevice);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput1_FindClosestMatchingMode1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput1::GetDisplaySurfaceData1
 */

struct npt_command_IDXGIOutput1_GetDisplaySurfaceData1 {
    void *_self;
    IDXGIResource * pDestination;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput1_GetDisplaySurfaceData1_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_IDXGIOutput1_GetDisplaySurfaceData1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDestination = (IDXGIResource *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIOutput1_GetDisplaySurfaceData1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput1_GetDisplaySurfaceData1 *args)
{
    args->pDestination = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDestination, NPT_OBJECT_TYPE_IDXGIRESOURCE);
}

static inline void
npt_encode_IDXGIOutput1_GetDisplaySurfaceData1_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_IDXGIOutput1_GetDisplaySurfaceData1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_GetDisplaySurfaceData1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput1_GetDisplaySurfaceData1(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_IDXGIOutput1_GetDisplaySurfaceData1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput1_GetDisplaySurfaceData1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_GetDisplaySurfaceData1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput1_GetDisplaySurfaceData1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_GetDisplaySurfaceData1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput1_GetDisplaySurfaceData1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput1_GetDisplaySurfaceData1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput1_GetDisplaySurfaceData1);

    if (ctx->idxgioutput1_dispatch_overrides && ctx->idxgioutput1_dispatch_overrides->GetDisplaySurfaceData1) {
        args.ret = ctx->idxgioutput1_dispatch_overrides->GetDisplaySurfaceData1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDestination);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput1_GetDisplaySurfaceData1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput1::DuplicateOutput
 */

struct npt_command_IDXGIOutput1_DuplicateOutput {
    void *_self;
    IUnknown * pDevice;
    IDXGIOutputDuplication ** ppOutputDuplication;
    /* Shadow: the guest-allocated id for ppOutputDuplication arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutputDuplication -> *ppOutputDuplication} in the
     * context object table. */
    uint64_t _guest_id_ppOutputDuplication;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput1_DuplicateOutput_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_IDXGIOutput1_DuplicateOutput *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppOutputDuplication = _gid;
        args->ppOutputDuplication = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppOutputDuplication) return;
        *args->ppOutputDuplication = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIOutput1_DuplicateOutput_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput1_DuplicateOutput *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIOutput1_DuplicateOutput_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_IDXGIOutput1_DuplicateOutput *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput1,
                                 NPT_METHOD_IDXGIOutput1_DuplicateOutput),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppOutputDuplication (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIOutput1_DuplicateOutput(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_IDXGIOutput1_DuplicateOutput args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput1_DuplicateOutput_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_DuplicateOutput on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput1_DuplicateOutput_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput1_DuplicateOutput: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput1_DuplicateOutput _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput1_DuplicateOutput, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput1_DuplicateOutput);

    if (ctx->idxgioutput1_dispatch_overrides && ctx->idxgioutput1_dispatch_overrides->DuplicateOutput) {
        args.ret = ctx->idxgioutput1_dispatch_overrides->DuplicateOutput(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDevice, args.ppOutputDuplication);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppOutputDuplication && *args.ppOutputDuplication)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppOutputDuplication, *args.ppOutputDuplication, NPT_OBJECT_TYPE_IDXGIOUTPUTDUPLICATION);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput1_DuplicateOutput_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput2::SupportsOverlays
 */

struct npt_command_IDXGIOutput2_SupportsOverlays {
    void *_self;
    BOOL ret;
};

static inline void
npt_decode_IDXGIOutput2_SupportsOverlays_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGIOutput2_SupportsOverlays *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIOutput2_SupportsOverlays_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput2_SupportsOverlays *args)
{
}

static inline void
npt_encode_IDXGIOutput2_SupportsOverlays_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGIOutput2_SupportsOverlays *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput2,
                                 NPT_METHOD_IDXGIOutput2_SupportsOverlays),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIOutput2_SupportsOverlays(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGIOutput2_SupportsOverlays args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput2_SupportsOverlays_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput2_SupportsOverlays on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput2_SupportsOverlays_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput2_SupportsOverlays: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput2_SupportsOverlays _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput2_SupportsOverlays, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput2_SupportsOverlays);

    if (ctx->idxgioutput2_dispatch_overrides && ctx->idxgioutput2_dispatch_overrides->SupportsOverlays) {
        args.ret = ctx->idxgioutput2_dispatch_overrides->SupportsOverlays(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput2_SupportsOverlays_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput3::CheckOverlaySupport
 */

struct npt_command_IDXGIOutput3_CheckOverlaySupport {
    void *_self;
    DXGI_FORMAT EnumFormat;
    IUnknown * pConcernedDevice;
    UINT * pFlags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput3_CheckOverlaySupport_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_IDXGIOutput3_CheckOverlaySupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->EnumFormat);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pConcernedDevice = (IUnknown *)npt_object_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */


    args->pFlags = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pFlags) return;
    memset(args->pFlags, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGIOutput3_CheckOverlaySupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput3_CheckOverlaySupport *args)
{
    args->pConcernedDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pConcernedDevice, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIOutput3_CheckOverlaySupport_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_IDXGIOutput3_CheckOverlaySupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput3,
                                 NPT_METHOD_IDXGIOutput3_CheckOverlaySupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pFlags))
        npt_encode_UINT(enc, args->pFlags);
}

static inline void
npt_dispatch_IDXGIOutput3_CheckOverlaySupport(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_IDXGIOutput3_CheckOverlaySupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput3_CheckOverlaySupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput3_CheckOverlaySupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput3_CheckOverlaySupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput3_CheckOverlaySupport: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput3_CheckOverlaySupport _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput3_CheckOverlaySupport, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput3_CheckOverlaySupport);

    if (ctx->idxgioutput3_dispatch_overrides && ctx->idxgioutput3_dispatch_overrides->CheckOverlaySupport) {
        args.ret = ctx->idxgioutput3_dispatch_overrides->CheckOverlaySupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.EnumFormat, args.pConcernedDevice, args.pFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput3_CheckOverlaySupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput4::CheckOverlayColorSpaceSupport
 */

struct npt_command_IDXGIOutput4_CheckOverlayColorSpaceSupport {
    void *_self;
    DXGI_FORMAT Format;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
    IUnknown * pConcernedDevice;
    UINT * pFlags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput4_CheckOverlayColorSpaceSupport_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_IDXGIOutput4_CheckOverlayColorSpaceSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->ColorSpace);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pConcernedDevice = (IUnknown *)npt_object_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */



    args->pFlags = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pFlags) return;
    memset(args->pFlags, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGIOutput4_CheckOverlayColorSpaceSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput4_CheckOverlayColorSpaceSupport *args)
{
    args->pConcernedDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pConcernedDevice, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIOutput4_CheckOverlayColorSpaceSupport_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_IDXGIOutput4_CheckOverlayColorSpaceSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput4,
                                 NPT_METHOD_IDXGIOutput4_CheckOverlayColorSpaceSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pFlags))
        npt_encode_UINT(enc, args->pFlags);
}

static inline void
npt_dispatch_IDXGIOutput4_CheckOverlayColorSpaceSupport(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_IDXGIOutput4_CheckOverlayColorSpaceSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput4_CheckOverlayColorSpaceSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput4_CheckOverlayColorSpaceSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput4_CheckOverlayColorSpaceSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput4_CheckOverlayColorSpaceSupport: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput4_CheckOverlayColorSpaceSupport _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput4_CheckOverlayColorSpaceSupport, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput4_CheckOverlayColorSpaceSupport);

    if (ctx->idxgioutput4_dispatch_overrides && ctx->idxgioutput4_dispatch_overrides->CheckOverlayColorSpaceSupport) {
        args.ret = ctx->idxgioutput4_dispatch_overrides->CheckOverlayColorSpaceSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Format, args.ColorSpace, args.pConcernedDevice, args.pFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput4_CheckOverlayColorSpaceSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput5::DuplicateOutput1
 */

struct npt_command_IDXGIOutput5_DuplicateOutput1 {
    void *_self;
    IUnknown * pDevice;
    UINT Flags;
    UINT SupportedFormatsCount;
    const DXGI_FORMAT * pSupportedFormats;
    IDXGIOutputDuplication ** ppOutputDuplication;
    /* Shadow: the guest-allocated id for ppOutputDuplication arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutputDuplication -> *ppOutputDuplication} in the
     * context object table. */
    uint64_t _guest_id_ppOutputDuplication;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput5_DuplicateOutput1_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGIOutput5_DuplicateOutput1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->Flags);
    npt_decode_UINT(dec, &args->SupportedFormatsCount);
    uint64_t _cnt_pSupportedFormats = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pSupportedFormats = npt_decode_array_count_unchecked(dec);
        args->pSupportedFormats = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_FORMAT), _cnt_pSupportedFormats);
        if (!args->pSupportedFormats) return;
        npt_decode_DXGI_FORMAT_array(dec, (DXGI_FORMAT *)args->pSupportedFormats, _cnt_pSupportedFormats);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pSupportedFormats = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppOutputDuplication = _gid;
        args->ppOutputDuplication = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppOutputDuplication) return;
        *args->ppOutputDuplication = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pSupportedFormats && _cnt_pSupportedFormats < (uint64_t)(args->SupportedFormatsCount)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_IDXGIOutput5_DuplicateOutput1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput5_DuplicateOutput1 *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_IDXGIOutput5_DuplicateOutput1_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGIOutput5_DuplicateOutput1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput5,
                                 NPT_METHOD_IDXGIOutput5_DuplicateOutput1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppOutputDuplication (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIOutput5_DuplicateOutput1(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGIOutput5_DuplicateOutput1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput5_DuplicateOutput1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput5_DuplicateOutput1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput5_DuplicateOutput1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput5_DuplicateOutput1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput5_DuplicateOutput1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput5_DuplicateOutput1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput5_DuplicateOutput1);

    if (ctx->idxgioutput5_dispatch_overrides && ctx->idxgioutput5_dispatch_overrides->DuplicateOutput1) {
        args.ret = ctx->idxgioutput5_dispatch_overrides->DuplicateOutput1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDevice, args.Flags, args.SupportedFormatsCount, args.pSupportedFormats, args.ppOutputDuplication);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppOutputDuplication && *args.ppOutputDuplication)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppOutputDuplication, *args.ppOutputDuplication, NPT_OBJECT_TYPE_IDXGIOUTPUTDUPLICATION);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput5_DuplicateOutput1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput6::GetDesc1
 */

struct npt_command_IDXGIOutput6_GetDesc1 {
    void *_self;
    DXGI_OUTPUT_DESC1 * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput6_GetDesc1_args_temp(struct npt_cs_decoder *dec,
                                           struct npt_command_IDXGIOutput6_GetDesc1 *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_OUTPUT_DESC1));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_OUTPUT_DESC1));
}

static inline void
npt_replace_IDXGIOutput6_GetDesc1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput6_GetDesc1 *args)
{
}

static inline void
npt_encode_IDXGIOutput6_GetDesc1_reply(struct npt_cs_encoder *enc,
                                       const struct npt_command_IDXGIOutput6_GetDesc1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                 NPT_METHOD_IDXGIOutput6_GetDesc1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_OUTPUT_DESC1(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGIOutput6_GetDesc1(struct npt_dispatch_context *ctx,
                                   uint32_t cmd_flags,
                                   uint64_t object_id)
{
    struct npt_command_IDXGIOutput6_GetDesc1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput6_GetDesc1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT6);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput6_GetDesc1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput6_GetDesc1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput6_GetDesc1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput6_GetDesc1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput6_GetDesc1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput6_GetDesc1);

    if (ctx->idxgioutput6_dispatch_overrides && ctx->idxgioutput6_dispatch_overrides->GetDesc1) {
        args.ret = ctx->idxgioutput6_dispatch_overrides->GetDesc1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput6_GetDesc1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIOutput6::CheckHardwareCompositionSupport
 */

struct npt_command_IDXGIOutput6_CheckHardwareCompositionSupport {
    void *_self;
    UINT * pFlags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIOutput6_CheckHardwareCompositionSupport_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_IDXGIOutput6_CheckHardwareCompositionSupport *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pFlags = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pFlags) return;
    memset(args->pFlags, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGIOutput6_CheckHardwareCompositionSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIOutput6_CheckHardwareCompositionSupport *args)
{
}

static inline void
npt_encode_IDXGIOutput6_CheckHardwareCompositionSupport_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_IDXGIOutput6_CheckHardwareCompositionSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIOutput6,
                                 NPT_METHOD_IDXGIOutput6_CheckHardwareCompositionSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pFlags))
        npt_encode_UINT(enc, args->pFlags);
}

static inline void
npt_dispatch_IDXGIOutput6_CheckHardwareCompositionSupport(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_IDXGIOutput6_CheckHardwareCompositionSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIOutput6_CheckHardwareCompositionSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIOUTPUT6);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput6_CheckHardwareCompositionSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIOutput6_CheckHardwareCompositionSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIOutput6_CheckHardwareCompositionSupport: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIOutput6_CheckHardwareCompositionSupport _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIOutput6_CheckHardwareCompositionSupport, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIOutput6_CheckHardwareCompositionSupport);

    if (ctx->idxgioutput6_dispatch_overrides && ctx->idxgioutput6_dispatch_overrides->CheckHardwareCompositionSupport) {
        args.ret = ctx->idxgioutput6_dispatch_overrides->CheckHardwareCompositionSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIOutput6_CheckHardwareCompositionSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGIOUTPUT_H */

/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11device.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11DEVICE_H
#define NPT_PROTOCOL_HOST_ID3D11DEVICE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11Device::CreateBuffer
 */

struct npt_command_ID3D11Device_CreateBuffer {
    void *_self;
    const D3D11_BUFFER_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Buffer ** ppBuffer;
    /* Shadow: the guest-allocated id for ppBuffer arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBuffer -> *ppBuffer} in the
     * context object table. */
    uint64_t _guest_id_ppBuffer;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateBuffer_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D11Device_CreateBuffer *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BUFFER_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_BUFFER_DESC(dec, (D3D11_BUFFER_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pInitialData = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_SUBRESOURCE_DATA));
        if (!args->pInitialData) return;
        npt_decode_D3D11_SUBRESOURCE_DATA(dec, (D3D11_SUBRESOURCE_DATA *)args->pInitialData);
    } else {
        args->pInitialData = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppBuffer = _gid;
        args->ppBuffer = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppBuffer) return;
        *args->ppBuffer = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateBuffer *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateBuffer_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D11Device_CreateBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateBuffer),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppBuffer (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateBuffer(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateBuffer: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateBuffer, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateBuffer);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateBuffer) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateBuffer(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pInitialData, args.ppBuffer);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppBuffer && *args.ppBuffer)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppBuffer, *args.ppBuffer, NPT_OBJECT_TYPE_ID3D11BUFFER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateTexture1D
 */

struct npt_command_ID3D11Device_CreateTexture1D {
    void *_self;
    const D3D11_TEXTURE1D_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture1D ** ppTexture1D;
    /* Shadow: the guest-allocated id for ppTexture1D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture1D -> *ppTexture1D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture1D;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateTexture1D_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D11Device_CreateTexture1D *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TEXTURE1D_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_TEXTURE1D_DESC(dec, (D3D11_TEXTURE1D_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    uint64_t _cnt_pInitialData = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pInitialData = npt_decode_array_count_unchecked(dec);
        args->pInitialData = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_SUBRESOURCE_DATA), _cnt_pInitialData);
        if (!args->pInitialData) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pInitialData; _i++)
            npt_decode_D3D11_SUBRESOURCE_DATA(dec, (D3D11_SUBRESOURCE_DATA *)&args->pInitialData[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pInitialData = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppTexture1D = _gid;
        args->ppTexture1D = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppTexture1D) return;
        *args->ppTexture1D = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pInitialData && _cnt_pInitialData < (uint64_t)(args->pDesc->MipLevels * args->pDesc->ArraySize)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateTexture1D_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateTexture1D *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateTexture1D_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D11Device_CreateTexture1D *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateTexture1D),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppTexture1D (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateTexture1D(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateTexture1D args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateTexture1D_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateTexture1D on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateTexture1D_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateTexture1D: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateTexture1D _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateTexture1D, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateTexture1D);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateTexture1D) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateTexture1D(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pInitialData, args.ppTexture1D);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppTexture1D && *args.ppTexture1D)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppTexture1D, *args.ppTexture1D, NPT_OBJECT_TYPE_ID3D11TEXTURE1D);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateTexture1D_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateTexture2D
 */

struct npt_command_ID3D11Device_CreateTexture2D {
    void *_self;
    const D3D11_TEXTURE2D_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture2D ** ppTexture2D;
    /* Shadow: the guest-allocated id for ppTexture2D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture2D -> *ppTexture2D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture2D;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateTexture2D_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D11Device_CreateTexture2D *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TEXTURE2D_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_TEXTURE2D_DESC(dec, (D3D11_TEXTURE2D_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    uint64_t _cnt_pInitialData = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pInitialData = npt_decode_array_count_unchecked(dec);
        args->pInitialData = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_SUBRESOURCE_DATA), _cnt_pInitialData);
        if (!args->pInitialData) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pInitialData; _i++)
            npt_decode_D3D11_SUBRESOURCE_DATA(dec, (D3D11_SUBRESOURCE_DATA *)&args->pInitialData[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pInitialData = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppTexture2D = _gid;
        args->ppTexture2D = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppTexture2D) return;
        *args->ppTexture2D = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pInitialData && _cnt_pInitialData < (uint64_t)(args->pDesc->MipLevels * args->pDesc->ArraySize)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateTexture2D_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateTexture2D *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateTexture2D_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D11Device_CreateTexture2D *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateTexture2D),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppTexture2D (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateTexture2D(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateTexture2D args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateTexture2D_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateTexture2D on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateTexture2D_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateTexture2D: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateTexture2D _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateTexture2D, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateTexture2D);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateTexture2D) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateTexture2D(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pInitialData, args.ppTexture2D);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppTexture2D && *args.ppTexture2D)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppTexture2D, *args.ppTexture2D, NPT_OBJECT_TYPE_ID3D11TEXTURE2D);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateTexture2D_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateTexture3D
 */

struct npt_command_ID3D11Device_CreateTexture3D {
    void *_self;
    const D3D11_TEXTURE3D_DESC * pDesc;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture3D ** ppTexture3D;
    /* Shadow: the guest-allocated id for ppTexture3D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture3D -> *ppTexture3D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture3D;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateTexture3D_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D11Device_CreateTexture3D *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TEXTURE3D_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_TEXTURE3D_DESC(dec, (D3D11_TEXTURE3D_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    uint64_t _cnt_pInitialData = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pInitialData = npt_decode_array_count_unchecked(dec);
        args->pInitialData = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_SUBRESOURCE_DATA), _cnt_pInitialData);
        if (!args->pInitialData) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pInitialData; _i++)
            npt_decode_D3D11_SUBRESOURCE_DATA(dec, (D3D11_SUBRESOURCE_DATA *)&args->pInitialData[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pInitialData = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppTexture3D = _gid;
        args->ppTexture3D = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppTexture3D) return;
        *args->ppTexture3D = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pInitialData && _cnt_pInitialData < (uint64_t)(args->pDesc->MipLevels)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateTexture3D_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateTexture3D *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateTexture3D_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D11Device_CreateTexture3D *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateTexture3D),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppTexture3D (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateTexture3D(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateTexture3D args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateTexture3D_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateTexture3D on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateTexture3D_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateTexture3D: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateTexture3D _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateTexture3D, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateTexture3D);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateTexture3D) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateTexture3D(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pInitialData, args.ppTexture3D);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppTexture3D && *args.ppTexture3D)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppTexture3D, *args.ppTexture3D, NPT_OBJECT_TYPE_ID3D11TEXTURE3D);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateTexture3D_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateShaderResourceView
 */

struct npt_command_ID3D11Device_CreateShaderResourceView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_SHADER_RESOURCE_VIEW_DESC * pDesc;
    ID3D11ShaderResourceView ** ppSRView;
    /* Shadow: the guest-allocated id for ppSRView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSRView -> *ppSRView} in the
     * context object table. */
    uint64_t _guest_id_ppSRView;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateShaderResourceView_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11Device_CreateShaderResourceView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_SHADER_RESOURCE_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_SHADER_RESOURCE_VIEW_DESC(dec, (D3D11_SHADER_RESOURCE_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSRView = _gid;
        args->ppSRView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSRView) return;
        *args->ppSRView = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateShaderResourceView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateShaderResourceView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device_CreateShaderResourceView_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11Device_CreateShaderResourceView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateShaderResourceView),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSRView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateShaderResourceView(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateShaderResourceView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateShaderResourceView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateShaderResourceView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateShaderResourceView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateShaderResourceView: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateShaderResourceView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateShaderResourceView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateShaderResourceView);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateShaderResourceView) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateShaderResourceView(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc, args.ppSRView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSRView && *args.ppSRView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSRView, *args.ppSRView, NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateShaderResourceView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateUnorderedAccessView
 */

struct npt_command_ID3D11Device_CreateUnorderedAccessView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_UNORDERED_ACCESS_VIEW_DESC * pDesc;
    ID3D11UnorderedAccessView ** ppUAView;
    /* Shadow: the guest-allocated id for ppUAView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppUAView -> *ppUAView} in the
     * context object table. */
    uint64_t _guest_id_ppUAView;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateUnorderedAccessView_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11Device_CreateUnorderedAccessView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_UNORDERED_ACCESS_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_UNORDERED_ACCESS_VIEW_DESC(dec, (D3D11_UNORDERED_ACCESS_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppUAView = _gid;
        args->ppUAView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppUAView) return;
        *args->ppUAView = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateUnorderedAccessView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateUnorderedAccessView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device_CreateUnorderedAccessView_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11Device_CreateUnorderedAccessView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateUnorderedAccessView),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppUAView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateUnorderedAccessView(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateUnorderedAccessView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateUnorderedAccessView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateUnorderedAccessView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateUnorderedAccessView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateUnorderedAccessView: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateUnorderedAccessView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateUnorderedAccessView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateUnorderedAccessView);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateUnorderedAccessView) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateUnorderedAccessView(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc, args.ppUAView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppUAView && *args.ppUAView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppUAView, *args.ppUAView, NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateUnorderedAccessView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateRenderTargetView
 */

struct npt_command_ID3D11Device_CreateRenderTargetView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_RENDER_TARGET_VIEW_DESC * pDesc;
    ID3D11RenderTargetView ** ppRTView;
    /* Shadow: the guest-allocated id for ppRTView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRTView -> *ppRTView} in the
     * context object table. */
    uint64_t _guest_id_ppRTView;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateRenderTargetView_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11Device_CreateRenderTargetView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_RENDER_TARGET_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_RENDER_TARGET_VIEW_DESC(dec, (D3D11_RENDER_TARGET_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppRTView = _gid;
        args->ppRTView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppRTView) return;
        *args->ppRTView = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateRenderTargetView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateRenderTargetView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device_CreateRenderTargetView_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11Device_CreateRenderTargetView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateRenderTargetView),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRTView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateRenderTargetView(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateRenderTargetView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateRenderTargetView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateRenderTargetView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateRenderTargetView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateRenderTargetView: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateRenderTargetView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateRenderTargetView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateRenderTargetView);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateRenderTargetView) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateRenderTargetView(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc, args.ppRTView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRTView && *args.ppRTView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppRTView, *args.ppRTView, NPT_OBJECT_TYPE_ID3D11RENDERTARGETVIEW);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateRenderTargetView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateDepthStencilView
 */

struct npt_command_ID3D11Device_CreateDepthStencilView {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_DEPTH_STENCIL_VIEW_DESC * pDesc;
    ID3D11DepthStencilView ** ppDepthStencilView;
    /* Shadow: the guest-allocated id for ppDepthStencilView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilView -> *ppDepthStencilView} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilView;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateDepthStencilView_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11Device_CreateDepthStencilView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_DEPTH_STENCIL_VIEW_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_DEPTH_STENCIL_VIEW_DESC(dec, (D3D11_DEPTH_STENCIL_VIEW_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDepthStencilView = _gid;
        args->ppDepthStencilView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDepthStencilView) return;
        *args->ppDepthStencilView = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_CreateDepthStencilView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateDepthStencilView *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device_CreateDepthStencilView_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11Device_CreateDepthStencilView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDepthStencilView),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDepthStencilView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateDepthStencilView(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateDepthStencilView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateDepthStencilView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDepthStencilView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateDepthStencilView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDepthStencilView: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateDepthStencilView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateDepthStencilView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateDepthStencilView);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateDepthStencilView) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateDepthStencilView(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc, args.ppDepthStencilView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDepthStencilView && *args.ppDepthStencilView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDepthStencilView, *args.ppDepthStencilView, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILVIEW);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateDepthStencilView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateInputLayout
 */

struct npt_command_ID3D11Device_CreateInputLayout {
    void *_self;
    const D3D11_INPUT_ELEMENT_DESC * pInputElementDescs;
    UINT NumElements;
    const void * pShaderBytecodeWithInputSignature;
    SIZE_T BytecodeLength;
    ID3D11InputLayout ** ppInputLayout;
    /* Shadow: the guest-allocated id for ppInputLayout arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInputLayout -> *ppInputLayout} in the
     * context object table. */
    uint64_t _guest_id_ppInputLayout;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateInputLayout_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11Device_CreateInputLayout *args)
{
    /* Decode input parameters from the wire */
    uint64_t _cnt_pInputElementDescs = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pInputElementDescs = npt_decode_array_count_unchecked(dec);
        args->pInputElementDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_INPUT_ELEMENT_DESC), _cnt_pInputElementDescs);
        if (!args->pInputElementDescs) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pInputElementDescs; _i++)
            npt_decode_D3D11_INPUT_ELEMENT_DESC(dec, (D3D11_INPUT_ELEMENT_DESC *)&args->pInputElementDescs[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pInputElementDescs = NULL;
    }
    npt_decode_UINT(dec, &args->NumElements);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecodeWithInputSignature = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecodeWithInputSignature) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecodeWithInputSignature, _blob_size);
        } else {
            args->pShaderBytecodeWithInputSignature = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppInputLayout = _gid;
        args->ppInputLayout = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppInputLayout) return;
        *args->ppInputLayout = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pInputElementDescs && _cnt_pInputElementDescs < (uint64_t)(args->NumElements)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11Device_CreateInputLayout_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateInputLayout *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateInputLayout_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11Device_CreateInputLayout *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateInputLayout),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppInputLayout (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateInputLayout(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateInputLayout args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateInputLayout_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateInputLayout on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateInputLayout_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateInputLayout: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateInputLayout _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateInputLayout, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateInputLayout);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateInputLayout) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateInputLayout(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pInputElementDescs, args.NumElements, args.pShaderBytecodeWithInputSignature, args.BytecodeLength, args.ppInputLayout);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppInputLayout && *args.ppInputLayout)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppInputLayout, *args.ppInputLayout, NPT_OBJECT_TYPE_ID3D11INPUTLAYOUT);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateInputLayout_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateVertexShader
 */

struct npt_command_ID3D11Device_CreateVertexShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11VertexShader ** ppVertexShader;
    /* Shadow: the guest-allocated id for ppVertexShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVertexShader -> *ppVertexShader} in the
     * context object table. */
    uint64_t _guest_id_ppVertexShader;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateVertexShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device_CreateVertexShader *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecode = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecode) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecode, _blob_size);
        } else {
            args->pShaderBytecode = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pClassLinkage = (ID3D11ClassLinkage *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppVertexShader = _gid;
        args->ppVertexShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppVertexShader) return;
        *args->ppVertexShader = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device_CreateVertexShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateVertexShader *args)
{
    args->pClassLinkage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pClassLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
}

static inline void
npt_encode_ID3D11Device_CreateVertexShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device_CreateVertexShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateVertexShader),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppVertexShader (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateVertexShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateVertexShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateVertexShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateVertexShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateVertexShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateVertexShader: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateVertexShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateVertexShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateVertexShader);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateVertexShader) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateVertexShader(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pShaderBytecode, args.BytecodeLength, args.pClassLinkage, args.ppVertexShader);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppVertexShader && *args.ppVertexShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppVertexShader, *args.ppVertexShader, NPT_OBJECT_TYPE_ID3D11VERTEXSHADER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateVertexShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateGeometryShader
 */

struct npt_command_ID3D11Device_CreateGeometryShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11GeometryShader ** ppGeometryShader;
    /* Shadow: the guest-allocated id for ppGeometryShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppGeometryShader -> *ppGeometryShader} in the
     * context object table. */
    uint64_t _guest_id_ppGeometryShader;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateGeometryShader_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11Device_CreateGeometryShader *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecode = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecode) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecode, _blob_size);
        } else {
            args->pShaderBytecode = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pClassLinkage = (ID3D11ClassLinkage *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppGeometryShader = _gid;
        args->ppGeometryShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppGeometryShader) return;
        *args->ppGeometryShader = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device_CreateGeometryShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateGeometryShader *args)
{
    args->pClassLinkage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pClassLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
}

static inline void
npt_encode_ID3D11Device_CreateGeometryShader_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11Device_CreateGeometryShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateGeometryShader),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppGeometryShader (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateGeometryShader(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateGeometryShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateGeometryShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateGeometryShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateGeometryShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateGeometryShader: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateGeometryShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateGeometryShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateGeometryShader);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateGeometryShader) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateGeometryShader(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pShaderBytecode, args.BytecodeLength, args.pClassLinkage, args.ppGeometryShader);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppGeometryShader && *args.ppGeometryShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppGeometryShader, *args.ppGeometryShader, NPT_OBJECT_TYPE_ID3D11GEOMETRYSHADER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateGeometryShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateGeometryShaderWithStreamOutput
 */

struct npt_command_ID3D11Device_CreateGeometryShaderWithStreamOutput {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    const D3D11_SO_DECLARATION_ENTRY * pSODeclaration;
    UINT NumEntries;
    const UINT * pBufferStrides;
    UINT NumStrides;
    UINT RasterizedStream;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11GeometryShader ** ppGeometryShader;
    /* Shadow: the guest-allocated id for ppGeometryShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppGeometryShader -> *ppGeometryShader} in the
     * context object table. */
    uint64_t _guest_id_ppGeometryShader;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateGeometryShaderWithStreamOutput_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11Device_CreateGeometryShaderWithStreamOutput *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecode = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecode) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecode, _blob_size);
        } else {
            args->pShaderBytecode = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    uint64_t _cnt_pSODeclaration = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pSODeclaration = npt_decode_array_count_unchecked(dec);
        args->pSODeclaration = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_SO_DECLARATION_ENTRY), _cnt_pSODeclaration);
        if (!args->pSODeclaration) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pSODeclaration; _i++)
            npt_decode_D3D11_SO_DECLARATION_ENTRY(dec, (D3D11_SO_DECLARATION_ENTRY *)&args->pSODeclaration[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pSODeclaration = NULL;
    }
    npt_decode_UINT(dec, &args->NumEntries);
    uint64_t _cnt_pBufferStrides = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pBufferStrides = npt_decode_array_count_unchecked(dec);
        args->pBufferStrides = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pBufferStrides);
        if (!args->pBufferStrides) return;
        npt_decode_UINT_array(dec, (UINT *)args->pBufferStrides, _cnt_pBufferStrides);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pBufferStrides = NULL;
    }
    npt_decode_UINT(dec, &args->NumStrides);
    npt_decode_UINT(dec, &args->RasterizedStream);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pClassLinkage = (ID3D11ClassLinkage *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppGeometryShader = _gid;
        args->ppGeometryShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppGeometryShader) return;
        *args->ppGeometryShader = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pSODeclaration && _cnt_pSODeclaration < (uint64_t)(args->NumEntries)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pBufferStrides && _cnt_pBufferStrides < (uint64_t)(args->NumStrides)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */









}

static inline void
npt_replace_ID3D11Device_CreateGeometryShaderWithStreamOutput_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateGeometryShaderWithStreamOutput *args)
{
    args->pClassLinkage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pClassLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
}

static inline void
npt_encode_ID3D11Device_CreateGeometryShaderWithStreamOutput_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11Device_CreateGeometryShaderWithStreamOutput *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateGeometryShaderWithStreamOutput),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppGeometryShader (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateGeometryShaderWithStreamOutput(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateGeometryShaderWithStreamOutput args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateGeometryShaderWithStreamOutput_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateGeometryShaderWithStreamOutput on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateGeometryShaderWithStreamOutput_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateGeometryShaderWithStreamOutput: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateGeometryShaderWithStreamOutput _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateGeometryShaderWithStreamOutput, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateGeometryShaderWithStreamOutput);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateGeometryShaderWithStreamOutput) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateGeometryShaderWithStreamOutput(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pShaderBytecode, args.BytecodeLength, args.pSODeclaration, args.NumEntries, args.pBufferStrides, args.NumStrides, args.RasterizedStream, args.pClassLinkage, args.ppGeometryShader);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppGeometryShader && *args.ppGeometryShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppGeometryShader, *args.ppGeometryShader, NPT_OBJECT_TYPE_ID3D11GEOMETRYSHADER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateGeometryShaderWithStreamOutput_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreatePixelShader
 */

struct npt_command_ID3D11Device_CreatePixelShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11PixelShader ** ppPixelShader;
    /* Shadow: the guest-allocated id for ppPixelShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPixelShader -> *ppPixelShader} in the
     * context object table. */
    uint64_t _guest_id_ppPixelShader;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreatePixelShader_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11Device_CreatePixelShader *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecode = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecode) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecode, _blob_size);
        } else {
            args->pShaderBytecode = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pClassLinkage = (ID3D11ClassLinkage *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPixelShader = _gid;
        args->ppPixelShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPixelShader) return;
        *args->ppPixelShader = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device_CreatePixelShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreatePixelShader *args)
{
    args->pClassLinkage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pClassLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
}

static inline void
npt_encode_ID3D11Device_CreatePixelShader_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11Device_CreatePixelShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreatePixelShader),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPixelShader (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreatePixelShader(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreatePixelShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreatePixelShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreatePixelShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreatePixelShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreatePixelShader: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreatePixelShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreatePixelShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreatePixelShader);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreatePixelShader) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreatePixelShader(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pShaderBytecode, args.BytecodeLength, args.pClassLinkage, args.ppPixelShader);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPixelShader && *args.ppPixelShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPixelShader, *args.ppPixelShader, NPT_OBJECT_TYPE_ID3D11PIXELSHADER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreatePixelShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateHullShader
 */

struct npt_command_ID3D11Device_CreateHullShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11HullShader ** ppHullShader;
    /* Shadow: the guest-allocated id for ppHullShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppHullShader -> *ppHullShader} in the
     * context object table. */
    uint64_t _guest_id_ppHullShader;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateHullShader_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Device_CreateHullShader *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecode = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecode) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecode, _blob_size);
        } else {
            args->pShaderBytecode = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pClassLinkage = (ID3D11ClassLinkage *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppHullShader = _gid;
        args->ppHullShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppHullShader) return;
        *args->ppHullShader = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device_CreateHullShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateHullShader *args)
{
    args->pClassLinkage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pClassLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
}

static inline void
npt_encode_ID3D11Device_CreateHullShader_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Device_CreateHullShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateHullShader),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppHullShader (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateHullShader(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateHullShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateHullShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateHullShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateHullShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateHullShader: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateHullShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateHullShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateHullShader);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateHullShader) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateHullShader(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pShaderBytecode, args.BytecodeLength, args.pClassLinkage, args.ppHullShader);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppHullShader && *args.ppHullShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppHullShader, *args.ppHullShader, NPT_OBJECT_TYPE_ID3D11HULLSHADER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateHullShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateDomainShader
 */

struct npt_command_ID3D11Device_CreateDomainShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11DomainShader ** ppDomainShader;
    /* Shadow: the guest-allocated id for ppDomainShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDomainShader -> *ppDomainShader} in the
     * context object table. */
    uint64_t _guest_id_ppDomainShader;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateDomainShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device_CreateDomainShader *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecode = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecode) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecode, _blob_size);
        } else {
            args->pShaderBytecode = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pClassLinkage = (ID3D11ClassLinkage *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDomainShader = _gid;
        args->ppDomainShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDomainShader) return;
        *args->ppDomainShader = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device_CreateDomainShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateDomainShader *args)
{
    args->pClassLinkage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pClassLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
}

static inline void
npt_encode_ID3D11Device_CreateDomainShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device_CreateDomainShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDomainShader),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDomainShader (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateDomainShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateDomainShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateDomainShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDomainShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateDomainShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDomainShader: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateDomainShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateDomainShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateDomainShader);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateDomainShader) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateDomainShader(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pShaderBytecode, args.BytecodeLength, args.pClassLinkage, args.ppDomainShader);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDomainShader && *args.ppDomainShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDomainShader, *args.ppDomainShader, NPT_OBJECT_TYPE_ID3D11DOMAINSHADER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateDomainShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateComputeShader
 */

struct npt_command_ID3D11Device_CreateComputeShader {
    void *_self;
    const void * pShaderBytecode;
    SIZE_T BytecodeLength;
    ID3D11ClassLinkage * pClassLinkage;
    ID3D11ComputeShader ** ppComputeShader;
    /* Shadow: the guest-allocated id for ppComputeShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppComputeShader -> *ppComputeShader} in the
     * context object table. */
    uint64_t _guest_id_ppComputeShader;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateComputeShader_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11Device_CreateComputeShader *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pShaderBytecode = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pShaderBytecode) return;
            npt_decode_blob_array(dec, (void *)args->pShaderBytecode, _blob_size);
        } else {
            args->pShaderBytecode = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->BytecodeLength);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pClassLinkage = (ID3D11ClassLinkage *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppComputeShader = _gid;
        args->ppComputeShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppComputeShader) return;
        *args->ppComputeShader = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device_CreateComputeShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateComputeShader *args)
{
    args->pClassLinkage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pClassLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
}

static inline void
npt_encode_ID3D11Device_CreateComputeShader_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11Device_CreateComputeShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateComputeShader),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppComputeShader (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateComputeShader(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateComputeShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateComputeShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateComputeShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateComputeShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateComputeShader: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateComputeShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateComputeShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateComputeShader);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateComputeShader) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateComputeShader(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pShaderBytecode, args.BytecodeLength, args.pClassLinkage, args.ppComputeShader);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppComputeShader && *args.ppComputeShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppComputeShader, *args.ppComputeShader, NPT_OBJECT_TYPE_ID3D11COMPUTESHADER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateComputeShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateClassLinkage
 */

struct npt_command_ID3D11Device_CreateClassLinkage {
    void *_self;
    ID3D11ClassLinkage ** ppLinkage;
    /* Shadow: the guest-allocated id for ppLinkage arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppLinkage -> *ppLinkage} in the
     * context object table. */
    uint64_t _guest_id_ppLinkage;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateClassLinkage_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device_CreateClassLinkage *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppLinkage = _gid;
        args->ppLinkage = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppLinkage) return;
        *args->ppLinkage = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Device_CreateClassLinkage_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateClassLinkage *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateClassLinkage_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device_CreateClassLinkage *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateClassLinkage),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppLinkage (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateClassLinkage(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateClassLinkage args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateClassLinkage_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateClassLinkage on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateClassLinkage_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateClassLinkage: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateClassLinkage _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateClassLinkage, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateClassLinkage);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateClassLinkage) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateClassLinkage(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ppLinkage);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppLinkage && *args.ppLinkage)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppLinkage, *args.ppLinkage, NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateClassLinkage_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateBlendState
 */

struct npt_command_ID3D11Device_CreateBlendState {
    void *_self;
    const D3D11_BLEND_DESC * pBlendStateDesc;
    ID3D11BlendState ** ppBlendState;
    /* Shadow: the guest-allocated id for ppBlendState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBlendState -> *ppBlendState} in the
     * context object table. */
    uint64_t _guest_id_ppBlendState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateBlendState_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Device_CreateBlendState *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pBlendStateDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BLEND_DESC));
        if (!args->pBlendStateDesc) return;
        npt_decode_D3D11_BLEND_DESC(dec, (D3D11_BLEND_DESC *)args->pBlendStateDesc);
    } else {
        args->pBlendStateDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppBlendState = _gid;
        args->ppBlendState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppBlendState) return;
        *args->ppBlendState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreateBlendState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateBlendState *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateBlendState_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Device_CreateBlendState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateBlendState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppBlendState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateBlendState(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateBlendState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateBlendState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateBlendState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateBlendState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateBlendState: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateBlendState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateBlendState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateBlendState);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateBlendState) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateBlendState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pBlendStateDesc, args.ppBlendState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppBlendState && *args.ppBlendState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppBlendState, *args.ppBlendState, NPT_OBJECT_TYPE_ID3D11BLENDSTATE);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateBlendState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateDepthStencilState
 */

struct npt_command_ID3D11Device_CreateDepthStencilState {
    void *_self;
    const D3D11_DEPTH_STENCIL_DESC * pDepthStencilDesc;
    ID3D11DepthStencilState ** ppDepthStencilState;
    /* Shadow: the guest-allocated id for ppDepthStencilState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilState -> *ppDepthStencilState} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateDepthStencilState_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11Device_CreateDepthStencilState *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDepthStencilDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_DEPTH_STENCIL_DESC));
        if (!args->pDepthStencilDesc) return;
        npt_decode_D3D11_DEPTH_STENCIL_DESC(dec, (D3D11_DEPTH_STENCIL_DESC *)args->pDepthStencilDesc);
    } else {
        args->pDepthStencilDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDepthStencilState = _gid;
        args->ppDepthStencilState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDepthStencilState) return;
        *args->ppDepthStencilState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreateDepthStencilState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateDepthStencilState *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateDepthStencilState_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11Device_CreateDepthStencilState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDepthStencilState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDepthStencilState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateDepthStencilState(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateDepthStencilState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateDepthStencilState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDepthStencilState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateDepthStencilState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDepthStencilState: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateDepthStencilState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateDepthStencilState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateDepthStencilState);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateDepthStencilState) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateDepthStencilState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDepthStencilDesc, args.ppDepthStencilState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDepthStencilState && *args.ppDepthStencilState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDepthStencilState, *args.ppDepthStencilState, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILSTATE);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateDepthStencilState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateRasterizerState
 */

struct npt_command_ID3D11Device_CreateRasterizerState {
    void *_self;
    const D3D11_RASTERIZER_DESC * pRasterizerDesc;
    ID3D11RasterizerState ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateRasterizerState_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11Device_CreateRasterizerState *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pRasterizerDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_RASTERIZER_DESC));
        if (!args->pRasterizerDesc) return;
        npt_decode_D3D11_RASTERIZER_DESC(dec, (D3D11_RASTERIZER_DESC *)args->pRasterizerDesc);
    } else {
        args->pRasterizerDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppRasterizerState = _gid;
        args->ppRasterizerState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppRasterizerState) return;
        *args->ppRasterizerState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreateRasterizerState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateRasterizerState *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateRasterizerState_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11Device_CreateRasterizerState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateRasterizerState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateRasterizerState(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateRasterizerState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateRasterizerState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateRasterizerState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateRasterizerState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateRasterizerState: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateRasterizerState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateRasterizerState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateRasterizerState);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateRasterizerState) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateRasterizerState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRasterizerDesc, args.ppRasterizerState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRasterizerState && *args.ppRasterizerState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppRasterizerState, *args.ppRasterizerState, NPT_OBJECT_TYPE_ID3D11RASTERIZERSTATE);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateRasterizerState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateSamplerState
 */

struct npt_command_ID3D11Device_CreateSamplerState {
    void *_self;
    const D3D11_SAMPLER_DESC * pSamplerDesc;
    ID3D11SamplerState ** ppSamplerState;
    /* Shadow: the guest-allocated id for ppSamplerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSamplerState -> *ppSamplerState} in the
     * context object table. */
    uint64_t _guest_id_ppSamplerState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateSamplerState_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device_CreateSamplerState *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pSamplerDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_SAMPLER_DESC));
        if (!args->pSamplerDesc) return;
        npt_decode_D3D11_SAMPLER_DESC(dec, (D3D11_SAMPLER_DESC *)args->pSamplerDesc);
    } else {
        args->pSamplerDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSamplerState = _gid;
        args->ppSamplerState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSamplerState) return;
        *args->ppSamplerState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreateSamplerState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateSamplerState *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateSamplerState_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device_CreateSamplerState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateSamplerState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSamplerState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateSamplerState(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateSamplerState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateSamplerState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateSamplerState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateSamplerState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateSamplerState: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateSamplerState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateSamplerState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateSamplerState);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateSamplerState) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateSamplerState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pSamplerDesc, args.ppSamplerState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSamplerState && *args.ppSamplerState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSamplerState, *args.ppSamplerState, NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateSamplerState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateQuery
 */

struct npt_command_ID3D11Device_CreateQuery {
    void *_self;
    const D3D11_QUERY_DESC * pQueryDesc;
    ID3D11Query ** ppQuery;
    /* Shadow: the guest-allocated id for ppQuery arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppQuery -> *ppQuery} in the
     * context object table. */
    uint64_t _guest_id_ppQuery;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateQuery_args_temp(struct npt_cs_decoder *dec,
                                              struct npt_command_ID3D11Device_CreateQuery *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pQueryDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_QUERY_DESC));
        if (!args->pQueryDesc) return;
        npt_decode_D3D11_QUERY_DESC(dec, (D3D11_QUERY_DESC *)args->pQueryDesc);
    } else {
        args->pQueryDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppQuery = _gid;
        args->ppQuery = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppQuery) return;
        *args->ppQuery = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreateQuery_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateQuery *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateQuery_reply(struct npt_cs_encoder *enc,
                                          const struct npt_command_ID3D11Device_CreateQuery *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateQuery),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppQuery (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateQuery(struct npt_dispatch_context *ctx,
                                      uint32_t cmd_flags,
                                      uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateQuery args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateQuery_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateQuery on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateQuery_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateQuery: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateQuery _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateQuery, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateQuery);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateQuery) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateQuery(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pQueryDesc, args.ppQuery);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppQuery && *args.ppQuery)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppQuery, *args.ppQuery, NPT_OBJECT_TYPE_ID3D11QUERY);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateQuery_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreatePredicate
 */

struct npt_command_ID3D11Device_CreatePredicate {
    void *_self;
    const D3D11_QUERY_DESC * pPredicateDesc;
    ID3D11Predicate ** ppPredicate;
    /* Shadow: the guest-allocated id for ppPredicate arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPredicate -> *ppPredicate} in the
     * context object table. */
    uint64_t _guest_id_ppPredicate;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreatePredicate_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D11Device_CreatePredicate *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pPredicateDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_QUERY_DESC));
        if (!args->pPredicateDesc) return;
        npt_decode_D3D11_QUERY_DESC(dec, (D3D11_QUERY_DESC *)args->pPredicateDesc);
    } else {
        args->pPredicateDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPredicate = _gid;
        args->ppPredicate = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPredicate) return;
        *args->ppPredicate = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreatePredicate_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreatePredicate *args)
{
}

static inline void
npt_encode_ID3D11Device_CreatePredicate_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D11Device_CreatePredicate *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreatePredicate),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPredicate (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreatePredicate(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreatePredicate args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreatePredicate_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreatePredicate on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreatePredicate_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreatePredicate: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreatePredicate _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreatePredicate, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreatePredicate);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreatePredicate) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreatePredicate(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pPredicateDesc, args.ppPredicate);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPredicate && *args.ppPredicate)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPredicate, *args.ppPredicate, NPT_OBJECT_TYPE_ID3D11PREDICATE);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreatePredicate_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateCounter
 */

struct npt_command_ID3D11Device_CreateCounter {
    void *_self;
    const D3D11_COUNTER_DESC * pCounterDesc;
    ID3D11Counter ** ppCounter;
    /* Shadow: the guest-allocated id for ppCounter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCounter -> *ppCounter} in the
     * context object table. */
    uint64_t _guest_id_ppCounter;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateCounter_args_temp(struct npt_cs_decoder *dec,
                                                struct npt_command_ID3D11Device_CreateCounter *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pCounterDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_COUNTER_DESC));
        if (!args->pCounterDesc) return;
        npt_decode_D3D11_COUNTER_DESC(dec, (D3D11_COUNTER_DESC *)args->pCounterDesc);
    } else {
        args->pCounterDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCounter = _gid;
        args->ppCounter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCounter) return;
        *args->ppCounter = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreateCounter_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateCounter *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateCounter_reply(struct npt_cs_encoder *enc,
                                            const struct npt_command_ID3D11Device_CreateCounter *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateCounter),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCounter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateCounter(struct npt_dispatch_context *ctx,
                                        uint32_t cmd_flags,
                                        uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateCounter args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateCounter_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateCounter on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateCounter_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateCounter: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateCounter _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateCounter, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateCounter);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateCounter) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateCounter(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCounterDesc, args.ppCounter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCounter && *args.ppCounter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCounter, *args.ppCounter, NPT_OBJECT_TYPE_ID3D11COUNTER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateCounter_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CreateDeferredContext
 */

struct npt_command_ID3D11Device_CreateDeferredContext {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CreateDeferredContext_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11Device_CreateDeferredContext *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->ContextFlags);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDeferredContext = _gid;
        args->ppDeferredContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDeferredContext) return;
        *args->ppDeferredContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_CreateDeferredContext_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CreateDeferredContext *args)
{
}

static inline void
npt_encode_ID3D11Device_CreateDeferredContext_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11Device_CreateDeferredContext *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CreateDeferredContext),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_CreateDeferredContext(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11Device_CreateDeferredContext args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CreateDeferredContext_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDeferredContext on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CreateDeferredContext_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CreateDeferredContext: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CreateDeferredContext _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CreateDeferredContext, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CreateDeferredContext);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CreateDeferredContext) {
        args.ret = ctx->id3d11device_dispatch_overrides->CreateDeferredContext(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ContextFlags, args.ppDeferredContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDeferredContext && *args.ppDeferredContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDeferredContext, *args.ppDeferredContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CreateDeferredContext_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::OpenSharedResource
 */

struct npt_command_ID3D11Device_OpenSharedResource {
    void *_self;
    HANDLE hResource;
    const IID * ReturnedInterface;
    void ** ppResource;
    /* Shadow: the guest-allocated id for ppResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource -> *ppResource} in the
     * context object table. */
    uint64_t _guest_id_ppResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_OpenSharedResource_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device_OpenSharedResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hResource = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->ReturnedInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->ReturnedInterface) return;
        npt_decode_IID(dec, (IID *)args->ReturnedInterface);
    } else {
        args->ReturnedInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppResource = _gid;
        args->ppResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppResource) return;
        *args->ppResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_OpenSharedResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_OpenSharedResource *args)
{
    args->hResource = (HANDLE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->hResource);
}

static inline void
npt_encode_ID3D11Device_OpenSharedResource_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device_OpenSharedResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_OpenSharedResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_OpenSharedResource(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device_OpenSharedResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_OpenSharedResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_OpenSharedResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_OpenSharedResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_OpenSharedResource: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_OpenSharedResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_OpenSharedResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_OpenSharedResource);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->OpenSharedResource) {
        args.ret = ctx->id3d11device_dispatch_overrides->OpenSharedResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hResource, args.ReturnedInterface, args.ppResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppResource && *args.ppResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppResource, *args.ppResource,
            npt_object_type_from_iid(args.ReturnedInterface));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_OpenSharedResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CheckFormatSupport
 */

struct npt_command_ID3D11Device_CheckFormatSupport {
    void *_self;
    DXGI_FORMAT Format;
    UINT * pFormatSupport;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CheckFormatSupport_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device_CheckFormatSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->Format);

    /* Allocate temp storage for output-only parameters */

    args->pFormatSupport = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pFormatSupport) return;
    memset(args->pFormatSupport, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11Device_CheckFormatSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CheckFormatSupport *args)
{
}

static inline void
npt_encode_ID3D11Device_CheckFormatSupport_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device_CheckFormatSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckFormatSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pFormatSupport))
        npt_encode_UINT(enc, args->pFormatSupport);
}

static inline void
npt_dispatch_ID3D11Device_CheckFormatSupport(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device_CheckFormatSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CheckFormatSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckFormatSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CheckFormatSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckFormatSupport: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CheckFormatSupport _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CheckFormatSupport, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CheckFormatSupport);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CheckFormatSupport) {
        args.ret = ctx->id3d11device_dispatch_overrides->CheckFormatSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Format, args.pFormatSupport);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CheckFormatSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CheckMultisampleQualityLevels
 */

struct npt_command_ID3D11Device_CheckMultisampleQualityLevels {
    void *_self;
    DXGI_FORMAT Format;
    UINT SampleCount;
    UINT * pNumQualityLevels;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CheckMultisampleQualityLevels_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11Device_CheckMultisampleQualityLevels *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    npt_decode_UINT(dec, &args->SampleCount);

    /* Allocate temp storage for output-only parameters */


    args->pNumQualityLevels = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pNumQualityLevels) return;
    memset(args->pNumQualityLevels, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11Device_CheckMultisampleQualityLevels_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CheckMultisampleQualityLevels *args)
{
}

static inline void
npt_encode_ID3D11Device_CheckMultisampleQualityLevels_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11Device_CheckMultisampleQualityLevels *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckMultisampleQualityLevels),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumQualityLevels))
        npt_encode_UINT(enc, args->pNumQualityLevels);
}

static inline void
npt_dispatch_ID3D11Device_CheckMultisampleQualityLevels(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11Device_CheckMultisampleQualityLevels args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CheckMultisampleQualityLevels_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckMultisampleQualityLevels on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CheckMultisampleQualityLevels_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckMultisampleQualityLevels: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CheckMultisampleQualityLevels _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CheckMultisampleQualityLevels, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CheckMultisampleQualityLevels);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CheckMultisampleQualityLevels) {
        args.ret = ctx->id3d11device_dispatch_overrides->CheckMultisampleQualityLevels(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Format, args.SampleCount, args.pNumQualityLevels);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CheckMultisampleQualityLevels_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CheckCounterInfo
 */

struct npt_command_ID3D11Device_CheckCounterInfo {
    void *_self;
    D3D11_COUNTER_INFO * pCounterInfo;
};

static inline void
npt_decode_ID3D11Device_CheckCounterInfo_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Device_CheckCounterInfo *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pCounterInfo = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_COUNTER_INFO));
    if (!args->pCounterInfo) return;
    memset(args->pCounterInfo, 0, sizeof(D3D11_COUNTER_INFO));
}

static inline void
npt_replace_ID3D11Device_CheckCounterInfo_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CheckCounterInfo *args)
{
}

static inline void
npt_encode_ID3D11Device_CheckCounterInfo_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Device_CheckCounterInfo *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckCounterInfo),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pCounterInfo))
        npt_encode_D3D11_COUNTER_INFO(enc, args->pCounterInfo);
}

static inline void
npt_dispatch_ID3D11Device_CheckCounterInfo(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Device_CheckCounterInfo args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CheckCounterInfo_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckCounterInfo on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CheckCounterInfo_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckCounterInfo: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CheckCounterInfo _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CheckCounterInfo, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CheckCounterInfo);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CheckCounterInfo) {
        ctx->id3d11device_dispatch_overrides->CheckCounterInfo(ctx, &args, _original);
    } else {
        _original(args._self, args.pCounterInfo);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CheckCounterInfo_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CheckCounter
 */

struct npt_command_ID3D11Device_CheckCounter {
    void *_self;
    const D3D11_COUNTER_DESC * pDesc;
    D3D11_COUNTER_TYPE * pType;
    UINT * pActiveCounters;
    CHAR * szName;
    UINT * pNameLength;
    CHAR * szUnits;
    UINT * pUnitsLength;
    CHAR * szDescription;
    UINT * pDescriptionLength;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CheckCounter_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D11Device_CheckCounter *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_COUNTER_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D11_COUNTER_DESC(dec, (D3D11_COUNTER_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }



    if (npt_decode_simple_pointer(dec)) {
        args->pNameLength = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNameLength) return;
        npt_decode_UINT(dec, (UINT *)args->pNameLength);
    } else {
        args->pNameLength = NULL;
    }

    if (npt_decode_simple_pointer(dec)) {
        args->pUnitsLength = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pUnitsLength) return;
        npt_decode_UINT(dec, (UINT *)args->pUnitsLength);
    } else {
        args->pUnitsLength = NULL;
    }

    if (npt_decode_simple_pointer(dec)) {
        args->pDescriptionLength = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pDescriptionLength) return;
        npt_decode_UINT(dec, (UINT *)args->pDescriptionLength);
    } else {
        args->pDescriptionLength = NULL;
    }
    /* Allocate temp storage for output-only parameters */

    args->pType = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_COUNTER_TYPE));
    if (!args->pType) return;
    memset(args->pType, 0, sizeof(D3D11_COUNTER_TYPE));
    args->pActiveCounters = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pActiveCounters) return;
    memset(args->pActiveCounters, 0, sizeof(UINT));
    if ((args->pNameLength ? *args->pNameLength : 0)) {
        args->szName = npt_cs_decoder_alloc_temp_array(dec, sizeof(CHAR), (args->pNameLength ? *args->pNameLength : 0));
        if (!args->szName) return;
        memset(args->szName, 0, sizeof(CHAR) * (args->pNameLength ? *args->pNameLength : 0));
    } else {
        args->szName = NULL;
    }

    if ((args->pUnitsLength ? *args->pUnitsLength : 0)) {
        args->szUnits = npt_cs_decoder_alloc_temp_array(dec, sizeof(CHAR), (args->pUnitsLength ? *args->pUnitsLength : 0));
        if (!args->szUnits) return;
        memset(args->szUnits, 0, sizeof(CHAR) * (args->pUnitsLength ? *args->pUnitsLength : 0));
    } else {
        args->szUnits = NULL;
    }

    if ((args->pDescriptionLength ? *args->pDescriptionLength : 0)) {
        args->szDescription = npt_cs_decoder_alloc_temp_array(dec, sizeof(CHAR), (args->pDescriptionLength ? *args->pDescriptionLength : 0));
        if (!args->szDescription) return;
        memset(args->szDescription, 0, sizeof(CHAR) * (args->pDescriptionLength ? *args->pDescriptionLength : 0));
    } else {
        args->szDescription = NULL;
    }

}

static inline void
npt_replace_ID3D11Device_CheckCounter_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CheckCounter *args)
{
}

static inline void
npt_encode_ID3D11Device_CheckCounter_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D11Device_CheckCounter *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckCounter),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pType))
        npt_encode_D3D11_COUNTER_TYPE(enc, args->pType);
    if (npt_encode_simple_pointer(enc, args->pActiveCounters))
        npt_encode_UINT(enc, args->pActiveCounters);
    if (args->szName) {
        npt_encode_array_count(enc, (args->pNameLength ? *args->pNameLength : 0));
        npt_encode_CHAR_array(enc, args->szName, (args->pNameLength ? *args->pNameLength : 0));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pNameLength))
        npt_encode_UINT(enc, args->pNameLength);
    if (args->szUnits) {
        npt_encode_array_count(enc, (args->pUnitsLength ? *args->pUnitsLength : 0));
        npt_encode_CHAR_array(enc, args->szUnits, (args->pUnitsLength ? *args->pUnitsLength : 0));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pUnitsLength))
        npt_encode_UINT(enc, args->pUnitsLength);
    if (args->szDescription) {
        npt_encode_array_count(enc, (args->pDescriptionLength ? *args->pDescriptionLength : 0));
        npt_encode_CHAR_array(enc, args->szDescription, (args->pDescriptionLength ? *args->pDescriptionLength : 0));
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pDescriptionLength))
        npt_encode_UINT(enc, args->pDescriptionLength);
}

static inline void
npt_dispatch_ID3D11Device_CheckCounter(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D11Device_CheckCounter args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CheckCounter_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckCounter on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CheckCounter_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckCounter: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CheckCounter _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CheckCounter, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CheckCounter);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CheckCounter) {
        args.ret = ctx->id3d11device_dispatch_overrides->CheckCounter(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.pType, args.pActiveCounters, args.szName, args.pNameLength, args.szUnits, args.pUnitsLength, args.szDescription, args.pDescriptionLength);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CheckCounter_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::CheckFeatureSupport
 */

struct npt_command_ID3D11Device_CheckFeatureSupport {
    void *_self;
    D3D11_FEATURE Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_CheckFeatureSupport_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11Device_CheckFeatureSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D11_FEATURE(dec, &args->Feature);

    npt_decode_UINT(dec, &args->FeatureSupportDataSize);
    /* Allocate temp storage for output-only parameters */

    if (args->FeatureSupportDataSize) {
        args->pFeatureSupportData = npt_cs_decoder_alloc_temp(dec, args->FeatureSupportDataSize);
        if (!args->pFeatureSupportData) return;
        memset(args->pFeatureSupportData, 0, args->FeatureSupportDataSize);
    } else {
        args->pFeatureSupportData = NULL;
    }

}

static inline void
npt_replace_ID3D11Device_CheckFeatureSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_CheckFeatureSupport *args)
{
}

static inline void
npt_encode_ID3D11Device_CheckFeatureSupport_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11Device_CheckFeatureSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_CheckFeatureSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pFeatureSupportData) {
        npt_encode_array_count(enc, args->FeatureSupportDataSize);
        npt_encode_blob_array(enc, args->pFeatureSupportData, args->FeatureSupportDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11Device_CheckFeatureSupport(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11Device_CheckFeatureSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_CheckFeatureSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckFeatureSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_CheckFeatureSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_CheckFeatureSupport: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_CheckFeatureSupport _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_CheckFeatureSupport, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_CheckFeatureSupport);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->CheckFeatureSupport) {
        args.ret = ctx->id3d11device_dispatch_overrides->CheckFeatureSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Feature, args.pFeatureSupportData, args.FeatureSupportDataSize);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_CheckFeatureSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::GetPrivateData
 */

struct npt_command_ID3D11Device_GetPrivateData {
    void *_self;
    const GUID * guid;
    UINT * pDataSize;
    void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_GetPrivateData_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D11Device_GetPrivateData *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDataSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pDataSize) return;
        npt_decode_UINT(dec, (UINT *)args->pDataSize);
    } else {
        args->pDataSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */


    if (*args->pDataSize) {
        args->pData = npt_cs_decoder_alloc_temp(dec, *args->pDataSize);
        if (!args->pData) return;
        memset(args->pData, 0, *args->pDataSize);
    } else {
        args->pData = NULL;
    }
}

static inline void
npt_replace_ID3D11Device_GetPrivateData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_GetPrivateData *args)
{
}

static inline void
npt_encode_ID3D11Device_GetPrivateData_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D11Device_GetPrivateData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetPrivateData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDataSize))
        npt_encode_UINT(enc, args->pDataSize);
    if (args->pData) {
        npt_encode_array_count(enc, *args->pDataSize);
        npt_encode_blob_array(enc, args->pData, *args->pDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11Device_GetPrivateData(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D11Device_GetPrivateData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_GetPrivateData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetPrivateData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_GetPrivateData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetPrivateData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_GetPrivateData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_GetPrivateData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_GetPrivateData);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->GetPrivateData) {
        args.ret = ctx->id3d11device_dispatch_overrides->GetPrivateData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.pDataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_GetPrivateData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::SetPrivateData
 */

struct npt_command_ID3D11Device_SetPrivateData {
    void *_self;
    const GUID * guid;
    UINT DataSize;
    const void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_SetPrivateData_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D11Device_SetPrivateData *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device_SetPrivateData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_SetPrivateData *args)
{
}

static inline void
npt_encode_ID3D11Device_SetPrivateData_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D11Device_SetPrivateData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_SetPrivateData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device_SetPrivateData(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D11Device_SetPrivateData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_SetPrivateData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_SetPrivateData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_SetPrivateData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_SetPrivateData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_SetPrivateData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_SetPrivateData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_SetPrivateData);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->SetPrivateData) {
        args.ret = ctx->id3d11device_dispatch_overrides->SetPrivateData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_SetPrivateData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::SetPrivateDataInterface
 */

struct npt_command_ID3D11Device_SetPrivateDataInterface {
    void *_self;
    const GUID * guid;
    const IUnknown * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_SetPrivateDataInterface_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11Device_SetPrivateDataInterface *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pData = (IUnknown *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device_SetPrivateDataInterface_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_SetPrivateDataInterface *args)
{
    args->pData = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pData, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_ID3D11Device_SetPrivateDataInterface_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11Device_SetPrivateDataInterface *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_SetPrivateDataInterface),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device_SetPrivateDataInterface(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11Device_SetPrivateDataInterface args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_SetPrivateDataInterface_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_SetPrivateDataInterface on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_SetPrivateDataInterface_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_SetPrivateDataInterface: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_SetPrivateDataInterface _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_SetPrivateDataInterface, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_SetPrivateDataInterface);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->SetPrivateDataInterface) {
        args.ret = ctx->id3d11device_dispatch_overrides->SetPrivateDataInterface(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_SetPrivateDataInterface_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::GetFeatureLevel
 */

struct npt_command_ID3D11Device_GetFeatureLevel {
    void *_self;
    D3D_FEATURE_LEVEL ret;
};

static inline void
npt_decode_ID3D11Device_GetFeatureLevel_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D11Device_GetFeatureLevel *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11Device_GetFeatureLevel_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_GetFeatureLevel *args)
{
}

static inline void
npt_encode_ID3D11Device_GetFeatureLevel_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D11Device_GetFeatureLevel *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetFeatureLevel),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device_GetFeatureLevel(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D11Device_GetFeatureLevel args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_GetFeatureLevel_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetFeatureLevel on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_GetFeatureLevel_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetFeatureLevel: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_GetFeatureLevel _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_GetFeatureLevel, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_GetFeatureLevel);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->GetFeatureLevel) {
        args.ret = ctx->id3d11device_dispatch_overrides->GetFeatureLevel(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_GetFeatureLevel_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::GetCreationFlags
 */

struct npt_command_ID3D11Device_GetCreationFlags {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_ID3D11Device_GetCreationFlags_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Device_GetCreationFlags *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11Device_GetCreationFlags_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_GetCreationFlags *args)
{
}

static inline void
npt_encode_ID3D11Device_GetCreationFlags_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Device_GetCreationFlags *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetCreationFlags),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device_GetCreationFlags(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Device_GetCreationFlags args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_GetCreationFlags_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetCreationFlags on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_GetCreationFlags_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetCreationFlags: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_GetCreationFlags _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_GetCreationFlags, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_GetCreationFlags);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->GetCreationFlags) {
        args.ret = ctx->id3d11device_dispatch_overrides->GetCreationFlags(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_GetCreationFlags_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::GetDeviceRemovedReason
 */

struct npt_command_ID3D11Device_GetDeviceRemovedReason {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_GetDeviceRemovedReason_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11Device_GetDeviceRemovedReason *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11Device_GetDeviceRemovedReason_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_GetDeviceRemovedReason *args)
{
}

static inline void
npt_encode_ID3D11Device_GetDeviceRemovedReason_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11Device_GetDeviceRemovedReason *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetDeviceRemovedReason),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device_GetDeviceRemovedReason(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11Device_GetDeviceRemovedReason args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_GetDeviceRemovedReason_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetDeviceRemovedReason on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_GetDeviceRemovedReason_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetDeviceRemovedReason: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_GetDeviceRemovedReason _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_GetDeviceRemovedReason, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_GetDeviceRemovedReason);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->GetDeviceRemovedReason) {
        args.ret = ctx->id3d11device_dispatch_overrides->GetDeviceRemovedReason(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_GetDeviceRemovedReason_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::GetImmediateContext
 */

struct npt_command_ID3D11Device_GetImmediateContext {
    void *_self;
    ID3D11DeviceContext ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline void
npt_decode_ID3D11Device_GetImmediateContext_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11Device_GetImmediateContext *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppImmediateContext = _gid;
        args->ppImmediateContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppImmediateContext) return;
        *args->ppImmediateContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Device_GetImmediateContext_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_GetImmediateContext *args)
{
}

static inline void
npt_encode_ID3D11Device_GetImmediateContext_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11Device_GetImmediateContext *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetImmediateContext),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device_GetImmediateContext(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11Device_GetImmediateContext args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_GetImmediateContext_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetImmediateContext on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_GetImmediateContext_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetImmediateContext: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_GetImmediateContext _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_GetImmediateContext, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_GetImmediateContext);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->GetImmediateContext) {
        ctx->id3d11device_dispatch_overrides->GetImmediateContext(ctx, &args, _original);
    } else {
        _original(args._self, args.ppImmediateContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppImmediateContext && *args.ppImmediateContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppImmediateContext, *args.ppImmediateContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_GetImmediateContext_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::SetExceptionMode
 */

struct npt_command_ID3D11Device_SetExceptionMode {
    void *_self;
    UINT RaiseFlags;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device_SetExceptionMode_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Device_SetExceptionMode *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RaiseFlags);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Device_SetExceptionMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_SetExceptionMode *args)
{
}

static inline void
npt_encode_ID3D11Device_SetExceptionMode_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Device_SetExceptionMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_SetExceptionMode),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device_SetExceptionMode(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Device_SetExceptionMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_SetExceptionMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_SetExceptionMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_SetExceptionMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_SetExceptionMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_SetExceptionMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_SetExceptionMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_SetExceptionMode);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->SetExceptionMode) {
        args.ret = ctx->id3d11device_dispatch_overrides->SetExceptionMode(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.RaiseFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_SetExceptionMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device::GetExceptionMode
 */

struct npt_command_ID3D11Device_GetExceptionMode {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_ID3D11Device_GetExceptionMode_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Device_GetExceptionMode *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11Device_GetExceptionMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device_GetExceptionMode *args)
{
}

static inline void
npt_encode_ID3D11Device_GetExceptionMode_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Device_GetExceptionMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device,
                                 NPT_METHOD_ID3D11Device_GetExceptionMode),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device_GetExceptionMode(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Device_GetExceptionMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device_GetExceptionMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetExceptionMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device_GetExceptionMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device_GetExceptionMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device_GetExceptionMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device_GetExceptionMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device_GetExceptionMode);

    if (ctx->id3d11device_dispatch_overrides && ctx->id3d11device_dispatch_overrides->GetExceptionMode) {
        args.ret = ctx->id3d11device_dispatch_overrides->GetExceptionMode(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device_GetExceptionMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device1::GetImmediateContext1
 */

struct npt_command_ID3D11Device1_GetImmediateContext1 {
    void *_self;
    ID3D11DeviceContext1 ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline void
npt_decode_ID3D11Device1_GetImmediateContext1_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11Device1_GetImmediateContext1 *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppImmediateContext = _gid;
        args->ppImmediateContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppImmediateContext) return;
        *args->ppImmediateContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Device1_GetImmediateContext1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device1_GetImmediateContext1 *args)
{
}

static inline void
npt_encode_ID3D11Device1_GetImmediateContext1_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11Device1_GetImmediateContext1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_GetImmediateContext1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device1_GetImmediateContext1(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11Device1_GetImmediateContext1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device1_GetImmediateContext1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_GetImmediateContext1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device1_GetImmediateContext1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_GetImmediateContext1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device1_GetImmediateContext1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device1_GetImmediateContext1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device1_GetImmediateContext1);

    if (ctx->id3d11device1_dispatch_overrides && ctx->id3d11device1_dispatch_overrides->GetImmediateContext1) {
        ctx->id3d11device1_dispatch_overrides->GetImmediateContext1(ctx, &args, _original);
    } else {
        _original(args._self, args.ppImmediateContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppImmediateContext && *args.ppImmediateContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppImmediateContext, *args.ppImmediateContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device1_GetImmediateContext1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device1::CreateDeferredContext1
 */

struct npt_command_ID3D11Device1_CreateDeferredContext1 {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext1 ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device1_CreateDeferredContext1_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11Device1_CreateDeferredContext1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->ContextFlags);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDeferredContext = _gid;
        args->ppDeferredContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDeferredContext) return;
        *args->ppDeferredContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device1_CreateDeferredContext1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device1_CreateDeferredContext1 *args)
{
}

static inline void
npt_encode_ID3D11Device1_CreateDeferredContext1_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11Device1_CreateDeferredContext1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateDeferredContext1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device1_CreateDeferredContext1(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11Device1_CreateDeferredContext1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device1_CreateDeferredContext1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateDeferredContext1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device1_CreateDeferredContext1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateDeferredContext1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device1_CreateDeferredContext1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device1_CreateDeferredContext1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device1_CreateDeferredContext1);

    if (ctx->id3d11device1_dispatch_overrides && ctx->id3d11device1_dispatch_overrides->CreateDeferredContext1) {
        args.ret = ctx->id3d11device1_dispatch_overrides->CreateDeferredContext1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ContextFlags, args.ppDeferredContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDeferredContext && *args.ppDeferredContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDeferredContext, *args.ppDeferredContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device1_CreateDeferredContext1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device1::CreateBlendState1
 */

struct npt_command_ID3D11Device1_CreateBlendState1 {
    void *_self;
    const D3D11_BLEND_DESC1 * pBlendStateDesc;
    ID3D11BlendState1 ** ppBlendState;
    /* Shadow: the guest-allocated id for ppBlendState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBlendState -> *ppBlendState} in the
     * context object table. */
    uint64_t _guest_id_ppBlendState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device1_CreateBlendState1_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device1_CreateBlendState1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pBlendStateDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BLEND_DESC1));
        if (!args->pBlendStateDesc) return;
        npt_decode_D3D11_BLEND_DESC1(dec, (D3D11_BLEND_DESC1 *)args->pBlendStateDesc);
    } else {
        args->pBlendStateDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppBlendState = _gid;
        args->ppBlendState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppBlendState) return;
        *args->ppBlendState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device1_CreateBlendState1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device1_CreateBlendState1 *args)
{
}

static inline void
npt_encode_ID3D11Device1_CreateBlendState1_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device1_CreateBlendState1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateBlendState1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppBlendState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device1_CreateBlendState1(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device1_CreateBlendState1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device1_CreateBlendState1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateBlendState1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device1_CreateBlendState1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateBlendState1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device1_CreateBlendState1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device1_CreateBlendState1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device1_CreateBlendState1);

    if (ctx->id3d11device1_dispatch_overrides && ctx->id3d11device1_dispatch_overrides->CreateBlendState1) {
        args.ret = ctx->id3d11device1_dispatch_overrides->CreateBlendState1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pBlendStateDesc, args.ppBlendState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppBlendState && *args.ppBlendState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppBlendState, *args.ppBlendState, NPT_OBJECT_TYPE_ID3D11BLENDSTATE1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device1_CreateBlendState1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device1::CreateRasterizerState1
 */

struct npt_command_ID3D11Device1_CreateRasterizerState1 {
    void *_self;
    const D3D11_RASTERIZER_DESC1 * pRasterizerDesc;
    ID3D11RasterizerState1 ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device1_CreateRasterizerState1_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11Device1_CreateRasterizerState1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pRasterizerDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_RASTERIZER_DESC1));
        if (!args->pRasterizerDesc) return;
        npt_decode_D3D11_RASTERIZER_DESC1(dec, (D3D11_RASTERIZER_DESC1 *)args->pRasterizerDesc);
    } else {
        args->pRasterizerDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppRasterizerState = _gid;
        args->ppRasterizerState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppRasterizerState) return;
        *args->ppRasterizerState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device1_CreateRasterizerState1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device1_CreateRasterizerState1 *args)
{
}

static inline void
npt_encode_ID3D11Device1_CreateRasterizerState1_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11Device1_CreateRasterizerState1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateRasterizerState1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device1_CreateRasterizerState1(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11Device1_CreateRasterizerState1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device1_CreateRasterizerState1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateRasterizerState1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device1_CreateRasterizerState1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateRasterizerState1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device1_CreateRasterizerState1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device1_CreateRasterizerState1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device1_CreateRasterizerState1);

    if (ctx->id3d11device1_dispatch_overrides && ctx->id3d11device1_dispatch_overrides->CreateRasterizerState1) {
        args.ret = ctx->id3d11device1_dispatch_overrides->CreateRasterizerState1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRasterizerDesc, args.ppRasterizerState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRasterizerState && *args.ppRasterizerState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppRasterizerState, *args.ppRasterizerState, NPT_OBJECT_TYPE_ID3D11RASTERIZERSTATE1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device1_CreateRasterizerState1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device1::CreateDeviceContextState
 */

struct npt_command_ID3D11Device1_CreateDeviceContextState {
    void *_self;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    UINT SDKVersion;
    const IID * EmulatedInterface;
    D3D_FEATURE_LEVEL * pChosenFeatureLevel;
    ID3DDeviceContextState ** ppContextState;
    /* Shadow: the guest-allocated id for ppContextState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppContextState -> *ppContextState} in the
     * context object table. */
    uint64_t _guest_id_ppContextState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device1_CreateDeviceContextState_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11Device1_CreateDeviceContextState *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Flags);
    uint64_t _cnt_pFeatureLevels = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFeatureLevels = npt_decode_array_count_unchecked(dec);
        args->pFeatureLevels = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D_FEATURE_LEVEL), _cnt_pFeatureLevels);
        if (!args->pFeatureLevels) return;
        npt_decode_D3D_FEATURE_LEVEL_array(dec, (D3D_FEATURE_LEVEL *)args->pFeatureLevels, _cnt_pFeatureLevels);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFeatureLevels = NULL;
    }
    npt_decode_UINT(dec, &args->FeatureLevels);
    npt_decode_UINT(dec, &args->SDKVersion);
    if (npt_decode_simple_pointer(dec)) {
        args->EmulatedInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->EmulatedInterface) return;
        npt_decode_IID(dec, (IID *)args->EmulatedInterface);
    } else {
        args->EmulatedInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppContextState = _gid;
        args->ppContextState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppContextState) return;
        *args->ppContextState = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pFeatureLevels && _cnt_pFeatureLevels < (uint64_t)(args->FeatureLevels)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





    args->pChosenFeatureLevel = npt_cs_decoder_alloc_temp(dec, sizeof(D3D_FEATURE_LEVEL));
    if (!args->pChosenFeatureLevel) return;
    memset(args->pChosenFeatureLevel, 0, sizeof(D3D_FEATURE_LEVEL));

}

static inline void
npt_replace_ID3D11Device1_CreateDeviceContextState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device1_CreateDeviceContextState *args)
{
}

static inline void
npt_encode_ID3D11Device1_CreateDeviceContextState_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11Device1_CreateDeviceContextState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_CreateDeviceContextState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pChosenFeatureLevel))
        npt_encode_D3D_FEATURE_LEVEL(enc, args->pChosenFeatureLevel);
/* skip args->ppContextState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device1_CreateDeviceContextState(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11Device1_CreateDeviceContextState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device1_CreateDeviceContextState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateDeviceContextState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device1_CreateDeviceContextState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_CreateDeviceContextState: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device1_CreateDeviceContextState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device1_CreateDeviceContextState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device1_CreateDeviceContextState);

    if (ctx->id3d11device1_dispatch_overrides && ctx->id3d11device1_dispatch_overrides->CreateDeviceContextState) {
        args.ret = ctx->id3d11device1_dispatch_overrides->CreateDeviceContextState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Flags, args.pFeatureLevels, args.FeatureLevels, args.SDKVersion, args.EmulatedInterface, args.pChosenFeatureLevel, args.ppContextState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppContextState && *args.ppContextState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppContextState, *args.ppContextState, NPT_OBJECT_TYPE_ID3DDEVICECONTEXTSTATE);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device1_CreateDeviceContextState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device1::OpenSharedResource1
 */

struct npt_command_ID3D11Device1_OpenSharedResource1 {
    void *_self;
    HANDLE hResource;
    const IID * returnedInterface;
    void ** ppResource;
    /* Shadow: the guest-allocated id for ppResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource -> *ppResource} in the
     * context object table. */
    uint64_t _guest_id_ppResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device1_OpenSharedResource1_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11Device1_OpenSharedResource1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hResource = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->returnedInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->returnedInterface) return;
        npt_decode_IID(dec, (IID *)args->returnedInterface);
    } else {
        args->returnedInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppResource = _gid;
        args->ppResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppResource) return;
        *args->ppResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device1_OpenSharedResource1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device1_OpenSharedResource1 *args)
{
    args->hResource = (HANDLE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->hResource);
}

static inline void
npt_encode_ID3D11Device1_OpenSharedResource1_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11Device1_OpenSharedResource1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_OpenSharedResource1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device1_OpenSharedResource1(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11Device1_OpenSharedResource1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device1_OpenSharedResource1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_OpenSharedResource1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device1_OpenSharedResource1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_OpenSharedResource1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device1_OpenSharedResource1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device1_OpenSharedResource1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device1_OpenSharedResource1);

    if (ctx->id3d11device1_dispatch_overrides && ctx->id3d11device1_dispatch_overrides->OpenSharedResource1) {
        args.ret = ctx->id3d11device1_dispatch_overrides->OpenSharedResource1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hResource, args.returnedInterface, args.ppResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppResource && *args.ppResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppResource, *args.ppResource,
            npt_object_type_from_iid(args.returnedInterface));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device1_OpenSharedResource1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device1::OpenSharedResourceByName
 */

struct npt_command_ID3D11Device1_OpenSharedResourceByName {
    void *_self;
    const WCHAR * lpName;
    DWORD dwDesiredAccess;
    const IID * returnedInterface;
    void ** ppResource;
    /* Shadow: the guest-allocated id for ppResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource -> *ppResource} in the
     * context object table. */
    uint64_t _guest_id_ppResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device1_OpenSharedResourceByName_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11Device1_OpenSharedResourceByName *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->lpName = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->lpName) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->lpName, _wstr_size);
        } else {
            args->lpName = NULL;
        }
    }
    npt_decode_DWORD(dec, &args->dwDesiredAccess);
    if (npt_decode_simple_pointer(dec)) {
        args->returnedInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->returnedInterface) return;
        npt_decode_IID(dec, (IID *)args->returnedInterface);
    } else {
        args->returnedInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppResource = _gid;
        args->ppResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppResource) return;
        *args->ppResource = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device1_OpenSharedResourceByName_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device1_OpenSharedResourceByName *args)
{
}

static inline void
npt_encode_ID3D11Device1_OpenSharedResourceByName_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11Device1_OpenSharedResourceByName *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device1,
                                 NPT_METHOD_ID3D11Device1_OpenSharedResourceByName),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppResource (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device1_OpenSharedResourceByName(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11Device1_OpenSharedResourceByName args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device1_OpenSharedResourceByName_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_OpenSharedResourceByName on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device1_OpenSharedResourceByName_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device1_OpenSharedResourceByName: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device1_OpenSharedResourceByName _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device1_OpenSharedResourceByName, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device1_OpenSharedResourceByName);

    if (ctx->id3d11device1_dispatch_overrides && ctx->id3d11device1_dispatch_overrides->OpenSharedResourceByName) {
        args.ret = ctx->id3d11device1_dispatch_overrides->OpenSharedResourceByName(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.lpName, args.dwDesiredAccess, args.returnedInterface, args.ppResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppResource && *args.ppResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppResource, *args.ppResource,
            npt_object_type_from_iid(args.returnedInterface));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device1_OpenSharedResourceByName_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device2::GetImmediateContext2
 */

struct npt_command_ID3D11Device2_GetImmediateContext2 {
    void *_self;
    ID3D11DeviceContext2 ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline void
npt_decode_ID3D11Device2_GetImmediateContext2_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11Device2_GetImmediateContext2 *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppImmediateContext = _gid;
        args->ppImmediateContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppImmediateContext) return;
        *args->ppImmediateContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Device2_GetImmediateContext2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device2_GetImmediateContext2 *args)
{
}

static inline void
npt_encode_ID3D11Device2_GetImmediateContext2_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11Device2_GetImmediateContext2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_GetImmediateContext2),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device2_GetImmediateContext2(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11Device2_GetImmediateContext2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device2_GetImmediateContext2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_GetImmediateContext2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device2_GetImmediateContext2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_GetImmediateContext2: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device2_GetImmediateContext2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device2_GetImmediateContext2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device2_GetImmediateContext2);

    if (ctx->id3d11device2_dispatch_overrides && ctx->id3d11device2_dispatch_overrides->GetImmediateContext2) {
        ctx->id3d11device2_dispatch_overrides->GetImmediateContext2(ctx, &args, _original);
    } else {
        _original(args._self, args.ppImmediateContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppImmediateContext && *args.ppImmediateContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppImmediateContext, *args.ppImmediateContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device2_GetImmediateContext2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device2::CreateDeferredContext2
 */

struct npt_command_ID3D11Device2_CreateDeferredContext2 {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext2 ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device2_CreateDeferredContext2_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11Device2_CreateDeferredContext2 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->ContextFlags);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDeferredContext = _gid;
        args->ppDeferredContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDeferredContext) return;
        *args->ppDeferredContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device2_CreateDeferredContext2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device2_CreateDeferredContext2 *args)
{
}

static inline void
npt_encode_ID3D11Device2_CreateDeferredContext2_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11Device2_CreateDeferredContext2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_CreateDeferredContext2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device2_CreateDeferredContext2(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11Device2_CreateDeferredContext2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device2_CreateDeferredContext2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_CreateDeferredContext2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device2_CreateDeferredContext2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_CreateDeferredContext2: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device2_CreateDeferredContext2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device2_CreateDeferredContext2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device2_CreateDeferredContext2);

    if (ctx->id3d11device2_dispatch_overrides && ctx->id3d11device2_dispatch_overrides->CreateDeferredContext2) {
        args.ret = ctx->id3d11device2_dispatch_overrides->CreateDeferredContext2(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ContextFlags, args.ppDeferredContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDeferredContext && *args.ppDeferredContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDeferredContext, *args.ppDeferredContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device2_CreateDeferredContext2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device2::GetResourceTiling
 */

struct npt_command_ID3D11Device2_GetResourceTiling {
    void *_self;
    ID3D11Resource * pTiledResource;
    UINT * pNumTilesForEntireResource;
    D3D11_PACKED_MIP_DESC * pPackedMipDesc;
    D3D11_TILE_SHAPE * pStandardTileShapeForNonPackedMips;
    UINT * pNumSubresourceTilings;
    UINT FirstSubresourceTilingToGet;
    D3D11_SUBRESOURCE_TILING * pSubresourceTilingsForNonPackedMips;
};

static inline void
npt_decode_ID3D11Device2_GetResourceTiling_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11Device2_GetResourceTiling *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTiledResource = (ID3D11Resource *)npt_object_from_id(_id);
    }



    if (npt_decode_simple_pointer(dec)) {
        args->pNumSubresourceTilings = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumSubresourceTilings) return;
        npt_decode_UINT(dec, (UINT *)args->pNumSubresourceTilings);
    } else {
        args->pNumSubresourceTilings = NULL;
    }
    npt_decode_UINT(dec, &args->FirstSubresourceTilingToGet);

    /* Allocate temp storage for output-only parameters */

    args->pNumTilesForEntireResource = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pNumTilesForEntireResource) return;
    memset(args->pNumTilesForEntireResource, 0, sizeof(UINT));
    args->pPackedMipDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_PACKED_MIP_DESC));
    if (!args->pPackedMipDesc) return;
    memset(args->pPackedMipDesc, 0, sizeof(D3D11_PACKED_MIP_DESC));
    args->pStandardTileShapeForNonPackedMips = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILE_SHAPE));
    if (!args->pStandardTileShapeForNonPackedMips) return;
    memset(args->pStandardTileShapeForNonPackedMips, 0, sizeof(D3D11_TILE_SHAPE));


    if ((args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0)) {
        args->pSubresourceTilingsForNonPackedMips = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_SUBRESOURCE_TILING), (args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0));
        if (!args->pSubresourceTilingsForNonPackedMips) return;
        memset(args->pSubresourceTilingsForNonPackedMips, 0, sizeof(D3D11_SUBRESOURCE_TILING) * (args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0));
    } else {
        args->pSubresourceTilingsForNonPackedMips = NULL;
    }
}

static inline void
npt_replace_ID3D11Device2_GetResourceTiling_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device2_GetResourceTiling *args)
{
    args->pTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTiledResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device2_GetResourceTiling_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11Device2_GetResourceTiling *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_GetResourceTiling),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumTilesForEntireResource))
        npt_encode_UINT(enc, args->pNumTilesForEntireResource);
    if (npt_encode_simple_pointer(enc, args->pPackedMipDesc))
        npt_encode_D3D11_PACKED_MIP_DESC(enc, args->pPackedMipDesc);
    if (npt_encode_simple_pointer(enc, args->pStandardTileShapeForNonPackedMips))
        npt_encode_D3D11_TILE_SHAPE(enc, args->pStandardTileShapeForNonPackedMips);
    if (npt_encode_simple_pointer(enc, args->pNumSubresourceTilings))
        npt_encode_UINT(enc, args->pNumSubresourceTilings);
    if (args->pSubresourceTilingsForNonPackedMips) {
        npt_encode_array_count(enc, (args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0));
        for (uint32_t _i = 0; _i < (uint32_t)((args->pNumSubresourceTilings ? *args->pNumSubresourceTilings : 0)); _i++)
            npt_encode_D3D11_SUBRESOURCE_TILING(enc, &args->pSubresourceTilingsForNonPackedMips[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11Device2_GetResourceTiling(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11Device2_GetResourceTiling args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device2_GetResourceTiling_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_GetResourceTiling on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device2_GetResourceTiling_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_GetResourceTiling: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device2_GetResourceTiling _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device2_GetResourceTiling, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device2_GetResourceTiling);

    if (ctx->id3d11device2_dispatch_overrides && ctx->id3d11device2_dispatch_overrides->GetResourceTiling) {
        ctx->id3d11device2_dispatch_overrides->GetResourceTiling(ctx, &args, _original);
    } else {
        _original(args._self, args.pTiledResource, args.pNumTilesForEntireResource, args.pPackedMipDesc, args.pStandardTileShapeForNonPackedMips, args.pNumSubresourceTilings, args.FirstSubresourceTilingToGet, args.pSubresourceTilingsForNonPackedMips);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device2_GetResourceTiling_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device2::CheckMultisampleQualityLevels1
 */

struct npt_command_ID3D11Device2_CheckMultisampleQualityLevels1 {
    void *_self;
    DXGI_FORMAT Format;
    UINT SampleCount;
    UINT Flags;
    UINT * pNumQualityLevels;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device2_CheckMultisampleQualityLevels1_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D11Device2_CheckMultisampleQualityLevels1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    npt_decode_UINT(dec, &args->SampleCount);
    npt_decode_UINT(dec, &args->Flags);

    /* Allocate temp storage for output-only parameters */



    args->pNumQualityLevels = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pNumQualityLevels) return;
    memset(args->pNumQualityLevels, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11Device2_CheckMultisampleQualityLevels1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device2_CheckMultisampleQualityLevels1 *args)
{
}

static inline void
npt_encode_ID3D11Device2_CheckMultisampleQualityLevels1_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D11Device2_CheckMultisampleQualityLevels1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device2,
                                 NPT_METHOD_ID3D11Device2_CheckMultisampleQualityLevels1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumQualityLevels))
        npt_encode_UINT(enc, args->pNumQualityLevels);
}

static inline void
npt_dispatch_ID3D11Device2_CheckMultisampleQualityLevels1(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D11Device2_CheckMultisampleQualityLevels1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device2_CheckMultisampleQualityLevels1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_CheckMultisampleQualityLevels1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device2_CheckMultisampleQualityLevels1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device2_CheckMultisampleQualityLevels1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device2_CheckMultisampleQualityLevels1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device2_CheckMultisampleQualityLevels1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device2_CheckMultisampleQualityLevels1);

    if (ctx->id3d11device2_dispatch_overrides && ctx->id3d11device2_dispatch_overrides->CheckMultisampleQualityLevels1) {
        args.ret = ctx->id3d11device2_dispatch_overrides->CheckMultisampleQualityLevels1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Format, args.SampleCount, args.Flags, args.pNumQualityLevels);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device2_CheckMultisampleQualityLevels1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateTexture2D1
 */

struct npt_command_ID3D11Device3_CreateTexture2D1 {
    void *_self;
    const D3D11_TEXTURE2D_DESC1 * pDesc1;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture2D1 ** ppTexture2D;
    /* Shadow: the guest-allocated id for ppTexture2D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture2D -> *ppTexture2D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture2D;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateTexture2D1_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11Device3_CreateTexture2D1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc1 = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TEXTURE2D_DESC1));
        if (!args->pDesc1) return;
        npt_decode_D3D11_TEXTURE2D_DESC1(dec, (D3D11_TEXTURE2D_DESC1 *)args->pDesc1);
    } else {
        args->pDesc1 = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    uint64_t _cnt_pInitialData = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pInitialData = npt_decode_array_count_unchecked(dec);
        args->pInitialData = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_SUBRESOURCE_DATA), _cnt_pInitialData);
        if (!args->pInitialData) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pInitialData; _i++)
            npt_decode_D3D11_SUBRESOURCE_DATA(dec, (D3D11_SUBRESOURCE_DATA *)&args->pInitialData[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pInitialData = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppTexture2D = _gid;
        args->ppTexture2D = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppTexture2D) return;
        *args->ppTexture2D = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pInitialData && _cnt_pInitialData < (uint64_t)(args->pDesc1->MipLevels * args->pDesc1->ArraySize)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device3_CreateTexture2D1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateTexture2D1 *args)
{
}

static inline void
npt_encode_ID3D11Device3_CreateTexture2D1_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11Device3_CreateTexture2D1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateTexture2D1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppTexture2D (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateTexture2D1(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateTexture2D1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateTexture2D1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateTexture2D1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateTexture2D1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateTexture2D1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateTexture2D1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateTexture2D1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateTexture2D1);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateTexture2D1) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateTexture2D1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc1, args.pInitialData, args.ppTexture2D);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppTexture2D && *args.ppTexture2D)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppTexture2D, *args.ppTexture2D, NPT_OBJECT_TYPE_ID3D11TEXTURE2D1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateTexture2D1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateTexture3D1
 */

struct npt_command_ID3D11Device3_CreateTexture3D1 {
    void *_self;
    const D3D11_TEXTURE3D_DESC1 * pDesc1;
    const D3D11_SUBRESOURCE_DATA * pInitialData;
    ID3D11Texture3D1 ** ppTexture3D;
    /* Shadow: the guest-allocated id for ppTexture3D arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTexture3D -> *ppTexture3D} in the
     * context object table. */
    uint64_t _guest_id_ppTexture3D;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateTexture3D1_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11Device3_CreateTexture3D1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc1 = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TEXTURE3D_DESC1));
        if (!args->pDesc1) return;
        npt_decode_D3D11_TEXTURE3D_DESC1(dec, (D3D11_TEXTURE3D_DESC1 *)args->pDesc1);
    } else {
        args->pDesc1 = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    uint64_t _cnt_pInitialData = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pInitialData = npt_decode_array_count_unchecked(dec);
        args->pInitialData = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_SUBRESOURCE_DATA), _cnt_pInitialData);
        if (!args->pInitialData) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pInitialData; _i++)
            npt_decode_D3D11_SUBRESOURCE_DATA(dec, (D3D11_SUBRESOURCE_DATA *)&args->pInitialData[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pInitialData = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppTexture3D = _gid;
        args->ppTexture3D = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppTexture3D) return;
        *args->ppTexture3D = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pInitialData && _cnt_pInitialData < (uint64_t)(args->pDesc1->MipLevels)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device3_CreateTexture3D1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateTexture3D1 *args)
{
}

static inline void
npt_encode_ID3D11Device3_CreateTexture3D1_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11Device3_CreateTexture3D1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateTexture3D1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppTexture3D (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateTexture3D1(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateTexture3D1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateTexture3D1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateTexture3D1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateTexture3D1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateTexture3D1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateTexture3D1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateTexture3D1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateTexture3D1);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateTexture3D1) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateTexture3D1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc1, args.pInitialData, args.ppTexture3D);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppTexture3D && *args.ppTexture3D)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppTexture3D, *args.ppTexture3D, NPT_OBJECT_TYPE_ID3D11TEXTURE3D1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateTexture3D1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateRasterizerState2
 */

struct npt_command_ID3D11Device3_CreateRasterizerState2 {
    void *_self;
    const D3D11_RASTERIZER_DESC2 * pRasterizerDesc;
    ID3D11RasterizerState2 ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateRasterizerState2_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11Device3_CreateRasterizerState2 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pRasterizerDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_RASTERIZER_DESC2));
        if (!args->pRasterizerDesc) return;
        npt_decode_D3D11_RASTERIZER_DESC2(dec, (D3D11_RASTERIZER_DESC2 *)args->pRasterizerDesc);
    } else {
        args->pRasterizerDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppRasterizerState = _gid;
        args->ppRasterizerState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppRasterizerState) return;
        *args->ppRasterizerState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device3_CreateRasterizerState2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateRasterizerState2 *args)
{
}

static inline void
npt_encode_ID3D11Device3_CreateRasterizerState2_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11Device3_CreateRasterizerState2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateRasterizerState2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateRasterizerState2(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateRasterizerState2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateRasterizerState2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateRasterizerState2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateRasterizerState2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateRasterizerState2: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateRasterizerState2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateRasterizerState2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateRasterizerState2);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateRasterizerState2) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateRasterizerState2(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRasterizerDesc, args.ppRasterizerState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRasterizerState && *args.ppRasterizerState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppRasterizerState, *args.ppRasterizerState, NPT_OBJECT_TYPE_ID3D11RASTERIZERSTATE2);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateRasterizerState2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateShaderResourceView1
 */

struct npt_command_ID3D11Device3_CreateShaderResourceView1 {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_SHADER_RESOURCE_VIEW_DESC1 * pDesc1;
    ID3D11ShaderResourceView1 ** ppSRView1;
    /* Shadow: the guest-allocated id for ppSRView1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSRView1 -> *ppSRView1} in the
     * context object table. */
    uint64_t _guest_id_ppSRView1;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateShaderResourceView1_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D11Device3_CreateShaderResourceView1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc1 = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_SHADER_RESOURCE_VIEW_DESC1));
        if (!args->pDesc1) return;
        npt_decode_D3D11_SHADER_RESOURCE_VIEW_DESC1(dec, (D3D11_SHADER_RESOURCE_VIEW_DESC1 *)args->pDesc1);
    } else {
        args->pDesc1 = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSRView1 = _gid;
        args->ppSRView1 = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSRView1) return;
        *args->ppSRView1 = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device3_CreateShaderResourceView1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateShaderResourceView1 *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device3_CreateShaderResourceView1_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D11Device3_CreateShaderResourceView1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateShaderResourceView1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSRView1 (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateShaderResourceView1(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateShaderResourceView1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateShaderResourceView1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateShaderResourceView1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateShaderResourceView1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateShaderResourceView1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateShaderResourceView1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateShaderResourceView1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateShaderResourceView1);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateShaderResourceView1) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateShaderResourceView1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc1, args.ppSRView1);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSRView1 && *args.ppSRView1)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSRView1, *args.ppSRView1, NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateShaderResourceView1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateUnorderedAccessView1
 */

struct npt_command_ID3D11Device3_CreateUnorderedAccessView1 {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_UNORDERED_ACCESS_VIEW_DESC1 * pDesc1;
    ID3D11UnorderedAccessView1 ** ppUAView1;
    /* Shadow: the guest-allocated id for ppUAView1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppUAView1 -> *ppUAView1} in the
     * context object table. */
    uint64_t _guest_id_ppUAView1;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateUnorderedAccessView1_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11Device3_CreateUnorderedAccessView1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc1 = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_UNORDERED_ACCESS_VIEW_DESC1));
        if (!args->pDesc1) return;
        npt_decode_D3D11_UNORDERED_ACCESS_VIEW_DESC1(dec, (D3D11_UNORDERED_ACCESS_VIEW_DESC1 *)args->pDesc1);
    } else {
        args->pDesc1 = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppUAView1 = _gid;
        args->ppUAView1 = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppUAView1) return;
        *args->ppUAView1 = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device3_CreateUnorderedAccessView1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateUnorderedAccessView1 *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device3_CreateUnorderedAccessView1_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11Device3_CreateUnorderedAccessView1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateUnorderedAccessView1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppUAView1 (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateUnorderedAccessView1(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateUnorderedAccessView1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateUnorderedAccessView1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateUnorderedAccessView1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateUnorderedAccessView1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateUnorderedAccessView1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateUnorderedAccessView1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateUnorderedAccessView1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateUnorderedAccessView1);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateUnorderedAccessView1) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateUnorderedAccessView1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc1, args.ppUAView1);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppUAView1 && *args.ppUAView1)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppUAView1, *args.ppUAView1, NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateUnorderedAccessView1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateRenderTargetView1
 */

struct npt_command_ID3D11Device3_CreateRenderTargetView1 {
    void *_self;
    ID3D11Resource * pResource;
    const D3D11_RENDER_TARGET_VIEW_DESC1 * pDesc1;
    ID3D11RenderTargetView1 ** ppRTView1;
    /* Shadow: the guest-allocated id for ppRTView1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRTView1 -> *ppRTView1} in the
     * context object table. */
    uint64_t _guest_id_ppRTView1;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateRenderTargetView1_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11Device3_CreateRenderTargetView1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc1 = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_RENDER_TARGET_VIEW_DESC1));
        if (!args->pDesc1) return;
        npt_decode_D3D11_RENDER_TARGET_VIEW_DESC1(dec, (D3D11_RENDER_TARGET_VIEW_DESC1 *)args->pDesc1);
    } else {
        args->pDesc1 = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppRTView1 = _gid;
        args->ppRTView1 = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppRTView1) return;
        *args->ppRTView1 = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device3_CreateRenderTargetView1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateRenderTargetView1 *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device3_CreateRenderTargetView1_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11Device3_CreateRenderTargetView1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateRenderTargetView1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRTView1 (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateRenderTargetView1(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateRenderTargetView1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateRenderTargetView1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateRenderTargetView1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateRenderTargetView1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateRenderTargetView1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateRenderTargetView1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateRenderTargetView1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateRenderTargetView1);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateRenderTargetView1) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateRenderTargetView1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.pDesc1, args.ppRTView1);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRTView1 && *args.ppRTView1)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppRTView1, *args.ppRTView1, NPT_OBJECT_TYPE_ID3D11RENDERTARGETVIEW1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateRenderTargetView1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateQuery1
 */

struct npt_command_ID3D11Device3_CreateQuery1 {
    void *_self;
    const D3D11_QUERY_DESC1 * pQueryDesc1;
    ID3D11Query1 ** ppQuery1;
    /* Shadow: the guest-allocated id for ppQuery1 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppQuery1 -> *ppQuery1} in the
     * context object table. */
    uint64_t _guest_id_ppQuery1;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateQuery1_args_temp(struct npt_cs_decoder *dec,
                                                struct npt_command_ID3D11Device3_CreateQuery1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pQueryDesc1 = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_QUERY_DESC1));
        if (!args->pQueryDesc1) return;
        npt_decode_D3D11_QUERY_DESC1(dec, (D3D11_QUERY_DESC1 *)args->pQueryDesc1);
    } else {
        args->pQueryDesc1 = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppQuery1 = _gid;
        args->ppQuery1 = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppQuery1) return;
        *args->ppQuery1 = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device3_CreateQuery1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateQuery1 *args)
{
}

static inline void
npt_encode_ID3D11Device3_CreateQuery1_reply(struct npt_cs_encoder *enc,
                                            const struct npt_command_ID3D11Device3_CreateQuery1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateQuery1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppQuery1 (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateQuery1(struct npt_dispatch_context *ctx,
                                        uint32_t cmd_flags,
                                        uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateQuery1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateQuery1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateQuery1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateQuery1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateQuery1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateQuery1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateQuery1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateQuery1);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateQuery1) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateQuery1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pQueryDesc1, args.ppQuery1);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppQuery1 && *args.ppQuery1)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppQuery1, *args.ppQuery1, NPT_OBJECT_TYPE_ID3D11QUERY1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateQuery1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::GetImmediateContext3
 */

struct npt_command_ID3D11Device3_GetImmediateContext3 {
    void *_self;
    ID3D11DeviceContext3 ** ppImmediateContext;
    /* Shadow: the guest-allocated id for ppImmediateContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppImmediateContext -> *ppImmediateContext} in the
     * context object table. */
    uint64_t _guest_id_ppImmediateContext;
};

static inline void
npt_decode_ID3D11Device3_GetImmediateContext3_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11Device3_GetImmediateContext3 *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppImmediateContext = _gid;
        args->ppImmediateContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppImmediateContext) return;
        *args->ppImmediateContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Device3_GetImmediateContext3_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_GetImmediateContext3 *args)
{
}

static inline void
npt_encode_ID3D11Device3_GetImmediateContext3_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11Device3_GetImmediateContext3 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_GetImmediateContext3),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_GetImmediateContext3(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11Device3_GetImmediateContext3 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_GetImmediateContext3_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_GetImmediateContext3 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_GetImmediateContext3_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_GetImmediateContext3: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_GetImmediateContext3 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_GetImmediateContext3, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_GetImmediateContext3);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->GetImmediateContext3) {
        ctx->id3d11device3_dispatch_overrides->GetImmediateContext3(ctx, &args, _original);
    } else {
        _original(args._self, args.ppImmediateContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppImmediateContext && *args.ppImmediateContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppImmediateContext, *args.ppImmediateContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT3);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_GetImmediateContext3_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::CreateDeferredContext3
 */

struct npt_command_ID3D11Device3_CreateDeferredContext3 {
    void *_self;
    UINT ContextFlags;
    ID3D11DeviceContext3 ** ppDeferredContext;
    /* Shadow: the guest-allocated id for ppDeferredContext arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDeferredContext -> *ppDeferredContext} in the
     * context object table. */
    uint64_t _guest_id_ppDeferredContext;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device3_CreateDeferredContext3_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11Device3_CreateDeferredContext3 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->ContextFlags);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDeferredContext = _gid;
        args->ppDeferredContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDeferredContext) return;
        *args->ppDeferredContext = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Device3_CreateDeferredContext3_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_CreateDeferredContext3 *args)
{
}

static inline void
npt_encode_ID3D11Device3_CreateDeferredContext3_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11Device3_CreateDeferredContext3 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_CreateDeferredContext3),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDeferredContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device3_CreateDeferredContext3(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11Device3_CreateDeferredContext3 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_CreateDeferredContext3_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateDeferredContext3 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_CreateDeferredContext3_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_CreateDeferredContext3: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_CreateDeferredContext3 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_CreateDeferredContext3, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_CreateDeferredContext3);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->CreateDeferredContext3) {
        args.ret = ctx->id3d11device3_dispatch_overrides->CreateDeferredContext3(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ContextFlags, args.ppDeferredContext);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDeferredContext && *args.ppDeferredContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDeferredContext, *args.ppDeferredContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT3);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_CreateDeferredContext3_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::WriteToSubresource
 */

struct npt_command_ID3D11Device3_WriteToSubresource {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    const D3D11_BOX * pDstBox;
    const void * pSrcData;
    UINT SrcRowPitch;
    UINT SrcDepthPitch;
};

static inline void
npt_decode_ID3D11Device3_WriteToSubresource_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11Device3_WriteToSubresource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    if (npt_decode_simple_pointer(dec)) {
        args->pDstBox = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BOX));
        if (!args->pDstBox) return;
        npt_decode_D3D11_BOX(dec, (D3D11_BOX *)args->pDstBox);
    } else {
        args->pDstBox = NULL;
    }
    /* ERROR: pSrcData (void) is not serializable */
    return;
    npt_decode_UINT(dec, &args->SrcRowPitch);
    npt_decode_UINT(dec, &args->SrcDepthPitch);
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D11Device3_WriteToSubresource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_WriteToSubresource *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device3_WriteToSubresource_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11Device3_WriteToSubresource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_WriteToSubresource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device3_WriteToSubresource(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11Device3_WriteToSubresource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_WriteToSubresource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_WriteToSubresource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_WriteToSubresource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_WriteToSubresource: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_WriteToSubresource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_WriteToSubresource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_WriteToSubresource);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->WriteToSubresource) {
        ctx->id3d11device3_dispatch_overrides->WriteToSubresource(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.pDstBox, args.pSrcData, args.SrcRowPitch, args.SrcDepthPitch);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_WriteToSubresource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device3::ReadFromSubresource
 */

struct npt_command_ID3D11Device3_ReadFromSubresource {
    void *_self;
    void * pDstData;
    UINT DstRowPitch;
    UINT DstDepthPitch;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    const D3D11_BOX * pSrcBox;
};

static inline void
npt_decode_ID3D11Device3_ReadFromSubresource_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11Device3_ReadFromSubresource *args)
{
    /* Decode input parameters from the wire */

    npt_decode_UINT(dec, &args->DstRowPitch);
    npt_decode_UINT(dec, &args->DstDepthPitch);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->SrcSubresource);
    if (npt_decode_simple_pointer(dec)) {
        args->pSrcBox = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BOX));
        if (!args->pSrcBox) return;
        npt_decode_D3D11_BOX(dec, (D3D11_BOX *)args->pSrcBox);
    } else {
        args->pSrcBox = NULL;
    }
    /* Allocate temp storage for output-only parameters */
    args->pDstData = npt_cs_decoder_alloc_temp(dec, 1);
    if (!args->pDstData) return;
    memset(args->pDstData, 0, 1);





}

static inline void
npt_replace_ID3D11Device3_ReadFromSubresource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device3_ReadFromSubresource *args)
{
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11Device3_ReadFromSubresource_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11Device3_ReadFromSubresource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device3,
                                 NPT_METHOD_ID3D11Device3_ReadFromSubresource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    /* ERROR: pDstData (void) is not serializable */
    return;
}

static inline void
npt_dispatch_ID3D11Device3_ReadFromSubresource(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11Device3_ReadFromSubresource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device3_ReadFromSubresource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_ReadFromSubresource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device3_ReadFromSubresource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device3_ReadFromSubresource: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device3_ReadFromSubresource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device3_ReadFromSubresource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device3_ReadFromSubresource);

    if (ctx->id3d11device3_dispatch_overrides && ctx->id3d11device3_dispatch_overrides->ReadFromSubresource) {
        ctx->id3d11device3_dispatch_overrides->ReadFromSubresource(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstData, args.DstRowPitch, args.DstDepthPitch, args.pSrcResource, args.SrcSubresource, args.pSrcBox);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device3_ReadFromSubresource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device4::RegisterDeviceRemovedEvent
 */

struct npt_command_ID3D11Device4_RegisterDeviceRemovedEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device4_RegisterDeviceRemovedEvent_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11Device4_RegisterDeviceRemovedEvent *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_ID3D11Device4_RegisterDeviceRemovedEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device4_RegisterDeviceRemovedEvent *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_ID3D11Device4_RegisterDeviceRemovedEvent_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11Device4_RegisterDeviceRemovedEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device4,
                                 NPT_METHOD_ID3D11Device4_RegisterDeviceRemovedEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_ID3D11Device4_RegisterDeviceRemovedEvent(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11Device4_RegisterDeviceRemovedEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device4_RegisterDeviceRemovedEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device4_RegisterDeviceRemovedEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device4_RegisterDeviceRemovedEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device4_RegisterDeviceRemovedEvent: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device4_RegisterDeviceRemovedEvent _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device4_RegisterDeviceRemovedEvent, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device4_RegisterDeviceRemovedEvent);

    if (ctx->id3d11device4_dispatch_overrides && ctx->id3d11device4_dispatch_overrides->RegisterDeviceRemovedEvent) {
        args.ret = ctx->id3d11device4_dispatch_overrides->RegisterDeviceRemovedEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hEvent, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device4_RegisterDeviceRemovedEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device4::UnregisterDeviceRemoved
 */

struct npt_command_ID3D11Device4_UnregisterDeviceRemoved {
    void *_self;
    DWORD dwCookie;
};

static inline void
npt_decode_ID3D11Device4_UnregisterDeviceRemoved_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11Device4_UnregisterDeviceRemoved *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DWORD(dec, &args->dwCookie);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Device4_UnregisterDeviceRemoved_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device4_UnregisterDeviceRemoved *args)
{
}

static inline void
npt_encode_ID3D11Device4_UnregisterDeviceRemoved_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11Device4_UnregisterDeviceRemoved *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device4,
                                 NPT_METHOD_ID3D11Device4_UnregisterDeviceRemoved),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Device4_UnregisterDeviceRemoved(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11Device4_UnregisterDeviceRemoved args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device4_UnregisterDeviceRemoved_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device4_UnregisterDeviceRemoved on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device4_UnregisterDeviceRemoved_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device4_UnregisterDeviceRemoved: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device4_UnregisterDeviceRemoved _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device4_UnregisterDeviceRemoved, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device4_UnregisterDeviceRemoved);

    if (ctx->id3d11device4_dispatch_overrides && ctx->id3d11device4_dispatch_overrides->UnregisterDeviceRemoved) {
        ctx->id3d11device4_dispatch_overrides->UnregisterDeviceRemoved(ctx, &args, _original);
    } else {
        _original(args._self, args.dwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device4_UnregisterDeviceRemoved_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device5::OpenSharedFence
 */

struct npt_command_ID3D11Device5_OpenSharedFence {
    void *_self;
    HANDLE hFence;
    const IID * ReturnedInterface;
    void ** ppFence;
    /* Shadow: the guest-allocated id for ppFence arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppFence -> *ppFence} in the
     * context object table. */
    uint64_t _guest_id_ppFence;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device5_OpenSharedFence_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Device5_OpenSharedFence *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hFence = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->ReturnedInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->ReturnedInterface) return;
        npt_decode_IID(dec, (IID *)args->ReturnedInterface);
    } else {
        args->ReturnedInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppFence = _gid;
        args->ppFence = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppFence) return;
        *args->ppFence = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11Device5_OpenSharedFence_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device5_OpenSharedFence *args)
{
    args->hFence = (HANDLE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->hFence);
}

static inline void
npt_encode_ID3D11Device5_OpenSharedFence_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Device5_OpenSharedFence *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                 NPT_METHOD_ID3D11Device5_OpenSharedFence),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppFence (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device5_OpenSharedFence(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Device5_OpenSharedFence args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device5_OpenSharedFence_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device5_OpenSharedFence on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device5_OpenSharedFence_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device5_OpenSharedFence: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device5_OpenSharedFence _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device5_OpenSharedFence, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device5_OpenSharedFence);

    if (ctx->id3d11device5_dispatch_overrides && ctx->id3d11device5_dispatch_overrides->OpenSharedFence) {
        args.ret = ctx->id3d11device5_dispatch_overrides->OpenSharedFence(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hFence, args.ReturnedInterface, args.ppFence);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppFence && *args.ppFence)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppFence, *args.ppFence,
            npt_object_type_from_iid(args.ReturnedInterface));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device5_OpenSharedFence_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Device5::CreateFence
 */

struct npt_command_ID3D11Device5_CreateFence {
    void *_self;
    UINT64 InitialValue;
    D3D11_FENCE_FLAG Flags;
    const IID * ReturnedInterface;
    void ** ppFence;
    /* Shadow: the guest-allocated id for ppFence arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppFence -> *ppFence} in the
     * context object table. */
    uint64_t _guest_id_ppFence;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Device5_CreateFence_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D11Device5_CreateFence *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT64(dec, &args->InitialValue);
    npt_decode_D3D11_FENCE_FLAG(dec, &args->Flags);
    if (npt_decode_simple_pointer(dec)) {
        args->ReturnedInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->ReturnedInterface) return;
        npt_decode_IID(dec, (IID *)args->ReturnedInterface);
    } else {
        args->ReturnedInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppFence = _gid;
        args->ppFence = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppFence) return;
        *args->ppFence = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11Device5_CreateFence_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Device5_CreateFence *args)
{
}

static inline void
npt_encode_ID3D11Device5_CreateFence_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D11Device5_CreateFence *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Device5,
                                 NPT_METHOD_ID3D11Device5_CreateFence),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppFence (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11Device5_CreateFence(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D11Device5_CreateFence args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Device5_CreateFence_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICE5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device5_CreateFence on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Device5_CreateFence_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Device5_CreateFence: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Device5_CreateFence _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Device5_CreateFence, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Device5_CreateFence);

    if (ctx->id3d11device5_dispatch_overrides && ctx->id3d11device5_dispatch_overrides->CreateFence) {
        args.ret = ctx->id3d11device5_dispatch_overrides->CreateFence(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.InitialValue, args.Flags, args.ReturnedInterface, args.ppFence);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppFence && *args.ppFence)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppFence, *args.ppFence,
            npt_object_type_from_iid(args.ReturnedInterface));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Device5_CreateFence_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11DEVICE_H */

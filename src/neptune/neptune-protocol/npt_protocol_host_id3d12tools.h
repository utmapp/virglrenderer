/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12tools.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D12TOOLS_H
#define NPT_PROTOCOL_HOST_ID3D12TOOLS_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12Tools::EnableShaderInstrumentation
 */

struct npt_command_ID3D12Tools_EnableShaderInstrumentation {
    void *_self;
    BOOL bEnable;
};

static inline void
npt_decode_ID3D12Tools_EnableShaderInstrumentation_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D12Tools_EnableShaderInstrumentation *args)
{
    /* Decode input parameters from the wire */
    npt_decode_BOOL(dec, &args->bEnable);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12Tools_EnableShaderInstrumentation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Tools_EnableShaderInstrumentation *args)
{
}

static inline void
npt_encode_ID3D12Tools_EnableShaderInstrumentation_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D12Tools_EnableShaderInstrumentation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Tools,
                                 NPT_METHOD_ID3D12Tools_EnableShaderInstrumentation),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Tools_EnableShaderInstrumentation(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D12Tools_EnableShaderInstrumentation args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Tools_EnableShaderInstrumentation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12TOOLS);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Tools_EnableShaderInstrumentation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Tools_EnableShaderInstrumentation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Tools_EnableShaderInstrumentation: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Tools_EnableShaderInstrumentation _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Tools_EnableShaderInstrumentation, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Tools_EnableShaderInstrumentation);

    if (ctx->id3d12tools_dispatch_overrides && ctx->id3d12tools_dispatch_overrides->EnableShaderInstrumentation) {
        ctx->id3d12tools_dispatch_overrides->EnableShaderInstrumentation(ctx, &args, _original);
    } else {
        _original(args._self, args.bEnable);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Tools_EnableShaderInstrumentation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Tools::ShaderInstrumentationEnabled
 */

struct npt_command_ID3D12Tools_ShaderInstrumentationEnabled {
    void *_self;
    BOOL ret;
};

static inline void
npt_decode_ID3D12Tools_ShaderInstrumentationEnabled_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D12Tools_ShaderInstrumentationEnabled *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12Tools_ShaderInstrumentationEnabled_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Tools_ShaderInstrumentationEnabled *args)
{
}

static inline void
npt_encode_ID3D12Tools_ShaderInstrumentationEnabled_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D12Tools_ShaderInstrumentationEnabled *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Tools,
                                 NPT_METHOD_ID3D12Tools_ShaderInstrumentationEnabled),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Tools_ShaderInstrumentationEnabled(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D12Tools_ShaderInstrumentationEnabled args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Tools_ShaderInstrumentationEnabled_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12TOOLS);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Tools_ShaderInstrumentationEnabled on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Tools_ShaderInstrumentationEnabled_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Tools_ShaderInstrumentationEnabled: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Tools_ShaderInstrumentationEnabled _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Tools_ShaderInstrumentationEnabled, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Tools_ShaderInstrumentationEnabled);

    if (ctx->id3d12tools_dispatch_overrides && ctx->id3d12tools_dispatch_overrides->ShaderInstrumentationEnabled) {
        args.ret = ctx->id3d12tools_dispatch_overrides->ShaderInstrumentationEnabled(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Tools_ShaderInstrumentationEnabled_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D12TOOLS_H */

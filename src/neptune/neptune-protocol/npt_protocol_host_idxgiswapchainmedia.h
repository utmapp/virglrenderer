/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiswapchainmedia.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGISWAPCHAINMEDIA_H
#define NPT_PROTOCOL_HOST_IDXGISWAPCHAINMEDIA_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGISwapChainMedia::GetFrameStatisticsMedia
 */

struct npt_command_IDXGISwapChainMedia_GetFrameStatisticsMedia {
    void *_self;
    DXGI_FRAME_STATISTICS_MEDIA * pStats;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChainMedia_GetFrameStatisticsMedia_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_IDXGISwapChainMedia_GetFrameStatisticsMedia *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pStats = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_FRAME_STATISTICS_MEDIA));
    if (!args->pStats) return;
    memset(args->pStats, 0, sizeof(DXGI_FRAME_STATISTICS_MEDIA));
}

static inline void
npt_replace_IDXGISwapChainMedia_GetFrameStatisticsMedia_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChainMedia_GetFrameStatisticsMedia *args)
{
}

static inline void
npt_encode_IDXGISwapChainMedia_GetFrameStatisticsMedia_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_IDXGISwapChainMedia_GetFrameStatisticsMedia *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                 NPT_METHOD_IDXGISwapChainMedia_GetFrameStatisticsMedia),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pStats))
        npt_encode_DXGI_FRAME_STATISTICS_MEDIA(enc, args->pStats);
}

static inline void
npt_dispatch_IDXGISwapChainMedia_GetFrameStatisticsMedia(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_IDXGISwapChainMedia_GetFrameStatisticsMedia args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChainMedia_GetFrameStatisticsMedia_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAINMEDIA);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChainMedia_GetFrameStatisticsMedia on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChainMedia_GetFrameStatisticsMedia_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChainMedia_GetFrameStatisticsMedia: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChainMedia_GetFrameStatisticsMedia _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChainMedia_GetFrameStatisticsMedia, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChainMedia_GetFrameStatisticsMedia);

    if (ctx->idxgiswapchainmedia_dispatch_overrides && ctx->idxgiswapchainmedia_dispatch_overrides->GetFrameStatisticsMedia) {
        args.ret = ctx->idxgiswapchainmedia_dispatch_overrides->GetFrameStatisticsMedia(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pStats);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChainMedia_GetFrameStatisticsMedia_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChainMedia::SetPresentDuration
 */

struct npt_command_IDXGISwapChainMedia_SetPresentDuration {
    void *_self;
    UINT Duration;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChainMedia_SetPresentDuration_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_IDXGISwapChainMedia_SetPresentDuration *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Duration);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChainMedia_SetPresentDuration_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChainMedia_SetPresentDuration *args)
{
}

static inline void
npt_encode_IDXGISwapChainMedia_SetPresentDuration_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_IDXGISwapChainMedia_SetPresentDuration *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                 NPT_METHOD_IDXGISwapChainMedia_SetPresentDuration),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChainMedia_SetPresentDuration(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_IDXGISwapChainMedia_SetPresentDuration args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChainMedia_SetPresentDuration_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAINMEDIA);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChainMedia_SetPresentDuration on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChainMedia_SetPresentDuration_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChainMedia_SetPresentDuration: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChainMedia_SetPresentDuration _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChainMedia_SetPresentDuration, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChainMedia_SetPresentDuration);

    if (ctx->idxgiswapchainmedia_dispatch_overrides && ctx->idxgiswapchainmedia_dispatch_overrides->SetPresentDuration) {
        args.ret = ctx->idxgiswapchainmedia_dispatch_overrides->SetPresentDuration(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Duration);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChainMedia_SetPresentDuration_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChainMedia::CheckPresentDurationSupport
 */

struct npt_command_IDXGISwapChainMedia_CheckPresentDurationSupport {
    void *_self;
    UINT DesiredPresentDuration;
    UINT * pClosestSmallerPresentDuration;
    UINT * pClosestLargerPresentDuration;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChainMedia_CheckPresentDurationSupport_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_IDXGISwapChainMedia_CheckPresentDurationSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->DesiredPresentDuration);


    /* Allocate temp storage for output-only parameters */

    args->pClosestSmallerPresentDuration = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pClosestSmallerPresentDuration) return;
    memset(args->pClosestSmallerPresentDuration, 0, sizeof(UINT));
    args->pClosestLargerPresentDuration = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pClosestLargerPresentDuration) return;
    memset(args->pClosestLargerPresentDuration, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGISwapChainMedia_CheckPresentDurationSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChainMedia_CheckPresentDurationSupport *args)
{
}

static inline void
npt_encode_IDXGISwapChainMedia_CheckPresentDurationSupport_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_IDXGISwapChainMedia_CheckPresentDurationSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChainMedia,
                                 NPT_METHOD_IDXGISwapChainMedia_CheckPresentDurationSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pClosestSmallerPresentDuration))
        npt_encode_UINT(enc, args->pClosestSmallerPresentDuration);
    if (npt_encode_simple_pointer(enc, args->pClosestLargerPresentDuration))
        npt_encode_UINT(enc, args->pClosestLargerPresentDuration);
}

static inline void
npt_dispatch_IDXGISwapChainMedia_CheckPresentDurationSupport(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_IDXGISwapChainMedia_CheckPresentDurationSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChainMedia_CheckPresentDurationSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAINMEDIA);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChainMedia_CheckPresentDurationSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChainMedia_CheckPresentDurationSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChainMedia_CheckPresentDurationSupport: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChainMedia_CheckPresentDurationSupport _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChainMedia_CheckPresentDurationSupport, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChainMedia_CheckPresentDurationSupport);

    if (ctx->idxgiswapchainmedia_dispatch_overrides && ctx->idxgiswapchainmedia_dispatch_overrides->CheckPresentDurationSupport) {
        args.ret = ctx->idxgiswapchainmedia_dispatch_overrides->CheckPresentDurationSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.DesiredPresentDuration, args.pClosestSmallerPresentDuration, args.pClosestLargerPresentDuration);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChainMedia_CheckPresentDurationSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGISWAPCHAINMEDIA_H */

/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgidevice.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGIDEVICE_H
#define NPT_PROTOCOL_HOST_IDXGIDEVICE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIDevice::GetAdapter
 */

struct npt_command_IDXGIDevice_GetAdapter {
    void *_self;
    IDXGIAdapter ** pAdapter;
    /* Shadow: the guest-allocated id for pAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_pAdapter -> *pAdapter} in the
     * context object table. */
    uint64_t _guest_id_pAdapter;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice_GetAdapter_args_temp(struct npt_cs_decoder *dec,
                                            struct npt_command_IDXGIDevice_GetAdapter *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_pAdapter = _gid;
        args->pAdapter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->pAdapter) return;
        *args->pAdapter = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIDevice_GetAdapter_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice_GetAdapter *args)
{
}

static inline void
npt_encode_IDXGIDevice_GetAdapter_reply(struct npt_cs_encoder *enc,
                                        const struct npt_command_IDXGIDevice_GetAdapter *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_GetAdapter),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->pAdapter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIDevice_GetAdapter(struct npt_dispatch_context *ctx,
                                    uint32_t cmd_flags,
                                    uint64_t object_id)
{
    struct npt_command_IDXGIDevice_GetAdapter args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice_GetAdapter_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_GetAdapter on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_pAdapter);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice_GetAdapter_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_GetAdapter: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_pAdapter);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice_GetAdapter _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice_GetAdapter, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice_GetAdapter);

    if (ctx->idxgidevice_dispatch_overrides && ctx->idxgidevice_dispatch_overrides->GetAdapter) {
        args.ret = ctx->idxgidevice_dispatch_overrides->GetAdapter(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pAdapter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.pAdapter && *args.pAdapter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_pAdapter, *args.pAdapter, NPT_OBJECT_TYPE_IDXGIADAPTER);
    else if (args.pAdapter)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_pAdapter);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice_GetAdapter_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice::CreateSurface
 */

struct npt_command_IDXGIDevice_CreateSurface {
    void *_self;
    const DXGI_SURFACE_DESC * pDesc;
    UINT NumSurfaces;
    DXGI_USAGE Usage;
    const DXGI_SHARED_RESOURCE * pSharedResource;
    IDXGISurface ** ppSurface;
    /* Shadow: per-element guest-allocated ids for ppSurface.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSurface;
    uint32_t _guest_id_count_ppSurface;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice_CreateSurface_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_IDXGIDevice_CreateSurface *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SURFACE_DESC));
        if (!args->pDesc) return;
        npt_decode_DXGI_SURFACE_DESC(dec, (DXGI_SURFACE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->NumSurfaces);
    npt_decode_DXGI_USAGE(dec, &args->Usage);
    if (npt_decode_simple_pointer(dec)) {
        args->pSharedResource = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SHARED_RESOURCE));
        if (!args->pSharedResource) return;
        npt_decode_DXGI_SHARED_RESOURCE(dec, (DXGI_SHARED_RESOURCE *)args->pSharedResource);
    } else {
        args->pSharedResource = NULL;
    }
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSurface = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSurface = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSurface = (uint32_t)_cnt;
        if (_cnt && (!args->ppSurface || !args->_guest_ids_ppSurface)) return;
        if (_cnt) memset(args->ppSurface, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSurface[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_IDXGIDevice_CreateSurface_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice_CreateSurface *args)
{
    if (args->pSharedResource)
        npt_replace_DXGI_SHARED_RESOURCE_handle(ctx, (DXGI_SHARED_RESOURCE *)args->pSharedResource);
}

static inline void
npt_encode_IDXGIDevice_CreateSurface_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_IDXGIDevice_CreateSurface *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_CreateSurface),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSurface (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIDevice_CreateSurface(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_IDXGIDevice_CreateSurface args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice_CreateSurface_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_CreateSurface on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice_CreateSurface_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_CreateSurface: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice_CreateSurface _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice_CreateSurface, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice_CreateSurface);

    if (ctx->idxgidevice_dispatch_overrides && ctx->idxgidevice_dispatch_overrides->CreateSurface) {
        args.ret = ctx->idxgidevice_dispatch_overrides->CreateSurface(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc, args.NumSurfaces, args.Usage, args.pSharedResource, args.ppSurface);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSurface && args._guest_ids_ppSurface) {
        const uint32_t _n = args._guest_id_count_ppSurface;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSurface[_i], args.ppSurface[_i], NPT_OBJECT_TYPE_IDXGISURFACE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice_CreateSurface_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice::QueryResourceResidency
 */

struct npt_command_IDXGIDevice_QueryResourceResidency {
    void *_self;
    IUnknown ** ppResources;
    DXGI_RESIDENCY * pResidencyStatus;
    UINT NumResources;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice_QueryResourceResidency_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDevice_QueryResourceResidency *args)
{
    /* Decode input parameters from the wire */
    uint64_t _cnt_ppResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppResources) {
        args->ppResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppResources);
        if (!args->ppResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppResources = NULL;
    }

    npt_decode_UINT(dec, &args->NumResources);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppResources && _cnt_ppResources < (uint64_t)(args->NumResources)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */

    if (args->NumResources) {
        args->pResidencyStatus = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_RESIDENCY), args->NumResources);
        if (!args->pResidencyStatus) return;
        memset(args->pResidencyStatus, 0, sizeof(DXGI_RESIDENCY) * args->NumResources);
    } else {
        args->pResidencyStatus = NULL;
    }

}

static inline void
npt_replace_IDXGIDevice_QueryResourceResidency_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice_QueryResourceResidency *args)
{
    if (args->ppResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumResources); _i++)
            args->ppResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppResources[_i], NPT_OBJECT_TYPE_IUNKNOWN);
    }
}

static inline void
npt_encode_IDXGIDevice_QueryResourceResidency_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDevice_QueryResourceResidency *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_QueryResourceResidency),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pResidencyStatus) {
        npt_encode_array_count(enc, args->NumResources);
        npt_encode_DXGI_RESIDENCY_array(enc, args->pResidencyStatus, args->NumResources);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_IDXGIDevice_QueryResourceResidency(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDevice_QueryResourceResidency args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice_QueryResourceResidency_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_QueryResourceResidency on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice_QueryResourceResidency_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_QueryResourceResidency: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice_QueryResourceResidency _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice_QueryResourceResidency, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice_QueryResourceResidency);

    if (ctx->idxgidevice_dispatch_overrides && ctx->idxgidevice_dispatch_overrides->QueryResourceResidency) {
        args.ret = ctx->idxgidevice_dispatch_overrides->QueryResourceResidency(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ppResources, args.pResidencyStatus, args.NumResources);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice_QueryResourceResidency_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice::SetGPUThreadPriority
 */

struct npt_command_IDXGIDevice_SetGPUThreadPriority {
    void *_self;
    INT Priority;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice_SetGPUThreadPriority_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_IDXGIDevice_SetGPUThreadPriority *args)
{
    /* Decode input parameters from the wire */
    npt_decode_INT(dec, &args->Priority);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIDevice_SetGPUThreadPriority_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice_SetGPUThreadPriority *args)
{
}

static inline void
npt_encode_IDXGIDevice_SetGPUThreadPriority_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_IDXGIDevice_SetGPUThreadPriority *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_SetGPUThreadPriority),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDevice_SetGPUThreadPriority(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_IDXGIDevice_SetGPUThreadPriority args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice_SetGPUThreadPriority_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_SetGPUThreadPriority on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice_SetGPUThreadPriority_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_SetGPUThreadPriority: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice_SetGPUThreadPriority _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice_SetGPUThreadPriority, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice_SetGPUThreadPriority);

    if (ctx->idxgidevice_dispatch_overrides && ctx->idxgidevice_dispatch_overrides->SetGPUThreadPriority) {
        args.ret = ctx->idxgidevice_dispatch_overrides->SetGPUThreadPriority(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Priority);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice_SetGPUThreadPriority_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice::GetGPUThreadPriority
 */

struct npt_command_IDXGIDevice_GetGPUThreadPriority {
    void *_self;
    INT * pPriority;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice_GetGPUThreadPriority_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_IDXGIDevice_GetGPUThreadPriority *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pPriority = npt_cs_decoder_alloc_temp(dec, sizeof(INT));
    if (!args->pPriority) return;
    memset(args->pPriority, 0, sizeof(INT));
}

static inline void
npt_replace_IDXGIDevice_GetGPUThreadPriority_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice_GetGPUThreadPriority *args)
{
}

static inline void
npt_encode_IDXGIDevice_GetGPUThreadPriority_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_IDXGIDevice_GetGPUThreadPriority *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice,
                                 NPT_METHOD_IDXGIDevice_GetGPUThreadPriority),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pPriority))
        npt_encode_INT(enc, args->pPriority);
}

static inline void
npt_dispatch_IDXGIDevice_GetGPUThreadPriority(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_IDXGIDevice_GetGPUThreadPriority args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice_GetGPUThreadPriority_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_GetGPUThreadPriority on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice_GetGPUThreadPriority_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice_GetGPUThreadPriority: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice_GetGPUThreadPriority _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice_GetGPUThreadPriority, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice_GetGPUThreadPriority);

    if (ctx->idxgidevice_dispatch_overrides && ctx->idxgidevice_dispatch_overrides->GetGPUThreadPriority) {
        args.ret = ctx->idxgidevice_dispatch_overrides->GetGPUThreadPriority(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pPriority);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice_GetGPUThreadPriority_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice1::SetMaximumFrameLatency
 */

struct npt_command_IDXGIDevice1_SetMaximumFrameLatency {
    void *_self;
    UINT MaxLatency;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice1_SetMaximumFrameLatency_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_IDXGIDevice1_SetMaximumFrameLatency *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->MaxLatency);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIDevice1_SetMaximumFrameLatency_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice1_SetMaximumFrameLatency *args)
{
}

static inline void
npt_encode_IDXGIDevice1_SetMaximumFrameLatency_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_IDXGIDevice1_SetMaximumFrameLatency *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                 NPT_METHOD_IDXGIDevice1_SetMaximumFrameLatency),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDevice1_SetMaximumFrameLatency(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_IDXGIDevice1_SetMaximumFrameLatency args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice1_SetMaximumFrameLatency_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice1_SetMaximumFrameLatency on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice1_SetMaximumFrameLatency_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice1_SetMaximumFrameLatency: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice1_SetMaximumFrameLatency _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice1_SetMaximumFrameLatency, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice1_SetMaximumFrameLatency);

    if (ctx->idxgidevice1_dispatch_overrides && ctx->idxgidevice1_dispatch_overrides->SetMaximumFrameLatency) {
        args.ret = ctx->idxgidevice1_dispatch_overrides->SetMaximumFrameLatency(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.MaxLatency);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice1_SetMaximumFrameLatency_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice1::GetMaximumFrameLatency
 */

struct npt_command_IDXGIDevice1_GetMaximumFrameLatency {
    void *_self;
    UINT * pMaxLatency;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice1_GetMaximumFrameLatency_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_IDXGIDevice1_GetMaximumFrameLatency *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pMaxLatency = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pMaxLatency) return;
    memset(args->pMaxLatency, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGIDevice1_GetMaximumFrameLatency_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice1_GetMaximumFrameLatency *args)
{
}

static inline void
npt_encode_IDXGIDevice1_GetMaximumFrameLatency_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_IDXGIDevice1_GetMaximumFrameLatency *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice1,
                                 NPT_METHOD_IDXGIDevice1_GetMaximumFrameLatency),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pMaxLatency))
        npt_encode_UINT(enc, args->pMaxLatency);
}

static inline void
npt_dispatch_IDXGIDevice1_GetMaximumFrameLatency(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_IDXGIDevice1_GetMaximumFrameLatency args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice1_GetMaximumFrameLatency_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice1_GetMaximumFrameLatency on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice1_GetMaximumFrameLatency_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice1_GetMaximumFrameLatency: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice1_GetMaximumFrameLatency _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice1_GetMaximumFrameLatency, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice1_GetMaximumFrameLatency);

    if (ctx->idxgidevice1_dispatch_overrides && ctx->idxgidevice1_dispatch_overrides->GetMaximumFrameLatency) {
        args.ret = ctx->idxgidevice1_dispatch_overrides->GetMaximumFrameLatency(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pMaxLatency);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice1_GetMaximumFrameLatency_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice2::OfferResources
 */

struct npt_command_IDXGIDevice2_OfferResources {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    DXGI_OFFER_RESOURCE_PRIORITY Priority;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice2_OfferResources_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_IDXGIDevice2_OfferResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumResources);
    uint64_t _cnt_ppResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppResources) {
        args->ppResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppResources);
        if (!args->ppResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppResources = NULL;
    }
    npt_decode_DXGI_OFFER_RESOURCE_PRIORITY(dec, &args->Priority);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppResources && _cnt_ppResources < (uint64_t)(args->NumResources)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGIDevice2_OfferResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice2_OfferResources *args)
{
    if (args->ppResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumResources); _i++)
            args->ppResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppResources[_i], NPT_OBJECT_TYPE_IDXGIRESOURCE);
    }
}

static inline void
npt_encode_IDXGIDevice2_OfferResources_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_IDXGIDevice2_OfferResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                 NPT_METHOD_IDXGIDevice2_OfferResources),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDevice2_OfferResources(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_IDXGIDevice2_OfferResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice2_OfferResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice2_OfferResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice2_OfferResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice2_OfferResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice2_OfferResources _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice2_OfferResources, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice2_OfferResources);

    if (ctx->idxgidevice2_dispatch_overrides && ctx->idxgidevice2_dispatch_overrides->OfferResources) {
        args.ret = ctx->idxgidevice2_dispatch_overrides->OfferResources(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumResources, args.ppResources, args.Priority);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice2_OfferResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice2::ReclaimResources
 */

struct npt_command_IDXGIDevice2_ReclaimResources {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    BOOL * pDiscarded;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice2_ReclaimResources_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGIDevice2_ReclaimResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumResources);
    uint64_t _cnt_ppResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppResources) {
        args->ppResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppResources);
        if (!args->ppResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppResources = NULL;
    }

    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppResources && _cnt_ppResources < (uint64_t)(args->NumResources)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


    if (args->NumResources) {
        args->pDiscarded = npt_cs_decoder_alloc_temp_array(dec, sizeof(BOOL), args->NumResources);
        if (!args->pDiscarded) return;
        memset(args->pDiscarded, 0, sizeof(BOOL) * args->NumResources);
    } else {
        args->pDiscarded = NULL;
    }
}

static inline void
npt_replace_IDXGIDevice2_ReclaimResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice2_ReclaimResources *args)
{
    if (args->ppResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumResources); _i++)
            args->ppResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppResources[_i], NPT_OBJECT_TYPE_IDXGIRESOURCE);
    }
}

static inline void
npt_encode_IDXGIDevice2_ReclaimResources_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGIDevice2_ReclaimResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                 NPT_METHOD_IDXGIDevice2_ReclaimResources),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pDiscarded) {
        npt_encode_array_count(enc, args->NumResources);
        npt_encode_BOOL_array(enc, args->pDiscarded, args->NumResources);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_IDXGIDevice2_ReclaimResources(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGIDevice2_ReclaimResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice2_ReclaimResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice2_ReclaimResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice2_ReclaimResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice2_ReclaimResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice2_ReclaimResources _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice2_ReclaimResources, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice2_ReclaimResources);

    if (ctx->idxgidevice2_dispatch_overrides && ctx->idxgidevice2_dispatch_overrides->ReclaimResources) {
        args.ret = ctx->idxgidevice2_dispatch_overrides->ReclaimResources(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumResources, args.ppResources, args.pDiscarded);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice2_ReclaimResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice2::EnqueueSetEvent
 */

struct npt_command_IDXGIDevice2_EnqueueSetEvent {
    void *_self;
    HANDLE hEvent;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice2_EnqueueSetEvent_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_IDXGIDevice2_EnqueueSetEvent *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIDevice2_EnqueueSetEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice2_EnqueueSetEvent *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_IDXGIDevice2_EnqueueSetEvent_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_IDXGIDevice2_EnqueueSetEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice2,
                                 NPT_METHOD_IDXGIDevice2_EnqueueSetEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDevice2_EnqueueSetEvent(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_IDXGIDevice2_EnqueueSetEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice2_EnqueueSetEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice2_EnqueueSetEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice2_EnqueueSetEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice2_EnqueueSetEvent: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice2_EnqueueSetEvent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice2_EnqueueSetEvent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice2_EnqueueSetEvent);

    if (ctx->idxgidevice2_dispatch_overrides && ctx->idxgidevice2_dispatch_overrides->EnqueueSetEvent) {
        args.ret = ctx->idxgidevice2_dispatch_overrides->EnqueueSetEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hEvent);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice2_EnqueueSetEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice3::Trim
 */

struct npt_command_IDXGIDevice3_Trim {
    void *_self;
};

static inline void
npt_decode_IDXGIDevice3_Trim_args_temp(struct npt_cs_decoder *dec,
                                       struct npt_command_IDXGIDevice3_Trim *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIDevice3_Trim_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice3_Trim *args)
{
}

static inline void
npt_encode_IDXGIDevice3_Trim_reply(struct npt_cs_encoder *enc,
                                   const struct npt_command_IDXGIDevice3_Trim *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice3,
                                 NPT_METHOD_IDXGIDevice3_Trim),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDevice3_Trim(struct npt_dispatch_context *ctx,
                               uint32_t cmd_flags,
                               uint64_t object_id)
{
    struct npt_command_IDXGIDevice3_Trim args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice3_Trim_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice3_Trim on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice3_Trim_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice3_Trim: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice3_Trim _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice3_Trim, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice3_Trim);

    if (ctx->idxgidevice3_dispatch_overrides && ctx->idxgidevice3_dispatch_overrides->Trim) {
        ctx->idxgidevice3_dispatch_overrides->Trim(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice3_Trim_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice4::OfferResources1
 */

struct npt_command_IDXGIDevice4_OfferResources1 {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    DXGI_OFFER_RESOURCE_PRIORITY Priority;
    UINT Flags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice4_OfferResources1_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_IDXGIDevice4_OfferResources1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumResources);
    uint64_t _cnt_ppResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppResources) {
        args->ppResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppResources);
        if (!args->ppResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppResources = NULL;
    }
    npt_decode_DXGI_OFFER_RESOURCE_PRIORITY(dec, &args->Priority);
    npt_decode_UINT(dec, &args->Flags);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppResources && _cnt_ppResources < (uint64_t)(args->NumResources)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_IDXGIDevice4_OfferResources1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice4_OfferResources1 *args)
{
    if (args->ppResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumResources); _i++)
            args->ppResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppResources[_i], NPT_OBJECT_TYPE_IDXGIRESOURCE);
    }
}

static inline void
npt_encode_IDXGIDevice4_OfferResources1_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_IDXGIDevice4_OfferResources1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                 NPT_METHOD_IDXGIDevice4_OfferResources1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDevice4_OfferResources1(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_IDXGIDevice4_OfferResources1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice4_OfferResources1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice4_OfferResources1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice4_OfferResources1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice4_OfferResources1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice4_OfferResources1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice4_OfferResources1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice4_OfferResources1);

    if (ctx->idxgidevice4_dispatch_overrides && ctx->idxgidevice4_dispatch_overrides->OfferResources1) {
        args.ret = ctx->idxgidevice4_dispatch_overrides->OfferResources1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumResources, args.ppResources, args.Priority, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice4_OfferResources1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDevice4::ReclaimResources1
 */

struct npt_command_IDXGIDevice4_ReclaimResources1 {
    void *_self;
    UINT NumResources;
    IDXGIResource ** ppResources;
    DXGI_RECLAIM_RESOURCE_RESULTS * pResults;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDevice4_ReclaimResources1_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_IDXGIDevice4_ReclaimResources1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumResources);
    uint64_t _cnt_ppResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppResources) {
        args->ppResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppResources);
        if (!args->ppResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppResources = NULL;
    }

    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppResources && _cnt_ppResources < (uint64_t)(args->NumResources)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


    if (args->NumResources) {
        args->pResults = npt_cs_decoder_alloc_temp_array(dec, sizeof(DXGI_RECLAIM_RESOURCE_RESULTS), args->NumResources);
        if (!args->pResults) return;
        memset(args->pResults, 0, sizeof(DXGI_RECLAIM_RESOURCE_RESULTS) * args->NumResources);
    } else {
        args->pResults = NULL;
    }
}

static inline void
npt_replace_IDXGIDevice4_ReclaimResources1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDevice4_ReclaimResources1 *args)
{
    if (args->ppResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumResources); _i++)
            args->ppResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppResources[_i], NPT_OBJECT_TYPE_IDXGIRESOURCE);
    }
}

static inline void
npt_encode_IDXGIDevice4_ReclaimResources1_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_IDXGIDevice4_ReclaimResources1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDevice4,
                                 NPT_METHOD_IDXGIDevice4_ReclaimResources1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pResults) {
        npt_encode_array_count(enc, args->NumResources);
        npt_encode_DXGI_RECLAIM_RESOURCE_RESULTS_array(enc, args->pResults, args->NumResources);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_IDXGIDevice4_ReclaimResources1(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_IDXGIDevice4_ReclaimResources1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDevice4_ReclaimResources1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDEVICE4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice4_ReclaimResources1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDevice4_ReclaimResources1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDevice4_ReclaimResources1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDevice4_ReclaimResources1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDevice4_ReclaimResources1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDevice4_ReclaimResources1);

    if (ctx->idxgidevice4_dispatch_overrides && ctx->idxgidevice4_dispatch_overrides->ReclaimResources1) {
        args.ret = ctx->idxgidevice4_dispatch_overrides->ReclaimResources1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumResources, args.ppResources, args.pResults);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDevice4_ReclaimResources1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGIDEVICE_H */

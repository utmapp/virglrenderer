/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11videocontext.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11VIDEOCONTEXT_H
#define NPT_PROTOCOL_HOST_ID3D11VIDEOCONTEXT_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11VideoContext::GetDecoderBuffer
 */

struct npt_command_ID3D11VideoContext_GetDecoderBuffer {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    D3D11_VIDEO_DECODER_BUFFER_TYPE Type;
    UINT * pBufferSize;
    void ** ppBuffer;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_GetDecoderBuffer_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11VideoContext_GetDecoderBuffer *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    npt_decode_D3D11_VIDEO_DECODER_BUFFER_TYPE(dec, &args->Type);


    /* Allocate temp storage for output-only parameters */


    args->pBufferSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pBufferSize) return;
    memset(args->pBufferSize, 0, sizeof(UINT));
    args->ppBuffer = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
    if (!args->ppBuffer) return;
    *args->ppBuffer = NULL;
}

static inline void
npt_replace_ID3D11VideoContext_GetDecoderBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_GetDecoderBuffer *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext_GetDecoderBuffer_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11VideoContext_GetDecoderBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_GetDecoderBuffer),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pBufferSize))
        npt_encode_UINT(enc, args->pBufferSize);
    if (args->ppBuffer) {
        npt_encode_array_count(enc, *args->pBufferSize);
        for (uint32_t _i = 0; _i < (uint32_t)(*args->pBufferSize); _i++)
            npt_encode_void(enc, args->ppBuffer[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_GetDecoderBuffer(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_GetDecoderBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_GetDecoderBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_GetDecoderBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_GetDecoderBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_GetDecoderBuffer: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_GetDecoderBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_GetDecoderBuffer, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_GetDecoderBuffer);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->GetDecoderBuffer) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->GetDecoderBuffer(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.Type, args.pBufferSize, args.ppBuffer);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_GetDecoderBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::ReleaseDecoderBuffer
 */

struct npt_command_ID3D11VideoContext_ReleaseDecoderBuffer {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    D3D11_VIDEO_DECODER_BUFFER_TYPE Type;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_ReleaseDecoderBuffer_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D11VideoContext_ReleaseDecoderBuffer *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    npt_decode_D3D11_VIDEO_DECODER_BUFFER_TYPE(dec, &args->Type);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoContext_ReleaseDecoderBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_ReleaseDecoderBuffer *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext_ReleaseDecoderBuffer_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D11VideoContext_ReleaseDecoderBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_ReleaseDecoderBuffer),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_ReleaseDecoderBuffer(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_ReleaseDecoderBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_ReleaseDecoderBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_ReleaseDecoderBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_ReleaseDecoderBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_ReleaseDecoderBuffer: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_ReleaseDecoderBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_ReleaseDecoderBuffer, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_ReleaseDecoderBuffer);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->ReleaseDecoderBuffer) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->ReleaseDecoderBuffer(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.Type);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_ReleaseDecoderBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::DecoderBeginFrame
 */

struct npt_command_ID3D11VideoContext_DecoderBeginFrame {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    ID3D11VideoDecoderOutputView * pView;
    UINT ContentKeySize;
    const void * pContentKey;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_DecoderBeginFrame_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11VideoContext_DecoderBeginFrame *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pView = (ID3D11VideoDecoderOutputView *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->ContentKeySize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pContentKey = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pContentKey) return;
            npt_decode_blob_array(dec, (void *)args->pContentKey, _blob_size);
        } else {
            args->pContentKey = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext_DecoderBeginFrame_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_DecoderBeginFrame *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
    args->pView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pView, NPT_OBJECT_TYPE_ID3D11VIDEODECODEROUTPUTVIEW);
}

static inline void
npt_encode_ID3D11VideoContext_DecoderBeginFrame_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11VideoContext_DecoderBeginFrame *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecoderBeginFrame),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_DecoderBeginFrame(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_DecoderBeginFrame args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_DecoderBeginFrame_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecoderBeginFrame on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_DecoderBeginFrame_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecoderBeginFrame: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_DecoderBeginFrame _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_DecoderBeginFrame, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_DecoderBeginFrame);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->DecoderBeginFrame) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->DecoderBeginFrame(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.pView, args.ContentKeySize, args.pContentKey);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_DecoderBeginFrame_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::DecoderEndFrame
 */

struct npt_command_ID3D11VideoContext_DecoderEndFrame {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_DecoderEndFrame_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11VideoContext_DecoderEndFrame *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11VideoContext_DecoderEndFrame_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_DecoderEndFrame *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext_DecoderEndFrame_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11VideoContext_DecoderEndFrame *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecoderEndFrame),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_DecoderEndFrame(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_DecoderEndFrame args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_DecoderEndFrame_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecoderEndFrame on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_DecoderEndFrame_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecoderEndFrame: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_DecoderEndFrame _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_DecoderEndFrame, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_DecoderEndFrame);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->DecoderEndFrame) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->DecoderEndFrame(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_DecoderEndFrame_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::SubmitDecoderBuffers
 */

struct npt_command_ID3D11VideoContext_SubmitDecoderBuffers {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    UINT NumBuffers;
    const D3D11_VIDEO_DECODER_BUFFER_DESC * pBufferDesc;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_SubmitDecoderBuffers_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D11VideoContext_SubmitDecoderBuffers *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_pBufferDesc = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pBufferDesc = npt_decode_array_count_unchecked(dec);
        args->pBufferDesc = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_VIDEO_DECODER_BUFFER_DESC), _cnt_pBufferDesc);
        if (!args->pBufferDesc) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pBufferDesc; _i++)
            npt_decode_D3D11_VIDEO_DECODER_BUFFER_DESC(dec, (D3D11_VIDEO_DECODER_BUFFER_DESC *)&args->pBufferDesc[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pBufferDesc = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pBufferDesc && _cnt_pBufferDesc < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_SubmitDecoderBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_SubmitDecoderBuffers *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext_SubmitDecoderBuffers_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D11VideoContext_SubmitDecoderBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_SubmitDecoderBuffers),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_SubmitDecoderBuffers(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_SubmitDecoderBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_SubmitDecoderBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_SubmitDecoderBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_SubmitDecoderBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_SubmitDecoderBuffers: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_SubmitDecoderBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_SubmitDecoderBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_SubmitDecoderBuffers);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->SubmitDecoderBuffers) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->SubmitDecoderBuffers(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.NumBuffers, args.pBufferDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_SubmitDecoderBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::DecoderExtension
 */

struct npt_command_ID3D11VideoContext_DecoderExtension {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    const D3D11_VIDEO_DECODER_EXTENSION * pExtensionData;
    APP_DEPRECATED_HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_DecoderExtension_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11VideoContext_DecoderExtension *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pExtensionData = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_DECODER_EXTENSION));
        if (!args->pExtensionData) return;
        npt_decode_D3D11_VIDEO_DECODER_EXTENSION(dec, (D3D11_VIDEO_DECODER_EXTENSION *)args->pExtensionData);
    } else {
        args->pExtensionData = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoContext_DecoderExtension_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_DecoderExtension *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
    if (args->pExtensionData)
        npt_replace_D3D11_VIDEO_DECODER_EXTENSION_handle(ctx, (D3D11_VIDEO_DECODER_EXTENSION *)args->pExtensionData);
}

static inline void
npt_encode_ID3D11VideoContext_DecoderExtension_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11VideoContext_DecoderExtension *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecoderExtension),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_DecoderExtension(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_DecoderExtension args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_DecoderExtension_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecoderExtension on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_DecoderExtension_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecoderExtension: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_DecoderExtension _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_DecoderExtension, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_DecoderExtension);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->DecoderExtension) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->DecoderExtension(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.pExtensionData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_DecoderExtension_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputTargetRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputTargetRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL Enable;
    const RECT * pRect;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputTargetRect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->Enable);
    if (npt_decode_simple_pointer(dec)) {
        args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
        if (!args->pRect) return;
        npt_decode_RECT(dec, (RECT *)args->pRect);
    } else {
        args->pRect = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputTargetRect *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputTargetRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputTargetRect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetOutputTargetRect(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputTargetRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputTargetRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputTargetRect: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetOutputTargetRect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetOutputTargetRect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetOutputTargetRect);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputTargetRect) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputTargetRect(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.Enable, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetOutputTargetRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputBackgroundColor
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL YCbCr;
    const D3D11_VIDEO_COLOR * pColor;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_args_temp(struct npt_cs_decoder *dec,
                                                                               struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->YCbCr);
    if (npt_decode_simple_pointer(dec)) {
        args->pColor = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_COLOR));
        if (!args->pColor) return;
        npt_decode_D3D11_VIDEO_COLOR(dec, (D3D11_VIDEO_COLOR *)args->pColor);
    } else {
        args->pColor = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_reply(struct npt_cs_encoder *enc,
                                                                           const struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor(struct npt_dispatch_context *ctx,
                                                                       uint32_t cmd_flags,
                                                                       uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputBackgroundColor) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputBackgroundColor(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.YCbCr, args.pColor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetOutputBackgroundColor_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputColorSpace *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pColorSpace = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_COLOR_SPACE));
        if (!args->pColorSpace) return;
        npt_decode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(dec, (D3D11_VIDEO_PROCESSOR_COLOR_SPACE *)args->pColorSpace);
    } else {
        args->pColorSpace = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputColorSpace *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputColorSpace *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputColorSpace),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetOutputColorSpace(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputColorSpace args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputColorSpace on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputColorSpace: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetOutputColorSpace _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetOutputColorSpace, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetOutputColorSpace);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputColorSpace) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputColorSpace(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetOutputColorSpace_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputAlphaFillMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE AlphaFillMode;
    UINT StreamIndex;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_args_temp(struct npt_cs_decoder *dec,
                                                                             struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE(dec, &args->AlphaFillMode);
    npt_decode_UINT(dec, &args->StreamIndex);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_reply(struct npt_cs_encoder *enc,
                                                                         const struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode(struct npt_dispatch_context *ctx,
                                                                     uint32_t cmd_flags,
                                                                     uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputAlphaFillMode) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputAlphaFillMode(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.AlphaFillMode, args.StreamIndex);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetOutputAlphaFillMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputConstriction
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputConstriction {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL Enable;
    SIZE Size;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputConstriction_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputConstriction *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->Enable);
    npt_decode_SIZE(dec, &args->Size);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetOutputConstriction_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputConstriction *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputConstriction_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputConstriction *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputConstriction),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetOutputConstriction(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputConstriction args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetOutputConstriction_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputConstriction on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetOutputConstriction_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputConstriction: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetOutputConstriction _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetOutputConstriction, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetOutputConstriction);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputConstriction) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputConstriction(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.Enable, args.Size);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetOutputConstriction_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputStereoMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputStereoMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL Enable;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputStereoMode *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->Enable);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputStereoMode *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputStereoMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputStereoMode),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetOutputStereoMode(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputStereoMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputStereoMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputStereoMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetOutputStereoMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetOutputStereoMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetOutputStereoMode);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputStereoMode) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputStereoMode(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.Enable);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetOutputStereoMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetOutputExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetOutputExtension_args_temp(struct npt_cs_decoder *dec,
                                                                         struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputExtension *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pExtensionGuid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pExtensionGuid) return;
        npt_decode_GUID(dec, (GUID *)args->pExtensionGuid);
    } else {
        args->pExtensionGuid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);
    /* ERROR: pData (void) is not serializable */
    return;
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetOutputExtension_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputExtension *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetOutputExtension_reply(struct npt_cs_encoder *enc,
                                                                     const struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputExtension *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetOutputExtension),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetOutputExtension(struct npt_dispatch_context *ctx,
                                                                 uint32_t cmd_flags,
                                                                 uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetOutputExtension args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetOutputExtension_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputExtension on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetOutputExtension_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetOutputExtension: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetOutputExtension _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetOutputExtension, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetOutputExtension);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputExtension) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetOutputExtension(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pVideoProcessor, args.pExtensionGuid, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetOutputExtension_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputTargetRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputTargetRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * Enabled;
    RECT * pRect;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputTargetRect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }


    /* Allocate temp storage for output-only parameters */

    args->Enabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->Enabled) return;
    memset(args->Enabled, 0, sizeof(BOOL));
    args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
    if (!args->pRect) return;
    memset(args->pRect, 0, sizeof(RECT));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputTargetRect *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputTargetRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputTargetRect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->Enabled))
        npt_encode_BOOL(enc, args->Enabled);
    if (npt_encode_simple_pointer(enc, args->pRect))
        npt_encode_RECT(enc, args->pRect);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetOutputTargetRect(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputTargetRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputTargetRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputTargetRect: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetOutputTargetRect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetOutputTargetRect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetOutputTargetRect);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputTargetRect) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputTargetRect(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.Enabled, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetOutputTargetRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputBackgroundColor
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pYCbCr;
    D3D11_VIDEO_COLOR * pColor;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_args_temp(struct npt_cs_decoder *dec,
                                                                               struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }


    /* Allocate temp storage for output-only parameters */

    args->pYCbCr = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pYCbCr) return;
    memset(args->pYCbCr, 0, sizeof(BOOL));
    args->pColor = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_COLOR));
    if (!args->pColor) return;
    memset(args->pColor, 0, sizeof(D3D11_VIDEO_COLOR));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_reply(struct npt_cs_encoder *enc,
                                                                           const struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pYCbCr))
        npt_encode_BOOL(enc, args->pYCbCr);
    if (npt_encode_simple_pointer(enc, args->pColor))
        npt_encode_D3D11_VIDEO_COLOR(enc, args->pColor);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor(struct npt_dispatch_context *ctx,
                                                                       uint32_t cmd_flags,
                                                                       uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputBackgroundColor) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputBackgroundColor(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pYCbCr, args.pColor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetOutputBackgroundColor_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputColorSpace *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pColorSpace = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_COLOR_SPACE));
    if (!args->pColorSpace) return;
    memset(args->pColorSpace, 0, sizeof(D3D11_VIDEO_PROCESSOR_COLOR_SPACE));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputColorSpace *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputColorSpace *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputColorSpace),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pColorSpace))
        npt_encode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(enc, args->pColorSpace);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetOutputColorSpace(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputColorSpace args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputColorSpace on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputColorSpace: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetOutputColorSpace _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetOutputColorSpace, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetOutputColorSpace);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputColorSpace) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputColorSpace(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetOutputColorSpace_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputAlphaFillMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE * pAlphaFillMode;
    UINT * pStreamIndex;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_args_temp(struct npt_cs_decoder *dec,
                                                                             struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }


    /* Allocate temp storage for output-only parameters */

    args->pAlphaFillMode = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE));
    if (!args->pAlphaFillMode) return;
    memset(args->pAlphaFillMode, 0, sizeof(D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE));
    args->pStreamIndex = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pStreamIndex) return;
    memset(args->pStreamIndex, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_reply(struct npt_cs_encoder *enc,
                                                                         const struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pAlphaFillMode))
        npt_encode_D3D11_VIDEO_PROCESSOR_ALPHA_FILL_MODE(enc, args->pAlphaFillMode);
    if (npt_encode_simple_pointer(enc, args->pStreamIndex))
        npt_encode_UINT(enc, args->pStreamIndex);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode(struct npt_dispatch_context *ctx,
                                                                     uint32_t cmd_flags,
                                                                     uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputAlphaFillMode) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputAlphaFillMode(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pAlphaFillMode, args.pStreamIndex);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetOutputAlphaFillMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputConstriction
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputConstriction {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pEnabled;
    SIZE * pSize;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputConstriction_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputConstriction *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }


    /* Allocate temp storage for output-only parameters */

    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
    args->pSize = npt_cs_decoder_alloc_temp(dec, sizeof(SIZE));
    if (!args->pSize) return;
    memset(args->pSize, 0, sizeof(SIZE));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetOutputConstriction_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputConstriction *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputConstriction_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputConstriction *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputConstriction),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
    if (npt_encode_simple_pointer(enc, args->pSize))
        npt_encode_SIZE(enc, args->pSize);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetOutputConstriction(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputConstriction args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetOutputConstriction_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputConstriction on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetOutputConstriction_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputConstriction: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetOutputConstriction _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetOutputConstriction, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetOutputConstriction);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputConstriction) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputConstriction(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pEnabled, args.pSize);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetOutputConstriction_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputStereoMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputStereoMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pEnabled;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputStereoMode *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputStereoMode *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputStereoMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputStereoMode),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetOutputStereoMode(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputStereoMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputStereoMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputStereoMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetOutputStereoMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetOutputStereoMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetOutputStereoMode);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputStereoMode) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputStereoMode(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pEnabled);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetOutputStereoMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetOutputExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetOutputExtension_args_temp(struct npt_cs_decoder *dec,
                                                                         struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputExtension *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pExtensionGuid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pExtensionGuid) return;
        npt_decode_GUID(dec, (GUID *)args->pExtensionGuid);
    } else {
        args->pExtensionGuid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);

    /* Allocate temp storage for output-only parameters */



    if (args->DataSize) {
        args->pData = npt_cs_decoder_alloc_temp(dec, args->DataSize);
        if (!args->pData) return;
        memset(args->pData, 0, args->DataSize);
    } else {
        args->pData = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetOutputExtension_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputExtension *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetOutputExtension_reply(struct npt_cs_encoder *enc,
                                                                     const struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputExtension *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetOutputExtension),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pData) {
        npt_encode_array_count(enc, args->DataSize);
        npt_encode_blob_array(enc, args->pData, args->DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetOutputExtension(struct npt_dispatch_context *ctx,
                                                                 uint32_t cmd_flags,
                                                                 uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetOutputExtension args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetOutputExtension_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputExtension on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetOutputExtension_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetOutputExtension: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetOutputExtension _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetOutputExtension, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetOutputExtension);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputExtension) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetOutputExtension(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pVideoProcessor, args.pExtensionGuid, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetOutputExtension_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamFrameFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_FRAME_FORMAT FrameFormat;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_args_temp(struct npt_cs_decoder *dec,
                                                                           struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_D3D11_VIDEO_FRAME_FORMAT(dec, &args->FrameFormat);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_reply(struct npt_cs_encoder *enc,
                                                                       const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat(struct npt_dispatch_context *ctx,
                                                                   uint32_t cmd_flags,
                                                                   uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamFrameFormat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamFrameFormat: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamFrameFormat) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamFrameFormat(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.FrameFormat);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFrameFormat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    const D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamColorSpace *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    if (npt_decode_simple_pointer(dec)) {
        args->pColorSpace = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_COLOR_SPACE));
        if (!args->pColorSpace) return;
        npt_decode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(dec, (D3D11_VIDEO_PROCESSOR_COLOR_SPACE *)args->pColorSpace);
    } else {
        args->pColorSpace = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamColorSpace *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamColorSpace *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamColorSpace),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamColorSpace(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamColorSpace args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamColorSpace on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamColorSpace: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamColorSpace _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamColorSpace, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamColorSpace);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamColorSpace) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamColorSpace(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamColorSpace_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamOutputRate
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamOutputRate {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_OUTPUT_RATE OutputRate;
    BOOL RepeatFrame;
    const DXGI_RATIONAL * pCustomRate;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamOutputRate *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_D3D11_VIDEO_PROCESSOR_OUTPUT_RATE(dec, &args->OutputRate);
    npt_decode_BOOL(dec, &args->RepeatFrame);
    if (npt_decode_simple_pointer(dec)) {
        args->pCustomRate = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
        if (!args->pCustomRate) return;
        npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)args->pCustomRate);
    } else {
        args->pCustomRate = NULL;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamOutputRate *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamOutputRate *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamOutputRate),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamOutputRate(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamOutputRate args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamOutputRate on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamOutputRate: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamOutputRate _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamOutputRate, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamOutputRate);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamOutputRate) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamOutputRate(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.OutputRate, args.RepeatFrame, args.pCustomRate);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamOutputRate_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamSourceRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamSourceRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    const RECT * pRect;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamSourceRect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    if (npt_decode_simple_pointer(dec)) {
        args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
        if (!args->pRect) return;
        npt_decode_RECT(dec, (RECT *)args->pRect);
    } else {
        args->pRect = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamSourceRect *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamSourceRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamSourceRect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamSourceRect(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamSourceRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamSourceRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamSourceRect: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamSourceRect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamSourceRect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamSourceRect);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamSourceRect) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamSourceRect(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamSourceRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamDestRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamDestRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    const RECT * pRect;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamDestRect_args_temp(struct npt_cs_decoder *dec,
                                                                        struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamDestRect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    if (npt_decode_simple_pointer(dec)) {
        args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
        if (!args->pRect) return;
        npt_decode_RECT(dec, (RECT *)args->pRect);
    } else {
        args->pRect = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamDestRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamDestRect *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamDestRect_reply(struct npt_cs_encoder *enc,
                                                                    const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamDestRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamDestRect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamDestRect(struct npt_dispatch_context *ctx,
                                                                uint32_t cmd_flags,
                                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamDestRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamDestRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamDestRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamDestRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamDestRect: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamDestRect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamDestRect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamDestRect);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamDestRect) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamDestRect(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamDestRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamAlpha
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAlpha {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    FLOAT Alpha;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamAlpha_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAlpha *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    npt_decode_FLOAT(dec, &args->Alpha);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamAlpha_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAlpha *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAlpha_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAlpha *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamAlpha),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamAlpha(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAlpha args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamAlpha_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamAlpha on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamAlpha_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamAlpha: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamAlpha _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamAlpha, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamAlpha);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamAlpha) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamAlpha(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.Alpha);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAlpha_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamPalette
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPalette {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    UINT Count;
    const UINT * pEntries;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamPalette_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPalette *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_UINT(dec, &args->Count);
    uint64_t _cnt_pEntries = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pEntries = npt_decode_array_count_unchecked(dec);
        args->pEntries = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pEntries);
        if (!args->pEntries) return;
        npt_decode_UINT_array(dec, (UINT *)args->pEntries, _cnt_pEntries);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pEntries = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pEntries && _cnt_pEntries < (uint64_t)(args->Count)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamPalette_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPalette *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPalette_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPalette *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamPalette),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamPalette(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPalette args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamPalette_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamPalette on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamPalette_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamPalette: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamPalette _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamPalette, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamPalette);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamPalette) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamPalette(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Count, args.pEntries);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPalette_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamPixelAspectRatio
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    const DXGI_RATIONAL * pSourceAspectRatio;
    const DXGI_RATIONAL * pDestinationAspectRatio;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_args_temp(struct npt_cs_decoder *dec,
                                                                                struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    if (npt_decode_simple_pointer(dec)) {
        args->pSourceAspectRatio = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
        if (!args->pSourceAspectRatio) return;
        npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)args->pSourceAspectRatio);
    } else {
        args->pSourceAspectRatio = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDestinationAspectRatio = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
        if (!args->pDestinationAspectRatio) return;
        npt_decode_DXGI_RATIONAL(dec, (DXGI_RATIONAL *)args->pDestinationAspectRatio);
    } else {
        args->pDestinationAspectRatio = NULL;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_reply(struct npt_cs_encoder *enc,
                                                                            const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio(struct npt_dispatch_context *ctx,
                                                                        uint32_t cmd_flags,
                                                                        uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamPixelAspectRatio) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamPixelAspectRatio(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.pSourceAspectRatio, args.pDestinationAspectRatio);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamPixelAspectRatio_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamLumaKey
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamLumaKey {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    FLOAT Lower;
    FLOAT Upper;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamLumaKey *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    npt_decode_FLOAT(dec, &args->Lower);
    npt_decode_FLOAT(dec, &args->Upper);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamLumaKey *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamLumaKey *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamLumaKey),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamLumaKey(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamLumaKey args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamLumaKey on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamLumaKey: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamLumaKey _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamLumaKey, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamLumaKey);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamLumaKey) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamLumaKey(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.Lower, args.Upper);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamLumaKey_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamStereoFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    D3D11_VIDEO_PROCESSOR_STEREO_FORMAT Format;
    BOOL LeftViewFrame0;
    BOOL BaseViewFrame0;
    D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE FlipMode;
    int MonoOffset;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    npt_decode_D3D11_VIDEO_PROCESSOR_STEREO_FORMAT(dec, &args->Format);
    npt_decode_BOOL(dec, &args->LeftViewFrame0);
    npt_decode_BOOL(dec, &args->BaseViewFrame0);
    npt_decode_D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE(dec, &args->FlipMode);
    npt_decode_int(dec, &args->MonoOffset);
    /* Allocate temp storage for output-only parameters */








}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamStereoFormat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamStereoFormat: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamStereoFormat) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamStereoFormat(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.Format, args.LeftViewFrame0, args.BaseViewFrame0, args.FlipMode, args.MonoOffset);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamStereoFormat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamAutoProcessingMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_args_temp(struct npt_cs_decoder *dec,
                                                                                  struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_reply(struct npt_cs_encoder *enc,
                                                                              const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode(struct npt_dispatch_context *ctx,
                                                                          uint32_t cmd_flags,
                                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamAutoProcessingMode) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamAutoProcessingMode(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamAutoProcessingMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamFilter
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFilter {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_FILTER Filter;
    BOOL Enable;
    int Level;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamFilter_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFilter *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_D3D11_VIDEO_PROCESSOR_FILTER(dec, &args->Filter);
    npt_decode_BOOL(dec, &args->Enable);
    npt_decode_int(dec, &args->Level);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamFilter_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFilter *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFilter_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFilter *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamFilter),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamFilter(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamFilter args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamFilter_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamFilter on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamFilter_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamFilter: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamFilter _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamFilter, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamFilter);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamFilter) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamFilter(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Filter, args.Enable, args.Level);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamFilter_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamExtension_args_temp(struct npt_cs_decoder *dec,
                                                                         struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamExtension *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    if (npt_decode_simple_pointer(dec)) {
        args->pExtensionGuid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pExtensionGuid) return;
        npt_decode_GUID(dec, (GUID *)args->pExtensionGuid);
    } else {
        args->pExtensionGuid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);
    /* ERROR: pData (void) is not serializable */
    return;
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamExtension_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamExtension *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamExtension_reply(struct npt_cs_encoder *enc,
                                                                     const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamExtension *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamExtension),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamExtension(struct npt_dispatch_context *ctx,
                                                                 uint32_t cmd_flags,
                                                                 uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamExtension args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamExtension_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamExtension on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamExtension_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamExtension: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamExtension _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamExtension, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamExtension);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamExtension) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamExtension(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pExtensionGuid, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamExtension_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamFrameFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_FRAME_FORMAT * pFrameFormat;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_args_temp(struct npt_cs_decoder *dec,
                                                                           struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);

    /* Allocate temp storage for output-only parameters */


    args->pFrameFormat = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_FRAME_FORMAT));
    if (!args->pFrameFormat) return;
    memset(args->pFrameFormat, 0, sizeof(D3D11_VIDEO_FRAME_FORMAT));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_reply(struct npt_cs_encoder *enc,
                                                                       const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pFrameFormat))
        npt_encode_D3D11_VIDEO_FRAME_FORMAT(enc, args->pFrameFormat);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat(struct npt_dispatch_context *ctx,
                                                                   uint32_t cmd_flags,
                                                                   uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamFrameFormat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamFrameFormat: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamFrameFormat) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamFrameFormat(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pFrameFormat);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFrameFormat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamColorSpace
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamColorSpace {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_COLOR_SPACE * pColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamColorSpace *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);

    /* Allocate temp storage for output-only parameters */


    args->pColorSpace = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_COLOR_SPACE));
    if (!args->pColorSpace) return;
    memset(args->pColorSpace, 0, sizeof(D3D11_VIDEO_PROCESSOR_COLOR_SPACE));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamColorSpace *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamColorSpace *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamColorSpace),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pColorSpace))
        npt_encode_D3D11_VIDEO_PROCESSOR_COLOR_SPACE(enc, args->pColorSpace);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamColorSpace(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamColorSpace args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamColorSpace on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamColorSpace: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamColorSpace _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamColorSpace, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamColorSpace);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamColorSpace) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamColorSpace(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamColorSpace_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamOutputRate
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamOutputRate {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_OUTPUT_RATE * pOutputRate;
    BOOL * pRepeatFrame;
    DXGI_RATIONAL * pCustomRate;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamOutputRate *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);



    /* Allocate temp storage for output-only parameters */


    args->pOutputRate = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_OUTPUT_RATE));
    if (!args->pOutputRate) return;
    memset(args->pOutputRate, 0, sizeof(D3D11_VIDEO_PROCESSOR_OUTPUT_RATE));
    args->pRepeatFrame = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pRepeatFrame) return;
    memset(args->pRepeatFrame, 0, sizeof(BOOL));
    args->pCustomRate = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
    if (!args->pCustomRate) return;
    memset(args->pCustomRate, 0, sizeof(DXGI_RATIONAL));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamOutputRate *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamOutputRate *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamOutputRate),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pOutputRate))
        npt_encode_D3D11_VIDEO_PROCESSOR_OUTPUT_RATE(enc, args->pOutputRate);
    if (npt_encode_simple_pointer(enc, args->pRepeatFrame))
        npt_encode_BOOL(enc, args->pRepeatFrame);
    if (npt_encode_simple_pointer(enc, args->pCustomRate))
        npt_encode_DXGI_RATIONAL(enc, args->pCustomRate);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamOutputRate(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamOutputRate args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamOutputRate on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamOutputRate: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamOutputRate _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamOutputRate, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamOutputRate);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamOutputRate) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamOutputRate(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pOutputRate, args.pRepeatFrame, args.pCustomRate);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamOutputRate_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamSourceRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamSourceRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    RECT * pRect;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamSourceRect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);


    /* Allocate temp storage for output-only parameters */


    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
    args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
    if (!args->pRect) return;
    memset(args->pRect, 0, sizeof(RECT));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamSourceRect *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamSourceRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamSourceRect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
    if (npt_encode_simple_pointer(enc, args->pRect))
        npt_encode_RECT(enc, args->pRect);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamSourceRect(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamSourceRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamSourceRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamSourceRect: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamSourceRect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamSourceRect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamSourceRect);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamSourceRect) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamSourceRect(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnabled, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamSourceRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamDestRect
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamDestRect {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    RECT * pRect;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamDestRect_args_temp(struct npt_cs_decoder *dec,
                                                                        struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamDestRect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);


    /* Allocate temp storage for output-only parameters */


    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
    args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
    if (!args->pRect) return;
    memset(args->pRect, 0, sizeof(RECT));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamDestRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamDestRect *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamDestRect_reply(struct npt_cs_encoder *enc,
                                                                    const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamDestRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamDestRect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
    if (npt_encode_simple_pointer(enc, args->pRect))
        npt_encode_RECT(enc, args->pRect);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamDestRect(struct npt_dispatch_context *ctx,
                                                                uint32_t cmd_flags,
                                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamDestRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamDestRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamDestRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamDestRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamDestRect: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamDestRect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamDestRect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamDestRect);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamDestRect) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamDestRect(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnabled, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamDestRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamAlpha
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAlpha {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    FLOAT * pAlpha;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAlpha_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAlpha *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);


    /* Allocate temp storage for output-only parameters */


    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
    args->pAlpha = npt_cs_decoder_alloc_temp(dec, sizeof(FLOAT));
    if (!args->pAlpha) return;
    memset(args->pAlpha, 0, sizeof(FLOAT));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamAlpha_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAlpha *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAlpha_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAlpha *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAlpha),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
    if (npt_encode_simple_pointer(enc, args->pAlpha))
        npt_encode_FLOAT(enc, args->pAlpha);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamAlpha(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAlpha args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAlpha_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamAlpha on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamAlpha_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamAlpha: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamAlpha _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamAlpha, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamAlpha);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamAlpha) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamAlpha(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnabled, args.pAlpha);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAlpha_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamPalette
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPalette {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    UINT Count;
    UINT * pEntries;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPalette_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPalette *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_UINT(dec, &args->Count);

    /* Allocate temp storage for output-only parameters */



    if (args->Count) {
        args->pEntries = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->Count);
        if (!args->pEntries) return;
        memset(args->pEntries, 0, sizeof(UINT) * args->Count);
    } else {
        args->pEntries = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamPalette_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPalette *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPalette_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPalette *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPalette),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pEntries) {
        npt_encode_array_count(enc, args->Count);
        npt_encode_UINT_array(enc, args->pEntries, args->Count);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamPalette(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPalette args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPalette_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamPalette on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamPalette_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamPalette: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamPalette _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamPalette, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamPalette);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamPalette) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamPalette(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Count, args.pEntries);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPalette_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamPixelAspectRatio
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    DXGI_RATIONAL * pSourceAspectRatio;
    DXGI_RATIONAL * pDestinationAspectRatio;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_args_temp(struct npt_cs_decoder *dec,
                                                                                struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);



    /* Allocate temp storage for output-only parameters */


    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
    args->pSourceAspectRatio = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
    if (!args->pSourceAspectRatio) return;
    memset(args->pSourceAspectRatio, 0, sizeof(DXGI_RATIONAL));
    args->pDestinationAspectRatio = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RATIONAL));
    if (!args->pDestinationAspectRatio) return;
    memset(args->pDestinationAspectRatio, 0, sizeof(DXGI_RATIONAL));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_reply(struct npt_cs_encoder *enc,
                                                                            const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
    if (npt_encode_simple_pointer(enc, args->pSourceAspectRatio))
        npt_encode_DXGI_RATIONAL(enc, args->pSourceAspectRatio);
    if (npt_encode_simple_pointer(enc, args->pDestinationAspectRatio))
        npt_encode_DXGI_RATIONAL(enc, args->pDestinationAspectRatio);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio(struct npt_dispatch_context *ctx,
                                                                        uint32_t cmd_flags,
                                                                        uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamPixelAspectRatio) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamPixelAspectRatio(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnabled, args.pSourceAspectRatio, args.pDestinationAspectRatio);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamPixelAspectRatio_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamLumaKey
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamLumaKey {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
    FLOAT * pLower;
    FLOAT * pUpper;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamLumaKey *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);



    /* Allocate temp storage for output-only parameters */


    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
    args->pLower = npt_cs_decoder_alloc_temp(dec, sizeof(FLOAT));
    if (!args->pLower) return;
    memset(args->pLower, 0, sizeof(FLOAT));
    args->pUpper = npt_cs_decoder_alloc_temp(dec, sizeof(FLOAT));
    if (!args->pUpper) return;
    memset(args->pUpper, 0, sizeof(FLOAT));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamLumaKey *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamLumaKey *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamLumaKey),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
    if (npt_encode_simple_pointer(enc, args->pLower))
        npt_encode_FLOAT(enc, args->pLower);
    if (npt_encode_simple_pointer(enc, args->pUpper))
        npt_encode_FLOAT(enc, args->pUpper);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamLumaKey(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamLumaKey args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamLumaKey on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamLumaKey: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamLumaKey _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamLumaKey, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamLumaKey);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamLumaKey) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamLumaKey(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnabled, args.pLower, args.pUpper);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamLumaKey_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamStereoFormat
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnable;
    D3D11_VIDEO_PROCESSOR_STEREO_FORMAT * pFormat;
    BOOL * pLeftViewFrame0;
    BOOL * pBaseViewFrame0;
    D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE * pFlipMode;
    int * MonoOffset;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);






    /* Allocate temp storage for output-only parameters */


    args->pEnable = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnable) return;
    memset(args->pEnable, 0, sizeof(BOOL));
    args->pFormat = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_STEREO_FORMAT));
    if (!args->pFormat) return;
    memset(args->pFormat, 0, sizeof(D3D11_VIDEO_PROCESSOR_STEREO_FORMAT));
    args->pLeftViewFrame0 = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pLeftViewFrame0) return;
    memset(args->pLeftViewFrame0, 0, sizeof(BOOL));
    args->pBaseViewFrame0 = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pBaseViewFrame0) return;
    memset(args->pBaseViewFrame0, 0, sizeof(BOOL));
    args->pFlipMode = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE));
    if (!args->pFlipMode) return;
    memset(args->pFlipMode, 0, sizeof(D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE));
    args->MonoOffset = npt_cs_decoder_alloc_temp(dec, sizeof(int));
    if (!args->MonoOffset) return;
    memset(args->MonoOffset, 0, sizeof(int));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnable))
        npt_encode_BOOL(enc, args->pEnable);
    if (npt_encode_simple_pointer(enc, args->pFormat))
        npt_encode_D3D11_VIDEO_PROCESSOR_STEREO_FORMAT(enc, args->pFormat);
    if (npt_encode_simple_pointer(enc, args->pLeftViewFrame0))
        npt_encode_BOOL(enc, args->pLeftViewFrame0);
    if (npt_encode_simple_pointer(enc, args->pBaseViewFrame0))
        npt_encode_BOOL(enc, args->pBaseViewFrame0);
    if (npt_encode_simple_pointer(enc, args->pFlipMode))
        npt_encode_D3D11_VIDEO_PROCESSOR_STEREO_FLIP_MODE(enc, args->pFlipMode);
    if (npt_encode_simple_pointer(enc, args->MonoOffset))
        npt_encode_int(enc, args->MonoOffset);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamStereoFormat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamStereoFormat: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamStereoFormat) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamStereoFormat(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnable, args.pFormat, args.pLeftViewFrame0, args.pBaseViewFrame0, args.pFlipMode, args.MonoOffset);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamStereoFormat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamAutoProcessingMode
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnabled;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_args_temp(struct npt_cs_decoder *dec,
                                                                                  struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);

    /* Allocate temp storage for output-only parameters */


    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_reply(struct npt_cs_encoder *enc,
                                                                              const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode(struct npt_dispatch_context *ctx,
                                                                          uint32_t cmd_flags,
                                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamAutoProcessingMode) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamAutoProcessingMode(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnabled);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamAutoProcessingMode_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamFilter
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFilter {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    D3D11_VIDEO_PROCESSOR_FILTER Filter;
    BOOL * pEnabled;
    int * pLevel;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFilter_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFilter *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_D3D11_VIDEO_PROCESSOR_FILTER(dec, &args->Filter);


    /* Allocate temp storage for output-only parameters */



    args->pEnabled = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnabled) return;
    memset(args->pEnabled, 0, sizeof(BOOL));
    args->pLevel = npt_cs_decoder_alloc_temp(dec, sizeof(int));
    if (!args->pLevel) return;
    memset(args->pLevel, 0, sizeof(int));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamFilter_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFilter *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFilter_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFilter *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamFilter),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnabled))
        npt_encode_BOOL(enc, args->pEnabled);
    if (npt_encode_simple_pointer(enc, args->pLevel))
        npt_encode_int(enc, args->pLevel);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamFilter(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamFilter args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamFilter_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamFilter on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamFilter_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamFilter: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamFilter _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamFilter, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamFilter);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamFilter) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamFilter(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Filter, args.pEnabled, args.pLevel);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamFilter_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamExtension
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamExtension {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    const GUID * pExtensionGuid;
    UINT DataSize;
    void * pData;
    APP_DEPRECATED_HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamExtension_args_temp(struct npt_cs_decoder *dec,
                                                                         struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamExtension *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    if (npt_decode_simple_pointer(dec)) {
        args->pExtensionGuid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->pExtensionGuid) return;
        npt_decode_GUID(dec, (GUID *)args->pExtensionGuid);
    } else {
        args->pExtensionGuid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);

    /* Allocate temp storage for output-only parameters */




    if (args->DataSize) {
        args->pData = npt_cs_decoder_alloc_temp(dec, args->DataSize);
        if (!args->pData) return;
        memset(args->pData, 0, args->DataSize);
    } else {
        args->pData = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamExtension_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamExtension *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamExtension_reply(struct npt_cs_encoder *enc,
                                                                     const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamExtension *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamExtension),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pData) {
        npt_encode_array_count(enc, args->DataSize);
        npt_encode_blob_array(enc, args->pData, args->DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamExtension(struct npt_dispatch_context *ctx,
                                                                 uint32_t cmd_flags,
                                                                 uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamExtension args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamExtension_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamExtension on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamExtension_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamExtension: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamExtension _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamExtension, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamExtension);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamExtension) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamExtension(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pExtensionGuid, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamExtension_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorBlt
 */

struct npt_command_ID3D11VideoContext_VideoProcessorBlt {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    ID3D11VideoProcessorOutputView * pView;
    UINT OutputFrame;
    UINT StreamCount;
    const D3D11_VIDEO_PROCESSOR_STREAM * pStreams;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorBlt_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11VideoContext_VideoProcessorBlt *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pView = (ID3D11VideoProcessorOutputView *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->OutputFrame);
    npt_decode_UINT(dec, &args->StreamCount);
    uint64_t _cnt_pStreams = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pStreams = npt_decode_array_count_unchecked(dec);
        args->pStreams = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_VIDEO_PROCESSOR_STREAM), _cnt_pStreams);
        if (!args->pStreams) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pStreams; _i++)
            npt_decode_D3D11_VIDEO_PROCESSOR_STREAM(dec, (D3D11_VIDEO_PROCESSOR_STREAM *)&args->pStreams[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pStreams = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pStreams && _cnt_pStreams < (uint64_t)(args->StreamCount)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorBlt_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorBlt *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
    args->pView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pView, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOROUTPUTVIEW);
    if (args->pStreams) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->StreamCount); _i++)
            npt_replace_D3D11_VIDEO_PROCESSOR_STREAM_handle(ctx, &((D3D11_VIDEO_PROCESSOR_STREAM *)args->pStreams)[_i]);
    }
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorBlt_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11VideoContext_VideoProcessorBlt *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorBlt),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorBlt(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorBlt args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorBlt_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorBlt on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorBlt_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorBlt: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorBlt _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorBlt, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorBlt);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorBlt) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->VideoProcessorBlt(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pVideoProcessor, args.pView, args.OutputFrame, args.StreamCount, args.pStreams);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorBlt_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::NegotiateCryptoSessionKeyExchange
 */

struct npt_command_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT DataSize;
    void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DataSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
}

static inline void
npt_encode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pData) {
        npt_encode_array_count(enc, args->DataSize);
        npt_encode_blob_array(enc, args->pData, args->DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_NegotiateCryptoSessionKeyExchange on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_NegotiateCryptoSessionKeyExchange: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->NegotiateCryptoSessionKeyExchange) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->NegotiateCryptoSessionKeyExchange(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoSession, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_NegotiateCryptoSessionKeyExchange_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::EncryptionBlt
 */

struct npt_command_ID3D11VideoContext_EncryptionBlt {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    ID3D11Texture2D * pSrcSurface;
    ID3D11Texture2D * pDstSurface;
    UINT IVSize;
    void * pIV;
};

static inline void
npt_decode_ID3D11VideoContext_EncryptionBlt_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11VideoContext_EncryptionBlt *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcSurface = (ID3D11Texture2D *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstSurface = (ID3D11Texture2D *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->IVSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pIV = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pIV) return;
            npt_decode_blob_array(dec, (void *)args->pIV, _blob_size);
        } else {
            args->pIV = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext_EncryptionBlt_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_EncryptionBlt *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
    args->pSrcSurface = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcSurface, NPT_OBJECT_TYPE_ID3D11TEXTURE2D);
    args->pDstSurface = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstSurface, NPT_OBJECT_TYPE_ID3D11TEXTURE2D);
}

static inline void
npt_encode_ID3D11VideoContext_EncryptionBlt_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11VideoContext_EncryptionBlt *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_EncryptionBlt),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pIV) {
        npt_encode_array_count(enc, args->IVSize);
        npt_encode_blob_array(enc, args->pIV, args->IVSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_EncryptionBlt(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_EncryptionBlt args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_EncryptionBlt_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_EncryptionBlt on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_EncryptionBlt_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_EncryptionBlt: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_EncryptionBlt _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_EncryptionBlt, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_EncryptionBlt);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->EncryptionBlt) {
        ctx->id3d11videocontext_dispatch_overrides->EncryptionBlt(ctx, &args, _original);
    } else {
        _original(args._self, args.pCryptoSession, args.pSrcSurface, args.pDstSurface, args.IVSize, args.pIV);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_EncryptionBlt_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::DecryptionBlt
 */

struct npt_command_ID3D11VideoContext_DecryptionBlt {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    ID3D11Texture2D * pSrcSurface;
    ID3D11Texture2D * pDstSurface;
    D3D11_ENCRYPTED_BLOCK_INFO * pEncryptedBlockInfo;
    UINT ContentKeySize;
    const void * pContentKey;
    UINT IVSize;
    void * pIV;
};

static inline void
npt_decode_ID3D11VideoContext_DecryptionBlt_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11VideoContext_DecryptionBlt *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcSurface = (ID3D11Texture2D *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstSurface = (ID3D11Texture2D *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pEncryptedBlockInfo = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_ENCRYPTED_BLOCK_INFO));
        if (!args->pEncryptedBlockInfo) return;
        npt_decode_D3D11_ENCRYPTED_BLOCK_INFO(dec, (D3D11_ENCRYPTED_BLOCK_INFO *)args->pEncryptedBlockInfo);
    } else {
        args->pEncryptedBlockInfo = NULL;
    }
    npt_decode_UINT(dec, &args->ContentKeySize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pContentKey = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pContentKey) return;
            npt_decode_blob_array(dec, (void *)args->pContentKey, _blob_size);
        } else {
            args->pContentKey = NULL;
        }
    }
    npt_decode_UINT(dec, &args->IVSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pIV = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pIV) return;
            npt_decode_blob_array(dec, (void *)args->pIV, _blob_size);
        } else {
            args->pIV = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */








}

static inline void
npt_replace_ID3D11VideoContext_DecryptionBlt_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_DecryptionBlt *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
    args->pSrcSurface = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcSurface, NPT_OBJECT_TYPE_ID3D11TEXTURE2D);
    args->pDstSurface = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstSurface, NPT_OBJECT_TYPE_ID3D11TEXTURE2D);
}

static inline void
npt_encode_ID3D11VideoContext_DecryptionBlt_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11VideoContext_DecryptionBlt *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_DecryptionBlt),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pIV) {
        npt_encode_array_count(enc, args->IVSize);
        npt_encode_blob_array(enc, args->pIV, args->IVSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_DecryptionBlt(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_DecryptionBlt args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_DecryptionBlt_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecryptionBlt on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_DecryptionBlt_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_DecryptionBlt: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_DecryptionBlt _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_DecryptionBlt, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_DecryptionBlt);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->DecryptionBlt) {
        ctx->id3d11videocontext_dispatch_overrides->DecryptionBlt(ctx, &args, _original);
    } else {
        _original(args._self, args.pCryptoSession, args.pSrcSurface, args.pDstSurface, args.pEncryptedBlockInfo, args.ContentKeySize, args.pContentKey, args.IVSize, args.pIV);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_DecryptionBlt_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::StartSessionKeyRefresh
 */

struct npt_command_ID3D11VideoContext_StartSessionKeyRefresh {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT RandomNumberSize;
    void * pRandomNumber;
};

static inline void
npt_decode_ID3D11VideoContext_StartSessionKeyRefresh_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11VideoContext_StartSessionKeyRefresh *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->RandomNumberSize);

    /* Allocate temp storage for output-only parameters */


    if (args->RandomNumberSize) {
        args->pRandomNumber = npt_cs_decoder_alloc_temp(dec, args->RandomNumberSize);
        if (!args->pRandomNumber) return;
        memset(args->pRandomNumber, 0, args->RandomNumberSize);
    } else {
        args->pRandomNumber = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext_StartSessionKeyRefresh_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_StartSessionKeyRefresh *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
}

static inline void
npt_encode_ID3D11VideoContext_StartSessionKeyRefresh_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11VideoContext_StartSessionKeyRefresh *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_StartSessionKeyRefresh),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pRandomNumber) {
        npt_encode_array_count(enc, args->RandomNumberSize);
        npt_encode_blob_array(enc, args->pRandomNumber, args->RandomNumberSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_StartSessionKeyRefresh(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_StartSessionKeyRefresh args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_StartSessionKeyRefresh_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_StartSessionKeyRefresh on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_StartSessionKeyRefresh_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_StartSessionKeyRefresh: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_StartSessionKeyRefresh _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_StartSessionKeyRefresh, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_StartSessionKeyRefresh);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->StartSessionKeyRefresh) {
        ctx->id3d11videocontext_dispatch_overrides->StartSessionKeyRefresh(ctx, &args, _original);
    } else {
        _original(args._self, args.pCryptoSession, args.RandomNumberSize, args.pRandomNumber);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_StartSessionKeyRefresh_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::FinishSessionKeyRefresh
 */

struct npt_command_ID3D11VideoContext_FinishSessionKeyRefresh {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
};

static inline void
npt_decode_ID3D11VideoContext_FinishSessionKeyRefresh_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11VideoContext_FinishSessionKeyRefresh *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11VideoContext_FinishSessionKeyRefresh_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_FinishSessionKeyRefresh *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
}

static inline void
npt_encode_ID3D11VideoContext_FinishSessionKeyRefresh_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11VideoContext_FinishSessionKeyRefresh *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_FinishSessionKeyRefresh),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_FinishSessionKeyRefresh(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_FinishSessionKeyRefresh args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_FinishSessionKeyRefresh_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_FinishSessionKeyRefresh on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_FinishSessionKeyRefresh_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_FinishSessionKeyRefresh: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_FinishSessionKeyRefresh _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_FinishSessionKeyRefresh, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_FinishSessionKeyRefresh);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->FinishSessionKeyRefresh) {
        ctx->id3d11videocontext_dispatch_overrides->FinishSessionKeyRefresh(ctx, &args, _original);
    } else {
        _original(args._self, args.pCryptoSession);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_FinishSessionKeyRefresh_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::GetEncryptionBltKey
 */

struct npt_command_ID3D11VideoContext_GetEncryptionBltKey {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT KeySize;
    void * pReadbackKey;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_GetEncryptionBltKey_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11VideoContext_GetEncryptionBltKey *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->KeySize);

    /* Allocate temp storage for output-only parameters */


    if (args->KeySize) {
        args->pReadbackKey = npt_cs_decoder_alloc_temp(dec, args->KeySize);
        if (!args->pReadbackKey) return;
        memset(args->pReadbackKey, 0, args->KeySize);
    } else {
        args->pReadbackKey = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext_GetEncryptionBltKey_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_GetEncryptionBltKey *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
}

static inline void
npt_encode_ID3D11VideoContext_GetEncryptionBltKey_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11VideoContext_GetEncryptionBltKey *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_GetEncryptionBltKey),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pReadbackKey) {
        npt_encode_array_count(enc, args->KeySize);
        npt_encode_blob_array(enc, args->pReadbackKey, args->KeySize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_GetEncryptionBltKey(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_GetEncryptionBltKey args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_GetEncryptionBltKey_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_GetEncryptionBltKey on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_GetEncryptionBltKey_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_GetEncryptionBltKey: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_GetEncryptionBltKey _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_GetEncryptionBltKey, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_GetEncryptionBltKey);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->GetEncryptionBltKey) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->GetEncryptionBltKey(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoSession, args.KeySize, args.pReadbackKey);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_GetEncryptionBltKey_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::NegotiateAuthenticatedChannelKeyExchange
 */

struct npt_command_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange {
    void *_self;
    ID3D11AuthenticatedChannel * pChannel;
    UINT DataSize;
    void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_args_temp(struct npt_cs_decoder *dec,
                                                                                 struct npt_command_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pChannel = (ID3D11AuthenticatedChannel *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DataSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange *args)
{
    args->pChannel = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pChannel, NPT_OBJECT_TYPE_ID3D11AUTHENTICATEDCHANNEL);
}

static inline void
npt_encode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_reply(struct npt_cs_encoder *enc,
                                                                             const struct npt_command_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pData) {
        npt_encode_array_count(enc, args->DataSize);
        npt_encode_blob_array(enc, args->pData, args->DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange(struct npt_dispatch_context *ctx,
                                                                         uint32_t cmd_flags,
                                                                         uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->NegotiateAuthenticatedChannelKeyExchange) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->NegotiateAuthenticatedChannelKeyExchange(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pChannel, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_NegotiateAuthenticatedChannelKeyExchange_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::QueryAuthenticatedChannel
 */

struct npt_command_ID3D11VideoContext_QueryAuthenticatedChannel {
    void *_self;
    ID3D11AuthenticatedChannel * pChannel;
    UINT InputSize;
    const void * pInput;
    UINT OutputSize;
    void * pOutput;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_QueryAuthenticatedChannel_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D11VideoContext_QueryAuthenticatedChannel *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pChannel = (ID3D11AuthenticatedChannel *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->InputSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pInput = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pInput) return;
            npt_decode_blob_array(dec, (void *)args->pInput, _blob_size);
        } else {
            args->pInput = NULL;
        }
    }
    npt_decode_UINT(dec, &args->OutputSize);

    /* Allocate temp storage for output-only parameters */




    if (args->OutputSize) {
        args->pOutput = npt_cs_decoder_alloc_temp(dec, args->OutputSize);
        if (!args->pOutput) return;
        memset(args->pOutput, 0, args->OutputSize);
    } else {
        args->pOutput = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext_QueryAuthenticatedChannel_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_QueryAuthenticatedChannel *args)
{
    args->pChannel = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pChannel, NPT_OBJECT_TYPE_ID3D11AUTHENTICATEDCHANNEL);
}

static inline void
npt_encode_ID3D11VideoContext_QueryAuthenticatedChannel_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D11VideoContext_QueryAuthenticatedChannel *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_QueryAuthenticatedChannel),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pOutput) {
        npt_encode_array_count(enc, args->OutputSize);
        npt_encode_blob_array(enc, args->pOutput, args->OutputSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext_QueryAuthenticatedChannel(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_QueryAuthenticatedChannel args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_QueryAuthenticatedChannel_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_QueryAuthenticatedChannel on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_QueryAuthenticatedChannel_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_QueryAuthenticatedChannel: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_QueryAuthenticatedChannel _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_QueryAuthenticatedChannel, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_QueryAuthenticatedChannel);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->QueryAuthenticatedChannel) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->QueryAuthenticatedChannel(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pChannel, args.InputSize, args.pInput, args.OutputSize, args.pOutput);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_QueryAuthenticatedChannel_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::ConfigureAuthenticatedChannel
 */

struct npt_command_ID3D11VideoContext_ConfigureAuthenticatedChannel {
    void *_self;
    ID3D11AuthenticatedChannel * pChannel;
    UINT InputSize;
    const void * pInput;
    D3D11_AUTHENTICATED_CONFIGURE_OUTPUT * pOutput;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext_ConfigureAuthenticatedChannel_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11VideoContext_ConfigureAuthenticatedChannel *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pChannel = (ID3D11AuthenticatedChannel *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->InputSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pInput = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pInput) return;
            npt_decode_blob_array(dec, (void *)args->pInput, _blob_size);
        } else {
            args->pInput = NULL;
        }
    }

    /* Allocate temp storage for output-only parameters */



    args->pOutput = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_AUTHENTICATED_CONFIGURE_OUTPUT));
    if (!args->pOutput) return;
    memset(args->pOutput, 0, sizeof(D3D11_AUTHENTICATED_CONFIGURE_OUTPUT));
}

static inline void
npt_replace_ID3D11VideoContext_ConfigureAuthenticatedChannel_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_ConfigureAuthenticatedChannel *args)
{
    args->pChannel = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pChannel, NPT_OBJECT_TYPE_ID3D11AUTHENTICATEDCHANNEL);
    if (args->pOutput)
        npt_replace_D3D11_AUTHENTICATED_CONFIGURE_OUTPUT_handle(ctx, (D3D11_AUTHENTICATED_CONFIGURE_OUTPUT *)args->pOutput);
}

static inline void
npt_encode_ID3D11VideoContext_ConfigureAuthenticatedChannel_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11VideoContext_ConfigureAuthenticatedChannel *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_ConfigureAuthenticatedChannel),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pOutput))
        npt_encode_D3D11_AUTHENTICATED_CONFIGURE_OUTPUT(enc, args->pOutput);
}

static inline void
npt_dispatch_ID3D11VideoContext_ConfigureAuthenticatedChannel(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_ConfigureAuthenticatedChannel args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_ConfigureAuthenticatedChannel_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_ConfigureAuthenticatedChannel on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_ConfigureAuthenticatedChannel_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_ConfigureAuthenticatedChannel: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_ConfigureAuthenticatedChannel _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_ConfigureAuthenticatedChannel, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_ConfigureAuthenticatedChannel);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->ConfigureAuthenticatedChannel) {
        args.ret = ctx->id3d11videocontext_dispatch_overrides->ConfigureAuthenticatedChannel(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pChannel, args.InputSize, args.pInput, args.pOutput);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_ConfigureAuthenticatedChannel_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorSetStreamRotation
 */

struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamRotation {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    D3D11_VIDEO_PROCESSOR_ROTATION Rotation;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorSetStreamRotation_args_temp(struct npt_cs_decoder *dec,
                                                                        struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamRotation *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    npt_decode_D3D11_VIDEO_PROCESSOR_ROTATION(dec, &args->Rotation);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorSetStreamRotation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamRotation *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorSetStreamRotation_reply(struct npt_cs_encoder *enc,
                                                                    const struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamRotation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorSetStreamRotation),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorSetStreamRotation(struct npt_dispatch_context *ctx,
                                                                uint32_t cmd_flags,
                                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorSetStreamRotation args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorSetStreamRotation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamRotation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorSetStreamRotation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorSetStreamRotation: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorSetStreamRotation _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorSetStreamRotation, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorSetStreamRotation);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamRotation) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorSetStreamRotation(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.Rotation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorSetStreamRotation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext::VideoProcessorGetStreamRotation
 */

struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamRotation {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnable;
    D3D11_VIDEO_PROCESSOR_ROTATION * pRotation;
};

static inline void
npt_decode_ID3D11VideoContext_VideoProcessorGetStreamRotation_args_temp(struct npt_cs_decoder *dec,
                                                                        struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamRotation *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);


    /* Allocate temp storage for output-only parameters */


    args->pEnable = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnable) return;
    memset(args->pEnable, 0, sizeof(BOOL));
    args->pRotation = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_ROTATION));
    if (!args->pRotation) return;
    memset(args->pRotation, 0, sizeof(D3D11_VIDEO_PROCESSOR_ROTATION));
}

static inline void
npt_replace_ID3D11VideoContext_VideoProcessorGetStreamRotation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamRotation *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext_VideoProcessorGetStreamRotation_reply(struct npt_cs_encoder *enc,
                                                                    const struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamRotation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext,
                                 NPT_METHOD_ID3D11VideoContext_VideoProcessorGetStreamRotation),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnable))
        npt_encode_BOOL(enc, args->pEnable);
    if (npt_encode_simple_pointer(enc, args->pRotation))
        npt_encode_D3D11_VIDEO_PROCESSOR_ROTATION(enc, args->pRotation);
}

static inline void
npt_dispatch_ID3D11VideoContext_VideoProcessorGetStreamRotation(struct npt_dispatch_context *ctx,
                                                                uint32_t cmd_flags,
                                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext_VideoProcessorGetStreamRotation args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext_VideoProcessorGetStreamRotation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamRotation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext_VideoProcessorGetStreamRotation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext_VideoProcessorGetStreamRotation: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext_VideoProcessorGetStreamRotation _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext_VideoProcessorGetStreamRotation, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext_VideoProcessorGetStreamRotation);

    if (ctx->id3d11videocontext_dispatch_overrides && ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamRotation) {
        ctx->id3d11videocontext_dispatch_overrides->VideoProcessorGetStreamRotation(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnable, args.pRotation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext_VideoProcessorGetStreamRotation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::SubmitDecoderBuffers1
 */

struct npt_command_ID3D11VideoContext1_SubmitDecoderBuffers1 {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    UINT NumBuffers;
    const D3D11_VIDEO_DECODER_BUFFER_DESC1 * pBufferDesc;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext1_SubmitDecoderBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11VideoContext1_SubmitDecoderBuffers1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_pBufferDesc = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pBufferDesc = npt_decode_array_count_unchecked(dec);
        args->pBufferDesc = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_VIDEO_DECODER_BUFFER_DESC1), _cnt_pBufferDesc);
        if (!args->pBufferDesc) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pBufferDesc; _i++)
            npt_decode_D3D11_VIDEO_DECODER_BUFFER_DESC1(dec, (D3D11_VIDEO_DECODER_BUFFER_DESC1 *)&args->pBufferDesc[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pBufferDesc = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pBufferDesc && _cnt_pBufferDesc < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext1_SubmitDecoderBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_SubmitDecoderBuffers1 *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext1_SubmitDecoderBuffers1_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11VideoContext1_SubmitDecoderBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_SubmitDecoderBuffers1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext1_SubmitDecoderBuffers1(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_SubmitDecoderBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_SubmitDecoderBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_SubmitDecoderBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_SubmitDecoderBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_SubmitDecoderBuffers1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_SubmitDecoderBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_SubmitDecoderBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_SubmitDecoderBuffers1);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->SubmitDecoderBuffers1) {
        args.ret = ctx->id3d11videocontext1_dispatch_overrides->SubmitDecoderBuffers1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.NumBuffers, args.pBufferDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_SubmitDecoderBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::GetDataForNewHardwareKey
 */

struct npt_command_ID3D11VideoContext1_GetDataForNewHardwareKey {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    UINT PrivateInputSize;
    const void * pPrivatInputData;
    UINT64 * pPrivateOutputData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext1_GetDataForNewHardwareKey_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D11VideoContext1_GetDataForNewHardwareKey *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->PrivateInputSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pPrivatInputData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pPrivatInputData) return;
            npt_decode_blob_array(dec, (void *)args->pPrivatInputData, _blob_size);
        } else {
            args->pPrivatInputData = NULL;
        }
    }

    /* Allocate temp storage for output-only parameters */



    args->pPrivateOutputData = npt_cs_decoder_alloc_temp(dec, sizeof(UINT64));
    if (!args->pPrivateOutputData) return;
    memset(args->pPrivateOutputData, 0, sizeof(UINT64));
}

static inline void
npt_replace_ID3D11VideoContext1_GetDataForNewHardwareKey_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_GetDataForNewHardwareKey *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
}

static inline void
npt_encode_ID3D11VideoContext1_GetDataForNewHardwareKey_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D11VideoContext1_GetDataForNewHardwareKey *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_GetDataForNewHardwareKey),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pPrivateOutputData))
        npt_encode_UINT64(enc, args->pPrivateOutputData);
}

static inline void
npt_dispatch_ID3D11VideoContext1_GetDataForNewHardwareKey(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_GetDataForNewHardwareKey args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_GetDataForNewHardwareKey_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_GetDataForNewHardwareKey on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_GetDataForNewHardwareKey_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_GetDataForNewHardwareKey: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_GetDataForNewHardwareKey _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_GetDataForNewHardwareKey, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_GetDataForNewHardwareKey);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->GetDataForNewHardwareKey) {
        args.ret = ctx->id3d11videocontext1_dispatch_overrides->GetDataForNewHardwareKey(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoSession, args.PrivateInputSize, args.pPrivatInputData, args.pPrivateOutputData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_GetDataForNewHardwareKey_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::CheckCryptoSessionStatus
 */

struct npt_command_ID3D11VideoContext1_CheckCryptoSessionStatus {
    void *_self;
    ID3D11CryptoSession * pCryptoSession;
    D3D11_CRYPTO_SESSION_STATUS * pStatus;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext1_CheckCryptoSessionStatus_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D11VideoContext1_CheckCryptoSessionStatus *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCryptoSession = (ID3D11CryptoSession *)npt_object_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pStatus = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_CRYPTO_SESSION_STATUS));
    if (!args->pStatus) return;
    memset(args->pStatus, 0, sizeof(D3D11_CRYPTO_SESSION_STATUS));
}

static inline void
npt_replace_ID3D11VideoContext1_CheckCryptoSessionStatus_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_CheckCryptoSessionStatus *args)
{
    args->pCryptoSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCryptoSession, NPT_OBJECT_TYPE_ID3D11CRYPTOSESSION);
}

static inline void
npt_encode_ID3D11VideoContext1_CheckCryptoSessionStatus_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D11VideoContext1_CheckCryptoSessionStatus *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_CheckCryptoSessionStatus),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pStatus))
        npt_encode_D3D11_CRYPTO_SESSION_STATUS(enc, args->pStatus);
}

static inline void
npt_dispatch_ID3D11VideoContext1_CheckCryptoSessionStatus(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_CheckCryptoSessionStatus args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_CheckCryptoSessionStatus_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_CheckCryptoSessionStatus on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_CheckCryptoSessionStatus_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_CheckCryptoSessionStatus: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_CheckCryptoSessionStatus _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_CheckCryptoSessionStatus, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_CheckCryptoSessionStatus);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->CheckCryptoSessionStatus) {
        args.ret = ctx->id3d11videocontext1_dispatch_overrides->CheckCryptoSessionStatus(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCryptoSession, args.pStatus);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_CheckCryptoSessionStatus_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::DecoderEnableDownsampling
 */

struct npt_command_ID3D11VideoContext1_DecoderEnableDownsampling {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc;
    UINT ReferenceFrameCount;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext1_DecoderEnableDownsampling_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D11VideoContext1_DecoderEnableDownsampling *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->InputColorSpace);
    if (npt_decode_simple_pointer(dec)) {
        args->pOutputDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_SAMPLE_DESC));
        if (!args->pOutputDesc) return;
        npt_decode_D3D11_VIDEO_SAMPLE_DESC(dec, (D3D11_VIDEO_SAMPLE_DESC *)args->pOutputDesc);
    } else {
        args->pOutputDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->ReferenceFrameCount);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext1_DecoderEnableDownsampling_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_DecoderEnableDownsampling *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext1_DecoderEnableDownsampling_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D11VideoContext1_DecoderEnableDownsampling *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_DecoderEnableDownsampling),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext1_DecoderEnableDownsampling(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_DecoderEnableDownsampling args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_DecoderEnableDownsampling_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_DecoderEnableDownsampling on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_DecoderEnableDownsampling_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_DecoderEnableDownsampling: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_DecoderEnableDownsampling _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_DecoderEnableDownsampling, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_DecoderEnableDownsampling);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->DecoderEnableDownsampling) {
        args.ret = ctx->id3d11videocontext1_dispatch_overrides->DecoderEnableDownsampling(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.InputColorSpace, args.pOutputDesc, args.ReferenceFrameCount);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_DecoderEnableDownsampling_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::DecoderUpdateDownsampling
 */

struct npt_command_ID3D11VideoContext1_DecoderUpdateDownsampling {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    const D3D11_VIDEO_SAMPLE_DESC * pOutputDesc;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext1_DecoderUpdateDownsampling_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D11VideoContext1_DecoderUpdateDownsampling *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pOutputDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_SAMPLE_DESC));
        if (!args->pOutputDesc) return;
        npt_decode_D3D11_VIDEO_SAMPLE_DESC(dec, (D3D11_VIDEO_SAMPLE_DESC *)args->pOutputDesc);
    } else {
        args->pOutputDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoContext1_DecoderUpdateDownsampling_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_DecoderUpdateDownsampling *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext1_DecoderUpdateDownsampling_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D11VideoContext1_DecoderUpdateDownsampling *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_DecoderUpdateDownsampling),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext1_DecoderUpdateDownsampling(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_DecoderUpdateDownsampling args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_DecoderUpdateDownsampling_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_DecoderUpdateDownsampling on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_DecoderUpdateDownsampling_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_DecoderUpdateDownsampling: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_DecoderUpdateDownsampling _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_DecoderUpdateDownsampling, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_DecoderUpdateDownsampling);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->DecoderUpdateDownsampling) {
        args.ret = ctx->id3d11videocontext1_dispatch_overrides->DecoderUpdateDownsampling(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.pOutputDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_DecoderUpdateDownsampling_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorSetOutputColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->ColorSpace);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetOutputColorSpace1) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetOutputColorSpace1(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.ColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputColorSpace1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorSetOutputShaderUsage
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL ShaderUsage;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->ShaderUsage);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetOutputShaderUsage) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetOutputShaderUsage(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.ShaderUsage);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorSetOutputShaderUsage_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorGetOutputColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_COLOR_SPACE_TYPE * pColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pColorSpace = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_COLOR_SPACE_TYPE));
    if (!args->pColorSpace) return;
    memset(args->pColorSpace, 0, sizeof(DXGI_COLOR_SPACE_TYPE));
}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pColorSpace))
        npt_encode_DXGI_COLOR_SPACE_TYPE(enc, args->pColorSpace);
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetOutputColorSpace1) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetOutputColorSpace1(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputColorSpace1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorGetOutputShaderUsage
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    BOOL * pShaderUsage;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pShaderUsage = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pShaderUsage) return;
    memset(args->pShaderUsage, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pShaderUsage))
        npt_encode_BOOL(enc, args->pShaderUsage);
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetOutputShaderUsage) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetOutputShaderUsage(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pShaderUsage);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorGetOutputShaderUsage_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorSetStreamColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->ColorSpace);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetStreamColorSpace1) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetStreamColorSpace1(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.ColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamColorSpace1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorSetStreamMirror
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamMirror {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL Enable;
    BOOL FlipHorizontal;
    BOOL FlipVertical;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorSetStreamMirror_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamMirror *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_BOOL(dec, &args->Enable);
    npt_decode_BOOL(dec, &args->FlipHorizontal);
    npt_decode_BOOL(dec, &args->FlipVertical);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorSetStreamMirror_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamMirror *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamMirror_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamMirror *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorSetStreamMirror),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorSetStreamMirror(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorSetStreamMirror args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorSetStreamMirror_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetStreamMirror on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorSetStreamMirror_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorSetStreamMirror: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorSetStreamMirror _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorSetStreamMirror, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorSetStreamMirror);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetStreamMirror) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorSetStreamMirror(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Enable, args.FlipHorizontal, args.FlipVertical);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorSetStreamMirror_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorGetStreamColorSpace1
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_COLOR_SPACE_TYPE * pColorSpace;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);

    /* Allocate temp storage for output-only parameters */


    args->pColorSpace = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_COLOR_SPACE_TYPE));
    if (!args->pColorSpace) return;
    memset(args->pColorSpace, 0, sizeof(DXGI_COLOR_SPACE_TYPE));
}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pColorSpace))
        npt_encode_DXGI_COLOR_SPACE_TYPE(enc, args->pColorSpace);
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetStreamColorSpace1) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetStreamColorSpace1(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamColorSpace1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorGetStreamMirror
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamMirror {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    BOOL * pEnable;
    BOOL * pFlipHorizontal;
    BOOL * pFlipVertical;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamMirror_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamMirror *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);



    /* Allocate temp storage for output-only parameters */


    args->pEnable = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pEnable) return;
    memset(args->pEnable, 0, sizeof(BOOL));
    args->pFlipHorizontal = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pFlipHorizontal) return;
    memset(args->pFlipHorizontal, 0, sizeof(BOOL));
    args->pFlipVertical = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pFlipVertical) return;
    memset(args->pFlipVertical, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorGetStreamMirror_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamMirror *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamMirror_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamMirror *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetStreamMirror),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pEnable))
        npt_encode_BOOL(enc, args->pEnable);
    if (npt_encode_simple_pointer(enc, args->pFlipHorizontal))
        npt_encode_BOOL(enc, args->pFlipHorizontal);
    if (npt_encode_simple_pointer(enc, args->pFlipVertical))
        npt_encode_BOOL(enc, args->pFlipVertical);
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorGetStreamMirror(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorGetStreamMirror args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorGetStreamMirror_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetStreamMirror on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorGetStreamMirror_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetStreamMirror: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorGetStreamMirror _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorGetStreamMirror, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorGetStreamMirror);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetStreamMirror) {
        ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetStreamMirror(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pEnable, args.pFlipHorizontal, args.pFlipVertical);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorGetStreamMirror_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext1::VideoProcessorGetBehaviorHints
 */

struct npt_command_ID3D11VideoContext1_VideoProcessorGetBehaviorHints {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT OutputWidth;
    UINT OutputHeight;
    DXGI_FORMAT OutputFormat;
    UINT StreamCount;
    const D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT * pStreams;
    UINT * pBehaviorHints;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_args_temp(struct npt_cs_decoder *dec,
                                                                        struct npt_command_ID3D11VideoContext1_VideoProcessorGetBehaviorHints *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->OutputWidth);
    npt_decode_UINT(dec, &args->OutputHeight);
    npt_decode_DXGI_FORMAT(dec, &args->OutputFormat);
    npt_decode_UINT(dec, &args->StreamCount);
    uint64_t _cnt_pStreams = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pStreams = npt_decode_array_count_unchecked(dec);
        args->pStreams = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT), _cnt_pStreams);
        if (!args->pStreams) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pStreams; _i++)
            npt_decode_D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT(dec, (D3D11_VIDEO_PROCESSOR_STREAM_BEHAVIOR_HINT *)&args->pStreams[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pStreams = NULL;
    }

    /* A counted array must carry every element the callee will read. */
    if (_cnt_pStreams && _cnt_pStreams < (uint64_t)(args->StreamCount)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */






    args->pBehaviorHints = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pBehaviorHints) return;
    memset(args->pBehaviorHints, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext1_VideoProcessorGetBehaviorHints *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_reply(struct npt_cs_encoder *enc,
                                                                    const struct npt_command_ID3D11VideoContext1_VideoProcessorGetBehaviorHints *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext1,
                                 NPT_METHOD_ID3D11VideoContext1_VideoProcessorGetBehaviorHints),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pBehaviorHints))
        npt_encode_UINT(enc, args->pBehaviorHints);
}

static inline void
npt_dispatch_ID3D11VideoContext1_VideoProcessorGetBehaviorHints(struct npt_dispatch_context *ctx,
                                                                uint32_t cmd_flags,
                                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext1_VideoProcessorGetBehaviorHints args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetBehaviorHints on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext1_VideoProcessorGetBehaviorHints: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext1_VideoProcessorGetBehaviorHints _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext1_VideoProcessorGetBehaviorHints, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext1_VideoProcessorGetBehaviorHints);

    if (ctx->id3d11videocontext1_dispatch_overrides && ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetBehaviorHints) {
        args.ret = ctx->id3d11videocontext1_dispatch_overrides->VideoProcessorGetBehaviorHints(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pVideoProcessor, args.OutputWidth, args.OutputHeight, args.OutputFormat, args.StreamCount, args.pStreams, args.pBehaviorHints);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext1_VideoProcessorGetBehaviorHints_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext2::VideoProcessorSetOutputHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_HDR_METADATA_TYPE Type;
    UINT Size;
    const void * pHDRMetaData;
};

static inline void
npt_decode_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_DXGI_HDR_METADATA_TYPE(dec, &args->Type);
    npt_decode_UINT(dec, &args->Size);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pHDRMetaData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pHDRMetaData) return;
            npt_decode_blob_array(dec, (void *)args->pHDRMetaData, _blob_size);
        } else {
            args->pHDRMetaData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData);

    if (ctx->id3d11videocontext2_dispatch_overrides && ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorSetOutputHDRMetaData) {
        ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorSetOutputHDRMetaData(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.Type, args.Size, args.pHDRMetaData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext2_VideoProcessorSetOutputHDRMetaData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext2::VideoProcessorGetOutputHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    DXGI_HDR_METADATA_TYPE * pType;
    UINT Size;
    void * pMetaData;
};

static inline void
npt_decode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }

    npt_decode_UINT(dec, &args->Size);

    /* Allocate temp storage for output-only parameters */

    args->pType = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_HDR_METADATA_TYPE));
    if (!args->pType) return;
    memset(args->pType, 0, sizeof(DXGI_HDR_METADATA_TYPE));

    if (args->Size) {
        args->pMetaData = npt_cs_decoder_alloc_temp(dec, args->Size);
        if (!args->pMetaData) return;
        memset(args->pMetaData, 0, args->Size);
    } else {
        args->pMetaData = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pType))
        npt_encode_DXGI_HDR_METADATA_TYPE(enc, args->pType);
    if (args->pMetaData) {
        npt_encode_array_count(enc, args->Size);
        npt_encode_blob_array(enc, args->pMetaData, args->Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData);

    if (ctx->id3d11videocontext2_dispatch_overrides && ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorGetOutputHDRMetaData) {
        ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorGetOutputHDRMetaData(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.pType, args.Size, args.pMetaData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext2_VideoProcessorGetOutputHDRMetaData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext2::VideoProcessorSetStreamHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_HDR_METADATA_TYPE Type;
    UINT Size;
    const void * pHDRMetaData;
};

static inline void
npt_decode_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);
    npt_decode_DXGI_HDR_METADATA_TYPE(dec, &args->Type);
    npt_decode_UINT(dec, &args->Size);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pHDRMetaData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pHDRMetaData) return;
            npt_decode_blob_array(dec, (void *)args->pHDRMetaData, _blob_size);
        } else {
            args->pHDRMetaData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData);

    if (ctx->id3d11videocontext2_dispatch_overrides && ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorSetStreamHDRMetaData) {
        ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorSetStreamHDRMetaData(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.Type, args.Size, args.pHDRMetaData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext2_VideoProcessorSetStreamHDRMetaData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext2::VideoProcessorGetStreamHDRMetaData
 */

struct npt_command_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData {
    void *_self;
    ID3D11VideoProcessor * pVideoProcessor;
    UINT StreamIndex;
    DXGI_HDR_METADATA_TYPE * pType;
    UINT Size;
    void * pMetaData;
};

static inline void
npt_decode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVideoProcessor = (ID3D11VideoProcessor *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StreamIndex);

    npt_decode_UINT(dec, &args->Size);

    /* Allocate temp storage for output-only parameters */


    args->pType = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_HDR_METADATA_TYPE));
    if (!args->pType) return;
    memset(args->pType, 0, sizeof(DXGI_HDR_METADATA_TYPE));

    if (args->Size) {
        args->pMetaData = npt_cs_decoder_alloc_temp(dec, args->Size);
        if (!args->pMetaData) return;
        memset(args->pMetaData, 0, args->Size);
    } else {
        args->pMetaData = NULL;
    }
}

static inline void
npt_replace_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData *args)
{
    args->pVideoProcessor = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVideoProcessor, NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSOR);
}

static inline void
npt_encode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext2,
                                 NPT_METHOD_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pType))
        npt_encode_DXGI_HDR_METADATA_TYPE(enc, args->pType);
    if (args->pMetaData) {
        npt_encode_array_count(enc, args->Size);
        npt_encode_blob_array(enc, args->pMetaData, args->Size);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData);

    if (ctx->id3d11videocontext2_dispatch_overrides && ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorGetStreamHDRMetaData) {
        ctx->id3d11videocontext2_dispatch_overrides->VideoProcessorGetStreamHDRMetaData(ctx, &args, _original);
    } else {
        _original(args._self, args.pVideoProcessor, args.StreamIndex, args.pType, args.Size, args.pMetaData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext2_VideoProcessorGetStreamHDRMetaData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext3::DecoderBeginFrame1
 */

struct npt_command_ID3D11VideoContext3_DecoderBeginFrame1 {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    ID3D11VideoDecoderOutputView * pView;
    UINT ContentKeySize;
    const void * pContentKey;
    UINT NumComponentHistograms;
    const UINT * pHistogramOffsets;
    ID3D11Buffer ** ppHistogramBuffers;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext3_DecoderBeginFrame1_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11VideoContext3_DecoderBeginFrame1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pView = (ID3D11VideoDecoderOutputView *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->ContentKeySize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pContentKey = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pContentKey) return;
            npt_decode_blob_array(dec, (void *)args->pContentKey, _blob_size);
        } else {
            args->pContentKey = NULL;
        }
    }
    npt_decode_UINT(dec, &args->NumComponentHistograms);
    uint64_t _cnt_pHistogramOffsets = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pHistogramOffsets = npt_decode_array_count_unchecked(dec);
        args->pHistogramOffsets = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pHistogramOffsets);
        if (!args->pHistogramOffsets) return;
        npt_decode_UINT_array(dec, (UINT *)args->pHistogramOffsets, _cnt_pHistogramOffsets);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pHistogramOffsets = NULL;
    }
    uint64_t _cnt_ppHistogramBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppHistogramBuffers) {
        args->ppHistogramBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppHistogramBuffers);
        if (!args->ppHistogramBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppHistogramBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppHistogramBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppHistogramBuffers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pHistogramOffsets && _cnt_pHistogramOffsets < (uint64_t)(args->NumComponentHistograms)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_ppHistogramBuffers && _cnt_ppHistogramBuffers < (uint64_t)(args->NumComponentHistograms)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D11VideoContext3_DecoderBeginFrame1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext3_DecoderBeginFrame1 *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
    args->pView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pView, NPT_OBJECT_TYPE_ID3D11VIDEODECODEROUTPUTVIEW);
    if (args->ppHistogramBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumComponentHistograms); _i++)
            args->ppHistogramBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppHistogramBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11VideoContext3_DecoderBeginFrame1_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11VideoContext3_DecoderBeginFrame1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                 NPT_METHOD_ID3D11VideoContext3_DecoderBeginFrame1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext3_DecoderBeginFrame1(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext3_DecoderBeginFrame1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext3_DecoderBeginFrame1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext3_DecoderBeginFrame1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext3_DecoderBeginFrame1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext3_DecoderBeginFrame1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext3_DecoderBeginFrame1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext3_DecoderBeginFrame1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext3_DecoderBeginFrame1);

    if (ctx->id3d11videocontext3_dispatch_overrides && ctx->id3d11videocontext3_dispatch_overrides->DecoderBeginFrame1) {
        args.ret = ctx->id3d11videocontext3_dispatch_overrides->DecoderBeginFrame1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.pView, args.ContentKeySize, args.pContentKey, args.NumComponentHistograms, args.pHistogramOffsets, args.ppHistogramBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext3_DecoderBeginFrame1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoContext3::SubmitDecoderBuffers2
 */

struct npt_command_ID3D11VideoContext3_SubmitDecoderBuffers2 {
    void *_self;
    ID3D11VideoDecoder * pDecoder;
    UINT NumBuffers;
    const D3D11_VIDEO_DECODER_BUFFER_DESC2 * pBufferDesc;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoContext3_SubmitDecoderBuffers2_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11VideoContext3_SubmitDecoderBuffers2 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDecoder = (ID3D11VideoDecoder *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_pBufferDesc = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pBufferDesc = npt_decode_array_count_unchecked(dec);
        args->pBufferDesc = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_VIDEO_DECODER_BUFFER_DESC2), _cnt_pBufferDesc);
        if (!args->pBufferDesc) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pBufferDesc; _i++)
            npt_decode_D3D11_VIDEO_DECODER_BUFFER_DESC2(dec, (D3D11_VIDEO_DECODER_BUFFER_DESC2 *)&args->pBufferDesc[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pBufferDesc = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pBufferDesc && _cnt_pBufferDesc < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11VideoContext3_SubmitDecoderBuffers2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoContext3_SubmitDecoderBuffers2 *args)
{
    args->pDecoder = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDecoder, NPT_OBJECT_TYPE_ID3D11VIDEODECODER);
}

static inline void
npt_encode_ID3D11VideoContext3_SubmitDecoderBuffers2_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11VideoContext3_SubmitDecoderBuffers2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoContext3,
                                 NPT_METHOD_ID3D11VideoContext3_SubmitDecoderBuffers2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11VideoContext3_SubmitDecoderBuffers2(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11VideoContext3_SubmitDecoderBuffers2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoContext3_SubmitDecoderBuffers2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOCONTEXT3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext3_SubmitDecoderBuffers2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoContext3_SubmitDecoderBuffers2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoContext3_SubmitDecoderBuffers2: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoContext3_SubmitDecoderBuffers2 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoContext3_SubmitDecoderBuffers2, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoContext3_SubmitDecoderBuffers2);

    if (ctx->id3d11videocontext3_dispatch_overrides && ctx->id3d11videocontext3_dispatch_overrides->SubmitDecoderBuffers2) {
        args.ret = ctx->id3d11videocontext3_dispatch_overrides->SubmitDecoderBuffers2(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDecoder, args.NumBuffers, args.pBufferDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoContext3_SubmitDecoderBuffers2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11VIDEOCONTEXT_H */

/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgidecodeswapchain.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGIDECODESWAPCHAIN_H
#define NPT_PROTOCOL_HOST_IDXGIDECODESWAPCHAIN_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIDecodeSwapChain::PresentBuffer
 */

struct npt_command_IDXGIDecodeSwapChain_PresentBuffer {
    void *_self;
    UINT BufferToPresent;
    UINT SyncInterval;
    UINT Flags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_PresentBuffer_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDecodeSwapChain_PresentBuffer *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->BufferToPresent);
    npt_decode_UINT(dec, &args->SyncInterval);
    npt_decode_UINT(dec, &args->Flags);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGIDecodeSwapChain_PresentBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_PresentBuffer *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_PresentBuffer_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDecodeSwapChain_PresentBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_PresentBuffer),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_PresentBuffer(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_PresentBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_PresentBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_PresentBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_PresentBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_PresentBuffer: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_PresentBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_PresentBuffer, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_PresentBuffer);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->PresentBuffer) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->PresentBuffer(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.BufferToPresent, args.SyncInterval, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_PresentBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::SetSourceRect
 */

struct npt_command_IDXGIDecodeSwapChain_SetSourceRect {
    void *_self;
    const RECT * pRect;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_SetSourceRect_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDecodeSwapChain_SetSourceRect *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
        if (!args->pRect) return;
        npt_decode_RECT(dec, (RECT *)args->pRect);
    } else {
        args->pRect = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIDecodeSwapChain_SetSourceRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_SetSourceRect *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetSourceRect_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDecodeSwapChain_SetSourceRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetSourceRect),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_SetSourceRect(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_SetSourceRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_SetSourceRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetSourceRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_SetSourceRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetSourceRect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_SetSourceRect _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_SetSourceRect, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_SetSourceRect);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->SetSourceRect) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->SetSourceRect(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_SetSourceRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::SetTargetRect
 */

struct npt_command_IDXGIDecodeSwapChain_SetTargetRect {
    void *_self;
    const RECT * pRect;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_SetTargetRect_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDecodeSwapChain_SetTargetRect *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
        if (!args->pRect) return;
        npt_decode_RECT(dec, (RECT *)args->pRect);
    } else {
        args->pRect = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIDecodeSwapChain_SetTargetRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_SetTargetRect *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetTargetRect_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDecodeSwapChain_SetTargetRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetTargetRect),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_SetTargetRect(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_SetTargetRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_SetTargetRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetTargetRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_SetTargetRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetTargetRect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_SetTargetRect _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_SetTargetRect, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_SetTargetRect);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->SetTargetRect) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->SetTargetRect(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_SetTargetRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::SetDestSize
 */

struct npt_command_IDXGIDecodeSwapChain_SetDestSize {
    void *_self;
    UINT Width;
    UINT Height;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_SetDestSize_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_IDXGIDecodeSwapChain_SetDestSize *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Width);
    npt_decode_UINT(dec, &args->Height);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIDecodeSwapChain_SetDestSize_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_SetDestSize *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetDestSize_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_IDXGIDecodeSwapChain_SetDestSize *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetDestSize),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_SetDestSize(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_SetDestSize args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_SetDestSize_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetDestSize on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_SetDestSize_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetDestSize: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_SetDestSize _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_SetDestSize, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_SetDestSize);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->SetDestSize) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->SetDestSize(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Width, args.Height);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_SetDestSize_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::GetSourceRect
 */

struct npt_command_IDXGIDecodeSwapChain_GetSourceRect {
    void *_self;
    RECT * pRect;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_GetSourceRect_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDecodeSwapChain_GetSourceRect *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
    if (!args->pRect) return;
    memset(args->pRect, 0, sizeof(RECT));
}

static inline void
npt_replace_IDXGIDecodeSwapChain_GetSourceRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_GetSourceRect *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetSourceRect_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDecodeSwapChain_GetSourceRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetSourceRect),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pRect))
        npt_encode_RECT(enc, args->pRect);
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_GetSourceRect(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_GetSourceRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_GetSourceRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetSourceRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_GetSourceRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetSourceRect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_GetSourceRect _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_GetSourceRect, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_GetSourceRect);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->GetSourceRect) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->GetSourceRect(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_GetSourceRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::GetTargetRect
 */

struct npt_command_IDXGIDecodeSwapChain_GetTargetRect {
    void *_self;
    RECT * pRect;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_GetTargetRect_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDecodeSwapChain_GetTargetRect *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pRect = npt_cs_decoder_alloc_temp(dec, sizeof(RECT));
    if (!args->pRect) return;
    memset(args->pRect, 0, sizeof(RECT));
}

static inline void
npt_replace_IDXGIDecodeSwapChain_GetTargetRect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_GetTargetRect *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetTargetRect_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDecodeSwapChain_GetTargetRect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetTargetRect),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pRect))
        npt_encode_RECT(enc, args->pRect);
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_GetTargetRect(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_GetTargetRect args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_GetTargetRect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetTargetRect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_GetTargetRect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetTargetRect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_GetTargetRect _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_GetTargetRect, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_GetTargetRect);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->GetTargetRect) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->GetTargetRect(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRect);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_GetTargetRect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::GetDestSize
 */

struct npt_command_IDXGIDecodeSwapChain_GetDestSize {
    void *_self;
    UINT * pWidth;
    UINT * pHeight;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_GetDestSize_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_IDXGIDecodeSwapChain_GetDestSize *args)
{
    /* Decode input parameters from the wire */


    /* Allocate temp storage for output-only parameters */
    args->pWidth = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pWidth) return;
    memset(args->pWidth, 0, sizeof(UINT));
    args->pHeight = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pHeight) return;
    memset(args->pHeight, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGIDecodeSwapChain_GetDestSize_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_GetDestSize *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetDestSize_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_IDXGIDecodeSwapChain_GetDestSize *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetDestSize),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pWidth))
        npt_encode_UINT(enc, args->pWidth);
    if (npt_encode_simple_pointer(enc, args->pHeight))
        npt_encode_UINT(enc, args->pHeight);
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_GetDestSize(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_GetDestSize args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_GetDestSize_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetDestSize on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_GetDestSize_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetDestSize: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_GetDestSize _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_GetDestSize, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_GetDestSize);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->GetDestSize) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->GetDestSize(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pWidth, args.pHeight);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_GetDestSize_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::SetColorSpace
 */

struct npt_command_IDXGIDecodeSwapChain_SetColorSpace {
    void *_self;
    DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ColorSpace;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_SetColorSpace_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDecodeSwapChain_SetColorSpace *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS(dec, &args->ColorSpace);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIDecodeSwapChain_SetColorSpace_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_SetColorSpace *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_SetColorSpace_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDecodeSwapChain_SetColorSpace *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_SetColorSpace),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_SetColorSpace(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_SetColorSpace args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_SetColorSpace_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetColorSpace on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_SetColorSpace_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_SetColorSpace: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_SetColorSpace _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_SetColorSpace, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_SetColorSpace);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->SetColorSpace) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->SetColorSpace(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_SetColorSpace_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIDecodeSwapChain::GetColorSpace
 */

struct npt_command_IDXGIDecodeSwapChain_GetColorSpace {
    void *_self;
    DXGI_MULTIPLANE_OVERLAY_YCbCr_FLAGS ret;
};

static inline void
npt_decode_IDXGIDecodeSwapChain_GetColorSpace_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIDecodeSwapChain_GetColorSpace *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIDecodeSwapChain_GetColorSpace_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIDecodeSwapChain_GetColorSpace *args)
{
}

static inline void
npt_encode_IDXGIDecodeSwapChain_GetColorSpace_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIDecodeSwapChain_GetColorSpace *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIDecodeSwapChain,
                                 NPT_METHOD_IDXGIDecodeSwapChain_GetColorSpace),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIDecodeSwapChain_GetColorSpace(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIDecodeSwapChain_GetColorSpace args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIDecodeSwapChain_GetColorSpace_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIDECODESWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetColorSpace on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIDecodeSwapChain_GetColorSpace_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIDecodeSwapChain_GetColorSpace: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIDecodeSwapChain_GetColorSpace _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIDecodeSwapChain_GetColorSpace, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIDecodeSwapChain_GetColorSpace);

    if (ctx->idxgidecodeswapchain_dispatch_overrides && ctx->idxgidecodeswapchain_dispatch_overrides->GetColorSpace) {
        args.ret = ctx->idxgidecodeswapchain_dispatch_overrides->GetColorSpace(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIDecodeSwapChain_GetColorSpace_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGIDECODESWAPCHAIN_H */

/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11videoprocessorenumerator.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11VIDEOPROCESSORENUMERATOR_H
#define NPT_PROTOCOL_HOST_ID3D11VIDEOPROCESSORENUMERATOR_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorContentDesc
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc {
    void *_self;
    D3D11_VIDEO_PROCESSOR_CONTENT_DESC * pContentDesc;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_args_temp(struct npt_cs_decoder *dec,
                                                                                 struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pContentDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_CONTENT_DESC));
    if (!args->pContentDesc) return;
    memset(args->pContentDesc, 0, sizeof(D3D11_VIDEO_PROCESSOR_CONTENT_DESC));
}

static inline void
npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc *args)
{
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_reply(struct npt_cs_encoder *enc,
                                                                             const struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pContentDesc))
        npt_encode_D3D11_VIDEO_PROCESSOR_CONTENT_DESC(enc, args->pContentDesc);
}

static inline void
npt_dispatch_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc(struct npt_dispatch_context *ctx,
                                                                         uint32_t cmd_flags,
                                                                         uint64_t object_id)
{
    struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc);

    if (ctx->id3d11videoprocessorenumerator_dispatch_overrides && ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorContentDesc) {
        args.ret = ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorContentDesc(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pContentDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorContentDesc_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoProcessorEnumerator::CheckVideoProcessorFormat
 */

struct npt_command_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat {
    void *_self;
    DXGI_FORMAT Format;
    UINT * pFlags;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_args_temp(struct npt_cs_decoder *dec,
                                                                              struct npt_command_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->Format);

    /* Allocate temp storage for output-only parameters */

    args->pFlags = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pFlags) return;
    memset(args->pFlags, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat *args)
{
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_reply(struct npt_cs_encoder *enc,
                                                                          const struct npt_command_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pFlags))
        npt_encode_UINT(enc, args->pFlags);
}

static inline void
npt_dispatch_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat(struct npt_dispatch_context *ctx,
                                                                      uint32_t cmd_flags,
                                                                      uint64_t object_id)
{
    struct npt_command_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat);

    if (ctx->id3d11videoprocessorenumerator_dispatch_overrides && ctx->id3d11videoprocessorenumerator_dispatch_overrides->CheckVideoProcessorFormat) {
        args.ret = ctx->id3d11videoprocessorenumerator_dispatch_overrides->CheckVideoProcessorFormat(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Format, args.pFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoProcessorEnumerator_CheckVideoProcessorFormat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorCaps
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps {
    void *_self;
    D3D11_VIDEO_PROCESSOR_CAPS * pCaps;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_args_temp(struct npt_cs_decoder *dec,
                                                                          struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pCaps = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_CAPS));
    if (!args->pCaps) return;
    memset(args->pCaps, 0, sizeof(D3D11_VIDEO_PROCESSOR_CAPS));
}

static inline void
npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps *args)
{
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_reply(struct npt_cs_encoder *enc,
                                                                      const struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pCaps))
        npt_encode_D3D11_VIDEO_PROCESSOR_CAPS(enc, args->pCaps);
}

static inline void
npt_dispatch_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps(struct npt_dispatch_context *ctx,
                                                                  uint32_t cmd_flags,
                                                                  uint64_t object_id)
{
    struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps);

    if (ctx->id3d11videoprocessorenumerator_dispatch_overrides && ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorCaps) {
        args.ret = ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorCaps(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pCaps);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCaps_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorRateConversionCaps
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps {
    void *_self;
    UINT TypeIndex;
    D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS * pCaps;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_args_temp(struct npt_cs_decoder *dec,
                                                                                        struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->TypeIndex);

    /* Allocate temp storage for output-only parameters */

    args->pCaps = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS));
    if (!args->pCaps) return;
    memset(args->pCaps, 0, sizeof(D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS));
}

static inline void
npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps *args)
{
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_reply(struct npt_cs_encoder *enc,
                                                                                    const struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pCaps))
        npt_encode_D3D11_VIDEO_PROCESSOR_RATE_CONVERSION_CAPS(enc, args->pCaps);
}

static inline void
npt_dispatch_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps(struct npt_dispatch_context *ctx,
                                                                                uint32_t cmd_flags,
                                                                                uint64_t object_id)
{
    struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps);

    if (ctx->id3d11videoprocessorenumerator_dispatch_overrides && ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorRateConversionCaps) {
        args.ret = ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorRateConversionCaps(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.TypeIndex, args.pCaps);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorRateConversionCaps_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorCustomRate
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate {
    void *_self;
    UINT TypeIndex;
    UINT CustomRateIndex;
    D3D11_VIDEO_PROCESSOR_CUSTOM_RATE * pRate;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_args_temp(struct npt_cs_decoder *dec,
                                                                                struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->TypeIndex);
    npt_decode_UINT(dec, &args->CustomRateIndex);

    /* Allocate temp storage for output-only parameters */


    args->pRate = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_CUSTOM_RATE));
    if (!args->pRate) return;
    memset(args->pRate, 0, sizeof(D3D11_VIDEO_PROCESSOR_CUSTOM_RATE));
}

static inline void
npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate *args)
{
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_reply(struct npt_cs_encoder *enc,
                                                                            const struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pRate))
        npt_encode_D3D11_VIDEO_PROCESSOR_CUSTOM_RATE(enc, args->pRate);
}

static inline void
npt_dispatch_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate(struct npt_dispatch_context *ctx,
                                                                        uint32_t cmd_flags,
                                                                        uint64_t object_id)
{
    struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate);

    if (ctx->id3d11videoprocessorenumerator_dispatch_overrides && ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorCustomRate) {
        args.ret = ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorCustomRate(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.TypeIndex, args.CustomRateIndex, args.pRate);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorCustomRate_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoProcessorEnumerator::GetVideoProcessorFilterRange
 */

struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange {
    void *_self;
    D3D11_VIDEO_PROCESSOR_FILTER Filter;
    D3D11_VIDEO_PROCESSOR_FILTER_RANGE * pRange;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_args_temp(struct npt_cs_decoder *dec,
                                                                                 struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D11_VIDEO_PROCESSOR_FILTER(dec, &args->Filter);

    /* Allocate temp storage for output-only parameters */

    args->pRange = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_VIDEO_PROCESSOR_FILTER_RANGE));
    if (!args->pRange) return;
    memset(args->pRange, 0, sizeof(D3D11_VIDEO_PROCESSOR_FILTER_RANGE));
}

static inline void
npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange *args)
{
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_reply(struct npt_cs_encoder *enc,
                                                                             const struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pRange))
        npt_encode_D3D11_VIDEO_PROCESSOR_FILTER_RANGE(enc, args->pRange);
}

static inline void
npt_dispatch_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange(struct npt_dispatch_context *ctx,
                                                                         uint32_t cmd_flags,
                                                                         uint64_t object_id)
{
    struct npt_command_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange);

    if (ctx->id3d11videoprocessorenumerator_dispatch_overrides && ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorFilterRange) {
        args.ret = ctx->id3d11videoprocessorenumerator_dispatch_overrides->GetVideoProcessorFilterRange(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Filter, args.pRange);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoProcessorEnumerator_GetVideoProcessorFilterRange_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11VideoProcessorEnumerator1::CheckVideoProcessorFormatConversion
 */

struct npt_command_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion {
    void *_self;
    DXGI_FORMAT InputFormat;
    DXGI_COLOR_SPACE_TYPE InputColorSpace;
    DXGI_FORMAT OutputFormat;
    DXGI_COLOR_SPACE_TYPE OutputColorSpace;
    BOOL * pSupported;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_args_temp(struct npt_cs_decoder *dec,
                                                                                         struct npt_command_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FORMAT(dec, &args->InputFormat);
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->InputColorSpace);
    npt_decode_DXGI_FORMAT(dec, &args->OutputFormat);
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->OutputColorSpace);

    /* Allocate temp storage for output-only parameters */




    args->pSupported = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pSupported) return;
    memset(args->pSupported, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion *args)
{
}

static inline void
npt_encode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_reply(struct npt_cs_encoder *enc,
                                                                                     const struct npt_command_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11VideoProcessorEnumerator1,
                                 NPT_METHOD_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pSupported))
        npt_encode_BOOL(enc, args->pSupported);
}

static inline void
npt_dispatch_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion(struct npt_dispatch_context *ctx,
                                                                                 uint32_t cmd_flags,
                                                                                 uint64_t object_id)
{
    struct npt_command_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11VIDEOPROCESSORENUMERATOR1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion);

    if (ctx->id3d11videoprocessorenumerator1_dispatch_overrides && ctx->id3d11videoprocessorenumerator1_dispatch_overrides->CheckVideoProcessorFormatConversion) {
        args.ret = ctx->id3d11videoprocessorenumerator1_dispatch_overrides->CheckVideoProcessorFormatConversion(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.InputFormat, args.InputColorSpace, args.OutputFormat, args.OutputColorSpace, args.pSupported);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11VideoProcessorEnumerator1_CheckVideoProcessorFormatConversion_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11VIDEOPROCESSORENUMERATOR_H */

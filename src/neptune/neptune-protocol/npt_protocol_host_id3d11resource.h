/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11resource.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11RESOURCE_H
#define NPT_PROTOCOL_HOST_ID3D11RESOURCE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11Resource::GetType
 */

struct npt_command_ID3D11Resource_GetType {
    void *_self;
    D3D11_RESOURCE_DIMENSION * pResourceDimension;
};

static inline void
npt_decode_ID3D11Resource_GetType_args_temp(struct npt_cs_decoder *dec,
                                            struct npt_command_ID3D11Resource_GetType *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pResourceDimension = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_RESOURCE_DIMENSION));
    if (!args->pResourceDimension) return;
    memset(args->pResourceDimension, 0, sizeof(D3D11_RESOURCE_DIMENSION));
}

static inline void
npt_replace_ID3D11Resource_GetType_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Resource_GetType *args)
{
}

static inline void
npt_encode_ID3D11Resource_GetType_reply(struct npt_cs_encoder *enc,
                                        const struct npt_command_ID3D11Resource_GetType *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                 NPT_METHOD_ID3D11Resource_GetType),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pResourceDimension))
        npt_encode_D3D11_RESOURCE_DIMENSION(enc, args->pResourceDimension);
}

static inline void
npt_dispatch_ID3D11Resource_GetType(struct npt_dispatch_context *ctx,
                                    uint32_t cmd_flags,
                                    uint64_t object_id)
{
    struct npt_command_ID3D11Resource_GetType args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Resource_GetType_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11RESOURCE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Resource_GetType on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Resource_GetType_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Resource_GetType: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Resource_GetType _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Resource_GetType, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Resource_GetType);

    if (ctx->id3d11resource_dispatch_overrides && ctx->id3d11resource_dispatch_overrides->GetType) {
        ctx->id3d11resource_dispatch_overrides->GetType(ctx, &args, _original);
    } else {
        _original(args._self, args.pResourceDimension);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Resource_GetType_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Resource::SetEvictionPriority
 */

struct npt_command_ID3D11Resource_SetEvictionPriority {
    void *_self;
    UINT EvictionPriority;
};

static inline void
npt_decode_ID3D11Resource_SetEvictionPriority_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11Resource_SetEvictionPriority *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->EvictionPriority);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11Resource_SetEvictionPriority_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Resource_SetEvictionPriority *args)
{
}

static inline void
npt_encode_ID3D11Resource_SetEvictionPriority_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11Resource_SetEvictionPriority *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                 NPT_METHOD_ID3D11Resource_SetEvictionPriority),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Resource_SetEvictionPriority(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11Resource_SetEvictionPriority args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Resource_SetEvictionPriority_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11RESOURCE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Resource_SetEvictionPriority on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Resource_SetEvictionPriority_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Resource_SetEvictionPriority: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Resource_SetEvictionPriority _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Resource_SetEvictionPriority, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Resource_SetEvictionPriority);

    if (ctx->id3d11resource_dispatch_overrides && ctx->id3d11resource_dispatch_overrides->SetEvictionPriority) {
        ctx->id3d11resource_dispatch_overrides->SetEvictionPriority(ctx, &args, _original);
    } else {
        _original(args._self, args.EvictionPriority);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Resource_SetEvictionPriority_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Resource::GetEvictionPriority
 */

struct npt_command_ID3D11Resource_GetEvictionPriority {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_ID3D11Resource_GetEvictionPriority_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11Resource_GetEvictionPriority *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11Resource_GetEvictionPriority_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Resource_GetEvictionPriority *args)
{
}

static inline void
npt_encode_ID3D11Resource_GetEvictionPriority_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11Resource_GetEvictionPriority *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Resource,
                                 NPT_METHOD_ID3D11Resource_GetEvictionPriority),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Resource_GetEvictionPriority(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11Resource_GetEvictionPriority args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Resource_GetEvictionPriority_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11RESOURCE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Resource_GetEvictionPriority on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Resource_GetEvictionPriority_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Resource_GetEvictionPriority: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Resource_GetEvictionPriority _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Resource_GetEvictionPriority, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Resource_GetEvictionPriority);

    if (ctx->id3d11resource_dispatch_overrides && ctx->id3d11resource_dispatch_overrides->GetEvictionPriority) {
        args.ret = ctx->id3d11resource_dispatch_overrides->GetEvictionPriority(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Resource_GetEvictionPriority_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11RESOURCE_H */

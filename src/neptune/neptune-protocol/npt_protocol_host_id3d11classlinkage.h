/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11classlinkage.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11CLASSLINKAGE_H
#define NPT_PROTOCOL_HOST_ID3D11CLASSLINKAGE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11ClassLinkage::GetClassInstance
 */

struct npt_command_ID3D11ClassLinkage_GetClassInstance {
    void *_self;
    const CHAR * pClassInstanceName;
    UINT InstanceIndex;
    ID3D11ClassInstance ** ppInstance;
    /* Shadow: the guest-allocated id for ppInstance arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInstance -> *ppInstance} in the
     * context object table. */
    uint64_t _guest_id_ppInstance;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11ClassLinkage_GetClassInstance_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11ClassLinkage_GetClassInstance *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _str_size = npt_decode_array_count_unchecked(dec);
        if (_str_size) {
            args->pClassInstanceName = npt_cs_decoder_alloc_temp(dec, _str_size);
            if (!args->pClassInstanceName) return;
            npt_decode_char_array(dec, (char *)args->pClassInstanceName, _str_size);
        } else {
            args->pClassInstanceName = NULL;
        }
    }
    npt_decode_UINT(dec, &args->InstanceIndex);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppInstance = _gid;
        args->ppInstance = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppInstance) return;
        *args->ppInstance = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11ClassLinkage_GetClassInstance_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11ClassLinkage_GetClassInstance *args)
{
}

static inline void
npt_encode_ID3D11ClassLinkage_GetClassInstance_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11ClassLinkage_GetClassInstance *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                 NPT_METHOD_ID3D11ClassLinkage_GetClassInstance),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppInstance (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11ClassLinkage_GetClassInstance(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11ClassLinkage_GetClassInstance args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11ClassLinkage_GetClassInstance_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11ClassLinkage_GetClassInstance on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInstance);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11ClassLinkage_GetClassInstance_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11ClassLinkage_GetClassInstance: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInstance);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11ClassLinkage_GetClassInstance _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11ClassLinkage_GetClassInstance, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11ClassLinkage_GetClassInstance);

    if (ctx->id3d11classlinkage_dispatch_overrides && ctx->id3d11classlinkage_dispatch_overrides->GetClassInstance) {
        args.ret = ctx->id3d11classlinkage_dispatch_overrides->GetClassInstance(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pClassInstanceName, args.InstanceIndex, args.ppInstance);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppInstance && *args.ppInstance)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppInstance, *args.ppInstance, NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    else if (args.ppInstance)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInstance);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11ClassLinkage_GetClassInstance_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11ClassLinkage::CreateClassInstance
 */

struct npt_command_ID3D11ClassLinkage_CreateClassInstance {
    void *_self;
    const CHAR * pClassTypeName;
    UINT ConstantBufferOffset;
    UINT ConstantVectorOffset;
    UINT TextureOffset;
    UINT SamplerOffset;
    ID3D11ClassInstance ** ppInstance;
    /* Shadow: the guest-allocated id for ppInstance arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInstance -> *ppInstance} in the
     * context object table. */
    uint64_t _guest_id_ppInstance;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11ClassLinkage_CreateClassInstance_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11ClassLinkage_CreateClassInstance *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _str_size = npt_decode_array_count_unchecked(dec);
        if (_str_size) {
            args->pClassTypeName = npt_cs_decoder_alloc_temp(dec, _str_size);
            if (!args->pClassTypeName) return;
            npt_decode_char_array(dec, (char *)args->pClassTypeName, _str_size);
        } else {
            args->pClassTypeName = NULL;
        }
    }
    npt_decode_UINT(dec, &args->ConstantBufferOffset);
    npt_decode_UINT(dec, &args->ConstantVectorOffset);
    npt_decode_UINT(dec, &args->TextureOffset);
    npt_decode_UINT(dec, &args->SamplerOffset);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppInstance = _gid;
        args->ppInstance = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppInstance) return;
        *args->ppInstance = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D11ClassLinkage_CreateClassInstance_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11ClassLinkage_CreateClassInstance *args)
{
}

static inline void
npt_encode_ID3D11ClassLinkage_CreateClassInstance_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11ClassLinkage_CreateClassInstance *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11ClassLinkage,
                                 NPT_METHOD_ID3D11ClassLinkage_CreateClassInstance),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppInstance (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11ClassLinkage_CreateClassInstance(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11ClassLinkage_CreateClassInstance args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11ClassLinkage_CreateClassInstance_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11CLASSLINKAGE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11ClassLinkage_CreateClassInstance on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInstance);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11ClassLinkage_CreateClassInstance_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11ClassLinkage_CreateClassInstance: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInstance);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11ClassLinkage_CreateClassInstance _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11ClassLinkage_CreateClassInstance, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11ClassLinkage_CreateClassInstance);

    if (ctx->id3d11classlinkage_dispatch_overrides && ctx->id3d11classlinkage_dispatch_overrides->CreateClassInstance) {
        args.ret = ctx->id3d11classlinkage_dispatch_overrides->CreateClassInstance(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pClassTypeName, args.ConstantBufferOffset, args.ConstantVectorOffset, args.TextureOffset, args.SamplerOffset, args.ppInstance);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppInstance && *args.ppInstance)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppInstance, *args.ppInstance, NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    else if (args.ppInstance)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInstance);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11ClassLinkage_CreateClassInstance_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11CLASSLINKAGE_H */

/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11devicecontext.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11DEVICECONTEXT_H
#define NPT_PROTOCOL_HOST_ID3D11DEVICECONTEXT_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11DeviceContext::VSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_VSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_VSSetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_VSSetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSSetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSSetConstantBuffers *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_VSSetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSSetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSSetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSSetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSSetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSSetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSSetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSSetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSSetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->VSSetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSSetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_PSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_PSSetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_PSSetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_ppShaderResourceViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppShaderResourceViews) {
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppShaderResourceViews);
        if (!args->ppShaderResourceViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppShaderResourceViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppShaderResourceViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppShaderResourceViews && _cnt_ppShaderResourceViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSSetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSSetShaderResources *args)
{
    if (args->ppShaderResourceViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumViews); _i++)
            args->ppShaderResourceViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_PSSetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSSetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSSetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSSetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSSetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSSetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSSetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSSetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSSetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->PSSetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSSetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSSetShader
 */

struct npt_command_ID3D11DeviceContext_PSSetShader {
    void *_self;
    ID3D11PixelShader * pPixelShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_PSSetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_PSSetShader *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pPixelShader = (ID3D11PixelShader *)npt_object_from_id(_id);
    }
    uint64_t _cnt_ppClassInstances = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppClassInstances) {
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppClassInstances);
        if (!args->ppClassInstances) return;
        for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppClassInstances[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppClassInstances = NULL;
    }
    npt_decode_UINT(dec, &args->NumClassInstances);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppClassInstances && _cnt_ppClassInstances < (uint64_t)(args->NumClassInstances)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSSetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSSetShader *args)
{
    args->pPixelShader = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pPixelShader, NPT_OBJECT_TYPE_ID3D11PIXELSHADER);
    if (args->ppClassInstances) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumClassInstances); _i++)
            args->ppClassInstances[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_PSSetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSSetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSSetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSSetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSSetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetShader: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSSetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSSetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSSetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSSetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->PSSetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.pPixelShader, args.ppClassInstances, args.NumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSSetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_PSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_PSSetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_PSSetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    uint64_t _cnt_ppSamplers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppSamplers) {
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppSamplers);
        if (!args->ppSamplers) return;
        for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppSamplers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppSamplers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppSamplers && _cnt_ppSamplers < (uint64_t)(args->NumSamplers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSSetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSSetSamplers *args)
{
    if (args->ppSamplers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSamplers); _i++)
            args->ppSamplers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_PSSetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSSetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSSetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSSetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSSetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSSetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSSetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSSetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSSetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->PSSetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSSetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::VSSetShader
 */

struct npt_command_ID3D11DeviceContext_VSSetShader {
    void *_self;
    ID3D11VertexShader * pVertexShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_VSSetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_VSSetShader *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pVertexShader = (ID3D11VertexShader *)npt_object_from_id(_id);
    }
    uint64_t _cnt_ppClassInstances = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppClassInstances) {
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppClassInstances);
        if (!args->ppClassInstances) return;
        for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppClassInstances[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppClassInstances = NULL;
    }
    npt_decode_UINT(dec, &args->NumClassInstances);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppClassInstances && _cnt_ppClassInstances < (uint64_t)(args->NumClassInstances)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSSetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSSetShader *args)
{
    args->pVertexShader = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pVertexShader, NPT_OBJECT_TYPE_ID3D11VERTEXSHADER);
    if (args->ppClassInstances) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumClassInstances); _i++)
            args->ppClassInstances[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_VSSetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSSetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSSetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSSetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSSetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetShader: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSSetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSSetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSSetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSSetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->VSSetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.pVertexShader, args.ppClassInstances, args.NumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSSetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DrawIndexed
 */

struct npt_command_ID3D11DeviceContext_DrawIndexed {
    void *_self;
    UINT IndexCount;
    UINT StartIndexLocation;
    INT BaseVertexLocation;
};

static inline void
npt_decode_ID3D11DeviceContext_DrawIndexed_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_DrawIndexed *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->IndexCount);
    npt_decode_UINT(dec, &args->StartIndexLocation);
    npt_decode_INT(dec, &args->BaseVertexLocation);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DrawIndexed_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DrawIndexed *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DrawIndexed_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_DrawIndexed *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawIndexed),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DrawIndexed(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DrawIndexed args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DrawIndexed_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawIndexed on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DrawIndexed_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawIndexed: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DrawIndexed _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DrawIndexed, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DrawIndexed);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DrawIndexed) {
        ctx->id3d11devicecontext_dispatch_overrides->DrawIndexed(ctx, &args, _original);
    } else {
        _original(args._self, args.IndexCount, args.StartIndexLocation, args.BaseVertexLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DrawIndexed_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::Draw
 */

struct npt_command_ID3D11DeviceContext_Draw {
    void *_self;
    UINT VertexCount;
    UINT StartVertexLocation;
};

static inline void
npt_decode_ID3D11DeviceContext_Draw_args_temp(struct npt_cs_decoder *dec,
                                              struct npt_command_ID3D11DeviceContext_Draw *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->VertexCount);
    npt_decode_UINT(dec, &args->StartVertexLocation);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_Draw_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_Draw *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_Draw_reply(struct npt_cs_encoder *enc,
                                          const struct npt_command_ID3D11DeviceContext_Draw *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Draw),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_Draw(struct npt_dispatch_context *ctx,
                                      uint32_t cmd_flags,
                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_Draw args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_Draw_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Draw on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_Draw_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Draw: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_Draw _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_Draw, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_Draw);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->Draw) {
        ctx->id3d11devicecontext_dispatch_overrides->Draw(ctx, &args, _original);
    } else {
        _original(args._self, args.VertexCount, args.StartVertexLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_Draw_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::Map
 */

struct npt_command_ID3D11DeviceContext_Map {
    void *_self;
    ID3D11Resource * pResource;
    UINT Subresource;
    D3D11_MAP MapType;
    UINT MapFlags;
    D3D11_MAPPED_SUBRESOURCE * pMappedResource;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext_Map_args_temp(struct npt_cs_decoder *dec,
                                             struct npt_command_ID3D11DeviceContext_Map *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->Subresource);
    npt_decode_D3D11_MAP(dec, &args->MapType);
    npt_decode_UINT(dec, &args->MapFlags);

    /* Allocate temp storage for output-only parameters */




    args->pMappedResource = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_MAPPED_SUBRESOURCE));
    if (!args->pMappedResource) return;
    memset(args->pMappedResource, 0, sizeof(D3D11_MAPPED_SUBRESOURCE));
}

static inline void
npt_replace_ID3D11DeviceContext_Map_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_Map *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_Map_reply(struct npt_cs_encoder *enc,
                                         const struct npt_command_ID3D11DeviceContext_Map *args)
{
    /* ID3D11DeviceContext::Map: reply contains a type with
     * runtime-unbounded wire size (e.g. DRED chain pointers, root-
     * signature parameter arrays).  A pre-reserved fixed-size reply
     * slot cannot represent this without truncating the host data, so
     * the guest gets a clean reply-mismatch via the encoder fatal
     * flag rather than a corrupted stream. */
    npt_cs_encoder_set_fatal(enc);
    return;
}

static inline void
npt_dispatch_ID3D11DeviceContext_Map(struct npt_dispatch_context *ctx,
                                     uint32_t cmd_flags,
                                     uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_Map args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_Map_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Map on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_Map_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Map: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_Map _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_Map, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_Map);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->Map) {
        args.ret = ctx->id3d11devicecontext_dispatch_overrides->Map(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource, args.Subresource, args.MapType, args.MapFlags, args.pMappedResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_Map_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::Unmap
 */

struct npt_command_ID3D11DeviceContext_Unmap {
    void *_self;
    ID3D11Resource * pResource;
    UINT Subresource;
};

static inline void
npt_decode_ID3D11DeviceContext_Unmap_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D11DeviceContext_Unmap *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->Subresource);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_Unmap_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_Unmap *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_Unmap_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D11DeviceContext_Unmap *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Unmap),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_Unmap(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_Unmap args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_Unmap_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Unmap on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_Unmap_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Unmap: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_Unmap _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_Unmap, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_Unmap);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->Unmap) {
        ctx->id3d11devicecontext_dispatch_overrides->Unmap(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource, args.Subresource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_Unmap_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_PSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_PSSetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_PSSetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSSetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSSetConstantBuffers *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_PSSetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_PSSetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSSetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSSetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSSetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSSetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSSetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSSetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSSetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSSetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSSetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSSetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->PSSetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSSetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IASetInputLayout
 */

struct npt_command_ID3D11DeviceContext_IASetInputLayout {
    void *_self;
    ID3D11InputLayout * pInputLayout;
};

static inline void
npt_decode_ID3D11DeviceContext_IASetInputLayout_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11DeviceContext_IASetInputLayout *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pInputLayout = (ID3D11InputLayout *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_IASetInputLayout_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IASetInputLayout *args)
{
    args->pInputLayout = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pInputLayout, NPT_OBJECT_TYPE_ID3D11INPUTLAYOUT);
}

static inline void
npt_encode_ID3D11DeviceContext_IASetInputLayout_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11DeviceContext_IASetInputLayout *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetInputLayout),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_IASetInputLayout(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IASetInputLayout args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IASetInputLayout_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetInputLayout on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IASetInputLayout_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetInputLayout: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IASetInputLayout _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IASetInputLayout, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IASetInputLayout);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IASetInputLayout) {
        ctx->id3d11devicecontext_dispatch_overrides->IASetInputLayout(ctx, &args, _original);
    } else {
        _original(args._self, args.pInputLayout);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IASetInputLayout_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IASetVertexBuffers
 */

struct npt_command_ID3D11DeviceContext_IASetVertexBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppVertexBuffers;
    const UINT * pStrides;
    const UINT * pOffsets;
};

static inline void
npt_decode_ID3D11DeviceContext_IASetVertexBuffers_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11DeviceContext_IASetVertexBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppVertexBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppVertexBuffers) {
        args->ppVertexBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppVertexBuffers);
        if (!args->ppVertexBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppVertexBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppVertexBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppVertexBuffers = NULL;
    }
    uint64_t _cnt_pStrides = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pStrides = npt_decode_array_count_unchecked(dec);
        args->pStrides = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pStrides);
        if (!args->pStrides) return;
        npt_decode_UINT_array(dec, (UINT *)args->pStrides, _cnt_pStrides);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pStrides = NULL;
    }
    uint64_t _cnt_pOffsets = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pOffsets = npt_decode_array_count_unchecked(dec);
        args->pOffsets = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pOffsets);
        if (!args->pOffsets) return;
        npt_decode_UINT_array(dec, (UINT *)args->pOffsets, _cnt_pOffsets);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pOffsets = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppVertexBuffers && _cnt_ppVertexBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pStrides && _cnt_pStrides < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pOffsets && _cnt_pOffsets < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext_IASetVertexBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IASetVertexBuffers *args)
{
    if (args->ppVertexBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppVertexBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppVertexBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_IASetVertexBuffers_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11DeviceContext_IASetVertexBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetVertexBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_IASetVertexBuffers(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IASetVertexBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IASetVertexBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetVertexBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IASetVertexBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetVertexBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IASetVertexBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IASetVertexBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IASetVertexBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IASetVertexBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->IASetVertexBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppVertexBuffers, args.pStrides, args.pOffsets);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IASetVertexBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IASetIndexBuffer
 */

struct npt_command_ID3D11DeviceContext_IASetIndexBuffer {
    void *_self;
    ID3D11Buffer * pIndexBuffer;
    DXGI_FORMAT Format;
    UINT Offset;
};

static inline void
npt_decode_ID3D11DeviceContext_IASetIndexBuffer_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11DeviceContext_IASetIndexBuffer *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pIndexBuffer = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    npt_decode_UINT(dec, &args->Offset);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_IASetIndexBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IASetIndexBuffer *args)
{
    args->pIndexBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pIndexBuffer, NPT_OBJECT_TYPE_ID3D11BUFFER);
}

static inline void
npt_encode_ID3D11DeviceContext_IASetIndexBuffer_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11DeviceContext_IASetIndexBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetIndexBuffer),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_IASetIndexBuffer(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IASetIndexBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IASetIndexBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetIndexBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IASetIndexBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetIndexBuffer: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IASetIndexBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IASetIndexBuffer, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IASetIndexBuffer);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IASetIndexBuffer) {
        ctx->id3d11devicecontext_dispatch_overrides->IASetIndexBuffer(ctx, &args, _original);
    } else {
        _original(args._self, args.pIndexBuffer, args.Format, args.Offset);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IASetIndexBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DrawIndexedInstanced
 */

struct npt_command_ID3D11DeviceContext_DrawIndexedInstanced {
    void *_self;
    UINT IndexCountPerInstance;
    UINT InstanceCount;
    UINT StartIndexLocation;
    INT BaseVertexLocation;
    UINT StartInstanceLocation;
};

static inline void
npt_decode_ID3D11DeviceContext_DrawIndexedInstanced_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_DrawIndexedInstanced *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->IndexCountPerInstance);
    npt_decode_UINT(dec, &args->InstanceCount);
    npt_decode_UINT(dec, &args->StartIndexLocation);
    npt_decode_INT(dec, &args->BaseVertexLocation);
    npt_decode_UINT(dec, &args->StartInstanceLocation);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext_DrawIndexedInstanced_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DrawIndexedInstanced *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DrawIndexedInstanced_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_DrawIndexedInstanced *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawIndexedInstanced),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DrawIndexedInstanced(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DrawIndexedInstanced args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DrawIndexedInstanced_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawIndexedInstanced on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DrawIndexedInstanced_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawIndexedInstanced: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DrawIndexedInstanced _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DrawIndexedInstanced, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DrawIndexedInstanced);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DrawIndexedInstanced) {
        ctx->id3d11devicecontext_dispatch_overrides->DrawIndexedInstanced(ctx, &args, _original);
    } else {
        _original(args._self, args.IndexCountPerInstance, args.InstanceCount, args.StartIndexLocation, args.BaseVertexLocation, args.StartInstanceLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DrawIndexedInstanced_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DrawInstanced
 */

struct npt_command_ID3D11DeviceContext_DrawInstanced {
    void *_self;
    UINT VertexCountPerInstance;
    UINT InstanceCount;
    UINT StartVertexLocation;
    UINT StartInstanceLocation;
};

static inline void
npt_decode_ID3D11DeviceContext_DrawInstanced_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_DrawInstanced *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->VertexCountPerInstance);
    npt_decode_UINT(dec, &args->InstanceCount);
    npt_decode_UINT(dec, &args->StartVertexLocation);
    npt_decode_UINT(dec, &args->StartInstanceLocation);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11DeviceContext_DrawInstanced_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DrawInstanced *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DrawInstanced_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_DrawInstanced *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawInstanced),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DrawInstanced(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DrawInstanced args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DrawInstanced_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawInstanced on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DrawInstanced_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawInstanced: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DrawInstanced _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DrawInstanced, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DrawInstanced);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DrawInstanced) {
        ctx->id3d11devicecontext_dispatch_overrides->DrawInstanced(ctx, &args, _original);
    } else {
        _original(args._self, args.VertexCountPerInstance, args.InstanceCount, args.StartVertexLocation, args.StartInstanceLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DrawInstanced_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_GSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_GSSetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_GSSetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSSetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSSetConstantBuffers *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_GSSetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSSetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSSetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSSetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSSetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSSetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSSetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSSetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSSetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->GSSetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSSetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSSetShader
 */

struct npt_command_ID3D11DeviceContext_GSSetShader {
    void *_self;
    ID3D11GeometryShader * pShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_GSSetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_GSSetShader *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pShader = (ID3D11GeometryShader *)npt_object_from_id(_id);
    }
    uint64_t _cnt_ppClassInstances = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppClassInstances) {
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppClassInstances);
        if (!args->ppClassInstances) return;
        for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppClassInstances[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppClassInstances = NULL;
    }
    npt_decode_UINT(dec, &args->NumClassInstances);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppClassInstances && _cnt_ppClassInstances < (uint64_t)(args->NumClassInstances)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSSetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSSetShader *args)
{
    args->pShader = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pShader, NPT_OBJECT_TYPE_ID3D11GEOMETRYSHADER);
    if (args->ppClassInstances) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumClassInstances); _i++)
            args->ppClassInstances[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_GSSetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSSetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSSetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSSetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSSetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetShader: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSSetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSSetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSSetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSSetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->GSSetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.pShader, args.ppClassInstances, args.NumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSSetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IASetPrimitiveTopology
 */

struct npt_command_ID3D11DeviceContext_IASetPrimitiveTopology {
    void *_self;
    D3D11_PRIMITIVE_TOPOLOGY Topology;
};

static inline void
npt_decode_ID3D11DeviceContext_IASetPrimitiveTopology_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext_IASetPrimitiveTopology *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D11_PRIMITIVE_TOPOLOGY(dec, &args->Topology);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_IASetPrimitiveTopology_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IASetPrimitiveTopology *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_IASetPrimitiveTopology_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext_IASetPrimitiveTopology *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IASetPrimitiveTopology),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_IASetPrimitiveTopology(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IASetPrimitiveTopology args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IASetPrimitiveTopology_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetPrimitiveTopology on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IASetPrimitiveTopology_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IASetPrimitiveTopology: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IASetPrimitiveTopology _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IASetPrimitiveTopology, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IASetPrimitiveTopology);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IASetPrimitiveTopology) {
        ctx->id3d11devicecontext_dispatch_overrides->IASetPrimitiveTopology(ctx, &args, _original);
    } else {
        _original(args._self, args.Topology);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IASetPrimitiveTopology_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::VSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_VSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_VSSetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_VSSetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_ppShaderResourceViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppShaderResourceViews) {
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppShaderResourceViews);
        if (!args->ppShaderResourceViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppShaderResourceViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppShaderResourceViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppShaderResourceViews && _cnt_ppShaderResourceViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSSetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSSetShaderResources *args)
{
    if (args->ppShaderResourceViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumViews); _i++)
            args->ppShaderResourceViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_VSSetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSSetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSSetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSSetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSSetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSSetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSSetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSSetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSSetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->VSSetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSSetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::VSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_VSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_VSSetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_VSSetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    uint64_t _cnt_ppSamplers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppSamplers) {
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppSamplers);
        if (!args->ppSamplers) return;
        for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppSamplers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppSamplers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppSamplers && _cnt_ppSamplers < (uint64_t)(args->NumSamplers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSSetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSSetSamplers *args)
{
    if (args->ppSamplers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSamplers); _i++)
            args->ppSamplers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_VSSetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_VSSetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSSetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSSetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSSetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSSetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSSetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSSetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSSetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSSetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSSetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSSetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->VSSetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSSetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::Begin
 */

struct npt_command_ID3D11DeviceContext_Begin {
    void *_self;
    ID3D11Asynchronous * pAsync;
};

static inline void
npt_decode_ID3D11DeviceContext_Begin_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D11DeviceContext_Begin *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pAsync = (ID3D11Asynchronous *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_Begin_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_Begin *args)
{
    args->pAsync = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pAsync, NPT_OBJECT_TYPE_ID3D11ASYNCHRONOUS);
}

static inline void
npt_encode_ID3D11DeviceContext_Begin_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D11DeviceContext_Begin *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Begin),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_Begin(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_Begin args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_Begin_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Begin on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_Begin_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Begin: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_Begin _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_Begin, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_Begin);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->Begin) {
        ctx->id3d11devicecontext_dispatch_overrides->Begin(ctx, &args, _original);
    } else {
        _original(args._self, args.pAsync);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_Begin_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::End
 */

struct npt_command_ID3D11DeviceContext_End {
    void *_self;
    ID3D11Asynchronous * pAsync;
};

static inline void
npt_decode_ID3D11DeviceContext_End_args_temp(struct npt_cs_decoder *dec,
                                             struct npt_command_ID3D11DeviceContext_End *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pAsync = (ID3D11Asynchronous *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_End_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_End *args)
{
    args->pAsync = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pAsync, NPT_OBJECT_TYPE_ID3D11ASYNCHRONOUS);
}

static inline void
npt_encode_ID3D11DeviceContext_End_reply(struct npt_cs_encoder *enc,
                                         const struct npt_command_ID3D11DeviceContext_End *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_End),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_End(struct npt_dispatch_context *ctx,
                                     uint32_t cmd_flags,
                                     uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_End args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_End_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_End on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_End_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_End: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_End _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_End, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_End);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->End) {
        ctx->id3d11devicecontext_dispatch_overrides->End(ctx, &args, _original);
    } else {
        _original(args._self, args.pAsync);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_End_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GetData
 */

struct npt_command_ID3D11DeviceContext_GetData {
    void *_self;
    ID3D11Asynchronous * pAsync;
    void * pData;
    UINT DataSize;
    UINT GetDataFlags;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext_GetData_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D11DeviceContext_GetData *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pAsync = (ID3D11Asynchronous *)npt_object_from_id(_id);
    }

    npt_decode_UINT(dec, &args->DataSize);
    npt_decode_UINT(dec, &args->GetDataFlags);
    /* Allocate temp storage for output-only parameters */

    if (args->DataSize) {
        args->pData = npt_cs_decoder_alloc_temp(dec, args->DataSize);
        if (!args->pData) return;
        memset(args->pData, 0, args->DataSize);
    } else {
        args->pData = NULL;
    }


}

static inline void
npt_replace_ID3D11DeviceContext_GetData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GetData *args)
{
    args->pAsync = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pAsync, NPT_OBJECT_TYPE_ID3D11ASYNCHRONOUS);
}

static inline void
npt_encode_ID3D11DeviceContext_GetData_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D11DeviceContext_GetData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pData) {
        npt_encode_array_count(enc, args->DataSize);
        npt_encode_blob_array(enc, args->pData, args->DataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext_GetData(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GetData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GetData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GetData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetData: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GetData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GetData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GetData);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GetData) {
        args.ret = ctx->id3d11devicecontext_dispatch_overrides->GetData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pAsync, args.pData, args.DataSize, args.GetDataFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GetData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::SetPredication
 */

struct npt_command_ID3D11DeviceContext_SetPredication {
    void *_self;
    ID3D11Predicate * pPredicate;
    BOOL PredicateValue;
};

static inline void
npt_decode_ID3D11DeviceContext_SetPredication_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11DeviceContext_SetPredication *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pPredicate = (ID3D11Predicate *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->PredicateValue);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_SetPredication_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_SetPredication *args)
{
    args->pPredicate = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pPredicate, NPT_OBJECT_TYPE_ID3D11PREDICATE);
}

static inline void
npt_encode_ID3D11DeviceContext_SetPredication_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11DeviceContext_SetPredication *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SetPredication),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_SetPredication(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_SetPredication args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_SetPredication_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SetPredication on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_SetPredication_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SetPredication: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_SetPredication _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_SetPredication, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_SetPredication);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->SetPredication) {
        ctx->id3d11devicecontext_dispatch_overrides->SetPredication(ctx, &args, _original);
    } else {
        _original(args._self, args.pPredicate, args.PredicateValue);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_SetPredication_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_GSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_GSSetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_GSSetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_ppShaderResourceViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppShaderResourceViews) {
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppShaderResourceViews);
        if (!args->ppShaderResourceViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppShaderResourceViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppShaderResourceViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppShaderResourceViews && _cnt_ppShaderResourceViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSSetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSSetShaderResources *args)
{
    if (args->ppShaderResourceViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumViews); _i++)
            args->ppShaderResourceViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_GSSetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSSetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSSetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSSetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSSetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSSetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSSetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSSetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSSetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->GSSetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSSetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_GSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_GSSetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_GSSetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    uint64_t _cnt_ppSamplers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppSamplers) {
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppSamplers);
        if (!args->ppSamplers) return;
        for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppSamplers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppSamplers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppSamplers && _cnt_ppSamplers < (uint64_t)(args->NumSamplers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSSetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSSetSamplers *args)
{
    if (args->ppSamplers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSamplers); _i++)
            args->ppSamplers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_GSSetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_GSSetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSSetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSSetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSSetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSSetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSSetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSSetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSSetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSSetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSSetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSSetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->GSSetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSSetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMSetRenderTargets
 */

struct npt_command_ID3D11DeviceContext_OMSetRenderTargets {
    void *_self;
    UINT NumViews;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView * pDepthStencilView;
};

static inline void
npt_decode_ID3D11DeviceContext_OMSetRenderTargets_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11DeviceContext_OMSetRenderTargets *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_ppRenderTargetViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppRenderTargetViews) {
        args->ppRenderTargetViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppRenderTargetViews);
        if (!args->ppRenderTargetViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppRenderTargetViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppRenderTargetViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppRenderTargetViews = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDepthStencilView = (ID3D11DepthStencilView *)npt_object_from_id(_id);
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppRenderTargetViews && _cnt_ppRenderTargetViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_OMSetRenderTargets_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMSetRenderTargets *args)
{
    if (args->ppRenderTargetViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumViews); _i++)
            args->ppRenderTargetViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppRenderTargetViews[_i], NPT_OBJECT_TYPE_ID3D11RENDERTARGETVIEW);
    }
    args->pDepthStencilView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDepthStencilView, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILVIEW);
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetRenderTargets_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11DeviceContext_OMSetRenderTargets *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetRenderTargets),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMSetRenderTargets(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMSetRenderTargets args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMSetRenderTargets_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetRenderTargets on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMSetRenderTargets_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetRenderTargets: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMSetRenderTargets _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMSetRenderTargets, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMSetRenderTargets);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMSetRenderTargets) {
        ctx->id3d11devicecontext_dispatch_overrides->OMSetRenderTargets(ctx, &args, _original);
    } else {
        _original(args._self, args.NumViews, args.ppRenderTargetViews, args.pDepthStencilView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMSetRenderTargets_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMSetRenderTargetsAndUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews {
    void *_self;
    UINT NumRTVs;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView * pDepthStencilView;
    UINT UAVStartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    const UINT * pUAVInitialCounts;
};

static inline void
npt_decode_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_args_temp(struct npt_cs_decoder *dec,
                                                                                   struct npt_command_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumRTVs);
    uint64_t _cnt_ppRenderTargetViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppRenderTargetViews) {
        args->ppRenderTargetViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppRenderTargetViews);
        if (!args->ppRenderTargetViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppRenderTargetViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppRenderTargetViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppRenderTargetViews = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDepthStencilView = (ID3D11DepthStencilView *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->UAVStartSlot);
    npt_decode_UINT(dec, &args->NumUAVs);
    uint64_t _cnt_ppUnorderedAccessViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppUnorderedAccessViews) {
        args->ppUnorderedAccessViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppUnorderedAccessViews);
        if (!args->ppUnorderedAccessViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppUnorderedAccessViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppUnorderedAccessViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppUnorderedAccessViews = NULL;
    }
    uint64_t _cnt_pUAVInitialCounts = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pUAVInitialCounts = npt_decode_array_count_unchecked(dec);
        args->pUAVInitialCounts = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pUAVInitialCounts);
        if (!args->pUAVInitialCounts) return;
        npt_decode_UINT_array(dec, (UINT *)args->pUAVInitialCounts, _cnt_pUAVInitialCounts);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pUAVInitialCounts = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppRenderTargetViews && _cnt_ppRenderTargetViews < (uint64_t)(args->NumRTVs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_ppUnorderedAccessViews && _cnt_ppUnorderedAccessViews < (uint64_t)(args->NumUAVs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pUAVInitialCounts && _cnt_pUAVInitialCounts < (uint64_t)(args->NumUAVs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews *args)
{
    if (args->ppRenderTargetViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumRTVs); _i++)
            args->ppRenderTargetViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppRenderTargetViews[_i], NPT_OBJECT_TYPE_ID3D11RENDERTARGETVIEW);
    }
    args->pDepthStencilView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDepthStencilView, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILVIEW);
    if (args->ppUnorderedAccessViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumUAVs); _i++)
            args->ppUnorderedAccessViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppUnorderedAccessViews[_i], NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_reply(struct npt_cs_encoder *enc,
                                                                               const struct npt_command_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews(struct npt_dispatch_context *ctx,
                                                                           uint32_t cmd_flags,
                                                                           uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMSetRenderTargetsAndUnorderedAccessViews) {
        ctx->id3d11devicecontext_dispatch_overrides->OMSetRenderTargetsAndUnorderedAccessViews(ctx, &args, _original);
    } else {
        _original(args._self, args.NumRTVs, args.ppRenderTargetViews, args.pDepthStencilView, args.UAVStartSlot, args.NumUAVs, args.ppUnorderedAccessViews, args.pUAVInitialCounts);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMSetRenderTargetsAndUnorderedAccessViews_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMSetBlendState
 */

struct npt_command_ID3D11DeviceContext_OMSetBlendState {
    void *_self;
    ID3D11BlendState * pBlendState;
    const FLOAT * BlendFactor;
    UINT SampleMask;
};

static inline void
npt_decode_ID3D11DeviceContext_OMSetBlendState_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11DeviceContext_OMSetBlendState *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pBlendState = (ID3D11BlendState *)npt_object_from_id(_id);
    }
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count) {
            if (_count != (4)) { npt_cs_decoder_set_fatal(dec); return; }
            args->BlendFactor = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
            if (!args->BlendFactor) return;
            npt_decode_FLOAT_array(dec, (FLOAT *)args->BlendFactor, 4);
        } else {
            args->BlendFactor = NULL;
        }
    }
    npt_decode_UINT(dec, &args->SampleMask);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_OMSetBlendState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMSetBlendState *args)
{
    args->pBlendState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pBlendState, NPT_OBJECT_TYPE_ID3D11BLENDSTATE);
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetBlendState_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11DeviceContext_OMSetBlendState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetBlendState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMSetBlendState(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMSetBlendState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMSetBlendState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetBlendState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMSetBlendState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetBlendState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMSetBlendState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMSetBlendState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMSetBlendState);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMSetBlendState) {
        ctx->id3d11devicecontext_dispatch_overrides->OMSetBlendState(ctx, &args, _original);
    } else {
        _original(args._self, args.pBlendState, args.BlendFactor, args.SampleMask);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMSetBlendState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMSetDepthStencilState
 */

struct npt_command_ID3D11DeviceContext_OMSetDepthStencilState {
    void *_self;
    ID3D11DepthStencilState * pDepthStencilState;
    UINT StencilRef;
};

static inline void
npt_decode_ID3D11DeviceContext_OMSetDepthStencilState_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext_OMSetDepthStencilState *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDepthStencilState = (ID3D11DepthStencilState *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->StencilRef);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_OMSetDepthStencilState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMSetDepthStencilState *args)
{
    args->pDepthStencilState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDepthStencilState, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILSTATE);
}

static inline void
npt_encode_ID3D11DeviceContext_OMSetDepthStencilState_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext_OMSetDepthStencilState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMSetDepthStencilState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMSetDepthStencilState(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMSetDepthStencilState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMSetDepthStencilState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetDepthStencilState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMSetDepthStencilState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMSetDepthStencilState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMSetDepthStencilState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMSetDepthStencilState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMSetDepthStencilState);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMSetDepthStencilState) {
        ctx->id3d11devicecontext_dispatch_overrides->OMSetDepthStencilState(ctx, &args, _original);
    } else {
        _original(args._self, args.pDepthStencilState, args.StencilRef);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMSetDepthStencilState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::SOSetTargets
 */

struct npt_command_ID3D11DeviceContext_SOSetTargets {
    void *_self;
    UINT NumBuffers;
    ID3D11Buffer ** ppSOTargets;
    const UINT * pOffsets;
};

static inline void
npt_decode_ID3D11DeviceContext_SOSetTargets_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11DeviceContext_SOSetTargets *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppSOTargets = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppSOTargets) {
        args->ppSOTargets = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppSOTargets);
        if (!args->ppSOTargets) return;
        for (uint64_t _i = 0; _i < _cnt_ppSOTargets; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppSOTargets[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppSOTargets = NULL;
    }
    uint64_t _cnt_pOffsets = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pOffsets = npt_decode_array_count_unchecked(dec);
        args->pOffsets = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pOffsets);
        if (!args->pOffsets) return;
        npt_decode_UINT_array(dec, (UINT *)args->pOffsets, _cnt_pOffsets);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pOffsets = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppSOTargets && _cnt_ppSOTargets < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pOffsets && _cnt_pOffsets < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_SOSetTargets_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_SOSetTargets *args)
{
    if (args->ppSOTargets) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppSOTargets[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppSOTargets[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_SOSetTargets_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11DeviceContext_SOSetTargets *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SOSetTargets),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_SOSetTargets(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_SOSetTargets args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_SOSetTargets_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SOSetTargets on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_SOSetTargets_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SOSetTargets: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_SOSetTargets _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_SOSetTargets, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_SOSetTargets);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->SOSetTargets) {
        ctx->id3d11devicecontext_dispatch_overrides->SOSetTargets(ctx, &args, _original);
    } else {
        _original(args._self, args.NumBuffers, args.ppSOTargets, args.pOffsets);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_SOSetTargets_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DrawAuto
 */

struct npt_command_ID3D11DeviceContext_DrawAuto {
    void *_self;
};

static inline void
npt_decode_ID3D11DeviceContext_DrawAuto_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D11DeviceContext_DrawAuto *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11DeviceContext_DrawAuto_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DrawAuto *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DrawAuto_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D11DeviceContext_DrawAuto *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawAuto),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DrawAuto(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DrawAuto args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DrawAuto_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawAuto on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DrawAuto_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawAuto: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DrawAuto _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DrawAuto, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DrawAuto);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DrawAuto) {
        ctx->id3d11devicecontext_dispatch_overrides->DrawAuto(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DrawAuto_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DrawIndexedInstancedIndirect
 */

struct npt_command_ID3D11DeviceContext_DrawIndexedInstancedIndirect {
    void *_self;
    ID3D11Buffer * pBufferForArgs;
    UINT AlignedByteOffsetForArgs;
};

static inline void
npt_decode_ID3D11DeviceContext_DrawIndexedInstancedIndirect_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11DeviceContext_DrawIndexedInstancedIndirect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pBufferForArgs = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->AlignedByteOffsetForArgs);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_DrawIndexedInstancedIndirect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DrawIndexedInstancedIndirect *args)
{
    args->pBufferForArgs = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pBufferForArgs, NPT_OBJECT_TYPE_ID3D11BUFFER);
}

static inline void
npt_encode_ID3D11DeviceContext_DrawIndexedInstancedIndirect_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11DeviceContext_DrawIndexedInstancedIndirect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawIndexedInstancedIndirect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DrawIndexedInstancedIndirect(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DrawIndexedInstancedIndirect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DrawIndexedInstancedIndirect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawIndexedInstancedIndirect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DrawIndexedInstancedIndirect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawIndexedInstancedIndirect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DrawIndexedInstancedIndirect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DrawIndexedInstancedIndirect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DrawIndexedInstancedIndirect);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DrawIndexedInstancedIndirect) {
        ctx->id3d11devicecontext_dispatch_overrides->DrawIndexedInstancedIndirect(ctx, &args, _original);
    } else {
        _original(args._self, args.pBufferForArgs, args.AlignedByteOffsetForArgs);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DrawIndexedInstancedIndirect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DrawInstancedIndirect
 */

struct npt_command_ID3D11DeviceContext_DrawInstancedIndirect {
    void *_self;
    ID3D11Buffer * pBufferForArgs;
    UINT AlignedByteOffsetForArgs;
};

static inline void
npt_decode_ID3D11DeviceContext_DrawInstancedIndirect_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11DeviceContext_DrawInstancedIndirect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pBufferForArgs = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->AlignedByteOffsetForArgs);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_DrawInstancedIndirect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DrawInstancedIndirect *args)
{
    args->pBufferForArgs = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pBufferForArgs, NPT_OBJECT_TYPE_ID3D11BUFFER);
}

static inline void
npt_encode_ID3D11DeviceContext_DrawInstancedIndirect_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11DeviceContext_DrawInstancedIndirect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DrawInstancedIndirect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DrawInstancedIndirect(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DrawInstancedIndirect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DrawInstancedIndirect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawInstancedIndirect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DrawInstancedIndirect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DrawInstancedIndirect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DrawInstancedIndirect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DrawInstancedIndirect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DrawInstancedIndirect);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DrawInstancedIndirect) {
        ctx->id3d11devicecontext_dispatch_overrides->DrawInstancedIndirect(ctx, &args, _original);
    } else {
        _original(args._self, args.pBufferForArgs, args.AlignedByteOffsetForArgs);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DrawInstancedIndirect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::Dispatch
 */

struct npt_command_ID3D11DeviceContext_Dispatch {
    void *_self;
    UINT ThreadGroupCountX;
    UINT ThreadGroupCountY;
    UINT ThreadGroupCountZ;
};

static inline void
npt_decode_ID3D11DeviceContext_Dispatch_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D11DeviceContext_Dispatch *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->ThreadGroupCountX);
    npt_decode_UINT(dec, &args->ThreadGroupCountY);
    npt_decode_UINT(dec, &args->ThreadGroupCountZ);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_Dispatch_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_Dispatch *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_Dispatch_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D11DeviceContext_Dispatch *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Dispatch),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_Dispatch(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_Dispatch args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_Dispatch_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Dispatch on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_Dispatch_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Dispatch: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_Dispatch _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_Dispatch, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_Dispatch);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->Dispatch) {
        ctx->id3d11devicecontext_dispatch_overrides->Dispatch(ctx, &args, _original);
    } else {
        _original(args._self, args.ThreadGroupCountX, args.ThreadGroupCountY, args.ThreadGroupCountZ);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_Dispatch_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DispatchIndirect
 */

struct npt_command_ID3D11DeviceContext_DispatchIndirect {
    void *_self;
    ID3D11Buffer * pBufferForArgs;
    UINT AlignedByteOffsetForArgs;
};

static inline void
npt_decode_ID3D11DeviceContext_DispatchIndirect_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11DeviceContext_DispatchIndirect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pBufferForArgs = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->AlignedByteOffsetForArgs);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_DispatchIndirect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DispatchIndirect *args)
{
    args->pBufferForArgs = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pBufferForArgs, NPT_OBJECT_TYPE_ID3D11BUFFER);
}

static inline void
npt_encode_ID3D11DeviceContext_DispatchIndirect_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11DeviceContext_DispatchIndirect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DispatchIndirect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DispatchIndirect(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DispatchIndirect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DispatchIndirect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DispatchIndirect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DispatchIndirect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DispatchIndirect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DispatchIndirect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DispatchIndirect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DispatchIndirect);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DispatchIndirect) {
        ctx->id3d11devicecontext_dispatch_overrides->DispatchIndirect(ctx, &args, _original);
    } else {
        _original(args._self, args.pBufferForArgs, args.AlignedByteOffsetForArgs);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DispatchIndirect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::RSSetState
 */

struct npt_command_ID3D11DeviceContext_RSSetState {
    void *_self;
    ID3D11RasterizerState * pRasterizerState;
};

static inline void
npt_decode_ID3D11DeviceContext_RSSetState_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11DeviceContext_RSSetState *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRasterizerState = (ID3D11RasterizerState *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_RSSetState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_RSSetState *args)
{
    args->pRasterizerState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRasterizerState, NPT_OBJECT_TYPE_ID3D11RASTERIZERSTATE);
}

static inline void
npt_encode_ID3D11DeviceContext_RSSetState_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11DeviceContext_RSSetState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSSetState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_RSSetState(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_RSSetState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_RSSetState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSSetState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_RSSetState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSSetState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_RSSetState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_RSSetState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_RSSetState);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->RSSetState) {
        ctx->id3d11devicecontext_dispatch_overrides->RSSetState(ctx, &args, _original);
    } else {
        _original(args._self, args.pRasterizerState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_RSSetState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::RSSetViewports
 */

struct npt_command_ID3D11DeviceContext_RSSetViewports {
    void *_self;
    UINT NumViewports;
    const D3D11_VIEWPORT * pViewports;
};

static inline void
npt_decode_ID3D11DeviceContext_RSSetViewports_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11DeviceContext_RSSetViewports *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumViewports);
    uint64_t _cnt_pViewports = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pViewports = npt_decode_array_count_unchecked(dec);
        args->pViewports = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_VIEWPORT), _cnt_pViewports);
        if (!args->pViewports) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pViewports; _i++)
            npt_decode_D3D11_VIEWPORT(dec, (D3D11_VIEWPORT *)&args->pViewports[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pViewports = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pViewports && _cnt_pViewports < (uint64_t)(args->NumViewports)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_RSSetViewports_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_RSSetViewports *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_RSSetViewports_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11DeviceContext_RSSetViewports *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSSetViewports),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_RSSetViewports(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_RSSetViewports args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_RSSetViewports_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSSetViewports on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_RSSetViewports_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSSetViewports: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_RSSetViewports _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_RSSetViewports, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_RSSetViewports);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->RSSetViewports) {
        ctx->id3d11devicecontext_dispatch_overrides->RSSetViewports(ctx, &args, _original);
    } else {
        _original(args._self, args.NumViewports, args.pViewports);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_RSSetViewports_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::RSSetScissorRects
 */

struct npt_command_ID3D11DeviceContext_RSSetScissorRects {
    void *_self;
    UINT NumRects;
    const D3D11_RECT * pRects;
};

static inline void
npt_decode_ID3D11DeviceContext_RSSetScissorRects_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11DeviceContext_RSSetScissorRects *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumRects);
    uint64_t _cnt_pRects = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRects = npt_decode_array_count_unchecked(dec);
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_RECT), _cnt_pRects);
        if (!args->pRects) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRects; _i++)
            npt_decode_D3D11_RECT(dec, (D3D11_RECT *)&args->pRects[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRects && _cnt_pRects < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_RSSetScissorRects_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_RSSetScissorRects *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_RSSetScissorRects_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11DeviceContext_RSSetScissorRects *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSSetScissorRects),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_RSSetScissorRects(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_RSSetScissorRects args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_RSSetScissorRects_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSSetScissorRects on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_RSSetScissorRects_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSSetScissorRects: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_RSSetScissorRects _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_RSSetScissorRects, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_RSSetScissorRects);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->RSSetScissorRects) {
        ctx->id3d11devicecontext_dispatch_overrides->RSSetScissorRects(ctx, &args, _original);
    } else {
        _original(args._self, args.NumRects, args.pRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_RSSetScissorRects_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CopySubresourceRegion
 */

struct npt_command_ID3D11DeviceContext_CopySubresourceRegion {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    UINT DstX;
    UINT DstY;
    UINT DstZ;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    const D3D11_BOX * pSrcBox;
};

static inline void
npt_decode_ID3D11DeviceContext_CopySubresourceRegion_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11DeviceContext_CopySubresourceRegion *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    npt_decode_UINT(dec, &args->DstX);
    npt_decode_UINT(dec, &args->DstY);
    npt_decode_UINT(dec, &args->DstZ);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->SrcSubresource);
    if (npt_decode_simple_pointer(dec)) {
        args->pSrcBox = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BOX));
        if (!args->pSrcBox) return;
        npt_decode_D3D11_BOX(dec, (D3D11_BOX *)args->pSrcBox);
    } else {
        args->pSrcBox = NULL;
    }
    /* Allocate temp storage for output-only parameters */








}

static inline void
npt_replace_ID3D11DeviceContext_CopySubresourceRegion_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CopySubresourceRegion *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_CopySubresourceRegion_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11DeviceContext_CopySubresourceRegion *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CopySubresourceRegion),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CopySubresourceRegion(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CopySubresourceRegion args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CopySubresourceRegion_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CopySubresourceRegion on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CopySubresourceRegion_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CopySubresourceRegion: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CopySubresourceRegion _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CopySubresourceRegion, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CopySubresourceRegion);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CopySubresourceRegion) {
        ctx->id3d11devicecontext_dispatch_overrides->CopySubresourceRegion(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.DstX, args.DstY, args.DstZ, args.pSrcResource, args.SrcSubresource, args.pSrcBox);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CopySubresourceRegion_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CopyResource
 */

struct npt_command_ID3D11DeviceContext_CopyResource {
    void *_self;
    ID3D11Resource * pDstResource;
    ID3D11Resource * pSrcResource;
};

static inline void
npt_decode_ID3D11DeviceContext_CopyResource_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11DeviceContext_CopyResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_CopyResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CopyResource *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_CopyResource_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11DeviceContext_CopyResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CopyResource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CopyResource(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CopyResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CopyResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CopyResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CopyResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CopyResource: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CopyResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CopyResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CopyResource);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CopyResource) {
        ctx->id3d11devicecontext_dispatch_overrides->CopyResource(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.pSrcResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CopyResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::UpdateSubresource
 */

struct npt_command_ID3D11DeviceContext_UpdateSubresource {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    const D3D11_BOX * pDstBox;
    const void * pSrcData;
    UINT SrcRowPitch;
    UINT SrcDepthPitch;
};

static inline void
npt_decode_ID3D11DeviceContext_UpdateSubresource_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11DeviceContext_UpdateSubresource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    if (npt_decode_simple_pointer(dec)) {
        args->pDstBox = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BOX));
        if (!args->pDstBox) return;
        npt_decode_D3D11_BOX(dec, (D3D11_BOX *)args->pDstBox);
    } else {
        args->pDstBox = NULL;
    }
    /* ERROR: pSrcData (void) is not serializable */
    return;
    npt_decode_UINT(dec, &args->SrcRowPitch);
    npt_decode_UINT(dec, &args->SrcDepthPitch);
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D11DeviceContext_UpdateSubresource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_UpdateSubresource *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_UpdateSubresource_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11DeviceContext_UpdateSubresource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_UpdateSubresource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_UpdateSubresource(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_UpdateSubresource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_UpdateSubresource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_UpdateSubresource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_UpdateSubresource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_UpdateSubresource: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_UpdateSubresource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_UpdateSubresource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_UpdateSubresource);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->UpdateSubresource) {
        ctx->id3d11devicecontext_dispatch_overrides->UpdateSubresource(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.pDstBox, args.pSrcData, args.SrcRowPitch, args.SrcDepthPitch);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_UpdateSubresource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CopyStructureCount
 */

struct npt_command_ID3D11DeviceContext_CopyStructureCount {
    void *_self;
    ID3D11Buffer * pDstBuffer;
    UINT DstAlignedByteOffset;
    ID3D11UnorderedAccessView * pSrcView;
};

static inline void
npt_decode_ID3D11DeviceContext_CopyStructureCount_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11DeviceContext_CopyStructureCount *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstBuffer = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstAlignedByteOffset);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcView = (ID3D11UnorderedAccessView *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CopyStructureCount_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CopyStructureCount *args)
{
    args->pDstBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstBuffer, NPT_OBJECT_TYPE_ID3D11BUFFER);
    args->pSrcView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcView, NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);
}

static inline void
npt_encode_ID3D11DeviceContext_CopyStructureCount_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11DeviceContext_CopyStructureCount *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CopyStructureCount),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CopyStructureCount(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CopyStructureCount args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CopyStructureCount_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CopyStructureCount on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CopyStructureCount_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CopyStructureCount: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CopyStructureCount _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CopyStructureCount, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CopyStructureCount);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CopyStructureCount) {
        ctx->id3d11devicecontext_dispatch_overrides->CopyStructureCount(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstBuffer, args.DstAlignedByteOffset, args.pSrcView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CopyStructureCount_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::ClearRenderTargetView
 */

struct npt_command_ID3D11DeviceContext_ClearRenderTargetView {
    void *_self;
    ID3D11RenderTargetView * pRenderTargetView;
    const FLOAT * ColorRGBA;
};

static inline void
npt_decode_ID3D11DeviceContext_ClearRenderTargetView_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11DeviceContext_ClearRenderTargetView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRenderTargetView = (ID3D11RenderTargetView *)npt_object_from_id(_id);
    }
    (void)npt_decode_array_count(dec, 4);
    args->ColorRGBA = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
    if (!args->ColorRGBA) return;
    npt_decode_FLOAT_array(dec, (FLOAT *)args->ColorRGBA, 4);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_ClearRenderTargetView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_ClearRenderTargetView *args)
{
    args->pRenderTargetView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRenderTargetView, NPT_OBJECT_TYPE_ID3D11RENDERTARGETVIEW);
}

static inline void
npt_encode_ID3D11DeviceContext_ClearRenderTargetView_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11DeviceContext_ClearRenderTargetView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearRenderTargetView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_ClearRenderTargetView(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_ClearRenderTargetView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_ClearRenderTargetView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearRenderTargetView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_ClearRenderTargetView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearRenderTargetView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_ClearRenderTargetView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_ClearRenderTargetView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_ClearRenderTargetView);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->ClearRenderTargetView) {
        ctx->id3d11devicecontext_dispatch_overrides->ClearRenderTargetView(ctx, &args, _original);
    } else {
        _original(args._self, args.pRenderTargetView, args.ColorRGBA);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_ClearRenderTargetView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::ClearUnorderedAccessViewUint
 */

struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewUint {
    void *_self;
    ID3D11UnorderedAccessView * pUnorderedAccessView;
    const UINT * Values;
};

static inline void
npt_decode_ID3D11DeviceContext_ClearUnorderedAccessViewUint_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewUint *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pUnorderedAccessView = (ID3D11UnorderedAccessView *)npt_object_from_id(_id);
    }
    (void)npt_decode_array_count(dec, 4);
    args->Values = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), (4));
    if (!args->Values) return;
    npt_decode_UINT_array(dec, (UINT *)args->Values, 4);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_ClearUnorderedAccessViewUint_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewUint *args)
{
    args->pUnorderedAccessView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pUnorderedAccessView, NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);
}

static inline void
npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewUint_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewUint *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearUnorderedAccessViewUint),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_ClearUnorderedAccessViewUint(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewUint args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_ClearUnorderedAccessViewUint_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearUnorderedAccessViewUint on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_ClearUnorderedAccessViewUint_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearUnorderedAccessViewUint: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_ClearUnorderedAccessViewUint _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_ClearUnorderedAccessViewUint, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_ClearUnorderedAccessViewUint);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->ClearUnorderedAccessViewUint) {
        ctx->id3d11devicecontext_dispatch_overrides->ClearUnorderedAccessViewUint(ctx, &args, _original);
    } else {
        _original(args._self, args.pUnorderedAccessView, args.Values);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewUint_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::ClearUnorderedAccessViewFloat
 */

struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewFloat {
    void *_self;
    ID3D11UnorderedAccessView * pUnorderedAccessView;
    const FLOAT * Values;
};

static inline void
npt_decode_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewFloat *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pUnorderedAccessView = (ID3D11UnorderedAccessView *)npt_object_from_id(_id);
    }
    (void)npt_decode_array_count(dec, 4);
    args->Values = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
    if (!args->Values) return;
    npt_decode_FLOAT_array(dec, (FLOAT *)args->Values, 4);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewFloat *args)
{
    args->pUnorderedAccessView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pUnorderedAccessView, NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);
}

static inline void
npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewFloat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearUnorderedAccessViewFloat),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_ClearUnorderedAccessViewFloat(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_ClearUnorderedAccessViewFloat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearUnorderedAccessViewFloat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearUnorderedAccessViewFloat: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_ClearUnorderedAccessViewFloat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_ClearUnorderedAccessViewFloat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_ClearUnorderedAccessViewFloat);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->ClearUnorderedAccessViewFloat) {
        ctx->id3d11devicecontext_dispatch_overrides->ClearUnorderedAccessViewFloat(ctx, &args, _original);
    } else {
        _original(args._self, args.pUnorderedAccessView, args.Values);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_ClearUnorderedAccessViewFloat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::ClearDepthStencilView
 */

struct npt_command_ID3D11DeviceContext_ClearDepthStencilView {
    void *_self;
    ID3D11DepthStencilView * pDepthStencilView;
    UINT ClearFlags;
    FLOAT Depth;
    UINT8 Stencil;
};

static inline void
npt_decode_ID3D11DeviceContext_ClearDepthStencilView_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11DeviceContext_ClearDepthStencilView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDepthStencilView = (ID3D11DepthStencilView *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->ClearFlags);
    npt_decode_FLOAT(dec, &args->Depth);
    npt_decode_UINT8(dec, &args->Stencil);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11DeviceContext_ClearDepthStencilView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_ClearDepthStencilView *args)
{
    args->pDepthStencilView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDepthStencilView, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILVIEW);
}

static inline void
npt_encode_ID3D11DeviceContext_ClearDepthStencilView_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11DeviceContext_ClearDepthStencilView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearDepthStencilView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_ClearDepthStencilView(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_ClearDepthStencilView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_ClearDepthStencilView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearDepthStencilView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_ClearDepthStencilView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearDepthStencilView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_ClearDepthStencilView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_ClearDepthStencilView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_ClearDepthStencilView);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->ClearDepthStencilView) {
        ctx->id3d11devicecontext_dispatch_overrides->ClearDepthStencilView(ctx, &args, _original);
    } else {
        _original(args._self, args.pDepthStencilView, args.ClearFlags, args.Depth, args.Stencil);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_ClearDepthStencilView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GenerateMips
 */

struct npt_command_ID3D11DeviceContext_GenerateMips {
    void *_self;
    ID3D11ShaderResourceView * pShaderResourceView;
};

static inline void
npt_decode_ID3D11DeviceContext_GenerateMips_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11DeviceContext_GenerateMips *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pShaderResourceView = (ID3D11ShaderResourceView *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_GenerateMips_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GenerateMips *args)
{
    args->pShaderResourceView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pShaderResourceView, NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
}

static inline void
npt_encode_ID3D11DeviceContext_GenerateMips_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11DeviceContext_GenerateMips *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GenerateMips),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GenerateMips(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GenerateMips args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GenerateMips_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GenerateMips on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GenerateMips_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GenerateMips: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GenerateMips _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GenerateMips, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GenerateMips);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GenerateMips) {
        ctx->id3d11devicecontext_dispatch_overrides->GenerateMips(ctx, &args, _original);
    } else {
        _original(args._self, args.pShaderResourceView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GenerateMips_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::SetResourceMinLOD
 */

struct npt_command_ID3D11DeviceContext_SetResourceMinLOD {
    void *_self;
    ID3D11Resource * pResource;
    FLOAT MinLOD;
};

static inline void
npt_decode_ID3D11DeviceContext_SetResourceMinLOD_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11DeviceContext_SetResourceMinLOD *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_FLOAT(dec, &args->MinLOD);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_SetResourceMinLOD_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_SetResourceMinLOD *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_SetResourceMinLOD_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11DeviceContext_SetResourceMinLOD *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SetResourceMinLOD),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_SetResourceMinLOD(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_SetResourceMinLOD args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_SetResourceMinLOD_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SetResourceMinLOD on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_SetResourceMinLOD_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SetResourceMinLOD: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_SetResourceMinLOD _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_SetResourceMinLOD, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_SetResourceMinLOD);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->SetResourceMinLOD) {
        ctx->id3d11devicecontext_dispatch_overrides->SetResourceMinLOD(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource, args.MinLOD);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_SetResourceMinLOD_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GetResourceMinLOD
 */

struct npt_command_ID3D11DeviceContext_GetResourceMinLOD {
    void *_self;
    ID3D11Resource * pResource;
    FLOAT ret;
};

static inline void
npt_decode_ID3D11DeviceContext_GetResourceMinLOD_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11DeviceContext_GetResourceMinLOD *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_GetResourceMinLOD_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GetResourceMinLOD *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_GetResourceMinLOD_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11DeviceContext_GetResourceMinLOD *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetResourceMinLOD),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GetResourceMinLOD(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GetResourceMinLOD args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GetResourceMinLOD_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetResourceMinLOD on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GetResourceMinLOD_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetResourceMinLOD: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GetResourceMinLOD _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GetResourceMinLOD, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GetResourceMinLOD);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GetResourceMinLOD) {
        args.ret = ctx->id3d11devicecontext_dispatch_overrides->GetResourceMinLOD(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GetResourceMinLOD_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::ResolveSubresource
 */

struct npt_command_ID3D11DeviceContext_ResolveSubresource {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    DXGI_FORMAT Format;
};

static inline void
npt_decode_ID3D11DeviceContext_ResolveSubresource_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11DeviceContext_ResolveSubresource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->SrcSubresource);
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext_ResolveSubresource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_ResolveSubresource *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext_ResolveSubresource_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11DeviceContext_ResolveSubresource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ResolveSubresource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_ResolveSubresource(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_ResolveSubresource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_ResolveSubresource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ResolveSubresource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_ResolveSubresource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ResolveSubresource: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_ResolveSubresource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_ResolveSubresource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_ResolveSubresource);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->ResolveSubresource) {
        ctx->id3d11devicecontext_dispatch_overrides->ResolveSubresource(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.pSrcResource, args.SrcSubresource, args.Format);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_ResolveSubresource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::ExecuteCommandList
 */

struct npt_command_ID3D11DeviceContext_ExecuteCommandList {
    void *_self;
    ID3D11CommandList * pCommandList;
    BOOL RestoreContextState;
};

static inline void
npt_decode_ID3D11DeviceContext_ExecuteCommandList_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11DeviceContext_ExecuteCommandList *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCommandList = (ID3D11CommandList *)npt_object_from_id(_id);
    }
    npt_decode_BOOL(dec, &args->RestoreContextState);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_ExecuteCommandList_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_ExecuteCommandList *args)
{
    args->pCommandList = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCommandList, NPT_OBJECT_TYPE_ID3D11COMMANDLIST);
}

static inline void
npt_encode_ID3D11DeviceContext_ExecuteCommandList_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11DeviceContext_ExecuteCommandList *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ExecuteCommandList),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_ExecuteCommandList(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_ExecuteCommandList args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_ExecuteCommandList_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ExecuteCommandList on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_ExecuteCommandList_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ExecuteCommandList: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_ExecuteCommandList _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_ExecuteCommandList, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_ExecuteCommandList);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->ExecuteCommandList) {
        ctx->id3d11devicecontext_dispatch_overrides->ExecuteCommandList(ctx, &args, _original);
    } else {
        _original(args._self, args.pCommandList, args.RestoreContextState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_ExecuteCommandList_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_HSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_HSSetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_HSSetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_ppShaderResourceViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppShaderResourceViews) {
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppShaderResourceViews);
        if (!args->ppShaderResourceViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppShaderResourceViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppShaderResourceViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppShaderResourceViews && _cnt_ppShaderResourceViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSSetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSSetShaderResources *args)
{
    if (args->ppShaderResourceViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumViews); _i++)
            args->ppShaderResourceViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_HSSetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSSetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSSetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSSetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSSetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSSetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSSetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSSetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSSetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->HSSetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSSetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSSetShader
 */

struct npt_command_ID3D11DeviceContext_HSSetShader {
    void *_self;
    ID3D11HullShader * pHullShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_HSSetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_HSSetShader *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pHullShader = (ID3D11HullShader *)npt_object_from_id(_id);
    }
    uint64_t _cnt_ppClassInstances = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppClassInstances) {
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppClassInstances);
        if (!args->ppClassInstances) return;
        for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppClassInstances[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppClassInstances = NULL;
    }
    npt_decode_UINT(dec, &args->NumClassInstances);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppClassInstances && _cnt_ppClassInstances < (uint64_t)(args->NumClassInstances)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSSetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSSetShader *args)
{
    args->pHullShader = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pHullShader, NPT_OBJECT_TYPE_ID3D11HULLSHADER);
    if (args->ppClassInstances) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumClassInstances); _i++)
            args->ppClassInstances[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_HSSetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSSetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSSetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSSetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSSetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetShader: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSSetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSSetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSSetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSSetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->HSSetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.pHullShader, args.ppClassInstances, args.NumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSSetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_HSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_HSSetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_HSSetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    uint64_t _cnt_ppSamplers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppSamplers) {
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppSamplers);
        if (!args->ppSamplers) return;
        for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppSamplers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppSamplers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppSamplers && _cnt_ppSamplers < (uint64_t)(args->NumSamplers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSSetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSSetSamplers *args)
{
    if (args->ppSamplers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSamplers); _i++)
            args->ppSamplers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_HSSetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSSetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSSetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSSetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSSetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSSetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSSetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSSetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSSetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->HSSetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSSetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_HSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_HSSetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_HSSetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSSetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSSetConstantBuffers *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_HSSetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_HSSetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSSetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSSetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSSetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSSetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSSetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSSetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSSetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSSetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSSetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSSetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->HSSetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSSetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_DSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_DSSetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_DSSetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_ppShaderResourceViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppShaderResourceViews) {
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppShaderResourceViews);
        if (!args->ppShaderResourceViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppShaderResourceViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppShaderResourceViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppShaderResourceViews && _cnt_ppShaderResourceViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSSetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSSetShaderResources *args)
{
    if (args->ppShaderResourceViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumViews); _i++)
            args->ppShaderResourceViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_DSSetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSSetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSSetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSSetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSSetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSSetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSSetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSSetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSSetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->DSSetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSSetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSSetShader
 */

struct npt_command_ID3D11DeviceContext_DSSetShader {
    void *_self;
    ID3D11DomainShader * pDomainShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_DSSetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_DSSetShader *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDomainShader = (ID3D11DomainShader *)npt_object_from_id(_id);
    }
    uint64_t _cnt_ppClassInstances = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppClassInstances) {
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppClassInstances);
        if (!args->ppClassInstances) return;
        for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppClassInstances[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppClassInstances = NULL;
    }
    npt_decode_UINT(dec, &args->NumClassInstances);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppClassInstances && _cnt_ppClassInstances < (uint64_t)(args->NumClassInstances)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSSetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSSetShader *args)
{
    args->pDomainShader = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDomainShader, NPT_OBJECT_TYPE_ID3D11DOMAINSHADER);
    if (args->ppClassInstances) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumClassInstances); _i++)
            args->ppClassInstances[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_DSSetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSSetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSSetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSSetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSSetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetShader: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSSetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSSetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSSetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSSetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->DSSetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.pDomainShader, args.ppClassInstances, args.NumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSSetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_DSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_DSSetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_DSSetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    uint64_t _cnt_ppSamplers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppSamplers) {
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppSamplers);
        if (!args->ppSamplers) return;
        for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppSamplers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppSamplers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppSamplers && _cnt_ppSamplers < (uint64_t)(args->NumSamplers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSSetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSSetSamplers *args)
{
    if (args->ppSamplers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSamplers); _i++)
            args->ppSamplers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_DSSetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSSetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSSetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSSetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSSetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSSetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSSetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSSetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSSetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->DSSetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSSetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_DSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_DSSetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_DSSetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSSetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSSetConstantBuffers *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_DSSetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_DSSetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSSetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSSetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSSetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSSetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSSetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSSetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSSetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSSetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSSetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSSetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->DSSetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSSetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSSetShaderResources
 */

struct npt_command_ID3D11DeviceContext_CSSetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_CSSetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_CSSetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_ppShaderResourceViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppShaderResourceViews) {
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppShaderResourceViews);
        if (!args->ppShaderResourceViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppShaderResourceViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppShaderResourceViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppShaderResourceViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppShaderResourceViews && _cnt_ppShaderResourceViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSSetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSSetShaderResources *args)
{
    if (args->ppShaderResourceViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumViews); _i++)
            args->ppShaderResourceViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_CSSetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSSetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSSetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSSetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSSetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSSetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSSetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSSetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSSetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->CSSetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSSetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSSetUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_CSSetUnorderedAccessViews {
    void *_self;
    UINT StartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    const UINT * pUAVInitialCounts;
};

static inline void
npt_decode_ID3D11DeviceContext_CSSetUnorderedAccessViews_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D11DeviceContext_CSSetUnorderedAccessViews *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumUAVs);
    uint64_t _cnt_ppUnorderedAccessViews = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppUnorderedAccessViews) {
        args->ppUnorderedAccessViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppUnorderedAccessViews);
        if (!args->ppUnorderedAccessViews) return;
        for (uint64_t _i = 0; _i < _cnt_ppUnorderedAccessViews; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppUnorderedAccessViews[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppUnorderedAccessViews = NULL;
    }
    uint64_t _cnt_pUAVInitialCounts = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pUAVInitialCounts = npt_decode_array_count_unchecked(dec);
        args->pUAVInitialCounts = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pUAVInitialCounts);
        if (!args->pUAVInitialCounts) return;
        npt_decode_UINT_array(dec, (UINT *)args->pUAVInitialCounts, _cnt_pUAVInitialCounts);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pUAVInitialCounts = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppUnorderedAccessViews && _cnt_ppUnorderedAccessViews < (uint64_t)(args->NumUAVs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pUAVInitialCounts && _cnt_pUAVInitialCounts < (uint64_t)(args->NumUAVs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11DeviceContext_CSSetUnorderedAccessViews_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSSetUnorderedAccessViews *args)
{
    if (args->ppUnorderedAccessViews) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumUAVs); _i++)
            args->ppUnorderedAccessViews[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppUnorderedAccessViews[_i], NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetUnorderedAccessViews_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D11DeviceContext_CSSetUnorderedAccessViews *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetUnorderedAccessViews),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSSetUnorderedAccessViews(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSSetUnorderedAccessViews args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSSetUnorderedAccessViews_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetUnorderedAccessViews on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSSetUnorderedAccessViews_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetUnorderedAccessViews: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSSetUnorderedAccessViews _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSSetUnorderedAccessViews, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSSetUnorderedAccessViews);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSSetUnorderedAccessViews) {
        ctx->id3d11devicecontext_dispatch_overrides->CSSetUnorderedAccessViews(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumUAVs, args.ppUnorderedAccessViews, args.pUAVInitialCounts);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSSetUnorderedAccessViews_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSSetShader
 */

struct npt_command_ID3D11DeviceContext_CSSetShader {
    void *_self;
    ID3D11ComputeShader * pComputeShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT NumClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_CSSetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_CSSetShader *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pComputeShader = (ID3D11ComputeShader *)npt_object_from_id(_id);
    }
    uint64_t _cnt_ppClassInstances = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppClassInstances) {
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppClassInstances);
        if (!args->ppClassInstances) return;
        for (uint64_t _i = 0; _i < _cnt_ppClassInstances; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppClassInstances[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppClassInstances = NULL;
    }
    npt_decode_UINT(dec, &args->NumClassInstances);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppClassInstances && _cnt_ppClassInstances < (uint64_t)(args->NumClassInstances)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSSetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSSetShader *args)
{
    args->pComputeShader = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pComputeShader, NPT_OBJECT_TYPE_ID3D11COMPUTESHADER);
    if (args->ppClassInstances) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumClassInstances); _i++)
            args->ppClassInstances[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_CSSetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSSetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSSetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSSetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSSetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetShader: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSSetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSSetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSSetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSSetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->CSSetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.pComputeShader, args.ppClassInstances, args.NumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSSetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSSetSamplers
 */

struct npt_command_ID3D11DeviceContext_CSSetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_CSSetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_CSSetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    uint64_t _cnt_ppSamplers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppSamplers) {
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppSamplers);
        if (!args->ppSamplers) return;
        for (uint64_t _i = 0; _i < _cnt_ppSamplers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppSamplers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppSamplers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppSamplers && _cnt_ppSamplers < (uint64_t)(args->NumSamplers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSSetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSSetSamplers *args)
{
    if (args->ppSamplers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSamplers); _i++)
            args->ppSamplers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_CSSetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSSetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSSetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSSetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSSetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSSetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSSetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSSetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSSetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->CSSetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSSetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSSetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_CSSetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_CSSetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_CSSetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSSetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSSetConstantBuffers *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext_CSSetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_CSSetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSSetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSSetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSSetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSSetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSSetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSSetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSSetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSSetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSSetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSSetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->CSSetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSSetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::VSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_VSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_VSGetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_VSGetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSGetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSGetConstantBuffers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_VSGetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSGetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSGetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSGetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSGetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSGetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSGetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSGetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSGetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->VSGetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSGetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_PSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_PSGetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_PSGetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppShaderResourceViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppShaderResourceViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppShaderResourceViews || !args->_guest_ids_ppShaderResourceViews)) return;
        if (_cnt) memset(args->ppShaderResourceViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppShaderResourceViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSGetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSGetShaderResources *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_PSGetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSGetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSGetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSGetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSGetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSGetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSGetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSGetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSGetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->PSGetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppShaderResourceViews && args._guest_ids_ppShaderResourceViews) {
        const uint32_t _n = args._guest_id_count_ppShaderResourceViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppShaderResourceViews[_i], args.ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSGetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSGetShader
 */

struct npt_command_ID3D11DeviceContext_PSGetShader {
    void *_self;
    ID3D11PixelShader ** ppPixelShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppPixelShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPixelShader -> *ppPixelShader} in the
     * context object table. */
    uint64_t _guest_id_ppPixelShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_PSGetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_PSGetShader *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPixelShader = _gid;
        args->ppPixelShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPixelShader) return;
        *args->ppPixelShader = NULL;
    }
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppClassInstances = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppClassInstances = (uint32_t)_cnt;
        if (_cnt && (!args->ppClassInstances || !args->_guest_ids_ppClassInstances)) return;
        if (_cnt) memset(args->ppClassInstances, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppClassInstances[_i] = _gid;
        }
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pNumClassInstances = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumClassInstances) return;
        npt_decode_UINT(dec, (UINT *)args->pNumClassInstances);
    } else {
        args->pNumClassInstances = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSGetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSGetShader *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_PSGetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPixelShader (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pNumClassInstances))
        npt_encode_UINT(enc, args->pNumClassInstances);
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSGetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSGetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSGetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPixelShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSGetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetShader: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPixelShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSGetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSGetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSGetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSGetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->PSGetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.ppPixelShader, args.ppClassInstances, args.pNumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPixelShader && *args.ppPixelShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPixelShader, *args.ppPixelShader, NPT_OBJECT_TYPE_ID3D11PIXELSHADER);
    else if (args.ppPixelShader)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPixelShader);
    if (args.ppClassInstances && args._guest_ids_ppClassInstances) {
        const uint32_t _n = args._guest_id_count_ppClassInstances;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppClassInstances[_i], args.ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSGetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_PSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_PSGetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_PSGetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSamplers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSamplers = (uint32_t)_cnt;
        if (_cnt && (!args->ppSamplers || !args->_guest_ids_ppSamplers)) return;
        if (_cnt) memset(args->ppSamplers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSamplers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSGetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSGetSamplers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_PSGetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSamplers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSGetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSGetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSGetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSGetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSGetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSGetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSGetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSGetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->PSGetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSamplers && args._guest_ids_ppSamplers) {
        const uint32_t _n = args._guest_id_count_ppSamplers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSamplers[_i], args.ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSGetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::VSGetShader
 */

struct npt_command_ID3D11DeviceContext_VSGetShader {
    void *_self;
    ID3D11VertexShader ** ppVertexShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppVertexShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppVertexShader -> *ppVertexShader} in the
     * context object table. */
    uint64_t _guest_id_ppVertexShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_VSGetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_VSGetShader *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppVertexShader = _gid;
        args->ppVertexShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppVertexShader) return;
        *args->ppVertexShader = NULL;
    }
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppClassInstances = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppClassInstances = (uint32_t)_cnt;
        if (_cnt && (!args->ppClassInstances || !args->_guest_ids_ppClassInstances)) return;
        if (_cnt) memset(args->ppClassInstances, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppClassInstances[_i] = _gid;
        }
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pNumClassInstances = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumClassInstances) return;
        npt_decode_UINT(dec, (UINT *)args->pNumClassInstances);
    } else {
        args->pNumClassInstances = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSGetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSGetShader *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_VSGetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppVertexShader (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pNumClassInstances))
        npt_encode_UINT(enc, args->pNumClassInstances);
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSGetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSGetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSGetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVertexShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSGetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetShader: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVertexShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSGetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSGetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSGetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSGetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->VSGetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.ppVertexShader, args.ppClassInstances, args.pNumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppVertexShader && *args.ppVertexShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppVertexShader, *args.ppVertexShader, NPT_OBJECT_TYPE_ID3D11VERTEXSHADER);
    else if (args.ppVertexShader)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppVertexShader);
    if (args.ppClassInstances && args._guest_ids_ppClassInstances) {
        const uint32_t _n = args._guest_id_count_ppClassInstances;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppClassInstances[_i], args.ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSGetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::PSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_PSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_PSGetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_PSGetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_PSGetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_PSGetConstantBuffers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_PSGetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_PSGetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_PSGetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_PSGetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_PSGetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_PSGetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_PSGetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_PSGetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_PSGetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_PSGetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_PSGetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->PSGetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->PSGetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_PSGetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IAGetInputLayout
 */

struct npt_command_ID3D11DeviceContext_IAGetInputLayout {
    void *_self;
    ID3D11InputLayout ** ppInputLayout;
    /* Shadow: the guest-allocated id for ppInputLayout arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppInputLayout -> *ppInputLayout} in the
     * context object table. */
    uint64_t _guest_id_ppInputLayout;
};

static inline void
npt_decode_ID3D11DeviceContext_IAGetInputLayout_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11DeviceContext_IAGetInputLayout *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppInputLayout = _gid;
        args->ppInputLayout = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppInputLayout) return;
        *args->ppInputLayout = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_IAGetInputLayout_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IAGetInputLayout *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetInputLayout_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11DeviceContext_IAGetInputLayout *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetInputLayout),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppInputLayout (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_IAGetInputLayout(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IAGetInputLayout args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IAGetInputLayout_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetInputLayout on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInputLayout);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IAGetInputLayout_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetInputLayout: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInputLayout);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IAGetInputLayout _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IAGetInputLayout, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IAGetInputLayout);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IAGetInputLayout) {
        ctx->id3d11devicecontext_dispatch_overrides->IAGetInputLayout(ctx, &args, _original);
    } else {
        _original(args._self, args.ppInputLayout);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppInputLayout && *args.ppInputLayout)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppInputLayout, *args.ppInputLayout, NPT_OBJECT_TYPE_ID3D11INPUTLAYOUT);
    else if (args.ppInputLayout)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppInputLayout);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IAGetInputLayout_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IAGetVertexBuffers
 */

struct npt_command_ID3D11DeviceContext_IAGetVertexBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppVertexBuffers;
    UINT * pStrides;
    UINT * pOffsets;
    /* Shadow: per-element guest-allocated ids for ppVertexBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppVertexBuffers;
    uint32_t _guest_id_count_ppVertexBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_IAGetVertexBuffers_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11DeviceContext_IAGetVertexBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppVertexBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppVertexBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppVertexBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppVertexBuffers || !args->_guest_ids_ppVertexBuffers)) return;
        if (_cnt) memset(args->ppVertexBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppVertexBuffers[_i] = _gid;
        }
    }


    /* Allocate temp storage for output-only parameters */



    if (args->NumBuffers) {
        args->pStrides = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pStrides) return;
        memset(args->pStrides, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pStrides = NULL;
    }
    if (args->NumBuffers) {
        args->pOffsets = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pOffsets) return;
        memset(args->pOffsets, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pOffsets = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext_IAGetVertexBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IAGetVertexBuffers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetVertexBuffers_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11DeviceContext_IAGetVertexBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetVertexBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppVertexBuffers (guest-allocated id, registered host-side; not in reply) */
    if (args->pStrides) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pStrides, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pOffsets) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pOffsets, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext_IAGetVertexBuffers(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IAGetVertexBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IAGetVertexBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetVertexBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IAGetVertexBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetVertexBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IAGetVertexBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IAGetVertexBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IAGetVertexBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IAGetVertexBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->IAGetVertexBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppVertexBuffers, args.pStrides, args.pOffsets);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppVertexBuffers && args._guest_ids_ppVertexBuffers) {
        const uint32_t _n = args._guest_id_count_ppVertexBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppVertexBuffers[_i], args.ppVertexBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IAGetVertexBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IAGetIndexBuffer
 */

struct npt_command_ID3D11DeviceContext_IAGetIndexBuffer {
    void *_self;
    ID3D11Buffer ** pIndexBuffer;
    DXGI_FORMAT * Format;
    UINT * Offset;
    /* Shadow: the guest-allocated id for pIndexBuffer arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_pIndexBuffer -> *pIndexBuffer} in the
     * context object table. */
    uint64_t _guest_id_pIndexBuffer;
};

static inline void
npt_decode_ID3D11DeviceContext_IAGetIndexBuffer_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11DeviceContext_IAGetIndexBuffer *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_pIndexBuffer = _gid;
        args->pIndexBuffer = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->pIndexBuffer) return;
        *args->pIndexBuffer = NULL;
    }


    /* Allocate temp storage for output-only parameters */

    args->Format = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_FORMAT));
    if (!args->Format) return;
    memset(args->Format, 0, sizeof(DXGI_FORMAT));
    args->Offset = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->Offset) return;
    memset(args->Offset, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11DeviceContext_IAGetIndexBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IAGetIndexBuffer *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetIndexBuffer_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11DeviceContext_IAGetIndexBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetIndexBuffer),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->pIndexBuffer (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->Format))
        npt_encode_DXGI_FORMAT(enc, args->Format);
    if (npt_encode_simple_pointer(enc, args->Offset))
        npt_encode_UINT(enc, args->Offset);
}

static inline void
npt_dispatch_ID3D11DeviceContext_IAGetIndexBuffer(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IAGetIndexBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IAGetIndexBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetIndexBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_pIndexBuffer);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IAGetIndexBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetIndexBuffer: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_pIndexBuffer);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IAGetIndexBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IAGetIndexBuffer, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IAGetIndexBuffer);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IAGetIndexBuffer) {
        ctx->id3d11devicecontext_dispatch_overrides->IAGetIndexBuffer(ctx, &args, _original);
    } else {
        _original(args._self, args.pIndexBuffer, args.Format, args.Offset);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.pIndexBuffer && *args.pIndexBuffer)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_pIndexBuffer, *args.pIndexBuffer, NPT_OBJECT_TYPE_ID3D11BUFFER);
    else if (args.pIndexBuffer)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_pIndexBuffer);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IAGetIndexBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_GSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_GSGetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_GSGetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSGetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSGetConstantBuffers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_GSGetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSGetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSGetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSGetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSGetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSGetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSGetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSGetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSGetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->GSGetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSGetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSGetShader
 */

struct npt_command_ID3D11DeviceContext_GSGetShader {
    void *_self;
    ID3D11GeometryShader ** ppGeometryShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppGeometryShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppGeometryShader -> *ppGeometryShader} in the
     * context object table. */
    uint64_t _guest_id_ppGeometryShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_GSGetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_GSGetShader *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppGeometryShader = _gid;
        args->ppGeometryShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppGeometryShader) return;
        *args->ppGeometryShader = NULL;
    }
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppClassInstances = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppClassInstances = (uint32_t)_cnt;
        if (_cnt && (!args->ppClassInstances || !args->_guest_ids_ppClassInstances)) return;
        if (_cnt) memset(args->ppClassInstances, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppClassInstances[_i] = _gid;
        }
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pNumClassInstances = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumClassInstances) return;
        npt_decode_UINT(dec, (UINT *)args->pNumClassInstances);
    } else {
        args->pNumClassInstances = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSGetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSGetShader *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_GSGetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppGeometryShader (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pNumClassInstances))
        npt_encode_UINT(enc, args->pNumClassInstances);
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSGetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSGetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSGetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppGeometryShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSGetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetShader: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppGeometryShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSGetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSGetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSGetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSGetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->GSGetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.ppGeometryShader, args.ppClassInstances, args.pNumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppGeometryShader && *args.ppGeometryShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppGeometryShader, *args.ppGeometryShader, NPT_OBJECT_TYPE_ID3D11GEOMETRYSHADER);
    else if (args.ppGeometryShader)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppGeometryShader);
    if (args.ppClassInstances && args._guest_ids_ppClassInstances) {
        const uint32_t _n = args._guest_id_count_ppClassInstances;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppClassInstances[_i], args.ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSGetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::IAGetPrimitiveTopology
 */

struct npt_command_ID3D11DeviceContext_IAGetPrimitiveTopology {
    void *_self;
    D3D11_PRIMITIVE_TOPOLOGY * pTopology;
};

static inline void
npt_decode_ID3D11DeviceContext_IAGetPrimitiveTopology_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext_IAGetPrimitiveTopology *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pTopology = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_PRIMITIVE_TOPOLOGY));
    if (!args->pTopology) return;
    memset(args->pTopology, 0, sizeof(D3D11_PRIMITIVE_TOPOLOGY));
}

static inline void
npt_replace_ID3D11DeviceContext_IAGetPrimitiveTopology_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_IAGetPrimitiveTopology *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_IAGetPrimitiveTopology_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext_IAGetPrimitiveTopology *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_IAGetPrimitiveTopology),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pTopology))
        npt_encode_D3D11_PRIMITIVE_TOPOLOGY(enc, args->pTopology);
}

static inline void
npt_dispatch_ID3D11DeviceContext_IAGetPrimitiveTopology(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_IAGetPrimitiveTopology args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_IAGetPrimitiveTopology_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetPrimitiveTopology on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_IAGetPrimitiveTopology_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_IAGetPrimitiveTopology: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_IAGetPrimitiveTopology _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_IAGetPrimitiveTopology, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_IAGetPrimitiveTopology);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->IAGetPrimitiveTopology) {
        ctx->id3d11devicecontext_dispatch_overrides->IAGetPrimitiveTopology(ctx, &args, _original);
    } else {
        _original(args._self, args.pTopology);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_IAGetPrimitiveTopology_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::VSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_VSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_VSGetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_VSGetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppShaderResourceViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppShaderResourceViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppShaderResourceViews || !args->_guest_ids_ppShaderResourceViews)) return;
        if (_cnt) memset(args->ppShaderResourceViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppShaderResourceViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSGetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSGetShaderResources *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_VSGetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSGetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSGetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSGetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSGetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSGetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSGetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSGetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSGetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->VSGetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppShaderResourceViews && args._guest_ids_ppShaderResourceViews) {
        const uint32_t _n = args._guest_id_count_ppShaderResourceViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppShaderResourceViews[_i], args.ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSGetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::VSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_VSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_VSGetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_VSGetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSamplers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSamplers = (uint32_t)_cnt;
        if (_cnt && (!args->ppSamplers || !args->_guest_ids_ppSamplers)) return;
        if (_cnt) memset(args->ppSamplers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSamplers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_VSGetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_VSGetSamplers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_VSGetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_VSGetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_VSGetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSamplers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_VSGetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_VSGetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_VSGetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_VSGetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_VSGetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_VSGetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_VSGetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_VSGetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->VSGetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->VSGetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSamplers && args._guest_ids_ppSamplers) {
        const uint32_t _n = args._guest_id_count_ppSamplers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSamplers[_i], args.ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_VSGetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GetPredication
 */

struct npt_command_ID3D11DeviceContext_GetPredication {
    void *_self;
    ID3D11Predicate ** ppPredicate;
    BOOL * pPredicateValue;
    /* Shadow: the guest-allocated id for ppPredicate arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPredicate -> *ppPredicate} in the
     * context object table. */
    uint64_t _guest_id_ppPredicate;
};

static inline void
npt_decode_ID3D11DeviceContext_GetPredication_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11DeviceContext_GetPredication *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPredicate = _gid;
        args->ppPredicate = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPredicate) return;
        *args->ppPredicate = NULL;
    }

    /* Allocate temp storage for output-only parameters */

    args->pPredicateValue = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pPredicateValue) return;
    memset(args->pPredicateValue, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11DeviceContext_GetPredication_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GetPredication *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_GetPredication_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11DeviceContext_GetPredication *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetPredication),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPredicate (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pPredicateValue))
        npt_encode_BOOL(enc, args->pPredicateValue);
}

static inline void
npt_dispatch_ID3D11DeviceContext_GetPredication(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GetPredication args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GetPredication_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetPredication on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPredicate);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GetPredication_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetPredication: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPredicate);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GetPredication _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GetPredication, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GetPredication);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GetPredication) {
        ctx->id3d11devicecontext_dispatch_overrides->GetPredication(ctx, &args, _original);
    } else {
        _original(args._self, args.ppPredicate, args.pPredicateValue);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPredicate && *args.ppPredicate)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPredicate, *args.ppPredicate, NPT_OBJECT_TYPE_ID3D11PREDICATE);
    else if (args.ppPredicate)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPredicate);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GetPredication_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_GSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_GSGetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_GSGetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppShaderResourceViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppShaderResourceViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppShaderResourceViews || !args->_guest_ids_ppShaderResourceViews)) return;
        if (_cnt) memset(args->ppShaderResourceViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppShaderResourceViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSGetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSGetShaderResources *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_GSGetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSGetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSGetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSGetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSGetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSGetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSGetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSGetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSGetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->GSGetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppShaderResourceViews && args._guest_ids_ppShaderResourceViews) {
        const uint32_t _n = args._guest_id_count_ppShaderResourceViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppShaderResourceViews[_i], args.ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSGetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_GSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_GSGetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_GSGetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSamplers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSamplers = (uint32_t)_cnt;
        if (_cnt && (!args->ppSamplers || !args->_guest_ids_ppSamplers)) return;
        if (_cnt) memset(args->ppSamplers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSamplers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_GSGetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GSGetSamplers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_GSGetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_GSGetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GSGetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSamplers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_GSGetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GSGetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GSGetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GSGetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GSGetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GSGetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GSGetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GSGetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GSGetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->GSGetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSamplers && args._guest_ids_ppSamplers) {
        const uint32_t _n = args._guest_id_count_ppSamplers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSamplers[_i], args.ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GSGetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMGetRenderTargets
 */

struct npt_command_ID3D11DeviceContext_OMGetRenderTargets {
    void *_self;
    UINT NumViews;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView ** ppDepthStencilView;
    /* Shadow: per-element guest-allocated ids for ppRenderTargetViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppRenderTargetViews;
    uint32_t _guest_id_count_ppRenderTargetViews;
    /* Shadow: the guest-allocated id for ppDepthStencilView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilView -> *ppDepthStencilView} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilView;
};

static inline void
npt_decode_ID3D11DeviceContext_OMGetRenderTargets_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11DeviceContext_OMGetRenderTargets *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumViews);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppRenderTargetViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppRenderTargetViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppRenderTargetViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppRenderTargetViews || !args->_guest_ids_ppRenderTargetViews)) return;
        if (_cnt) memset(args->ppRenderTargetViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppRenderTargetViews[_i] = _gid;
        }
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDepthStencilView = _gid;
        args->ppDepthStencilView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDepthStencilView) return;
        *args->ppDepthStencilView = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_OMGetRenderTargets_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMGetRenderTargets *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetRenderTargets_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11DeviceContext_OMGetRenderTargets *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargets),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRenderTargetViews (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppDepthStencilView (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMGetRenderTargets(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMGetRenderTargets args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMGetRenderTargets_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetRenderTargets on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMGetRenderTargets_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetRenderTargets: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMGetRenderTargets _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMGetRenderTargets, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMGetRenderTargets);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMGetRenderTargets) {
        ctx->id3d11devicecontext_dispatch_overrides->OMGetRenderTargets(ctx, &args, _original);
    } else {
        _original(args._self, args.NumViews, args.ppRenderTargetViews, args.ppDepthStencilView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRenderTargetViews && args._guest_ids_ppRenderTargetViews) {
        const uint32_t _n = args._guest_id_count_ppRenderTargetViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppRenderTargetViews[_i], args.ppRenderTargetViews[_i], NPT_OBJECT_TYPE_ID3D11RENDERTARGETVIEW);
    }
    if (args.ppDepthStencilView && *args.ppDepthStencilView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDepthStencilView, *args.ppDepthStencilView, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILVIEW);
    else if (args.ppDepthStencilView)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilView);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMGetRenderTargets_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMGetRenderTargetsAndUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews {
    void *_self;
    UINT NumRTVs;
    ID3D11RenderTargetView ** ppRenderTargetViews;
    ID3D11DepthStencilView ** ppDepthStencilView;
    UINT UAVStartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    /* Shadow: per-element guest-allocated ids for ppRenderTargetViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppRenderTargetViews;
    uint32_t _guest_id_count_ppRenderTargetViews;
    /* Shadow: the guest-allocated id for ppDepthStencilView arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilView -> *ppDepthStencilView} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilView;
    /* Shadow: per-element guest-allocated ids for ppUnorderedAccessViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppUnorderedAccessViews;
    uint32_t _guest_id_count_ppUnorderedAccessViews;
};

static inline void
npt_decode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_args_temp(struct npt_cs_decoder *dec,
                                                                                   struct npt_command_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumRTVs);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppRenderTargetViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppRenderTargetViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppRenderTargetViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppRenderTargetViews || !args->_guest_ids_ppRenderTargetViews)) return;
        if (_cnt) memset(args->ppRenderTargetViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppRenderTargetViews[_i] = _gid;
        }
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDepthStencilView = _gid;
        args->ppDepthStencilView = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDepthStencilView) return;
        *args->ppDepthStencilView = NULL;
    }
    npt_decode_UINT(dec, &args->UAVStartSlot);
    npt_decode_UINT(dec, &args->NumUAVs);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppUnorderedAccessViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppUnorderedAccessViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppUnorderedAccessViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppUnorderedAccessViews || !args->_guest_ids_ppUnorderedAccessViews)) return;
        if (_cnt) memset(args->ppUnorderedAccessViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppUnorderedAccessViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_reply(struct npt_cs_encoder *enc,
                                                                               const struct npt_command_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRenderTargetViews (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppDepthStencilView (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppUnorderedAccessViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews(struct npt_dispatch_context *ctx,
                                                                           uint32_t cmd_flags,
                                                                           uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilView);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMGetRenderTargetsAndUnorderedAccessViews) {
        ctx->id3d11devicecontext_dispatch_overrides->OMGetRenderTargetsAndUnorderedAccessViews(ctx, &args, _original);
    } else {
        _original(args._self, args.NumRTVs, args.ppRenderTargetViews, args.ppDepthStencilView, args.UAVStartSlot, args.NumUAVs, args.ppUnorderedAccessViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRenderTargetViews && args._guest_ids_ppRenderTargetViews) {
        const uint32_t _n = args._guest_id_count_ppRenderTargetViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppRenderTargetViews[_i], args.ppRenderTargetViews[_i], NPT_OBJECT_TYPE_ID3D11RENDERTARGETVIEW);
    }
    if (args.ppDepthStencilView && *args.ppDepthStencilView)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDepthStencilView, *args.ppDepthStencilView, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILVIEW);
    else if (args.ppDepthStencilView)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilView);
    if (args.ppUnorderedAccessViews && args._guest_ids_ppUnorderedAccessViews) {
        const uint32_t _n = args._guest_id_count_ppUnorderedAccessViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppUnorderedAccessViews[_i], args.ppUnorderedAccessViews[_i], NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMGetRenderTargetsAndUnorderedAccessViews_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMGetBlendState
 */

struct npt_command_ID3D11DeviceContext_OMGetBlendState {
    void *_self;
    ID3D11BlendState ** ppBlendState;
    FLOAT * BlendFactor;
    UINT * pSampleMask;
    /* Shadow: the guest-allocated id for ppBlendState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppBlendState -> *ppBlendState} in the
     * context object table. */
    uint64_t _guest_id_ppBlendState;
};

static inline void
npt_decode_ID3D11DeviceContext_OMGetBlendState_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11DeviceContext_OMGetBlendState *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppBlendState = _gid;
        args->ppBlendState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppBlendState) return;
        *args->ppBlendState = NULL;
    }


    /* Allocate temp storage for output-only parameters */

    args->BlendFactor = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), 4);
    if (!args->BlendFactor) return;
    memset(args->BlendFactor, 0, sizeof(FLOAT) * (4));
    args->pSampleMask = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pSampleMask) return;
    memset(args->pSampleMask, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11DeviceContext_OMGetBlendState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMGetBlendState *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetBlendState_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11DeviceContext_OMGetBlendState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetBlendState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppBlendState (guest-allocated id, registered host-side; not in reply) */
    if (args->BlendFactor) {
        npt_encode_array_count(enc, 4);
        npt_encode_FLOAT_array(enc, (const FLOAT *)args->BlendFactor, 4);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pSampleMask))
        npt_encode_UINT(enc, args->pSampleMask);
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMGetBlendState(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMGetBlendState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMGetBlendState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetBlendState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppBlendState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMGetBlendState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetBlendState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppBlendState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMGetBlendState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMGetBlendState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMGetBlendState);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMGetBlendState) {
        ctx->id3d11devicecontext_dispatch_overrides->OMGetBlendState(ctx, &args, _original);
    } else {
        _original(args._self, args.ppBlendState, args.BlendFactor, args.pSampleMask);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppBlendState && *args.ppBlendState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppBlendState, *args.ppBlendState, NPT_OBJECT_TYPE_ID3D11BLENDSTATE);
    else if (args.ppBlendState)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppBlendState);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMGetBlendState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::OMGetDepthStencilState
 */

struct npt_command_ID3D11DeviceContext_OMGetDepthStencilState {
    void *_self;
    ID3D11DepthStencilState ** ppDepthStencilState;
    UINT * pStencilRef;
    /* Shadow: the guest-allocated id for ppDepthStencilState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDepthStencilState -> *ppDepthStencilState} in the
     * context object table. */
    uint64_t _guest_id_ppDepthStencilState;
};

static inline void
npt_decode_ID3D11DeviceContext_OMGetDepthStencilState_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext_OMGetDepthStencilState *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDepthStencilState = _gid;
        args->ppDepthStencilState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDepthStencilState) return;
        *args->ppDepthStencilState = NULL;
    }

    /* Allocate temp storage for output-only parameters */

    args->pStencilRef = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pStencilRef) return;
    memset(args->pStencilRef, 0, sizeof(UINT));
}

static inline void
npt_replace_ID3D11DeviceContext_OMGetDepthStencilState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_OMGetDepthStencilState *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_OMGetDepthStencilState_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext_OMGetDepthStencilState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_OMGetDepthStencilState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDepthStencilState (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pStencilRef))
        npt_encode_UINT(enc, args->pStencilRef);
}

static inline void
npt_dispatch_ID3D11DeviceContext_OMGetDepthStencilState(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_OMGetDepthStencilState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_OMGetDepthStencilState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetDepthStencilState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_OMGetDepthStencilState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_OMGetDepthStencilState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_OMGetDepthStencilState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_OMGetDepthStencilState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_OMGetDepthStencilState);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->OMGetDepthStencilState) {
        ctx->id3d11devicecontext_dispatch_overrides->OMGetDepthStencilState(ctx, &args, _original);
    } else {
        _original(args._self, args.ppDepthStencilState, args.pStencilRef);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDepthStencilState && *args.ppDepthStencilState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDepthStencilState, *args.ppDepthStencilState, NPT_OBJECT_TYPE_ID3D11DEPTHSTENCILSTATE);
    else if (args.ppDepthStencilState)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDepthStencilState);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_OMGetDepthStencilState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::SOGetTargets
 */

struct npt_command_ID3D11DeviceContext_SOGetTargets {
    void *_self;
    UINT NumBuffers;
    ID3D11Buffer ** ppSOTargets;
    /* Shadow: per-element guest-allocated ids for ppSOTargets.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSOTargets;
    uint32_t _guest_id_count_ppSOTargets;
};

static inline void
npt_decode_ID3D11DeviceContext_SOGetTargets_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11DeviceContext_SOGetTargets *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSOTargets = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSOTargets = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSOTargets = (uint32_t)_cnt;
        if (_cnt && (!args->ppSOTargets || !args->_guest_ids_ppSOTargets)) return;
        if (_cnt) memset(args->ppSOTargets, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSOTargets[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_SOGetTargets_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_SOGetTargets *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_SOGetTargets_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11DeviceContext_SOGetTargets *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_SOGetTargets),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSOTargets (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_SOGetTargets(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_SOGetTargets args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_SOGetTargets_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SOGetTargets on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_SOGetTargets_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_SOGetTargets: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_SOGetTargets _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_SOGetTargets, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_SOGetTargets);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->SOGetTargets) {
        ctx->id3d11devicecontext_dispatch_overrides->SOGetTargets(ctx, &args, _original);
    } else {
        _original(args._self, args.NumBuffers, args.ppSOTargets);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSOTargets && args._guest_ids_ppSOTargets) {
        const uint32_t _n = args._guest_id_count_ppSOTargets;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSOTargets[_i], args.ppSOTargets[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_SOGetTargets_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::RSGetState
 */

struct npt_command_ID3D11DeviceContext_RSGetState {
    void *_self;
    ID3D11RasterizerState ** ppRasterizerState;
    /* Shadow: the guest-allocated id for ppRasterizerState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRasterizerState -> *ppRasterizerState} in the
     * context object table. */
    uint64_t _guest_id_ppRasterizerState;
};

static inline void
npt_decode_ID3D11DeviceContext_RSGetState_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11DeviceContext_RSGetState *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppRasterizerState = _gid;
        args->ppRasterizerState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppRasterizerState) return;
        *args->ppRasterizerState = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext_RSGetState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_RSGetState *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_RSGetState_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11DeviceContext_RSGetState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSGetState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRasterizerState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_RSGetState(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_RSGetState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_RSGetState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSGetState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppRasterizerState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_RSGetState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSGetState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppRasterizerState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_RSGetState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_RSGetState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_RSGetState);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->RSGetState) {
        ctx->id3d11devicecontext_dispatch_overrides->RSGetState(ctx, &args, _original);
    } else {
        _original(args._self, args.ppRasterizerState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRasterizerState && *args.ppRasterizerState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppRasterizerState, *args.ppRasterizerState, NPT_OBJECT_TYPE_ID3D11RASTERIZERSTATE);
    else if (args.ppRasterizerState)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppRasterizerState);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_RSGetState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::RSGetViewports
 */

struct npt_command_ID3D11DeviceContext_RSGetViewports {
    void *_self;
    UINT * pNumViewports;
    D3D11_VIEWPORT * pViewports;
};

static inline void
npt_decode_ID3D11DeviceContext_RSGetViewports_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11DeviceContext_RSGetViewports *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pNumViewports = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumViewports) return;
        npt_decode_UINT(dec, (UINT *)args->pNumViewports);
    } else {
        args->pNumViewports = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */

    if (*args->pNumViewports) {
        args->pViewports = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_VIEWPORT), *args->pNumViewports);
        if (!args->pViewports) return;
        memset(args->pViewports, 0, sizeof(D3D11_VIEWPORT) * *args->pNumViewports);
    } else {
        args->pViewports = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext_RSGetViewports_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_RSGetViewports *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_RSGetViewports_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11DeviceContext_RSGetViewports *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSGetViewports),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumViewports))
        npt_encode_UINT(enc, args->pNumViewports);
    if (args->pViewports) {
        npt_encode_array_count(enc, *args->pNumViewports);
        for (uint32_t _i = 0; _i < (uint32_t)(*args->pNumViewports); _i++)
            npt_encode_D3D11_VIEWPORT(enc, &args->pViewports[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext_RSGetViewports(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_RSGetViewports args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_RSGetViewports_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSGetViewports on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_RSGetViewports_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSGetViewports: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_RSGetViewports _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_RSGetViewports, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_RSGetViewports);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->RSGetViewports) {
        ctx->id3d11devicecontext_dispatch_overrides->RSGetViewports(ctx, &args, _original);
    } else {
        _original(args._self, args.pNumViewports, args.pViewports);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_RSGetViewports_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::RSGetScissorRects
 */

struct npt_command_ID3D11DeviceContext_RSGetScissorRects {
    void *_self;
    UINT * pNumRects;
    D3D11_RECT * pRects;
};

static inline void
npt_decode_ID3D11DeviceContext_RSGetScissorRects_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11DeviceContext_RSGetScissorRects *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pNumRects = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumRects) return;
        npt_decode_UINT(dec, (UINT *)args->pNumRects);
    } else {
        args->pNumRects = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */

    if (*args->pNumRects) {
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_RECT), *args->pNumRects);
        if (!args->pRects) return;
        memset(args->pRects, 0, sizeof(D3D11_RECT) * *args->pNumRects);
    } else {
        args->pRects = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext_RSGetScissorRects_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_RSGetScissorRects *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_RSGetScissorRects_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11DeviceContext_RSGetScissorRects *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_RSGetScissorRects),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pNumRects))
        npt_encode_UINT(enc, args->pNumRects);
    if (args->pRects) {
        npt_encode_array_count(enc, *args->pNumRects);
        for (uint32_t _i = 0; _i < (uint32_t)(*args->pNumRects); _i++)
            npt_encode_D3D11_RECT(enc, &args->pRects[_i]);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext_RSGetScissorRects(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_RSGetScissorRects args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_RSGetScissorRects_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSGetScissorRects on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_RSGetScissorRects_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_RSGetScissorRects: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_RSGetScissorRects _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_RSGetScissorRects, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_RSGetScissorRects);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->RSGetScissorRects) {
        ctx->id3d11devicecontext_dispatch_overrides->RSGetScissorRects(ctx, &args, _original);
    } else {
        _original(args._self, args.pNumRects, args.pRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_RSGetScissorRects_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_HSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_HSGetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_HSGetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppShaderResourceViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppShaderResourceViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppShaderResourceViews || !args->_guest_ids_ppShaderResourceViews)) return;
        if (_cnt) memset(args->ppShaderResourceViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppShaderResourceViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSGetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSGetShaderResources *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_HSGetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSGetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSGetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSGetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSGetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSGetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSGetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSGetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSGetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->HSGetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppShaderResourceViews && args._guest_ids_ppShaderResourceViews) {
        const uint32_t _n = args._guest_id_count_ppShaderResourceViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppShaderResourceViews[_i], args.ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSGetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSGetShader
 */

struct npt_command_ID3D11DeviceContext_HSGetShader {
    void *_self;
    ID3D11HullShader ** ppHullShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppHullShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppHullShader -> *ppHullShader} in the
     * context object table. */
    uint64_t _guest_id_ppHullShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_HSGetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_HSGetShader *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppHullShader = _gid;
        args->ppHullShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppHullShader) return;
        *args->ppHullShader = NULL;
    }
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppClassInstances = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppClassInstances = (uint32_t)_cnt;
        if (_cnt && (!args->ppClassInstances || !args->_guest_ids_ppClassInstances)) return;
        if (_cnt) memset(args->ppClassInstances, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppClassInstances[_i] = _gid;
        }
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pNumClassInstances = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumClassInstances) return;
        npt_decode_UINT(dec, (UINT *)args->pNumClassInstances);
    } else {
        args->pNumClassInstances = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSGetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSGetShader *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_HSGetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppHullShader (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pNumClassInstances))
        npt_encode_UINT(enc, args->pNumClassInstances);
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSGetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSGetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSGetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppHullShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSGetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetShader: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppHullShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSGetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSGetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSGetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSGetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->HSGetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.ppHullShader, args.ppClassInstances, args.pNumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppHullShader && *args.ppHullShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppHullShader, *args.ppHullShader, NPT_OBJECT_TYPE_ID3D11HULLSHADER);
    else if (args.ppHullShader)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppHullShader);
    if (args.ppClassInstances && args._guest_ids_ppClassInstances) {
        const uint32_t _n = args._guest_id_count_ppClassInstances;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppClassInstances[_i], args.ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSGetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_HSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_HSGetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_HSGetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSamplers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSamplers = (uint32_t)_cnt;
        if (_cnt && (!args->ppSamplers || !args->_guest_ids_ppSamplers)) return;
        if (_cnt) memset(args->ppSamplers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSamplers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSGetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSGetSamplers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_HSGetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSamplers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSGetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSGetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSGetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSGetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSGetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSGetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSGetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSGetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->HSGetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSamplers && args._guest_ids_ppSamplers) {
        const uint32_t _n = args._guest_id_count_ppSamplers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSamplers[_i], args.ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSGetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::HSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_HSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_HSGetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_HSGetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_HSGetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_HSGetConstantBuffers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_HSGetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_HSGetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_HSGetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_HSGetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_HSGetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_HSGetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_HSGetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_HSGetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_HSGetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_HSGetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_HSGetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->HSGetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->HSGetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_HSGetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_DSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_DSGetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_DSGetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppShaderResourceViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppShaderResourceViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppShaderResourceViews || !args->_guest_ids_ppShaderResourceViews)) return;
        if (_cnt) memset(args->ppShaderResourceViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppShaderResourceViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSGetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSGetShaderResources *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_DSGetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSGetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSGetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSGetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSGetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSGetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSGetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSGetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSGetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->DSGetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppShaderResourceViews && args._guest_ids_ppShaderResourceViews) {
        const uint32_t _n = args._guest_id_count_ppShaderResourceViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppShaderResourceViews[_i], args.ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSGetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSGetShader
 */

struct npt_command_ID3D11DeviceContext_DSGetShader {
    void *_self;
    ID3D11DomainShader ** ppDomainShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppDomainShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppDomainShader -> *ppDomainShader} in the
     * context object table. */
    uint64_t _guest_id_ppDomainShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_DSGetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_DSGetShader *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDomainShader = _gid;
        args->ppDomainShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDomainShader) return;
        *args->ppDomainShader = NULL;
    }
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppClassInstances = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppClassInstances = (uint32_t)_cnt;
        if (_cnt && (!args->ppClassInstances || !args->_guest_ids_ppClassInstances)) return;
        if (_cnt) memset(args->ppClassInstances, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppClassInstances[_i] = _gid;
        }
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pNumClassInstances = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumClassInstances) return;
        npt_decode_UINT(dec, (UINT *)args->pNumClassInstances);
    } else {
        args->pNumClassInstances = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSGetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSGetShader *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_DSGetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDomainShader (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pNumClassInstances))
        npt_encode_UINT(enc, args->pNumClassInstances);
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSGetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSGetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSGetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDomainShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSGetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetShader: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDomainShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSGetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSGetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSGetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSGetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->DSGetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.ppDomainShader, args.ppClassInstances, args.pNumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppDomainShader && *args.ppDomainShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDomainShader, *args.ppDomainShader, NPT_OBJECT_TYPE_ID3D11DOMAINSHADER);
    else if (args.ppDomainShader)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDomainShader);
    if (args.ppClassInstances && args._guest_ids_ppClassInstances) {
        const uint32_t _n = args._guest_id_count_ppClassInstances;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppClassInstances[_i], args.ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSGetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_DSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_DSGetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_DSGetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSamplers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSamplers = (uint32_t)_cnt;
        if (_cnt && (!args->ppSamplers || !args->_guest_ids_ppSamplers)) return;
        if (_cnt) memset(args->ppSamplers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSamplers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSGetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSGetSamplers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_DSGetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSamplers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSGetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSGetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSGetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSGetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSGetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSGetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSGetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSGetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->DSGetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSamplers && args._guest_ids_ppSamplers) {
        const uint32_t _n = args._guest_id_count_ppSamplers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSamplers[_i], args.ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSGetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::DSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_DSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_DSGetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_DSGetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_DSGetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_DSGetConstantBuffers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_DSGetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_DSGetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_DSGetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_DSGetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_DSGetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_DSGetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_DSGetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_DSGetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_DSGetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_DSGetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_DSGetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->DSGetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->DSGetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_DSGetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSGetShaderResources
 */

struct npt_command_ID3D11DeviceContext_CSGetShaderResources {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    ID3D11ShaderResourceView ** ppShaderResourceViews;
    /* Shadow: per-element guest-allocated ids for ppShaderResourceViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppShaderResourceViews;
    uint32_t _guest_id_count_ppShaderResourceViews;
};

static inline void
npt_decode_ID3D11DeviceContext_CSGetShaderResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_CSGetShaderResources *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppShaderResourceViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppShaderResourceViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppShaderResourceViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppShaderResourceViews || !args->_guest_ids_ppShaderResourceViews)) return;
        if (_cnt) memset(args->ppShaderResourceViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppShaderResourceViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSGetShaderResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSGetShaderResources *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetShaderResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_CSGetShaderResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetShaderResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppShaderResourceViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSGetShaderResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSGetShaderResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSGetShaderResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetShaderResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSGetShaderResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetShaderResources: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSGetShaderResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSGetShaderResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSGetShaderResources);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSGetShaderResources) {
        ctx->id3d11devicecontext_dispatch_overrides->CSGetShaderResources(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.ppShaderResourceViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppShaderResourceViews && args._guest_ids_ppShaderResourceViews) {
        const uint32_t _n = args._guest_id_count_ppShaderResourceViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppShaderResourceViews[_i], args.ppShaderResourceViews[_i], NPT_OBJECT_TYPE_ID3D11SHADERRESOURCEVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSGetShaderResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSGetUnorderedAccessViews
 */

struct npt_command_ID3D11DeviceContext_CSGetUnorderedAccessViews {
    void *_self;
    UINT StartSlot;
    UINT NumUAVs;
    ID3D11UnorderedAccessView ** ppUnorderedAccessViews;
    /* Shadow: per-element guest-allocated ids for ppUnorderedAccessViews.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppUnorderedAccessViews;
    uint32_t _guest_id_count_ppUnorderedAccessViews;
};

static inline void
npt_decode_ID3D11DeviceContext_CSGetUnorderedAccessViews_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D11DeviceContext_CSGetUnorderedAccessViews *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumUAVs);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppUnorderedAccessViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppUnorderedAccessViews = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppUnorderedAccessViews = (uint32_t)_cnt;
        if (_cnt && (!args->ppUnorderedAccessViews || !args->_guest_ids_ppUnorderedAccessViews)) return;
        if (_cnt) memset(args->ppUnorderedAccessViews, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppUnorderedAccessViews[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSGetUnorderedAccessViews_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSGetUnorderedAccessViews *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetUnorderedAccessViews_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D11DeviceContext_CSGetUnorderedAccessViews *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetUnorderedAccessViews),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppUnorderedAccessViews (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSGetUnorderedAccessViews(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSGetUnorderedAccessViews args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSGetUnorderedAccessViews_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetUnorderedAccessViews on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSGetUnorderedAccessViews_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetUnorderedAccessViews: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSGetUnorderedAccessViews _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSGetUnorderedAccessViews, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSGetUnorderedAccessViews);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSGetUnorderedAccessViews) {
        ctx->id3d11devicecontext_dispatch_overrides->CSGetUnorderedAccessViews(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumUAVs, args.ppUnorderedAccessViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppUnorderedAccessViews && args._guest_ids_ppUnorderedAccessViews) {
        const uint32_t _n = args._guest_id_count_ppUnorderedAccessViews;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppUnorderedAccessViews[_i], args.ppUnorderedAccessViews[_i], NPT_OBJECT_TYPE_ID3D11UNORDEREDACCESSVIEW);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSGetUnorderedAccessViews_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSGetShader
 */

struct npt_command_ID3D11DeviceContext_CSGetShader {
    void *_self;
    ID3D11ComputeShader ** ppComputeShader;
    ID3D11ClassInstance ** ppClassInstances;
    UINT * pNumClassInstances;
    /* Shadow: the guest-allocated id for ppComputeShader arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppComputeShader -> *ppComputeShader} in the
     * context object table. */
    uint64_t _guest_id_ppComputeShader;
    /* Shadow: per-element guest-allocated ids for ppClassInstances.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppClassInstances;
    uint32_t _guest_id_count_ppClassInstances;
};

static inline void
npt_decode_ID3D11DeviceContext_CSGetShader_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D11DeviceContext_CSGetShader *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppComputeShader = _gid;
        args->ppComputeShader = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppComputeShader) return;
        *args->ppComputeShader = NULL;
    }
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppClassInstances = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppClassInstances = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppClassInstances = (uint32_t)_cnt;
        if (_cnt && (!args->ppClassInstances || !args->_guest_ids_ppClassInstances)) return;
        if (_cnt) memset(args->ppClassInstances, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppClassInstances[_i] = _gid;
        }
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pNumClassInstances = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pNumClassInstances) return;
        npt_decode_UINT(dec, (UINT *)args->pNumClassInstances);
    } else {
        args->pNumClassInstances = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSGetShader_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSGetShader *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetShader_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D11DeviceContext_CSGetShader *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetShader),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppComputeShader (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppClassInstances (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pNumClassInstances))
        npt_encode_UINT(enc, args->pNumClassInstances);
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSGetShader(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSGetShader args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSGetShader_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetShader on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppComputeShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSGetShader_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetShader: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppComputeShader);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSGetShader _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSGetShader, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSGetShader);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSGetShader) {
        ctx->id3d11devicecontext_dispatch_overrides->CSGetShader(ctx, &args, _original);
    } else {
        _original(args._self, args.ppComputeShader, args.ppClassInstances, args.pNumClassInstances);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppComputeShader && *args.ppComputeShader)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppComputeShader, *args.ppComputeShader, NPT_OBJECT_TYPE_ID3D11COMPUTESHADER);
    else if (args.ppComputeShader)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppComputeShader);
    if (args.ppClassInstances && args._guest_ids_ppClassInstances) {
        const uint32_t _n = args._guest_id_count_ppClassInstances;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppClassInstances[_i], args.ppClassInstances[_i], NPT_OBJECT_TYPE_ID3D11CLASSINSTANCE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSGetShader_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSGetSamplers
 */

struct npt_command_ID3D11DeviceContext_CSGetSamplers {
    void *_self;
    UINT StartSlot;
    UINT NumSamplers;
    ID3D11SamplerState ** ppSamplers;
    /* Shadow: per-element guest-allocated ids for ppSamplers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppSamplers;
    uint32_t _guest_id_count_ppSamplers;
};

static inline void
npt_decode_ID3D11DeviceContext_CSGetSamplers_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext_CSGetSamplers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumSamplers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppSamplers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppSamplers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppSamplers = (uint32_t)_cnt;
        if (_cnt && (!args->ppSamplers || !args->_guest_ids_ppSamplers)) return;
        if (_cnt) memset(args->ppSamplers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppSamplers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSGetSamplers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSGetSamplers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetSamplers_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext_CSGetSamplers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetSamplers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSamplers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSGetSamplers(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSGetSamplers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSGetSamplers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetSamplers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSGetSamplers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetSamplers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSGetSamplers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSGetSamplers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSGetSamplers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSGetSamplers) {
        ctx->id3d11devicecontext_dispatch_overrides->CSGetSamplers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumSamplers, args.ppSamplers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSamplers && args._guest_ids_ppSamplers) {
        const uint32_t _n = args._guest_id_count_ppSamplers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppSamplers[_i], args.ppSamplers[_i], NPT_OBJECT_TYPE_ID3D11SAMPLERSTATE);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSGetSamplers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::CSGetConstantBuffers
 */

struct npt_command_ID3D11DeviceContext_CSGetConstantBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext_CSGetConstantBuffers_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext_CSGetConstantBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext_CSGetConstantBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_CSGetConstantBuffers *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_CSGetConstantBuffers_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext_CSGetConstantBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_CSGetConstantBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_CSGetConstantBuffers(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_CSGetConstantBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_CSGetConstantBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetConstantBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_CSGetConstantBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_CSGetConstantBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_CSGetConstantBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_CSGetConstantBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_CSGetConstantBuffers);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->CSGetConstantBuffers) {
        ctx->id3d11devicecontext_dispatch_overrides->CSGetConstantBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_CSGetConstantBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::ClearState
 */

struct npt_command_ID3D11DeviceContext_ClearState {
    void *_self;
};

static inline void
npt_decode_ID3D11DeviceContext_ClearState_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11DeviceContext_ClearState *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11DeviceContext_ClearState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_ClearState *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_ClearState_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11DeviceContext_ClearState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_ClearState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_ClearState(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_ClearState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_ClearState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_ClearState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_ClearState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_ClearState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_ClearState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_ClearState);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->ClearState) {
        ctx->id3d11devicecontext_dispatch_overrides->ClearState(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_ClearState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::Flush
 */

struct npt_command_ID3D11DeviceContext_Flush {
    void *_self;
};

static inline void
npt_decode_ID3D11DeviceContext_Flush_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D11DeviceContext_Flush *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11DeviceContext_Flush_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_Flush *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_Flush_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D11DeviceContext_Flush *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_Flush),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_Flush(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_Flush args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_Flush_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Flush on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_Flush_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_Flush: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_Flush _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_Flush, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_Flush);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->Flush) {
        ctx->id3d11devicecontext_dispatch_overrides->Flush(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_Flush_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GetType
 */

struct npt_command_ID3D11DeviceContext_GetType {
    void *_self;
    D3D11_DEVICE_CONTEXT_TYPE ret;
};

static inline void
npt_decode_ID3D11DeviceContext_GetType_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D11DeviceContext_GetType *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11DeviceContext_GetType_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GetType *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_GetType_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D11DeviceContext_GetType *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetType),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GetType(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GetType args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GetType_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetType on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GetType_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetType: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GetType _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GetType, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GetType);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GetType) {
        args.ret = ctx->id3d11devicecontext_dispatch_overrides->GetType(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GetType_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::GetContextFlags
 */

struct npt_command_ID3D11DeviceContext_GetContextFlags {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_ID3D11DeviceContext_GetContextFlags_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11DeviceContext_GetContextFlags *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11DeviceContext_GetContextFlags_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_GetContextFlags *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_GetContextFlags_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11DeviceContext_GetContextFlags *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_GetContextFlags),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext_GetContextFlags(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_GetContextFlags args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_GetContextFlags_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetContextFlags on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_GetContextFlags_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_GetContextFlags: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_GetContextFlags _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_GetContextFlags, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_GetContextFlags);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->GetContextFlags) {
        args.ret = ctx->id3d11devicecontext_dispatch_overrides->GetContextFlags(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_GetContextFlags_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext::FinishCommandList
 */

struct npt_command_ID3D11DeviceContext_FinishCommandList {
    void *_self;
    BOOL RestoreDeferredContextState;
    ID3D11CommandList ** ppCommandList;
    /* Shadow: the guest-allocated id for ppCommandList arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppCommandList -> *ppCommandList} in the
     * context object table. */
    uint64_t _guest_id_ppCommandList;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext_FinishCommandList_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11DeviceContext_FinishCommandList *args)
{
    /* Decode input parameters from the wire */
    npt_decode_BOOL(dec, &args->RestoreDeferredContextState);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppCommandList = _gid;
        args->ppCommandList = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppCommandList) return;
        *args->ppCommandList = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext_FinishCommandList_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext_FinishCommandList *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext_FinishCommandList_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11DeviceContext_FinishCommandList *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext,
                                 NPT_METHOD_ID3D11DeviceContext_FinishCommandList),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppCommandList (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext_FinishCommandList(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext_FinishCommandList args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext_FinishCommandList_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_FinishCommandList on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext_FinishCommandList_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext_FinishCommandList: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext_FinishCommandList _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext_FinishCommandList, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext_FinishCommandList);

    if (ctx->id3d11devicecontext_dispatch_overrides && ctx->id3d11devicecontext_dispatch_overrides->FinishCommandList) {
        args.ret = ctx->id3d11devicecontext_dispatch_overrides->FinishCommandList(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.RestoreDeferredContextState, args.ppCommandList);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppCommandList && *args.ppCommandList)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppCommandList, *args.ppCommandList, NPT_OBJECT_TYPE_ID3D11COMMANDLIST);
    else if (args.ppCommandList)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppCommandList);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext_FinishCommandList_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::CopySubresourceRegion1
 */

struct npt_command_ID3D11DeviceContext1_CopySubresourceRegion1 {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    UINT DstX;
    UINT DstY;
    UINT DstZ;
    ID3D11Resource * pSrcResource;
    UINT SrcSubresource;
    const D3D11_BOX * pSrcBox;
    UINT CopyFlags;
};

static inline void
npt_decode_ID3D11DeviceContext1_CopySubresourceRegion1_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D11DeviceContext1_CopySubresourceRegion1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    npt_decode_UINT(dec, &args->DstX);
    npt_decode_UINT(dec, &args->DstY);
    npt_decode_UINT(dec, &args->DstZ);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->SrcSubresource);
    if (npt_decode_simple_pointer(dec)) {
        args->pSrcBox = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BOX));
        if (!args->pSrcBox) return;
        npt_decode_D3D11_BOX(dec, (D3D11_BOX *)args->pSrcBox);
    } else {
        args->pSrcBox = NULL;
    }
    npt_decode_UINT(dec, &args->CopyFlags);
    /* Allocate temp storage for output-only parameters */









}

static inline void
npt_replace_ID3D11DeviceContext1_CopySubresourceRegion1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_CopySubresourceRegion1 *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext1_CopySubresourceRegion1_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D11DeviceContext1_CopySubresourceRegion1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_CopySubresourceRegion1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_CopySubresourceRegion1(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_CopySubresourceRegion1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_CopySubresourceRegion1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_CopySubresourceRegion1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_CopySubresourceRegion1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_CopySubresourceRegion1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_CopySubresourceRegion1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_CopySubresourceRegion1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_CopySubresourceRegion1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->CopySubresourceRegion1) {
        ctx->id3d11devicecontext1_dispatch_overrides->CopySubresourceRegion1(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.DstX, args.DstY, args.DstZ, args.pSrcResource, args.SrcSubresource, args.pSrcBox, args.CopyFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_CopySubresourceRegion1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::UpdateSubresource1
 */

struct npt_command_ID3D11DeviceContext1_UpdateSubresource1 {
    void *_self;
    ID3D11Resource * pDstResource;
    UINT DstSubresource;
    const D3D11_BOX * pDstBox;
    const void * pSrcData;
    UINT SrcRowPitch;
    UINT SrcDepthPitch;
    UINT CopyFlags;
};

static inline void
npt_decode_ID3D11DeviceContext1_UpdateSubresource1_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D11DeviceContext1_UpdateSubresource1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    if (npt_decode_simple_pointer(dec)) {
        args->pDstBox = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_BOX));
        if (!args->pDstBox) return;
        npt_decode_D3D11_BOX(dec, (D3D11_BOX *)args->pDstBox);
    } else {
        args->pDstBox = NULL;
    }
    /* ERROR: pSrcData (void) is not serializable */
    return;
    npt_decode_UINT(dec, &args->SrcRowPitch);
    npt_decode_UINT(dec, &args->SrcDepthPitch);
    npt_decode_UINT(dec, &args->CopyFlags);
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D11DeviceContext1_UpdateSubresource1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_UpdateSubresource1 *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext1_UpdateSubresource1_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D11DeviceContext1_UpdateSubresource1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_UpdateSubresource1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_UpdateSubresource1(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_UpdateSubresource1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_UpdateSubresource1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_UpdateSubresource1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_UpdateSubresource1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_UpdateSubresource1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_UpdateSubresource1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_UpdateSubresource1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_UpdateSubresource1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->UpdateSubresource1) {
        ctx->id3d11devicecontext1_dispatch_overrides->UpdateSubresource1(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.pDstBox, args.pSrcData, args.SrcRowPitch, args.SrcDepthPitch, args.CopyFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_UpdateSubresource1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::DiscardResource
 */

struct npt_command_ID3D11DeviceContext1_DiscardResource {
    void *_self;
    ID3D11Resource * pResource;
};

static inline void
npt_decode_ID3D11DeviceContext1_DiscardResource_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D11DeviceContext1_DiscardResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext1_DiscardResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_DiscardResource *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext1_DiscardResource_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D11DeviceContext1_DiscardResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DiscardResource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_DiscardResource(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_DiscardResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_DiscardResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DiscardResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_DiscardResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DiscardResource: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_DiscardResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_DiscardResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_DiscardResource);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->DiscardResource) {
        ctx->id3d11devicecontext1_dispatch_overrides->DiscardResource(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_DiscardResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::DiscardView
 */

struct npt_command_ID3D11DeviceContext1_DiscardView {
    void *_self;
    ID3D11View * pResourceView;
};

static inline void
npt_decode_ID3D11DeviceContext1_DiscardView_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11DeviceContext1_DiscardView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResourceView = (ID3D11View *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext1_DiscardView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_DiscardView *args)
{
    args->pResourceView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResourceView, NPT_OBJECT_TYPE_ID3D11VIEW);
}

static inline void
npt_encode_ID3D11DeviceContext1_DiscardView_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11DeviceContext1_DiscardView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DiscardView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_DiscardView(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_DiscardView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_DiscardView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DiscardView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_DiscardView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DiscardView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_DiscardView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_DiscardView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_DiscardView);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->DiscardView) {
        ctx->id3d11devicecontext1_dispatch_overrides->DiscardView(ctx, &args, _original);
    } else {
        _original(args._self, args.pResourceView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_DiscardView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::VSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_VSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline void
npt_decode_ID3D11DeviceContext1_VSSetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_VSSetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    uint64_t _cnt_pFirstConstant = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFirstConstant = npt_decode_array_count_unchecked(dec);
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pFirstConstant);
        if (!args->pFirstConstant) return;
        npt_decode_UINT_array(dec, (UINT *)args->pFirstConstant, _cnt_pFirstConstant);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFirstConstant = NULL;
    }
    uint64_t _cnt_pNumConstants = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pNumConstants = npt_decode_array_count_unchecked(dec);
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pNumConstants);
        if (!args->pNumConstants) return;
        npt_decode_UINT_array(dec, (UINT *)args->pNumConstants, _cnt_pNumConstants);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pNumConstants = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pFirstConstant && _cnt_pFirstConstant < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pNumConstants && _cnt_pNumConstants < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext1_VSSetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_VSSetConstantBuffers1 *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext1_VSSetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_VSSetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_VSSetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_VSSetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_VSSetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_VSSetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_VSSetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_VSSetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_VSSetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_VSSetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_VSSetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_VSSetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->VSSetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->VSSetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_VSSetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::HSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_HSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline void
npt_decode_ID3D11DeviceContext1_HSSetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_HSSetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    uint64_t _cnt_pFirstConstant = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFirstConstant = npt_decode_array_count_unchecked(dec);
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pFirstConstant);
        if (!args->pFirstConstant) return;
        npt_decode_UINT_array(dec, (UINT *)args->pFirstConstant, _cnt_pFirstConstant);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFirstConstant = NULL;
    }
    uint64_t _cnt_pNumConstants = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pNumConstants = npt_decode_array_count_unchecked(dec);
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pNumConstants);
        if (!args->pNumConstants) return;
        npt_decode_UINT_array(dec, (UINT *)args->pNumConstants, _cnt_pNumConstants);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pNumConstants = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pFirstConstant && _cnt_pFirstConstant < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pNumConstants && _cnt_pNumConstants < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext1_HSSetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_HSSetConstantBuffers1 *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext1_HSSetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_HSSetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_HSSetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_HSSetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_HSSetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_HSSetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_HSSetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_HSSetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_HSSetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_HSSetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_HSSetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_HSSetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->HSSetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->HSSetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_HSSetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::DSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_DSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline void
npt_decode_ID3D11DeviceContext1_DSSetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_DSSetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    uint64_t _cnt_pFirstConstant = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFirstConstant = npt_decode_array_count_unchecked(dec);
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pFirstConstant);
        if (!args->pFirstConstant) return;
        npt_decode_UINT_array(dec, (UINT *)args->pFirstConstant, _cnt_pFirstConstant);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFirstConstant = NULL;
    }
    uint64_t _cnt_pNumConstants = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pNumConstants = npt_decode_array_count_unchecked(dec);
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pNumConstants);
        if (!args->pNumConstants) return;
        npt_decode_UINT_array(dec, (UINT *)args->pNumConstants, _cnt_pNumConstants);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pNumConstants = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pFirstConstant && _cnt_pFirstConstant < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pNumConstants && _cnt_pNumConstants < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext1_DSSetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_DSSetConstantBuffers1 *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext1_DSSetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_DSSetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DSSetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_DSSetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_DSSetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_DSSetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DSSetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_DSSetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DSSetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_DSSetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_DSSetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_DSSetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->DSSetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->DSSetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_DSSetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::GSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_GSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline void
npt_decode_ID3D11DeviceContext1_GSSetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_GSSetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    uint64_t _cnt_pFirstConstant = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFirstConstant = npt_decode_array_count_unchecked(dec);
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pFirstConstant);
        if (!args->pFirstConstant) return;
        npt_decode_UINT_array(dec, (UINT *)args->pFirstConstant, _cnt_pFirstConstant);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFirstConstant = NULL;
    }
    uint64_t _cnt_pNumConstants = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pNumConstants = npt_decode_array_count_unchecked(dec);
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pNumConstants);
        if (!args->pNumConstants) return;
        npt_decode_UINT_array(dec, (UINT *)args->pNumConstants, _cnt_pNumConstants);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pNumConstants = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pFirstConstant && _cnt_pFirstConstant < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pNumConstants && _cnt_pNumConstants < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext1_GSSetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_GSSetConstantBuffers1 *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext1_GSSetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_GSSetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_GSSetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_GSSetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_GSSetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_GSSetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_GSSetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_GSSetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_GSSetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_GSSetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_GSSetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_GSSetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->GSSetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->GSSetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_GSSetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::PSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_PSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline void
npt_decode_ID3D11DeviceContext1_PSSetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_PSSetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    uint64_t _cnt_pFirstConstant = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFirstConstant = npt_decode_array_count_unchecked(dec);
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pFirstConstant);
        if (!args->pFirstConstant) return;
        npt_decode_UINT_array(dec, (UINT *)args->pFirstConstant, _cnt_pFirstConstant);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFirstConstant = NULL;
    }
    uint64_t _cnt_pNumConstants = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pNumConstants = npt_decode_array_count_unchecked(dec);
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pNumConstants);
        if (!args->pNumConstants) return;
        npt_decode_UINT_array(dec, (UINT *)args->pNumConstants, _cnt_pNumConstants);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pNumConstants = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pFirstConstant && _cnt_pFirstConstant < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pNumConstants && _cnt_pNumConstants < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext1_PSSetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_PSSetConstantBuffers1 *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext1_PSSetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_PSSetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_PSSetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_PSSetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_PSSetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_PSSetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_PSSetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_PSSetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_PSSetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_PSSetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_PSSetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_PSSetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->PSSetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->PSSetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_PSSetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::CSSetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_CSSetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    const UINT * pFirstConstant;
    const UINT * pNumConstants;
};

static inline void
npt_decode_ID3D11DeviceContext1_CSSetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_CSSetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    uint64_t _cnt_ppConstantBuffers = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppConstantBuffers) {
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppConstantBuffers);
        if (!args->ppConstantBuffers) return;
        for (uint64_t _i = 0; _i < _cnt_ppConstantBuffers; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppConstantBuffers[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppConstantBuffers = NULL;
    }
    uint64_t _cnt_pFirstConstant = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFirstConstant = npt_decode_array_count_unchecked(dec);
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pFirstConstant);
        if (!args->pFirstConstant) return;
        npt_decode_UINT_array(dec, (UINT *)args->pFirstConstant, _cnt_pFirstConstant);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFirstConstant = NULL;
    }
    uint64_t _cnt_pNumConstants = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pNumConstants = npt_decode_array_count_unchecked(dec);
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pNumConstants);
        if (!args->pNumConstants) return;
        npt_decode_UINT_array(dec, (UINT *)args->pNumConstants, _cnt_pNumConstants);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pNumConstants = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppConstantBuffers && _cnt_ppConstantBuffers < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pFirstConstant && _cnt_pFirstConstant < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pNumConstants && _cnt_pNumConstants < (uint64_t)(args->NumBuffers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext1_CSSetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_CSSetConstantBuffers1 *args)
{
    if (args->ppConstantBuffers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBuffers); _i++)
            args->ppConstantBuffers[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }
}

static inline void
npt_encode_ID3D11DeviceContext1_CSSetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_CSSetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_CSSetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_CSSetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_CSSetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_CSSetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_CSSetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_CSSetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_CSSetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_CSSetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_CSSetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_CSSetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->CSSetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->CSSetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_CSSetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::VSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_VSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext1_VSGetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_VSGetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }


    /* Allocate temp storage for output-only parameters */



    if (args->NumBuffers) {
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pFirstConstant) return;
        memset(args->pFirstConstant, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pFirstConstant = NULL;
    }
    if (args->NumBuffers) {
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pNumConstants) return;
        memset(args->pNumConstants, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pNumConstants = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext1_VSGetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_VSGetConstantBuffers1 *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext1_VSGetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_VSGetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_VSGetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    if (args->pFirstConstant) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pFirstConstant, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumConstants) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pNumConstants, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext1_VSGetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_VSGetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_VSGetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_VSGetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_VSGetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_VSGetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_VSGetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_VSGetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_VSGetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->VSGetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->VSGetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_VSGetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::HSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_HSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext1_HSGetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_HSGetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }


    /* Allocate temp storage for output-only parameters */



    if (args->NumBuffers) {
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pFirstConstant) return;
        memset(args->pFirstConstant, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pFirstConstant = NULL;
    }
    if (args->NumBuffers) {
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pNumConstants) return;
        memset(args->pNumConstants, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pNumConstants = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext1_HSGetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_HSGetConstantBuffers1 *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext1_HSGetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_HSGetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_HSGetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    if (args->pFirstConstant) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pFirstConstant, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumConstants) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pNumConstants, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext1_HSGetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_HSGetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_HSGetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_HSGetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_HSGetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_HSGetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_HSGetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_HSGetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_HSGetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->HSGetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->HSGetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_HSGetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::DSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_DSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext1_DSGetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_DSGetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }


    /* Allocate temp storage for output-only parameters */



    if (args->NumBuffers) {
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pFirstConstant) return;
        memset(args->pFirstConstant, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pFirstConstant = NULL;
    }
    if (args->NumBuffers) {
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pNumConstants) return;
        memset(args->pNumConstants, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pNumConstants = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext1_DSGetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_DSGetConstantBuffers1 *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext1_DSGetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_DSGetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DSGetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    if (args->pFirstConstant) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pFirstConstant, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumConstants) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pNumConstants, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext1_DSGetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_DSGetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_DSGetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DSGetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_DSGetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DSGetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_DSGetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_DSGetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_DSGetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->DSGetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->DSGetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_DSGetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::GSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_GSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext1_GSGetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_GSGetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }


    /* Allocate temp storage for output-only parameters */



    if (args->NumBuffers) {
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pFirstConstant) return;
        memset(args->pFirstConstant, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pFirstConstant = NULL;
    }
    if (args->NumBuffers) {
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pNumConstants) return;
        memset(args->pNumConstants, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pNumConstants = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext1_GSGetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_GSGetConstantBuffers1 *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext1_GSGetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_GSGetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_GSGetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    if (args->pFirstConstant) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pFirstConstant, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumConstants) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pNumConstants, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext1_GSGetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_GSGetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_GSGetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_GSGetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_GSGetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_GSGetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_GSGetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_GSGetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_GSGetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->GSGetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->GSGetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_GSGetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::PSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_PSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext1_PSGetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_PSGetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }


    /* Allocate temp storage for output-only parameters */



    if (args->NumBuffers) {
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pFirstConstant) return;
        memset(args->pFirstConstant, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pFirstConstant = NULL;
    }
    if (args->NumBuffers) {
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pNumConstants) return;
        memset(args->pNumConstants, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pNumConstants = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext1_PSGetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_PSGetConstantBuffers1 *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext1_PSGetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_PSGetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_PSGetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    if (args->pFirstConstant) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pFirstConstant, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumConstants) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pNumConstants, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext1_PSGetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_PSGetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_PSGetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_PSGetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_PSGetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_PSGetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_PSGetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_PSGetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_PSGetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->PSGetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->PSGetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_PSGetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::CSGetConstantBuffers1
 */

struct npt_command_ID3D11DeviceContext1_CSGetConstantBuffers1 {
    void *_self;
    UINT StartSlot;
    UINT NumBuffers;
    ID3D11Buffer ** ppConstantBuffers;
    UINT * pFirstConstant;
    UINT * pNumConstants;
    /* Shadow: per-element guest-allocated ids for ppConstantBuffers.  Array and
     * count are temp-allocated from the decoder pool in the same pass
     * that decodes the wire ids. */
    uint64_t *_guest_ids_ppConstantBuffers;
    uint32_t _guest_id_count_ppConstantBuffers;
};

static inline void
npt_decode_ID3D11DeviceContext1_CSGetConstantBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11DeviceContext1_CSGetConstantBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumBuffers);
    {
        const uint64_t _cnt = npt_decode_array_count_unchecked(dec);
        args->ppConstantBuffers = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt);
        args->_guest_ids_ppConstantBuffers = _cnt ? npt_cs_decoder_alloc_temp_array(dec, sizeof(uint64_t), _cnt) : NULL;
        args->_guest_id_count_ppConstantBuffers = (uint32_t)_cnt;
        if (_cnt && (!args->ppConstantBuffers || !args->_guest_ids_ppConstantBuffers)) return;
        if (_cnt) memset(args->ppConstantBuffers, 0, sizeof(void *) * _cnt);
        for (uint64_t _i = 0; _i < _cnt; _i++) {
            uint64_t _gid;
            npt_decode_uint64_t(dec, &_gid);
            args->_guest_ids_ppConstantBuffers[_i] = _gid;
        }
    }


    /* Allocate temp storage for output-only parameters */



    if (args->NumBuffers) {
        args->pFirstConstant = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pFirstConstant) return;
        memset(args->pFirstConstant, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pFirstConstant = NULL;
    }
    if (args->NumBuffers) {
        args->pNumConstants = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), args->NumBuffers);
        if (!args->pNumConstants) return;
        memset(args->pNumConstants, 0, sizeof(UINT) * args->NumBuffers);
    } else {
        args->pNumConstants = NULL;
    }
}

static inline void
npt_replace_ID3D11DeviceContext1_CSGetConstantBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_CSGetConstantBuffers1 *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext1_CSGetConstantBuffers1_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11DeviceContext1_CSGetConstantBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_CSGetConstantBuffers1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppConstantBuffers (guest-allocated id, registered host-side; not in reply) */
    if (args->pFirstConstant) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pFirstConstant, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (args->pNumConstants) {
        npt_encode_array_count(enc, args->NumBuffers);
        npt_encode_UINT_array(enc, args->pNumConstants, args->NumBuffers);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D11DeviceContext1_CSGetConstantBuffers1(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_CSGetConstantBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_CSGetConstantBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_CSGetConstantBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_CSGetConstantBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_CSGetConstantBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_CSGetConstantBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_CSGetConstantBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_CSGetConstantBuffers1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->CSGetConstantBuffers1) {
        ctx->id3d11devicecontext1_dispatch_overrides->CSGetConstantBuffers1(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumBuffers, args.ppConstantBuffers, args.pFirstConstant, args.pNumConstants);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppConstantBuffers && args._guest_ids_ppConstantBuffers) {
        const uint32_t _n = args._guest_id_count_ppConstantBuffers;
        for (uint32_t _i = 0; _i < _n; _i++)
            npt_cs_handle_register_guest_id(ctx,
                args._guest_ids_ppConstantBuffers[_i], args.ppConstantBuffers[_i], NPT_OBJECT_TYPE_ID3D11BUFFER);
    }

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_CSGetConstantBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::SwapDeviceContextState
 */

struct npt_command_ID3D11DeviceContext1_SwapDeviceContextState {
    void *_self;
    ID3DDeviceContextState * pState;
    ID3DDeviceContextState ** ppPreviousState;
    /* Shadow: the guest-allocated id for ppPreviousState arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppPreviousState -> *ppPreviousState} in the
     * context object table. */
    uint64_t _guest_id_ppPreviousState;
};

static inline void
npt_decode_ID3D11DeviceContext1_SwapDeviceContextState_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D11DeviceContext1_SwapDeviceContextState *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pState = (ID3DDeviceContextState *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppPreviousState = _gid;
        args->ppPreviousState = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppPreviousState) return;
        *args->ppPreviousState = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext1_SwapDeviceContextState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_SwapDeviceContextState *args)
{
    args->pState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pState, NPT_OBJECT_TYPE_ID3DDEVICECONTEXTSTATE);
}

static inline void
npt_encode_ID3D11DeviceContext1_SwapDeviceContextState_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D11DeviceContext1_SwapDeviceContextState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_SwapDeviceContextState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppPreviousState (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11DeviceContext1_SwapDeviceContextState(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_SwapDeviceContextState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_SwapDeviceContextState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_SwapDeviceContextState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPreviousState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_SwapDeviceContextState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_SwapDeviceContextState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPreviousState);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_SwapDeviceContextState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_SwapDeviceContextState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_SwapDeviceContextState);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->SwapDeviceContextState) {
        ctx->id3d11devicecontext1_dispatch_overrides->SwapDeviceContextState(ctx, &args, _original);
    } else {
        _original(args._self, args.pState, args.ppPreviousState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppPreviousState && *args.ppPreviousState)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppPreviousState, *args.ppPreviousState, NPT_OBJECT_TYPE_ID3DDEVICECONTEXTSTATE);
    else if (args.ppPreviousState)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppPreviousState);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_SwapDeviceContextState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::ClearView
 */

struct npt_command_ID3D11DeviceContext1_ClearView {
    void *_self;
    ID3D11View * pView;
    const FLOAT * Color;
    const D3D11_RECT * pRect;
    UINT NumRects;
};

static inline void
npt_decode_ID3D11DeviceContext1_ClearView_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11DeviceContext1_ClearView *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pView = (ID3D11View *)npt_object_from_id(_id);
    }
    (void)npt_decode_array_count(dec, 4);
    args->Color = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
    if (!args->Color) return;
    npt_decode_FLOAT_array(dec, (FLOAT *)args->Color, 4);
    uint64_t _cnt_pRect = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRect = npt_decode_array_count_unchecked(dec);
        args->pRect = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_RECT), _cnt_pRect);
        if (!args->pRect) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRect; _i++)
            npt_decode_D3D11_RECT(dec, (D3D11_RECT *)&args->pRect[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRect = NULL;
    }
    npt_decode_UINT(dec, &args->NumRects);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRect && _cnt_pRect < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11DeviceContext1_ClearView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_ClearView *args)
{
    args->pView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pView, NPT_OBJECT_TYPE_ID3D11VIEW);
}

static inline void
npt_encode_ID3D11DeviceContext1_ClearView_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11DeviceContext1_ClearView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_ClearView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_ClearView(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_ClearView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_ClearView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_ClearView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_ClearView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_ClearView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_ClearView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_ClearView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_ClearView);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->ClearView) {
        ctx->id3d11devicecontext1_dispatch_overrides->ClearView(ctx, &args, _original);
    } else {
        _original(args._self, args.pView, args.Color, args.pRect, args.NumRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_ClearView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext1::DiscardView1
 */

struct npt_command_ID3D11DeviceContext1_DiscardView1 {
    void *_self;
    ID3D11View * pResourceView;
    const D3D11_RECT * pRects;
    UINT NumRects;
};

static inline void
npt_decode_ID3D11DeviceContext1_DiscardView1_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext1_DiscardView1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResourceView = (ID3D11View *)npt_object_from_id(_id);
    }
    uint64_t _cnt_pRects = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRects = npt_decode_array_count_unchecked(dec);
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_RECT), _cnt_pRects);
        if (!args->pRects) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRects; _i++)
            npt_decode_D3D11_RECT(dec, (D3D11_RECT *)&args->pRects[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRects = NULL;
    }
    npt_decode_UINT(dec, &args->NumRects);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRects && _cnt_pRects < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D11DeviceContext1_DiscardView1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext1_DiscardView1 *args)
{
    args->pResourceView = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResourceView, NPT_OBJECT_TYPE_ID3D11VIEW);
}

static inline void
npt_encode_ID3D11DeviceContext1_DiscardView1_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext1_DiscardView1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext1,
                                 NPT_METHOD_ID3D11DeviceContext1_DiscardView1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext1_DiscardView1(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext1_DiscardView1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext1_DiscardView1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DiscardView1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext1_DiscardView1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext1_DiscardView1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext1_DiscardView1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext1_DiscardView1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext1_DiscardView1);

    if (ctx->id3d11devicecontext1_dispatch_overrides && ctx->id3d11devicecontext1_dispatch_overrides->DiscardView1) {
        ctx->id3d11devicecontext1_dispatch_overrides->DiscardView1(ctx, &args, _original);
    } else {
        _original(args._self, args.pResourceView, args.pRects, args.NumRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext1_DiscardView1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::UpdateTileMappings
 */

struct npt_command_ID3D11DeviceContext2_UpdateTileMappings {
    void *_self;
    ID3D11Resource * pTiledResource;
    UINT NumTiledResourceRegions;
    const D3D11_TILED_RESOURCE_COORDINATE * pTiledResourceRegionStartCoordinates;
    const D3D11_TILE_REGION_SIZE * pTiledResourceRegionSizes;
    ID3D11Buffer * pTilePool;
    UINT NumRanges;
    const UINT * pRangeFlags;
    const UINT * pTilePoolStartOffsets;
    const UINT * pRangeTileCounts;
    UINT Flags;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext2_UpdateTileMappings_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D11DeviceContext2_UpdateTileMappings *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTiledResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->NumTiledResourceRegions);
    uint64_t _cnt_pTiledResourceRegionStartCoordinates = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pTiledResourceRegionStartCoordinates = npt_decode_array_count_unchecked(dec);
        args->pTiledResourceRegionStartCoordinates = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_TILED_RESOURCE_COORDINATE), _cnt_pTiledResourceRegionStartCoordinates);
        if (!args->pTiledResourceRegionStartCoordinates) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pTiledResourceRegionStartCoordinates; _i++)
            npt_decode_D3D11_TILED_RESOURCE_COORDINATE(dec, (D3D11_TILED_RESOURCE_COORDINATE *)&args->pTiledResourceRegionStartCoordinates[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pTiledResourceRegionStartCoordinates = NULL;
    }
    uint64_t _cnt_pTiledResourceRegionSizes = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pTiledResourceRegionSizes = npt_decode_array_count_unchecked(dec);
        args->pTiledResourceRegionSizes = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D11_TILE_REGION_SIZE), _cnt_pTiledResourceRegionSizes);
        if (!args->pTiledResourceRegionSizes) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pTiledResourceRegionSizes; _i++)
            npt_decode_D3D11_TILE_REGION_SIZE(dec, (D3D11_TILE_REGION_SIZE *)&args->pTiledResourceRegionSizes[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pTiledResourceRegionSizes = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTilePool = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->NumRanges);
    uint64_t _cnt_pRangeFlags = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRangeFlags = npt_decode_array_count_unchecked(dec);
        args->pRangeFlags = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pRangeFlags);
        if (!args->pRangeFlags) return;
        npt_decode_UINT_array(dec, (UINT *)args->pRangeFlags, _cnt_pRangeFlags);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRangeFlags = NULL;
    }
    uint64_t _cnt_pTilePoolStartOffsets = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pTilePoolStartOffsets = npt_decode_array_count_unchecked(dec);
        args->pTilePoolStartOffsets = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pTilePoolStartOffsets);
        if (!args->pTilePoolStartOffsets) return;
        npt_decode_UINT_array(dec, (UINT *)args->pTilePoolStartOffsets, _cnt_pTilePoolStartOffsets);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pTilePoolStartOffsets = NULL;
    }
    uint64_t _cnt_pRangeTileCounts = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRangeTileCounts = npt_decode_array_count_unchecked(dec);
        args->pRangeTileCounts = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pRangeTileCounts);
        if (!args->pRangeTileCounts) return;
        npt_decode_UINT_array(dec, (UINT *)args->pRangeTileCounts, _cnt_pRangeTileCounts);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRangeTileCounts = NULL;
    }
    npt_decode_UINT(dec, &args->Flags);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pTiledResourceRegionStartCoordinates && _cnt_pTiledResourceRegionStartCoordinates < (uint64_t)(args->NumTiledResourceRegions)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pTiledResourceRegionSizes && _cnt_pTiledResourceRegionSizes < (uint64_t)(args->NumTiledResourceRegions)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pRangeFlags && _cnt_pRangeFlags < (uint64_t)(args->NumRanges)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pTilePoolStartOffsets && _cnt_pTilePoolStartOffsets < (uint64_t)(args->NumRanges)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pRangeTileCounts && _cnt_pRangeTileCounts < (uint64_t)(args->NumRanges)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */










}

static inline void
npt_replace_ID3D11DeviceContext2_UpdateTileMappings_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_UpdateTileMappings *args)
{
    args->pTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTiledResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pTilePool = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTilePool, NPT_OBJECT_TYPE_ID3D11BUFFER);
}

static inline void
npt_encode_ID3D11DeviceContext2_UpdateTileMappings_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D11DeviceContext2_UpdateTileMappings *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_UpdateTileMappings),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_UpdateTileMappings(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_UpdateTileMappings args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_UpdateTileMappings_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_UpdateTileMappings on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_UpdateTileMappings_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_UpdateTileMappings: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_UpdateTileMappings _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_UpdateTileMappings, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_UpdateTileMappings);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->UpdateTileMappings) {
        args.ret = ctx->id3d11devicecontext2_dispatch_overrides->UpdateTileMappings(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pTiledResource, args.NumTiledResourceRegions, args.pTiledResourceRegionStartCoordinates, args.pTiledResourceRegionSizes, args.pTilePool, args.NumRanges, args.pRangeFlags, args.pTilePoolStartOffsets, args.pRangeTileCounts, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_UpdateTileMappings_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::CopyTileMappings
 */

struct npt_command_ID3D11DeviceContext2_CopyTileMappings {
    void *_self;
    ID3D11Resource * pDestTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pDestRegionStartCoordinate;
    ID3D11Resource * pSourceTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pSourceRegionStartCoordinate;
    const D3D11_TILE_REGION_SIZE * pTileRegionSize;
    UINT Flags;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext2_CopyTileMappings_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_ID3D11DeviceContext2_CopyTileMappings *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDestTiledResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDestRegionStartCoordinate = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILED_RESOURCE_COORDINATE));
        if (!args->pDestRegionStartCoordinate) return;
        npt_decode_D3D11_TILED_RESOURCE_COORDINATE(dec, (D3D11_TILED_RESOURCE_COORDINATE *)args->pDestRegionStartCoordinate);
    } else {
        args->pDestRegionStartCoordinate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSourceTiledResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pSourceRegionStartCoordinate = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILED_RESOURCE_COORDINATE));
        if (!args->pSourceRegionStartCoordinate) return;
        npt_decode_D3D11_TILED_RESOURCE_COORDINATE(dec, (D3D11_TILED_RESOURCE_COORDINATE *)args->pSourceRegionStartCoordinate);
    } else {
        args->pSourceRegionStartCoordinate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pTileRegionSize = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILE_REGION_SIZE));
        if (!args->pTileRegionSize) return;
        npt_decode_D3D11_TILE_REGION_SIZE(dec, (D3D11_TILE_REGION_SIZE *)args->pTileRegionSize);
    } else {
        args->pTileRegionSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->Flags);
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D11DeviceContext2_CopyTileMappings_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_CopyTileMappings *args)
{
    args->pDestTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDestTiledResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pSourceTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSourceTiledResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext2_CopyTileMappings_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_ID3D11DeviceContext2_CopyTileMappings *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_CopyTileMappings),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_CopyTileMappings(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_CopyTileMappings args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_CopyTileMappings_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_CopyTileMappings on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_CopyTileMappings_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_CopyTileMappings: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_CopyTileMappings _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_CopyTileMappings, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_CopyTileMappings);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->CopyTileMappings) {
        args.ret = ctx->id3d11devicecontext2_dispatch_overrides->CopyTileMappings(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDestTiledResource, args.pDestRegionStartCoordinate, args.pSourceTiledResource, args.pSourceRegionStartCoordinate, args.pTileRegionSize, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_CopyTileMappings_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::CopyTiles
 */

struct npt_command_ID3D11DeviceContext2_CopyTiles {
    void *_self;
    ID3D11Resource * pTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate;
    const D3D11_TILE_REGION_SIZE * pTileRegionSize;
    ID3D11Buffer * pBuffer;
    UINT64 BufferStartOffsetInBytes;
    UINT Flags;
};

static inline void
npt_decode_ID3D11DeviceContext2_CopyTiles_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11DeviceContext2_CopyTiles *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTiledResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pTileRegionStartCoordinate = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILED_RESOURCE_COORDINATE));
        if (!args->pTileRegionStartCoordinate) return;
        npt_decode_D3D11_TILED_RESOURCE_COORDINATE(dec, (D3D11_TILED_RESOURCE_COORDINATE *)args->pTileRegionStartCoordinate);
    } else {
        args->pTileRegionStartCoordinate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pTileRegionSize = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILE_REGION_SIZE));
        if (!args->pTileRegionSize) return;
        npt_decode_D3D11_TILE_REGION_SIZE(dec, (D3D11_TILE_REGION_SIZE *)args->pTileRegionSize);
    } else {
        args->pTileRegionSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pBuffer = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->BufferStartOffsetInBytes);
    npt_decode_UINT(dec, &args->Flags);
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D11DeviceContext2_CopyTiles_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_CopyTiles *args)
{
    args->pTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTiledResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pBuffer, NPT_OBJECT_TYPE_ID3D11BUFFER);
}

static inline void
npt_encode_ID3D11DeviceContext2_CopyTiles_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11DeviceContext2_CopyTiles *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_CopyTiles),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_CopyTiles(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_CopyTiles args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_CopyTiles_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_CopyTiles on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_CopyTiles_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_CopyTiles: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_CopyTiles _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_CopyTiles, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_CopyTiles);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->CopyTiles) {
        ctx->id3d11devicecontext2_dispatch_overrides->CopyTiles(ctx, &args, _original);
    } else {
        _original(args._self, args.pTiledResource, args.pTileRegionStartCoordinate, args.pTileRegionSize, args.pBuffer, args.BufferStartOffsetInBytes, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_CopyTiles_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::UpdateTiles
 */

struct npt_command_ID3D11DeviceContext2_UpdateTiles {
    void *_self;
    ID3D11Resource * pDestTiledResource;
    const D3D11_TILED_RESOURCE_COORDINATE * pDestTileRegionStartCoordinate;
    const D3D11_TILE_REGION_SIZE * pDestTileRegionSize;
    const void * pSourceTileData;
    UINT Flags;
};

static inline void
npt_decode_ID3D11DeviceContext2_UpdateTiles_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11DeviceContext2_UpdateTiles *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDestTiledResource = (ID3D11Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDestTileRegionStartCoordinate = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILED_RESOURCE_COORDINATE));
        if (!args->pDestTileRegionStartCoordinate) return;
        npt_decode_D3D11_TILED_RESOURCE_COORDINATE(dec, (D3D11_TILED_RESOURCE_COORDINATE *)args->pDestTileRegionStartCoordinate);
    } else {
        args->pDestTileRegionStartCoordinate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDestTileRegionSize = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_TILE_REGION_SIZE));
        if (!args->pDestTileRegionSize) return;
        npt_decode_D3D11_TILE_REGION_SIZE(dec, (D3D11_TILE_REGION_SIZE *)args->pDestTileRegionSize);
    } else {
        args->pDestTileRegionSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* ERROR: pSourceTileData (void) is not serializable */
    return;
    npt_decode_UINT(dec, &args->Flags);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D11DeviceContext2_UpdateTiles_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_UpdateTiles *args)
{
    args->pDestTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDestTiledResource, NPT_OBJECT_TYPE_ID3D11RESOURCE);
}

static inline void
npt_encode_ID3D11DeviceContext2_UpdateTiles_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11DeviceContext2_UpdateTiles *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_UpdateTiles),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_UpdateTiles(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_UpdateTiles args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_UpdateTiles_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_UpdateTiles on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_UpdateTiles_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_UpdateTiles: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_UpdateTiles _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_UpdateTiles, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_UpdateTiles);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->UpdateTiles) {
        ctx->id3d11devicecontext2_dispatch_overrides->UpdateTiles(ctx, &args, _original);
    } else {
        _original(args._self, args.pDestTiledResource, args.pDestTileRegionStartCoordinate, args.pDestTileRegionSize, args.pSourceTileData, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_UpdateTiles_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::ResizeTilePool
 */

struct npt_command_ID3D11DeviceContext2_ResizeTilePool {
    void *_self;
    ID3D11Buffer * pTilePool;
    UINT64 NewSizeInBytes;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext2_ResizeTilePool_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D11DeviceContext2_ResizeTilePool *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTilePool = (ID3D11Buffer *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->NewSizeInBytes);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext2_ResizeTilePool_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_ResizeTilePool *args)
{
    args->pTilePool = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTilePool, NPT_OBJECT_TYPE_ID3D11BUFFER);
}

static inline void
npt_encode_ID3D11DeviceContext2_ResizeTilePool_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D11DeviceContext2_ResizeTilePool *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_ResizeTilePool),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_ResizeTilePool(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_ResizeTilePool args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_ResizeTilePool_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_ResizeTilePool on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_ResizeTilePool_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_ResizeTilePool: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_ResizeTilePool _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_ResizeTilePool, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_ResizeTilePool);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->ResizeTilePool) {
        args.ret = ctx->id3d11devicecontext2_dispatch_overrides->ResizeTilePool(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pTilePool, args.NewSizeInBytes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_ResizeTilePool_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::TiledResourceBarrier
 */

struct npt_command_ID3D11DeviceContext2_TiledResourceBarrier {
    void *_self;
    ID3D11DeviceChild * pTiledResourceOrViewAccessBeforeBarrier;
    ID3D11DeviceChild * pTiledResourceOrViewAccessAfterBarrier;
};

static inline void
npt_decode_ID3D11DeviceContext2_TiledResourceBarrier_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D11DeviceContext2_TiledResourceBarrier *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTiledResourceOrViewAccessBeforeBarrier = (ID3D11DeviceChild *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTiledResourceOrViewAccessAfterBarrier = (ID3D11DeviceChild *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext2_TiledResourceBarrier_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_TiledResourceBarrier *args)
{
    args->pTiledResourceOrViewAccessBeforeBarrier = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTiledResourceOrViewAccessBeforeBarrier, NPT_OBJECT_TYPE_ID3D11DEVICECHILD);
    args->pTiledResourceOrViewAccessAfterBarrier = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTiledResourceOrViewAccessAfterBarrier, NPT_OBJECT_TYPE_ID3D11DEVICECHILD);
}

static inline void
npt_encode_ID3D11DeviceContext2_TiledResourceBarrier_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D11DeviceContext2_TiledResourceBarrier *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_TiledResourceBarrier),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_TiledResourceBarrier(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_TiledResourceBarrier args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_TiledResourceBarrier_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_TiledResourceBarrier on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_TiledResourceBarrier_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_TiledResourceBarrier: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_TiledResourceBarrier _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_TiledResourceBarrier, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_TiledResourceBarrier);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->TiledResourceBarrier) {
        ctx->id3d11devicecontext2_dispatch_overrides->TiledResourceBarrier(ctx, &args, _original);
    } else {
        _original(args._self, args.pTiledResourceOrViewAccessBeforeBarrier, args.pTiledResourceOrViewAccessAfterBarrier);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_TiledResourceBarrier_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::IsAnnotationEnabled
 */

struct npt_command_ID3D11DeviceContext2_IsAnnotationEnabled {
    void *_self;
    BOOL ret;
};

static inline void
npt_decode_ID3D11DeviceContext2_IsAnnotationEnabled_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11DeviceContext2_IsAnnotationEnabled *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11DeviceContext2_IsAnnotationEnabled_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_IsAnnotationEnabled *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext2_IsAnnotationEnabled_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11DeviceContext2_IsAnnotationEnabled *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_IsAnnotationEnabled),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_IsAnnotationEnabled(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_IsAnnotationEnabled args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_IsAnnotationEnabled_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_IsAnnotationEnabled on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_IsAnnotationEnabled_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_IsAnnotationEnabled: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_IsAnnotationEnabled _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_IsAnnotationEnabled, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_IsAnnotationEnabled);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->IsAnnotationEnabled) {
        args.ret = ctx->id3d11devicecontext2_dispatch_overrides->IsAnnotationEnabled(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_IsAnnotationEnabled_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::SetMarkerInt
 */

struct npt_command_ID3D11DeviceContext2_SetMarkerInt {
    void *_self;
    const WCHAR * pLabel;
    INT Data;
};

static inline void
npt_decode_ID3D11DeviceContext2_SetMarkerInt_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_ID3D11DeviceContext2_SetMarkerInt *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->pLabel = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->pLabel) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->pLabel, _wstr_size);
        } else {
            args->pLabel = NULL;
        }
    }
    npt_decode_INT(dec, &args->Data);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext2_SetMarkerInt_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_SetMarkerInt *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext2_SetMarkerInt_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_ID3D11DeviceContext2_SetMarkerInt *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_SetMarkerInt),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_SetMarkerInt(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_SetMarkerInt args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_SetMarkerInt_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_SetMarkerInt on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_SetMarkerInt_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_SetMarkerInt: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_SetMarkerInt _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_SetMarkerInt, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_SetMarkerInt);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->SetMarkerInt) {
        ctx->id3d11devicecontext2_dispatch_overrides->SetMarkerInt(ctx, &args, _original);
    } else {
        _original(args._self, args.pLabel, args.Data);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_SetMarkerInt_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::BeginEventInt
 */

struct npt_command_ID3D11DeviceContext2_BeginEventInt {
    void *_self;
    const WCHAR * pLabel;
    INT Data;
};

static inline void
npt_decode_ID3D11DeviceContext2_BeginEventInt_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D11DeviceContext2_BeginEventInt *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->pLabel = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->pLabel) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->pLabel, _wstr_size);
        } else {
            args->pLabel = NULL;
        }
    }
    npt_decode_INT(dec, &args->Data);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext2_BeginEventInt_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_BeginEventInt *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext2_BeginEventInt_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D11DeviceContext2_BeginEventInt *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_BeginEventInt),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_BeginEventInt(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_BeginEventInt args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_BeginEventInt_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_BeginEventInt on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_BeginEventInt_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_BeginEventInt: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_BeginEventInt _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_BeginEventInt, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_BeginEventInt);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->BeginEventInt) {
        ctx->id3d11devicecontext2_dispatch_overrides->BeginEventInt(ctx, &args, _original);
    } else {
        _original(args._self, args.pLabel, args.Data);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_BeginEventInt_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext2::EndEvent
 */

struct npt_command_ID3D11DeviceContext2_EndEvent {
    void *_self;
};

static inline void
npt_decode_ID3D11DeviceContext2_EndEvent_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11DeviceContext2_EndEvent *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11DeviceContext2_EndEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext2_EndEvent *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext2_EndEvent_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11DeviceContext2_EndEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext2,
                                 NPT_METHOD_ID3D11DeviceContext2_EndEvent),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext2_EndEvent(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext2_EndEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext2_EndEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_EndEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext2_EndEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext2_EndEvent: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext2_EndEvent _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext2_EndEvent, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext2_EndEvent);

    if (ctx->id3d11devicecontext2_dispatch_overrides && ctx->id3d11devicecontext2_dispatch_overrides->EndEvent) {
        ctx->id3d11devicecontext2_dispatch_overrides->EndEvent(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext2_EndEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext3::Flush1
 */

struct npt_command_ID3D11DeviceContext3_Flush1 {
    void *_self;
    D3D11_CONTEXT_TYPE ContextType;
    HANDLE hEvent;
};

static inline void
npt_decode_ID3D11DeviceContext3_Flush1_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D11DeviceContext3_Flush1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D11_CONTEXT_TYPE(dec, &args->ContextType);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext3_Flush1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext3_Flush1 *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_ID3D11DeviceContext3_Flush1_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D11DeviceContext3_Flush1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                 NPT_METHOD_ID3D11DeviceContext3_Flush1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext3_Flush1(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext3_Flush1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext3_Flush1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext3_Flush1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext3_Flush1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext3_Flush1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext3_Flush1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext3_Flush1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext3_Flush1);

    if (ctx->id3d11devicecontext3_dispatch_overrides && ctx->id3d11devicecontext3_dispatch_overrides->Flush1) {
        ctx->id3d11devicecontext3_dispatch_overrides->Flush1(ctx, &args, _original);
    } else {
        _original(args._self, args.ContextType, args.hEvent);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext3_Flush1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext3::SetHardwareProtectionState
 */

struct npt_command_ID3D11DeviceContext3_SetHardwareProtectionState {
    void *_self;
    BOOL HwProtectionEnable;
};

static inline void
npt_decode_ID3D11DeviceContext3_SetHardwareProtectionState_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D11DeviceContext3_SetHardwareProtectionState *args)
{
    /* Decode input parameters from the wire */
    npt_decode_BOOL(dec, &args->HwProtectionEnable);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D11DeviceContext3_SetHardwareProtectionState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext3_SetHardwareProtectionState *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext3_SetHardwareProtectionState_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D11DeviceContext3_SetHardwareProtectionState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                 NPT_METHOD_ID3D11DeviceContext3_SetHardwareProtectionState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext3_SetHardwareProtectionState(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext3_SetHardwareProtectionState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext3_SetHardwareProtectionState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext3_SetHardwareProtectionState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext3_SetHardwareProtectionState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext3_SetHardwareProtectionState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext3_SetHardwareProtectionState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext3_SetHardwareProtectionState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext3_SetHardwareProtectionState);

    if (ctx->id3d11devicecontext3_dispatch_overrides && ctx->id3d11devicecontext3_dispatch_overrides->SetHardwareProtectionState) {
        ctx->id3d11devicecontext3_dispatch_overrides->SetHardwareProtectionState(ctx, &args, _original);
    } else {
        _original(args._self, args.HwProtectionEnable);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext3_SetHardwareProtectionState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext3::GetHardwareProtectionState
 */

struct npt_command_ID3D11DeviceContext3_GetHardwareProtectionState {
    void *_self;
    BOOL * pHwProtectionEnable;
};

static inline void
npt_decode_ID3D11DeviceContext3_GetHardwareProtectionState_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D11DeviceContext3_GetHardwareProtectionState *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pHwProtectionEnable = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pHwProtectionEnable) return;
    memset(args->pHwProtectionEnable, 0, sizeof(BOOL));
}

static inline void
npt_replace_ID3D11DeviceContext3_GetHardwareProtectionState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext3_GetHardwareProtectionState *args)
{
}

static inline void
npt_encode_ID3D11DeviceContext3_GetHardwareProtectionState_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D11DeviceContext3_GetHardwareProtectionState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext3,
                                 NPT_METHOD_ID3D11DeviceContext3_GetHardwareProtectionState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pHwProtectionEnable))
        npt_encode_BOOL(enc, args->pHwProtectionEnable);
}

static inline void
npt_dispatch_ID3D11DeviceContext3_GetHardwareProtectionState(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext3_GetHardwareProtectionState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext3_GetHardwareProtectionState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext3_GetHardwareProtectionState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext3_GetHardwareProtectionState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext3_GetHardwareProtectionState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext3_GetHardwareProtectionState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext3_GetHardwareProtectionState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext3_GetHardwareProtectionState);

    if (ctx->id3d11devicecontext3_dispatch_overrides && ctx->id3d11devicecontext3_dispatch_overrides->GetHardwareProtectionState) {
        ctx->id3d11devicecontext3_dispatch_overrides->GetHardwareProtectionState(ctx, &args, _original);
    } else {
        _original(args._self, args.pHwProtectionEnable);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext3_GetHardwareProtectionState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext4::Signal
 */

struct npt_command_ID3D11DeviceContext4_Signal {
    void *_self;
    ID3D11Fence * pFence;
    UINT64 Value;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext4_Signal_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D11DeviceContext4_Signal *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pFence = (ID3D11Fence *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->Value);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext4_Signal_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext4_Signal *args)
{
    args->pFence = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pFence, NPT_OBJECT_TYPE_ID3D11FENCE);
}

static inline void
npt_encode_ID3D11DeviceContext4_Signal_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D11DeviceContext4_Signal *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                 NPT_METHOD_ID3D11DeviceContext4_Signal),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext4_Signal(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext4_Signal args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext4_Signal_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext4_Signal on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext4_Signal_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext4_Signal: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext4_Signal _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext4_Signal, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext4_Signal);

    if (ctx->id3d11devicecontext4_dispatch_overrides && ctx->id3d11devicecontext4_dispatch_overrides->Signal) {
        args.ret = ctx->id3d11devicecontext4_dispatch_overrides->Signal(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pFence, args.Value);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext4_Signal_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11DeviceContext4::Wait
 */

struct npt_command_ID3D11DeviceContext4_Wait {
    void *_self;
    ID3D11Fence * pFence;
    UINT64 Value;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11DeviceContext4_Wait_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_ID3D11DeviceContext4_Wait *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pFence = (ID3D11Fence *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->Value);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11DeviceContext4_Wait_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11DeviceContext4_Wait *args)
{
    args->pFence = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pFence, NPT_OBJECT_TYPE_ID3D11FENCE);
}

static inline void
npt_encode_ID3D11DeviceContext4_Wait_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_ID3D11DeviceContext4_Wait *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11DeviceContext4,
                                 NPT_METHOD_ID3D11DeviceContext4_Wait),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11DeviceContext4_Wait(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_ID3D11DeviceContext4_Wait args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11DeviceContext4_Wait_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext4_Wait on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11DeviceContext4_Wait_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11DeviceContext4_Wait: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11DeviceContext4_Wait _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11DeviceContext4_Wait, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11DeviceContext4_Wait);

    if (ctx->id3d11devicecontext4_dispatch_overrides && ctx->id3d11devicecontext4_dispatch_overrides->Wait) {
        args.ret = ctx->id3d11devicecontext4_dispatch_overrides->Wait(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pFence, args.Value);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11DeviceContext4_Wait_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11DEVICECONTEXT_H */

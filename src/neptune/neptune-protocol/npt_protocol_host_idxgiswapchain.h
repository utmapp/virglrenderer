/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiswapchain.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGISWAPCHAIN_H
#define NPT_PROTOCOL_HOST_IDXGISWAPCHAIN_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGISwapChain::Present
 */

struct npt_command_IDXGISwapChain_Present {
    void *_self;
    UINT SyncInterval;
    UINT Flags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_Present_args_temp(struct npt_cs_decoder *dec,
                                            struct npt_command_IDXGISwapChain_Present *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->SyncInterval);
    npt_decode_UINT(dec, &args->Flags);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGISwapChain_Present_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_Present *args)
{
}

static inline void
npt_encode_IDXGISwapChain_Present_reply(struct npt_cs_encoder *enc,
                                        const struct npt_command_IDXGISwapChain_Present *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_Present),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain_Present(struct npt_dispatch_context *ctx,
                                    uint32_t cmd_flags,
                                    uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_Present args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_Present_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_Present on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_Present_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_Present: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_Present _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_Present, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_Present);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->Present) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->Present(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.SyncInterval, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_Present_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::GetBuffer
 */

struct npt_command_IDXGISwapChain_GetBuffer {
    void *_self;
    UINT Buffer;
    const IID * riid;
    void ** ppSurface;
    /* Shadow: the guest-allocated id for ppSurface arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSurface -> *ppSurface} in the
     * context object table. */
    uint64_t _guest_id_ppSurface;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_GetBuffer_args_temp(struct npt_cs_decoder *dec,
                                              struct npt_command_IDXGISwapChain_GetBuffer *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Buffer);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSurface = _gid;
        args->ppSurface = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSurface) return;
        *args->ppSurface = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGISwapChain_GetBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_GetBuffer *args)
{
}

static inline void
npt_encode_IDXGISwapChain_GetBuffer_reply(struct npt_cs_encoder *enc,
                                          const struct npt_command_IDXGISwapChain_GetBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetBuffer),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSurface (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGISwapChain_GetBuffer(struct npt_dispatch_context *ctx,
                                      uint32_t cmd_flags,
                                      uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_GetBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_GetBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSurface);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_GetBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetBuffer: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSurface);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_GetBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_GetBuffer, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_GetBuffer);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->GetBuffer) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->GetBuffer(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Buffer, args.riid, args.ppSurface);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSurface && *args.ppSurface)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSurface, *args.ppSurface,
            npt_object_type_from_iid(args.riid));
    else if (args.ppSurface)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSurface);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_GetBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::SetFullscreenState
 */

struct npt_command_IDXGISwapChain_SetFullscreenState {
    void *_self;
    BOOL Fullscreen;
    IDXGIOutput * pTarget;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_SetFullscreenState_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_IDXGISwapChain_SetFullscreenState *args)
{
    /* Decode input parameters from the wire */
    npt_decode_BOOL(dec, &args->Fullscreen);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTarget = (IDXGIOutput *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGISwapChain_SetFullscreenState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_SetFullscreenState *args)
{
    args->pTarget = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTarget, NPT_OBJECT_TYPE_IDXGIOUTPUT);
}

static inline void
npt_encode_IDXGISwapChain_SetFullscreenState_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_IDXGISwapChain_SetFullscreenState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_SetFullscreenState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain_SetFullscreenState(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_SetFullscreenState args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_SetFullscreenState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_SetFullscreenState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_SetFullscreenState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_SetFullscreenState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_SetFullscreenState _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_SetFullscreenState, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_SetFullscreenState);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->SetFullscreenState) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->SetFullscreenState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Fullscreen, args.pTarget);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_SetFullscreenState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::GetFullscreenState
 */

struct npt_command_IDXGISwapChain_GetFullscreenState {
    void *_self;
    BOOL * pFullscreen;
    IDXGIOutput ** ppTarget;
    /* Shadow: the guest-allocated id for ppTarget arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppTarget -> *ppTarget} in the
     * context object table. */
    uint64_t _guest_id_ppTarget;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_GetFullscreenState_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_IDXGISwapChain_GetFullscreenState *args)
{
    /* Decode input parameters from the wire */

    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppTarget = _gid;
        args->ppTarget = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppTarget) return;
        *args->ppTarget = NULL;
    }
    /* Allocate temp storage for output-only parameters */
    args->pFullscreen = npt_cs_decoder_alloc_temp(dec, sizeof(BOOL));
    if (!args->pFullscreen) return;
    memset(args->pFullscreen, 0, sizeof(BOOL));

}

static inline void
npt_replace_IDXGISwapChain_GetFullscreenState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_GetFullscreenState *args)
{
}

static inline void
npt_encode_IDXGISwapChain_GetFullscreenState_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_IDXGISwapChain_GetFullscreenState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetFullscreenState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pFullscreen))
        npt_encode_BOOL(enc, args->pFullscreen);
/* skip args->ppTarget (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGISwapChain_GetFullscreenState(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_GetFullscreenState args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_GetFullscreenState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetFullscreenState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppTarget);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_GetFullscreenState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetFullscreenState: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppTarget);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_GetFullscreenState _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_GetFullscreenState, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_GetFullscreenState);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->GetFullscreenState) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->GetFullscreenState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pFullscreen, args.ppTarget);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppTarget && *args.ppTarget)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppTarget, *args.ppTarget, NPT_OBJECT_TYPE_IDXGIOUTPUT);
    else if (args.ppTarget)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppTarget);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_GetFullscreenState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::GetDesc
 */

struct npt_command_IDXGISwapChain_GetDesc {
    void *_self;
    DXGI_SWAP_CHAIN_DESC * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_GetDesc_args_temp(struct npt_cs_decoder *dec,
                                            struct npt_command_IDXGISwapChain_GetDesc *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_DESC));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_SWAP_CHAIN_DESC));
}

static inline void
npt_replace_IDXGISwapChain_GetDesc_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_GetDesc *args)
{
    if (args->pDesc)
        npt_replace_DXGI_SWAP_CHAIN_DESC_handle(ctx, (DXGI_SWAP_CHAIN_DESC *)args->pDesc);
}

static inline void
npt_encode_IDXGISwapChain_GetDesc_reply(struct npt_cs_encoder *enc,
                                        const struct npt_command_IDXGISwapChain_GetDesc *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetDesc),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGISwapChain_GetDesc(struct npt_dispatch_context *ctx,
                                    uint32_t cmd_flags,
                                    uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_GetDesc args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_GetDesc_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetDesc on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_GetDesc_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetDesc: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_GetDesc _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_GetDesc, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_GetDesc);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->GetDesc) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->GetDesc(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_GetDesc_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::ResizeBuffers
 */

struct npt_command_IDXGISwapChain_ResizeBuffers {
    void *_self;
    UINT BufferCount;
    UINT Width;
    UINT Height;
    DXGI_FORMAT NewFormat;
    UINT SwapChainFlags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_ResizeBuffers_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_IDXGISwapChain_ResizeBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->BufferCount);
    npt_decode_UINT(dec, &args->Width);
    npt_decode_UINT(dec, &args->Height);
    npt_decode_DXGI_FORMAT(dec, &args->NewFormat);
    npt_decode_UINT(dec, &args->SwapChainFlags);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_IDXGISwapChain_ResizeBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_ResizeBuffers *args)
{
}

static inline void
npt_encode_IDXGISwapChain_ResizeBuffers_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_IDXGISwapChain_ResizeBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_ResizeBuffers),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain_ResizeBuffers(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_ResizeBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_ResizeBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_ResizeBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_ResizeBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_ResizeBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_ResizeBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_ResizeBuffers, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_ResizeBuffers);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->ResizeBuffers) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->ResizeBuffers(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.BufferCount, args.Width, args.Height, args.NewFormat, args.SwapChainFlags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_ResizeBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::ResizeTarget
 */

struct npt_command_IDXGISwapChain_ResizeTarget {
    void *_self;
    const DXGI_MODE_DESC * pNewTargetParameters;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_ResizeTarget_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_IDXGISwapChain_ResizeTarget *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pNewTargetParameters = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MODE_DESC));
        if (!args->pNewTargetParameters) return;
        npt_decode_DXGI_MODE_DESC(dec, (DXGI_MODE_DESC *)args->pNewTargetParameters);
    } else {
        args->pNewTargetParameters = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain_ResizeTarget_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_ResizeTarget *args)
{
}

static inline void
npt_encode_IDXGISwapChain_ResizeTarget_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_IDXGISwapChain_ResizeTarget *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_ResizeTarget),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain_ResizeTarget(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_ResizeTarget args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_ResizeTarget_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_ResizeTarget on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_ResizeTarget_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_ResizeTarget: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_ResizeTarget _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_ResizeTarget, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_ResizeTarget);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->ResizeTarget) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->ResizeTarget(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pNewTargetParameters);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_ResizeTarget_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::GetContainingOutput
 */

struct npt_command_IDXGISwapChain_GetContainingOutput {
    void *_self;
    IDXGIOutput ** ppOutput;
    /* Shadow: the guest-allocated id for ppOutput arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutput -> *ppOutput} in the
     * context object table. */
    uint64_t _guest_id_ppOutput;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_GetContainingOutput_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGISwapChain_GetContainingOutput *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppOutput = _gid;
        args->ppOutput = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppOutput) return;
        *args->ppOutput = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain_GetContainingOutput_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_GetContainingOutput *args)
{
}

static inline void
npt_encode_IDXGISwapChain_GetContainingOutput_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGISwapChain_GetContainingOutput *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetContainingOutput),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppOutput (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGISwapChain_GetContainingOutput(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_GetContainingOutput args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_GetContainingOutput_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetContainingOutput on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppOutput);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_GetContainingOutput_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetContainingOutput: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppOutput);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_GetContainingOutput _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_GetContainingOutput, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_GetContainingOutput);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->GetContainingOutput) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->GetContainingOutput(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ppOutput);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppOutput && *args.ppOutput)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppOutput, *args.ppOutput, NPT_OBJECT_TYPE_IDXGIOUTPUT);
    else if (args.ppOutput)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppOutput);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_GetContainingOutput_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::GetFrameStatistics
 */

struct npt_command_IDXGISwapChain_GetFrameStatistics {
    void *_self;
    DXGI_FRAME_STATISTICS * pStats;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_GetFrameStatistics_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_IDXGISwapChain_GetFrameStatistics *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pStats = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_FRAME_STATISTICS));
    if (!args->pStats) return;
    memset(args->pStats, 0, sizeof(DXGI_FRAME_STATISTICS));
}

static inline void
npt_replace_IDXGISwapChain_GetFrameStatistics_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_GetFrameStatistics *args)
{
}

static inline void
npt_encode_IDXGISwapChain_GetFrameStatistics_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_IDXGISwapChain_GetFrameStatistics *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetFrameStatistics),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pStats))
        npt_encode_DXGI_FRAME_STATISTICS(enc, args->pStats);
}

static inline void
npt_dispatch_IDXGISwapChain_GetFrameStatistics(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_GetFrameStatistics args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_GetFrameStatistics_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetFrameStatistics on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_GetFrameStatistics_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetFrameStatistics: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_GetFrameStatistics _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_GetFrameStatistics, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_GetFrameStatistics);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->GetFrameStatistics) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->GetFrameStatistics(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pStats);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_GetFrameStatistics_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain::GetLastPresentCount
 */

struct npt_command_IDXGISwapChain_GetLastPresentCount {
    void *_self;
    UINT * pLastPresentCount;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain_GetLastPresentCount_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGISwapChain_GetLastPresentCount *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pLastPresentCount = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pLastPresentCount) return;
    memset(args->pLastPresentCount, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGISwapChain_GetLastPresentCount_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain_GetLastPresentCount *args)
{
}

static inline void
npt_encode_IDXGISwapChain_GetLastPresentCount_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGISwapChain_GetLastPresentCount *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain,
                                 NPT_METHOD_IDXGISwapChain_GetLastPresentCount),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pLastPresentCount))
        npt_encode_UINT(enc, args->pLastPresentCount);
}

static inline void
npt_dispatch_IDXGISwapChain_GetLastPresentCount(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGISwapChain_GetLastPresentCount args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain_GetLastPresentCount_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetLastPresentCount on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain_GetLastPresentCount_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain_GetLastPresentCount: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain_GetLastPresentCount _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain_GetLastPresentCount, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain_GetLastPresentCount);

    if (ctx->idxgiswapchain_dispatch_overrides && ctx->idxgiswapchain_dispatch_overrides->GetLastPresentCount) {
        args.ret = ctx->idxgiswapchain_dispatch_overrides->GetLastPresentCount(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pLastPresentCount);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain_GetLastPresentCount_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::GetDesc1
 */

struct npt_command_IDXGISwapChain1_GetDesc1 {
    void *_self;
    DXGI_SWAP_CHAIN_DESC1 * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_GetDesc1_args_temp(struct npt_cs_decoder *dec,
                                              struct npt_command_IDXGISwapChain1_GetDesc1 *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_DESC1));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_SWAP_CHAIN_DESC1));
}

static inline void
npt_replace_IDXGISwapChain1_GetDesc1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_GetDesc1 *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_GetDesc1_reply(struct npt_cs_encoder *enc,
                                          const struct npt_command_IDXGISwapChain1_GetDesc1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetDesc1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_SWAP_CHAIN_DESC1(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGISwapChain1_GetDesc1(struct npt_dispatch_context *ctx,
                                      uint32_t cmd_flags,
                                      uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_GetDesc1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_GetDesc1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetDesc1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_GetDesc1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetDesc1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_GetDesc1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_GetDesc1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_GetDesc1);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->GetDesc1) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->GetDesc1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_GetDesc1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::GetFullscreenDesc
 */

struct npt_command_IDXGISwapChain1_GetFullscreenDesc {
    void *_self;
    DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_GetFullscreenDesc_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_IDXGISwapChain1_GetFullscreenDesc *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_FULLSCREEN_DESC));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_SWAP_CHAIN_FULLSCREEN_DESC));
}

static inline void
npt_replace_IDXGISwapChain1_GetFullscreenDesc_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_GetFullscreenDesc *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_GetFullscreenDesc_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_IDXGISwapChain1_GetFullscreenDesc *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetFullscreenDesc),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_SWAP_CHAIN_FULLSCREEN_DESC(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGISwapChain1_GetFullscreenDesc(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_GetFullscreenDesc args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_GetFullscreenDesc_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetFullscreenDesc on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_GetFullscreenDesc_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetFullscreenDesc: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_GetFullscreenDesc _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_GetFullscreenDesc, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_GetFullscreenDesc);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->GetFullscreenDesc) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->GetFullscreenDesc(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_GetFullscreenDesc_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::GetHwnd
 */

struct npt_command_IDXGISwapChain1_GetHwnd {
    void *_self;
    HWND * pHwnd;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_GetHwnd_args_temp(struct npt_cs_decoder *dec,
                                             struct npt_command_IDXGISwapChain1_GetHwnd *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pHwnd = npt_cs_decoder_alloc_temp(dec, sizeof(HWND));
    if (!args->pHwnd) return;
    memset(args->pHwnd, 0, sizeof(HWND));
}

static inline void
npt_replace_IDXGISwapChain1_GetHwnd_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_GetHwnd *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_GetHwnd_reply(struct npt_cs_encoder *enc,
                                         const struct npt_command_IDXGISwapChain1_GetHwnd *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetHwnd),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)*args->pHwnd));
}

static inline void
npt_dispatch_IDXGISwapChain1_GetHwnd(struct npt_dispatch_context *ctx,
                                     uint32_t cmd_flags,
                                     uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_GetHwnd args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_GetHwnd_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetHwnd on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_GetHwnd_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetHwnd: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_GetHwnd _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_GetHwnd, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_GetHwnd);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->GetHwnd) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->GetHwnd(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pHwnd);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_GetHwnd_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::GetCoreWindow
 */

struct npt_command_IDXGISwapChain1_GetCoreWindow {
    void *_self;
    const IID * refiid;
    void ** ppUnk;
    /* Shadow: the guest-allocated id for ppUnk arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppUnk -> *ppUnk} in the
     * context object table. */
    uint64_t _guest_id_ppUnk;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_GetCoreWindow_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGISwapChain1_GetCoreWindow *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->refiid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->refiid) return;
        npt_decode_IID(dec, (IID *)args->refiid);
    } else {
        args->refiid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppUnk = _gid;
        args->ppUnk = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppUnk) return;
        *args->ppUnk = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGISwapChain1_GetCoreWindow_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_GetCoreWindow *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_GetCoreWindow_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGISwapChain1_GetCoreWindow *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetCoreWindow),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppUnk (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGISwapChain1_GetCoreWindow(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_GetCoreWindow args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_GetCoreWindow_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetCoreWindow on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppUnk);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_GetCoreWindow_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetCoreWindow: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppUnk);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_GetCoreWindow _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_GetCoreWindow, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_GetCoreWindow);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->GetCoreWindow) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->GetCoreWindow(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.refiid, args.ppUnk);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppUnk && *args.ppUnk)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppUnk, *args.ppUnk,
            npt_object_type_from_iid(args.refiid));
    else if (args.ppUnk)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppUnk);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_GetCoreWindow_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::Present1
 */

struct npt_command_IDXGISwapChain1_Present1 {
    void *_self;
    UINT SyncInterval;
    UINT PresentFlags;
    const DXGI_PRESENT_PARAMETERS * pPresentParameters;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_Present1_args_temp(struct npt_cs_decoder *dec,
                                              struct npt_command_IDXGISwapChain1_Present1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->SyncInterval);
    npt_decode_UINT(dec, &args->PresentFlags);
    if (npt_decode_simple_pointer(dec)) {
        args->pPresentParameters = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_PRESENT_PARAMETERS));
        if (!args->pPresentParameters) return;
        npt_decode_DXGI_PRESENT_PARAMETERS(dec, (DXGI_PRESENT_PARAMETERS *)args->pPresentParameters);
    } else {
        args->pPresentParameters = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGISwapChain1_Present1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_Present1 *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_Present1_reply(struct npt_cs_encoder *enc,
                                          const struct npt_command_IDXGISwapChain1_Present1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_Present1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain1_Present1(struct npt_dispatch_context *ctx,
                                      uint32_t cmd_flags,
                                      uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_Present1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_Present1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_Present1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_Present1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_Present1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_Present1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_Present1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_Present1);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->Present1) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->Present1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.SyncInterval, args.PresentFlags, args.pPresentParameters);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_Present1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::IsTemporaryMonoSupported
 */

struct npt_command_IDXGISwapChain1_IsTemporaryMonoSupported {
    void *_self;
    BOOL ret;
};

static inline void
npt_decode_IDXGISwapChain1_IsTemporaryMonoSupported_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_IDXGISwapChain1_IsTemporaryMonoSupported *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGISwapChain1_IsTemporaryMonoSupported_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_IsTemporaryMonoSupported *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_IsTemporaryMonoSupported_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_IDXGISwapChain1_IsTemporaryMonoSupported *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_IsTemporaryMonoSupported),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain1_IsTemporaryMonoSupported(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_IsTemporaryMonoSupported args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_IsTemporaryMonoSupported_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_IsTemporaryMonoSupported on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_IsTemporaryMonoSupported_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_IsTemporaryMonoSupported: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_IsTemporaryMonoSupported _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_IsTemporaryMonoSupported, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_IsTemporaryMonoSupported);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->IsTemporaryMonoSupported) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->IsTemporaryMonoSupported(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_IsTemporaryMonoSupported_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::GetRestrictToOutput
 */

struct npt_command_IDXGISwapChain1_GetRestrictToOutput {
    void *_self;
    IDXGIOutput ** ppRestrictToOutput;
    /* Shadow: the guest-allocated id for ppRestrictToOutput arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppRestrictToOutput -> *ppRestrictToOutput} in the
     * context object table. */
    uint64_t _guest_id_ppRestrictToOutput;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_GetRestrictToOutput_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_IDXGISwapChain1_GetRestrictToOutput *args)
{
    /* Decode input parameters from the wire */
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppRestrictToOutput = _gid;
        args->ppRestrictToOutput = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppRestrictToOutput) return;
        *args->ppRestrictToOutput = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain1_GetRestrictToOutput_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_GetRestrictToOutput *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_GetRestrictToOutput_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_IDXGISwapChain1_GetRestrictToOutput *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetRestrictToOutput),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppRestrictToOutput (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGISwapChain1_GetRestrictToOutput(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_GetRestrictToOutput args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_GetRestrictToOutput_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetRestrictToOutput on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppRestrictToOutput);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_GetRestrictToOutput_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetRestrictToOutput: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppRestrictToOutput);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_GetRestrictToOutput _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_GetRestrictToOutput, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_GetRestrictToOutput);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->GetRestrictToOutput) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->GetRestrictToOutput(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ppRestrictToOutput);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppRestrictToOutput && *args.ppRestrictToOutput)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppRestrictToOutput, *args.ppRestrictToOutput, NPT_OBJECT_TYPE_IDXGIOUTPUT);
    else if (args.ppRestrictToOutput)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppRestrictToOutput);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_GetRestrictToOutput_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::SetBackgroundColor
 */

struct npt_command_IDXGISwapChain1_SetBackgroundColor {
    void *_self;
    const DXGI_RGBA * pColor;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_SetBackgroundColor_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGISwapChain1_SetBackgroundColor *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pColor = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RGBA));
        if (!args->pColor) return;
        npt_decode_DXGI_RGBA(dec, (DXGI_RGBA *)args->pColor);
    } else {
        args->pColor = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain1_SetBackgroundColor_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_SetBackgroundColor *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_SetBackgroundColor_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGISwapChain1_SetBackgroundColor *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_SetBackgroundColor),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain1_SetBackgroundColor(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_SetBackgroundColor args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_SetBackgroundColor_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_SetBackgroundColor on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_SetBackgroundColor_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_SetBackgroundColor: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_SetBackgroundColor _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_SetBackgroundColor, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_SetBackgroundColor);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->SetBackgroundColor) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->SetBackgroundColor(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pColor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_SetBackgroundColor_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::GetBackgroundColor
 */

struct npt_command_IDXGISwapChain1_GetBackgroundColor {
    void *_self;
    DXGI_RGBA * pColor;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_GetBackgroundColor_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGISwapChain1_GetBackgroundColor *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pColor = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_RGBA));
    if (!args->pColor) return;
    memset(args->pColor, 0, sizeof(DXGI_RGBA));
}

static inline void
npt_replace_IDXGISwapChain1_GetBackgroundColor_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_GetBackgroundColor *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_GetBackgroundColor_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGISwapChain1_GetBackgroundColor *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetBackgroundColor),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pColor))
        npt_encode_DXGI_RGBA(enc, args->pColor);
}

static inline void
npt_dispatch_IDXGISwapChain1_GetBackgroundColor(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_GetBackgroundColor args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_GetBackgroundColor_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetBackgroundColor on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_GetBackgroundColor_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetBackgroundColor: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_GetBackgroundColor _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_GetBackgroundColor, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_GetBackgroundColor);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->GetBackgroundColor) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->GetBackgroundColor(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pColor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_GetBackgroundColor_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::SetRotation
 */

struct npt_command_IDXGISwapChain1_SetRotation {
    void *_self;
    DXGI_MODE_ROTATION Rotation;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_SetRotation_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_IDXGISwapChain1_SetRotation *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_MODE_ROTATION(dec, &args->Rotation);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain1_SetRotation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_SetRotation *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_SetRotation_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_IDXGISwapChain1_SetRotation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_SetRotation),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain1_SetRotation(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_SetRotation args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_SetRotation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_SetRotation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_SetRotation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_SetRotation: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_SetRotation _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_SetRotation, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_SetRotation);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->SetRotation) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->SetRotation(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Rotation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_SetRotation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain1::GetRotation
 */

struct npt_command_IDXGISwapChain1_GetRotation {
    void *_self;
    DXGI_MODE_ROTATION * pRotation;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain1_GetRotation_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_IDXGISwapChain1_GetRotation *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pRotation = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MODE_ROTATION));
    if (!args->pRotation) return;
    memset(args->pRotation, 0, sizeof(DXGI_MODE_ROTATION));
}

static inline void
npt_replace_IDXGISwapChain1_GetRotation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain1_GetRotation *args)
{
}

static inline void
npt_encode_IDXGISwapChain1_GetRotation_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_IDXGISwapChain1_GetRotation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain1,
                                 NPT_METHOD_IDXGISwapChain1_GetRotation),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pRotation))
        npt_encode_DXGI_MODE_ROTATION(enc, args->pRotation);
}

static inline void
npt_dispatch_IDXGISwapChain1_GetRotation(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_IDXGISwapChain1_GetRotation args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain1_GetRotation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetRotation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain1_GetRotation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain1_GetRotation: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain1_GetRotation _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain1_GetRotation, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain1_GetRotation);

    if (ctx->idxgiswapchain1_dispatch_overrides && ctx->idxgiswapchain1_dispatch_overrides->GetRotation) {
        args.ret = ctx->idxgiswapchain1_dispatch_overrides->GetRotation(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pRotation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain1_GetRotation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain2::SetSourceSize
 */

struct npt_command_IDXGISwapChain2_SetSourceSize {
    void *_self;
    UINT Width;
    UINT Height;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain2_SetSourceSize_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGISwapChain2_SetSourceSize *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Width);
    npt_decode_UINT(dec, &args->Height);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGISwapChain2_SetSourceSize_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain2_SetSourceSize *args)
{
}

static inline void
npt_encode_IDXGISwapChain2_SetSourceSize_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGISwapChain2_SetSourceSize *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_SetSourceSize),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain2_SetSourceSize(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGISwapChain2_SetSourceSize args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain2_SetSourceSize_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_SetSourceSize on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain2_SetSourceSize_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_SetSourceSize: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain2_SetSourceSize _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain2_SetSourceSize, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain2_SetSourceSize);

    if (ctx->idxgiswapchain2_dispatch_overrides && ctx->idxgiswapchain2_dispatch_overrides->SetSourceSize) {
        args.ret = ctx->idxgiswapchain2_dispatch_overrides->SetSourceSize(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Width, args.Height);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain2_SetSourceSize_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain2::GetSourceSize
 */

struct npt_command_IDXGISwapChain2_GetSourceSize {
    void *_self;
    UINT * pWidth;
    UINT * pHeight;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain2_GetSourceSize_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGISwapChain2_GetSourceSize *args)
{
    /* Decode input parameters from the wire */


    /* Allocate temp storage for output-only parameters */
    args->pWidth = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pWidth) return;
    memset(args->pWidth, 0, sizeof(UINT));
    args->pHeight = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pHeight) return;
    memset(args->pHeight, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGISwapChain2_GetSourceSize_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain2_GetSourceSize *args)
{
}

static inline void
npt_encode_IDXGISwapChain2_GetSourceSize_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGISwapChain2_GetSourceSize *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetSourceSize),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pWidth))
        npt_encode_UINT(enc, args->pWidth);
    if (npt_encode_simple_pointer(enc, args->pHeight))
        npt_encode_UINT(enc, args->pHeight);
}

static inline void
npt_dispatch_IDXGISwapChain2_GetSourceSize(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGISwapChain2_GetSourceSize args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain2_GetSourceSize_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetSourceSize on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain2_GetSourceSize_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetSourceSize: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain2_GetSourceSize _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain2_GetSourceSize, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain2_GetSourceSize);

    if (ctx->idxgiswapchain2_dispatch_overrides && ctx->idxgiswapchain2_dispatch_overrides->GetSourceSize) {
        args.ret = ctx->idxgiswapchain2_dispatch_overrides->GetSourceSize(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pWidth, args.pHeight);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain2_GetSourceSize_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain2::SetMaximumFrameLatency
 */

struct npt_command_IDXGISwapChain2_SetMaximumFrameLatency {
    void *_self;
    UINT MaxLatency;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain2_SetMaximumFrameLatency_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_IDXGISwapChain2_SetMaximumFrameLatency *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->MaxLatency);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain2_SetMaximumFrameLatency_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain2_SetMaximumFrameLatency *args)
{
}

static inline void
npt_encode_IDXGISwapChain2_SetMaximumFrameLatency_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_IDXGISwapChain2_SetMaximumFrameLatency *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_SetMaximumFrameLatency),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain2_SetMaximumFrameLatency(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_IDXGISwapChain2_SetMaximumFrameLatency args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain2_SetMaximumFrameLatency_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_SetMaximumFrameLatency on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain2_SetMaximumFrameLatency_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_SetMaximumFrameLatency: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain2_SetMaximumFrameLatency _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain2_SetMaximumFrameLatency, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain2_SetMaximumFrameLatency);

    if (ctx->idxgiswapchain2_dispatch_overrides && ctx->idxgiswapchain2_dispatch_overrides->SetMaximumFrameLatency) {
        args.ret = ctx->idxgiswapchain2_dispatch_overrides->SetMaximumFrameLatency(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.MaxLatency);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain2_SetMaximumFrameLatency_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain2::GetMaximumFrameLatency
 */

struct npt_command_IDXGISwapChain2_GetMaximumFrameLatency {
    void *_self;
    UINT * pMaxLatency;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain2_GetMaximumFrameLatency_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_IDXGISwapChain2_GetMaximumFrameLatency *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pMaxLatency = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pMaxLatency) return;
    memset(args->pMaxLatency, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGISwapChain2_GetMaximumFrameLatency_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain2_GetMaximumFrameLatency *args)
{
}

static inline void
npt_encode_IDXGISwapChain2_GetMaximumFrameLatency_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_IDXGISwapChain2_GetMaximumFrameLatency *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetMaximumFrameLatency),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pMaxLatency))
        npt_encode_UINT(enc, args->pMaxLatency);
}

static inline void
npt_dispatch_IDXGISwapChain2_GetMaximumFrameLatency(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_IDXGISwapChain2_GetMaximumFrameLatency args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain2_GetMaximumFrameLatency_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetMaximumFrameLatency on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain2_GetMaximumFrameLatency_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetMaximumFrameLatency: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain2_GetMaximumFrameLatency _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain2_GetMaximumFrameLatency, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain2_GetMaximumFrameLatency);

    if (ctx->idxgiswapchain2_dispatch_overrides && ctx->idxgiswapchain2_dispatch_overrides->GetMaximumFrameLatency) {
        args.ret = ctx->idxgiswapchain2_dispatch_overrides->GetMaximumFrameLatency(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pMaxLatency);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain2_GetMaximumFrameLatency_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain2::GetFrameLatencyWaitableObject
 */

struct npt_command_IDXGISwapChain2_GetFrameLatencyWaitableObject {
    void *_self;
    HANDLE ret;
};

static inline void
npt_decode_IDXGISwapChain2_GetFrameLatencyWaitableObject_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_IDXGISwapChain2_GetFrameLatencyWaitableObject *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGISwapChain2_GetFrameLatencyWaitableObject_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain2_GetFrameLatencyWaitableObject *args)
{
}

static inline void
npt_encode_IDXGISwapChain2_GetFrameLatencyWaitableObject_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_IDXGISwapChain2_GetFrameLatencyWaitableObject *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetFrameLatencyWaitableObject),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_HANDLE(enc, &args->ret);
}

static inline void
npt_dispatch_IDXGISwapChain2_GetFrameLatencyWaitableObject(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_IDXGISwapChain2_GetFrameLatencyWaitableObject args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain2_GetFrameLatencyWaitableObject_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetFrameLatencyWaitableObject on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain2_GetFrameLatencyWaitableObject_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetFrameLatencyWaitableObject: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain2_GetFrameLatencyWaitableObject _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain2_GetFrameLatencyWaitableObject, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain2_GetFrameLatencyWaitableObject);

    if (ctx->idxgiswapchain2_dispatch_overrides && ctx->idxgiswapchain2_dispatch_overrides->GetFrameLatencyWaitableObject) {
        args.ret = ctx->idxgiswapchain2_dispatch_overrides->GetFrameLatencyWaitableObject(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain2_GetFrameLatencyWaitableObject_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain2::SetMatrixTransform
 */

struct npt_command_IDXGISwapChain2_SetMatrixTransform {
    void *_self;
    const DXGI_MATRIX_3X2_F * pMatrix;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain2_SetMatrixTransform_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGISwapChain2_SetMatrixTransform *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pMatrix = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MATRIX_3X2_F));
        if (!args->pMatrix) return;
        npt_decode_DXGI_MATRIX_3X2_F(dec, (DXGI_MATRIX_3X2_F *)args->pMatrix);
    } else {
        args->pMatrix = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain2_SetMatrixTransform_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain2_SetMatrixTransform *args)
{
}

static inline void
npt_encode_IDXGISwapChain2_SetMatrixTransform_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGISwapChain2_SetMatrixTransform *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_SetMatrixTransform),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain2_SetMatrixTransform(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGISwapChain2_SetMatrixTransform args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain2_SetMatrixTransform_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_SetMatrixTransform on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain2_SetMatrixTransform_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_SetMatrixTransform: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain2_SetMatrixTransform _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain2_SetMatrixTransform, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain2_SetMatrixTransform);

    if (ctx->idxgiswapchain2_dispatch_overrides && ctx->idxgiswapchain2_dispatch_overrides->SetMatrixTransform) {
        args.ret = ctx->idxgiswapchain2_dispatch_overrides->SetMatrixTransform(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pMatrix);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain2_SetMatrixTransform_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain2::GetMatrixTransform
 */

struct npt_command_IDXGISwapChain2_GetMatrixTransform {
    void *_self;
    DXGI_MATRIX_3X2_F * pMatrix;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain2_GetMatrixTransform_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGISwapChain2_GetMatrixTransform *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pMatrix = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_MATRIX_3X2_F));
    if (!args->pMatrix) return;
    memset(args->pMatrix, 0, sizeof(DXGI_MATRIX_3X2_F));
}

static inline void
npt_replace_IDXGISwapChain2_GetMatrixTransform_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain2_GetMatrixTransform *args)
{
}

static inline void
npt_encode_IDXGISwapChain2_GetMatrixTransform_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGISwapChain2_GetMatrixTransform *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain2,
                                 NPT_METHOD_IDXGISwapChain2_GetMatrixTransform),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pMatrix))
        npt_encode_DXGI_MATRIX_3X2_F(enc, args->pMatrix);
}

static inline void
npt_dispatch_IDXGISwapChain2_GetMatrixTransform(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGISwapChain2_GetMatrixTransform args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain2_GetMatrixTransform_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetMatrixTransform on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain2_GetMatrixTransform_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain2_GetMatrixTransform: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain2_GetMatrixTransform _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain2_GetMatrixTransform, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain2_GetMatrixTransform);

    if (ctx->idxgiswapchain2_dispatch_overrides && ctx->idxgiswapchain2_dispatch_overrides->GetMatrixTransform) {
        args.ret = ctx->idxgiswapchain2_dispatch_overrides->GetMatrixTransform(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pMatrix);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain2_GetMatrixTransform_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain3::GetCurrentBackBufferIndex
 */

struct npt_command_IDXGISwapChain3_GetCurrentBackBufferIndex {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_IDXGISwapChain3_GetCurrentBackBufferIndex_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_IDXGISwapChain3_GetCurrentBackBufferIndex *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGISwapChain3_GetCurrentBackBufferIndex_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain3_GetCurrentBackBufferIndex *args)
{
}

static inline void
npt_encode_IDXGISwapChain3_GetCurrentBackBufferIndex_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_IDXGISwapChain3_GetCurrentBackBufferIndex *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_GetCurrentBackBufferIndex),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain3_GetCurrentBackBufferIndex(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_IDXGISwapChain3_GetCurrentBackBufferIndex args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain3_GetCurrentBackBufferIndex_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_GetCurrentBackBufferIndex on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain3_GetCurrentBackBufferIndex_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_GetCurrentBackBufferIndex: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain3_GetCurrentBackBufferIndex _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain3_GetCurrentBackBufferIndex, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain3_GetCurrentBackBufferIndex);

    if (ctx->idxgiswapchain3_dispatch_overrides && ctx->idxgiswapchain3_dispatch_overrides->GetCurrentBackBufferIndex) {
        args.ret = ctx->idxgiswapchain3_dispatch_overrides->GetCurrentBackBufferIndex(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain3_GetCurrentBackBufferIndex_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain3::CheckColorSpaceSupport
 */

struct npt_command_IDXGISwapChain3_CheckColorSpaceSupport {
    void *_self;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
    UINT * pColorSpaceSupport;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain3_CheckColorSpaceSupport_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_IDXGISwapChain3_CheckColorSpaceSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->ColorSpace);

    /* Allocate temp storage for output-only parameters */

    args->pColorSpaceSupport = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
    if (!args->pColorSpaceSupport) return;
    memset(args->pColorSpaceSupport, 0, sizeof(UINT));
}

static inline void
npt_replace_IDXGISwapChain3_CheckColorSpaceSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain3_CheckColorSpaceSupport *args)
{
}

static inline void
npt_encode_IDXGISwapChain3_CheckColorSpaceSupport_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_IDXGISwapChain3_CheckColorSpaceSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_CheckColorSpaceSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pColorSpaceSupport))
        npt_encode_UINT(enc, args->pColorSpaceSupport);
}

static inline void
npt_dispatch_IDXGISwapChain3_CheckColorSpaceSupport(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_IDXGISwapChain3_CheckColorSpaceSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain3_CheckColorSpaceSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_CheckColorSpaceSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain3_CheckColorSpaceSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_CheckColorSpaceSupport: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain3_CheckColorSpaceSupport _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain3_CheckColorSpaceSupport, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain3_CheckColorSpaceSupport);

    if (ctx->idxgiswapchain3_dispatch_overrides && ctx->idxgiswapchain3_dispatch_overrides->CheckColorSpaceSupport) {
        args.ret = ctx->idxgiswapchain3_dispatch_overrides->CheckColorSpaceSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ColorSpace, args.pColorSpaceSupport);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain3_CheckColorSpaceSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain3::SetColorSpace1
 */

struct npt_command_IDXGISwapChain3_SetColorSpace1 {
    void *_self;
    DXGI_COLOR_SPACE_TYPE ColorSpace;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain3_SetColorSpace1_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_IDXGISwapChain3_SetColorSpace1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_COLOR_SPACE_TYPE(dec, &args->ColorSpace);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGISwapChain3_SetColorSpace1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain3_SetColorSpace1 *args)
{
}

static inline void
npt_encode_IDXGISwapChain3_SetColorSpace1_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_IDXGISwapChain3_SetColorSpace1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_SetColorSpace1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain3_SetColorSpace1(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_IDXGISwapChain3_SetColorSpace1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain3_SetColorSpace1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_SetColorSpace1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain3_SetColorSpace1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_SetColorSpace1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain3_SetColorSpace1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain3_SetColorSpace1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain3_SetColorSpace1);

    if (ctx->idxgiswapchain3_dispatch_overrides && ctx->idxgiswapchain3_dispatch_overrides->SetColorSpace1) {
        args.ret = ctx->idxgiswapchain3_dispatch_overrides->SetColorSpace1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.ColorSpace);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain3_SetColorSpace1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain3::ResizeBuffers1
 */

struct npt_command_IDXGISwapChain3_ResizeBuffers1 {
    void *_self;
    UINT BufferCount;
    UINT Width;
    UINT Height;
    DXGI_FORMAT Format;
    UINT SwapChainFlags;
    const UINT * pCreationNodeMask;
    IUnknown ** ppPresentQueue;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain3_ResizeBuffers1_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_IDXGISwapChain3_ResizeBuffers1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->BufferCount);
    npt_decode_UINT(dec, &args->Width);
    npt_decode_UINT(dec, &args->Height);
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    npt_decode_UINT(dec, &args->SwapChainFlags);
    uint64_t _cnt_pCreationNodeMask = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pCreationNodeMask = npt_decode_array_count_unchecked(dec);
        args->pCreationNodeMask = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pCreationNodeMask);
        if (!args->pCreationNodeMask) return;
        npt_decode_UINT_array(dec, (UINT *)args->pCreationNodeMask, _cnt_pCreationNodeMask);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pCreationNodeMask = NULL;
    }
    uint64_t _cnt_ppPresentQueue = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppPresentQueue) {
        args->ppPresentQueue = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppPresentQueue);
        if (!args->ppPresentQueue) return;
        for (uint64_t _i = 0; _i < _cnt_ppPresentQueue; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppPresentQueue[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppPresentQueue = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pCreationNodeMask && _cnt_pCreationNodeMask < (uint64_t)(args->BufferCount)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_ppPresentQueue && _cnt_ppPresentQueue < (uint64_t)(args->BufferCount)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_IDXGISwapChain3_ResizeBuffers1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain3_ResizeBuffers1 *args)
{
    if (args->ppPresentQueue) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->BufferCount); _i++)
            args->ppPresentQueue[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppPresentQueue[_i], NPT_OBJECT_TYPE_IUNKNOWN);
    }
}

static inline void
npt_encode_IDXGISwapChain3_ResizeBuffers1_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_IDXGISwapChain3_ResizeBuffers1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain3,
                                 NPT_METHOD_IDXGISwapChain3_ResizeBuffers1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain3_ResizeBuffers1(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_IDXGISwapChain3_ResizeBuffers1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain3_ResizeBuffers1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_ResizeBuffers1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain3_ResizeBuffers1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain3_ResizeBuffers1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain3_ResizeBuffers1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain3_ResizeBuffers1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain3_ResizeBuffers1);

    if (ctx->idxgiswapchain3_dispatch_overrides && ctx->idxgiswapchain3_dispatch_overrides->ResizeBuffers1) {
        args.ret = ctx->idxgiswapchain3_dispatch_overrides->ResizeBuffers1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.BufferCount, args.Width, args.Height, args.Format, args.SwapChainFlags, args.pCreationNodeMask, args.ppPresentQueue);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain3_ResizeBuffers1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGISwapChain4::SetHDRMetaData
 */

struct npt_command_IDXGISwapChain4_SetHDRMetaData {
    void *_self;
    DXGI_HDR_METADATA_TYPE Type;
    UINT Size;
    void * pMetaData;
    HRESULT ret;
};

static inline void
npt_decode_IDXGISwapChain4_SetHDRMetaData_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_IDXGISwapChain4_SetHDRMetaData *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_HDR_METADATA_TYPE(dec, &args->Type);
    npt_decode_UINT(dec, &args->Size);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pMetaData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pMetaData) return;
            npt_decode_blob_array(dec, (void *)args->pMetaData, _blob_size);
        } else {
            args->pMetaData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGISwapChain4_SetHDRMetaData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGISwapChain4_SetHDRMetaData *args)
{
}

static inline void
npt_encode_IDXGISwapChain4_SetHDRMetaData_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_IDXGISwapChain4_SetHDRMetaData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGISwapChain4,
                                 NPT_METHOD_IDXGISwapChain4_SetHDRMetaData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGISwapChain4_SetHDRMetaData(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_IDXGISwapChain4_SetHDRMetaData args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGISwapChain4_SetHDRMetaData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGISWAPCHAIN4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain4_SetHDRMetaData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGISwapChain4_SetHDRMetaData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGISwapChain4_SetHDRMetaData: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGISwapChain4_SetHDRMetaData _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGISwapChain4_SetHDRMetaData, npt_com_vtable(args._self),
        NPT_VTBL_IDXGISwapChain4_SetHDRMetaData);

    if (ctx->idxgiswapchain4_dispatch_overrides && ctx->idxgiswapchain4_dispatch_overrides->SetHDRMetaData) {
        args.ret = ctx->idxgiswapchain4_dispatch_overrides->SetHDRMetaData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Type, args.Size, args.pMetaData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGISwapChain4_SetHDRMetaData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGISWAPCHAIN_H */

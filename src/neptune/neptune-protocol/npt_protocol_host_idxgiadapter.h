/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgiadapter.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGIADAPTER_H
#define NPT_PROTOCOL_HOST_IDXGIADAPTER_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIAdapter::EnumOutputs
 */

struct npt_command_IDXGIAdapter_EnumOutputs {
    void *_self;
    UINT Output;
    IDXGIOutput ** ppOutput;
    /* Shadow: the guest-allocated id for ppOutput arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppOutput -> *ppOutput} in the
     * context object table. */
    uint64_t _guest_id_ppOutput;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter_EnumOutputs_args_temp(struct npt_cs_decoder *dec,
                                              struct npt_command_IDXGIAdapter_EnumOutputs *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Output);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppOutput = _gid;
        args->ppOutput = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppOutput) return;
        *args->ppOutput = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIAdapter_EnumOutputs_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter_EnumOutputs *args)
{
}

static inline void
npt_encode_IDXGIAdapter_EnumOutputs_reply(struct npt_cs_encoder *enc,
                                          const struct npt_command_IDXGIAdapter_EnumOutputs *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                 NPT_METHOD_IDXGIAdapter_EnumOutputs),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppOutput (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIAdapter_EnumOutputs(struct npt_dispatch_context *ctx,
                                      uint32_t cmd_flags,
                                      uint64_t object_id)
{
    struct npt_command_IDXGIAdapter_EnumOutputs args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter_EnumOutputs_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter_EnumOutputs on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppOutput);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter_EnumOutputs_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter_EnumOutputs: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppOutput);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter_EnumOutputs _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter_EnumOutputs, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter_EnumOutputs);

    if (ctx->idxgiadapter_dispatch_overrides && ctx->idxgiadapter_dispatch_overrides->EnumOutputs) {
        args.ret = ctx->idxgiadapter_dispatch_overrides->EnumOutputs(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Output, args.ppOutput);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppOutput && *args.ppOutput)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppOutput, *args.ppOutput, NPT_OBJECT_TYPE_IDXGIOUTPUT);
    else if (args.ppOutput)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppOutput);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter_EnumOutputs_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter::GetDesc
 */

struct npt_command_IDXGIAdapter_GetDesc {
    void *_self;
    DXGI_ADAPTER_DESC * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter_GetDesc_args_temp(struct npt_cs_decoder *dec,
                                          struct npt_command_IDXGIAdapter_GetDesc *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_ADAPTER_DESC));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_ADAPTER_DESC));
}

static inline void
npt_replace_IDXGIAdapter_GetDesc_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter_GetDesc *args)
{
}

static inline void
npt_encode_IDXGIAdapter_GetDesc_reply(struct npt_cs_encoder *enc,
                                      const struct npt_command_IDXGIAdapter_GetDesc *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                 NPT_METHOD_IDXGIAdapter_GetDesc),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_ADAPTER_DESC(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGIAdapter_GetDesc(struct npt_dispatch_context *ctx,
                                  uint32_t cmd_flags,
                                  uint64_t object_id)
{
    struct npt_command_IDXGIAdapter_GetDesc args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter_GetDesc_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter_GetDesc on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter_GetDesc_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter_GetDesc: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter_GetDesc _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter_GetDesc, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter_GetDesc);

    if (ctx->idxgiadapter_dispatch_overrides && ctx->idxgiadapter_dispatch_overrides->GetDesc) {
        args.ret = ctx->idxgiadapter_dispatch_overrides->GetDesc(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter_GetDesc_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter::CheckInterfaceSupport
 */

struct npt_command_IDXGIAdapter_CheckInterfaceSupport {
    void *_self;
    const GUID * InterfaceName;
    LARGE_INTEGER * pUMDVersion;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter_CheckInterfaceSupport_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIAdapter_CheckInterfaceSupport *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->InterfaceName = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->InterfaceName) return;
        npt_decode_GUID(dec, (GUID *)args->InterfaceName);
    } else {
        args->InterfaceName = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */

    args->pUMDVersion = npt_cs_decoder_alloc_temp(dec, sizeof(LARGE_INTEGER));
    if (!args->pUMDVersion) return;
    memset(args->pUMDVersion, 0, sizeof(LARGE_INTEGER));
}

static inline void
npt_replace_IDXGIAdapter_CheckInterfaceSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter_CheckInterfaceSupport *args)
{
}

static inline void
npt_encode_IDXGIAdapter_CheckInterfaceSupport_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIAdapter_CheckInterfaceSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter,
                                 NPT_METHOD_IDXGIAdapter_CheckInterfaceSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pUMDVersion))
        npt_encode_LARGE_INTEGER(enc, args->pUMDVersion);
}

static inline void
npt_dispatch_IDXGIAdapter_CheckInterfaceSupport(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIAdapter_CheckInterfaceSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter_CheckInterfaceSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter_CheckInterfaceSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter_CheckInterfaceSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter_CheckInterfaceSupport: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter_CheckInterfaceSupport _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter_CheckInterfaceSupport, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter_CheckInterfaceSupport);

    if (ctx->idxgiadapter_dispatch_overrides && ctx->idxgiadapter_dispatch_overrides->CheckInterfaceSupport) {
        args.ret = ctx->idxgiadapter_dispatch_overrides->CheckInterfaceSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.InterfaceName, args.pUMDVersion);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter_CheckInterfaceSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter1::GetDesc1
 */

struct npt_command_IDXGIAdapter1_GetDesc1 {
    void *_self;
    DXGI_ADAPTER_DESC1 * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter1_GetDesc1_args_temp(struct npt_cs_decoder *dec,
                                            struct npt_command_IDXGIAdapter1_GetDesc1 *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_ADAPTER_DESC1));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_ADAPTER_DESC1));
}

static inline void
npt_replace_IDXGIAdapter1_GetDesc1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter1_GetDesc1 *args)
{
}

static inline void
npt_encode_IDXGIAdapter1_GetDesc1_reply(struct npt_cs_encoder *enc,
                                        const struct npt_command_IDXGIAdapter1_GetDesc1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter1,
                                 NPT_METHOD_IDXGIAdapter1_GetDesc1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_ADAPTER_DESC1(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGIAdapter1_GetDesc1(struct npt_dispatch_context *ctx,
                                    uint32_t cmd_flags,
                                    uint64_t object_id)
{
    struct npt_command_IDXGIAdapter1_GetDesc1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter1_GetDesc1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter1_GetDesc1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter1_GetDesc1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter1_GetDesc1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter1_GetDesc1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter1_GetDesc1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter1_GetDesc1);

    if (ctx->idxgiadapter1_dispatch_overrides && ctx->idxgiadapter1_dispatch_overrides->GetDesc1) {
        args.ret = ctx->idxgiadapter1_dispatch_overrides->GetDesc1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter1_GetDesc1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter2::GetDesc2
 */

struct npt_command_IDXGIAdapter2_GetDesc2 {
    void *_self;
    DXGI_ADAPTER_DESC2 * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter2_GetDesc2_args_temp(struct npt_cs_decoder *dec,
                                            struct npt_command_IDXGIAdapter2_GetDesc2 *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_ADAPTER_DESC2));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_ADAPTER_DESC2));
}

static inline void
npt_replace_IDXGIAdapter2_GetDesc2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter2_GetDesc2 *args)
{
}

static inline void
npt_encode_IDXGIAdapter2_GetDesc2_reply(struct npt_cs_encoder *enc,
                                        const struct npt_command_IDXGIAdapter2_GetDesc2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter2,
                                 NPT_METHOD_IDXGIAdapter2_GetDesc2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_ADAPTER_DESC2(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGIAdapter2_GetDesc2(struct npt_dispatch_context *ctx,
                                    uint32_t cmd_flags,
                                    uint64_t object_id)
{
    struct npt_command_IDXGIAdapter2_GetDesc2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter2_GetDesc2_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter2_GetDesc2 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter2_GetDesc2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter2_GetDesc2: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter2_GetDesc2 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter2_GetDesc2, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter2_GetDesc2);

    if (ctx->idxgiadapter2_dispatch_overrides && ctx->idxgiadapter2_dispatch_overrides->GetDesc2) {
        args.ret = ctx->idxgiadapter2_dispatch_overrides->GetDesc2(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter2_GetDesc2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter3::RegisterHardwareContentProtectionTeardownStatusEvent
 */

struct npt_command_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_args_temp(struct npt_cs_decoder *dec,
                                                                                        struct npt_command_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_reply(struct npt_cs_encoder *enc,
                                                                                    const struct npt_command_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent(struct npt_dispatch_context *ctx,
                                                                                uint32_t cmd_flags,
                                                                                uint64_t object_id)
{
    struct npt_command_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent);

    if (ctx->idxgiadapter3_dispatch_overrides && ctx->idxgiadapter3_dispatch_overrides->RegisterHardwareContentProtectionTeardownStatusEvent) {
        args.ret = ctx->idxgiadapter3_dispatch_overrides->RegisterHardwareContentProtectionTeardownStatusEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hEvent, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter3_RegisterHardwareContentProtectionTeardownStatusEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter3::UnregisterHardwareContentProtectionTeardownStatus
 */

struct npt_command_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus {
    void *_self;
    DWORD dwCookie;
};

static inline void
npt_decode_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_args_temp(struct npt_cs_decoder *dec,
                                                                                     struct npt_command_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DWORD(dec, &args->dwCookie);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus *args)
{
}

static inline void
npt_encode_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_reply(struct npt_cs_encoder *enc,
                                                                                 const struct npt_command_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus(struct npt_dispatch_context *ctx,
                                                                             uint32_t cmd_flags,
                                                                             uint64_t object_id)
{
    struct npt_command_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus);

    if (ctx->idxgiadapter3_dispatch_overrides && ctx->idxgiadapter3_dispatch_overrides->UnregisterHardwareContentProtectionTeardownStatus) {
        ctx->idxgiadapter3_dispatch_overrides->UnregisterHardwareContentProtectionTeardownStatus(ctx, &args, _original);
    } else {
        _original(args._self, args.dwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter3_UnregisterHardwareContentProtectionTeardownStatus_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter3::QueryVideoMemoryInfo
 */

struct npt_command_IDXGIAdapter3_QueryVideoMemoryInfo {
    void *_self;
    UINT NodeIndex;
    DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup;
    DXGI_QUERY_VIDEO_MEMORY_INFO * pVideoMemoryInfo;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter3_QueryVideoMemoryInfo_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIAdapter3_QueryVideoMemoryInfo *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NodeIndex);
    npt_decode_DXGI_MEMORY_SEGMENT_GROUP(dec, &args->MemorySegmentGroup);

    /* Allocate temp storage for output-only parameters */


    args->pVideoMemoryInfo = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_QUERY_VIDEO_MEMORY_INFO));
    if (!args->pVideoMemoryInfo) return;
    memset(args->pVideoMemoryInfo, 0, sizeof(DXGI_QUERY_VIDEO_MEMORY_INFO));
}

static inline void
npt_replace_IDXGIAdapter3_QueryVideoMemoryInfo_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter3_QueryVideoMemoryInfo *args)
{
}

static inline void
npt_encode_IDXGIAdapter3_QueryVideoMemoryInfo_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIAdapter3_QueryVideoMemoryInfo *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_QueryVideoMemoryInfo),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pVideoMemoryInfo))
        npt_encode_DXGI_QUERY_VIDEO_MEMORY_INFO(enc, args->pVideoMemoryInfo);
}

static inline void
npt_dispatch_IDXGIAdapter3_QueryVideoMemoryInfo(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIAdapter3_QueryVideoMemoryInfo args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter3_QueryVideoMemoryInfo_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_QueryVideoMemoryInfo on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter3_QueryVideoMemoryInfo_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_QueryVideoMemoryInfo: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter3_QueryVideoMemoryInfo _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter3_QueryVideoMemoryInfo, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter3_QueryVideoMemoryInfo);

    if (ctx->idxgiadapter3_dispatch_overrides && ctx->idxgiadapter3_dispatch_overrides->QueryVideoMemoryInfo) {
        args.ret = ctx->idxgiadapter3_dispatch_overrides->QueryVideoMemoryInfo(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NodeIndex, args.MemorySegmentGroup, args.pVideoMemoryInfo);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter3_QueryVideoMemoryInfo_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter3::SetVideoMemoryReservation
 */

struct npt_command_IDXGIAdapter3_SetVideoMemoryReservation {
    void *_self;
    UINT NodeIndex;
    DXGI_MEMORY_SEGMENT_GROUP MemorySegmentGroup;
    UINT64 Reservation;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter3_SetVideoMemoryReservation_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_IDXGIAdapter3_SetVideoMemoryReservation *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NodeIndex);
    npt_decode_DXGI_MEMORY_SEGMENT_GROUP(dec, &args->MemorySegmentGroup);
    npt_decode_UINT64(dec, &args->Reservation);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGIAdapter3_SetVideoMemoryReservation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter3_SetVideoMemoryReservation *args)
{
}

static inline void
npt_encode_IDXGIAdapter3_SetVideoMemoryReservation_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_IDXGIAdapter3_SetVideoMemoryReservation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_SetVideoMemoryReservation),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIAdapter3_SetVideoMemoryReservation(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_IDXGIAdapter3_SetVideoMemoryReservation args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter3_SetVideoMemoryReservation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_SetVideoMemoryReservation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter3_SetVideoMemoryReservation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_SetVideoMemoryReservation: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter3_SetVideoMemoryReservation _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter3_SetVideoMemoryReservation, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter3_SetVideoMemoryReservation);

    if (ctx->idxgiadapter3_dispatch_overrides && ctx->idxgiadapter3_dispatch_overrides->SetVideoMemoryReservation) {
        args.ret = ctx->idxgiadapter3_dispatch_overrides->SetVideoMemoryReservation(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NodeIndex, args.MemorySegmentGroup, args.Reservation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter3_SetVideoMemoryReservation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter3::RegisterVideoMemoryBudgetChangeNotificationEvent
 */

struct npt_command_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_args_temp(struct npt_cs_decoder *dec,
                                                                                    struct npt_command_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_reply(struct npt_cs_encoder *enc,
                                                                                const struct npt_command_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent(struct npt_dispatch_context *ctx,
                                                                            uint32_t cmd_flags,
                                                                            uint64_t object_id)
{
    struct npt_command_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent);

    if (ctx->idxgiadapter3_dispatch_overrides && ctx->idxgiadapter3_dispatch_overrides->RegisterVideoMemoryBudgetChangeNotificationEvent) {
        args.ret = ctx->idxgiadapter3_dispatch_overrides->RegisterVideoMemoryBudgetChangeNotificationEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hEvent, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter3_RegisterVideoMemoryBudgetChangeNotificationEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter3::UnregisterVideoMemoryBudgetChangeNotification
 */

struct npt_command_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification {
    void *_self;
    DWORD dwCookie;
};

static inline void
npt_decode_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_args_temp(struct npt_cs_decoder *dec,
                                                                                 struct npt_command_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DWORD(dec, &args->dwCookie);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification *args)
{
}

static inline void
npt_encode_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_reply(struct npt_cs_encoder *enc,
                                                                             const struct npt_command_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter3,
                                 NPT_METHOD_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification(struct npt_dispatch_context *ctx,
                                                                         uint32_t cmd_flags,
                                                                         uint64_t object_id)
{
    struct npt_command_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification);

    if (ctx->idxgiadapter3_dispatch_overrides && ctx->idxgiadapter3_dispatch_overrides->UnregisterVideoMemoryBudgetChangeNotification) {
        ctx->idxgiadapter3_dispatch_overrides->UnregisterVideoMemoryBudgetChangeNotification(ctx, &args, _original);
    } else {
        _original(args._self, args.dwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter3_UnregisterVideoMemoryBudgetChangeNotification_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIAdapter4::GetDesc3
 */

struct npt_command_IDXGIAdapter4_GetDesc3 {
    void *_self;
    DXGI_ADAPTER_DESC3 * pDesc;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIAdapter4_GetDesc3_args_temp(struct npt_cs_decoder *dec,
                                            struct npt_command_IDXGIAdapter4_GetDesc3 *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_ADAPTER_DESC3));
    if (!args->pDesc) return;
    memset(args->pDesc, 0, sizeof(DXGI_ADAPTER_DESC3));
}

static inline void
npt_replace_IDXGIAdapter4_GetDesc3_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIAdapter4_GetDesc3 *args)
{
}

static inline void
npt_encode_IDXGIAdapter4_GetDesc3_reply(struct npt_cs_encoder *enc,
                                        const struct npt_command_IDXGIAdapter4_GetDesc3 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIAdapter4,
                                 NPT_METHOD_IDXGIAdapter4_GetDesc3),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDesc))
        npt_encode_DXGI_ADAPTER_DESC3(enc, args->pDesc);
}

static inline void
npt_dispatch_IDXGIAdapter4_GetDesc3(struct npt_dispatch_context *ctx,
                                    uint32_t cmd_flags,
                                    uint64_t object_id)
{
    struct npt_command_IDXGIAdapter4_GetDesc3 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIAdapter4_GetDesc3_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIADAPTER4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter4_GetDesc3 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIAdapter4_GetDesc3_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIAdapter4_GetDesc3: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIAdapter4_GetDesc3 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIAdapter4_GetDesc3, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIAdapter4_GetDesc3);

    if (ctx->idxgiadapter4_dispatch_overrides && ctx->idxgiadapter4_dispatch_overrides->GetDesc3) {
        args.ret = ctx->idxgiadapter4_dispatch_overrides->GetDesc3(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIAdapter4_GetDesc3_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGIADAPTER_H */

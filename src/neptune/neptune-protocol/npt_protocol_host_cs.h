/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Host-side command stream interface.
 *
 * The consuming project must provide "npt_cs.h" with implementations of
 * the types and functions listed below, in addition to the common
 * requirements documented in npt_protocol_common_cs.h.
 */

#ifndef NPT_PROTOCOL_HOST_CS_H
#define NPT_PROTOCOL_HOST_CS_H

#include "npt_protocol_common_cs.h"

/*
 * Additional required types/functions for host side:
 *
 *   -- Types --
 *   struct npt_dispatch_context
 *
 *   -- Encoder --
 *   bool npt_cs_encoder_acquire(struct npt_cs_encoder *enc)
 *   void npt_cs_encoder_release(struct npt_cs_encoder *enc)
 *
 *   -- Decoder --
 *   bool npt_cs_decoder_get_fatal(const struct npt_cs_decoder *dec)
 *   bool npt_cs_decoder_take_handle_miss(struct npt_cs_decoder *dec)
 *   void *npt_cs_decoder_alloc_temp(struct npt_cs_decoder *dec, size_t size)
 *   void *npt_cs_decoder_alloc_temp_array(struct npt_cs_decoder *dec,
 *                                         size_t element_size, size_t count)
 *   void npt_cs_decoder_reset_temp_pool(struct npt_cs_decoder *dec)
 *
 *   -- Object/handle management --
 *   void *npt_cs_handle_lookup(struct npt_dispatch_context *ctx,
 *                              npt_object_id id, npt_object_type type)
 *   void npt_cs_handle_register_guest_id(struct npt_dispatch_context *ctx,
 *                                        npt_object_id guest_id,
 *                                        void *obj, npt_object_type type)
 *   void npt_cs_handle_register_failed_guest_id(struct npt_dispatch_context *ctx,
 *                                               uint64_t guest_id)
 *   void *npt_win32_handle_replace(struct npt_dispatch_context *ctx,
 *                                  npt_object_id id)
 *   void *npt_event_handle_replace(struct npt_dispatch_context *ctx,
 *                                  npt_object_id id)
 *
 *   npt_event_handle_replace is used for event HANDLEs (annotated
 *   handle="event" in the overlay): unlike npt_win32_handle_replace, an
 *   unregistered id must map to NULL rather than the identity fallback, so a
 *   guest that skipped REGISTER_EVENT cannot hand a raw token to the backend.
 *
 *   A failed npt_cs_handle_lookup of a nonzero id must be recorded on the
 *   dispatch context's decoder, where npt_cs_decoder_take_handle_miss reads
 *   and clears it.  The generated dispatch turns the miss into a dropped
 *   call or a decoder fatal depending on whether the command owes a reply.
 *   The record must be decoder-private state, not a shared fatal flag:
 *   dispatch absorbs it when it drops a call, and the absorb must not erase
 *   an error raised through another decoder.
 *
 *   The host implementation of npt_*_from_id is typically a no-op cast
 *   ((void *)(uintptr_t)id), since the host needs the raw ID stored in
 *   the field for the subsequent npt_replace_*_handle pass to convert
 *   it to a real handle via the dispatch context.
 *
 *   -- COM vtable access --
 *   void **npt_com_vtable(void *obj)
 *
 *   -- Logging --
 *   npt_log(fmt, ...)    (printf-style macro or function)
 */

#endif /* NPT_PROTOCOL_HOST_CS_H */

/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11on12device.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11ON12DEVICE_H
#define NPT_PROTOCOL_HOST_ID3D11ON12DEVICE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11On12Device::CreateWrappedResource
 */

struct npt_command_ID3D11On12Device_CreateWrappedResource {
    void *_self;
    IUnknown * pResource12;
    const D3D11_RESOURCE_FLAGS * pFlags11;
    D3D12_RESOURCE_STATES InState;
    D3D12_RESOURCE_STATES OutState;
    const IID * riid;
    void ** ppResource11;
    /* Shadow: the guest-allocated id for ppResource11 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppResource11 -> *ppResource11} in the
     * context object table. */
    uint64_t _guest_id_ppResource11;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11On12Device_CreateWrappedResource_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D11On12Device_CreateWrappedResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource12 = (IUnknown *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pFlags11 = npt_cs_decoder_alloc_temp(dec, sizeof(D3D11_RESOURCE_FLAGS));
        if (!args->pFlags11) return;
        npt_decode_D3D11_RESOURCE_FLAGS(dec, (D3D11_RESOURCE_FLAGS *)args->pFlags11);
    } else {
        args->pFlags11 = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->InState);
    npt_decode_D3D12_RESOURCE_STATES(dec, &args->OutState);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppResource11 = _gid;
        args->ppResource11 = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppResource11) return;
        *args->ppResource11 = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D11On12Device_CreateWrappedResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11On12Device_CreateWrappedResource *args)
{
    args->pResource12 = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource12, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_ID3D11On12Device_CreateWrappedResource_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D11On12Device_CreateWrappedResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                 NPT_METHOD_ID3D11On12Device_CreateWrappedResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppResource11 (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11On12Device_CreateWrappedResource(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D11On12Device_CreateWrappedResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11On12Device_CreateWrappedResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11ON12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device_CreateWrappedResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11On12Device_CreateWrappedResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device_CreateWrappedResource: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11On12Device_CreateWrappedResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11On12Device_CreateWrappedResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11On12Device_CreateWrappedResource);

    if (ctx->id3d11on12device_dispatch_overrides && ctx->id3d11on12device_dispatch_overrides->CreateWrappedResource) {
        args.ret = ctx->id3d11on12device_dispatch_overrides->CreateWrappedResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource12, args.pFlags11, args.InState, args.OutState, args.riid, args.ppResource11);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppResource11 && *args.ppResource11)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppResource11, *args.ppResource11,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11On12Device_CreateWrappedResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11On12Device::ReleaseWrappedResources
 */

struct npt_command_ID3D11On12Device_ReleaseWrappedResources {
    void *_self;
    ID3D11Resource ** ppResources;
    UINT NumResources;
};

static inline void
npt_decode_ID3D11On12Device_ReleaseWrappedResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11On12Device_ReleaseWrappedResources *args)
{
    /* Decode input parameters from the wire */
    uint64_t _cnt_ppResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppResources) {
        args->ppResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppResources);
        if (!args->ppResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppResources = NULL;
    }
    npt_decode_UINT(dec, &args->NumResources);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppResources && _cnt_ppResources < (uint64_t)(args->NumResources)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11On12Device_ReleaseWrappedResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11On12Device_ReleaseWrappedResources *args)
{
    if (args->ppResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumResources); _i++)
            args->ppResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppResources[_i], NPT_OBJECT_TYPE_ID3D11RESOURCE);
    }
}

static inline void
npt_encode_ID3D11On12Device_ReleaseWrappedResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11On12Device_ReleaseWrappedResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                 NPT_METHOD_ID3D11On12Device_ReleaseWrappedResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11On12Device_ReleaseWrappedResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11On12Device_ReleaseWrappedResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11On12Device_ReleaseWrappedResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11ON12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device_ReleaseWrappedResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11On12Device_ReleaseWrappedResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device_ReleaseWrappedResources: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11On12Device_ReleaseWrappedResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11On12Device_ReleaseWrappedResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11On12Device_ReleaseWrappedResources);

    if (ctx->id3d11on12device_dispatch_overrides && ctx->id3d11on12device_dispatch_overrides->ReleaseWrappedResources) {
        ctx->id3d11on12device_dispatch_overrides->ReleaseWrappedResources(ctx, &args, _original);
    } else {
        _original(args._self, args.ppResources, args.NumResources);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11On12Device_ReleaseWrappedResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11On12Device::AcquireWrappedResources
 */

struct npt_command_ID3D11On12Device_AcquireWrappedResources {
    void *_self;
    ID3D11Resource ** ppResources;
    UINT NumResources;
};

static inline void
npt_decode_ID3D11On12Device_AcquireWrappedResources_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D11On12Device_AcquireWrappedResources *args)
{
    /* Decode input parameters from the wire */
    uint64_t _cnt_ppResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppResources) {
        args->ppResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppResources);
        if (!args->ppResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppResources = NULL;
    }
    npt_decode_UINT(dec, &args->NumResources);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppResources && _cnt_ppResources < (uint64_t)(args->NumResources)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11On12Device_AcquireWrappedResources_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11On12Device_AcquireWrappedResources *args)
{
    if (args->ppResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumResources); _i++)
            args->ppResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppResources[_i], NPT_OBJECT_TYPE_ID3D11RESOURCE);
    }
}

static inline void
npt_encode_ID3D11On12Device_AcquireWrappedResources_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D11On12Device_AcquireWrappedResources *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device,
                                 NPT_METHOD_ID3D11On12Device_AcquireWrappedResources),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11On12Device_AcquireWrappedResources(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D11On12Device_AcquireWrappedResources args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11On12Device_AcquireWrappedResources_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11ON12DEVICE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device_AcquireWrappedResources on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11On12Device_AcquireWrappedResources_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device_AcquireWrappedResources: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11On12Device_AcquireWrappedResources _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11On12Device_AcquireWrappedResources, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11On12Device_AcquireWrappedResources);

    if (ctx->id3d11on12device_dispatch_overrides && ctx->id3d11on12device_dispatch_overrides->AcquireWrappedResources) {
        ctx->id3d11on12device_dispatch_overrides->AcquireWrappedResources(ctx, &args, _original);
    } else {
        _original(args._self, args.ppResources, args.NumResources);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11On12Device_AcquireWrappedResources_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11On12Device1::GetD3D12Device
 */

struct npt_command_ID3D11On12Device1_GetD3D12Device {
    void *_self;
    const IID * riid;
    void ** ppvDevice;
    /* Shadow: the guest-allocated id for ppvDevice arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvDevice -> *ppvDevice} in the
     * context object table. */
    uint64_t _guest_id_ppvDevice;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11On12Device1_GetD3D12Device_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11On12Device1_GetD3D12Device *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvDevice = _gid;
        args->ppvDevice = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvDevice) return;
        *args->ppvDevice = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11On12Device1_GetD3D12Device_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11On12Device1_GetD3D12Device *args)
{
}

static inline void
npt_encode_ID3D11On12Device1_GetD3D12Device_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11On12Device1_GetD3D12Device *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device1,
                                 NPT_METHOD_ID3D11On12Device1_GetD3D12Device),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvDevice (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11On12Device1_GetD3D12Device(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11On12Device1_GetD3D12Device args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11On12Device1_GetD3D12Device_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11ON12DEVICE1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device1_GetD3D12Device on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11On12Device1_GetD3D12Device_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device1_GetD3D12Device: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11On12Device1_GetD3D12Device _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11On12Device1_GetD3D12Device, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11On12Device1_GetD3D12Device);

    if (ctx->id3d11on12device1_dispatch_overrides && ctx->id3d11on12device1_dispatch_overrides->GetD3D12Device) {
        args.ret = ctx->id3d11on12device1_dispatch_overrides->GetD3D12Device(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.riid, args.ppvDevice);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvDevice && *args.ppvDevice)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvDevice, *args.ppvDevice,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11On12Device1_GetD3D12Device_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11On12Device2::UnwrapUnderlyingResource
 */

struct npt_command_ID3D11On12Device2_UnwrapUnderlyingResource {
    void *_self;
    ID3D11Resource * pResource11;
    ID3D12CommandQueue * pCommandQueue;
    const IID * riid;
    void ** ppvResource12;
    /* Shadow: the guest-allocated id for ppvResource12 arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource12 -> *ppvResource12} in the
     * context object table. */
    uint64_t _guest_id_ppvResource12;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11On12Device2_UnwrapUnderlyingResource_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11On12Device2_UnwrapUnderlyingResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource11 = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_com_handle(dec, (npt_object_id *)&args->pCommandQueue);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource12 = _gid;
        args->ppvResource12 = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource12) return;
        *args->ppvResource12 = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11On12Device2_UnwrapUnderlyingResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11On12Device2_UnwrapUnderlyingResource *args)
{
    args->pResource11 = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource11, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    args->pCommandQueue = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCommandQueue, NPT_OBJECT_TYPE_ID3D12COMMANDQUEUE);
}

static inline void
npt_encode_ID3D11On12Device2_UnwrapUnderlyingResource_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11On12Device2_UnwrapUnderlyingResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                 NPT_METHOD_ID3D11On12Device2_UnwrapUnderlyingResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource12 (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D11On12Device2_UnwrapUnderlyingResource(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11On12Device2_UnwrapUnderlyingResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11On12Device2_UnwrapUnderlyingResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11ON12DEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device2_UnwrapUnderlyingResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11On12Device2_UnwrapUnderlyingResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device2_UnwrapUnderlyingResource: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11On12Device2_UnwrapUnderlyingResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11On12Device2_UnwrapUnderlyingResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11On12Device2_UnwrapUnderlyingResource);

    if (ctx->id3d11on12device2_dispatch_overrides && ctx->id3d11on12device2_dispatch_overrides->UnwrapUnderlyingResource) {
        args.ret = ctx->id3d11on12device2_dispatch_overrides->UnwrapUnderlyingResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource11, args.pCommandQueue, args.riid, args.ppvResource12);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource12 && *args.ppvResource12)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource12, *args.ppvResource12,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11On12Device2_UnwrapUnderlyingResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11On12Device2::ReturnUnderlyingResource
 */

struct npt_command_ID3D11On12Device2_ReturnUnderlyingResource {
    void *_self;
    ID3D11Resource * pResource11;
    UINT NumSync;
    UINT64 * pSignalValues;
    ID3D12Fence ** ppFences;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11On12Device2_ReturnUnderlyingResource_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D11On12Device2_ReturnUnderlyingResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource11 = (ID3D11Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->NumSync);
    uint64_t _cnt_pSignalValues = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pSignalValues = npt_decode_array_count_unchecked(dec);
        args->pSignalValues = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT64), _cnt_pSignalValues);
        if (!args->pSignalValues) return;
        npt_decode_UINT64_array(dec, (UINT64 *)args->pSignalValues, _cnt_pSignalValues);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pSignalValues = NULL;
    }
    uint64_t _cnt_ppFences = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppFences) {
        args->ppFences = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppFences);
        if (!args->ppFences) return;
        for (uint64_t _i = 0; _i < _cnt_ppFences; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppFences[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppFences = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pSignalValues && _cnt_pSignalValues < (uint64_t)(args->NumSync)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_ppFences && _cnt_ppFences < (uint64_t)(args->NumSync)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D11On12Device2_ReturnUnderlyingResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11On12Device2_ReturnUnderlyingResource *args)
{
    args->pResource11 = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource11, NPT_OBJECT_TYPE_ID3D11RESOURCE);
    if (args->ppFences) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumSync); _i++)
            args->ppFences[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppFences[_i], NPT_OBJECT_TYPE_ID3D12FENCE);
    }
}

static inline void
npt_encode_ID3D11On12Device2_ReturnUnderlyingResource_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D11On12Device2_ReturnUnderlyingResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11On12Device2,
                                 NPT_METHOD_ID3D11On12Device2_ReturnUnderlyingResource),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11On12Device2_ReturnUnderlyingResource(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D11On12Device2_ReturnUnderlyingResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11On12Device2_ReturnUnderlyingResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11ON12DEVICE2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device2_ReturnUnderlyingResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11On12Device2_ReturnUnderlyingResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11On12Device2_ReturnUnderlyingResource: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11On12Device2_ReturnUnderlyingResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11On12Device2_ReturnUnderlyingResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11On12Device2_ReturnUnderlyingResource);

    if (ctx->id3d11on12device2_dispatch_overrides && ctx->id3d11on12device2_dispatch_overrides->ReturnUnderlyingResource) {
        args.ret = ctx->id3d11on12device2_dispatch_overrides->ReturnUnderlyingResource(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pResource11, args.NumSync, args.pSignalValues, args.ppFences);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11On12Device2_ReturnUnderlyingResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11ON12DEVICE_H */

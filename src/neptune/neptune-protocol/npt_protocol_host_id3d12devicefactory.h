/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12devicefactory.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D12DEVICEFACTORY_H
#define NPT_PROTOCOL_HOST_ID3D12DEVICEFACTORY_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12DeviceFactory::InitializeFromGlobalState
 */

struct npt_command_ID3D12DeviceFactory_InitializeFromGlobalState {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12DeviceFactory_InitializeFromGlobalState_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D12DeviceFactory_InitializeFromGlobalState *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12DeviceFactory_InitializeFromGlobalState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceFactory_InitializeFromGlobalState *args)
{
}

static inline void
npt_encode_ID3D12DeviceFactory_InitializeFromGlobalState_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D12DeviceFactory_InitializeFromGlobalState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_InitializeFromGlobalState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceFactory_InitializeFromGlobalState(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D12DeviceFactory_InitializeFromGlobalState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceFactory_InitializeFromGlobalState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_InitializeFromGlobalState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceFactory_InitializeFromGlobalState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_InitializeFromGlobalState: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceFactory_InitializeFromGlobalState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceFactory_InitializeFromGlobalState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceFactory_InitializeFromGlobalState);

    if (ctx->id3d12devicefactory_dispatch_overrides && ctx->id3d12devicefactory_dispatch_overrides->InitializeFromGlobalState) {
        args.ret = ctx->id3d12devicefactory_dispatch_overrides->InitializeFromGlobalState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceFactory_InitializeFromGlobalState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceFactory::ApplyToGlobalState
 */

struct npt_command_ID3D12DeviceFactory_ApplyToGlobalState {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12DeviceFactory_ApplyToGlobalState_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12DeviceFactory_ApplyToGlobalState *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12DeviceFactory_ApplyToGlobalState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceFactory_ApplyToGlobalState *args)
{
}

static inline void
npt_encode_ID3D12DeviceFactory_ApplyToGlobalState_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12DeviceFactory_ApplyToGlobalState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_ApplyToGlobalState),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceFactory_ApplyToGlobalState(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12DeviceFactory_ApplyToGlobalState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceFactory_ApplyToGlobalState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_ApplyToGlobalState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceFactory_ApplyToGlobalState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_ApplyToGlobalState: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceFactory_ApplyToGlobalState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceFactory_ApplyToGlobalState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceFactory_ApplyToGlobalState);

    if (ctx->id3d12devicefactory_dispatch_overrides && ctx->id3d12devicefactory_dispatch_overrides->ApplyToGlobalState) {
        args.ret = ctx->id3d12devicefactory_dispatch_overrides->ApplyToGlobalState(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceFactory_ApplyToGlobalState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceFactory::SetFlags
 */

struct npt_command_ID3D12DeviceFactory_SetFlags {
    void *_self;
    D3D12_DEVICE_FACTORY_FLAGS flags;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12DeviceFactory_SetFlags_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D12DeviceFactory_SetFlags *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_DEVICE_FACTORY_FLAGS(dec, &args->flags);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12DeviceFactory_SetFlags_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceFactory_SetFlags *args)
{
}

static inline void
npt_encode_ID3D12DeviceFactory_SetFlags_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D12DeviceFactory_SetFlags *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_SetFlags),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceFactory_SetFlags(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D12DeviceFactory_SetFlags args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceFactory_SetFlags_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_SetFlags on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceFactory_SetFlags_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_SetFlags: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceFactory_SetFlags _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceFactory_SetFlags, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceFactory_SetFlags);

    if (ctx->id3d12devicefactory_dispatch_overrides && ctx->id3d12devicefactory_dispatch_overrides->SetFlags) {
        args.ret = ctx->id3d12devicefactory_dispatch_overrides->SetFlags(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceFactory_SetFlags_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceFactory::GetFlags
 */

struct npt_command_ID3D12DeviceFactory_GetFlags {
    void *_self;
    D3D12_DEVICE_FACTORY_FLAGS ret;
};

static inline void
npt_decode_ID3D12DeviceFactory_GetFlags_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_ID3D12DeviceFactory_GetFlags *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12DeviceFactory_GetFlags_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceFactory_GetFlags *args)
{
}

static inline void
npt_encode_ID3D12DeviceFactory_GetFlags_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_ID3D12DeviceFactory_GetFlags *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_GetFlags),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceFactory_GetFlags(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_ID3D12DeviceFactory_GetFlags args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceFactory_GetFlags_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_GetFlags on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceFactory_GetFlags_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_GetFlags: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceFactory_GetFlags _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceFactory_GetFlags, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceFactory_GetFlags);

    if (ctx->id3d12devicefactory_dispatch_overrides && ctx->id3d12devicefactory_dispatch_overrides->GetFlags) {
        args.ret = ctx->id3d12devicefactory_dispatch_overrides->GetFlags(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceFactory_GetFlags_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceFactory::GetConfigurationInterface
 */

struct npt_command_ID3D12DeviceFactory_GetConfigurationInterface {
    void *_self;
    const CLSID * clsid;
    const IID * iid;
    void ** ppv;
    /* Shadow: the guest-allocated id for ppv arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppv -> *ppv} in the
     * context object table. */
    uint64_t _guest_id_ppv;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12DeviceFactory_GetConfigurationInterface_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D12DeviceFactory_GetConfigurationInterface *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->clsid = npt_cs_decoder_alloc_temp(dec, sizeof(CLSID));
        if (!args->clsid) return;
        npt_decode_CLSID(dec, (CLSID *)args->clsid);
    } else {
        args->clsid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->iid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->iid) return;
        npt_decode_IID(dec, (IID *)args->iid);
    } else {
        args->iid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppv = _gid;
        args->ppv = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppv) return;
        *args->ppv = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12DeviceFactory_GetConfigurationInterface_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceFactory_GetConfigurationInterface *args)
{
}

static inline void
npt_encode_ID3D12DeviceFactory_GetConfigurationInterface_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D12DeviceFactory_GetConfigurationInterface *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_GetConfigurationInterface),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppv (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12DeviceFactory_GetConfigurationInterface(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D12DeviceFactory_GetConfigurationInterface args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceFactory_GetConfigurationInterface_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_GetConfigurationInterface on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceFactory_GetConfigurationInterface_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_GetConfigurationInterface: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceFactory_GetConfigurationInterface _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceFactory_GetConfigurationInterface, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceFactory_GetConfigurationInterface);

    if (ctx->id3d12devicefactory_dispatch_overrides && ctx->id3d12devicefactory_dispatch_overrides->GetConfigurationInterface) {
        args.ret = ctx->id3d12devicefactory_dispatch_overrides->GetConfigurationInterface(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.clsid, args.iid, args.ppv);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppv && *args.ppv)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppv, *args.ppv,
            npt_object_type_from_iid(args.iid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceFactory_GetConfigurationInterface_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceFactory::EnableExperimentalFeatures
 */

struct npt_command_ID3D12DeviceFactory_EnableExperimentalFeatures {
    void *_self;
    UINT NumFeatures;
    const IID * pIIDs;
    void * pConfigurationStructs;
    UINT * pConfigurationStructSizes;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12DeviceFactory_EnableExperimentalFeatures_args_temp(struct npt_cs_decoder *dec,
                                                                    struct npt_command_ID3D12DeviceFactory_EnableExperimentalFeatures *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumFeatures);
    uint64_t _cnt_pIIDs = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pIIDs = npt_decode_array_count_unchecked(dec);
        args->pIIDs = npt_cs_decoder_alloc_temp_array(dec, sizeof(IID), _cnt_pIIDs);
        if (!args->pIIDs) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pIIDs; _i++)
            npt_decode_IID(dec, (IID *)&args->pIIDs[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pIIDs = NULL;
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pConfigurationStructs = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pConfigurationStructs) return;
            npt_decode_blob_array(dec, (void *)args->pConfigurationStructs, _blob_size);
        } else {
            args->pConfigurationStructs = NULL;
        }
    }
    uint64_t _cnt_pConfigurationStructSizes = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pConfigurationStructSizes = npt_decode_array_count_unchecked(dec);
        args->pConfigurationStructSizes = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), _cnt_pConfigurationStructSizes);
        if (!args->pConfigurationStructSizes) return;
        npt_decode_UINT_array(dec, (UINT *)args->pConfigurationStructSizes, _cnt_pConfigurationStructSizes);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pConfigurationStructSizes = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pIIDs && _cnt_pIIDs < (uint64_t)(args->NumFeatures)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pConfigurationStructSizes && _cnt_pConfigurationStructSizes < (uint64_t)(args->NumFeatures)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12DeviceFactory_EnableExperimentalFeatures_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceFactory_EnableExperimentalFeatures *args)
{
}

static inline void
npt_encode_ID3D12DeviceFactory_EnableExperimentalFeatures_reply(struct npt_cs_encoder *enc,
                                                                const struct npt_command_ID3D12DeviceFactory_EnableExperimentalFeatures *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_EnableExperimentalFeatures),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceFactory_EnableExperimentalFeatures(struct npt_dispatch_context *ctx,
                                                            uint32_t cmd_flags,
                                                            uint64_t object_id)
{
    struct npt_command_ID3D12DeviceFactory_EnableExperimentalFeatures args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceFactory_EnableExperimentalFeatures_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_EnableExperimentalFeatures on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceFactory_EnableExperimentalFeatures_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_EnableExperimentalFeatures: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceFactory_EnableExperimentalFeatures _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceFactory_EnableExperimentalFeatures, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceFactory_EnableExperimentalFeatures);

    if (ctx->id3d12devicefactory_dispatch_overrides && ctx->id3d12devicefactory_dispatch_overrides->EnableExperimentalFeatures) {
        args.ret = ctx->id3d12devicefactory_dispatch_overrides->EnableExperimentalFeatures(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.NumFeatures, args.pIIDs, args.pConfigurationStructs, args.pConfigurationStructSizes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceFactory_EnableExperimentalFeatures_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceFactory::CreateDevice
 */

struct npt_command_ID3D12DeviceFactory_CreateDevice {
    void *_self;
    IUnknown * adapter;
    D3D_FEATURE_LEVEL FeatureLevel;
    const IID * riid;
    void ** ppvDevice;
    /* Shadow: the guest-allocated id for ppvDevice arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvDevice -> *ppvDevice} in the
     * context object table. */
    uint64_t _guest_id_ppvDevice;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12DeviceFactory_CreateDevice_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D12DeviceFactory_CreateDevice *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->adapter = (IUnknown *)npt_object_from_id(_id);
    }
    npt_decode_D3D_FEATURE_LEVEL(dec, &args->FeatureLevel);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvDevice = _gid;
        args->ppvDevice = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvDevice) return;
        *args->ppvDevice = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12DeviceFactory_CreateDevice_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceFactory_CreateDevice *args)
{
    args->adapter = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->adapter, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_ID3D12DeviceFactory_CreateDevice_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D12DeviceFactory_CreateDevice *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceFactory,
                                 NPT_METHOD_ID3D12DeviceFactory_CreateDevice),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvDevice (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12DeviceFactory_CreateDevice(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D12DeviceFactory_CreateDevice args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceFactory_CreateDevice_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_CreateDevice on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceFactory_CreateDevice_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceFactory_CreateDevice: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceFactory_CreateDevice _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceFactory_CreateDevice, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceFactory_CreateDevice);

    if (ctx->id3d12devicefactory_dispatch_overrides && ctx->id3d12devicefactory_dispatch_overrides->CreateDevice) {
        args.ret = ctx->id3d12devicefactory_dispatch_overrides->CreateDevice(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.adapter, args.FeatureLevel, args.riid, args.ppvDevice);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvDevice && *args.ppvDevice)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvDevice, *args.ppvDevice,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceFactory_CreateDevice_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D12DEVICEFACTORY_H */

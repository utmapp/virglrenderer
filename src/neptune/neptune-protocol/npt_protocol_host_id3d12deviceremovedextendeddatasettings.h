/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12deviceremovedextendeddatasettings.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS_H
#define NPT_PROTOCOL_HOST_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12DeviceRemovedExtendedDataSettings::SetAutoBreadcrumbsEnablement
 */

struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement {
    void *_self;
    D3D12_DRED_ENABLEMENT Enablement;
};

static inline void
npt_decode_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement_args_temp(struct npt_cs_decoder *dec,
                                                                                          struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_DRED_ENABLEMENT(dec, &args->Enablement);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement *args)
{
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement_reply(struct npt_cs_encoder *enc,
                                                                                      const struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedDataSettings,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement(struct npt_dispatch_context *ctx,
                                                                                  uint32_t cmd_flags,
                                                                                  uint64_t object_id)
{
    struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement);

    if (ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides && ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides->SetAutoBreadcrumbsEnablement) {
        ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides->SetAutoBreadcrumbsEnablement(ctx, &args, _original);
    } else {
        _original(args._self, args.Enablement);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceRemovedExtendedDataSettings_SetAutoBreadcrumbsEnablement_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceRemovedExtendedDataSettings::SetPageFaultEnablement
 */

struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement {
    void *_self;
    D3D12_DRED_ENABLEMENT Enablement;
};

static inline void
npt_decode_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement_args_temp(struct npt_cs_decoder *dec,
                                                                                    struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_DRED_ENABLEMENT(dec, &args->Enablement);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement *args)
{
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement_reply(struct npt_cs_encoder *enc,
                                                                                const struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedDataSettings,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement(struct npt_dispatch_context *ctx,
                                                                            uint32_t cmd_flags,
                                                                            uint64_t object_id)
{
    struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement);

    if (ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides && ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides->SetPageFaultEnablement) {
        ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides->SetPageFaultEnablement(ctx, &args, _original);
    } else {
        _original(args._self, args.Enablement);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceRemovedExtendedDataSettings_SetPageFaultEnablement_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceRemovedExtendedDataSettings::SetWatsonDumpEnablement
 */

struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement {
    void *_self;
    D3D12_DRED_ENABLEMENT Enablement;
};

static inline void
npt_decode_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement_args_temp(struct npt_cs_decoder *dec,
                                                                                     struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_DRED_ENABLEMENT(dec, &args->Enablement);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement *args)
{
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement_reply(struct npt_cs_encoder *enc,
                                                                                 const struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedDataSettings,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement(struct npt_dispatch_context *ctx,
                                                                             uint32_t cmd_flags,
                                                                             uint64_t object_id)
{
    struct npt_command_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement);

    if (ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides && ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides->SetWatsonDumpEnablement) {
        ctx->id3d12deviceremovedextendeddatasettings_dispatch_overrides->SetWatsonDumpEnablement(ctx, &args, _original);
    } else {
        _original(args._self, args.Enablement);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceRemovedExtendedDataSettings_SetWatsonDumpEnablement_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceRemovedExtendedDataSettings1::SetBreadcrumbContextEnablement
 */

struct npt_command_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement {
    void *_self;
    D3D12_DRED_ENABLEMENT Enablement;
};

static inline void
npt_decode_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement_args_temp(struct npt_cs_decoder *dec,
                                                                                             struct npt_command_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_DRED_ENABLEMENT(dec, &args->Enablement);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement *args)
{
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement_reply(struct npt_cs_encoder *enc,
                                                                                         const struct npt_command_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedDataSettings1,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement(struct npt_dispatch_context *ctx,
                                                                                     uint32_t cmd_flags,
                                                                                     uint64_t object_id)
{
    struct npt_command_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement);

    if (ctx->id3d12deviceremovedextendeddatasettings1_dispatch_overrides && ctx->id3d12deviceremovedextendeddatasettings1_dispatch_overrides->SetBreadcrumbContextEnablement) {
        ctx->id3d12deviceremovedextendeddatasettings1_dispatch_overrides->SetBreadcrumbContextEnablement(ctx, &args, _original);
    } else {
        _original(args._self, args.Enablement);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceRemovedExtendedDataSettings1_SetBreadcrumbContextEnablement_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12DeviceRemovedExtendedDataSettings2::UseMarkersOnlyAutoBreadcrumbs
 */

struct npt_command_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs {
    void *_self;
    BOOL MarkersOnly;
};

static inline void
npt_decode_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs_args_temp(struct npt_cs_decoder *dec,
                                                                                            struct npt_command_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs *args)
{
    /* Decode input parameters from the wire */
    npt_decode_BOOL(dec, &args->MarkersOnly);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs *args)
{
}

static inline void
npt_encode_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs_reply(struct npt_cs_encoder *enc,
                                                                                        const struct npt_command_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12DeviceRemovedExtendedDataSettings2,
                                 NPT_METHOD_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs(struct npt_dispatch_context *ctx,
                                                                                    uint32_t cmd_flags,
                                                                                    uint64_t object_id)
{
    struct npt_command_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs);

    if (ctx->id3d12deviceremovedextendeddatasettings2_dispatch_overrides && ctx->id3d12deviceremovedextendeddatasettings2_dispatch_overrides->UseMarkersOnlyAutoBreadcrumbs) {
        ctx->id3d12deviceremovedextendeddatasettings2_dispatch_overrides->UseMarkersOnlyAutoBreadcrumbs(ctx, &args, _original);
    } else {
        _original(args._self, args.MarkersOnly);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12DeviceRemovedExtendedDataSettings2_UseMarkersOnlyAutoBreadcrumbs_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D12DEVICEREMOVEDEXTENDEDDATASETTINGS_H */

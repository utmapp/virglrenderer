/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12graphicscommandlist.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D12GRAPHICSCOMMANDLIST_H
#define NPT_PROTOCOL_HOST_ID3D12GRAPHICSCOMMANDLIST_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12GraphicsCommandList::Close
 */

struct npt_command_ID3D12GraphicsCommandList_Close {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_Close_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D12GraphicsCommandList_Close *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12GraphicsCommandList_Close_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_Close *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_Close_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D12GraphicsCommandList_Close *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_Close),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_Close(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_Close args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_Close_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_Close on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_Close_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_Close: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_Close _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_Close, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_Close);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->Close) {
        args.ret = ctx->id3d12graphicscommandlist_dispatch_overrides->Close(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_Close_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::Reset
 */

struct npt_command_ID3D12GraphicsCommandList_Reset {
    void *_self;
    ID3D12CommandAllocator * pAllocator;
    ID3D12PipelineState * pInitialState;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_Reset_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_ID3D12GraphicsCommandList_Reset *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pAllocator = (ID3D12CommandAllocator *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pInitialState = (ID3D12PipelineState *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_Reset_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_Reset *args)
{
    args->pAllocator = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pAllocator, NPT_OBJECT_TYPE_ID3D12COMMANDALLOCATOR);
    args->pInitialState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pInitialState, NPT_OBJECT_TYPE_ID3D12PIPELINESTATE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_Reset_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_ID3D12GraphicsCommandList_Reset *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_Reset),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_Reset(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_Reset args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_Reset_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_Reset on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_Reset_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_Reset: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_Reset _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_Reset, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_Reset);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->Reset) {
        args.ret = ctx->id3d12graphicscommandlist_dispatch_overrides->Reset(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pAllocator, args.pInitialState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_Reset_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ClearState
 */

struct npt_command_ID3D12GraphicsCommandList_ClearState {
    void *_self;
    ID3D12PipelineState * pPipelineState;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ClearState_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12GraphicsCommandList_ClearState *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pPipelineState = (ID3D12PipelineState *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_ClearState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ClearState *args)
{
    args->pPipelineState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pPipelineState, NPT_OBJECT_TYPE_ID3D12PIPELINESTATE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearState_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12GraphicsCommandList_ClearState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ClearState(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ClearState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ClearState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ClearState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ClearState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ClearState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ClearState);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ClearState) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ClearState(ctx, &args, _original);
    } else {
        _original(args._self, args.pPipelineState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ClearState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::DrawInstanced
 */

struct npt_command_ID3D12GraphicsCommandList_DrawInstanced {
    void *_self;
    UINT VertexCountPerInstance;
    UINT InstanceCount;
    UINT StartVertexLocation;
    UINT StartInstanceLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_DrawInstanced_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D12GraphicsCommandList_DrawInstanced *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->VertexCountPerInstance);
    npt_decode_UINT(dec, &args->InstanceCount);
    npt_decode_UINT(dec, &args->StartVertexLocation);
    npt_decode_UINT(dec, &args->StartInstanceLocation);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12GraphicsCommandList_DrawInstanced_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_DrawInstanced *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_DrawInstanced_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D12GraphicsCommandList_DrawInstanced *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_DrawInstanced),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_DrawInstanced(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_DrawInstanced args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_DrawInstanced_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_DrawInstanced on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_DrawInstanced_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_DrawInstanced: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_DrawInstanced _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_DrawInstanced, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_DrawInstanced);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->DrawInstanced) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->DrawInstanced(ctx, &args, _original);
    } else {
        _original(args._self, args.VertexCountPerInstance, args.InstanceCount, args.StartVertexLocation, args.StartInstanceLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_DrawInstanced_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::DrawIndexedInstanced
 */

struct npt_command_ID3D12GraphicsCommandList_DrawIndexedInstanced {
    void *_self;
    UINT IndexCountPerInstance;
    UINT InstanceCount;
    UINT StartIndexLocation;
    INT BaseVertexLocation;
    UINT StartInstanceLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_DrawIndexedInstanced_args_temp(struct npt_cs_decoder *dec,
                                                                    struct npt_command_ID3D12GraphicsCommandList_DrawIndexedInstanced *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->IndexCountPerInstance);
    npt_decode_UINT(dec, &args->InstanceCount);
    npt_decode_UINT(dec, &args->StartIndexLocation);
    npt_decode_INT(dec, &args->BaseVertexLocation);
    npt_decode_UINT(dec, &args->StartInstanceLocation);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12GraphicsCommandList_DrawIndexedInstanced_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_DrawIndexedInstanced *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_DrawIndexedInstanced_reply(struct npt_cs_encoder *enc,
                                                                const struct npt_command_ID3D12GraphicsCommandList_DrawIndexedInstanced *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_DrawIndexedInstanced),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_DrawIndexedInstanced(struct npt_dispatch_context *ctx,
                                                            uint32_t cmd_flags,
                                                            uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_DrawIndexedInstanced args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_DrawIndexedInstanced_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_DrawIndexedInstanced on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_DrawIndexedInstanced_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_DrawIndexedInstanced: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_DrawIndexedInstanced _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_DrawIndexedInstanced, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_DrawIndexedInstanced);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->DrawIndexedInstanced) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->DrawIndexedInstanced(ctx, &args, _original);
    } else {
        _original(args._self, args.IndexCountPerInstance, args.InstanceCount, args.StartIndexLocation, args.BaseVertexLocation, args.StartInstanceLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_DrawIndexedInstanced_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::Dispatch
 */

struct npt_command_ID3D12GraphicsCommandList_Dispatch {
    void *_self;
    UINT ThreadGroupCountX;
    UINT ThreadGroupCountY;
    UINT ThreadGroupCountZ;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_Dispatch_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D12GraphicsCommandList_Dispatch *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->ThreadGroupCountX);
    npt_decode_UINT(dec, &args->ThreadGroupCountY);
    npt_decode_UINT(dec, &args->ThreadGroupCountZ);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_Dispatch_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_Dispatch *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_Dispatch_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D12GraphicsCommandList_Dispatch *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_Dispatch),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_Dispatch(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_Dispatch args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_Dispatch_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_Dispatch on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_Dispatch_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_Dispatch: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_Dispatch _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_Dispatch, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_Dispatch);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->Dispatch) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->Dispatch(ctx, &args, _original);
    } else {
        _original(args._self, args.ThreadGroupCountX, args.ThreadGroupCountY, args.ThreadGroupCountZ);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_Dispatch_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::CopyBufferRegion
 */

struct npt_command_ID3D12GraphicsCommandList_CopyBufferRegion {
    void *_self;
    ID3D12Resource * pDstBuffer;
    UINT64 DstOffset;
    ID3D12Resource * pSrcBuffer;
    UINT64 SrcOffset;
    UINT64 NumBytes;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_CopyBufferRegion_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D12GraphicsCommandList_CopyBufferRegion *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->DstOffset);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->SrcOffset);
    npt_decode_UINT64(dec, &args->NumBytes);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12GraphicsCommandList_CopyBufferRegion_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_CopyBufferRegion *args)
{
    args->pDstBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pSrcBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyBufferRegion_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D12GraphicsCommandList_CopyBufferRegion *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyBufferRegion),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_CopyBufferRegion(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_CopyBufferRegion args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_CopyBufferRegion_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyBufferRegion on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_CopyBufferRegion_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyBufferRegion: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_CopyBufferRegion _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_CopyBufferRegion, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_CopyBufferRegion);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->CopyBufferRegion) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->CopyBufferRegion(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstBuffer, args.DstOffset, args.pSrcBuffer, args.SrcOffset, args.NumBytes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_CopyBufferRegion_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::CopyTextureRegion
 */

struct npt_command_ID3D12GraphicsCommandList_CopyTextureRegion {
    void *_self;
    const D3D12_TEXTURE_COPY_LOCATION * pDst;
    UINT DstX;
    UINT DstY;
    UINT DstZ;
    const D3D12_TEXTURE_COPY_LOCATION * pSrc;
    const D3D12_BOX * pSrcBox;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_CopyTextureRegion_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12GraphicsCommandList_CopyTextureRegion *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDst = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_TEXTURE_COPY_LOCATION));
        if (!args->pDst) return;
        npt_decode_D3D12_TEXTURE_COPY_LOCATION(dec, (D3D12_TEXTURE_COPY_LOCATION *)args->pDst);
    } else {
        args->pDst = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DstX);
    npt_decode_UINT(dec, &args->DstY);
    npt_decode_UINT(dec, &args->DstZ);
    if (npt_decode_simple_pointer(dec)) {
        args->pSrc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_TEXTURE_COPY_LOCATION));
        if (!args->pSrc) return;
        npt_decode_D3D12_TEXTURE_COPY_LOCATION(dec, (D3D12_TEXTURE_COPY_LOCATION *)args->pSrc);
    } else {
        args->pSrc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pSrcBox = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_BOX));
        if (!args->pSrcBox) return;
        npt_decode_D3D12_BOX(dec, (D3D12_BOX *)args->pSrcBox);
    } else {
        args->pSrcBox = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12GraphicsCommandList_CopyTextureRegion_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_CopyTextureRegion *args)
{
    if (args->pDst)
        npt_replace_D3D12_TEXTURE_COPY_LOCATION_handle(ctx, (D3D12_TEXTURE_COPY_LOCATION *)args->pDst);
    if (args->pSrc)
        npt_replace_D3D12_TEXTURE_COPY_LOCATION_handle(ctx, (D3D12_TEXTURE_COPY_LOCATION *)args->pSrc);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyTextureRegion_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12GraphicsCommandList_CopyTextureRegion *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyTextureRegion),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_CopyTextureRegion(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_CopyTextureRegion args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_CopyTextureRegion_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyTextureRegion on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_CopyTextureRegion_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyTextureRegion: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_CopyTextureRegion _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_CopyTextureRegion, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_CopyTextureRegion);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->CopyTextureRegion) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->CopyTextureRegion(ctx, &args, _original);
    } else {
        _original(args._self, args.pDst, args.DstX, args.DstY, args.DstZ, args.pSrc, args.pSrcBox);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_CopyTextureRegion_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::CopyResource
 */

struct npt_command_ID3D12GraphicsCommandList_CopyResource {
    void *_self;
    ID3D12Resource * pDstResource;
    ID3D12Resource * pSrcResource;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_CopyResource_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12GraphicsCommandList_CopyResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_CopyResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_CopyResource *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyResource_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12GraphicsCommandList_CopyResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyResource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_CopyResource(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_CopyResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_CopyResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_CopyResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyResource: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_CopyResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_CopyResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_CopyResource);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->CopyResource) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->CopyResource(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.pSrcResource);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_CopyResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::CopyTiles
 */

struct npt_command_ID3D12GraphicsCommandList_CopyTiles {
    void *_self;
    ID3D12Resource * pTiledResource;
    const D3D12_TILED_RESOURCE_COORDINATE * pTileRegionStartCoordinate;
    const D3D12_TILE_REGION_SIZE * pTileRegionSize;
    ID3D12Resource * pBuffer;
    UINT64 BufferStartOffsetInBytes;
    D3D12_TILE_COPY_FLAGS Flags;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_CopyTiles_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12GraphicsCommandList_CopyTiles *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pTiledResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pTileRegionStartCoordinate = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_TILED_RESOURCE_COORDINATE));
        if (!args->pTileRegionStartCoordinate) return;
        npt_decode_D3D12_TILED_RESOURCE_COORDINATE(dec, (D3D12_TILED_RESOURCE_COORDINATE *)args->pTileRegionStartCoordinate);
    } else {
        args->pTileRegionStartCoordinate = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pTileRegionSize = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_TILE_REGION_SIZE));
        if (!args->pTileRegionSize) return;
        npt_decode_D3D12_TILE_REGION_SIZE(dec, (D3D12_TILE_REGION_SIZE *)args->pTileRegionSize);
    } else {
        args->pTileRegionSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->BufferStartOffsetInBytes);
    npt_decode_D3D12_TILE_COPY_FLAGS(dec, &args->Flags);
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12GraphicsCommandList_CopyTiles_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_CopyTiles *args)
{
    args->pTiledResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pTiledResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_CopyTiles_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12GraphicsCommandList_CopyTiles *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_CopyTiles),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_CopyTiles(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_CopyTiles args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_CopyTiles_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyTiles on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_CopyTiles_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_CopyTiles: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_CopyTiles _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_CopyTiles, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_CopyTiles);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->CopyTiles) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->CopyTiles(ctx, &args, _original);
    } else {
        _original(args._self, args.pTiledResource, args.pTileRegionStartCoordinate, args.pTileRegionSize, args.pBuffer, args.BufferStartOffsetInBytes, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_CopyTiles_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ResolveSubresource
 */

struct npt_command_ID3D12GraphicsCommandList_ResolveSubresource {
    void *_self;
    ID3D12Resource * pDstResource;
    UINT DstSubresource;
    ID3D12Resource * pSrcResource;
    UINT SrcSubresource;
    DXGI_FORMAT Format;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ResolveSubresource_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D12GraphicsCommandList_ResolveSubresource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->SrcSubresource);
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_ID3D12GraphicsCommandList_ResolveSubresource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ResolveSubresource *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ResolveSubresource_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D12GraphicsCommandList_ResolveSubresource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ResolveSubresource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ResolveSubresource(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ResolveSubresource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ResolveSubresource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ResolveSubresource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ResolveSubresource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ResolveSubresource: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ResolveSubresource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ResolveSubresource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ResolveSubresource);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ResolveSubresource) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ResolveSubresource(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.pSrcResource, args.SrcSubresource, args.Format);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ResolveSubresource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::IASetPrimitiveTopology
 */

struct npt_command_ID3D12GraphicsCommandList_IASetPrimitiveTopology {
    void *_self;
    D3D12_PRIMITIVE_TOPOLOGY PrimitiveTopology;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_IASetPrimitiveTopology_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D12GraphicsCommandList_IASetPrimitiveTopology *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_PRIMITIVE_TOPOLOGY(dec, &args->PrimitiveTopology);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_IASetPrimitiveTopology_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_IASetPrimitiveTopology *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_IASetPrimitiveTopology_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D12GraphicsCommandList_IASetPrimitiveTopology *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_IASetPrimitiveTopology),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_IASetPrimitiveTopology(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_IASetPrimitiveTopology args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_IASetPrimitiveTopology_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_IASetPrimitiveTopology on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_IASetPrimitiveTopology_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_IASetPrimitiveTopology: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_IASetPrimitiveTopology _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_IASetPrimitiveTopology, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_IASetPrimitiveTopology);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->IASetPrimitiveTopology) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->IASetPrimitiveTopology(ctx, &args, _original);
    } else {
        _original(args._self, args.PrimitiveTopology);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_IASetPrimitiveTopology_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::RSSetViewports
 */

struct npt_command_ID3D12GraphicsCommandList_RSSetViewports {
    void *_self;
    UINT NumViewports;
    const D3D12_VIEWPORT * pViewports;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_RSSetViewports_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D12GraphicsCommandList_RSSetViewports *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumViewports);
    uint64_t _cnt_pViewports = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pViewports = npt_decode_array_count_unchecked(dec);
        args->pViewports = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_VIEWPORT), _cnt_pViewports);
        if (!args->pViewports) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pViewports; _i++)
            npt_decode_D3D12_VIEWPORT(dec, (D3D12_VIEWPORT *)&args->pViewports[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pViewports = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pViewports && _cnt_pViewports < (uint64_t)(args->NumViewports)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_RSSetViewports_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_RSSetViewports *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_RSSetViewports_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D12GraphicsCommandList_RSSetViewports *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_RSSetViewports),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_RSSetViewports(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_RSSetViewports args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_RSSetViewports_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_RSSetViewports on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_RSSetViewports_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_RSSetViewports: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_RSSetViewports _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_RSSetViewports, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_RSSetViewports);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->RSSetViewports) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->RSSetViewports(ctx, &args, _original);
    } else {
        _original(args._self, args.NumViewports, args.pViewports);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_RSSetViewports_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::RSSetScissorRects
 */

struct npt_command_ID3D12GraphicsCommandList_RSSetScissorRects {
    void *_self;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_RSSetScissorRects_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12GraphicsCommandList_RSSetScissorRects *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumRects);
    uint64_t _cnt_pRects = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRects = npt_decode_array_count_unchecked(dec);
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RECT), _cnt_pRects);
        if (!args->pRects) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRects; _i++)
            npt_decode_D3D12_RECT(dec, (D3D12_RECT *)&args->pRects[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRects && _cnt_pRects < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_RSSetScissorRects_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_RSSetScissorRects *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_RSSetScissorRects_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12GraphicsCommandList_RSSetScissorRects *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_RSSetScissorRects),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_RSSetScissorRects(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_RSSetScissorRects args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_RSSetScissorRects_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_RSSetScissorRects on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_RSSetScissorRects_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_RSSetScissorRects: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_RSSetScissorRects _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_RSSetScissorRects, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_RSSetScissorRects);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->RSSetScissorRects) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->RSSetScissorRects(ctx, &args, _original);
    } else {
        _original(args._self, args.NumRects, args.pRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_RSSetScissorRects_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::OMSetBlendFactor
 */

struct npt_command_ID3D12GraphicsCommandList_OMSetBlendFactor {
    void *_self;
    const FLOAT * BlendFactor;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_OMSetBlendFactor_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D12GraphicsCommandList_OMSetBlendFactor *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _count = npt_decode_array_count_unchecked(dec);
        if (_count) {
            if (_count != (4)) { npt_cs_decoder_set_fatal(dec); return; }
            args->BlendFactor = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
            if (!args->BlendFactor) return;
            npt_decode_FLOAT_array(dec, (FLOAT *)args->BlendFactor, 4);
        } else {
            args->BlendFactor = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_OMSetBlendFactor_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_OMSetBlendFactor *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_OMSetBlendFactor_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D12GraphicsCommandList_OMSetBlendFactor *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_OMSetBlendFactor),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_OMSetBlendFactor(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_OMSetBlendFactor args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_OMSetBlendFactor_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_OMSetBlendFactor on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_OMSetBlendFactor_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_OMSetBlendFactor: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_OMSetBlendFactor _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_OMSetBlendFactor, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_OMSetBlendFactor);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->OMSetBlendFactor) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->OMSetBlendFactor(ctx, &args, _original);
    } else {
        _original(args._self, args.BlendFactor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_OMSetBlendFactor_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::OMSetStencilRef
 */

struct npt_command_ID3D12GraphicsCommandList_OMSetStencilRef {
    void *_self;
    UINT StencilRef;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_OMSetStencilRef_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12GraphicsCommandList_OMSetStencilRef *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StencilRef);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_OMSetStencilRef_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_OMSetStencilRef *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_OMSetStencilRef_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12GraphicsCommandList_OMSetStencilRef *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_OMSetStencilRef),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_OMSetStencilRef(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_OMSetStencilRef args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_OMSetStencilRef_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_OMSetStencilRef on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_OMSetStencilRef_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_OMSetStencilRef: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_OMSetStencilRef _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_OMSetStencilRef, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_OMSetStencilRef);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->OMSetStencilRef) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->OMSetStencilRef(ctx, &args, _original);
    } else {
        _original(args._self, args.StencilRef);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_OMSetStencilRef_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetPipelineState
 */

struct npt_command_ID3D12GraphicsCommandList_SetPipelineState {
    void *_self;
    ID3D12PipelineState * pPipelineState;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetPipelineState_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D12GraphicsCommandList_SetPipelineState *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pPipelineState = (ID3D12PipelineState *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetPipelineState_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetPipelineState *args)
{
    args->pPipelineState = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pPipelineState, NPT_OBJECT_TYPE_ID3D12PIPELINESTATE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetPipelineState_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D12GraphicsCommandList_SetPipelineState *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetPipelineState),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetPipelineState(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetPipelineState args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetPipelineState_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetPipelineState on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetPipelineState_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetPipelineState: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetPipelineState _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetPipelineState, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetPipelineState);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetPipelineState) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetPipelineState(ctx, &args, _original);
    } else {
        _original(args._self, args.pPipelineState);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetPipelineState_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ResourceBarrier
 */

struct npt_command_ID3D12GraphicsCommandList_ResourceBarrier {
    void *_self;
    UINT NumBarriers;
    const D3D12_RESOURCE_BARRIER * pBarriers;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ResourceBarrier_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12GraphicsCommandList_ResourceBarrier *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumBarriers);
    uint64_t _cnt_pBarriers = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pBarriers = npt_decode_array_count_unchecked(dec);
        args->pBarriers = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RESOURCE_BARRIER), _cnt_pBarriers);
        if (!args->pBarriers) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pBarriers; _i++)
            npt_decode_D3D12_RESOURCE_BARRIER(dec, (D3D12_RESOURCE_BARRIER *)&args->pBarriers[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pBarriers = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pBarriers && _cnt_pBarriers < (uint64_t)(args->NumBarriers)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_ResourceBarrier_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ResourceBarrier *args)
{
    if (args->pBarriers) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBarriers); _i++)
            npt_replace_D3D12_RESOURCE_BARRIER_handle(ctx, &((D3D12_RESOURCE_BARRIER *)args->pBarriers)[_i]);
    }
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ResourceBarrier_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12GraphicsCommandList_ResourceBarrier *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ResourceBarrier),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ResourceBarrier(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ResourceBarrier args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ResourceBarrier_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ResourceBarrier on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ResourceBarrier_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ResourceBarrier: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ResourceBarrier _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ResourceBarrier, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ResourceBarrier);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ResourceBarrier) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ResourceBarrier(ctx, &args, _original);
    } else {
        _original(args._self, args.NumBarriers, args.pBarriers);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ResourceBarrier_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ExecuteBundle
 */

struct npt_command_ID3D12GraphicsCommandList_ExecuteBundle {
    void *_self;
    ID3D12GraphicsCommandList * pCommandList;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ExecuteBundle_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D12GraphicsCommandList_ExecuteBundle *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCommandList = (ID3D12GraphicsCommandList *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_ExecuteBundle_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ExecuteBundle *args)
{
    args->pCommandList = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCommandList, NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ExecuteBundle_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D12GraphicsCommandList_ExecuteBundle *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ExecuteBundle),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ExecuteBundle(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ExecuteBundle args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ExecuteBundle_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ExecuteBundle on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ExecuteBundle_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ExecuteBundle: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ExecuteBundle _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ExecuteBundle, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ExecuteBundle);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ExecuteBundle) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ExecuteBundle(ctx, &args, _original);
    } else {
        _original(args._self, args.pCommandList);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ExecuteBundle_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetDescriptorHeaps
 */

struct npt_command_ID3D12GraphicsCommandList_SetDescriptorHeaps {
    void *_self;
    UINT NumDescriptorHeaps;
    ID3D12DescriptorHeap ** ppDescriptorHeaps;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetDescriptorHeaps_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D12GraphicsCommandList_SetDescriptorHeaps *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumDescriptorHeaps);
    uint64_t _cnt_ppDescriptorHeaps = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppDescriptorHeaps) {
        args->ppDescriptorHeaps = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppDescriptorHeaps);
        if (!args->ppDescriptorHeaps) return;
        for (uint64_t _i = 0; _i < _cnt_ppDescriptorHeaps; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppDescriptorHeaps[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppDescriptorHeaps = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppDescriptorHeaps && _cnt_ppDescriptorHeaps < (uint64_t)(args->NumDescriptorHeaps)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetDescriptorHeaps_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetDescriptorHeaps *args)
{
    if (args->ppDescriptorHeaps) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumDescriptorHeaps); _i++)
            args->ppDescriptorHeaps[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppDescriptorHeaps[_i], NPT_OBJECT_TYPE_ID3D12DESCRIPTORHEAP);
    }
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetDescriptorHeaps_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D12GraphicsCommandList_SetDescriptorHeaps *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetDescriptorHeaps),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetDescriptorHeaps(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetDescriptorHeaps args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetDescriptorHeaps_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetDescriptorHeaps on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetDescriptorHeaps_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetDescriptorHeaps: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetDescriptorHeaps _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetDescriptorHeaps, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetDescriptorHeaps);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetDescriptorHeaps) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetDescriptorHeaps(ctx, &args, _original);
    } else {
        _original(args._self, args.NumDescriptorHeaps, args.ppDescriptorHeaps);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetDescriptorHeaps_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootSignature
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootSignature {
    void *_self;
    ID3D12RootSignature * pRootSignature;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetComputeRootSignature_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D12GraphicsCommandList_SetComputeRootSignature *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRootSignature = (ID3D12RootSignature *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetComputeRootSignature_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetComputeRootSignature *args)
{
    args->pRootSignature = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRootSignature, NPT_OBJECT_TYPE_ID3D12ROOTSIGNATURE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootSignature_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D12GraphicsCommandList_SetComputeRootSignature *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootSignature),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetComputeRootSignature(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetComputeRootSignature args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetComputeRootSignature_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootSignature on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetComputeRootSignature_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootSignature: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetComputeRootSignature _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetComputeRootSignature, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetComputeRootSignature);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootSignature) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootSignature(ctx, &args, _original);
    } else {
        _original(args._self, args.pRootSignature);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetComputeRootSignature_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootSignature
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootSignature {
    void *_self;
    ID3D12RootSignature * pRootSignature;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootSignature_args_temp(struct npt_cs_decoder *dec,
                                                                        struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootSignature *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRootSignature = (ID3D12RootSignature *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootSignature_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootSignature *args)
{
    args->pRootSignature = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRootSignature, NPT_OBJECT_TYPE_ID3D12ROOTSIGNATURE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootSignature_reply(struct npt_cs_encoder *enc,
                                                                    const struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootSignature *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootSignature),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetGraphicsRootSignature(struct npt_dispatch_context *ctx,
                                                                uint32_t cmd_flags,
                                                                uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootSignature args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootSignature_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootSignature on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootSignature_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootSignature: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetGraphicsRootSignature _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetGraphicsRootSignature, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetGraphicsRootSignature);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootSignature) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootSignature(ctx, &args, _original);
    } else {
        _original(args._self, args.pRootSignature);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootSignature_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootDescriptorTable
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_args_temp(struct npt_cs_decoder *dec,
                                                                             struct npt_command_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_DESCRIPTOR_HANDLE(dec, &args->BaseDescriptor);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_reply(struct npt_cs_encoder *enc,
                                                                         const struct npt_command_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable(struct npt_dispatch_context *ctx,
                                                                     uint32_t cmd_flags,
                                                                     uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootDescriptorTable on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootDescriptorTable: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootDescriptorTable) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootDescriptorTable(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BaseDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetComputeRootDescriptorTable_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootDescriptorTable
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_DESCRIPTOR_HANDLE BaseDescriptor;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_args_temp(struct npt_cs_decoder *dec,
                                                                              struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_DESCRIPTOR_HANDLE(dec, &args->BaseDescriptor);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_reply(struct npt_cs_encoder *enc,
                                                                          const struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable(struct npt_dispatch_context *ctx,
                                                                      uint32_t cmd_flags,
                                                                      uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootDescriptorTable) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootDescriptorTable(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BaseDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootDescriptorTable_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetComputeRoot32BitConstant
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant {
    void *_self;
    UINT RootParameterIndex;
    UINT SrcData;
    UINT DestOffsetIn32BitValues;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_args_temp(struct npt_cs_decoder *dec,
                                                                           struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_UINT(dec, &args->SrcData);
    npt_decode_UINT(dec, &args->DestOffsetIn32BitValues);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_reply(struct npt_cs_encoder *enc,
                                                                       const struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant(struct npt_dispatch_context *ctx,
                                                                   uint32_t cmd_flags,
                                                                   uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRoot32BitConstant on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRoot32BitConstant: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRoot32BitConstant) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRoot32BitConstant(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.SrcData, args.DestOffsetIn32BitValues);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstant_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRoot32BitConstant
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant {
    void *_self;
    UINT RootParameterIndex;
    UINT SrcData;
    UINT DestOffsetIn32BitValues;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_UINT(dec, &args->SrcData);
    npt_decode_UINT(dec, &args->DestOffsetIn32BitValues);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRoot32BitConstant) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRoot32BitConstant(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.SrcData, args.DestOffsetIn32BitValues);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstant_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetComputeRoot32BitConstants
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants {
    void *_self;
    UINT RootParameterIndex;
    UINT Num32BitValuesToSet;
    const void * pSrcData;
    UINT DestOffsetIn32BitValues;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_UINT(dec, &args->Num32BitValuesToSet);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pSrcData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pSrcData) return;
            npt_decode_blob_array(dec, (void *)args->pSrcData, _blob_size);
        } else {
            args->pSrcData = NULL;
        }
    }
    npt_decode_UINT(dec, &args->DestOffsetIn32BitValues);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRoot32BitConstants on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRoot32BitConstants: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRoot32BitConstants) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRoot32BitConstants(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.Num32BitValuesToSet, args.pSrcData, args.DestOffsetIn32BitValues);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetComputeRoot32BitConstants_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRoot32BitConstants
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants {
    void *_self;
    UINT RootParameterIndex;
    UINT Num32BitValuesToSet;
    const void * pSrcData;
    UINT DestOffsetIn32BitValues;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_args_temp(struct npt_cs_decoder *dec,
                                                                             struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_UINT(dec, &args->Num32BitValuesToSet);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pSrcData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pSrcData) return;
            npt_decode_blob_array(dec, (void *)args->pSrcData, _blob_size);
        } else {
            args->pSrcData = NULL;
        }
    }
    npt_decode_UINT(dec, &args->DestOffsetIn32BitValues);
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_reply(struct npt_cs_encoder *enc,
                                                                         const struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants(struct npt_dispatch_context *ctx,
                                                                     uint32_t cmd_flags,
                                                                     uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRoot32BitConstants) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRoot32BitConstants(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.Num32BitValuesToSet, args.pSrcData, args.DestOffsetIn32BitValues);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetGraphicsRoot32BitConstants_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootConstantBufferView
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_args_temp(struct npt_cs_decoder *dec,
                                                                                struct npt_command_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->BufferLocation);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_reply(struct npt_cs_encoder *enc,
                                                                            const struct npt_command_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView(struct npt_dispatch_context *ctx,
                                                                        uint32_t cmd_flags,
                                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootConstantBufferView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootConstantBufferView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootConstantBufferView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootConstantBufferView(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BufferLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetComputeRootConstantBufferView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootConstantBufferView
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_args_temp(struct npt_cs_decoder *dec,
                                                                                 struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->BufferLocation);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_reply(struct npt_cs_encoder *enc,
                                                                             const struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView(struct npt_dispatch_context *ctx,
                                                                         uint32_t cmd_flags,
                                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootConstantBufferView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootConstantBufferView(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BufferLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootConstantBufferView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootShaderResourceView
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_args_temp(struct npt_cs_decoder *dec,
                                                                                struct npt_command_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->BufferLocation);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_reply(struct npt_cs_encoder *enc,
                                                                            const struct npt_command_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView(struct npt_dispatch_context *ctx,
                                                                        uint32_t cmd_flags,
                                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootShaderResourceView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootShaderResourceView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootShaderResourceView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootShaderResourceView(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BufferLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetComputeRootShaderResourceView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootShaderResourceView
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_args_temp(struct npt_cs_decoder *dec,
                                                                                 struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->BufferLocation);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_reply(struct npt_cs_encoder *enc,
                                                                             const struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView(struct npt_dispatch_context *ctx,
                                                                         uint32_t cmd_flags,
                                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootShaderResourceView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootShaderResourceView(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BufferLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootShaderResourceView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetComputeRootUnorderedAccessView
 */

struct npt_command_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_args_temp(struct npt_cs_decoder *dec,
                                                                                 struct npt_command_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->BufferLocation);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_reply(struct npt_cs_encoder *enc,
                                                                             const struct npt_command_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView(struct npt_dispatch_context *ctx,
                                                                         uint32_t cmd_flags,
                                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootUnorderedAccessView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetComputeRootUnorderedAccessView(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BufferLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetComputeRootUnorderedAccessView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetGraphicsRootUnorderedAccessView
 */

struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView {
    void *_self;
    UINT RootParameterIndex;
    D3D12_GPU_VIRTUAL_ADDRESS BufferLocation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_args_temp(struct npt_cs_decoder *dec,
                                                                                  struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->RootParameterIndex);
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->BufferLocation);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_reply(struct npt_cs_encoder *enc,
                                                                              const struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView(struct npt_dispatch_context *ctx,
                                                                          uint32_t cmd_flags,
                                                                          uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootUnorderedAccessView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetGraphicsRootUnorderedAccessView(ctx, &args, _original);
    } else {
        _original(args._self, args.RootParameterIndex, args.BufferLocation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetGraphicsRootUnorderedAccessView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::IASetIndexBuffer
 */

struct npt_command_ID3D12GraphicsCommandList_IASetIndexBuffer {
    void *_self;
    const D3D12_INDEX_BUFFER_VIEW * pView;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_IASetIndexBuffer_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D12GraphicsCommandList_IASetIndexBuffer *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pView = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_INDEX_BUFFER_VIEW));
        if (!args->pView) return;
        npt_decode_D3D12_INDEX_BUFFER_VIEW(dec, (D3D12_INDEX_BUFFER_VIEW *)args->pView);
    } else {
        args->pView = NULL;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList_IASetIndexBuffer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_IASetIndexBuffer *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_IASetIndexBuffer_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D12GraphicsCommandList_IASetIndexBuffer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_IASetIndexBuffer),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_IASetIndexBuffer(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_IASetIndexBuffer args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_IASetIndexBuffer_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_IASetIndexBuffer on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_IASetIndexBuffer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_IASetIndexBuffer: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_IASetIndexBuffer _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_IASetIndexBuffer, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_IASetIndexBuffer);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->IASetIndexBuffer) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->IASetIndexBuffer(ctx, &args, _original);
    } else {
        _original(args._self, args.pView);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_IASetIndexBuffer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::IASetVertexBuffers
 */

struct npt_command_ID3D12GraphicsCommandList_IASetVertexBuffers {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    const D3D12_VERTEX_BUFFER_VIEW * pViews;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_IASetVertexBuffers_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D12GraphicsCommandList_IASetVertexBuffers *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_pViews = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pViews = npt_decode_array_count_unchecked(dec);
        args->pViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_VERTEX_BUFFER_VIEW), _cnt_pViews);
        if (!args->pViews) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pViews; _i++)
            npt_decode_D3D12_VERTEX_BUFFER_VIEW(dec, (D3D12_VERTEX_BUFFER_VIEW *)&args->pViews[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pViews && _cnt_pViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_IASetVertexBuffers_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_IASetVertexBuffers *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_IASetVertexBuffers_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D12GraphicsCommandList_IASetVertexBuffers *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_IASetVertexBuffers),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_IASetVertexBuffers(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_IASetVertexBuffers args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_IASetVertexBuffers_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_IASetVertexBuffers on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_IASetVertexBuffers_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_IASetVertexBuffers: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_IASetVertexBuffers _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_IASetVertexBuffers, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_IASetVertexBuffers);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->IASetVertexBuffers) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->IASetVertexBuffers(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.pViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_IASetVertexBuffers_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SOSetTargets
 */

struct npt_command_ID3D12GraphicsCommandList_SOSetTargets {
    void *_self;
    UINT StartSlot;
    UINT NumViews;
    const D3D12_STREAM_OUTPUT_BUFFER_VIEW * pViews;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SOSetTargets_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12GraphicsCommandList_SOSetTargets *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->StartSlot);
    npt_decode_UINT(dec, &args->NumViews);
    uint64_t _cnt_pViews = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pViews = npt_decode_array_count_unchecked(dec);
        args->pViews = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_STREAM_OUTPUT_BUFFER_VIEW), _cnt_pViews);
        if (!args->pViews) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pViews; _i++)
            npt_decode_D3D12_STREAM_OUTPUT_BUFFER_VIEW(dec, (D3D12_STREAM_OUTPUT_BUFFER_VIEW *)&args->pViews[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pViews = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pViews && _cnt_pViews < (uint64_t)(args->NumViews)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_SOSetTargets_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SOSetTargets *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SOSetTargets_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12GraphicsCommandList_SOSetTargets *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SOSetTargets),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SOSetTargets(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SOSetTargets args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SOSetTargets_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SOSetTargets on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SOSetTargets_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SOSetTargets: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SOSetTargets _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SOSetTargets, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SOSetTargets);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SOSetTargets) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SOSetTargets(ctx, &args, _original);
    } else {
        _original(args._self, args.StartSlot, args.NumViews, args.pViews);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SOSetTargets_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::OMSetRenderTargets
 */

struct npt_command_ID3D12GraphicsCommandList_OMSetRenderTargets {
    void *_self;
    UINT NumRenderTargetDescriptors;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pRenderTargetDescriptors;
    BOOL RTsSingleHandleToDescriptorRange;
    const D3D12_CPU_DESCRIPTOR_HANDLE * pDepthStencilDescriptor;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_OMSetRenderTargets_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D12GraphicsCommandList_OMSetRenderTargets *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumRenderTargetDescriptors);
    uint64_t _cnt_pRenderTargetDescriptors = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRenderTargetDescriptors = npt_decode_array_count_unchecked(dec);
        args->pRenderTargetDescriptors = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_CPU_DESCRIPTOR_HANDLE), _cnt_pRenderTargetDescriptors);
        if (!args->pRenderTargetDescriptors) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRenderTargetDescriptors; _i++)
            npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, (D3D12_CPU_DESCRIPTOR_HANDLE *)&args->pRenderTargetDescriptors[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRenderTargetDescriptors = NULL;
    }
    npt_decode_BOOL(dec, &args->RTsSingleHandleToDescriptorRange);
    if (npt_decode_simple_pointer(dec)) {
        args->pDepthStencilDescriptor = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_CPU_DESCRIPTOR_HANDLE));
        if (!args->pDepthStencilDescriptor) return;
        npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, (D3D12_CPU_DESCRIPTOR_HANDLE *)args->pDepthStencilDescriptor);
    } else {
        args->pDepthStencilDescriptor = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRenderTargetDescriptors && _cnt_pRenderTargetDescriptors < (uint64_t)(args->RTsSingleHandleToDescriptorRange ? 1 : args->NumRenderTargetDescriptors)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12GraphicsCommandList_OMSetRenderTargets_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_OMSetRenderTargets *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_OMSetRenderTargets_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D12GraphicsCommandList_OMSetRenderTargets *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_OMSetRenderTargets),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_OMSetRenderTargets(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_OMSetRenderTargets args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_OMSetRenderTargets_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_OMSetRenderTargets on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_OMSetRenderTargets_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_OMSetRenderTargets: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_OMSetRenderTargets _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_OMSetRenderTargets, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_OMSetRenderTargets);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->OMSetRenderTargets) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->OMSetRenderTargets(ctx, &args, _original);
    } else {
        _original(args._self, args.NumRenderTargetDescriptors, args.pRenderTargetDescriptors, args.RTsSingleHandleToDescriptorRange, args.pDepthStencilDescriptor);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_OMSetRenderTargets_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ClearDepthStencilView
 */

struct npt_command_ID3D12GraphicsCommandList_ClearDepthStencilView {
    void *_self;
    D3D12_CPU_DESCRIPTOR_HANDLE DepthStencilView;
    D3D12_CLEAR_FLAGS ClearFlags;
    FLOAT Depth;
    UINT8 Stencil;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ClearDepthStencilView_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D12GraphicsCommandList_ClearDepthStencilView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->DepthStencilView);
    npt_decode_D3D12_CLEAR_FLAGS(dec, &args->ClearFlags);
    npt_decode_FLOAT(dec, &args->Depth);
    npt_decode_UINT8(dec, &args->Stencil);
    npt_decode_UINT(dec, &args->NumRects);
    uint64_t _cnt_pRects = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRects = npt_decode_array_count_unchecked(dec);
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RECT), _cnt_pRects);
        if (!args->pRects) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRects; _i++)
            npt_decode_D3D12_RECT(dec, (D3D12_RECT *)&args->pRects[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRects && _cnt_pRects < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12GraphicsCommandList_ClearDepthStencilView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ClearDepthStencilView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearDepthStencilView_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D12GraphicsCommandList_ClearDepthStencilView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearDepthStencilView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ClearDepthStencilView(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ClearDepthStencilView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ClearDepthStencilView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearDepthStencilView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ClearDepthStencilView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearDepthStencilView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ClearDepthStencilView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ClearDepthStencilView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ClearDepthStencilView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ClearDepthStencilView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ClearDepthStencilView(ctx, &args, _original);
    } else {
        _original(args._self, args.DepthStencilView, args.ClearFlags, args.Depth, args.Stencil, args.NumRects, args.pRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ClearDepthStencilView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ClearRenderTargetView
 */

struct npt_command_ID3D12GraphicsCommandList_ClearRenderTargetView {
    void *_self;
    D3D12_CPU_DESCRIPTOR_HANDLE RenderTargetView;
    const FLOAT * ColorRGBA;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ClearRenderTargetView_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D12GraphicsCommandList_ClearRenderTargetView *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->RenderTargetView);
    (void)npt_decode_array_count(dec, 4);
    args->ColorRGBA = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
    if (!args->ColorRGBA) return;
    npt_decode_FLOAT_array(dec, (FLOAT *)args->ColorRGBA, 4);
    npt_decode_UINT(dec, &args->NumRects);
    uint64_t _cnt_pRects = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRects = npt_decode_array_count_unchecked(dec);
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RECT), _cnt_pRects);
        if (!args->pRects) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRects; _i++)
            npt_decode_D3D12_RECT(dec, (D3D12_RECT *)&args->pRects[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRects && _cnt_pRects < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12GraphicsCommandList_ClearRenderTargetView_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ClearRenderTargetView *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearRenderTargetView_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D12GraphicsCommandList_ClearRenderTargetView *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearRenderTargetView),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ClearRenderTargetView(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ClearRenderTargetView args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ClearRenderTargetView_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearRenderTargetView on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ClearRenderTargetView_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearRenderTargetView: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ClearRenderTargetView _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ClearRenderTargetView, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ClearRenderTargetView);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ClearRenderTargetView) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ClearRenderTargetView(ctx, &args, _original);
    } else {
        _original(args._self, args.RenderTargetView, args.ColorRGBA, args.NumRects, args.pRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ClearRenderTargetView_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ClearUnorderedAccessViewUint
 */

struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint {
    void *_self;
    D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap;
    D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle;
    ID3D12Resource * pResource;
    const UINT * Values;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_GPU_DESCRIPTOR_HANDLE(dec, &args->ViewGPUHandleInCurrentHeap);
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->ViewCPUHandle);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    (void)npt_decode_array_count(dec, 4);
    args->Values = npt_cs_decoder_alloc_temp_array(dec, sizeof(UINT), (4));
    if (!args->Values) return;
    npt_decode_UINT_array(dec, (UINT *)args->Values, 4);
    npt_decode_UINT(dec, &args->NumRects);
    uint64_t _cnt_pRects = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRects = npt_decode_array_count_unchecked(dec);
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RECT), _cnt_pRects);
        if (!args->pRects) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRects; _i++)
            npt_decode_D3D12_RECT(dec, (D3D12_RECT *)&args->pRects[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRects && _cnt_pRects < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ClearUnorderedAccessViewUint) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ClearUnorderedAccessViewUint(ctx, &args, _original);
    } else {
        _original(args._self, args.ViewGPUHandleInCurrentHeap, args.ViewCPUHandle, args.pResource, args.Values, args.NumRects, args.pRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewUint_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ClearUnorderedAccessViewFloat
 */

struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat {
    void *_self;
    D3D12_GPU_DESCRIPTOR_HANDLE ViewGPUHandleInCurrentHeap;
    D3D12_CPU_DESCRIPTOR_HANDLE ViewCPUHandle;
    ID3D12Resource * pResource;
    const FLOAT * Values;
    UINT NumRects;
    const D3D12_RECT * pRects;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_args_temp(struct npt_cs_decoder *dec,
                                                                             struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_GPU_DESCRIPTOR_HANDLE(dec, &args->ViewGPUHandleInCurrentHeap);
    npt_decode_D3D12_CPU_DESCRIPTOR_HANDLE(dec, &args->ViewCPUHandle);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    (void)npt_decode_array_count(dec, 4);
    args->Values = npt_cs_decoder_alloc_temp_array(dec, sizeof(FLOAT), (4));
    if (!args->Values) return;
    npt_decode_FLOAT_array(dec, (FLOAT *)args->Values, 4);
    npt_decode_UINT(dec, &args->NumRects);
    uint64_t _cnt_pRects = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRects = npt_decode_array_count_unchecked(dec);
        args->pRects = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RECT), _cnt_pRects);
        if (!args->pRects) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRects; _i++)
            npt_decode_D3D12_RECT(dec, (D3D12_RECT *)&args->pRects[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRects = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRects && _cnt_pRects < (uint64_t)(args->NumRects)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_reply(struct npt_cs_encoder *enc,
                                                                         const struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat(struct npt_dispatch_context *ctx,
                                                                     uint32_t cmd_flags,
                                                                     uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ClearUnorderedAccessViewFloat) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ClearUnorderedAccessViewFloat(ctx, &args, _original);
    } else {
        _original(args._self, args.ViewGPUHandleInCurrentHeap, args.ViewCPUHandle, args.pResource, args.Values, args.NumRects, args.pRects);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ClearUnorderedAccessViewFloat_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::DiscardResource
 */

struct npt_command_ID3D12GraphicsCommandList_DiscardResource {
    void *_self;
    ID3D12Resource * pResource;
    const D3D12_DISCARD_REGION * pRegion;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_DiscardResource_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12GraphicsCommandList_DiscardResource *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pRegion = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_DISCARD_REGION));
        if (!args->pRegion) return;
        npt_decode_D3D12_DISCARD_REGION(dec, (D3D12_DISCARD_REGION *)args->pRegion);
    } else {
        args->pRegion = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList_DiscardResource_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_DiscardResource *args)
{
    args->pResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_DiscardResource_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12GraphicsCommandList_DiscardResource *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_DiscardResource),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_DiscardResource(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_DiscardResource args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_DiscardResource_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_DiscardResource on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_DiscardResource_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_DiscardResource: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_DiscardResource _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_DiscardResource, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_DiscardResource);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->DiscardResource) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->DiscardResource(ctx, &args, _original);
    } else {
        _original(args._self, args.pResource, args.pRegion);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_DiscardResource_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::BeginQuery
 */

struct npt_command_ID3D12GraphicsCommandList_BeginQuery {
    void *_self;
    ID3D12QueryHeap * pQueryHeap;
    D3D12_QUERY_TYPE Type;
    UINT Index;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_BeginQuery_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12GraphicsCommandList_BeginQuery *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pQueryHeap = (ID3D12QueryHeap *)npt_object_from_id(_id);
    }
    npt_decode_D3D12_QUERY_TYPE(dec, &args->Type);
    npt_decode_UINT(dec, &args->Index);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_BeginQuery_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_BeginQuery *args)
{
    args->pQueryHeap = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pQueryHeap, NPT_OBJECT_TYPE_ID3D12QUERYHEAP);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_BeginQuery_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12GraphicsCommandList_BeginQuery *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_BeginQuery),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_BeginQuery(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_BeginQuery args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_BeginQuery_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_BeginQuery on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_BeginQuery_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_BeginQuery: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_BeginQuery _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_BeginQuery, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_BeginQuery);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->BeginQuery) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->BeginQuery(ctx, &args, _original);
    } else {
        _original(args._self, args.pQueryHeap, args.Type, args.Index);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_BeginQuery_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::EndQuery
 */

struct npt_command_ID3D12GraphicsCommandList_EndQuery {
    void *_self;
    ID3D12QueryHeap * pQueryHeap;
    D3D12_QUERY_TYPE Type;
    UINT Index;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_EndQuery_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D12GraphicsCommandList_EndQuery *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pQueryHeap = (ID3D12QueryHeap *)npt_object_from_id(_id);
    }
    npt_decode_D3D12_QUERY_TYPE(dec, &args->Type);
    npt_decode_UINT(dec, &args->Index);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_EndQuery_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_EndQuery *args)
{
    args->pQueryHeap = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pQueryHeap, NPT_OBJECT_TYPE_ID3D12QUERYHEAP);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_EndQuery_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D12GraphicsCommandList_EndQuery *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_EndQuery),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_EndQuery(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_EndQuery args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_EndQuery_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_EndQuery on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_EndQuery_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_EndQuery: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_EndQuery _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_EndQuery, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_EndQuery);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->EndQuery) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->EndQuery(ctx, &args, _original);
    } else {
        _original(args._self, args.pQueryHeap, args.Type, args.Index);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_EndQuery_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ResolveQueryData
 */

struct npt_command_ID3D12GraphicsCommandList_ResolveQueryData {
    void *_self;
    ID3D12QueryHeap * pQueryHeap;
    D3D12_QUERY_TYPE Type;
    UINT StartIndex;
    UINT NumQueries;
    ID3D12Resource * pDestinationBuffer;
    UINT64 AlignedDestinationBufferOffset;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ResolveQueryData_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D12GraphicsCommandList_ResolveQueryData *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pQueryHeap = (ID3D12QueryHeap *)npt_object_from_id(_id);
    }
    npt_decode_D3D12_QUERY_TYPE(dec, &args->Type);
    npt_decode_UINT(dec, &args->StartIndex);
    npt_decode_UINT(dec, &args->NumQueries);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDestinationBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->AlignedDestinationBufferOffset);
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12GraphicsCommandList_ResolveQueryData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ResolveQueryData *args)
{
    args->pQueryHeap = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pQueryHeap, NPT_OBJECT_TYPE_ID3D12QUERYHEAP);
    args->pDestinationBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDestinationBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ResolveQueryData_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D12GraphicsCommandList_ResolveQueryData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ResolveQueryData),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ResolveQueryData(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ResolveQueryData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ResolveQueryData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ResolveQueryData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ResolveQueryData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ResolveQueryData: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ResolveQueryData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ResolveQueryData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ResolveQueryData);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ResolveQueryData) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ResolveQueryData(ctx, &args, _original);
    } else {
        _original(args._self, args.pQueryHeap, args.Type, args.StartIndex, args.NumQueries, args.pDestinationBuffer, args.AlignedDestinationBufferOffset);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ResolveQueryData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetPredication
 */

struct npt_command_ID3D12GraphicsCommandList_SetPredication {
    void *_self;
    ID3D12Resource * pBuffer;
    UINT64 AlignedBufferOffset;
    D3D12_PREDICATION_OP Operation;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetPredication_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D12GraphicsCommandList_SetPredication *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->AlignedBufferOffset);
    npt_decode_D3D12_PREDICATION_OP(dec, &args->Operation);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetPredication_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetPredication *args)
{
    args->pBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetPredication_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D12GraphicsCommandList_SetPredication *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetPredication),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetPredication(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetPredication args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetPredication_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetPredication on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetPredication_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetPredication: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetPredication _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetPredication, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetPredication);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetPredication) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetPredication(ctx, &args, _original);
    } else {
        _original(args._self, args.pBuffer, args.AlignedBufferOffset, args.Operation);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetPredication_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::SetMarker
 */

struct npt_command_ID3D12GraphicsCommandList_SetMarker {
    void *_self;
    UINT Metadata;
    const void * pData;
    UINT Size;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_SetMarker_args_temp(struct npt_cs_decoder *dec,
                                                         struct npt_command_ID3D12GraphicsCommandList_SetMarker *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Metadata);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    npt_decode_UINT(dec, &args->Size);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_SetMarker_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_SetMarker *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_SetMarker_reply(struct npt_cs_encoder *enc,
                                                     const struct npt_command_ID3D12GraphicsCommandList_SetMarker *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_SetMarker),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_SetMarker(struct npt_dispatch_context *ctx,
                                                 uint32_t cmd_flags,
                                                 uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_SetMarker args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_SetMarker_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetMarker on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_SetMarker_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_SetMarker: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_SetMarker _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_SetMarker, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_SetMarker);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->SetMarker) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->SetMarker(ctx, &args, _original);
    } else {
        _original(args._self, args.Metadata, args.pData, args.Size);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_SetMarker_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::BeginEvent
 */

struct npt_command_ID3D12GraphicsCommandList_BeginEvent {
    void *_self;
    UINT Metadata;
    const void * pData;
    UINT Size;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_BeginEvent_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12GraphicsCommandList_BeginEvent *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Metadata);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    npt_decode_UINT(dec, &args->Size);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList_BeginEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_BeginEvent *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_BeginEvent_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12GraphicsCommandList_BeginEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_BeginEvent),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_BeginEvent(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_BeginEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_BeginEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_BeginEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_BeginEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_BeginEvent: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_BeginEvent _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_BeginEvent, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_BeginEvent);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->BeginEvent) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->BeginEvent(ctx, &args, _original);
    } else {
        _original(args._self, args.Metadata, args.pData, args.Size);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_BeginEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::EndEvent
 */

struct npt_command_ID3D12GraphicsCommandList_EndEvent {
    void *_self;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_EndEvent_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D12GraphicsCommandList_EndEvent *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12GraphicsCommandList_EndEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_EndEvent *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList_EndEvent_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D12GraphicsCommandList_EndEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_EndEvent),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_EndEvent(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_EndEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_EndEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_EndEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_EndEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_EndEvent: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_EndEvent _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_EndEvent, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_EndEvent);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->EndEvent) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->EndEvent(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_EndEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList::ExecuteIndirect
 */

struct npt_command_ID3D12GraphicsCommandList_ExecuteIndirect {
    void *_self;
    ID3D12CommandSignature * pCommandSignature;
    UINT MaxCommandCount;
    ID3D12Resource * pArgumentBuffer;
    UINT64 ArgumentBufferOffset;
    ID3D12Resource * pCountBuffer;
    UINT64 CountBufferOffset;
};

static inline void
npt_decode_ID3D12GraphicsCommandList_ExecuteIndirect_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12GraphicsCommandList_ExecuteIndirect *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCommandSignature = (ID3D12CommandSignature *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->MaxCommandCount);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pArgumentBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->ArgumentBufferOffset);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pCountBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->CountBufferOffset);
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_ID3D12GraphicsCommandList_ExecuteIndirect_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList_ExecuteIndirect *args)
{
    args->pCommandSignature = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCommandSignature, NPT_OBJECT_TYPE_ID3D12COMMANDSIGNATURE);
    args->pArgumentBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pArgumentBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pCountBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pCountBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList_ExecuteIndirect_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12GraphicsCommandList_ExecuteIndirect *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList,
                                 NPT_METHOD_ID3D12GraphicsCommandList_ExecuteIndirect),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList_ExecuteIndirect(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList_ExecuteIndirect args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList_ExecuteIndirect_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ExecuteIndirect on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList_ExecuteIndirect_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList_ExecuteIndirect: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList_ExecuteIndirect _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList_ExecuteIndirect, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList_ExecuteIndirect);

    if (ctx->id3d12graphicscommandlist_dispatch_overrides && ctx->id3d12graphicscommandlist_dispatch_overrides->ExecuteIndirect) {
        ctx->id3d12graphicscommandlist_dispatch_overrides->ExecuteIndirect(ctx, &args, _original);
    } else {
        _original(args._self, args.pCommandSignature, args.MaxCommandCount, args.pArgumentBuffer, args.ArgumentBufferOffset, args.pCountBuffer, args.CountBufferOffset);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList_ExecuteIndirect_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList1::AtomicCopyBufferUINT
 */

struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT {
    void *_self;
    ID3D12Resource * pDstBuffer;
    UINT64 DstOffset;
    ID3D12Resource * pSrcBuffer;
    UINT64 SrcOffset;
    UINT Dependencies;
    ID3D12Resource ** ppDependentResources;
    const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges;
};

static inline void
npt_decode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->DstOffset);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->SrcOffset);
    npt_decode_UINT(dec, &args->Dependencies);
    uint64_t _cnt_ppDependentResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppDependentResources) {
        args->ppDependentResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppDependentResources);
        if (!args->ppDependentResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppDependentResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppDependentResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppDependentResources = NULL;
    }
    uint64_t _cnt_pDependentSubresourceRanges = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pDependentSubresourceRanges = npt_decode_array_count_unchecked(dec);
        args->pDependentSubresourceRanges = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_SUBRESOURCE_RANGE_UINT64), _cnt_pDependentSubresourceRanges);
        if (!args->pDependentSubresourceRanges) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pDependentSubresourceRanges; _i++)
            npt_decode_D3D12_SUBRESOURCE_RANGE_UINT64(dec, (D3D12_SUBRESOURCE_RANGE_UINT64 *)&args->pDependentSubresourceRanges[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pDependentSubresourceRanges = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppDependentResources && _cnt_ppDependentResources < (uint64_t)(args->Dependencies)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pDependentSubresourceRanges && _cnt_pDependentSubresourceRanges < (uint64_t)(args->Dependencies)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT *args)
{
    args->pDstBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pSrcBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    if (args->ppDependentResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->Dependencies); _i++)
            args->ppDependentResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppDependentResources[_i], NPT_OBJECT_TYPE_ID3D12RESOURCE);
    }
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_AtomicCopyBufferUINT on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_AtomicCopyBufferUINT: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT);

    if (ctx->id3d12graphicscommandlist1_dispatch_overrides && ctx->id3d12graphicscommandlist1_dispatch_overrides->AtomicCopyBufferUINT) {
        ctx->id3d12graphicscommandlist1_dispatch_overrides->AtomicCopyBufferUINT(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstBuffer, args.DstOffset, args.pSrcBuffer, args.SrcOffset, args.Dependencies, args.ppDependentResources, args.pDependentSubresourceRanges);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList1::AtomicCopyBufferUINT64
 */

struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 {
    void *_self;
    ID3D12Resource * pDstBuffer;
    UINT64 DstOffset;
    ID3D12Resource * pSrcBuffer;
    UINT64 SrcOffset;
    UINT Dependencies;
    ID3D12Resource ** ppDependentResources;
    const D3D12_SUBRESOURCE_RANGE_UINT64 * pDependentSubresourceRanges;
};

static inline void
npt_decode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_args_temp(struct npt_cs_decoder *dec,
                                                                       struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->DstOffset);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcBuffer = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT64(dec, &args->SrcOffset);
    npt_decode_UINT(dec, &args->Dependencies);
    uint64_t _cnt_ppDependentResources = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppDependentResources) {
        args->ppDependentResources = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppDependentResources);
        if (!args->ppDependentResources) return;
        for (uint64_t _i = 0; _i < _cnt_ppDependentResources; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppDependentResources[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppDependentResources = NULL;
    }
    uint64_t _cnt_pDependentSubresourceRanges = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pDependentSubresourceRanges = npt_decode_array_count_unchecked(dec);
        args->pDependentSubresourceRanges = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_SUBRESOURCE_RANGE_UINT64), _cnt_pDependentSubresourceRanges);
        if (!args->pDependentSubresourceRanges) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pDependentSubresourceRanges; _i++)
            npt_decode_D3D12_SUBRESOURCE_RANGE_UINT64(dec, (D3D12_SUBRESOURCE_RANGE_UINT64 *)&args->pDependentSubresourceRanges[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pDependentSubresourceRanges = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_ppDependentResources && _cnt_ppDependentResources < (uint64_t)(args->Dependencies)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pDependentSubresourceRanges && _cnt_pDependentSubresourceRanges < (uint64_t)(args->Dependencies)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */







}

static inline void
npt_replace_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 *args)
{
    args->pDstBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pSrcBuffer = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcBuffer, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    if (args->ppDependentResources) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->Dependencies); _i++)
            args->ppDependentResources[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppDependentResources[_i], NPT_OBJECT_TYPE_ID3D12RESOURCE);
    }
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_reply(struct npt_cs_encoder *enc,
                                                                   const struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64(struct npt_dispatch_context *ctx,
                                                               uint32_t cmd_flags,
                                                               uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64);

    if (ctx->id3d12graphicscommandlist1_dispatch_overrides && ctx->id3d12graphicscommandlist1_dispatch_overrides->AtomicCopyBufferUINT64) {
        ctx->id3d12graphicscommandlist1_dispatch_overrides->AtomicCopyBufferUINT64(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstBuffer, args.DstOffset, args.pSrcBuffer, args.SrcOffset, args.Dependencies, args.ppDependentResources, args.pDependentSubresourceRanges);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList1_AtomicCopyBufferUINT64_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList1::OMSetDepthBounds
 */

struct npt_command_ID3D12GraphicsCommandList1_OMSetDepthBounds {
    void *_self;
    FLOAT Min;
    FLOAT Max;
};

static inline void
npt_decode_ID3D12GraphicsCommandList1_OMSetDepthBounds_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12GraphicsCommandList1_OMSetDepthBounds *args)
{
    /* Decode input parameters from the wire */
    npt_decode_FLOAT(dec, &args->Min);
    npt_decode_FLOAT(dec, &args->Max);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList1_OMSetDepthBounds_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList1_OMSetDepthBounds *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_OMSetDepthBounds_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12GraphicsCommandList1_OMSetDepthBounds *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_OMSetDepthBounds),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList1_OMSetDepthBounds(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList1_OMSetDepthBounds args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList1_OMSetDepthBounds_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_OMSetDepthBounds on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList1_OMSetDepthBounds_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_OMSetDepthBounds: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList1_OMSetDepthBounds _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList1_OMSetDepthBounds, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList1_OMSetDepthBounds);

    if (ctx->id3d12graphicscommandlist1_dispatch_overrides && ctx->id3d12graphicscommandlist1_dispatch_overrides->OMSetDepthBounds) {
        ctx->id3d12graphicscommandlist1_dispatch_overrides->OMSetDepthBounds(ctx, &args, _original);
    } else {
        _original(args._self, args.Min, args.Max);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList1_OMSetDepthBounds_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList1::SetSamplePositions
 */

struct npt_command_ID3D12GraphicsCommandList1_SetSamplePositions {
    void *_self;
    UINT NumSamplesPerPixel;
    UINT NumPixels;
    D3D12_SAMPLE_POSITION * pSamplePositions;
};

static inline void
npt_decode_ID3D12GraphicsCommandList1_SetSamplePositions_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D12GraphicsCommandList1_SetSamplePositions *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumSamplesPerPixel);
    npt_decode_UINT(dec, &args->NumPixels);
    uint64_t _cnt_pSamplePositions = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pSamplePositions = npt_decode_array_count_unchecked(dec);
        args->pSamplePositions = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_SAMPLE_POSITION), _cnt_pSamplePositions);
        if (!args->pSamplePositions) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pSamplePositions; _i++)
            npt_decode_D3D12_SAMPLE_POSITION(dec, (D3D12_SAMPLE_POSITION *)&args->pSamplePositions[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pSamplePositions = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pSamplePositions && _cnt_pSamplePositions < (uint64_t)(args->NumSamplesPerPixel * args->NumPixels)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList1_SetSamplePositions_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList1_SetSamplePositions *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_SetSamplePositions_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D12GraphicsCommandList1_SetSamplePositions *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_SetSamplePositions),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList1_SetSamplePositions(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList1_SetSamplePositions args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList1_SetSamplePositions_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_SetSamplePositions on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList1_SetSamplePositions_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_SetSamplePositions: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList1_SetSamplePositions _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList1_SetSamplePositions, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList1_SetSamplePositions);

    if (ctx->id3d12graphicscommandlist1_dispatch_overrides && ctx->id3d12graphicscommandlist1_dispatch_overrides->SetSamplePositions) {
        ctx->id3d12graphicscommandlist1_dispatch_overrides->SetSamplePositions(ctx, &args, _original);
    } else {
        _original(args._self, args.NumSamplesPerPixel, args.NumPixels, args.pSamplePositions);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList1_SetSamplePositions_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList1::ResolveSubresourceRegion
 */

struct npt_command_ID3D12GraphicsCommandList1_ResolveSubresourceRegion {
    void *_self;
    ID3D12Resource * pDstResource;
    UINT DstSubresource;
    UINT DstX;
    UINT DstY;
    ID3D12Resource * pSrcResource;
    UINT SrcSubresource;
    D3D12_RECT * pSrcRect;
    DXGI_FORMAT Format;
    D3D12_RESOLVE_MODE ResolveMode;
};

static inline void
npt_decode_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_args_temp(struct npt_cs_decoder *dec,
                                                                         struct npt_command_ID3D12GraphicsCommandList1_ResolveSubresourceRegion *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDstResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->DstSubresource);
    npt_decode_UINT(dec, &args->DstX);
    npt_decode_UINT(dec, &args->DstY);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pSrcResource = (ID3D12Resource *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->SrcSubresource);
    if (npt_decode_simple_pointer(dec)) {
        args->pSrcRect = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RECT));
        if (!args->pSrcRect) return;
        npt_decode_D3D12_RECT(dec, (D3D12_RECT *)args->pSrcRect);
    } else {
        args->pSrcRect = NULL;
    }
    npt_decode_DXGI_FORMAT(dec, &args->Format);
    npt_decode_D3D12_RESOLVE_MODE(dec, &args->ResolveMode);
    /* Allocate temp storage for output-only parameters */









}

static inline void
npt_replace_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList1_ResolveSubresourceRegion *args)
{
    args->pDstResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDstResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
    args->pSrcResource = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pSrcResource, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_reply(struct npt_cs_encoder *enc,
                                                                     const struct npt_command_ID3D12GraphicsCommandList1_ResolveSubresourceRegion *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_ResolveSubresourceRegion),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList1_ResolveSubresourceRegion(struct npt_dispatch_context *ctx,
                                                                 uint32_t cmd_flags,
                                                                 uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList1_ResolveSubresourceRegion args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_ResolveSubresourceRegion on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_ResolveSubresourceRegion: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList1_ResolveSubresourceRegion _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList1_ResolveSubresourceRegion, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList1_ResolveSubresourceRegion);

    if (ctx->id3d12graphicscommandlist1_dispatch_overrides && ctx->id3d12graphicscommandlist1_dispatch_overrides->ResolveSubresourceRegion) {
        ctx->id3d12graphicscommandlist1_dispatch_overrides->ResolveSubresourceRegion(ctx, &args, _original);
    } else {
        _original(args._self, args.pDstResource, args.DstSubresource, args.DstX, args.DstY, args.pSrcResource, args.SrcSubresource, args.pSrcRect, args.Format, args.ResolveMode);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList1_ResolveSubresourceRegion_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList1::SetViewInstanceMask
 */

struct npt_command_ID3D12GraphicsCommandList1_SetViewInstanceMask {
    void *_self;
    UINT Mask;
};

static inline void
npt_decode_ID3D12GraphicsCommandList1_SetViewInstanceMask_args_temp(struct npt_cs_decoder *dec,
                                                                    struct npt_command_ID3D12GraphicsCommandList1_SetViewInstanceMask *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Mask);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList1_SetViewInstanceMask_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList1_SetViewInstanceMask *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList1_SetViewInstanceMask_reply(struct npt_cs_encoder *enc,
                                                                const struct npt_command_ID3D12GraphicsCommandList1_SetViewInstanceMask *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList1,
                                 NPT_METHOD_ID3D12GraphicsCommandList1_SetViewInstanceMask),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList1_SetViewInstanceMask(struct npt_dispatch_context *ctx,
                                                            uint32_t cmd_flags,
                                                            uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList1_SetViewInstanceMask args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList1_SetViewInstanceMask_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_SetViewInstanceMask on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList1_SetViewInstanceMask_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList1_SetViewInstanceMask: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList1_SetViewInstanceMask _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList1_SetViewInstanceMask, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList1_SetViewInstanceMask);

    if (ctx->id3d12graphicscommandlist1_dispatch_overrides && ctx->id3d12graphicscommandlist1_dispatch_overrides->SetViewInstanceMask) {
        ctx->id3d12graphicscommandlist1_dispatch_overrides->SetViewInstanceMask(ctx, &args, _original);
    } else {
        _original(args._self, args.Mask);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList1_SetViewInstanceMask_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList2::WriteBufferImmediate
 */

struct npt_command_ID3D12GraphicsCommandList2_WriteBufferImmediate {
    void *_self;
    UINT Count;
    const D3D12_WRITEBUFFERIMMEDIATE_PARAMETER * pParams;
    const D3D12_WRITEBUFFERIMMEDIATE_MODE * pModes;
};

static inline void
npt_decode_ID3D12GraphicsCommandList2_WriteBufferImmediate_args_temp(struct npt_cs_decoder *dec,
                                                                     struct npt_command_ID3D12GraphicsCommandList2_WriteBufferImmediate *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Count);
    uint64_t _cnt_pParams = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pParams = npt_decode_array_count_unchecked(dec);
        args->pParams = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_WRITEBUFFERIMMEDIATE_PARAMETER), _cnt_pParams);
        if (!args->pParams) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pParams; _i++)
            npt_decode_D3D12_WRITEBUFFERIMMEDIATE_PARAMETER(dec, (D3D12_WRITEBUFFERIMMEDIATE_PARAMETER *)&args->pParams[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pParams = NULL;
    }
    uint64_t _cnt_pModes = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pModes = npt_decode_array_count_unchecked(dec);
        args->pModes = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_WRITEBUFFERIMMEDIATE_MODE), _cnt_pModes);
        if (!args->pModes) return;
        npt_decode_D3D12_WRITEBUFFERIMMEDIATE_MODE_array(dec, (D3D12_WRITEBUFFERIMMEDIATE_MODE *)args->pModes, _cnt_pModes);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pModes = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pParams && _cnt_pParams < (uint64_t)(args->Count)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_pModes && _cnt_pModes < (uint64_t)(args->Count)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList2_WriteBufferImmediate_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList2_WriteBufferImmediate *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList2_WriteBufferImmediate_reply(struct npt_cs_encoder *enc,
                                                                 const struct npt_command_ID3D12GraphicsCommandList2_WriteBufferImmediate *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList2,
                                 NPT_METHOD_ID3D12GraphicsCommandList2_WriteBufferImmediate),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList2_WriteBufferImmediate(struct npt_dispatch_context *ctx,
                                                             uint32_t cmd_flags,
                                                             uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList2_WriteBufferImmediate args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList2_WriteBufferImmediate_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList2_WriteBufferImmediate on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList2_WriteBufferImmediate_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList2_WriteBufferImmediate: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList2_WriteBufferImmediate _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList2_WriteBufferImmediate, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList2_WriteBufferImmediate);

    if (ctx->id3d12graphicscommandlist2_dispatch_overrides && ctx->id3d12graphicscommandlist2_dispatch_overrides->WriteBufferImmediate) {
        ctx->id3d12graphicscommandlist2_dispatch_overrides->WriteBufferImmediate(ctx, &args, _original);
    } else {
        _original(args._self, args.Count, args.pParams, args.pModes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList2_WriteBufferImmediate_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList3::SetProtectedResourceSession
 */

struct npt_command_ID3D12GraphicsCommandList3_SetProtectedResourceSession {
    void *_self;
    ID3D12ProtectedResourceSession * pProtectedResourceSession;
};

static inline void
npt_decode_ID3D12GraphicsCommandList3_SetProtectedResourceSession_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D12GraphicsCommandList3_SetProtectedResourceSession *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pProtectedResourceSession = (ID3D12ProtectedResourceSession *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList3_SetProtectedResourceSession_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList3_SetProtectedResourceSession *args)
{
    args->pProtectedResourceSession = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pProtectedResourceSession, NPT_OBJECT_TYPE_ID3D12PROTECTEDRESOURCESESSION);
}

static inline void
npt_encode_ID3D12GraphicsCommandList3_SetProtectedResourceSession_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D12GraphicsCommandList3_SetProtectedResourceSession *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList3,
                                 NPT_METHOD_ID3D12GraphicsCommandList3_SetProtectedResourceSession),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList3_SetProtectedResourceSession(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList3_SetProtectedResourceSession args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList3_SetProtectedResourceSession_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList3_SetProtectedResourceSession on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList3_SetProtectedResourceSession_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList3_SetProtectedResourceSession: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList3_SetProtectedResourceSession _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList3_SetProtectedResourceSession, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList3_SetProtectedResourceSession);

    if (ctx->id3d12graphicscommandlist3_dispatch_overrides && ctx->id3d12graphicscommandlist3_dispatch_overrides->SetProtectedResourceSession) {
        ctx->id3d12graphicscommandlist3_dispatch_overrides->SetProtectedResourceSession(ctx, &args, _original);
    } else {
        _original(args._self, args.pProtectedResourceSession);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList3_SetProtectedResourceSession_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::BeginRenderPass
 */

struct npt_command_ID3D12GraphicsCommandList4_BeginRenderPass {
    void *_self;
    UINT NumRenderTargets;
    const D3D12_RENDER_PASS_RENDER_TARGET_DESC * pRenderTargets;
    const D3D12_RENDER_PASS_DEPTH_STENCIL_DESC * pDepthStencil;
    D3D12_RENDER_PASS_FLAGS Flags;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_BeginRenderPass_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_ID3D12GraphicsCommandList4_BeginRenderPass *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->NumRenderTargets);
    uint64_t _cnt_pRenderTargets = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pRenderTargets = npt_decode_array_count_unchecked(dec);
        args->pRenderTargets = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RENDER_PASS_RENDER_TARGET_DESC), _cnt_pRenderTargets);
        if (!args->pRenderTargets) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pRenderTargets; _i++)
            npt_decode_D3D12_RENDER_PASS_RENDER_TARGET_DESC(dec, (D3D12_RENDER_PASS_RENDER_TARGET_DESC *)&args->pRenderTargets[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pRenderTargets = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDepthStencil = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RENDER_PASS_DEPTH_STENCIL_DESC));
        if (!args->pDepthStencil) return;
        npt_decode_D3D12_RENDER_PASS_DEPTH_STENCIL_DESC(dec, (D3D12_RENDER_PASS_DEPTH_STENCIL_DESC *)args->pDepthStencil);
    } else {
        args->pDepthStencil = NULL;
    }
    npt_decode_D3D12_RENDER_PASS_FLAGS(dec, &args->Flags);
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pRenderTargets && _cnt_pRenderTargets < (uint64_t)(args->NumRenderTargets)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12GraphicsCommandList4_BeginRenderPass_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_BeginRenderPass *args)
{
    if (args->pRenderTargets) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumRenderTargets); _i++)
            npt_replace_D3D12_RENDER_PASS_RENDER_TARGET_DESC_handle(ctx, &((D3D12_RENDER_PASS_RENDER_TARGET_DESC *)args->pRenderTargets)[_i]);
    }
    if (args->pDepthStencil)
        npt_replace_D3D12_RENDER_PASS_DEPTH_STENCIL_DESC_handle(ctx, (D3D12_RENDER_PASS_DEPTH_STENCIL_DESC *)args->pDepthStencil);
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_BeginRenderPass_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_ID3D12GraphicsCommandList4_BeginRenderPass *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_BeginRenderPass),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_BeginRenderPass(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_BeginRenderPass args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_BeginRenderPass_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_BeginRenderPass on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_BeginRenderPass_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_BeginRenderPass: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_BeginRenderPass _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_BeginRenderPass, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_BeginRenderPass);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->BeginRenderPass) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->BeginRenderPass(ctx, &args, _original);
    } else {
        _original(args._self, args.NumRenderTargets, args.pRenderTargets, args.pDepthStencil, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_BeginRenderPass_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::EndRenderPass
 */

struct npt_command_ID3D12GraphicsCommandList4_EndRenderPass {
    void *_self;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_EndRenderPass_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_ID3D12GraphicsCommandList4_EndRenderPass *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12GraphicsCommandList4_EndRenderPass_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_EndRenderPass *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_EndRenderPass_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_ID3D12GraphicsCommandList4_EndRenderPass *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_EndRenderPass),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_EndRenderPass(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_EndRenderPass args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_EndRenderPass_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_EndRenderPass on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_EndRenderPass_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_EndRenderPass: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_EndRenderPass _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_EndRenderPass, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_EndRenderPass);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->EndRenderPass) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->EndRenderPass(ctx, &args, _original);
    } else {
        _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_EndRenderPass_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::InitializeMetaCommand
 */

struct npt_command_ID3D12GraphicsCommandList4_InitializeMetaCommand {
    void *_self;
    ID3D12MetaCommand * pMetaCommand;
    const void * pInitializationParametersData;
    SIZE_T InitializationParametersDataSizeInBytes;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_InitializeMetaCommand_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D12GraphicsCommandList4_InitializeMetaCommand *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pMetaCommand = (ID3D12MetaCommand *)npt_object_from_id(_id);
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pInitializationParametersData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pInitializationParametersData) return;
            npt_decode_blob_array(dec, (void *)args->pInitializationParametersData, _blob_size);
        } else {
            args->pInitializationParametersData = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->InitializationParametersDataSizeInBytes);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList4_InitializeMetaCommand_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_InitializeMetaCommand *args)
{
    args->pMetaCommand = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pMetaCommand, NPT_OBJECT_TYPE_ID3D12METACOMMAND);
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_InitializeMetaCommand_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D12GraphicsCommandList4_InitializeMetaCommand *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_InitializeMetaCommand),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_InitializeMetaCommand(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_InitializeMetaCommand args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_InitializeMetaCommand_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_InitializeMetaCommand on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_InitializeMetaCommand_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_InitializeMetaCommand: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_InitializeMetaCommand _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_InitializeMetaCommand, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_InitializeMetaCommand);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->InitializeMetaCommand) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->InitializeMetaCommand(ctx, &args, _original);
    } else {
        _original(args._self, args.pMetaCommand, args.pInitializationParametersData, args.InitializationParametersDataSizeInBytes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_InitializeMetaCommand_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::ExecuteMetaCommand
 */

struct npt_command_ID3D12GraphicsCommandList4_ExecuteMetaCommand {
    void *_self;
    ID3D12MetaCommand * pMetaCommand;
    const void * pExecutionParametersData;
    SIZE_T ExecutionParametersDataSizeInBytes;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_ExecuteMetaCommand_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_ID3D12GraphicsCommandList4_ExecuteMetaCommand *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pMetaCommand = (ID3D12MetaCommand *)npt_object_from_id(_id);
    }
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pExecutionParametersData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pExecutionParametersData) return;
            npt_decode_blob_array(dec, (void *)args->pExecutionParametersData, _blob_size);
        } else {
            args->pExecutionParametersData = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->ExecutionParametersDataSizeInBytes);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList4_ExecuteMetaCommand_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_ExecuteMetaCommand *args)
{
    args->pMetaCommand = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pMetaCommand, NPT_OBJECT_TYPE_ID3D12METACOMMAND);
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_ExecuteMetaCommand_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_ID3D12GraphicsCommandList4_ExecuteMetaCommand *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_ExecuteMetaCommand),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_ExecuteMetaCommand(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags,
                                                           uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_ExecuteMetaCommand args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_ExecuteMetaCommand_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_ExecuteMetaCommand on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_ExecuteMetaCommand_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_ExecuteMetaCommand: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_ExecuteMetaCommand _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_ExecuteMetaCommand, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_ExecuteMetaCommand);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->ExecuteMetaCommand) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->ExecuteMetaCommand(ctx, &args, _original);
    } else {
        _original(args._self, args.pMetaCommand, args.pExecutionParametersData, args.ExecutionParametersDataSizeInBytes);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_ExecuteMetaCommand_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::BuildRaytracingAccelerationStructure
 */

struct npt_command_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure {
    void *_self;
    const D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC * pDesc;
    UINT NumPostbuildInfoDescs;
    const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pPostbuildInfoDescs;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_args_temp(struct npt_cs_decoder *dec,
                                                                                     struct npt_command_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC(dec, (D3D12_BUILD_RAYTRACING_ACCELERATION_STRUCTURE_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->NumPostbuildInfoDescs);
    uint64_t _cnt_pPostbuildInfoDescs = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pPostbuildInfoDescs = npt_decode_array_count_unchecked(dec);
        args->pPostbuildInfoDescs = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC), _cnt_pPostbuildInfoDescs);
        if (!args->pPostbuildInfoDescs) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pPostbuildInfoDescs; _i++)
            npt_decode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC(dec, (D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC *)&args->pPostbuildInfoDescs[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pPostbuildInfoDescs = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pPostbuildInfoDescs && _cnt_pPostbuildInfoDescs < (uint64_t)(args->NumPostbuildInfoDescs)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_reply(struct npt_cs_encoder *enc,
                                                                                 const struct npt_command_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure(struct npt_dispatch_context *ctx,
                                                                             uint32_t cmd_flags,
                                                                             uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->BuildRaytracingAccelerationStructure) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->BuildRaytracingAccelerationStructure(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc, args.NumPostbuildInfoDescs, args.pPostbuildInfoDescs);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_BuildRaytracingAccelerationStructure_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::EmitRaytracingAccelerationStructurePostbuildInfo
 */

struct npt_command_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo {
    void *_self;
    const D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC * pDesc;
    UINT NumSourceAccelerationStructures;
    const D3D12_GPU_VIRTUAL_ADDRESS * pSourceAccelerationStructureData;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_args_temp(struct npt_cs_decoder *dec,
                                                                                                 struct npt_command_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC(dec, (D3D12_RAYTRACING_ACCELERATION_STRUCTURE_POSTBUILD_INFO_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->NumSourceAccelerationStructures);
    uint64_t _cnt_pSourceAccelerationStructureData = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pSourceAccelerationStructureData = npt_decode_array_count_unchecked(dec);
        args->pSourceAccelerationStructureData = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_GPU_VIRTUAL_ADDRESS), _cnt_pSourceAccelerationStructureData);
        if (!args->pSourceAccelerationStructureData) return;
        npt_decode_D3D12_GPU_VIRTUAL_ADDRESS_array(dec, (D3D12_GPU_VIRTUAL_ADDRESS *)args->pSourceAccelerationStructureData, _cnt_pSourceAccelerationStructureData);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pSourceAccelerationStructureData = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pSourceAccelerationStructureData && _cnt_pSourceAccelerationStructureData < (uint64_t)(args->NumSourceAccelerationStructures)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_reply(struct npt_cs_encoder *enc,
                                                                                             const struct npt_command_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo(struct npt_dispatch_context *ctx,
                                                                                         uint32_t cmd_flags,
                                                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->EmitRaytracingAccelerationStructurePostbuildInfo) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->EmitRaytracingAccelerationStructurePostbuildInfo(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc, args.NumSourceAccelerationStructures, args.pSourceAccelerationStructureData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_EmitRaytracingAccelerationStructurePostbuildInfo_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::CopyRaytracingAccelerationStructure
 */

struct npt_command_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure {
    void *_self;
    D3D12_GPU_VIRTUAL_ADDRESS DestAccelerationStructureData;
    D3D12_GPU_VIRTUAL_ADDRESS SourceAccelerationStructureData;
    D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE Mode;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_args_temp(struct npt_cs_decoder *dec,
                                                                                    struct npt_command_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->DestAccelerationStructureData);
    npt_decode_D3D12_GPU_VIRTUAL_ADDRESS(dec, &args->SourceAccelerationStructureData);
    npt_decode_D3D12_RAYTRACING_ACCELERATION_STRUCTURE_COPY_MODE(dec, &args->Mode);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_reply(struct npt_cs_encoder *enc,
                                                                                const struct npt_command_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure(struct npt_dispatch_context *ctx,
                                                                            uint32_t cmd_flags,
                                                                            uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->CopyRaytracingAccelerationStructure) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->CopyRaytracingAccelerationStructure(ctx, &args, _original);
    } else {
        _original(args._self, args.DestAccelerationStructureData, args.SourceAccelerationStructureData, args.Mode);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_CopyRaytracingAccelerationStructure_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::SetPipelineState1
 */

struct npt_command_ID3D12GraphicsCommandList4_SetPipelineState1 {
    void *_self;
    ID3D12StateObject * pStateObject;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_SetPipelineState1_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_ID3D12GraphicsCommandList4_SetPipelineState1 *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pStateObject = (ID3D12StateObject *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList4_SetPipelineState1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_SetPipelineState1 *args)
{
    args->pStateObject = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pStateObject, NPT_OBJECT_TYPE_ID3D12STATEOBJECT);
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_SetPipelineState1_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_ID3D12GraphicsCommandList4_SetPipelineState1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_SetPipelineState1),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_SetPipelineState1(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_SetPipelineState1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_SetPipelineState1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_SetPipelineState1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_SetPipelineState1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_SetPipelineState1: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_SetPipelineState1 _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_SetPipelineState1, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_SetPipelineState1);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->SetPipelineState1) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->SetPipelineState1(ctx, &args, _original);
    } else {
        _original(args._self, args.pStateObject);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_SetPipelineState1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList4::DispatchRays
 */

struct npt_command_ID3D12GraphicsCommandList4_DispatchRays {
    void *_self;
    const D3D12_DISPATCH_RAYS_DESC * pDesc;
};

static inline void
npt_decode_ID3D12GraphicsCommandList4_DispatchRays_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D12GraphicsCommandList4_DispatchRays *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_DISPATCH_RAYS_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_DISPATCH_RAYS_DESC(dec, (D3D12_DISPATCH_RAYS_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList4_DispatchRays_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList4_DispatchRays *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList4_DispatchRays_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D12GraphicsCommandList4_DispatchRays *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList4,
                                 NPT_METHOD_ID3D12GraphicsCommandList4_DispatchRays),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList4_DispatchRays(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList4_DispatchRays args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList4_DispatchRays_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_DispatchRays on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList4_DispatchRays_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList4_DispatchRays: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList4_DispatchRays _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList4_DispatchRays, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList4_DispatchRays);

    if (ctx->id3d12graphicscommandlist4_dispatch_overrides && ctx->id3d12graphicscommandlist4_dispatch_overrides->DispatchRays) {
        ctx->id3d12graphicscommandlist4_dispatch_overrides->DispatchRays(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList4_DispatchRays_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList5::RSSetShadingRate
 */

struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRate {
    void *_self;
    D3D12_SHADING_RATE baseShadingRate;
    const D3D12_SHADING_RATE_COMBINER * combiners;
};

static inline void
npt_decode_ID3D12GraphicsCommandList5_RSSetShadingRate_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRate *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_SHADING_RATE(dec, &args->baseShadingRate);
    uint64_t _cnt_combiners = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_combiners = npt_decode_array_count_unchecked(dec);
        args->combiners = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_SHADING_RATE_COMBINER), _cnt_combiners);
        if (!args->combiners) return;
        npt_decode_D3D12_SHADING_RATE_COMBINER_array(dec, (D3D12_SHADING_RATE_COMBINER *)args->combiners, _cnt_combiners);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->combiners = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_combiners && _cnt_combiners < (uint64_t)(D3D12_RS_SET_SHADING_RATE_COMBINER_COUNT)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList5_RSSetShadingRate_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRate *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRate_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRate *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList5,
                                 NPT_METHOD_ID3D12GraphicsCommandList5_RSSetShadingRate),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList5_RSSetShadingRate(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRate args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList5_RSSetShadingRate_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList5_RSSetShadingRate on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList5_RSSetShadingRate_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList5_RSSetShadingRate: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList5_RSSetShadingRate _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList5_RSSetShadingRate, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList5_RSSetShadingRate);

    if (ctx->id3d12graphicscommandlist5_dispatch_overrides && ctx->id3d12graphicscommandlist5_dispatch_overrides->RSSetShadingRate) {
        ctx->id3d12graphicscommandlist5_dispatch_overrides->RSSetShadingRate(ctx, &args, _original);
    } else {
        _original(args._self, args.baseShadingRate, args.combiners);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRate_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList5::RSSetShadingRateImage
 */

struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRateImage {
    void *_self;
    ID3D12Resource * shadingRateImage;
};

static inline void
npt_decode_ID3D12GraphicsCommandList5_RSSetShadingRateImage_args_temp(struct npt_cs_decoder *dec,
                                                                      struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRateImage *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->shadingRateImage = (ID3D12Resource *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList5_RSSetShadingRateImage_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRateImage *args)
{
    args->shadingRateImage = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->shadingRateImage, NPT_OBJECT_TYPE_ID3D12RESOURCE);
}

static inline void
npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRateImage_reply(struct npt_cs_encoder *enc,
                                                                  const struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRateImage *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList5,
                                 NPT_METHOD_ID3D12GraphicsCommandList5_RSSetShadingRateImage),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList5_RSSetShadingRateImage(struct npt_dispatch_context *ctx,
                                                              uint32_t cmd_flags,
                                                              uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList5_RSSetShadingRateImage args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList5_RSSetShadingRateImage_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList5_RSSetShadingRateImage on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList5_RSSetShadingRateImage_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList5_RSSetShadingRateImage: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList5_RSSetShadingRateImage _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList5_RSSetShadingRateImage, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList5_RSSetShadingRateImage);

    if (ctx->id3d12graphicscommandlist5_dispatch_overrides && ctx->id3d12graphicscommandlist5_dispatch_overrides->RSSetShadingRateImage) {
        ctx->id3d12graphicscommandlist5_dispatch_overrides->RSSetShadingRateImage(ctx, &args, _original);
    } else {
        _original(args._self, args.shadingRateImage);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList5_RSSetShadingRateImage_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList6::DispatchMesh
 */

struct npt_command_ID3D12GraphicsCommandList6_DispatchMesh {
    void *_self;
    UINT ThreadGroupCountX;
    UINT ThreadGroupCountY;
    UINT ThreadGroupCountZ;
};

static inline void
npt_decode_ID3D12GraphicsCommandList6_DispatchMesh_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_ID3D12GraphicsCommandList6_DispatchMesh *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->ThreadGroupCountX);
    npt_decode_UINT(dec, &args->ThreadGroupCountY);
    npt_decode_UINT(dec, &args->ThreadGroupCountZ);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList6_DispatchMesh_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList6_DispatchMesh *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList6_DispatchMesh_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_ID3D12GraphicsCommandList6_DispatchMesh *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList6,
                                 NPT_METHOD_ID3D12GraphicsCommandList6_DispatchMesh),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList6_DispatchMesh(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList6_DispatchMesh args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList6_DispatchMesh_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST6);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList6_DispatchMesh on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList6_DispatchMesh_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList6_DispatchMesh: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList6_DispatchMesh _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList6_DispatchMesh, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList6_DispatchMesh);

    if (ctx->id3d12graphicscommandlist6_dispatch_overrides && ctx->id3d12graphicscommandlist6_dispatch_overrides->DispatchMesh) {
        ctx->id3d12graphicscommandlist6_dispatch_overrides->DispatchMesh(ctx, &args, _original);
    } else {
        _original(args._self, args.ThreadGroupCountX, args.ThreadGroupCountY, args.ThreadGroupCountZ);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList6_DispatchMesh_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList7::Barrier
 */

struct npt_command_ID3D12GraphicsCommandList7_Barrier {
    void *_self;
    UINT32 NumBarrierGroups;
    const D3D12_BARRIER_GROUP * pBarrierGroups;
};

static inline void
npt_decode_ID3D12GraphicsCommandList7_Barrier_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_ID3D12GraphicsCommandList7_Barrier *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT32(dec, &args->NumBarrierGroups);
    uint64_t _cnt_pBarrierGroups = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pBarrierGroups = npt_decode_array_count_unchecked(dec);
        args->pBarrierGroups = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D12_BARRIER_GROUP), _cnt_pBarrierGroups);
        if (!args->pBarrierGroups) return;
        for (uint32_t _i = 0; _i < (uint32_t)_cnt_pBarrierGroups; _i++)
            npt_decode_D3D12_BARRIER_GROUP(dec, (D3D12_BARRIER_GROUP *)&args->pBarrierGroups[_i]);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pBarrierGroups = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pBarrierGroups && _cnt_pBarrierGroups < (uint64_t)(args->NumBarrierGroups)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList7_Barrier_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList7_Barrier *args)
{
    if (args->pBarrierGroups) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumBarrierGroups); _i++)
            npt_replace_D3D12_BARRIER_GROUP_handle(ctx, &((D3D12_BARRIER_GROUP *)args->pBarrierGroups)[_i]);
    }
}

static inline void
npt_encode_ID3D12GraphicsCommandList7_Barrier_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_ID3D12GraphicsCommandList7_Barrier *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList7,
                                 NPT_METHOD_ID3D12GraphicsCommandList7_Barrier),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList7_Barrier(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList7_Barrier args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList7_Barrier_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST7);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList7_Barrier on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList7_Barrier_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList7_Barrier: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList7_Barrier _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList7_Barrier, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList7_Barrier);

    if (ctx->id3d12graphicscommandlist7_dispatch_overrides && ctx->id3d12graphicscommandlist7_dispatch_overrides->Barrier) {
        ctx->id3d12graphicscommandlist7_dispatch_overrides->Barrier(ctx, &args, _original);
    } else {
        _original(args._self, args.NumBarrierGroups, args.pBarrierGroups);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList7_Barrier_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList8::OMSetFrontAndBackStencilRef
 */

struct npt_command_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef {
    void *_self;
    UINT FrontStencilRef;
    UINT BackStencilRef;
};

static inline void
npt_decode_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_args_temp(struct npt_cs_decoder *dec,
                                                                            struct npt_command_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->FrontStencilRef);
    npt_decode_UINT(dec, &args->BackStencilRef);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_reply(struct npt_cs_encoder *enc,
                                                                        const struct npt_command_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList8,
                                 NPT_METHOD_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef(struct npt_dispatch_context *ctx,
                                                                    uint32_t cmd_flags,
                                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST8);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef);

    if (ctx->id3d12graphicscommandlist8_dispatch_overrides && ctx->id3d12graphicscommandlist8_dispatch_overrides->OMSetFrontAndBackStencilRef) {
        ctx->id3d12graphicscommandlist8_dispatch_overrides->OMSetFrontAndBackStencilRef(ctx, &args, _original);
    } else {
        _original(args._self, args.FrontStencilRef, args.BackStencilRef);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList8_OMSetFrontAndBackStencilRef_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList9::RSSetDepthBias
 */

struct npt_command_ID3D12GraphicsCommandList9_RSSetDepthBias {
    void *_self;
    FLOAT DepthBias;
    FLOAT DepthBiasClamp;
    FLOAT SlopeScaledDepthBias;
};

static inline void
npt_decode_ID3D12GraphicsCommandList9_RSSetDepthBias_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12GraphicsCommandList9_RSSetDepthBias *args)
{
    /* Decode input parameters from the wire */
    npt_decode_FLOAT(dec, &args->DepthBias);
    npt_decode_FLOAT(dec, &args->DepthBiasClamp);
    npt_decode_FLOAT(dec, &args->SlopeScaledDepthBias);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12GraphicsCommandList9_RSSetDepthBias_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList9_RSSetDepthBias *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList9_RSSetDepthBias_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12GraphicsCommandList9_RSSetDepthBias *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList9,
                                 NPT_METHOD_ID3D12GraphicsCommandList9_RSSetDepthBias),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList9_RSSetDepthBias(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList9_RSSetDepthBias args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList9_RSSetDepthBias_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST9);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList9_RSSetDepthBias on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList9_RSSetDepthBias_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList9_RSSetDepthBias: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList9_RSSetDepthBias _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList9_RSSetDepthBias, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList9_RSSetDepthBias);

    if (ctx->id3d12graphicscommandlist9_dispatch_overrides && ctx->id3d12graphicscommandlist9_dispatch_overrides->RSSetDepthBias) {
        ctx->id3d12graphicscommandlist9_dispatch_overrides->RSSetDepthBias(ctx, &args, _original);
    } else {
        _original(args._self, args.DepthBias, args.DepthBiasClamp, args.SlopeScaledDepthBias);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList9_RSSetDepthBias_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList9::IASetIndexBufferStripCutValue
 */

struct npt_command_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue {
    void *_self;
    D3D12_INDEX_BUFFER_STRIP_CUT_VALUE IBStripCutValue;
};

static inline void
npt_decode_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_args_temp(struct npt_cs_decoder *dec,
                                                                              struct npt_command_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue *args)
{
    /* Decode input parameters from the wire */
    npt_decode_D3D12_INDEX_BUFFER_STRIP_CUT_VALUE(dec, &args->IBStripCutValue);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_reply(struct npt_cs_encoder *enc,
                                                                          const struct npt_command_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList9,
                                 NPT_METHOD_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue(struct npt_dispatch_context *ctx,
                                                                      uint32_t cmd_flags,
                                                                      uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST9);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue);

    if (ctx->id3d12graphicscommandlist9_dispatch_overrides && ctx->id3d12graphicscommandlist9_dispatch_overrides->IASetIndexBufferStripCutValue) {
        ctx->id3d12graphicscommandlist9_dispatch_overrides->IASetIndexBufferStripCutValue(ctx, &args, _original);
    } else {
        _original(args._self, args.IBStripCutValue);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList9_IASetIndexBufferStripCutValue_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList10::SetProgram
 */

struct npt_command_ID3D12GraphicsCommandList10_SetProgram {
    void *_self;
    const D3D12_SET_PROGRAM_DESC * pDesc;
};

static inline void
npt_decode_ID3D12GraphicsCommandList10_SetProgram_args_temp(struct npt_cs_decoder *dec,
                                                            struct npt_command_ID3D12GraphicsCommandList10_SetProgram *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_SET_PROGRAM_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_SET_PROGRAM_DESC(dec, (D3D12_SET_PROGRAM_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList10_SetProgram_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList10_SetProgram *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList10_SetProgram_reply(struct npt_cs_encoder *enc,
                                                        const struct npt_command_ID3D12GraphicsCommandList10_SetProgram *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList10,
                                 NPT_METHOD_ID3D12GraphicsCommandList10_SetProgram),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList10_SetProgram(struct npt_dispatch_context *ctx,
                                                    uint32_t cmd_flags,
                                                    uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList10_SetProgram args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList10_SetProgram_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST10);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList10_SetProgram on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList10_SetProgram_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList10_SetProgram: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList10_SetProgram _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList10_SetProgram, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList10_SetProgram);

    if (ctx->id3d12graphicscommandlist10_dispatch_overrides && ctx->id3d12graphicscommandlist10_dispatch_overrides->SetProgram) {
        ctx->id3d12graphicscommandlist10_dispatch_overrides->SetProgram(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList10_SetProgram_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12GraphicsCommandList10::DispatchGraph
 */

struct npt_command_ID3D12GraphicsCommandList10_DispatchGraph {
    void *_self;
    const D3D12_DISPATCH_GRAPH_DESC * pDesc;
};

static inline void
npt_decode_ID3D12GraphicsCommandList10_DispatchGraph_args_temp(struct npt_cs_decoder *dec,
                                                               struct npt_command_ID3D12GraphicsCommandList10_DispatchGraph *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_DISPATCH_GRAPH_DESC));
        if (!args->pDesc) return;
        npt_decode_D3D12_DISPATCH_GRAPH_DESC(dec, (D3D12_DISPATCH_GRAPH_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12GraphicsCommandList10_DispatchGraph_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12GraphicsCommandList10_DispatchGraph *args)
{
}

static inline void
npt_encode_ID3D12GraphicsCommandList10_DispatchGraph_reply(struct npt_cs_encoder *enc,
                                                           const struct npt_command_ID3D12GraphicsCommandList10_DispatchGraph *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12GraphicsCommandList10,
                                 NPT_METHOD_ID3D12GraphicsCommandList10_DispatchGraph),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12GraphicsCommandList10_DispatchGraph(struct npt_dispatch_context *ctx,
                                                       uint32_t cmd_flags,
                                                       uint64_t object_id)
{
    struct npt_command_ID3D12GraphicsCommandList10_DispatchGraph args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12GraphicsCommandList10_DispatchGraph_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12GRAPHICSCOMMANDLIST10);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList10_DispatchGraph on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12GraphicsCommandList10_DispatchGraph_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12GraphicsCommandList10_DispatchGraph: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12GraphicsCommandList10_DispatchGraph _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12GraphicsCommandList10_DispatchGraph, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12GraphicsCommandList10_DispatchGraph);

    if (ctx->id3d12graphicscommandlist10_dispatch_overrides && ctx->id3d12graphicscommandlist10_dispatch_overrides->DispatchGraph) {
        ctx->id3d12graphicscommandlist10_dispatch_overrides->DispatchGraph(ctx, &args, _original);
    } else {
        _original(args._self, args.pDesc);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12GraphicsCommandList10_DispatchGraph_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D12GRAPHICSCOMMANDLIST_H */

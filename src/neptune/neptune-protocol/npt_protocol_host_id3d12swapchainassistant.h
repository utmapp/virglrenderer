/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12swapchainassistant.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D12SWAPCHAINASSISTANT_H
#define NPT_PROTOCOL_HOST_ID3D12SWAPCHAINASSISTANT_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12SwapChainAssistant::GetLUID
 */

struct npt_command_ID3D12SwapChainAssistant_GetLUID {
    void *_self;
    LUID ret;
};

static inline void
npt_decode_ID3D12SwapChainAssistant_GetLUID_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D12SwapChainAssistant_GetLUID *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12SwapChainAssistant_GetLUID_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12SwapChainAssistant_GetLUID *args)
{
}

static inline void
npt_encode_ID3D12SwapChainAssistant_GetLUID_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D12SwapChainAssistant_GetLUID *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_GetLUID),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_LUID(enc, &args->ret);
}

static inline void
npt_dispatch_ID3D12SwapChainAssistant_GetLUID(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D12SwapChainAssistant_GetLUID args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12SwapChainAssistant_GetLUID_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12SWAPCHAINASSISTANT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_GetLUID on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12SwapChainAssistant_GetLUID_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_GetLUID: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12SwapChainAssistant_GetLUID _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12SwapChainAssistant_GetLUID, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12SwapChainAssistant_GetLUID);

    if (ctx->id3d12swapchainassistant_dispatch_overrides && ctx->id3d12swapchainassistant_dispatch_overrides->GetLUID) {
        args.ret = ctx->id3d12swapchainassistant_dispatch_overrides->GetLUID(ctx, &args, _original);
    } else {
        /* COM x64 aggregate-return ABI: the hidden pointer (&args.ret)
         * rides in the call args; the returned pointer aliases it. */
        (void)_original(args._self, &args.ret);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12SwapChainAssistant_GetLUID_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12SwapChainAssistant::GetSwapChainObject
 */

struct npt_command_ID3D12SwapChainAssistant_GetSwapChainObject {
    void *_self;
    const IID * riid;
    void ** ppv;
    /* Shadow: the guest-allocated id for ppv arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppv -> *ppv} in the
     * context object table. */
    uint64_t _guest_id_ppv;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12SwapChainAssistant_GetSwapChainObject_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12SwapChainAssistant_GetSwapChainObject *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppv = _gid;
        args->ppv = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppv) return;
        *args->ppv = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12SwapChainAssistant_GetSwapChainObject_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12SwapChainAssistant_GetSwapChainObject *args)
{
}

static inline void
npt_encode_ID3D12SwapChainAssistant_GetSwapChainObject_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12SwapChainAssistant_GetSwapChainObject *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_GetSwapChainObject),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppv (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12SwapChainAssistant_GetSwapChainObject(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12SwapChainAssistant_GetSwapChainObject args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12SwapChainAssistant_GetSwapChainObject_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12SWAPCHAINASSISTANT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_GetSwapChainObject on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12SwapChainAssistant_GetSwapChainObject_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_GetSwapChainObject: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12SwapChainAssistant_GetSwapChainObject _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12SwapChainAssistant_GetSwapChainObject, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12SwapChainAssistant_GetSwapChainObject);

    if (ctx->id3d12swapchainassistant_dispatch_overrides && ctx->id3d12swapchainassistant_dispatch_overrides->GetSwapChainObject) {
        args.ret = ctx->id3d12swapchainassistant_dispatch_overrides->GetSwapChainObject(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.riid, args.ppv);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppv && *args.ppv)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppv, *args.ppv,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12SwapChainAssistant_GetSwapChainObject_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12SwapChainAssistant::GetCurrentResourceAndCommandQueue
 */

struct npt_command_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue {
    void *_self;
    const IID * riidResource;
    void ** ppvResource;
    const IID * riidQueue;
    void ** ppvQueue;
    /* Shadow: the guest-allocated id for ppvResource arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvResource -> *ppvResource} in the
     * context object table. */
    uint64_t _guest_id_ppvResource;
    /* Shadow: the guest-allocated id for ppvQueue arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvQueue -> *ppvQueue} in the
     * context object table. */
    uint64_t _guest_id_ppvQueue;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_args_temp(struct npt_cs_decoder *dec,
                                                                                struct npt_command_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riidResource = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riidResource) return;
        npt_decode_IID(dec, (IID *)args->riidResource);
    } else {
        args->riidResource = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvResource = _gid;
        args->ppvResource = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvResource) return;
        *args->ppvResource = NULL;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->riidQueue = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riidQueue) return;
        npt_decode_IID(dec, (IID *)args->riidQueue);
    } else {
        args->riidQueue = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvQueue = _gid;
        args->ppvQueue = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvQueue) return;
        *args->ppvQueue = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue *args)
{
}

static inline void
npt_encode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_reply(struct npt_cs_encoder *enc,
                                                                            const struct npt_command_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvResource (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppvQueue (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue(struct npt_dispatch_context *ctx,
                                                                        uint32_t cmd_flags,
                                                                        uint64_t object_id)
{
    struct npt_command_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12SWAPCHAINASSISTANT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue);

    if (ctx->id3d12swapchainassistant_dispatch_overrides && ctx->id3d12swapchainassistant_dispatch_overrides->GetCurrentResourceAndCommandQueue) {
        args.ret = ctx->id3d12swapchainassistant_dispatch_overrides->GetCurrentResourceAndCommandQueue(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.riidResource, args.ppvResource, args.riidQueue, args.ppvQueue);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvResource && *args.ppvResource)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvResource, *args.ppvResource,
            npt_object_type_from_iid(args.riidResource));
    if (args.ppvQueue && *args.ppvQueue)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvQueue, *args.ppvQueue,
            npt_object_type_from_iid(args.riidResource));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12SwapChainAssistant_GetCurrentResourceAndCommandQueue_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12SwapChainAssistant::InsertImplicitSync
 */

struct npt_command_ID3D12SwapChainAssistant_InsertImplicitSync {
    void *_self;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12SwapChainAssistant_InsertImplicitSync_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_ID3D12SwapChainAssistant_InsertImplicitSync *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D12SwapChainAssistant_InsertImplicitSync_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12SwapChainAssistant_InsertImplicitSync *args)
{
}

static inline void
npt_encode_ID3D12SwapChainAssistant_InsertImplicitSync_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_ID3D12SwapChainAssistant_InsertImplicitSync *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12SwapChainAssistant,
                                 NPT_METHOD_ID3D12SwapChainAssistant_InsertImplicitSync),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12SwapChainAssistant_InsertImplicitSync(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_ID3D12SwapChainAssistant_InsertImplicitSync args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12SwapChainAssistant_InsertImplicitSync_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12SWAPCHAINASSISTANT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_InsertImplicitSync on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12SwapChainAssistant_InsertImplicitSync_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12SwapChainAssistant_InsertImplicitSync: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12SwapChainAssistant_InsertImplicitSync _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12SwapChainAssistant_InsertImplicitSync, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12SwapChainAssistant_InsertImplicitSync);

    if (ctx->id3d12swapchainassistant_dispatch_overrides && ctx->id3d12swapchainassistant_dispatch_overrides->InsertImplicitSync) {
        args.ret = ctx->id3d12swapchainassistant_dispatch_overrides->InsertImplicitSync(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12SwapChainAssistant_InsertImplicitSync_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D12SWAPCHAINASSISTANT_H */

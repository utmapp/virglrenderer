/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d11fence.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D11FENCE_H
#define NPT_PROTOCOL_HOST_ID3D11FENCE_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D11Fence::CreateSharedHandle
 */

struct npt_command_ID3D11Fence_CreateSharedHandle {
    void *_self;
    const SECURITY_ATTRIBUTES * pAttributes;
    DWORD dwAccess;
    const WCHAR * lpName;
    HANDLE * pHandle;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Fence_CreateSharedHandle_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_ID3D11Fence_CreateSharedHandle *args)
{
    /* Decode input parameters from the wire */
    /* pAttributes: non-serializable, skipped */
    npt_decode_DWORD(dec, &args->dwAccess);
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->lpName = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->lpName) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->lpName, _wstr_size);
        } else {
            args->lpName = NULL;
        }
    }

    /* Allocate temp storage for output-only parameters */



    args->pHandle = npt_cs_decoder_alloc_temp(dec, sizeof(HANDLE));
    if (!args->pHandle) return;
    memset(args->pHandle, 0, sizeof(HANDLE));
}

static inline void
npt_replace_ID3D11Fence_CreateSharedHandle_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Fence_CreateSharedHandle *args)
{
}

static inline void
npt_encode_ID3D11Fence_CreateSharedHandle_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_ID3D11Fence_CreateSharedHandle *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                 NPT_METHOD_ID3D11Fence_CreateSharedHandle),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)*args->pHandle));
}

static inline void
npt_dispatch_ID3D11Fence_CreateSharedHandle(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_ID3D11Fence_CreateSharedHandle args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Fence_CreateSharedHandle_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11FENCE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Fence_CreateSharedHandle on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Fence_CreateSharedHandle_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Fence_CreateSharedHandle: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Fence_CreateSharedHandle _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Fence_CreateSharedHandle, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Fence_CreateSharedHandle);

    if (ctx->id3d11fence_dispatch_overrides && ctx->id3d11fence_dispatch_overrides->CreateSharedHandle) {
        args.ret = ctx->id3d11fence_dispatch_overrides->CreateSharedHandle(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pAttributes, args.dwAccess, args.lpName, args.pHandle);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Fence_CreateSharedHandle_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Fence::GetCompletedValue
 */

struct npt_command_ID3D11Fence_GetCompletedValue {
    void *_self;
    UINT64 ret;
};

static inline void
npt_decode_ID3D11Fence_GetCompletedValue_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_ID3D11Fence_GetCompletedValue *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_ID3D11Fence_GetCompletedValue_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Fence_GetCompletedValue *args)
{
}

static inline void
npt_encode_ID3D11Fence_GetCompletedValue_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_ID3D11Fence_GetCompletedValue *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                 NPT_METHOD_ID3D11Fence_GetCompletedValue),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_UINT64(enc, &args->ret);
}

static inline void
npt_dispatch_ID3D11Fence_GetCompletedValue(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_ID3D11Fence_GetCompletedValue args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Fence_GetCompletedValue_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11FENCE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Fence_GetCompletedValue on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Fence_GetCompletedValue_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Fence_GetCompletedValue: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Fence_GetCompletedValue _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Fence_GetCompletedValue, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Fence_GetCompletedValue);

    if (ctx->id3d11fence_dispatch_overrides && ctx->id3d11fence_dispatch_overrides->GetCompletedValue) {
        args.ret = ctx->id3d11fence_dispatch_overrides->GetCompletedValue(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Fence_GetCompletedValue_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D11Fence::SetEventOnCompletion
 */

struct npt_command_ID3D11Fence_SetEventOnCompletion {
    void *_self;
    UINT64 Value;
    HANDLE hEvent;
    HRESULT ret;
};

static inline void
npt_decode_ID3D11Fence_SetEventOnCompletion_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_ID3D11Fence_SetEventOnCompletion *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT64(dec, &args->Value);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D11Fence_SetEventOnCompletion_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D11Fence_SetEventOnCompletion *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_ID3D11Fence_SetEventOnCompletion_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_ID3D11Fence_SetEventOnCompletion *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D11Fence,
                                 NPT_METHOD_ID3D11Fence_SetEventOnCompletion),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D11Fence_SetEventOnCompletion(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags,
                                              uint64_t object_id)
{
    struct npt_command_ID3D11Fence_SetEventOnCompletion args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D11Fence_SetEventOnCompletion_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D11FENCE);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Fence_SetEventOnCompletion on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D11Fence_SetEventOnCompletion_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D11Fence_SetEventOnCompletion: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D11Fence_SetEventOnCompletion _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D11Fence_SetEventOnCompletion, npt_com_vtable(args._self),
        NPT_VTBL_ID3D11Fence_SetEventOnCompletion);

    if (ctx->id3d11fence_dispatch_overrides && ctx->id3d11fence_dispatch_overrides->SetEventOnCompletion) {
        args.ret = ctx->id3d11fence_dispatch_overrides->SetEventOnCompletion(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Value, args.hEvent);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D11Fence_SetEventOnCompletion_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D11FENCE_H */

/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for toplevel.
 */

#ifndef NPT_PROTOCOL_HOST_TOPLEVEL_H
#define NPT_PROTOCOL_HOST_TOPLEVEL_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * CreateDXGIFactory (group 1, id 0)
 */

struct npt_command_CreateDXGIFactory {
    const IID * riid;
    void ** ppFactory;
    /* Shadow: guest-allocated id for ppFactory. */
    uint64_t _guest_id_ppFactory;
    HRESULT ret;
};

static inline void
npt_decode_CreateDXGIFactory_args_temp(struct npt_cs_decoder *dec,
                                       struct npt_command_CreateDXGIFactory *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppFactory = _gid;
        args->ppFactory = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppFactory) return;
        *args->ppFactory = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_CreateDXGIFactory_args_handle(struct npt_dispatch_context *ctx, struct npt_command_CreateDXGIFactory *args)
{
}

static inline void
npt_encode_CreateDXGIFactory_reply(struct npt_cs_encoder *enc,
                                   const struct npt_command_CreateDXGIFactory *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 0),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppFactory (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_CreateDXGIFactory(struct npt_dispatch_context *ctx,
                               uint32_t cmd_flags)
{
    struct npt_command_CreateDXGIFactory args;
    memset(&args, 0, sizeof(args));

    npt_decode_CreateDXGIFactory_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_CreateDXGIFactory_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping CreateDXGIFactory: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFactory);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->CreateDXGIFactory) {
        args.ret = ctx->toplevel_dispatch_overrides->CreateDXGIFactory(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */
    if (args.ppFactory && *args.ppFactory)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppFactory, *args.ppFactory,
            npt_object_type_from_iid(args.riid));
    else if (args.ppFactory)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFactory);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_CreateDXGIFactory_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * CreateDXGIFactory1 (group 1, id 1)
 */

struct npt_command_CreateDXGIFactory1 {
    const IID * riid;
    void ** ppFactory;
    /* Shadow: guest-allocated id for ppFactory. */
    uint64_t _guest_id_ppFactory;
    HRESULT ret;
};

static inline void
npt_decode_CreateDXGIFactory1_args_temp(struct npt_cs_decoder *dec,
                                        struct npt_command_CreateDXGIFactory1 *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppFactory = _gid;
        args->ppFactory = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppFactory) return;
        *args->ppFactory = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_CreateDXGIFactory1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_CreateDXGIFactory1 *args)
{
}

static inline void
npt_encode_CreateDXGIFactory1_reply(struct npt_cs_encoder *enc,
                                    const struct npt_command_CreateDXGIFactory1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppFactory (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_CreateDXGIFactory1(struct npt_dispatch_context *ctx,
                                uint32_t cmd_flags)
{
    struct npt_command_CreateDXGIFactory1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_CreateDXGIFactory1_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_CreateDXGIFactory1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping CreateDXGIFactory1: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFactory);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->CreateDXGIFactory1) {
        args.ret = ctx->toplevel_dispatch_overrides->CreateDXGIFactory1(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */
    if (args.ppFactory && *args.ppFactory)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppFactory, *args.ppFactory,
            npt_object_type_from_iid(args.riid));
    else if (args.ppFactory)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFactory);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_CreateDXGIFactory1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * CreateDXGIFactory2 (group 1, id 2)
 */

struct npt_command_CreateDXGIFactory2 {
    UINT Flags;
    const IID * riid;
    void ** ppFactory;
    /* Shadow: guest-allocated id for ppFactory. */
    uint64_t _guest_id_ppFactory;
    HRESULT ret;
};

static inline void
npt_decode_CreateDXGIFactory2_args_temp(struct npt_cs_decoder *dec,
                                        struct npt_command_CreateDXGIFactory2 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Flags);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppFactory = _gid;
        args->ppFactory = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppFactory) return;
        *args->ppFactory = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_CreateDXGIFactory2_args_handle(struct npt_dispatch_context *ctx, struct npt_command_CreateDXGIFactory2 *args)
{
}

static inline void
npt_encode_CreateDXGIFactory2_reply(struct npt_cs_encoder *enc,
                                    const struct npt_command_CreateDXGIFactory2 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppFactory (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_CreateDXGIFactory2(struct npt_dispatch_context *ctx,
                                uint32_t cmd_flags)
{
    struct npt_command_CreateDXGIFactory2 args;
    memset(&args, 0, sizeof(args));

    npt_decode_CreateDXGIFactory2_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_CreateDXGIFactory2_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping CreateDXGIFactory2: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFactory);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->CreateDXGIFactory2) {
        args.ret = ctx->toplevel_dispatch_overrides->CreateDXGIFactory2(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */
    if (args.ppFactory && *args.ppFactory)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppFactory, *args.ppFactory,
            npt_object_type_from_iid(args.riid));
    else if (args.ppFactory)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppFactory);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_CreateDXGIFactory2_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * DXGIDeclareAdapterRemovalSupport (group 1, id 3)
 */

struct npt_command_DXGIDeclareAdapterRemovalSupport {
    HRESULT ret;
};

static inline void
npt_decode_DXGIDeclareAdapterRemovalSupport_args_temp(struct npt_cs_decoder *dec,
                                                      struct npt_command_DXGIDeclareAdapterRemovalSupport *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_DXGIDeclareAdapterRemovalSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_DXGIDeclareAdapterRemovalSupport *args)
{
}

static inline void
npt_encode_DXGIDeclareAdapterRemovalSupport_reply(struct npt_cs_encoder *enc,
                                                  const struct npt_command_DXGIDeclareAdapterRemovalSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(1, 3),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_DXGIDeclareAdapterRemovalSupport(struct npt_dispatch_context *ctx,
                                              uint32_t cmd_flags)
{
    struct npt_command_DXGIDeclareAdapterRemovalSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_DXGIDeclareAdapterRemovalSupport_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_DXGIDeclareAdapterRemovalSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping DXGIDeclareAdapterRemovalSupport: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->DXGIDeclareAdapterRemovalSupport) {
        args.ret = ctx->toplevel_dispatch_overrides->DXGIDeclareAdapterRemovalSupport(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_DXGIDeclareAdapterRemovalSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D11CreateDevice (group 2, id 0)
 */

struct npt_command_D3D11CreateDevice {
    IDXGIAdapter * pAdapter;
    D3D_DRIVER_TYPE DriverType;
    HMODULE Software;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    UINT SDKVersion;
    ID3D11Device ** ppDevice;
    D3D_FEATURE_LEVEL * pFeatureLevel;
    ID3D11DeviceContext ** ppImmediateContext;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    /* Shadow: guest-allocated id for ppImmediateContext. */
    uint64_t _guest_id_ppImmediateContext;
    HRESULT ret;
};

static inline void
npt_decode_D3D11CreateDevice_args_temp(struct npt_cs_decoder *dec,
                                       struct npt_command_D3D11CreateDevice *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pAdapter = (IDXGIAdapter *)npt_object_from_id(_id);
    }
    npt_decode_D3D_DRIVER_TYPE(dec, &args->DriverType);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->Software = (HMODULE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    npt_decode_UINT(dec, &args->Flags);
    uint64_t _cnt_pFeatureLevels = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFeatureLevels = npt_decode_array_count_unchecked(dec);
        args->pFeatureLevels = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D_FEATURE_LEVEL), _cnt_pFeatureLevels);
        if (!args->pFeatureLevels) return;
        npt_decode_D3D_FEATURE_LEVEL_array(dec, (D3D_FEATURE_LEVEL *)args->pFeatureLevels, _cnt_pFeatureLevels);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFeatureLevels = NULL;
    }
    npt_decode_UINT(dec, &args->FeatureLevels);
    npt_decode_UINT(dec, &args->SDKVersion);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDevice = _gid;
        args->ppDevice = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDevice) return;
        *args->ppDevice = NULL;
    }

    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppImmediateContext = _gid;
        args->ppImmediateContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppImmediateContext) return;
        *args->ppImmediateContext = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pFeatureLevels && _cnt_pFeatureLevels < (uint64_t)(args->FeatureLevels)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */








    args->pFeatureLevel = npt_cs_decoder_alloc_temp(dec, sizeof(D3D_FEATURE_LEVEL));
    if (!args->pFeatureLevel) return;
    memset(args->pFeatureLevel, 0, sizeof(D3D_FEATURE_LEVEL));

}

static inline void
npt_replace_D3D11CreateDevice_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D11CreateDevice *args)
{
    args->pAdapter = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pAdapter, NPT_OBJECT_TYPE_IDXGIADAPTER);
    args->Software = (HMODULE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->Software);
}

static inline void
npt_encode_D3D11CreateDevice_reply(struct npt_cs_encoder *enc,
                                   const struct npt_command_D3D11CreateDevice *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(2, 0),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDevice (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pFeatureLevel))
        npt_encode_D3D_FEATURE_LEVEL(enc, args->pFeatureLevel);
/* skip args->ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_D3D11CreateDevice(struct npt_dispatch_context *ctx,
                               uint32_t cmd_flags)
{
    struct npt_command_D3D11CreateDevice args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D11CreateDevice_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D11CreateDevice_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D11CreateDevice: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppImmediateContext);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D11CreateDevice) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D11CreateDevice(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */
    if (args.ppDevice && *args.ppDevice)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDevice, *args.ppDevice, NPT_OBJECT_TYPE_ID3D11DEVICE);
    else if (args.ppDevice)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);
    if (args.ppImmediateContext && *args.ppImmediateContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppImmediateContext, *args.ppImmediateContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    else if (args.ppImmediateContext)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppImmediateContext);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D11CreateDevice_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D11CreateDeviceAndSwapChain (group 2, id 1)
 */

struct npt_command_D3D11CreateDeviceAndSwapChain {
    IDXGIAdapter * pAdapter;
    D3D_DRIVER_TYPE DriverType;
    HMODULE Software;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    UINT SDKVersion;
    const DXGI_SWAP_CHAIN_DESC * pSwapChainDesc;
    IDXGISwapChain ** ppSwapChain;
    ID3D11Device ** ppDevice;
    D3D_FEATURE_LEVEL * pFeatureLevel;
    ID3D11DeviceContext ** ppImmediateContext;
    /* Shadow: guest-allocated id for ppSwapChain. */
    uint64_t _guest_id_ppSwapChain;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    /* Shadow: guest-allocated id for ppImmediateContext. */
    uint64_t _guest_id_ppImmediateContext;
    HRESULT ret;
};

static inline void
npt_decode_D3D11CreateDeviceAndSwapChain_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_D3D11CreateDeviceAndSwapChain *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pAdapter = (IDXGIAdapter *)npt_object_from_id(_id);
    }
    npt_decode_D3D_DRIVER_TYPE(dec, &args->DriverType);
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->Software = (HMODULE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    npt_decode_UINT(dec, &args->Flags);
    uint64_t _cnt_pFeatureLevels = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFeatureLevels = npt_decode_array_count_unchecked(dec);
        args->pFeatureLevels = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D_FEATURE_LEVEL), _cnt_pFeatureLevels);
        if (!args->pFeatureLevels) return;
        npt_decode_D3D_FEATURE_LEVEL_array(dec, (D3D_FEATURE_LEVEL *)args->pFeatureLevels, _cnt_pFeatureLevels);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFeatureLevels = NULL;
    }
    npt_decode_UINT(dec, &args->FeatureLevels);
    npt_decode_UINT(dec, &args->SDKVersion);
    if (npt_decode_simple_pointer(dec)) {
        args->pSwapChainDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_DESC));
        if (!args->pSwapChainDesc) return;
        npt_decode_DXGI_SWAP_CHAIN_DESC(dec, (DXGI_SWAP_CHAIN_DESC *)args->pSwapChainDesc);
    } else {
        args->pSwapChainDesc = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSwapChain = _gid;
        args->ppSwapChain = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSwapChain) return;
        *args->ppSwapChain = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDevice = _gid;
        args->ppDevice = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDevice) return;
        *args->ppDevice = NULL;
    }

    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppImmediateContext = _gid;
        args->ppImmediateContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppImmediateContext) return;
        *args->ppImmediateContext = NULL;
    }
    /* A counted array must carry every element the callee will read. */
    if (_cnt_pFeatureLevels && _cnt_pFeatureLevels < (uint64_t)(args->FeatureLevels)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */










    args->pFeatureLevel = npt_cs_decoder_alloc_temp(dec, sizeof(D3D_FEATURE_LEVEL));
    if (!args->pFeatureLevel) return;
    memset(args->pFeatureLevel, 0, sizeof(D3D_FEATURE_LEVEL));

}

static inline void
npt_replace_D3D11CreateDeviceAndSwapChain_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D11CreateDeviceAndSwapChain *args)
{
    args->pAdapter = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pAdapter, NPT_OBJECT_TYPE_IDXGIADAPTER);
    args->Software = (HMODULE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->Software);
    if (args->pSwapChainDesc)
        npt_replace_DXGI_SWAP_CHAIN_DESC_handle(ctx, (DXGI_SWAP_CHAIN_DESC *)args->pSwapChainDesc);
}

static inline void
npt_encode_D3D11CreateDeviceAndSwapChain_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_D3D11CreateDeviceAndSwapChain *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(2, 1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSwapChain (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppDevice (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pFeatureLevel))
        npt_encode_D3D_FEATURE_LEVEL(enc, args->pFeatureLevel);
/* skip args->ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_D3D11CreateDeviceAndSwapChain(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags)
{
    struct npt_command_D3D11CreateDeviceAndSwapChain args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D11CreateDeviceAndSwapChain_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D11CreateDeviceAndSwapChain_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D11CreateDeviceAndSwapChain: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSwapChain);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppImmediateContext);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D11CreateDeviceAndSwapChain) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D11CreateDeviceAndSwapChain(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */
    if (args.ppSwapChain && *args.ppSwapChain)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSwapChain, *args.ppSwapChain, NPT_OBJECT_TYPE_IDXGISWAPCHAIN);
    else if (args.ppSwapChain)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppSwapChain);
    if (args.ppDevice && *args.ppDevice)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDevice, *args.ppDevice, NPT_OBJECT_TYPE_ID3D11DEVICE);
    else if (args.ppDevice)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);
    if (args.ppImmediateContext && *args.ppImmediateContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppImmediateContext, *args.ppImmediateContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    else if (args.ppImmediateContext)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppImmediateContext);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D11CreateDeviceAndSwapChain_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D11On12CreateDevice (group 2, id 2)
 */

struct npt_command_D3D11On12CreateDevice {
    IUnknown * pDevice;
    UINT Flags;
    const D3D_FEATURE_LEVEL * pFeatureLevels;
    UINT FeatureLevels;
    const IUnknown ** ppCommandQueues;
    UINT NumQueues;
    UINT NodeMask;
    ID3D11Device ** ppDevice;
    ID3D11DeviceContext ** ppImmediateContext;
    D3D_FEATURE_LEVEL * pChosenFeatureLevel;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    /* Shadow: guest-allocated id for ppImmediateContext. */
    uint64_t _guest_id_ppImmediateContext;
    HRESULT ret;
};

static inline void
npt_decode_D3D11On12CreateDevice_args_temp(struct npt_cs_decoder *dec,
                                           struct npt_command_D3D11On12CreateDevice *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    npt_decode_UINT(dec, &args->Flags);
    uint64_t _cnt_pFeatureLevels = 0;
    if (npt_peek_array_count(dec)) {
        _cnt_pFeatureLevels = npt_decode_array_count_unchecked(dec);
        args->pFeatureLevels = npt_cs_decoder_alloc_temp_array(dec, sizeof(D3D_FEATURE_LEVEL), _cnt_pFeatureLevels);
        if (!args->pFeatureLevels) return;
        npt_decode_D3D_FEATURE_LEVEL_array(dec, (D3D_FEATURE_LEVEL *)args->pFeatureLevels, _cnt_pFeatureLevels);
    } else {
        (void)npt_decode_array_count_unchecked(dec); /* consume the 0 */
        args->pFeatureLevels = NULL;
    }
    npt_decode_UINT(dec, &args->FeatureLevels);
    uint64_t _cnt_ppCommandQueues = npt_decode_array_count_unchecked(dec);
    if (_cnt_ppCommandQueues) {
        args->ppCommandQueues = npt_cs_decoder_alloc_temp_array(dec, sizeof(void *), _cnt_ppCommandQueues);
        if (!args->ppCommandQueues) return;
        for (uint64_t _i = 0; _i < _cnt_ppCommandQueues; _i++) {
            npt_object_id _id;
            npt_decode_uint64_t(dec, &_id);
            args->ppCommandQueues[_i] = npt_object_from_id(_id);
        }
    } else {
        args->ppCommandQueues = NULL;
    }
    npt_decode_UINT(dec, &args->NumQueues);
    npt_decode_UINT(dec, &args->NodeMask);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDevice = _gid;
        args->ppDevice = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDevice) return;
        *args->ppDevice = NULL;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppImmediateContext = _gid;
        args->ppImmediateContext = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppImmediateContext) return;
        *args->ppImmediateContext = NULL;
    }

    /* A counted array must carry every element the callee will read. */
    if (_cnt_pFeatureLevels && _cnt_pFeatureLevels < (uint64_t)(args->FeatureLevels)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    if (_cnt_ppCommandQueues && _cnt_ppCommandQueues < (uint64_t)(args->NumQueues)) {
        npt_cs_decoder_set_fatal(dec);
        return;
    }
    /* Allocate temp storage for output-only parameters */









    args->pChosenFeatureLevel = npt_cs_decoder_alloc_temp(dec, sizeof(D3D_FEATURE_LEVEL));
    if (!args->pChosenFeatureLevel) return;
    memset(args->pChosenFeatureLevel, 0, sizeof(D3D_FEATURE_LEVEL));
}

static inline void
npt_replace_D3D11On12CreateDevice_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D11On12CreateDevice *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
    if (args->ppCommandQueues) {
        for (uint32_t _i = 0; _i < (uint32_t)(args->NumQueues); _i++)
            args->ppCommandQueues[_i] = npt_cs_handle_lookup(ctx,
                (npt_object_id)(uintptr_t)args->ppCommandQueues[_i], NPT_OBJECT_TYPE_IUNKNOWN);
    }
}

static inline void
npt_encode_D3D11On12CreateDevice_reply(struct npt_cs_encoder *enc,
                                       const struct npt_command_D3D11On12CreateDevice *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(2, 2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDevice (guest-allocated id, registered host-side; not in reply) */
/* skip args->ppImmediateContext (guest-allocated id, registered host-side; not in reply) */
    if (npt_encode_simple_pointer(enc, args->pChosenFeatureLevel))
        npt_encode_D3D_FEATURE_LEVEL(enc, args->pChosenFeatureLevel);
}

static inline void
npt_dispatch_D3D11On12CreateDevice(struct npt_dispatch_context *ctx,
                                   uint32_t cmd_flags)
{
    struct npt_command_D3D11On12CreateDevice args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D11On12CreateDevice_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D11On12CreateDevice_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D11On12CreateDevice: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppImmediateContext);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D11On12CreateDevice) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D11On12CreateDevice(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */
    if (args.ppDevice && *args.ppDevice)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDevice, *args.ppDevice, NPT_OBJECT_TYPE_ID3D11DEVICE);
    else if (args.ppDevice)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);
    if (args.ppImmediateContext && *args.ppImmediateContext)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppImmediateContext, *args.ppImmediateContext, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
    else if (args.ppImmediateContext)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppImmediateContext);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D11On12CreateDevice_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D12CreateDevice (group 3, id 0)
 */

struct npt_command_D3D12CreateDevice {
    IUnknown * pAdapter;
    D3D_FEATURE_LEVEL MinimumFeatureLevel;
    const IID * riid;
    void ** ppDevice;
    /* Shadow: guest-allocated id for ppDevice. */
    uint64_t _guest_id_ppDevice;
    HRESULT ret;
};

static inline void
npt_decode_D3D12CreateDevice_args_temp(struct npt_cs_decoder *dec,
                                       struct npt_command_D3D12CreateDevice *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pAdapter = (IUnknown *)npt_object_from_id(_id);
    }
    npt_decode_D3D_FEATURE_LEVEL(dec, &args->MinimumFeatureLevel);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppDevice = _gid;
        args->ppDevice = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppDevice) return;
        *args->ppDevice = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_D3D12CreateDevice_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D12CreateDevice *args)
{
    args->pAdapter = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pAdapter, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_D3D12CreateDevice_reply(struct npt_cs_encoder *enc,
                                   const struct npt_command_D3D12CreateDevice *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 0),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppDevice (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_D3D12CreateDevice(struct npt_dispatch_context *ctx,
                               uint32_t cmd_flags)
{
    struct npt_command_D3D12CreateDevice args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D12CreateDevice_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D12CreateDevice_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D12CreateDevice: an argument handle is unknown "
                    "to this context");
            npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D12CreateDevice) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D12CreateDevice(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */
    if (args.ppDevice && *args.ppDevice)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppDevice, *args.ppDevice,
            npt_object_type_from_iid(args.riid));
    else if (args.ppDevice)
        npt_cs_handle_register_failed_guest_id(ctx, args._guest_id_ppDevice);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D12CreateDevice_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D12CreateRootSignatureDeserializer (group 3, id 1)
 */

struct npt_command_D3D12CreateRootSignatureDeserializer {
    const VOID * pSrcData;
    SIZE_T SrcDataSizeInBytes;
    const IID * pRootSignatureDeserializerInterface;
    void ** ppRootSignatureDeserializer;
    HRESULT ret;
};

static inline void
npt_decode_D3D12CreateRootSignatureDeserializer_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_D3D12CreateRootSignatureDeserializer *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pSrcData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pSrcData) return;
            npt_decode_blob_array(dec, (void *)args->pSrcData, _blob_size);
        } else {
            args->pSrcData = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->SrcDataSizeInBytes);
    if (npt_decode_simple_pointer(dec)) {
        args->pRootSignatureDeserializerInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->pRootSignatureDeserializerInterface) return;
        npt_decode_IID(dec, (IID *)args->pRootSignatureDeserializerInterface);
    } else {
        args->pRootSignatureDeserializerInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */



    args->ppRootSignatureDeserializer = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
    if (!args->ppRootSignatureDeserializer) return;
    *args->ppRootSignatureDeserializer = NULL;
}

static inline void
npt_replace_D3D12CreateRootSignatureDeserializer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D12CreateRootSignatureDeserializer *args)
{
}

static inline void
npt_encode_D3D12CreateRootSignatureDeserializer_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_D3D12CreateRootSignatureDeserializer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    /* ERROR: ppRootSignatureDeserializer (void) is not serializable */
    return;
}

static inline void
npt_dispatch_D3D12CreateRootSignatureDeserializer(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags)
{
    struct npt_command_D3D12CreateRootSignatureDeserializer args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D12CreateRootSignatureDeserializer_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D12CreateRootSignatureDeserializer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D12CreateRootSignatureDeserializer: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D12CreateRootSignatureDeserializer) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D12CreateRootSignatureDeserializer(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D12CreateRootSignatureDeserializer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D12CreateVersionedRootSignatureDeserializer (group 3, id 2)
 */

struct npt_command_D3D12CreateVersionedRootSignatureDeserializer {
    const VOID * pSrcData;
    SIZE_T SrcDataSizeInBytes;
    const IID * pRootSignatureDeserializerInterface;
    void ** ppRootSignatureDeserializer;
    HRESULT ret;
};

static inline void
npt_decode_D3D12CreateVersionedRootSignatureDeserializer_args_temp(struct npt_cs_decoder *dec,
                                                                   struct npt_command_D3D12CreateVersionedRootSignatureDeserializer *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pSrcData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pSrcData) return;
            npt_decode_blob_array(dec, (void *)args->pSrcData, _blob_size);
        } else {
            args->pSrcData = NULL;
        }
    }
    npt_decode_SIZE_T(dec, &args->SrcDataSizeInBytes);
    if (npt_decode_simple_pointer(dec)) {
        args->pRootSignatureDeserializerInterface = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->pRootSignatureDeserializerInterface) return;
        npt_decode_IID(dec, (IID *)args->pRootSignatureDeserializerInterface);
    } else {
        args->pRootSignatureDeserializerInterface = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */



    args->ppRootSignatureDeserializer = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
    if (!args->ppRootSignatureDeserializer) return;
    *args->ppRootSignatureDeserializer = NULL;
}

static inline void
npt_replace_D3D12CreateVersionedRootSignatureDeserializer_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D12CreateVersionedRootSignatureDeserializer *args)
{
}

static inline void
npt_encode_D3D12CreateVersionedRootSignatureDeserializer_reply(struct npt_cs_encoder *enc,
                                                               const struct npt_command_D3D12CreateVersionedRootSignatureDeserializer *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 2),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    /* ERROR: ppRootSignatureDeserializer (void) is not serializable */
    return;
}

static inline void
npt_dispatch_D3D12CreateVersionedRootSignatureDeserializer(struct npt_dispatch_context *ctx,
                                                           uint32_t cmd_flags)
{
    struct npt_command_D3D12CreateVersionedRootSignatureDeserializer args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D12CreateVersionedRootSignatureDeserializer_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D12CreateVersionedRootSignatureDeserializer_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D12CreateVersionedRootSignatureDeserializer: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D12CreateVersionedRootSignatureDeserializer) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D12CreateVersionedRootSignatureDeserializer(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D12CreateVersionedRootSignatureDeserializer_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D12SerializeRootSignature (group 3, id 3)
 */

struct npt_command_D3D12SerializeRootSignature {
    const D3D12_ROOT_SIGNATURE_DESC * pRootSignature;
    D3D_ROOT_SIGNATURE_VERSION Version;
    UINT * pBlobSize;
    void * pBlobData;
    UINT * pErrorBlobSize;
    void * pErrorBlobData;
    HRESULT ret;
};

static inline void
npt_decode_D3D12SerializeRootSignature_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_D3D12SerializeRootSignature *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pRootSignature = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_ROOT_SIGNATURE_DESC));
        if (!args->pRootSignature) return;
        npt_decode_D3D12_ROOT_SIGNATURE_DESC(dec, (D3D12_ROOT_SIGNATURE_DESC *)args->pRootSignature);
    } else {
        args->pRootSignature = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_D3D_ROOT_SIGNATURE_VERSION(dec, &args->Version);
    if (npt_decode_simple_pointer(dec)) {
        args->pBlobSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pBlobSize) return;
        npt_decode_UINT(dec, (UINT *)args->pBlobSize);
    } else {
        args->pBlobSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    if (npt_decode_simple_pointer(dec)) {
        args->pErrorBlobSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pErrorBlobSize) return;
        npt_decode_UINT(dec, (UINT *)args->pErrorBlobSize);
    } else {
        args->pErrorBlobSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */



    if (*args->pBlobSize) {
        args->pBlobData = npt_cs_decoder_alloc_temp(dec, *args->pBlobSize);
        if (!args->pBlobData) return;
        memset(args->pBlobData, 0, *args->pBlobSize);
    } else {
        args->pBlobData = NULL;
    }

    if (*args->pErrorBlobSize) {
        args->pErrorBlobData = npt_cs_decoder_alloc_temp(dec, *args->pErrorBlobSize);
        if (!args->pErrorBlobData) return;
        memset(args->pErrorBlobData, 0, *args->pErrorBlobSize);
    } else {
        args->pErrorBlobData = NULL;
    }
}

static inline void
npt_replace_D3D12SerializeRootSignature_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D12SerializeRootSignature *args)
{
}

static inline void
npt_encode_D3D12SerializeRootSignature_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_D3D12SerializeRootSignature *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 3),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pBlobSize))
        npt_encode_UINT(enc, args->pBlobSize);
    if (args->pBlobData) {
        npt_encode_array_count(enc, *args->pBlobSize);
        npt_encode_blob_array(enc, args->pBlobData, *args->pBlobSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pErrorBlobSize))
        npt_encode_UINT(enc, args->pErrorBlobSize);
    if (args->pErrorBlobData) {
        npt_encode_array_count(enc, *args->pErrorBlobSize);
        npt_encode_blob_array(enc, args->pErrorBlobData, *args->pErrorBlobSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_D3D12SerializeRootSignature(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags)
{
    struct npt_command_D3D12SerializeRootSignature args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D12SerializeRootSignature_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D12SerializeRootSignature_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D12SerializeRootSignature: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D12SerializeRootSignature) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D12SerializeRootSignature(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D12SerializeRootSignature_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * D3D12SerializeVersionedRootSignature (group 3, id 4)
 */

struct npt_command_D3D12SerializeVersionedRootSignature {
    const D3D12_VERSIONED_ROOT_SIGNATURE_DESC * pRootSignature;
    UINT * pBlobSize;
    void * pBlobData;
    UINT * pErrorBlobSize;
    void * pErrorBlobData;
    HRESULT ret;
};

static inline void
npt_decode_D3D12SerializeVersionedRootSignature_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_D3D12SerializeVersionedRootSignature *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->pRootSignature = npt_cs_decoder_alloc_temp(dec, sizeof(D3D12_VERSIONED_ROOT_SIGNATURE_DESC));
        if (!args->pRootSignature) return;
        npt_decode_D3D12_VERSIONED_ROOT_SIGNATURE_DESC(dec, (D3D12_VERSIONED_ROOT_SIGNATURE_DESC *)args->pRootSignature);
    } else {
        args->pRootSignature = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pBlobSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pBlobSize) return;
        npt_decode_UINT(dec, (UINT *)args->pBlobSize);
    } else {
        args->pBlobSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    if (npt_decode_simple_pointer(dec)) {
        args->pErrorBlobSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pErrorBlobSize) return;
        npt_decode_UINT(dec, (UINT *)args->pErrorBlobSize);
    } else {
        args->pErrorBlobSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */


    if (*args->pBlobSize) {
        args->pBlobData = npt_cs_decoder_alloc_temp(dec, *args->pBlobSize);
        if (!args->pBlobData) return;
        memset(args->pBlobData, 0, *args->pBlobSize);
    } else {
        args->pBlobData = NULL;
    }

    if (*args->pErrorBlobSize) {
        args->pErrorBlobData = npt_cs_decoder_alloc_temp(dec, *args->pErrorBlobSize);
        if (!args->pErrorBlobData) return;
        memset(args->pErrorBlobData, 0, *args->pErrorBlobSize);
    } else {
        args->pErrorBlobData = NULL;
    }
}

static inline void
npt_replace_D3D12SerializeVersionedRootSignature_args_handle(struct npt_dispatch_context *ctx, struct npt_command_D3D12SerializeVersionedRootSignature *args)
{
}

static inline void
npt_encode_D3D12SerializeVersionedRootSignature_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_D3D12SerializeVersionedRootSignature *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE_TOPLEVEL(3, 4),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pBlobSize))
        npt_encode_UINT(enc, args->pBlobSize);
    if (args->pBlobData) {
        npt_encode_array_count(enc, *args->pBlobSize);
        npt_encode_blob_array(enc, args->pBlobData, *args->pBlobSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
    if (npt_encode_simple_pointer(enc, args->pErrorBlobSize))
        npt_encode_UINT(enc, args->pErrorBlobSize);
    if (args->pErrorBlobData) {
        npt_encode_array_count(enc, *args->pErrorBlobSize);
        npt_encode_blob_array(enc, args->pErrorBlobData, *args->pErrorBlobSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_D3D12SerializeVersionedRootSignature(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags)
{
    struct npt_command_D3D12SerializeVersionedRootSignature args;
    memset(&args, 0, sizeof(args));

    npt_decode_D3D12SerializeVersionedRootSignature_args_temp(ctx->decoder, &args);
    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    npt_replace_D3D12SerializeVersionedRootSignature_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing method self: drop when nothing awaits a reply, go
         * fatal when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping D3D12SerializeVersionedRootSignature: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    if (ctx->toplevel_dispatch_overrides &&
        ctx->toplevel_dispatch_overrides->D3D12SerializeVersionedRootSignature) {
        args.ret = ctx->toplevel_dispatch_overrides->D3D12SerializeVersionedRootSignature(ctx, &args);
    } else {
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    /* Register any output COM handles.  Top-level functions with a
     * riid + void** pair (CreateDXGIFactory1, D3D12CreateDevice, ...)
     * get runtime type resolution via npt_object_type_from_iid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_D3D12SerializeVersionedRootSignature_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_TOPLEVEL_H */

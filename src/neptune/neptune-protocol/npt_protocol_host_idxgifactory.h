/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for idxgifactory.
 */

#ifndef NPT_PROTOCOL_HOST_IDXGIFACTORY_H
#define NPT_PROTOCOL_HOST_IDXGIFACTORY_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * IDXGIFactory::EnumAdapters
 */

struct npt_command_IDXGIFactory_EnumAdapters {
    void *_self;
    UINT Adapter;
    IDXGIAdapter ** ppAdapter;
    /* Shadow: the guest-allocated id for ppAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAdapter -> *ppAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppAdapter;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory_EnumAdapters_args_temp(struct npt_cs_decoder *dec,
                                               struct npt_command_IDXGIFactory_EnumAdapters *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Adapter);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppAdapter = _gid;
        args->ppAdapter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppAdapter) return;
        *args->ppAdapter = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIFactory_EnumAdapters_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory_EnumAdapters *args)
{
}

static inline void
npt_encode_IDXGIFactory_EnumAdapters_reply(struct npt_cs_encoder *enc,
                                           const struct npt_command_IDXGIFactory_EnumAdapters *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_EnumAdapters),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppAdapter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory_EnumAdapters(struct npt_dispatch_context *ctx,
                                       uint32_t cmd_flags,
                                       uint64_t object_id)
{
    struct npt_command_IDXGIFactory_EnumAdapters args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory_EnumAdapters_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_EnumAdapters on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory_EnumAdapters_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_EnumAdapters: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory_EnumAdapters _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory_EnumAdapters, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory_EnumAdapters);

    if (ctx->idxgifactory_dispatch_overrides && ctx->idxgifactory_dispatch_overrides->EnumAdapters) {
        args.ret = ctx->idxgifactory_dispatch_overrides->EnumAdapters(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Adapter, args.ppAdapter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppAdapter && *args.ppAdapter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppAdapter, *args.ppAdapter, NPT_OBJECT_TYPE_IDXGIADAPTER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory_EnumAdapters_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory::MakeWindowAssociation
 */

struct npt_command_IDXGIFactory_MakeWindowAssociation {
    void *_self;
    HWND WindowHandle;
    UINT Flags;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory_MakeWindowAssociation_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIFactory_MakeWindowAssociation *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->WindowHandle = (HWND)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    npt_decode_UINT(dec, &args->Flags);
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIFactory_MakeWindowAssociation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory_MakeWindowAssociation *args)
{
    args->WindowHandle = (HWND)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->WindowHandle);
}

static inline void
npt_encode_IDXGIFactory_MakeWindowAssociation_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIFactory_MakeWindowAssociation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_MakeWindowAssociation),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIFactory_MakeWindowAssociation(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIFactory_MakeWindowAssociation args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory_MakeWindowAssociation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_MakeWindowAssociation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory_MakeWindowAssociation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_MakeWindowAssociation: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory_MakeWindowAssociation _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory_MakeWindowAssociation, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory_MakeWindowAssociation);

    if (ctx->idxgifactory_dispatch_overrides && ctx->idxgifactory_dispatch_overrides->MakeWindowAssociation) {
        args.ret = ctx->idxgifactory_dispatch_overrides->MakeWindowAssociation(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.WindowHandle, args.Flags);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory_MakeWindowAssociation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory::GetWindowAssociation
 */

struct npt_command_IDXGIFactory_GetWindowAssociation {
    void *_self;
    HWND * pWindowHandle;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory_GetWindowAssociation_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_IDXGIFactory_GetWindowAssociation *args)
{
    /* Decode input parameters from the wire */

    /* Allocate temp storage for output-only parameters */
    args->pWindowHandle = npt_cs_decoder_alloc_temp(dec, sizeof(HWND));
    if (!args->pWindowHandle) return;
    memset(args->pWindowHandle, 0, sizeof(HWND));
}

static inline void
npt_replace_IDXGIFactory_GetWindowAssociation_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory_GetWindowAssociation *args)
{
}

static inline void
npt_encode_IDXGIFactory_GetWindowAssociation_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_IDXGIFactory_GetWindowAssociation *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_GetWindowAssociation),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    npt_encode_win32_handle(enc, npt_win32_handle_get_id((const void *)(uintptr_t)*args->pWindowHandle));
}

static inline void
npt_dispatch_IDXGIFactory_GetWindowAssociation(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_IDXGIFactory_GetWindowAssociation args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory_GetWindowAssociation_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_GetWindowAssociation on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory_GetWindowAssociation_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_GetWindowAssociation: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory_GetWindowAssociation _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory_GetWindowAssociation, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory_GetWindowAssociation);

    if (ctx->idxgifactory_dispatch_overrides && ctx->idxgifactory_dispatch_overrides->GetWindowAssociation) {
        args.ret = ctx->idxgifactory_dispatch_overrides->GetWindowAssociation(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pWindowHandle);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory_GetWindowAssociation_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory::CreateSwapChain
 */

struct npt_command_IDXGIFactory_CreateSwapChain {
    void *_self;
    IUnknown * pDevice;
    DXGI_SWAP_CHAIN_DESC * pDesc;
    IDXGISwapChain ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory_CreateSwapChain_args_temp(struct npt_cs_decoder *dec,
                                                  struct npt_command_IDXGIFactory_CreateSwapChain *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_DESC));
        if (!args->pDesc) return;
        npt_decode_DXGI_SWAP_CHAIN_DESC(dec, (DXGI_SWAP_CHAIN_DESC *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSwapChain = _gid;
        args->ppSwapChain = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSwapChain) return;
        *args->ppSwapChain = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGIFactory_CreateSwapChain_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory_CreateSwapChain *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
    if (args->pDesc)
        npt_replace_DXGI_SWAP_CHAIN_DESC_handle(ctx, (DXGI_SWAP_CHAIN_DESC *)args->pDesc);
}

static inline void
npt_encode_IDXGIFactory_CreateSwapChain_reply(struct npt_cs_encoder *enc,
                                              const struct npt_command_IDXGIFactory_CreateSwapChain *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_CreateSwapChain),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSwapChain (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory_CreateSwapChain(struct npt_dispatch_context *ctx,
                                          uint32_t cmd_flags,
                                          uint64_t object_id)
{
    struct npt_command_IDXGIFactory_CreateSwapChain args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory_CreateSwapChain_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_CreateSwapChain on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory_CreateSwapChain_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_CreateSwapChain: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory_CreateSwapChain _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory_CreateSwapChain, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory_CreateSwapChain);

    if (ctx->idxgifactory_dispatch_overrides && ctx->idxgifactory_dispatch_overrides->CreateSwapChain) {
        args.ret = ctx->idxgifactory_dispatch_overrides->CreateSwapChain(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDevice, args.pDesc, args.ppSwapChain);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSwapChain && *args.ppSwapChain)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSwapChain, *args.ppSwapChain, NPT_OBJECT_TYPE_IDXGISWAPCHAIN);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory_CreateSwapChain_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory::CreateSoftwareAdapter
 */

struct npt_command_IDXGIFactory_CreateSoftwareAdapter {
    void *_self;
    HMODULE Module;
    IDXGIAdapter ** ppAdapter;
    /* Shadow: the guest-allocated id for ppAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAdapter -> *ppAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppAdapter;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory_CreateSoftwareAdapter_args_temp(struct npt_cs_decoder *dec,
                                                        struct npt_command_IDXGIFactory_CreateSoftwareAdapter *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->Module = (HMODULE)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppAdapter = _gid;
        args->ppAdapter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppAdapter) return;
        *args->ppAdapter = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIFactory_CreateSoftwareAdapter_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory_CreateSoftwareAdapter *args)
{
    args->Module = (HMODULE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->Module);
}

static inline void
npt_encode_IDXGIFactory_CreateSoftwareAdapter_reply(struct npt_cs_encoder *enc,
                                                    const struct npt_command_IDXGIFactory_CreateSoftwareAdapter *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory,
                                 NPT_METHOD_IDXGIFactory_CreateSoftwareAdapter),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppAdapter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory_CreateSoftwareAdapter(struct npt_dispatch_context *ctx,
                                                uint32_t cmd_flags,
                                                uint64_t object_id)
{
    struct npt_command_IDXGIFactory_CreateSoftwareAdapter args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory_CreateSoftwareAdapter_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_CreateSoftwareAdapter on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory_CreateSoftwareAdapter_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory_CreateSoftwareAdapter: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory_CreateSoftwareAdapter _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory_CreateSoftwareAdapter, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory_CreateSoftwareAdapter);

    if (ctx->idxgifactory_dispatch_overrides && ctx->idxgifactory_dispatch_overrides->CreateSoftwareAdapter) {
        args.ret = ctx->idxgifactory_dispatch_overrides->CreateSoftwareAdapter(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Module, args.ppAdapter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppAdapter && *args.ppAdapter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppAdapter, *args.ppAdapter, NPT_OBJECT_TYPE_IDXGIADAPTER);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory_CreateSoftwareAdapter_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory1::EnumAdapters1
 */

struct npt_command_IDXGIFactory1_EnumAdapters1 {
    void *_self;
    UINT Adapter;
    IDXGIAdapter1 ** ppAdapter;
    /* Shadow: the guest-allocated id for ppAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppAdapter -> *ppAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppAdapter;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory1_EnumAdapters1_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_IDXGIFactory1_EnumAdapters1 *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Adapter);
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppAdapter = _gid;
        args->ppAdapter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppAdapter) return;
        *args->ppAdapter = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIFactory1_EnumAdapters1_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory1_EnumAdapters1 *args)
{
}

static inline void
npt_encode_IDXGIFactory1_EnumAdapters1_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_IDXGIFactory1_EnumAdapters1 *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                 NPT_METHOD_IDXGIFactory1_EnumAdapters1),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppAdapter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory1_EnumAdapters1(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_IDXGIFactory1_EnumAdapters1 args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory1_EnumAdapters1_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory1_EnumAdapters1 on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory1_EnumAdapters1_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory1_EnumAdapters1: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory1_EnumAdapters1 _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory1_EnumAdapters1, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory1_EnumAdapters1);

    if (ctx->idxgifactory1_dispatch_overrides && ctx->idxgifactory1_dispatch_overrides->EnumAdapters1) {
        args.ret = ctx->idxgifactory1_dispatch_overrides->EnumAdapters1(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Adapter, args.ppAdapter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppAdapter && *args.ppAdapter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppAdapter, *args.ppAdapter, NPT_OBJECT_TYPE_IDXGIADAPTER1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory1_EnumAdapters1_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory1::IsCurrent
 */

struct npt_command_IDXGIFactory1_IsCurrent {
    void *_self;
    BOOL ret;
};

static inline void
npt_decode_IDXGIFactory1_IsCurrent_args_temp(struct npt_cs_decoder *dec,
                                             struct npt_command_IDXGIFactory1_IsCurrent *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIFactory1_IsCurrent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory1_IsCurrent *args)
{
}

static inline void
npt_encode_IDXGIFactory1_IsCurrent_reply(struct npt_cs_encoder *enc,
                                         const struct npt_command_IDXGIFactory1_IsCurrent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory1,
                                 NPT_METHOD_IDXGIFactory1_IsCurrent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIFactory1_IsCurrent(struct npt_dispatch_context *ctx,
                                     uint32_t cmd_flags,
                                     uint64_t object_id)
{
    struct npt_command_IDXGIFactory1_IsCurrent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory1_IsCurrent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY1);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory1_IsCurrent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory1_IsCurrent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory1_IsCurrent: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory1_IsCurrent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory1_IsCurrent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory1_IsCurrent);

    if (ctx->idxgifactory1_dispatch_overrides && ctx->idxgifactory1_dispatch_overrides->IsCurrent) {
        args.ret = ctx->idxgifactory1_dispatch_overrides->IsCurrent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory1_IsCurrent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::IsWindowedStereoEnabled
 */

struct npt_command_IDXGIFactory2_IsWindowedStereoEnabled {
    void *_self;
    BOOL ret;
};

static inline void
npt_decode_IDXGIFactory2_IsWindowedStereoEnabled_args_temp(struct npt_cs_decoder *dec,
                                                           struct npt_command_IDXGIFactory2_IsWindowedStereoEnabled *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIFactory2_IsWindowedStereoEnabled_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_IsWindowedStereoEnabled *args)
{
}

static inline void
npt_encode_IDXGIFactory2_IsWindowedStereoEnabled_reply(struct npt_cs_encoder *enc,
                                                       const struct npt_command_IDXGIFactory2_IsWindowedStereoEnabled *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_IsWindowedStereoEnabled),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIFactory2_IsWindowedStereoEnabled(struct npt_dispatch_context *ctx,
                                                   uint32_t cmd_flags,
                                                   uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_IsWindowedStereoEnabled args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_IsWindowedStereoEnabled_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_IsWindowedStereoEnabled on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_IsWindowedStereoEnabled_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_IsWindowedStereoEnabled: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_IsWindowedStereoEnabled _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_IsWindowedStereoEnabled, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_IsWindowedStereoEnabled);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->IsWindowedStereoEnabled) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->IsWindowedStereoEnabled(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_IsWindowedStereoEnabled_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::CreateSwapChainForHwnd
 */

struct npt_command_IDXGIFactory2_CreateSwapChainForHwnd {
    void *_self;
    IUnknown * pDevice;
    HWND hWnd;
    const DXGI_SWAP_CHAIN_DESC1 * pDesc;
    const DXGI_SWAP_CHAIN_FULLSCREEN_DESC * pFullscreenDesc;
    IDXGIOutput * pRestrictToOutput;
    IDXGISwapChain1 ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_CreateSwapChainForHwnd_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_IDXGIFactory2_CreateSwapChainForHwnd *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hWnd = (HWND)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_DESC1));
        if (!args->pDesc) return;
        npt_decode_DXGI_SWAP_CHAIN_DESC1(dec, (DXGI_SWAP_CHAIN_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pFullscreenDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_FULLSCREEN_DESC));
        if (!args->pFullscreenDesc) return;
        npt_decode_DXGI_SWAP_CHAIN_FULLSCREEN_DESC(dec, (DXGI_SWAP_CHAIN_FULLSCREEN_DESC *)args->pFullscreenDesc);
    } else {
        args->pFullscreenDesc = NULL;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRestrictToOutput = (IDXGIOutput *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSwapChain = _gid;
        args->ppSwapChain = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSwapChain) return;
        *args->ppSwapChain = NULL;
    }
    /* Allocate temp storage for output-only parameters */






}

static inline void
npt_replace_IDXGIFactory2_CreateSwapChainForHwnd_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_CreateSwapChainForHwnd *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
    args->hWnd = (HWND)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->hWnd);
    args->pRestrictToOutput = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRestrictToOutput, NPT_OBJECT_TYPE_IDXGIOUTPUT);
}

static inline void
npt_encode_IDXGIFactory2_CreateSwapChainForHwnd_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_IDXGIFactory2_CreateSwapChainForHwnd *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_CreateSwapChainForHwnd),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSwapChain (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory2_CreateSwapChainForHwnd(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_CreateSwapChainForHwnd args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_CreateSwapChainForHwnd_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_CreateSwapChainForHwnd on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_CreateSwapChainForHwnd_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_CreateSwapChainForHwnd: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_CreateSwapChainForHwnd _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_CreateSwapChainForHwnd, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_CreateSwapChainForHwnd);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->CreateSwapChainForHwnd) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->CreateSwapChainForHwnd(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDevice, args.hWnd, args.pDesc, args.pFullscreenDesc, args.pRestrictToOutput, args.ppSwapChain);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSwapChain && *args.ppSwapChain)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSwapChain, *args.ppSwapChain, NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_CreateSwapChainForHwnd_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::CreateSwapChainForCoreWindow
 */

struct npt_command_IDXGIFactory2_CreateSwapChainForCoreWindow {
    void *_self;
    IUnknown * pDevice;
    IUnknown * pWindow;
    const DXGI_SWAP_CHAIN_DESC1 * pDesc;
    IDXGIOutput * pRestrictToOutput;
    IDXGISwapChain1 ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_CreateSwapChainForCoreWindow_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_IDXGIFactory2_CreateSwapChainForCoreWindow *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pWindow = (IUnknown *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_DESC1));
        if (!args->pDesc) return;
        npt_decode_DXGI_SWAP_CHAIN_DESC1(dec, (DXGI_SWAP_CHAIN_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRestrictToOutput = (IDXGIOutput *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSwapChain = _gid;
        args->ppSwapChain = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSwapChain) return;
        *args->ppSwapChain = NULL;
    }
    /* Allocate temp storage for output-only parameters */





}

static inline void
npt_replace_IDXGIFactory2_CreateSwapChainForCoreWindow_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_CreateSwapChainForCoreWindow *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
    args->pWindow = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pWindow, NPT_OBJECT_TYPE_IUNKNOWN);
    args->pRestrictToOutput = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRestrictToOutput, NPT_OBJECT_TYPE_IDXGIOUTPUT);
}

static inline void
npt_encode_IDXGIFactory2_CreateSwapChainForCoreWindow_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_IDXGIFactory2_CreateSwapChainForCoreWindow *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_CreateSwapChainForCoreWindow),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSwapChain (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory2_CreateSwapChainForCoreWindow(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_CreateSwapChainForCoreWindow args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_CreateSwapChainForCoreWindow_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_CreateSwapChainForCoreWindow on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_CreateSwapChainForCoreWindow_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_CreateSwapChainForCoreWindow: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_CreateSwapChainForCoreWindow _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_CreateSwapChainForCoreWindow, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_CreateSwapChainForCoreWindow);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->CreateSwapChainForCoreWindow) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->CreateSwapChainForCoreWindow(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDevice, args.pWindow, args.pDesc, args.pRestrictToOutput, args.ppSwapChain);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSwapChain && *args.ppSwapChain)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSwapChain, *args.ppSwapChain, NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_CreateSwapChainForCoreWindow_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::GetSharedResourceAdapterLuid
 */

struct npt_command_IDXGIFactory2_GetSharedResourceAdapterLuid {
    void *_self;
    HANDLE hResource;
    LUID * pLuid;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_GetSharedResourceAdapterLuid_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_IDXGIFactory2_GetSharedResourceAdapterLuid *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hResource = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pLuid = npt_cs_decoder_alloc_temp(dec, sizeof(LUID));
    if (!args->pLuid) return;
    memset(args->pLuid, 0, sizeof(LUID));
}

static inline void
npt_replace_IDXGIFactory2_GetSharedResourceAdapterLuid_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_GetSharedResourceAdapterLuid *args)
{
    args->hResource = (HANDLE)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->hResource);
}

static inline void
npt_encode_IDXGIFactory2_GetSharedResourceAdapterLuid_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_IDXGIFactory2_GetSharedResourceAdapterLuid *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_GetSharedResourceAdapterLuid),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pLuid))
        npt_encode_LUID(enc, args->pLuid);
}

static inline void
npt_dispatch_IDXGIFactory2_GetSharedResourceAdapterLuid(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_GetSharedResourceAdapterLuid args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_GetSharedResourceAdapterLuid_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_GetSharedResourceAdapterLuid on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_GetSharedResourceAdapterLuid_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_GetSharedResourceAdapterLuid: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_GetSharedResourceAdapterLuid _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_GetSharedResourceAdapterLuid, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_GetSharedResourceAdapterLuid);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->GetSharedResourceAdapterLuid) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->GetSharedResourceAdapterLuid(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hResource, args.pLuid);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_GetSharedResourceAdapterLuid_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::RegisterStereoStatusWindow
 */

struct npt_command_IDXGIFactory2_RegisterStereoStatusWindow {
    void *_self;
    HWND WindowHandle;
    UINT wMsg;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_RegisterStereoStatusWindow_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_IDXGIFactory2_RegisterStereoStatusWindow *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->WindowHandle = (HWND)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    npt_decode_UINT(dec, &args->wMsg);

    /* Allocate temp storage for output-only parameters */


    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_IDXGIFactory2_RegisterStereoStatusWindow_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_RegisterStereoStatusWindow *args)
{
    args->WindowHandle = (HWND)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->WindowHandle);
}

static inline void
npt_encode_IDXGIFactory2_RegisterStereoStatusWindow_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_IDXGIFactory2_RegisterStereoStatusWindow *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterStereoStatusWindow),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_IDXGIFactory2_RegisterStereoStatusWindow(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_RegisterStereoStatusWindow args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_RegisterStereoStatusWindow_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterStereoStatusWindow on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_RegisterStereoStatusWindow_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterStereoStatusWindow: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_RegisterStereoStatusWindow _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_RegisterStereoStatusWindow, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_RegisterStereoStatusWindow);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->RegisterStereoStatusWindow) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->RegisterStereoStatusWindow(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.WindowHandle, args.wMsg, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_RegisterStereoStatusWindow_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::RegisterStereoStatusEvent
 */

struct npt_command_IDXGIFactory2_RegisterStereoStatusEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_RegisterStereoStatusEvent_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_IDXGIFactory2_RegisterStereoStatusEvent *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_IDXGIFactory2_RegisterStereoStatusEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_RegisterStereoStatusEvent *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_IDXGIFactory2_RegisterStereoStatusEvent_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_IDXGIFactory2_RegisterStereoStatusEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterStereoStatusEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_IDXGIFactory2_RegisterStereoStatusEvent(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_RegisterStereoStatusEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_RegisterStereoStatusEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterStereoStatusEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_RegisterStereoStatusEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterStereoStatusEvent: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_RegisterStereoStatusEvent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_RegisterStereoStatusEvent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_RegisterStereoStatusEvent);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->RegisterStereoStatusEvent) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->RegisterStereoStatusEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hEvent, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_RegisterStereoStatusEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::UnregisterStereoStatus
 */

struct npt_command_IDXGIFactory2_UnregisterStereoStatus {
    void *_self;
    DWORD dwCookie;
};

static inline void
npt_decode_IDXGIFactory2_UnregisterStereoStatus_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_IDXGIFactory2_UnregisterStereoStatus *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DWORD(dec, &args->dwCookie);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIFactory2_UnregisterStereoStatus_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_UnregisterStereoStatus *args)
{
}

static inline void
npt_encode_IDXGIFactory2_UnregisterStereoStatus_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_IDXGIFactory2_UnregisterStereoStatus *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_UnregisterStereoStatus),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIFactory2_UnregisterStereoStatus(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_UnregisterStereoStatus args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_UnregisterStereoStatus_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_UnregisterStereoStatus on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_UnregisterStereoStatus_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_UnregisterStereoStatus: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_UnregisterStereoStatus _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_UnregisterStereoStatus, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_UnregisterStereoStatus);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->UnregisterStereoStatus) {
        ctx->idxgifactory2_dispatch_overrides->UnregisterStereoStatus(ctx, &args, _original);
    } else {
        _original(args._self, args.dwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_UnregisterStereoStatus_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::RegisterOcclusionStatusWindow
 */

struct npt_command_IDXGIFactory2_RegisterOcclusionStatusWindow {
    void *_self;
    HWND WindowHandle;
    UINT wMsg;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_RegisterOcclusionStatusWindow_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_IDXGIFactory2_RegisterOcclusionStatusWindow *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->WindowHandle = (HWND)(uintptr_t)npt_win32_handle_from_id(_id);
    }
    npt_decode_UINT(dec, &args->wMsg);

    /* Allocate temp storage for output-only parameters */


    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_IDXGIFactory2_RegisterOcclusionStatusWindow_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_RegisterOcclusionStatusWindow *args)
{
    args->WindowHandle = (HWND)(uintptr_t)npt_win32_handle_replace(ctx, (npt_object_id)args->WindowHandle);
}

static inline void
npt_encode_IDXGIFactory2_RegisterOcclusionStatusWindow_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_IDXGIFactory2_RegisterOcclusionStatusWindow *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusWindow),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_IDXGIFactory2_RegisterOcclusionStatusWindow(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_RegisterOcclusionStatusWindow args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_RegisterOcclusionStatusWindow_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterOcclusionStatusWindow on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_RegisterOcclusionStatusWindow_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterOcclusionStatusWindow: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_RegisterOcclusionStatusWindow _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_RegisterOcclusionStatusWindow, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_RegisterOcclusionStatusWindow);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->RegisterOcclusionStatusWindow) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->RegisterOcclusionStatusWindow(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.WindowHandle, args.wMsg, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_RegisterOcclusionStatusWindow_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::RegisterOcclusionStatusEvent
 */

struct npt_command_IDXGIFactory2_RegisterOcclusionStatusEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_RegisterOcclusionStatusEvent_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_IDXGIFactory2_RegisterOcclusionStatusEvent *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_IDXGIFactory2_RegisterOcclusionStatusEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_RegisterOcclusionStatusEvent *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_IDXGIFactory2_RegisterOcclusionStatusEvent_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_IDXGIFactory2_RegisterOcclusionStatusEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_RegisterOcclusionStatusEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_IDXGIFactory2_RegisterOcclusionStatusEvent(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_RegisterOcclusionStatusEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_RegisterOcclusionStatusEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterOcclusionStatusEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_RegisterOcclusionStatusEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_RegisterOcclusionStatusEvent: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_RegisterOcclusionStatusEvent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_RegisterOcclusionStatusEvent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_RegisterOcclusionStatusEvent);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->RegisterOcclusionStatusEvent) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->RegisterOcclusionStatusEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hEvent, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_RegisterOcclusionStatusEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::UnregisterOcclusionStatus
 */

struct npt_command_IDXGIFactory2_UnregisterOcclusionStatus {
    void *_self;
    DWORD dwCookie;
};

static inline void
npt_decode_IDXGIFactory2_UnregisterOcclusionStatus_args_temp(struct npt_cs_decoder *dec,
                                                             struct npt_command_IDXGIFactory2_UnregisterOcclusionStatus *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DWORD(dec, &args->dwCookie);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIFactory2_UnregisterOcclusionStatus_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_UnregisterOcclusionStatus *args)
{
}

static inline void
npt_encode_IDXGIFactory2_UnregisterOcclusionStatus_reply(struct npt_cs_encoder *enc,
                                                         const struct npt_command_IDXGIFactory2_UnregisterOcclusionStatus *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_UnregisterOcclusionStatus),
        .cmd_return = 0,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIFactory2_UnregisterOcclusionStatus(struct npt_dispatch_context *ctx,
                                                     uint32_t cmd_flags,
                                                     uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_UnregisterOcclusionStatus args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_UnregisterOcclusionStatus_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_UnregisterOcclusionStatus on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_UnregisterOcclusionStatus_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_UnregisterOcclusionStatus: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_UnregisterOcclusionStatus _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_UnregisterOcclusionStatus, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_UnregisterOcclusionStatus);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->UnregisterOcclusionStatus) {
        ctx->idxgifactory2_dispatch_overrides->UnregisterOcclusionStatus(ctx, &args, _original);
    } else {
        _original(args._self, args.dwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_UnregisterOcclusionStatus_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory2::CreateSwapChainForComposition
 */

struct npt_command_IDXGIFactory2_CreateSwapChainForComposition {
    void *_self;
    IUnknown * pDevice;
    const DXGI_SWAP_CHAIN_DESC1 * pDesc;
    IDXGIOutput * pRestrictToOutput;
    IDXGISwapChain1 ** ppSwapChain;
    /* Shadow: the guest-allocated id for ppSwapChain arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppSwapChain -> *ppSwapChain} in the
     * context object table. */
    uint64_t _guest_id_ppSwapChain;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory2_CreateSwapChainForComposition_args_temp(struct npt_cs_decoder *dec,
                                                                 struct npt_command_IDXGIFactory2_CreateSwapChainForComposition *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pDevice = (IUnknown *)npt_object_from_id(_id);
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDesc = npt_cs_decoder_alloc_temp(dec, sizeof(DXGI_SWAP_CHAIN_DESC1));
        if (!args->pDesc) return;
        npt_decode_DXGI_SWAP_CHAIN_DESC1(dec, (DXGI_SWAP_CHAIN_DESC1 *)args->pDesc);
    } else {
        args->pDesc = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pRestrictToOutput = (IDXGIOutput *)npt_object_from_id(_id);
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppSwapChain = _gid;
        args->ppSwapChain = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppSwapChain) return;
        *args->ppSwapChain = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_IDXGIFactory2_CreateSwapChainForComposition_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory2_CreateSwapChainForComposition *args)
{
    args->pDevice = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pDevice, NPT_OBJECT_TYPE_IUNKNOWN);
    args->pRestrictToOutput = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pRestrictToOutput, NPT_OBJECT_TYPE_IDXGIOUTPUT);
}

static inline void
npt_encode_IDXGIFactory2_CreateSwapChainForComposition_reply(struct npt_cs_encoder *enc,
                                                             const struct npt_command_IDXGIFactory2_CreateSwapChainForComposition *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory2,
                                 NPT_METHOD_IDXGIFactory2_CreateSwapChainForComposition),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppSwapChain (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory2_CreateSwapChainForComposition(struct npt_dispatch_context *ctx,
                                                         uint32_t cmd_flags,
                                                         uint64_t object_id)
{
    struct npt_command_IDXGIFactory2_CreateSwapChainForComposition args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory2_CreateSwapChainForComposition_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY2);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_CreateSwapChainForComposition on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory2_CreateSwapChainForComposition_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory2_CreateSwapChainForComposition: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory2_CreateSwapChainForComposition _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory2_CreateSwapChainForComposition, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory2_CreateSwapChainForComposition);

    if (ctx->idxgifactory2_dispatch_overrides && ctx->idxgifactory2_dispatch_overrides->CreateSwapChainForComposition) {
        args.ret = ctx->idxgifactory2_dispatch_overrides->CreateSwapChainForComposition(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.pDevice, args.pDesc, args.pRestrictToOutput, args.ppSwapChain);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppSwapChain && *args.ppSwapChain)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppSwapChain, *args.ppSwapChain, NPT_OBJECT_TYPE_IDXGISWAPCHAIN1);

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory2_CreateSwapChainForComposition_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory3::GetCreationFlags
 */

struct npt_command_IDXGIFactory3_GetCreationFlags {
    void *_self;
    UINT ret;
};

static inline void
npt_decode_IDXGIFactory3_GetCreationFlags_args_temp(struct npt_cs_decoder *dec,
                                                    struct npt_command_IDXGIFactory3_GetCreationFlags *args)
{
    /* Decode input parameters from the wire */
    /* Allocate temp storage for output-only parameters */
}

static inline void
npt_replace_IDXGIFactory3_GetCreationFlags_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory3_GetCreationFlags *args)
{
}

static inline void
npt_encode_IDXGIFactory3_GetCreationFlags_reply(struct npt_cs_encoder *enc,
                                                const struct npt_command_IDXGIFactory3_GetCreationFlags *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory3,
                                 NPT_METHOD_IDXGIFactory3_GetCreationFlags),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIFactory3_GetCreationFlags(struct npt_dispatch_context *ctx,
                                            uint32_t cmd_flags,
                                            uint64_t object_id)
{
    struct npt_command_IDXGIFactory3_GetCreationFlags args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory3_GetCreationFlags_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY3);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory3_GetCreationFlags on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory3_GetCreationFlags_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory3_GetCreationFlags: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory3_GetCreationFlags _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory3_GetCreationFlags, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory3_GetCreationFlags);

    if (ctx->idxgifactory3_dispatch_overrides && ctx->idxgifactory3_dispatch_overrides->GetCreationFlags) {
        args.ret = ctx->idxgifactory3_dispatch_overrides->GetCreationFlags(ctx, &args, _original);
    } else {
        args.ret = _original(args._self);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory3_GetCreationFlags_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory4::EnumAdapterByLuid
 */

struct npt_command_IDXGIFactory4_EnumAdapterByLuid {
    void *_self;
    LUID AdapterLuid;
    const IID * riid;
    void ** ppvAdapter;
    /* Shadow: the guest-allocated id for ppvAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvAdapter -> *ppvAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppvAdapter;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory4_EnumAdapterByLuid_args_temp(struct npt_cs_decoder *dec,
                                                     struct npt_command_IDXGIFactory4_EnumAdapterByLuid *args)
{
    /* Decode input parameters from the wire */
    npt_decode_LUID(dec, &args->AdapterLuid);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvAdapter = _gid;
        args->ppvAdapter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvAdapter) return;
        *args->ppvAdapter = NULL;
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGIFactory4_EnumAdapterByLuid_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory4_EnumAdapterByLuid *args)
{
}

static inline void
npt_encode_IDXGIFactory4_EnumAdapterByLuid_reply(struct npt_cs_encoder *enc,
                                                 const struct npt_command_IDXGIFactory4_EnumAdapterByLuid *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                 NPT_METHOD_IDXGIFactory4_EnumAdapterByLuid),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvAdapter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory4_EnumAdapterByLuid(struct npt_dispatch_context *ctx,
                                             uint32_t cmd_flags,
                                             uint64_t object_id)
{
    struct npt_command_IDXGIFactory4_EnumAdapterByLuid args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory4_EnumAdapterByLuid_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory4_EnumAdapterByLuid on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory4_EnumAdapterByLuid_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory4_EnumAdapterByLuid: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory4_EnumAdapterByLuid _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory4_EnumAdapterByLuid, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory4_EnumAdapterByLuid);

    if (ctx->idxgifactory4_dispatch_overrides && ctx->idxgifactory4_dispatch_overrides->EnumAdapterByLuid) {
        args.ret = ctx->idxgifactory4_dispatch_overrides->EnumAdapterByLuid(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.AdapterLuid, args.riid, args.ppvAdapter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvAdapter && *args.ppvAdapter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvAdapter, *args.ppvAdapter,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory4_EnumAdapterByLuid_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory4::EnumWarpAdapter
 */

struct npt_command_IDXGIFactory4_EnumWarpAdapter {
    void *_self;
    const IID * riid;
    void ** ppvAdapter;
    /* Shadow: the guest-allocated id for ppvAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvAdapter -> *ppvAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppvAdapter;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory4_EnumWarpAdapter_args_temp(struct npt_cs_decoder *dec,
                                                   struct npt_command_IDXGIFactory4_EnumWarpAdapter *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvAdapter = _gid;
        args->ppvAdapter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvAdapter) return;
        *args->ppvAdapter = NULL;
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_IDXGIFactory4_EnumWarpAdapter_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory4_EnumWarpAdapter *args)
{
}

static inline void
npt_encode_IDXGIFactory4_EnumWarpAdapter_reply(struct npt_cs_encoder *enc,
                                               const struct npt_command_IDXGIFactory4_EnumWarpAdapter *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory4,
                                 NPT_METHOD_IDXGIFactory4_EnumWarpAdapter),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvAdapter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory4_EnumWarpAdapter(struct npt_dispatch_context *ctx,
                                           uint32_t cmd_flags,
                                           uint64_t object_id)
{
    struct npt_command_IDXGIFactory4_EnumWarpAdapter args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory4_EnumWarpAdapter_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY4);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory4_EnumWarpAdapter on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory4_EnumWarpAdapter_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory4_EnumWarpAdapter: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory4_EnumWarpAdapter _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory4_EnumWarpAdapter, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory4_EnumWarpAdapter);

    if (ctx->idxgifactory4_dispatch_overrides && ctx->idxgifactory4_dispatch_overrides->EnumWarpAdapter) {
        args.ret = ctx->idxgifactory4_dispatch_overrides->EnumWarpAdapter(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.riid, args.ppvAdapter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvAdapter && *args.ppvAdapter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvAdapter, *args.ppvAdapter,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory4_EnumWarpAdapter_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory5::CheckFeatureSupport
 */

struct npt_command_IDXGIFactory5_CheckFeatureSupport {
    void *_self;
    DXGI_FEATURE Feature;
    void * pFeatureSupportData;
    UINT FeatureSupportDataSize;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory5_CheckFeatureSupport_args_temp(struct npt_cs_decoder *dec,
                                                       struct npt_command_IDXGIFactory5_CheckFeatureSupport *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DXGI_FEATURE(dec, &args->Feature);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pFeatureSupportData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pFeatureSupportData) return;
            npt_decode_blob_array(dec, (void *)args->pFeatureSupportData, _blob_size);
        } else {
            args->pFeatureSupportData = NULL;
        }
    }
    npt_decode_UINT(dec, &args->FeatureSupportDataSize);
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_IDXGIFactory5_CheckFeatureSupport_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory5_CheckFeatureSupport *args)
{
}

static inline void
npt_encode_IDXGIFactory5_CheckFeatureSupport_reply(struct npt_cs_encoder *enc,
                                                   const struct npt_command_IDXGIFactory5_CheckFeatureSupport *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory5,
                                 NPT_METHOD_IDXGIFactory5_CheckFeatureSupport),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (args->pFeatureSupportData) {
        npt_encode_array_count(enc, args->FeatureSupportDataSize);
        npt_encode_blob_array(enc, args->pFeatureSupportData, args->FeatureSupportDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_IDXGIFactory5_CheckFeatureSupport(struct npt_dispatch_context *ctx,
                                               uint32_t cmd_flags,
                                               uint64_t object_id)
{
    struct npt_command_IDXGIFactory5_CheckFeatureSupport args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory5_CheckFeatureSupport_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY5);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory5_CheckFeatureSupport on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory5_CheckFeatureSupport_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory5_CheckFeatureSupport: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory5_CheckFeatureSupport _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory5_CheckFeatureSupport, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory5_CheckFeatureSupport);

    if (ctx->idxgifactory5_dispatch_overrides && ctx->idxgifactory5_dispatch_overrides->CheckFeatureSupport) {
        args.ret = ctx->idxgifactory5_dispatch_overrides->CheckFeatureSupport(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Feature, args.pFeatureSupportData, args.FeatureSupportDataSize);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory5_CheckFeatureSupport_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory6::EnumAdapterByGpuPreference
 */

struct npt_command_IDXGIFactory6_EnumAdapterByGpuPreference {
    void *_self;
    UINT Adapter;
    DXGI_GPU_PREFERENCE GpuPreference;
    const IID * riid;
    void ** ppvAdapter;
    /* Shadow: the guest-allocated id for ppvAdapter arrives in the command
     * body (no reply round-trip).  The post-dispatch register pass reads
     * this field and inserts {_guest_id_ppvAdapter -> *ppvAdapter} in the
     * context object table. */
    uint64_t _guest_id_ppvAdapter;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory6_EnumAdapterByGpuPreference_args_temp(struct npt_cs_decoder *dec,
                                                              struct npt_command_IDXGIFactory6_EnumAdapterByGpuPreference *args)
{
    /* Decode input parameters from the wire */
    npt_decode_UINT(dec, &args->Adapter);
    npt_decode_DXGI_GPU_PREFERENCE(dec, &args->GpuPreference);
    if (npt_decode_simple_pointer(dec)) {
        args->riid = npt_cs_decoder_alloc_temp(dec, sizeof(IID));
        if (!args->riid) return;
        npt_decode_IID(dec, (IID *)args->riid);
    } else {
        args->riid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        uint64_t _gid;
        npt_decode_uint64_t(dec, &_gid);
        args->_guest_id_ppvAdapter = _gid;
        args->ppvAdapter = npt_cs_decoder_alloc_temp(dec, sizeof(void *));
        if (!args->ppvAdapter) return;
        *args->ppvAdapter = NULL;
    }
    /* Allocate temp storage for output-only parameters */




}

static inline void
npt_replace_IDXGIFactory6_EnumAdapterByGpuPreference_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory6_EnumAdapterByGpuPreference *args)
{
}

static inline void
npt_encode_IDXGIFactory6_EnumAdapterByGpuPreference_reply(struct npt_cs_encoder *enc,
                                                          const struct npt_command_IDXGIFactory6_EnumAdapterByGpuPreference *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory6,
                                 NPT_METHOD_IDXGIFactory6_EnumAdapterByGpuPreference),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
/* skip args->ppvAdapter (guest-allocated id, registered host-side; not in reply) */
}

static inline void
npt_dispatch_IDXGIFactory6_EnumAdapterByGpuPreference(struct npt_dispatch_context *ctx,
                                                      uint32_t cmd_flags,
                                                      uint64_t object_id)
{
    struct npt_command_IDXGIFactory6_EnumAdapterByGpuPreference args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory6_EnumAdapterByGpuPreference_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY6);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory6_EnumAdapterByGpuPreference on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory6_EnumAdapterByGpuPreference_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory6_EnumAdapterByGpuPreference: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory6_EnumAdapterByGpuPreference _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory6_EnumAdapterByGpuPreference, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory6_EnumAdapterByGpuPreference);

    if (ctx->idxgifactory6_dispatch_overrides && ctx->idxgifactory6_dispatch_overrides->EnumAdapterByGpuPreference) {
        args.ret = ctx->idxgifactory6_dispatch_overrides->EnumAdapterByGpuPreference(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Adapter, args.GpuPreference, args.riid, args.ppvAdapter);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */
    if (args.ppvAdapter && *args.ppvAdapter)
        npt_cs_handle_register_guest_id(ctx, args._guest_id_ppvAdapter, *args.ppvAdapter,
            npt_object_type_from_iid(args.riid));

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory6_EnumAdapterByGpuPreference_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory7::RegisterAdaptersChangedEvent
 */

struct npt_command_IDXGIFactory7_RegisterAdaptersChangedEvent {
    void *_self;
    HANDLE hEvent;
    DWORD * pdwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory7_RegisterAdaptersChangedEvent_args_temp(struct npt_cs_decoder *dec,
                                                                struct npt_command_IDXGIFactory7_RegisterAdaptersChangedEvent *args)
{
    /* Decode input parameters from the wire */
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->hEvent = (HANDLE)(uintptr_t)npt_win32_handle_from_id(_id);
    }

    /* Allocate temp storage for output-only parameters */

    args->pdwCookie = npt_cs_decoder_alloc_temp(dec, sizeof(DWORD));
    if (!args->pdwCookie) return;
    memset(args->pdwCookie, 0, sizeof(DWORD));
}

static inline void
npt_replace_IDXGIFactory7_RegisterAdaptersChangedEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory7_RegisterAdaptersChangedEvent *args)
{
    args->hEvent = (HANDLE)(uintptr_t)npt_event_handle_replace(ctx, (npt_object_id)args->hEvent);
}

static inline void
npt_encode_IDXGIFactory7_RegisterAdaptersChangedEvent_reply(struct npt_cs_encoder *enc,
                                                            const struct npt_command_IDXGIFactory7_RegisterAdaptersChangedEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                 NPT_METHOD_IDXGIFactory7_RegisterAdaptersChangedEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pdwCookie))
        npt_encode_DWORD(enc, args->pdwCookie);
}

static inline void
npt_dispatch_IDXGIFactory7_RegisterAdaptersChangedEvent(struct npt_dispatch_context *ctx,
                                                        uint32_t cmd_flags,
                                                        uint64_t object_id)
{
    struct npt_command_IDXGIFactory7_RegisterAdaptersChangedEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory7_RegisterAdaptersChangedEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY7);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory7_RegisterAdaptersChangedEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory7_RegisterAdaptersChangedEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory7_RegisterAdaptersChangedEvent: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory7_RegisterAdaptersChangedEvent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory7_RegisterAdaptersChangedEvent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory7_RegisterAdaptersChangedEvent);

    if (ctx->idxgifactory7_dispatch_overrides && ctx->idxgifactory7_dispatch_overrides->RegisterAdaptersChangedEvent) {
        args.ret = ctx->idxgifactory7_dispatch_overrides->RegisterAdaptersChangedEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.hEvent, args.pdwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory7_RegisterAdaptersChangedEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * IDXGIFactory7::UnregisterAdaptersChangedEvent
 */

struct npt_command_IDXGIFactory7_UnregisterAdaptersChangedEvent {
    void *_self;
    DWORD dwCookie;
    HRESULT ret;
};

static inline void
npt_decode_IDXGIFactory7_UnregisterAdaptersChangedEvent_args_temp(struct npt_cs_decoder *dec,
                                                                  struct npt_command_IDXGIFactory7_UnregisterAdaptersChangedEvent *args)
{
    /* Decode input parameters from the wire */
    npt_decode_DWORD(dec, &args->dwCookie);
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_IDXGIFactory7_UnregisterAdaptersChangedEvent_args_handle(struct npt_dispatch_context *ctx, struct npt_command_IDXGIFactory7_UnregisterAdaptersChangedEvent *args)
{
}

static inline void
npt_encode_IDXGIFactory7_UnregisterAdaptersChangedEvent_reply(struct npt_cs_encoder *enc,
                                                              const struct npt_command_IDXGIFactory7_UnregisterAdaptersChangedEvent *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_IDXGIFactory7,
                                 NPT_METHOD_IDXGIFactory7_UnregisterAdaptersChangedEvent),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_IDXGIFactory7_UnregisterAdaptersChangedEvent(struct npt_dispatch_context *ctx,
                                                          uint32_t cmd_flags,
                                                          uint64_t object_id)
{
    struct npt_command_IDXGIFactory7_UnregisterAdaptersChangedEvent args;
    memset(&args, 0, sizeof(args));

    npt_decode_IDXGIFactory7_UnregisterAdaptersChangedEvent_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_IDXGIFACTORY7);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory7_UnregisterAdaptersChangedEvent on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_IDXGIFactory7_UnregisterAdaptersChangedEvent_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping IDXGIFactory7_UnregisterAdaptersChangedEvent: an argument handle is unknown "
                    "to this context");
            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_IDXGIFactory7_UnregisterAdaptersChangedEvent _original = NPT_COM_VTBL_FUNC(
        PFN_IDXGIFactory7_UnregisterAdaptersChangedEvent, npt_com_vtable(args._self),
        NPT_VTBL_IDXGIFactory7_UnregisterAdaptersChangedEvent);

    if (ctx->idxgifactory7_dispatch_overrides && ctx->idxgifactory7_dispatch_overrides->UnregisterAdaptersChangedEvent) {
        args.ret = ctx->idxgifactory7_dispatch_overrides->UnregisterAdaptersChangedEvent(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.dwCookie);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_IDXGIFactory7_UnregisterAdaptersChangedEvent_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_IDXGIFACTORY_H */

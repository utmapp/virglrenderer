/*
 * Copyright 2026 Turing Software LLC
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by npt_protocol.py -- do not edit.
 *
 * Command structs and encode/decode for id3d12object.
 */

#ifndef NPT_PROTOCOL_HOST_ID3D12OBJECT_H
#define NPT_PROTOCOL_HOST_ID3D12OBJECT_H

#include "npt_protocol_host_structs.h"
#include "npt_protocol_host_dispatch_types.h"

#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#pragma GCC diagnostic ignored "-Wpointer-arith"

/*
 * ID3D12Object::GetPrivateData
 */

struct npt_command_ID3D12Object_GetPrivateData {
    void *_self;
    const GUID * guid;
    UINT * pDataSize;
    void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Object_GetPrivateData_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D12Object_GetPrivateData *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    if (npt_decode_simple_pointer(dec)) {
        args->pDataSize = npt_cs_decoder_alloc_temp(dec, sizeof(UINT));
        if (!args->pDataSize) return;
        npt_decode_UINT(dec, (UINT *)args->pDataSize);
    } else {
        args->pDataSize = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }

    /* Allocate temp storage for output-only parameters */


    if (*args->pDataSize) {
        args->pData = npt_cs_decoder_alloc_temp(dec, *args->pDataSize);
        if (!args->pData) return;
        memset(args->pData, 0, *args->pDataSize);
    } else {
        args->pData = NULL;
    }
}

static inline void
npt_replace_ID3D12Object_GetPrivateData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Object_GetPrivateData *args)
{
}

static inline void
npt_encode_ID3D12Object_GetPrivateData_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D12Object_GetPrivateData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Object,
                                 NPT_METHOD_ID3D12Object_GetPrivateData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
    if (npt_encode_simple_pointer(enc, args->pDataSize))
        npt_encode_UINT(enc, args->pDataSize);
    if (args->pData) {
        npt_encode_array_count(enc, *args->pDataSize);
        npt_encode_blob_array(enc, args->pData, *args->pDataSize);
    } else {
        npt_encode_array_count(enc, 0);
    }
}

static inline void
npt_dispatch_ID3D12Object_GetPrivateData(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D12Object_GetPrivateData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Object_GetPrivateData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12OBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_GetPrivateData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Object_GetPrivateData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_GetPrivateData: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Object_GetPrivateData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Object_GetPrivateData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Object_GetPrivateData);

    if (ctx->id3d12object_dispatch_overrides && ctx->id3d12object_dispatch_overrides->GetPrivateData) {
        args.ret = ctx->id3d12object_dispatch_overrides->GetPrivateData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.pDataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Object_GetPrivateData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Object::SetPrivateData
 */

struct npt_command_ID3D12Object_SetPrivateData {
    void *_self;
    const GUID * guid;
    UINT DataSize;
    const void * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Object_SetPrivateData_args_temp(struct npt_cs_decoder *dec,
                                                 struct npt_command_ID3D12Object_SetPrivateData *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    npt_decode_UINT(dec, &args->DataSize);
    {
        const uint64_t _blob_size = npt_decode_array_count_unchecked(dec);
        if (_blob_size) {
            args->pData = npt_cs_decoder_alloc_temp(dec, _blob_size);
            if (!args->pData) return;
            npt_decode_blob_array(dec, (void *)args->pData, _blob_size);
        } else {
            args->pData = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */



}

static inline void
npt_replace_ID3D12Object_SetPrivateData_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Object_SetPrivateData *args)
{
}

static inline void
npt_encode_ID3D12Object_SetPrivateData_reply(struct npt_cs_encoder *enc,
                                             const struct npt_command_ID3D12Object_SetPrivateData *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Object,
                                 NPT_METHOD_ID3D12Object_SetPrivateData),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Object_SetPrivateData(struct npt_dispatch_context *ctx,
                                         uint32_t cmd_flags,
                                         uint64_t object_id)
{
    struct npt_command_ID3D12Object_SetPrivateData args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Object_SetPrivateData_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12OBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_SetPrivateData on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Object_SetPrivateData_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_SetPrivateData: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Object_SetPrivateData _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Object_SetPrivateData, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Object_SetPrivateData);

    if (ctx->id3d12object_dispatch_overrides && ctx->id3d12object_dispatch_overrides->SetPrivateData) {
        args.ret = ctx->id3d12object_dispatch_overrides->SetPrivateData(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.DataSize, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Object_SetPrivateData_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Object::SetPrivateDataInterface
 */

struct npt_command_ID3D12Object_SetPrivateDataInterface {
    void *_self;
    const GUID * guid;
    const IUnknown * pData;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Object_SetPrivateDataInterface_args_temp(struct npt_cs_decoder *dec,
                                                          struct npt_command_ID3D12Object_SetPrivateDataInterface *args)
{
    /* Decode input parameters from the wire */
    if (npt_decode_simple_pointer(dec)) {
        args->guid = npt_cs_decoder_alloc_temp(dec, sizeof(GUID));
        if (!args->guid) return;
        npt_decode_GUID(dec, (GUID *)args->guid);
    } else {
        args->guid = NULL;
        npt_cs_decoder_set_fatal(dec); /* non-optional pointer is NULL */
        return;
    }
    {
        npt_object_id _id;
        npt_decode_uint64_t(dec, &_id);
        args->pData = (IUnknown *)npt_object_from_id(_id);
    }
    /* Allocate temp storage for output-only parameters */


}

static inline void
npt_replace_ID3D12Object_SetPrivateDataInterface_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Object_SetPrivateDataInterface *args)
{
    args->pData = npt_cs_handle_lookup(ctx, (npt_object_id)(uintptr_t)args->pData, NPT_OBJECT_TYPE_IUNKNOWN);
}

static inline void
npt_encode_ID3D12Object_SetPrivateDataInterface_reply(struct npt_cs_encoder *enc,
                                                      const struct npt_command_ID3D12Object_SetPrivateDataInterface *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Object,
                                 NPT_METHOD_ID3D12Object_SetPrivateDataInterface),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Object_SetPrivateDataInterface(struct npt_dispatch_context *ctx,
                                                  uint32_t cmd_flags,
                                                  uint64_t object_id)
{
    struct npt_command_ID3D12Object_SetPrivateDataInterface args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Object_SetPrivateDataInterface_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12OBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_SetPrivateDataInterface on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Object_SetPrivateDataInterface_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_SetPrivateDataInterface: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Object_SetPrivateDataInterface _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Object_SetPrivateDataInterface, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Object_SetPrivateDataInterface);

    if (ctx->id3d12object_dispatch_overrides && ctx->id3d12object_dispatch_overrides->SetPrivateDataInterface) {
        args.ret = ctx->id3d12object_dispatch_overrides->SetPrivateDataInterface(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.guid, args.pData);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Object_SetPrivateDataInterface_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}

/*
 * ID3D12Object::SetName
 */

struct npt_command_ID3D12Object_SetName {
    void *_self;
    const WCHAR * Name;
    HRESULT ret;
};

static inline void
npt_decode_ID3D12Object_SetName_args_temp(struct npt_cs_decoder *dec,
                                          struct npt_command_ID3D12Object_SetName *args)
{
    /* Decode input parameters from the wire */
    {
        const uint64_t _wstr_size = npt_decode_array_count_unchecked(dec);
        if (_wstr_size) {
            args->Name = npt_cs_decoder_alloc_temp(dec, _wstr_size);
            if (!args->Name) return;
            npt_decode_wchar_array(dec, (WCHAR *)args->Name, _wstr_size);
        } else {
            args->Name = NULL;
        }
    }
    /* Allocate temp storage for output-only parameters */

}

static inline void
npt_replace_ID3D12Object_SetName_args_handle(struct npt_dispatch_context *ctx, struct npt_command_ID3D12Object_SetName *args)
{
}

static inline void
npt_encode_ID3D12Object_SetName_reply(struct npt_cs_encoder *enc,
                                      const struct npt_command_ID3D12Object_SetName *args)
{
    struct npt_reply_header _reply = {
        .cmd_type = NPT_CMD_TYPE(255, NPT_IFACE_ID_ID3D12Object,
                                 NPT_METHOD_ID3D12Object_SetName),
        .cmd_return = (uint32_t)args->ret,
    };
    npt_cs_encoder_write(enc, sizeof(_reply), &_reply, sizeof(_reply));
}

static inline void
npt_dispatch_ID3D12Object_SetName(struct npt_dispatch_context *ctx,
                                  uint32_t cmd_flags,
                                  uint64_t object_id)
{
    struct npt_command_ID3D12Object_SetName args;
    memset(&args, 0, sizeof(args));

    npt_decode_ID3D12Object_SetName_args_temp(ctx->decoder, &args);

    if (npt_cs_decoder_get_fatal(ctx->decoder))
        return;

    args._self = npt_cs_handle_lookup(ctx, object_id,
                                       NPT_OBJECT_TYPE_ID3D12OBJECT);
    if (!args._self) {
        /* The host no longer knows this object -- a guest-side lifetime
         * bug, or a teardown race between a release and a call already
         * on the wire.  A reply-less call is dropped: losing one
         * command on an object that is already gone is a smaller harm
         * than the context teardown a fatal costs.  A call that owes a
         * reply still goes fatal; there is no honest answer to encode,
         * and a silent drop would hang the caller.  Consume the
         * recorded miss either way so it cannot leak into the next
         * command. */
        (void)npt_cs_decoder_take_handle_miss(ctx->decoder);
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_SetName on unregistered object "
                    "0x%016" PRIx64, (uint64_t)object_id);

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    npt_replace_ID3D12Object_SetName_args_handle(ctx, &args);

    if (npt_cs_decoder_take_handle_miss(ctx->decoder)) {
        /* An argument handle failed to translate; the backend would
         * dereference the NULL left in its place.  Same policy as a
         * missing self: drop when nothing awaits a reply, go fatal
         * when something does. */
        if (!(cmd_flags & NPT_CMD_FLAG_REPLY)) {
            npt_log("dropping ID3D12Object_SetName: an argument handle is unknown "
                    "to this context");

            npt_cs_decoder_reset_temp_pool(ctx->decoder);
            return;
        }
        npt_cs_decoder_set_fatal(ctx->decoder);
        return;
    }

    PFN_ID3D12Object_SetName _original = NPT_COM_VTBL_FUNC(
        PFN_ID3D12Object_SetName, npt_com_vtable(args._self),
        NPT_VTBL_ID3D12Object_SetName);

    if (ctx->id3d12object_dispatch_overrides && ctx->id3d12object_dispatch_overrides->SetName) {
        args.ret = ctx->id3d12object_dispatch_overrides->SetName(ctx, &args, _original);
    } else {
        args.ret = _original(args._self, args.Name);
    }

    /* Register any output COM handles in the context object table so
     * later commands that reference them can be validated.  The riid
     * expression (if any) lets the generator resolve void** outputs
     * whose type is determined by the caller's riid. */

    if (cmd_flags & NPT_CMD_FLAG_REPLY) {
        if (!npt_cs_decoder_get_fatal(ctx->decoder)) {
            if (npt_cs_encoder_acquire(ctx->encoder)) {
                npt_encode_ID3D12Object_SetName_reply(ctx->encoder, &args);
                npt_cs_encoder_release(ctx->encoder);
            }
        }
    }

    npt_cs_decoder_reset_temp_pool(ctx->decoder);
}


#pragma GCC diagnostic pop

#endif /* NPT_PROTOCOL_HOST_ID3D12OBJECT_H */
